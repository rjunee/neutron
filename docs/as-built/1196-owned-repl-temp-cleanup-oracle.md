## 2026-09-26 — REPL cleanup tests inspect their own config paths

The two owner MCP cleanup tests previously counted every newly appearing `neutron-repl-*` directory in the shared temporary directory. A parallel REPL could therefore produce a false leak report. Each test now inspects the config path captured from its own spawn argv. The failed-spawn host records config existence and parent mode before throwing a distinct error; the test verifies that intended error before asserting those observations and removal of both paths. The concurrent-dispatch test verifies one child, config existence and a 0700 parent before shutdown, removal afterward, and exactly one kill. This changes only the test oracles, not production cleanup.

Recovery passed the consuming owner MCP file: 33 tests, zero failures. A physical frozen copyfile dependency install passed workspace dependency verification; root and Trident TypeScript checks and the complete local lint script passed. Earlier preparatory checks using borrowed workspace dependencies failed with unrelated type errors; their failed outputs were retained and are not valid receipts for the isolated candidate. All reported passes use the isolated install. Temporary semantic controls recreated the owned config and substituted an unintended host error, each failing its intended assertion. A separate temporary REPL directory survived shutdown while the scoped concurrent-dispatch test passed. The controls were reverted.

The canonical host command `bash scripts/check-shared-host.sh` exited zero on source candidate `6995f6803e6d633fc7490361d638d7b983b8b9d1`, unchanged at exit. All 51 TypeScript configs passed. The runner audited 1,699 declared, discovered, assigned and executed files: 1,465 general, 22 PGLite, 43 device and 169 real-HTTP files across 18 bounded-memory lanes, with zero failed lanes. The shared admission lock and defaults of four jobs and chunks of 100 remained in effect. The complete log has SHA-256 `ac73f6f9fb05725099838c99cff89124fe15a67f1135970ce27a165e0840bbce`. The private unassigned command receipt identifies explicit phase `test`, event `operator-command:7540a4e0-09af-4a86-ba14-57df25199a01`, start epoch milliseconds `1790413287591`, end `1790414446269` and exit code zero; model and usage remain unknown. Its receipt digest is `6f87fba8194a7538fd0308ab84bdd1efb539f92e1726931d80d427c5d64b7004`. This record does not establish completion of the broader issue #1196.
