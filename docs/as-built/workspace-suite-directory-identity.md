## 2026-09-24 — Preserve host suite proof across workspace scratch churn

Issue #1271 records two live runs whose host suites passed before their measured
input identities disagreed. The installed-tree observer follows workspace links
and formerly hashed directory size, modification time and change time verbatim.
Creating and removing a test scratch directory therefore invalidated otherwise
unchanged proof.

`open/wiring/project-build-dependencies.ts:129` canonicalizes only those three
fields for directories outside every `node_modules` path segment. The distinctive
`canonicalizeWorkspaceDirectory` binding supports a served-code positive control;
the former `hash.update(measured.stdout)` is the old-code negative control.
Directory path, kind, device, inode and permissions remain measured. Every child
is still visited, including ignored/generated files, local link targets and
dependencies in `pkg/tools/node_modules`. Regular-file and link records retain
their metadata. External link targets still refuse identity acquisition.

This preserves the measured-input and bidirectional guard requirements in
`docs/spec-items/trident-build-efficiency.md:180` and `:190`, and the host receipt
contract in G063–G065 of `docs/trident-gates-inventory.md:148`. It does not bypass
the retained gates in the locked pivot plan at
`docs/plans/harness-orchestrator-pivot-2026-09-11.md:271`.

The focused suite in `open/__tests__/project-suite-identity.test.ts:172` passes
14 tests. Controls cover nested scratch directory size/timestamp changes,
directory permission changes, ignored generated bytes, a local link target,
deep installed dependencies, same-size rewrites with restored modification times,
a clean tracked external symlink and retained nested dependency directory times.

The consuming case in `open/__tests__/project-build-e2e.test.ts:1790` installs a
real root workspace dependency, creates/removes scratch files during `runSuite`,
and reconstructs the host. Its 18 assertions require receipt reuse with one suite,
fresh proof after ignored content changes, refusal of a content change during
observation, and successful subsequent acquisition/reuse. Restoring raw directory
hashing fails two legitimate-case assertions. Skipping first-party link traversal
fails five assertions, including forbidden stale receipt reuse and external-link
refusal. Both mutants were restored before verification.

The full named Open E2E file plus the focused identity file pass 309 tests with
zero failures. All 51 TypeScript projects pass, including the root and Trident
configs; lint, dependency layering and the as-built guard pass. The local
whole-tree leak scan has 455 inherited denylist findings reproduced on the
fetched base (plus the linked worktree's untracked git pointer). Changed files
and the commit message scan have zero findings; the guard was not relaxed.

This repair is independent of the unsafe proposal in PR #1270. Deployment and a
fresh adopted-chat Work Board card reaching unattended merge remain required by
`docs/spec-items/trident-build-efficiency.md:203`; local regression evidence does
not establish that live outcome, and issue #1271 remains open for it.
