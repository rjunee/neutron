# Trident gates inventory — #545, enumeration slice

**18 NO TEST entries out of 165 preservation entries.** 156 entries are classified **Silent** if lost. The count is conservative: `NO TEST` means this audit did not locate a sufficiently specific pin, including cases where only a related happy-path or argument-threading test was found. It is not a claim that the repository contains no relevant test. The rows are the result of static inspection, not a fresh mutation certification of existing tests.

Baseline: `f7320fc76eb3e1d0775dabd56cf957da632904e5`. The three subject files were measured at 9,315, 6,316, and 1,167 lines respectively (16,798 total), matching the task brief. The staged issue contains no numerical source anchors to correct; the preservation requirement is `docs/spec-items/the-orchestrator-owns-the-build-loop.md:56`. This slice inventories the existing loop; it does not satisfy the replacement or end-to-end build criteria.

## Derivation and audit boundary

The starting point was **grep indexes, not a sequential read**. P1 enumerated refusal/outcome vocabulary across all three subjects. P2 independently indexed control flow, early returns, compound predicates, and conditional expressions, including guards without refusal words. P3 caught embedded shell and prompt requirements. Matched function neighborhoods were then read with line numbers. T independently enumerated test declarations under `trident/`, including `__tests__/`; V independently indexed the emitted verdict, terminal-cause, and error vocabularies. D followed the merge, proof, cleanup, and board boundaries reached from the subjects. Repeated matches in one predicate were folded into one row; a separate outcome or independently losable precondition earned another row. Delegated subgates supplement their call-site rows rather than counting as additional call sites.

Reproduce the searches with `rg -n '<pattern>' <scope>`. The following are actual match counts, including comments and repeated mentions, **not gate counts**. Each pattern has a positive control found by that same invocation:

| Search | Scope | Matching lines | Positive control |
| --- | --- | ---: | --- |
| P1 | three subjects | 1153 | `trident/inner-workflow.mjs:8494` |
| P2 | three subjects | 3076 | `trident/inner-loop.ts:585` |
| P3 | three subjects | 420 | `trident/inner-workflow.mjs:1351` |
| T | 128 files enumerated as below | 4868 | `trident/escalation-block.test.ts:537` |
| V | three subjects + terminal-cause.ts, merge.ts, escalation-block.ts | 91 | `trident/inner-workflow.mjs:4712` |
| D | merge.ts, mutation-prover.ts, worktree-cleanup.sh, board-reconcile.ts, escalation-block.ts | 1004 | `trident/board-reconcile.ts:122` |

Exact patterns (P1–P3 use the three subject paths; T uses the enumerated test files; V and D use the table scopes):

```text
P1  throw |refus|blocked|budget|cap\b|terminal|verdict|precondition|purity|leak|escalat
P2  \b(if|while|for|switch|catch|finally|return|break|continue)\b|\?|&&|\|\|
P3  exit [1-9]|MUST|NEVER|[Bb]udget|allowedTools|disallowedTools|[Tt]imeout|[Mm]ax[A-Z]|[Ll]imit
T  ^\s*(test|it|describe)(\.|\()
V  TERMINAL_CAUSES|TERMINAL_CAUSE_KINDS|block_kind:|status:|class .*Error|class Trident|return.*(genuine|infrastructure|REVIEW_NOT_RUN)
D  throw |refus|return |exit [1-9]|MAX_|if |detachRun
```

All abbreviated scope names above are under `trident/`. T's file set is the sorted output of `rg --files trident -g '*test.ts'`; declarations, not filenames alone, supplied the test index. The exploratory `test(` searches also match regular-expression `.test()` calls, so those matches were not accepted as tests. Tests cited below point to a declaration or its assertion, not merely the top of a file. Source-string checks count as structural pins only; a rewrite will need to replace those pins with behavioral checks against its new boundary.

`NO TEST` adjudication used both the T declaration index and searches for the matched enforcement symbol/refusal text in test bodies. The positive control for the latter is `enforceCrossModelGate` in `trident/inner-workflow.test.ts:979`. A related fixture is not proof of the refusal: for example, `trident/inner-workflow.test.ts:254` supplies a branch and tests argument threading, so it does not pin the missing-branch throw in G001. Likewise `trident/inner-workflow.test.ts:297` tests successful pinned-task selection, not G024's missing-plan/missing-task exits.

**Loss classification** is a conservative engineering inference, separate from test coverage. **Silent** means removing the boundary can admit wrong work, lose data, waste work, or leave a run stuck without a reliable downstream refusal of the same condition. An eventual timeout does not make wrong work loud. **Loud** means an identified downstream operation would ordinarily fail; it does not promise the same diagnosis, timing, or preservation. No row's “Silent” classification asserts that its current implementation emits no diagnostic.

## Dangerous set and missing pins

The **NO TEST** set, enumerated from the rows below: G001, G018, G021, G022, G023, G024, G031, G034, G075, G083, G085, G089, G094, G097, G111, G132, G133, G140.

The complete **Silent** subset of this inventory is grouped here for rewrite review (IDs refer to the evidence-bearing rows below):

- Launch and result boundary: G001, G003, G004, G005, G006, G008, G009, G010, G011, G012, G013, G014, G015, G016, G017, G018, G019.
- Planner, builder, and resume: G020, G021, G022, G023, G024, G025, G026, G027, G028, G029, G030, G032, G033, G034, G035, G036, G037, G038, G039, G040, G041, G042, G043.
- Review readiness and verdict gates: G044, G045, G046, G047, G048, G049, G050, G051, G052, G053, G054, G055, G056, G057, G058, G059, G060, G061, G062, G063, G064, G065, G066, G067.
- Escalation and budgets: G068, G069, G070, G071, G072, G073, G074, G075, G076, G077, G078, G079, G080, G081, G082.
- Publication and replay: G084, G085, G086, G087, G088, G090, G092, G093, G094, G095, G097, G098, G099, G100, G102, G103.
- Harvest, merge, recovery, and preservation: G104, G105, G106, G107, G108, G109, G110, G111, G112, G113, G114, G115, G116, G117, G118, G119, G120, G121, G122, G123, G124, G125, G126, G127, G128, G129, G130, G131.
- Prompt requirements and advisory boundaries retained for review: G132, G133, G134, G135, G136, G137, G138, G139, G140.
- Delegated proof and cleanup subgates: G141, G142, G143, G144, G145, G146, G147, G148, G149, G150, G151, G152, G154, G155, G156, G157, G158, G159, G160.
- Additional boundary checks from the cross-check: G161, G162, G163, G164, G165.

## Inventory

### Launch and result boundary

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G001 | A wave child must carry its assigned branch before workflow arguments can be built. | `trident/inner-loop.ts:585` | **NO TEST** | Silent: child could build on the wrong branch. |
| G002 | A fire requires a substrate or substrate factory; the actual condition rejects neither supplied, not both supplied. | `trident/inner-loop.ts:1030` | `trident/inner-loop.test.ts:1082` | Loud: starting without either would throw later. |
| G003 | A launcher stream must complete; an error, thrown start, or stream ending without completion is a failed fire. | `trident/inner-loop.ts:1061`; `trident/inner-loop.ts:1101`; `trident/inner-loop.ts:1118` | `trident/inner-loop.test.ts:839`; `trident/inner-loop.test.ts:849`; `trident/inner-loop.test.ts:1057` | Silent: launch failure could be reported as success. |
| G004 | The settle timer returns unconfirmed and leaves the launcher running for later confirmation rather than cancelling at the first deadline. The timer is bypassed for a nonpositive settle timeout. | `trident/inner-loop.ts:1139`; `trident/inner-loop.ts:1121` | `trident/inner-loop.test.ts:862`; `trident/inner-loop.test.ts:916` | Silent: premature cancellation can destroy live work. |
| G005 | A terminal result must parse as a non-null object before harvest; malformed JSON remains unharvestable (arrays are not explicitly excluded here). | `trident/inner-loop.ts:834` | `trident/inner-loop.test.ts:369` | Silent: invalid results could advance a run. |
| G006 | Only exact APPROVE and REQUEST_CHANGES tokens decode as verdicts; anything else becomes null. | `trident/inner-loop.ts:952` | `trident/inner-loop.test.ts:377` | Silent: malformed output could acquire authority. |
| G007 | A PR number must be a positive integer; malformed numbers become null rather than replacing the known PR. | `trident/inner-loop.ts:856` | `trident/inner-loop.test.ts:241` | Loud: downstream PR operations target an invalid number. |
| G008 | Reported rounds must be safe integers and cannot lower the outer row's recorded round. | `trident/inner-loop.ts:866`; `trident/orchestrator.ts:4977` | `trident/orchestrator.test.ts:4022`; `trident/orchestrator.test.ts:4033` | Silent: a resumed run could regain spent rounds. |
| G009 | Merge, publish, and child-built signals require exact booleans; a built child also needs a decoded full commit SHA. | `trident/inner-loop.ts:872`; `trident/inner-loop.ts:877`; `trident/inner-loop.ts:878`; `trident/orchestrator.ts:4987` | `trident/inner-loop.test.ts:220`; `trident/orchestrator.test.ts:330` | Silent: truthy junk could skip work or mark unbuilt work done. |
| G010 | Unknown block kinds and malformed escalation payloads cannot acquire a recognized routing kind. | `trident/inner-loop.ts:899`; `trident/inner-loop.ts:913` | `trident/inner-loop.test.ts:301`; `trident/escalation-block.test.ts:79`; `trident/escalation-block.test.ts:92` | Silent: an invented outcome could change retry or board routing. |
| G011 | A rejection is recorded as reviewed only with Argus provenance and findings, or a coherent escalation; otherwise it is REVIEW_NOT_RUN. | `trident/orchestrator.ts:1069`; `trident/orchestrator.ts:1101`; `trident/orchestrator.ts:1111` | `trident/escalation-block.test.ts:262`; `trident/escalation-block.test.ts:286`; `trident/escalation-block.test.ts:313` | Silent: an infrastructure failure could impersonate a review. |
| G012 | Terminal causes are trimmed and capped before persistence, and unknown cause kinds decode through the closed vocabulary. | `trident/inner-loop.ts:916`; `trident/inner-loop.ts:925` | `trident/inner-loop.test.ts:301`; `trident/inner-loop.test.ts:331` | Silent: unbounded or unclassified output reaches the run record. |
| G013 | A fresh PR build must fetch and resolve the remote base before firing; two failed fetches stop it. | `trident/orchestrator.ts:4142`; `trident/orchestrator.ts:4146`; `trident/orchestrator.ts:4161` | `trident/orchestrator.test.ts:9025` | Silent: the build can start on stale code. |
| G014 | A dispatch seed must still match the live head or it is discarded before a fresh launch. | `trident/orchestrator.ts:3989` | `trident/orchestrator.test.ts:8038` | Silent: stale work can masquerade as the requested build. |
| G015 | A resume whose checkpoint depends on head identity stops when the live head cannot be read, after bounded retries. | `trident/orchestrator.ts:2066`; `trident/orchestrator.ts:4044` | `trident/orchestrator.test.ts:7240`; `trident/orchestrator.test.ts:7263` | Silent: unreadability can be mistaken for absence or unchanged code. |
| G016 | A fresh launch refuses an existing local branch not contained in the base unless the prior pin and recorded work establish this run's own leftover. | `trident/orchestrator.ts:4418`; `trident/orchestrator.ts:4483`; `trident/orchestrator.ts:4514` | `trident/orchestrator.test.ts:7536`; `trident/orchestrator.test.ts:8335`; `trident/orchestrator.test.ts:8385` | Silent: one run can adopt another run's work. |
| G017 | Negative ancestry requires repeated exit-1 answers with complete history on both sides; unknown or shallow evidence refuses without alleging divergence. | `trident/orchestrator.ts:4312`; `trident/orchestrator.ts:4419`; `trident/orchestrator.ts:4446` | `trident/orchestrator.test.ts:7587`; `trident/orchestrator.test.ts:7742`; `trident/orchestrator.test.ts:7854` | Silent: a healthy branch can be falsely rejected with destructive advice. |
| G018 | The dispatch-ID minter must return a nonempty string before launch state advances. | `trident/orchestrator.ts:4552` | **NO TEST** | Silent: a run can lose its durable dispatch identity. |
| G019 | A bound-PR review failure terminates the review run instead of entering the build flow. | `trident/orchestrator.ts:3772`; `trident/orchestrator.ts:3812` | `trident/review-run.test.ts:490` | Silent: review failure could trigger an unintended build. |

### Planner, builder, and resume

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G020 | An unpinned diff base must be nonempty, unpadded, and not option-shaped before it becomes shell text. | `trident/inner-workflow.mjs:1555`; `trident/inner-workflow.mjs:1580`; `trident/inner-workflow.mjs:1585` | `trident/diff-base-option-shaped.test.ts:288`; `trident/diff-base-option-shaped.test.ts:799` | Silent: malformed operands can yield a plausible wrong diff. |
| G021 | A CLI build requires the launcher-supplied harness wrapper path rather than resolving a wrapper from the target repository. | `trident/inner-workflow.mjs:2333` | **NO TEST** | Silent: a different wrapper can change the build contract (argument-threading pin only). |
| G022 | A CLI build cannot proceed without a complete trailer or a connected result; bounded probes distinguish a recorded exit from an unknown wait. | `trident/inner-workflow.mjs:2366`; `trident/inner-workflow.mjs:2395`; `trident/inner-workflow.mjs:2397` | **NO TEST** | Silent: an unfinished build can be treated as complete (bridge contract pins). |
| G023 | A reported builder branch must agree with the deterministic assigned branch. | `trident/inner-workflow.mjs:2413` | **NO TEST** | Silent: subsequent review and cleanup could target different work. |
| G024 | A wave member must receive a plan and its pinned task must still be unchecked; it cannot select another task. | `trident/inner-workflow.mjs:7944`; `trident/inner-workflow.mjs:7950` | **NO TEST** | Silent: a child could silently execute the wrong task. |
| G025 | Ralph builds require a non-null planner result before Forge runs. | `trident/inner-workflow.mjs:8122` | `trident/inner-workflow-plan-next.test.ts:1278` | Silent: an agent could improvise work with no execution plan. |
| G026 | The cheap continuation planner requires the clean handoff and a valid positive round outside the periodic full-refresh interval. | `trident/inner-workflow.mjs:8012` | `trident/inner-workflow-plan-next.test.ts:508`; `trident/inner-workflow-plan-next.test.ts:1333` | Silent: an exhausted or deviated plan can be reused indefinitely. |
| G027 | The cheap planner requires a found nonempty committed plan with a positive integer unchecked count. | `trident/inner-workflow.mjs:8064` | `trident/inner-workflow-plan-next.test.ts:868`; `trident/inner-workflow-plan-next.test.ts:881` | Silent: a missing or exhausted plan can select no meaningful work. |
| G028 | The cheap planner requires the plan body's checksum and measured unchecked count to agree with the probe. | `trident/inner-workflow.mjs:1219`; `trident/inner-workflow.mjs:8072` | `trident/inner-workflow-plan-next.test.ts:924`; `trident/inner-workflow-plan-next.test.ts:1058` | Silent: a truncated or changed relay can become the execution plan. |
| G029 | A cheap planner's changed body, task, and remaining count are replaced by the committed measurements before execution. | `trident/inner-workflow.mjs:8145`; `trident/inner-workflow.mjs:8163`; `trident/inner-workflow.mjs:8192` | `trident/inner-workflow-plan-next.test.ts:1112`; `trident/inner-workflow-plan-next.test.ts:1127`; `trident/inner-workflow-plan-next.test.ts:1169` | Silent: model paraphrases could change task identity or end the run early. |
| G030 | Branch-log and branch-brief data are delimiter-neutralized and byte-capped before being used as untrusted prompt material. | `trident/inner-workflow.mjs:2685`; `trident/inner-workflow.mjs:2707`; `trident/inner-workflow.mjs:2721` | `trident/inner-workflow-plan-next.test.ts:543`; `trident/inner-workflow-plan-next.test.ts:588`; `trident/inner-workflow-plan-next.test.ts:609` | Silent: data could escape its prompt boundary or consume the context. |
| G031 | A null initial Forge result terminates instead of opening review. | `trident/inner-workflow.mjs:8249` | **NO TEST** | Loud: downstream artifact/head requirements would also reject an empty build. |
| G032 | Local builds and fixes require a readable full built head, with bounded read attempts, before trusting a reported diff. | `trident/inner-workflow.mjs:5973`; `trident/inner-workflow.mjs:8309`; `trident/inner-workflow.mjs:8871` | `trident/inner-workflow-built-head.test.ts:169`; `trident/inner-workflow-built-head.test.ts:178` | Silent: an unverified artifact can be reviewed as current work. |
| G033 | Local builder commit claims, when parseable, must prefix-match the measured head; PR claims are deferred to the publisher’s independent object check. | `trident/inner-workflow.mjs:8287`; `trident/inner-workflow.mjs:8854` | `trident/inner-workflow-built-head.test.ts:98`; `trident/inner-workflow-built-head.test.ts:147` | Silent: review can approve a different commit from the one built. |
| G034 | A wave member must produce a full commit before returning its terminal built result without publication or review. | `trident/inner-workflow.mjs:8371` | **NO TEST** | Silent: a join can consume an unbuilt member (outer boundary pin only). |
| G035 | A fresh review requires both a branch head and a diff artifact. | `trident/inner-workflow.mjs:8480` | `trident/inner-workflow-built-head.test.ts:229` | Silent: a panel can approve no change. |
| G036 | A confirmed merged PR terminates before re-fire or lost-round handling. | `trident/inner-workflow.mjs:8420`; `trident/inner-workflow.mjs:8941`; `trident/orchestrator.ts:5016` | `trident/orchestrator.test.ts:3610`; `trident/orchestrator.test.ts:3648` | Silent: already-landed work can be rebuilt or reported failed. |
| G037 | Intermediate Ralph work defers review and publication until the terminal task; the outer handoff clears the old result atomically. | `trident/inner-workflow.mjs:8446`; `trident/orchestrator.ts:5033` | `trident/inner-workflow-ralph-refire.test.ts:109`; `trident/orchestrator.test.ts:481`; `trident/orchestrator.test.ts:4551` | Silent: partial work can merge or an old result can be harvested repeatedly. |
| G038 | Resume fast paths require the same full recorded and live head; a missing or moved head rebuilds and an unreadable required head stops. | `trident/inner-workflow.mjs:4193`; `trident/inner-workflow.mjs:4208`; `trident/inner-workflow.mjs:4219` | `trident/inner-workflow-resume.test.ts:625`; `trident/inner-workflow-resume.test.ts:662`; `trident/inner-workflow-resume.test.ts:671` | Silent: stale approval can authorize changed code. |
| G039 | A resumed rejection may skip review only when recorded actionable code findings exist; advisory or lane findings require re-review. | `trident/inner-workflow.mjs:4242`; `trident/inner-workflow.mjs:8543` | `trident/inner-workflow-resume.test.ts:413`; `trident/inner-workflow-resume.test.ts:461`; `trident/inner-workflow-resume.test.ts:687` | Silent: Forge can repeat work against stale infrastructure findings. |
| G040 | Resumed review requires a regenerated diff from the pinned OID; an empty result falls back to rebuilding. | `trident/inner-workflow.mjs:6018`; `trident/inner-workflow.mjs:7776` | `trident/inner-workflow-resume.test.ts:317`; `trident/inner-workflow-resume.test.ts:681` | Silent: review can use absent or unrelated content. |
| G041 | A resumed fix inherits its spent round count instead of restarting the budget. | `trident/inner-workflow.mjs:4268`; `trident/inner-workflow.mjs:7927` | `trident/inner-workflow-resume.test.ts:580`; `trident/inner-workflow-resume.test.ts:597` | Silent: crash recovery can make the loop unbounded. |
| G042 | An unchanged or unreadable post-fix head stops the loop as round-lost after checking for an actual merge. | `trident/inner-workflow.mjs:4075`; `trident/inner-workflow.mjs:8948` | `trident/round-landed.test.ts:76`; `trident/round-landed.test.ts:87`; `trident/round-landed.test.ts:168` | Silent: review repeats while no fix lands. |
| G043 | A fix leaving no diff stops before paying for another review. | `trident/inner-workflow.mjs:8971` | `trident/inner-workflow-built-head.test.ts:314` | Silent: a panel can approve a fix that erased the change. |

### Review readiness and verdict gates

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G044 | Review readiness must know the required-check configuration; unknown configuration defers rather than declaring an unprotected base. | `trident/inner-workflow.mjs:5020`; `trident/inner-workflow.mjs:5394` | `trident/__tests__/ci-gate.test.ts:926`; `trident/__tests__/ci-gate.test.ts:1166` | Silent: unreadable protection can become permission to proceed. |
| G045 | Protection contexts and ruleset checks are combined, and an ambiguous protection 404 requires independent branch evidence. | `trident/inner-workflow.mjs:5192`; `trident/inner-workflow.mjs:5215` | `trident/__tests__/ci-gate.test.ts:1068`; `trident/__tests__/ci-gate.test.ts:1276` | Silent: one protection source can vanish from the required set. |
| G046 | Produced-check evidence must include complete check-run and status lists with valid counts; unreadability disables the fast configuration-fault inference. | `trident/inner-workflow.mjs:5262`; `trident/inner-workflow.mjs:5290` | `trident/__tests__/ci-gate.test.ts:1305`; `trident/__tests__/ci-gate.test.ts:1324` | Silent: a truncated producer list can falsely end the run. |
| G047 | Readiness requires parseable PR data and MERGEABLE status; conflicts stop immediately and unknown mergeability waits. | `trident/inner-workflow.mjs:5418`; `trident/inner-workflow.mjs:5432`; `trident/inner-workflow.mjs:5435` | `trident/__tests__/ci-gate.test.ts:490`; `trident/__tests__/ci-gate.test.ts:516` | Silent: paid review can run against an unreviewable PR. |
| G048 | An app-bound required name cannot be satisfied by a classic commit-status row; this checks row shape, not actual app identity. | `trident/inner-workflow.mjs:5472`; `trident/inner-workflow.mjs:5505` | `trident/__tests__/ci-gate.test.ts:2229` | Silent: the wrong producer can satisfy the requirement. |
| G049 | Every required check must have run and settled; missing, skipped-only, or running checks defer. | `trident/inner-workflow.mjs:5564`; `trident/inner-workflow.mjs:5568`; `trident/inner-workflow.mjs:5571` | `trident/__tests__/ci-gate.test.ts:503`; `trident/__tests__/ci-gate.test.ts:516` | Silent: pending or skipped checks can appear complete. |
| G050 | A missing required name becomes a configuration stop only after grace, nonempty base evidence, and a nonempty settled PR rollup. | `trident/inner-workflow.mjs:5493`; `trident/inner-workflow.mjs:5549` | `trident/__tests__/ci-gate.test.ts:814`; `trident/__tests__/ci-gate.test.ts:1669`; `trident/__tests__/ci-gate.test.ts:1697` | Silent: a queued or conditional job can be falsely declared impossible. |
| G051 | A fresh resolved configuration reclassifies a tentative configuration error before it is accepted. | `trident/inner-workflow.mjs:5380` | `trident/__tests__/ci-gate.test.ts:2486`; `trident/__tests__/ci-gate.test.ts:2493` | Silent: stale configuration can terminate a viable run. |
| G052 | Without named requirements, at least one non-skipped check must run and all participating rows must settle. | `trident/inner-workflow.mjs:5586`; `trident/inner-workflow.mjs:5588` | `trident/__tests__/ci-gate.test.ts:864`; `trident/__tests__/ci-gate.test.ts:883` | Silent: zero checks can look green. |
| G053 | Readiness waits have a 900000-ms budget and 30000-ms cadence; exhaustion defers without buying review rounds. | `trident/inner-workflow.mjs:4579`; `trident/inner-workflow.mjs:4628`; `trident/inner-workflow.mjs:5600` | `trident/__tests__/ci-gate.test.ts:565`; `trident/__tests__/ci-gate.test.ts:602` | Silent: absent checks can strand the run indefinitely. |
| G054 | Both passed and failed settled checks may enter review; conflicting, unknown, and configuration-error states cannot spend review. | `trident/inner-workflow.mjs:5604`; `trident/inner-workflow.mjs:5609` | `trident/__tests__/ci-gate.test.ts:531` | Silent: a rewrite may either skip required readiness or prevent review of repairable red CI. |
| G055 | New red CI findings force rejection, while the same failing check measured on the base becomes advisory. | `trident/inner-workflow.mjs:5683`; `trident/inner-workflow.mjs:7343`; `trident/inner-workflow.mjs:7511` | `trident/__tests__/ci-gate.test.ts:290`; `trident/__tests__/ci-gate.test.ts:311` | Silent: review can approve newly failing CI or endlessly fix pre-existing failures. |
| G056 | Pending or unreadable CI joins the deferred-peer gate rather than granting approval. | `trident/inner-workflow.mjs:5765`; `trident/inner-workflow.mjs:7546` | `trident/__tests__/ci-gate.test.ts:246`; `trident/__tests__/ci-gate.test.ts:256` | Silent: unavailable CI can be silently omitted. |
| G057 | Enabled core review seats need usable verdicts; a missing seat contributes a blocker. | `trident/inner-workflow.mjs:6774`; `trident/inner-workflow.mjs:7368` | `trident/inner-workflow.test.ts:1435`; `trident/inner-workflow.test.ts:1464` | Silent: an incomplete panel can approve. |
| G058 | A configured cross-model peer that defers prevents approval, whereas an unconnected or deliberately disabled peer is not required. | `trident/inner-workflow.mjs:6653`; `trident/inner-workflow.mjs:6947`; `trident/inner-workflow.mjs:3320` | `trident/inner-workflow.test.ts:978`; `trident/inner-workflow.test.ts:1268` | Silent: an unavailable required peer can be dropped. |
| G059 | Seat retries are bounded and only retry deferred results, never usable verdicts; a rate-limit answer skips immediate retry. | `trident/inner-workflow.mjs:3369`; `trident/inner-workflow.mjs:3380`; `trident/inner-workflow.mjs:3395` | `trident/lane-retry.test.ts:238`; `trident/lane-retry.test.ts:561`; `trident/lane-retry.test.ts:672` | Silent: a failure can consume unbounded calls or overwrite a real verdict. |
| G060 | Missing or unusable synthesis, including a thrown review round, becomes an infrastructure block rather than approval. | `trident/inner-workflow.mjs:3998`; `trident/inner-workflow.mjs:4040` | `trident/__tests__/synthesis-unavailable.test.ts:311`; `trident/__tests__/synthesis-unavailable.test.ts:656` | Silent: no final judgment can become approval. |
| G061 | Only a nonempty set consisting entirely of nonblocking findings upgrades REQUEST_CHANGES; empty or unknown severity does not earn that upgrade. | `trident/inner-workflow.mjs:3076`; `trident/inner-workflow.mjs:3218` | `trident/inner-workflow.test.ts:2209`; `trident/inner-workflow.test.ts:2178` | Silent: a malformed rejection can be waived or minor findings can waste rounds. |
| G062 | Model-supplied advisory and reserved lane/suite markers are stripped before gating. | `trident/inner-workflow.mjs:3108` | `trident/inner-workflow.test.ts:2346`; `trident/inner-workflow.test.ts:2426` | Silent: a reviewer can mark a blocker as exempt. |
| G063 | When a test strategy is supplied, an unproven full suite forces rejection; deferred is excused only for a dispatched subset task. | `trident/inner-workflow.mjs:6408`; `trident/inner-workflow.mjs:6432`; `trident/inner-workflow.mjs:6477`; `trident/inner-workflow.mjs:6539` | `trident/inner-workflow-assembly.test.ts:1090`; `trident/inner-workflow-assembly.test.ts:1207` | Silent: an incomplete test run can approve; an absent strategy currently bypasses this check. |
| G064 | A testsPassed=true report paired with any explicit non-passed outcome is contradictory and blocks. | `trident/inner-workflow.mjs:6410` | `trident/inner-workflow-assembly.test.ts:1313` | Silent: contradictory evidence can authorize approval. |
| G065 | A pre-existing-red suite claim needs nonempty comparison evidence to be advisory; an empty claim remains a blocker. | `trident/inner-workflow.mjs:6434` | `trident/inner-workflow-assembly.test.ts:1233`; `trident/inner-workflow-assembly.test.ts:1254` | Silent: an unsupported excuse can waive testing. |
| G066 | An empty panel rejection padded only by advisory suite findings stops as infra-only without purchasing a Forge round. | `trident/inner-workflow.mjs:6543`; `trident/inner-workflow.mjs:6573` | `trident/inner-workflow.test.ts:2266` | Silent: the loop can spend rounds on no actionable finding. |
| G067 | A completed panel with only advisory findings does not buy code fix rounds; infra-only blocks also stop the fix loop. | `trident/inner-workflow.mjs:3437`; `trident/inner-workflow.mjs:8737`; `trident/inner-workflow.mjs:8738` | `trident/review-round-cap.test.ts:201` | Silent: infrastructure and advisory holds can consume the entire code budget. |

### Escalation and budgets

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G068 | Self-declared escalations must use design-gap or missing-dependency and supply concrete nonempty whatIsMissing text. | `trident/inner-workflow.mjs:3681`; `trident/inner-workflow.mjs:3721`; `trident/inner-workflow.mjs:3725` | `trident/__tests__/escalation-gate.test.ts:486`; `trident/__tests__/escalation-gate.test.ts:500` | Silent: a bare complaint can terminate a build. |
| G069 | APPROVE plus any escalation payload cannot approve and cannot authorize that declaration. | `trident/inner-workflow.mjs:3717`; `trident/inner-workflow.mjs:3744`; `trident/inner-workflow.mjs:7599` | `trident/__tests__/escalation-gate.test.ts:113` | Silent: a contradictory reply can authorize an irreversible merge. |
| G070 | A repeated actionable finding across fix rounds triggers a stop independently of reviewer self-declaration. | `trident/inner-workflow.mjs:3614`; `trident/inner-workflow.mjs:3787`; `trident/inner-workflow.mjs:3829` | `trident/__tests__/escalation-gate.test.ts:388`; `trident/__tests__/escalation-gate.test.ts:525` | Silent: the same defect can consume repeated rounds. |
| G071 | A blocker/major count that fails to decrease across two readable code rounds triggers no-progress escalation. | `trident/inner-workflow.mjs:3659`; `trident/inner-workflow.mjs:3788` | `trident/__tests__/escalation-gate.test.ts:432`; `trident/__tests__/escalation-gate.test.ts:447` | Silent: nonconverging work can spend the rest of its budget. |
| G072 | Missing finding identity or unreadable counts remain undecidable, not evidence of repetition or progress. | `trident/inner-workflow.mjs:3567`; `trident/inner-workflow.mjs:3623`; `trident/inner-workflow.mjs:3660` | `trident/__tests__/escalation-gate.test.ts:406`; `trident/__tests__/escalation-gate.test.ts:456` | Silent: invented arithmetic can stop a healthy run. |
| G073 | One valid design gap may purchase one bounded re-plan; a later design gap or post-re-plan trigger stops. | `trident/inner-workflow.mjs:3817`; `trident/inner-workflow.mjs:8676` | `trident/__tests__/escalation-gate.test.ts:569`; `trident/__tests__/escalation-gate.test.ts:584` | Silent: repeated replanning can evade escalation. |
| G074 | A valid missing dependency stops immediately rather than purchasing a re-plan. | `trident/inner-workflow.mjs:3823`; `trident/inner-workflow.mjs:3829` | `trident/__tests__/escalation-gate.test.ts:592` | Silent: the loop keeps building without a prerequisite. |
| G075 | A failed bounded re-plan escalates instead of silently rebuilding against the old plan. | `trident/inner-workflow.mjs:8802` | **NO TEST** | Silent: an invalid plan can survive its attempted replacement. |
| G076 | The review loop requires remaining round budget and increments by exactly one; the configured cap is threaded from the run row. | `trident/inner-loop.ts:602`; `trident/inner-workflow.mjs:8734`; `trident/inner-workflow.mjs:8740` | `trident/review-round-cap.test.ts:93`; `trident/review-round-cap.test.ts:109` | Silent: a rewrite can lose the only general fix ceiling. |
| G077 | A pending re-plan at the final allowed round becomes a design-gap stop. | `trident/inner-workflow.mjs:9034` | `trident/__tests__/escalation-e2e.test.ts:408`; `trident/__tests__/escalation-e2e.test.ts:435` | Silent: a known design gap can be lost at budget exhaustion. |
| G078 | An escalation stops the loop and overrides a severity-upgraded APPROVE in the terminal result. | `trident/inner-workflow.mjs:8733`; `trident/inner-workflow.mjs:9061` | `trident/__tests__/escalation-e2e.test.ts:654` | Silent: a stopped run can be merged as approved. |
| G079 | Ralph re-fire stops when the next durable task round exceeds max_ralph_rounds. | `trident/orchestrator.ts:4879` | `trident/orchestrator.test.ts:4346`; `trident/orchestrator.test.ts:4373` | Silent: task continuation can run forever. |
| G080 | Infrastructure retry requires a measured qualifying failure; APPROVE and genuine code review failures are not retryable infrastructure. | `trident/orchestrator.ts:1022`; `trident/orchestrator.ts:1024`; `trident/orchestrator.ts:1034` | `trident/infra-retry.test.ts:155` | Silent: code rejection can be reset into a fresh attempt. |
| G081 | Infrastructure retries have a durable retry ceiling and a separate launch backoff. | `trident/orchestrator.ts:5133`; `trident/orchestrator.ts:5134`; `trident/orchestrator.ts:6220` | `trident/infra-retry.test.ts:183`; `trident/infra-retry.test.ts:125` | Silent: infrastructure can create an endless retry loop. |
| G082 | A lost atomic retry claim does not launch another workflow. | `trident/orchestrator.ts:5156`; `trident/orchestrator.ts:5618` | `trident/infra-retry.test.ts:270`; `trident/store.test.ts:1072` | Silent: concurrent ticks can create duplicate work. |

### Publication and replay

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G083 | Outer publication requires PR mode and a locally resolved full branch head. | `trident/orchestrator.ts:2537`; `trident/orchestrator.ts:2545` | **NO TEST** | Loud: later git/PR operations would fail for missing inputs. |
| G084 | A fix-round reviewed-head pin must be full width and the produced head must descend from it before publishing. | `trident/orchestrator.ts:2569`; `trident/orchestrator.ts:2580` | `trident/orchestrator.test.ts:1396`; `trident/orchestrator.test.ts:1407`; `trident/orchestrator.test.ts:1415` | Silent: a fix can abandon reviewed work. |
| G085 | Publication requires a readable remote branch state before choosing its lease. | `trident/orchestrator.ts:2603` | **NO TEST** | Silent: an unknown branch can be treated as absent. |
| G086 | First publication with a base pin requires the branch to contain that pinned launch base. | `trident/orchestrator.ts:2610`; `trident/orchestrator.ts:2615` | `trident/orchestrator.test.ts:2859` | Silent: work built on another lane can be published. |
| G087 | Replay requires a readable remote base, available base object, and a provable fork point rather than a stale local-base fallback. | `trident/orchestrator.ts:1571`; `trident/orchestrator.ts:1614`; `trident/orchestrator.ts:1663` | `trident/orchestrator.test.ts:1818` | Silent: replay can import unrelated commits. |
| G088 | Replay requires readable depth and successful unshallowing when needed before ancestry decisions. | `trident/orchestrator.ts:1495`; `trident/orchestrator.ts:1499`; `trident/orchestrator.ts:1519` | `trident/publish-rebase-realgit.test.ts:248` | Silent: shallow history can produce a wrong replay boundary. |
| G089 | Replay refuses an unreadable or empty patch and an unavailable scratch worktree. | `trident/orchestrator.ts:1712`; `trident/orchestrator.ts:1730`; `trident/orchestrator.ts:1732`; `trident/orchestrator.ts:1753` | **NO TEST** | Loud: apply/commit generally fail without these artifacts. |
| G090 | A failed apply with no unmerged paths is a wholesale failure, not permission to invoke the conflict resolver. | `trident/orchestrator.ts:1850` | `trident/orchestrator.test.ts:2686` | Silent: a resolver can fabricate work for the wrong patch. |
| G091 | Replay conflicts require a configured resolver that actually reports resolution; otherwise they raise the conflict attention outcome. | `trident/orchestrator.ts:1861`; `trident/orchestrator.ts:1881` | `trident/orchestrator.test.ts:1949`; `trident/orchestrator.test.ts:2107` | Loud: unresolved git state normally prevents commit. |
| G092 | A claimed resolution must be checked against readable unmerged state and staged content, including residual conflict markers. | `trident/orchestrator.ts:1797`; `trident/orchestrator.ts:1832`; `trident/orchestrator.ts:1882` | `trident/orchestrator.test.ts:2197`; `trident/orchestrator.test.ts:2258`; `trident/orchestrator.test.ts:2468` | Silent: a staged file containing conflict residue can commit successfully. |
| G093 | Each replay resolution round must shrink the remaining conflict set. | `trident/orchestrator.ts:1901` | `trident/orchestrator.test.ts:2149`; `trident/orchestrator.test.ts:2515` | Silent: a resolver can spend repeated calls making no progress. |
| G094 | Replay conflict resolution cannot exceed MAX_CONFLICT_ROUNDS (12). | `trident/orchestrator.ts:1869`; `trident/merge.ts:198` | **NO TEST** | Silent: partially progressing resolution can become unbounded. |
| G095 | Replay must read the original commit message before composing its replay commit. | `trident/orchestrator.ts:1924` | `trident/publish-rebase-realgit.test.ts:396` | Silent: metadata can replace the original subject and provenance. |
| G096 | A resolution that leaves nothing to commit is a conflict attention outcome; other commit failures also stop publication. | `trident/orchestrator.ts:1949`; `trident/orchestrator.ts:1964` | `trident/orchestrator.test.ts:2609`; `trident/orchestrator.test.ts:2655` | Loud: git rejects the commit, but taxonomy can still be lost. |
| G097 | Replay advances the branch with compare-and-swap against the old head. | `trident/orchestrator.ts:1976` | **NO TEST** | Silent: another writer's branch advance can be overwritten. |
| G098 | Push uses the observed remote OID as an explicit lease, including an empty lease for first publication. | `trident/orchestrator.ts:2718`; `trident/orchestrator.ts:2730` | `trident/orchestrator.test.ts:1486`; `trident/orchestrator.test.ts:1535`; `trident/orchestrator.test.ts:1574` | Silent: a concurrent remote advance can be overwritten. |
| G099 | Successful push must be followed by an exact remote witness for the head to publish. | `trident/orchestrator.ts:2736` | `trident/orchestrator.test.ts:974` | Silent: an unconfirmed push can unlock review. |
| G100 | A real builder-claim/head disagreement refuses PR creation and review after preserving the branch on origin; an unresolvable claim is absent. | `trident/orchestrator.ts:2748`; `trident/orchestrator.ts:2341` | `trident/orchestrator.test.ts:1257`; `trident/orchestrator.test.ts:1288`; `trident/orchestrator.test.ts:1318` | Silent: review can judge the wrong commit or good work can be stranded. |
| G101 | PR creation must succeed and an open PR must be confirmed before review handoff. | `trident/orchestrator.ts:2760`; `trident/orchestrator.ts:2763` | `trident/orchestrator.test.ts:1908` | Loud: the review-readiness probe otherwise cannot find a PR. |
| G102 | Review dispatch requires a readable nonempty base-to-head change and a successfully materialized diff artifact. | `trident/orchestrator.ts:2884`; `trident/orchestrator.ts:3024`; `trident/orchestrator.ts:3048` | `trident/orchestrator.test.ts:2731` | Silent: a panel can approve an empty or stale artifact. |
| G103 | Diff reordering is allowed only with a valid seen pin and a manageable unquoted, unpadded path list; uncertainty uses the full unordered diff. | `trident/orchestrator.ts:2983`; `trident/orchestrator.ts:3011` | `trident/orchestrator.test.ts:835`; `trident/orchestrator.test.ts:881`; `trident/orchestrator.test.ts:927` | Silent: an optimization can drop files from review. |

### Harvest, merge, recovery, and preservation

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G104 | A result claiming APPROVE must have the row's own argus-approved checkpoint before the outer merge path runs. | `trident/orchestrator.ts:5198`; `trident/orchestrator.ts:5380` | `trident/orchestrator.test.ts:3926` | Silent: self-asserted approval can merge unreviewed work. |
| G105 | Post-approval mutation proof must succeed at the reviewed head or earn an explicit exemption before merge. | `trident/orchestrator.ts:5251`; `trident/orchestrator.ts:5267`; `trident/orchestrator.ts:5309` | `trident/orchestrator.test.ts:999`; `trident/mutation-prover.test.ts:5145`; `trident/mutation-prover.test.ts:5390` | Silent: a convincing claim can replace executable evidence. |
| G106 | A missing inline mutation nomination may be read only from the reviewed commit; malformed or absent artifact input does not become a pass. | `trident/orchestrator.ts:5258` | `trident/mutation-claim-artifact.test.ts:151`; `trident/mutation-claim-artifact.test.ts:181`; `trident/mutation-claim-artifact.test.ts:391` | Silent: an unrelated or uncommitted nomination can authorize merge. |
| G107 | PR merge requires a PR number and a full reviewed-head pin, and the remote merge command pins that reviewed commit. | `trident/merge.ts:1755`; `trident/merge.ts:1768` | `trident/merge.test.ts:153`; `trident/merge.test.ts:185`; `trident/merge.test.ts:196` | Silent: code changed after review can merge. |
| G108 | PR drift assessment requires the PR's actual base, a same-repository head, and a resolvable branch; unreadable or material overlapping drift holds. | `trident/merge.ts:1805`; `trident/merge.ts:1822`; `trident/merge.ts:1844`; `trident/merge.ts:1920` | `trident/merge.test.ts:1692`; `trident/merge.test.ts:1717`; `trident/orchestrator.test.ts:3894` | Silent: changed base semantics can bypass review. |
| G109 | Local merge requires a branch, isolated reusable worktree, and acceptable base drift before landing. | `trident/merge.ts:1644`; `trident/merge.ts:1969`; `trident/merge.ts:2053` | `trident/merge.test.ts:1180` | Silent: unreviewed interaction with the base can land (drift pin). |
| G110 | Dirty or unverifiable merge worktrees are preserved and cannot be silently reused or force-removed. | `trident/merge.ts:1547`; `trident/merge.ts:1644`; `trident/merge.ts:1713` | `trident/merge.test.ts:2088`; `trident/merge.test.ts:2132`; `trident/merge.test.ts:2155` | Silent: cleanup can destroy uncommitted work. |
| G111 | A terminal run is a no-op, and a harvestable result is handled before crash/orphan recovery. | `trident/orchestrator.ts:5467`; `trident/orchestrator.ts:5509` | **NO TEST** | Silent: a finished run can relaunch or a valid result can be replaced by failure. |
| G112 | Crash recovery first recognizes an already merged PR and then requires a winning recovery claim. | `trident/orchestrator.ts:5534`; `trident/orchestrator.ts:5536` | `trident/crash-recovery.test.ts:336` | Silent: landed work can be rebuilt or two recoveries can launch. |
| G113 | Positive run-process activity defers crash relaunch only within the inflight ceiling. | `trident/orchestrator.ts:5569`; `trident/orchestrator.ts:5581` | `trident/orchestrator.test.ts:5424`; `trident/orchestrator.test.ts:5491` | Silent: a live detached build can be duplicated. |
| G114 | Crash recovery has a durable maximum and repeated launch throws have a separate bounded fault counter. | `trident/orchestrator.ts:5593`; `trident/orchestrator.ts:5635`; `trident/orchestrator.ts:6241` | `trident/crash-recovery.test.ts:167`; `trident/crash-recovery.test.ts:227`; `trident/launch-throw-bounded.test.ts:96` | Silent: recovery can retry forever after every fault. |
| G115 | Completed, failed, or crashed subagents with no parseable terminal result are reaped rather than left running. | `trident/orchestrator.ts:5666` | `trident/orchestrator.test.ts:6967`; `trident/orchestrator.test.ts:6990` | Silent: an unharvestable run can retain its slot forever. |
| G116 | An unconfirmed fire is tied to its dispatch and only a late settle or post-dispatch stage evidence can confirm it. | `trident/orchestrator.ts:5736`; `trident/orchestrator.ts:5753`; `trident/orchestrator.ts:5777` | `trident/orchestrator.test.ts:5041`; `trident/orchestrator.test.ts:5081`; `trident/orchestrator.test.ts:5371` | Silent: a stale launch can confirm or kill its replacement. |
| G117 | At the second fire deadline, positive launched/published evidence is checked before failing and cancelling the unconfirmed launcher. | `trident/orchestrator.ts:5826`; `trident/orchestrator.ts:5840`; `trident/orchestrator.ts:5850` | `trident/orchestrator.test.ts:5212`; `trident/orchestrator.test.ts:5254` | Silent: a live or published build can be falsely killed. |
| G118 | The no-advance watchdog reaps suspected hangs or measured death unless sufficiently fresh run evidence warrants standing down. | `trident/orchestrator.ts:5898`; `trident/orchestrator.ts:6059`; `trident/orchestrator.ts:6144` | `trident/orchestrator.test.ts:5707`; `trident/orchestrator.test.ts:6333` | Silent: hangs can retain slots or live work can be killed. |
| G119 | Only run-scoped liveness may restamp progress; a live shared launcher alone cannot renew the run's clock. | `trident/orchestrator.ts:6072` | `trident/orchestrator.test.ts:5872` | Silent: a healthy shared process can make a dead run immortal. |
| G120 | Unknown run evidence defers a suspected-hang kill, but cannot override positive launcher death or the inflight ceiling. | `trident/orchestrator.ts:6118` | `trident/orchestrator.test.ts:6368`; `trident/orchestrator.test.ts:6435` | Silent: a blind probe can authorize a kill or hold forever. |
| G121 | Orphan handling obeys fail/wait/redispatch policy and allows only one redispatch per process. | `trident/orchestrator.ts:6156`; `trident/orchestrator.ts:6174` | `trident/orchestrator.test.ts:6509`; `trident/orchestrator.test.ts:6632`; `trident/orchestrator.test.ts:6644` | Silent: restart can create repeated duplicate launches. |
| G122 | A live branch-holder prevents an orphan redispatch. | `trident/orchestrator.ts:6183`; `trident/orchestrator.ts:6195` | `trident/orchestrator.test.ts:6569` | Silent: two processes can build the same branch. |
| G123 | The inflight ceiling terminates a run even when ordinary liveness reprieves would keep it alive. | `trident/orchestrator.ts:6059`; `trident/orchestrator.ts:6265` | `trident/orchestrator.test.ts:5491` | Silent: a run can hold resources indefinitely. |
| G124 | A thrown workflow writes a typed failure with no review verdict and no remaining tasks; failure to write falls back to the outer stall guard. | `trident/inner-workflow.mjs:9215`; `trident/inner-workflow.mjs:9245`; `trident/orchestrator.ts:6265` | `trident/inner-workflow.test.ts:1831` | Silent: a crashed workflow can remain pending or re-fire as successful progress. |
| G125 | Cleanup runs in finally independently of the builder result and delegates deletion decisions to the deterministic cleanup script. | `trident/inner-workflow.mjs:9250`; `trident/inner-workflow.mjs:9279` | `trident/inner-workflow.test.ts:1622`; `trident/inner-workflow.test.ts:1648` | Silent: a crashed builder can strand or destroy its only work. |
| G126 | Cleanup preserves dirty/unverifiable trees and retains local-mode branches; PR branch deletion needs a matching remote copy. | `trident/inner-workflow.mjs:9278` | `trident/worktree-cleanup-sh.test.ts:126`; `trident/worktree-cleanup-sh.test.ts:200`; `trident/worktree-cleanup-sh.test.ts:589` | Silent: unmerged commits or untracked work can be deleted (delegated script pin). |
| G127 | Cleanup reporting cannot treat missing exit evidence as success or confuse preserved output with successful removal. | `trident/inner-workflow.mjs:4498`; `trident/inner-workflow.mjs:9300` | `trident/inner-workflow.test.ts:1688`; `trident/inner-workflow.test.ts:1753` | Silent: preserved or uninspected work can disappear from the operator's view. |
| G128 | Failure salvage inspects only an attributable run worktree, excluding shared checkout, mismatched recorded path, and ambiguous candidates. | `trident/orchestrator.ts:3385`; `trident/orchestrator.ts:3390`; `trident/orchestrator.ts:3406`; `trident/orchestrator.ts:3410` | `trident/stranded-salvage-realgit.test.ts:527`; `trident/stranded-salvage-realgit.test.ts:549` | Silent: salvage can snapshot another lane's edits (live-owner pin only). |
| G129 | A dirty-work snapshot requires successful git staging/tree/commit/ref operations before it is reported recoverable. | `trident/orchestrator.ts:3255`; `trident/orchestrator.ts:3262`; `trident/orchestrator.ts:3288`; `trident/orchestrator.ts:3303` | `trident/stranded-salvage-realgit.test.ts:589`; `trident/stranded-salvage-realgit.test.ts:621` | Silent: a false preservation receipt can conceal data loss. |
| G130 | Board reconciliation may only detach this run into its own terminal lane; escalation payloads cannot reorder the queue. | `trident/board-reconcile.ts:49`; `trident/board-reconcile.ts:106`; `trident/board-reconcile.ts:122` | `trident/escalation-block.test.ts:537`; `trident/escalation-block.test.ts:579` | Silent: model output can reprioritize unrelated work. |
| G131 | Blocked-board routing requires failed phase, a harvest marker, and a coherent kind/payload pair. | `trident/escalation-block.ts:97`; `trident/escalation-block.ts:100`; `trident/escalation-block.ts:105` | `trident/escalation-block.test.ts:130`; `trident/escalation-block.test.ts:136`; `trident/escalation-block.test.ts:143` | Silent: stale or partial results can relabel unrelated failures as blocked. |

### Prompt requirements and advisory boundaries retained for review

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G132 | Workers are instructed never to ask interactive questions and to abort with a reason instead; this is a prompt requirement, not an independent refusal in this file. | `trident/inner-workflow.mjs:1350`; `trident/inner-loop.ts:787` | **NO TEST** | Silent: a worker can hang waiting for input (prompt pin only). |
| G133 | Workers are instructed to redirect verbose output rather than exhaust their context. | `trident/inner-workflow.mjs:1353` | **NO TEST** | Silent: useful task context can be lost; included as a resource precondition. |
| G134 | Workers are instructed never to kill by pattern and to kill only processes they started and can identify. | `trident/inner-workflow.mjs:1377`; `trident/inner-workflow.mjs:9289` | `trident/inner-workflow-assembly.test.ts:882` | Silent: a worker can terminate sibling lanes; prompt-only restriction. |
| G135 | Forge is instructed to use the resolved-HEAD commit wrapper, which rejects invalid HEAD or branch state before commit. | `trident/inner-workflow.mjs:1686` | `trident/commit-with-resolved-head-realgit.test.ts:43`; `trident/commit-with-resolved-head-realgit.test.ts:137` | Silent: a commit can land on the wrong branch; invocation is prompt-enforced. |
| G136 | Forge must stop after committing; member builds cannot publish and PR publication belongs to the outer loop. | `trident/inner-workflow.mjs:1687`; `trident/inner-workflow.mjs:1690` | `trident/inner-workflow.test.ts:590` | Silent: a worker can publish before the outer checks; prompt-only prohibition. |
| G137 | Reflection guidance is restricted to builder context and excluded from independent reviewers and verdict synthesis. | `trident/inner-loop.ts:667`; `trident/inner-workflow.mjs:7403` | `trident/inner-workflow.test.ts:1938`; `trident/inner-workflow.test.ts:1948` | Silent: mutable guidance can influence the review standard; included as a trust boundary. |
| G138 | The Argus rubric requires evidence-backed minority-veto review; its prose requirements remain distinct from deterministic severity arithmetic. | `trident/inner-workflow.mjs:2438`; `trident/inner-workflow.mjs:7429` | `trident/inner-workflow.test.ts:421` | Silent: losing the prompt can weaken what a valid review means. |
| G139 | Leak preflight must run before opening the PR, but unresolved findings or gate errors are logged and publication continues for CI enforcement. | `trident/orchestrator.ts:2669`; `trident/orchestrator.ts:2685`; `trident/orchestrator.ts:2772` | `trident/orchestrator.test.ts:9391`; `trident/orchestrator.test.ts:9434` | Silent: losing preflight loses early detection; it is NOT a publish veto. |
| G140 | A panel configured entirely within one model family emits a warning but accepts the configuration. | `trident/inner-workflow.mjs:7278` | **NO TEST** | Silent: losing the warning hides reduced independence; NOT a refusal. |

### Delegated proof and cleanup subgates

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G141 | Mutation proof requires a readable nonempty diff and a nomination within that diff unless the measured change qualifies for the prose/no-legal-target exemptions. | `trident/mutation-prover.ts:4539`; `trident/mutation-prover.ts:4591`; `trident/mutation-prover.ts:4625`; `trident/mutation-prover.ts:4634`; `trident/mutation-prover.ts:4642` | `trident/mutation-prover.test.ts:5376`; `trident/mutation-prover.test.ts:5761`; `trident/mutation-prover.test.ts:5772` | Silent: a missing claim or unreadable diff can become an exemption. |
| G142 | Mutation proof validates the branch and requires the pinned proof head to equal the caller's expected reviewed head. | `trident/mutation-prover.ts:4478`; `trident/mutation-prover.ts:4510`; `trident/mutation-prover.ts:4524` | `trident/mutation-prover.test.ts:6069`; `trident/mutation-prover.test.ts:5390` | Silent: proof can refer to a different branch or commit. |
| G143 | Mutation target and test argv must meet the prover's path, runner, separate-test, and distinct guard/control rules before execution. | `trident/mutation-prover.ts:2921`; `trident/mutation-prover.ts:646`; `trident/mutation-prover.ts:654` | `trident/mutation-prover.test.ts:1543`; `trident/mutation-prover.test.ts:1737`; `trident/mutation-prover.test.ts:1765`; `trident/mutation-prover.test.ts:5267` | Silent: tautological or redirected commands can manufacture evidence; detailed parser subcases stay owned by the prover tests. |
| G144 | Python module runners must not be shadowed by the branch's modules, pytest configuration, or importable packages. | `trident/mutation-prover.ts:3222`; `trident/mutation-prover.ts:3235`; `trident/mutation-prover.ts:3243`; `trident/mutation-prover.ts:3255` | `trident/mutation-prover.test.ts:638`; `trident/mutation-prover.test.ts:708`; `trident/mutation-prover.test.ts:739`; `trident/mutation-prover.test.ts:812` | Silent: branch code can impersonate the installed runner. |
| G145 | Runner load hooks and resolution-map values must not be branch-authored, and inherited preloads must not target changed files. | `trident/mutation-prover.ts:3320`; `trident/mutation-prover.ts:3333` | `trident/mutation-prover.test.ts:930`; `trident/mutation-prover.test.ts:1029`; `trident/mutation-prover.test.ts:1192` | Silent: unrelated tests can be made to execute the mutated file before assertions. |
| G146 | The mutation must change the target bytes. | `trident/mutation-prover.ts:3399`; `trident/mutation-prover.ts:3576` | `trident/mutation-prover.test.ts:1528` | Silent: unchanged input can be reported as a mutation experiment. |
| G147 | The mutated guard must exit red without timing out. | `trident/mutation-prover.ts:3579`; `trident/mutation-prover.ts:3582` | `trident/mutation-prover.test.ts:282`; `trident/mutation-prover.test.ts:312` | Silent: a timeout or passing guard can be sold as mutation coverage. |
| G148 | The control must stay green under the mutation without timing out. | `trident/mutation-prover.ts:3596` | `trident/mutation-prover.test.ts:291` | Silent: a broad crash can be sold as a specific behavioral test. |
| G149 | Restoration must restore the exact original bytes and the guard must return green without timing out. | `trident/mutation-prover.ts:3602`; `trident/mutation-prover.ts:3605` | `trident/mutation-prover.test.ts:298`; `trident/mutation-prover.test.ts:407` | Silent: the experiment can leave broken code or claim recovery that never happened. |
| G150 | Mutated and restored guard output hashes must differ. | `trident/mutation-prover.ts:3608` | `trident/mutation-prover.test.ts:305` | Silent: identical observations can be presented as opposite results. |
| G151 | Proof evidence must pass schema, run/head identity, token authentication, and observation checks before the outer gate accepts it. | `trident/mutation-prover.ts:3531`; `trident/mutation-prover.ts:3538`; `trident/mutation-prover.ts:3552`; `trident/mutation-prover.ts:4710` | `trident/mutation-prover.test.ts:5834` | Silent: handwritten or replayed evidence can authorize merge. |
| G152 | The branch must still equal the proof pin after execution and on exempt paths. | `trident/mutation-prover.ts:4543`; `trident/mutation-prover.ts:4593`; `trident/mutation-prover.ts:4698` | `trident/mutation-prover.test.ts:5350` | Silent: the proved code can differ from the merged code. |
| G153 | Cleanup refuses invalid arguments, unknown modes, or a non-repository before attempting deletion. | `trident/worktree-cleanup.sh:108`; `trident/worktree-cleanup.sh:117`; `trident/worktree-cleanup.sh:124` | `trident/worktree-cleanup-sh.test.ts:605`; `trident/worktree-cleanup-sh.test.ts:613`; `trident/worktree-cleanup-sh.test.ts:619` | Loud: later git commands generally reject invalid inputs. |
| G154 | An unreadable worktree enumeration is preservation, not an empty successful cleanup. | `trident/worktree-cleanup.sh:166` | `trident/worktree-cleanup-sh.test.ts:426` | Silent: uninspected work can be reported cleaned. |
| G155 | Cleanup skips the shared checkout and paths that are no longer the registered worktree root. | `trident/worktree-cleanup.sh:169`; `trident/worktree-cleanup.sh:209` | `trident/worktree-cleanup-sh.test.ts:335`; `trident/worktree-cleanup-sh.test.ts:381` | Silent: another checkout can be removed or falsely attributed to this run. |
| G156 | Cleanup preserves a PR branch when any worktree was preserved, the remote cannot be read, the branch was never pushed, or remote and local heads disagree. | `trident/worktree-cleanup.sh:265`; `trident/worktree-cleanup.sh:282`; `trident/worktree-cleanup.sh:288`; `trident/worktree-cleanup.sh:292` | `trident/worktree-cleanup-sh.test.ts:552`; `trident/worktree-cleanup-sh.test.ts:563`; `trident/worktree-cleanup-sh.test.ts:578` | Silent: the only ref to built work can be deleted. |
| G157 | Cleanup bounds remote reads when timeout is available and disables interactive git authentication. | `trident/worktree-cleanup.sh:99` | `trident/worktree-cleanup-sh.test.ts:500`; `trident/worktree-cleanup-sh.test.ts:523` | Silent: cleanup can hang after the run ends; timeout executable availability remains a limitation. |
| G158 | Merge conflict resolution has a 12-attempt ceiling, and a second-opinion retry requires remaining rounds and the one-arbitration-per-rebase allowance. | `trident/merge.ts:3639`; `trident/merge.ts:3706`; `trident/merge.ts:3707` | `trident/arbiter-wiring.test.ts:532`; `trident/arbiter-wiring.test.ts:587` | Silent: repeated conflict resolution can monopolize the serial sweep. |
| G159 | A truncated or unspecified Codex coverage flag is presented to synthesis as partial coverage, not a whole-change approval. | `trident/inner-workflow.mjs:7029`; `trident/inner-workflow.mjs:7032` | `trident/inner-workflow.test.ts:1116`; `trident/inner-workflow.test.ts:1147` | Silent: a partial review can be interpreted as complete; prompt-level constraint. |
| G160 | Fully base-excused red CI still holds REQUEST_CHANGES without buying a fix round, and a dead synthesis is not assigned a verdict. | `trident/inner-workflow.mjs:7534`; `trident/inner-workflow.mjs:7537` | `trident/__tests__/ci-gate.test.ts:3053`; `trident/__tests__/ci-gate.test.ts:3079`; `trident/__tests__/ci-gate.test.ts:3106` | Silent: pre-existing red can be silently merged or cause endless code fixes. |

### Additional boundary checks from the cross-check

| ID | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- |
| G161 | A proof must re-provision a hook-disabled worktree at the same commit before the restored observation and verify its bytes match the commit. | `trident/mutation-prover.ts:3000`; `trident/mutation-prover.ts:3006`; `trident/mutation-prover.ts:3009` | `trident/mutation-prover.test.ts:430`; `trident/mutation-prover.test.ts:457`; `trident/mutation-prover.test.ts:489`; `trident/mutation-prover.test.ts:507` | Silent: nominated commands can edit the restored test and forge green. |
| G162 | The mutation find string must occur exactly once and the target must remain within the worktree after symlink resolution. | `trident/mutation-prover.ts:3039`; `trident/mutation-prover.ts:3384` | `trident/mutation-prover.test.ts:1512`; `trident/mutation-prover.test.ts:1520`; `trident/mutation-prover.test.ts:1552` | Silent: a mutation can land on the wrong occurrence or outside its tree. |
| G163 | The proof shares one wall-clock deadline across mutated guard, control, and restored guard. | `trident/mutation-prover.ts:3019`; `trident/mutation-prover.ts:3412`; `trident/mutation-prover.ts:3505` | `trident/mutation-prover.test.ts:340` | Silent: multiple command budgets can make a proof run indefinitely longer than its allowance. |
| G164 | A bound review cannot record REQUEST_CHANGES without findings; it becomes REVIEW_NOT_RUN without invoking the build path. | `trident/orchestrator.ts:3847` | `trident/review-run.test.ts:423`; `trident/review-run.test.ts:460` | Silent: an unwritable or invented rejection can loop forever. |
| G165 | The delta-classification helper permits its cheap path only for readable nonempty documentation paths outside its denylist; production reachability is not established by this audit. | `trident/inner-workflow.mjs:3859`; `trident/inner-workflow.mjs:3899` | `trident/__tests__/delta-classifier.test.ts:43`; `trident/__tests__/delta-classifier.test.ts:66`; `trident/__tests__/delta-classifier.test.ts:90` | Silent: if reused without its predicate, an optimization can skip code review; included as uncertain/helper-only. |

## Independent vocabulary cross-check

The terminal vocabulary was enumerated from `trident/inner-workflow.mjs:4712` and checked against `trident/terminal-cause.ts:67`, rather than inferred from successful paths. All 16 entries map into this inventory:

| Vocabulary values | Rows that preserve the transition |
| --- | --- |
| `review-approved`, `resume-approved-unchanged` | G038, G061, G104–G107 |
| `round-budget-exhausted` | G041, G076 |
| `review-advisory-only`, `review-infra-only` | G056–G067, G160 |
| `review-escalated` | G068–G078 |
| `round-lost-work`, `round-lost-no-diff` | G042–G043 |
| `pr-already-merged` | G036, G112 |
| `resume-head-unreadable`, `built-head-unverified` | G015, G032–G033, G038 |
| `wave-member-built` | G009, G024, G034 |
| `handoff-publish`, `ralph-task-built` | G037, G083–G103 |
| `workflow-threw`, `unknown` | G005–G012, G124 |

`parseTerminalCause` rejects unrecognized values to null (`trident/terminal-cause.ts:162`); `unknown` is an explicit member, not the same thing as a missing field. `reviewLoopTerminalCause` selects lost work/diff, escalation, approval, infra/advisory stop, and budget exhaustion, then falls back to unknown (`trident/inner-workflow.mjs:4891`). This is a reporting vocabulary: adding a word alone does not add a gate.

The eight decoded block kinds are `none`, `code`, `infra-only`, `advisory-only`, `round-lost`, `design-gap`, `missing-dependency`, and `not-converging` (`trident/inner-loop.ts:899`). Unknown kinds become null. G011, G067–G078, and G131 pin their authority: code can purchase fixes, infra/advisory cannot, and escalation needs matching evidence. Fire outcomes are `fired`, `failed`, and `unconfirmed` (G003–G004, G116–G117), with an incomplete stream explicitly failed at `trident/inner-loop.ts:1118`.

The outer merge catch distinguishes `TridentBaseDriftHold` and `TridentMergeConflictEscalation`, then treats other errors as generic merge failures while retaining the approval (`trident/orchestrator.ts:5351`, `trident/orchestrator.ts:5362`, `trident/orchestrator.ts:5370`). Replay conflicts use `TridentRebaseConflict` (G090–G096), not a new code-review verdict. Infrastructure classification defaults to genuine and admits retries only on measured qualifying shapes (`trident/orchestrator.ts:1022`). Board routing defaults to failed except done or a derived coherent escalation (`trident/board-reconcile.ts:106`). These defaults are part of preservation: a new outcome that falls through can buy retries, suppress them, or label infrastructure as a code rejection.

## Limits, uncertainties, and continuous enforcement

This is a bounded inventory of the three named loop files and the explicitly followed boundary owners, **not a proof that every transitive dependency is covered**. Compound conditions are grouped by the decision they protect; rows and raw grep hits do not correspond one-to-one. Lexical searches can miss computed dispatch, dynamically constructed commands, a new synonym, an indirect helper, or a condition whose significance is not evident from its name. Test declarations can miss assertions inside generic fixtures or generated cases; this is why NO TEST is an unresolved pin, not an absence theorem. A positive control proves the search works on that spelling, not that the spelling spans every implementation.

The delegated mutation validator (G143) retains ownership of its many argument spellings; this document does not enumerate each runner-option alias. Transitive rebase/arbiter and bound-review checks are not exhaustively expanded here. Host credential resolution, dispatch admission, scheduler concurrency, Workflow runtime token accounting, GitHub branch protection, and CI's own leak scanner are outside the three-file audit boundary. They must not be mistaken for guards implemented by these files. This slice adds no runtime token-budget gate merely because `budget.spent()` appears in logging (`trident/inner-workflow.mjs:7630`).

G139 and G140 are deliberately included even though they do not refuse: preflight and single-family warnings are easy to mistake for hard gates. G132–G138 and G159 include prompt requirements and data boundaries because losing them changes what a review or build can mean. They depend on the agent following its instructions; they are not independent enforcement. The cleanup command and commit wrapper contain deterministic checks, but the prompt is still the invocation boundary (`trident/inner-workflow.mjs:1686`, `trident/inner-workflow.mjs:9291`).

Code-level gates are maintained by the loop's comparisons on every transition. Retry/recovery bounds also rely on persisted counts and atomic claims (G079–G082, G114). The outer watchdog remains a backstop when the inner workflow cannot persist a failure (G123–G124), so that backstop does not require the failed inner agent to cooperate; it still requires the outer ticker and storage to run. Cleanup in finally similarly does not require a valid builder result, but process death can prevent finally from executing (G125). This document introduces no new invariant or enforcement mechanism and makes no broader crash-survival claim.

No code or tests were changed, no new guard was added, and no mutation experiment was performed in this documentation slice. Existing test references are preservation targets for the replacement lane, not permission to weaken tests or a claim that all cited tests were run here. In particular, the existing ten-round default and per-run cap (G076) are what this inventory records; it does not silently substitute a prose review-round policy for the implemented condition.
