# Trident gates inventory — #545, enumeration slice

> **Current terminology; historical anchors stay intact.** Fresh builds use the
> initial planner's persisted `single` or `task_sequence` strategy. In pre-cutover
> evidence, stored `ralph = 1` maps to `task_sequence` and `ralph = 0` maps to
> `single`; `ralph_round` → `task_iteration`, `max_ralph_rounds` →
> `max_task_iterations`, `ralph_task_total` → `task_total`, `ralph-plan` →
> `task-plan`, `ralph-task` → `task-build`, and
> `ralph-task-built[-deviated]` → `task-built[-deviated]`. Gate IDs, old source
> anchors, migration names, test filenames, and recorded checkpoint strings are
> deliberately unchanged so this inventory continues to identify the evidence it
> originally enumerated.

**3 NO TEST entries out of 166 preservation entries.** 156 entries are classified **Silent** if lost and 10 are **Loud**. The counts are direct row enumerations: `awk -F'|' '/^\\| G[0-9][0-9][0-9] / { if ($6 ~ /NO TEST/) u++; if ($7 ~ /Silent:/) s++; else if ($7 ~ /Loud:/) l++; n++ } END { print n, u+0, s, l }' docs/trident-gates-inventory.md` prints `166 3 156 10`. G135 moved from Silent to Loud when #818 added independent pre-commit enforcement; its row changed, but the summary did not. No other loss-classification row moved. The count is conservative: `NO TEST` means this audit did not locate a sufficiently specific pin, including cases where only a related happy-path or argument-threading test was found. It is not a claim that the repository contains no relevant test. The original rows are the result of static inspection. The #745 follow-up mutation-certified G021, G022, G023, G024, G034 and G075 with complete workflow execution in `trident/inner-workflow-gates.test.ts`; other rows are not freshly certified.

Baseline: `f7320fc76eb3e1d0775dabd56cf957da632904e5`. The three subject files were measured at 9,315, 6,316, and 1,167 lines respectively (16,798 total), matching the task brief. The staged issue contains no numerical source anchors to correct; the preservation requirement is `docs/spec-items/the-orchestrator-owns-the-build-loop.md:56`. This slice inventories the existing loop; it does not satisfy the replacement or end-to-end build criteria.

## Derivation and audit boundary

The starting point was **grep indexes, not a sequential read**. P1 enumerated refusal/outcome vocabulary across all three subjects. P2 independently indexed control flow, early returns, compound predicates, and conditional expressions, including guards without refusal words. P3 caught embedded shell and prompt requirements. Matched function neighborhoods were then read with line numbers. T independently enumerated test declarations under `trident/`, including `__tests__/`; V independently indexed the emitted verdict, terminal-cause, and error vocabularies. D followed the merge, proof, cleanup, and board boundaries reached from the subjects. Repeated matches in one predicate were folded into one row; a separate outcome or independently losable precondition earned another row. Delegated subgates supplement their call-site rows rather than counting as additional call sites.

Reproduce the searches with `rg -n '<pattern>' <scope>`. The following are actual match counts, including comments and repeated mentions, **not gate counts**. Each pattern has a positive control found by that same invocation:

| Search | Scope | Matching lines | Positive control |
| --- | --- | ---: | --- |
| P1 | three subjects | 1153 | `pre-#845 trident/inner-workflow.mjs:8494` |
| P2 | three subjects | 3076 | `trident/inner-loop.ts:585` |
| P3 | three subjects | 420 | `pre-#845 trident/inner-workflow.mjs:1351` |
| T | 128 files enumerated as below | 4868 | `trident/escalation-block.test.ts:537` |
| V | three subjects + terminal-cause.ts, merge.ts, escalation-block.ts | 91 | `pre-#845 trident/inner-workflow.mjs:4712` |
| D | merge.ts, mutation-prover.ts, worktree-cleanup.sh, board-reconcile.ts, escalation-block.ts | 1004 | `trident/board-reconcile.ts:122` |

Exact patterns (P1–P3 use the three subject paths; T uses the enumerated test files; V and D use the table scopes):

```text
P1  throw |refus|blocked|budget|cap\b|terminal|verdict|precondition|purity|leak|escalat
P2  \b(if|while|for|switch|catch|finally|return|break|continue)\b|\?|&&|\|\|
P3  exit [1-9]|MUST|NEVER|[Bb]udget|allowedTools|disallowedTools|[Tt]imeout|[Mm]ax[A-Z]|[Ll]imit
T  ^\s*(test|it|describe)(\.|\()
V  TERMINAL_CAUSES|TERMINAL_CAUSE_KINDS|block_kind:|status:|class .*Error|class Trident|return.*(genuine|infrastructure|REVIEW_NOT_RUN)
D  throw |refus|return |exit [1-9]|MAX_|if |detachRun
```

All abbreviated scope names above are under `trident/`. T's file set is the sorted output of `rg --files trident -g '*test.ts'`; declarations, not filenames alone, supplied the test index. The exploratory `test(` searches also match regular-expression `.test()` calls, so those matches were not accepted as tests. Tests cited below point to a declaration or its assertion, not merely the top of a file. Source-string checks count as structural pins only; a rewrite will need to replace those pins with behavioral checks against its new boundary.

`NO TEST` adjudication used both the T declaration index and searches for the matched enforcement symbol/refusal text in test bodies. The positive control for the latter is `enforceCrossModelGate` in `trident/inner-workflow.test.ts:995`. A related fixture is not proof of the refusal: for example, `trident/inner-workflow.test.ts:254` supplies a branch and tests argument threading, so it does not pin the missing-branch throw in G001. Likewise `trident/inner-workflow.test.ts:297` tests successful pinned-task selection, not G024's missing-plan/missing-task exits.

**Loss classification** is a conservative engineering inference, separate from test coverage. **Silent** means removing the boundary can admit wrong work, lose data, waste work, or leave a run stuck without a reliable downstream refusal of the same condition. An eventual timeout does not make wrong work loud. **Loud** means an identified downstream operation would ordinarily fail; it does not promise the same diagnosis, timing, or preservation. No row's “Silent” classification asserts that its current implementation emits no diagnostic.

## Dangerous set and missing pins

The **NO TEST** set, enumerated from the rows below: G001, G132, G133.

The complete **Silent** subset of this inventory is grouped here for rewrite review (IDs refer to the evidence-bearing rows below):

- Launch and result boundary: G001, G003, G004, G005, G006, G008, G009, G010, G011, G012, G013, G014, G015, G016, G017, G018, G019.
- Planner, builder, and resume: G020, G021, G022, G023, G024, G025, G026, G027, G028, G029, G030, G032, G033, G034, G035, G036, G037, G038, G039, G040, G041, G042, G043.
- Review readiness and verdict gates: G044, G045, G046, G047, G048, G049, G050, G051, G052, G053, G054, G055, G056, G057, G058, G059, G060, G061, G062, G063, G064, G065, G066, G067.
- Escalation and budgets: G068, G069, G070, G071, G072, G073, G074, G075, G076, G077, G078, G079, G080, G081, G082.
- Publication and replay: G084, G085, G086, G087, G088, G090, G092, G093, G094, G095, G097, G098, G099, G100, G102, G103, G166.
- Harvest, merge, recovery, and preservation: G104, G105, G106, G107, G108, G109, G110, G111, G112, G113, G114, G115, G116, G117, G118, G119, G120, G121, G122, G123, G124, G125, G126, G127, G128, G129, G130, G131.
- Prompt requirements and advisory boundaries retained for review: G132, G133, G134, G135, G136, G137, G138, G139, G140.
- Delegated proof and cleanup subgates: G141, G142, G143, G144, G145, G146, G147, G148, G149, G150, G151, G152, G154, G155, G156, G157, G158, G159, G160.
- Additional boundary checks from the cross-check: G161, G162, G163, G164, G165.

## Inventory

Every row has one cutover fate. `keep-in-place` means its current TypeScript, shell, or hook owner remains a host-owned service; `re-home-to-TS` means behavior embedded in `inner-workflow.mjs` moves to the sole TypeScript replacement; `deleted-with-the-mechanism` means the condition exists only because of a component the locked cutover deletes. The deleted set is G002, G003, G004 (fire construction/stream/settlement), G021 (the launcher-supplied wrapper protocol), G116 and G117 (unconfirmed-fire settlement deadlines), and G119 (shared-launcher liveness). G022 is re-homed rather than deleted because the replacement still distinguishes complete, failed, and unknown bounded-work results.

All `inner-workflow.mjs:NNNN` anchors in this inventory are explicitly labeled `pre-#845`. They preserve the audit's historical evidence without pretending that the old line numbers address the shortened post-#845 file.

### Launch and result boundary

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G001 | keep-in-place | A wave child must carry its assigned branch before workflow arguments can be built. | `trident/inner-loop.ts:585` | **NO TEST** | Silent: child could build on the wrong branch. |
| G002 | deleted-with-the-mechanism | A fire requires a substrate or substrate factory; the actual condition rejects neither supplied, not both supplied. | `trident/inner-loop.ts:1030` | `trident/inner-loop.test.ts:1082` | Loud: starting without either would throw later. |
| G003 | deleted-with-the-mechanism | A launcher stream must complete; an error, thrown start, or stream ending without completion is a failed fire. | `trident/inner-loop.ts:1061`; `trident/inner-loop.ts:1101`; `trident/inner-loop.ts:1118` | `trident/inner-loop.test.ts:839`; `trident/inner-loop.test.ts:849`; `trident/inner-loop.test.ts:1057` | Silent: launch failure could be reported as success. |
| G004 | deleted-with-the-mechanism | The settle timer returns unconfirmed and leaves the launcher running for later confirmation rather than cancelling at the first deadline. The timer is bypassed for a nonpositive settle timeout. | `trident/inner-loop.ts:1139`; `trident/inner-loop.ts:1121` | `trident/inner-loop.test.ts:862`; `trident/inner-loop.test.ts:916` | Silent: premature cancellation can destroy live work. |
| G005 | keep-in-place | A terminal result must parse as a non-null object before harvest; malformed JSON remains unharvestable (arrays are not explicitly excluded here). | `trident/inner-loop.ts:834` | `trident/inner-loop.test.ts:369` | Silent: invalid results could advance a run. |
| G006 | keep-in-place | Only exact APPROVE and REQUEST_CHANGES tokens decode as verdicts; anything else becomes null. | `trident/inner-loop.ts:952` | `trident/inner-loop.test.ts:377` | Silent: malformed output could acquire authority. |
| G007 | keep-in-place | A PR number must be a positive integer; malformed numbers become null rather than replacing the known PR. | `trident/inner-loop.ts:856` | `trident/inner-loop.test.ts:241` | Loud: downstream PR operations target an invalid number. |
| G008 | keep-in-place | Reported rounds must be safe integers and cannot lower the outer row's recorded round. | `trident/inner-loop.ts:866`; `trident/orchestrator.ts:4977` | `trident/orchestrator.test.ts:4023`; `trident/orchestrator.test.ts:4034` | Silent: a resumed run could regain spent rounds. |
| G009 | keep-in-place | Merge, publish, and child-built signals require exact booleans; a built child also needs a decoded full commit SHA. | `trident/inner-loop.ts:872`; `trident/inner-loop.ts:877`; `trident/inner-loop.ts:878`; `trident/orchestrator.ts:4987` | `trident/inner-loop.test.ts:220`; `trident/orchestrator.test.ts:330` | Silent: truthy junk could skip work or mark unbuilt work done. |
| G010 | keep-in-place | Unknown block kinds and malformed escalation payloads cannot acquire a recognized routing kind. | `trident/inner-loop.ts:899`; `trident/inner-loop.ts:913` | `trident/inner-loop.test.ts:301`; `trident/escalation-block.test.ts:79`; `trident/escalation-block.test.ts:92` | Silent: an invented outcome could change retry or board routing. |
| G011 | keep-in-place | A rejection is recorded as reviewed only with Argus provenance and findings, or a coherent escalation; otherwise it is REVIEW_NOT_RUN. | `trident/bound-review.ts:49`; `trident/bound-review.ts:78`; `trident/bound-review.ts:94` | `trident/escalation-block.test.ts:277`; `trident/escalation-block.test.ts:295`; `trident/escalation-block.test.ts:319` | Silent: an infrastructure failure could impersonate a review. |
| G012 | keep-in-place | Terminal causes are trimmed and capped before persistence, and unknown cause kinds decode through the closed vocabulary. | `trident/inner-loop.ts:916`; `trident/inner-loop.ts:925` | `trident/inner-loop.test.ts:301`; `trident/inner-loop.test.ts:331` | Silent: unbounded or unclassified output reaches the run record. |
| G013 | keep-in-place | A fresh PR build must fetch and resolve the remote base before firing; two failed fetches stop it. | `trident/launch-preparation.ts:260`; `trident/launch-preparation.ts:288`; `trident/launch-preparation.ts:300` | `trident/orchestrator.test.ts:9026` | Silent: the build can start on stale code. |
| G014 | keep-in-place | A dispatch seed must still match the live head or it is discarded before a fresh launch. | `trident/launch-preparation.ts:146` | `trident/orchestrator.test.ts:8039` | Silent: stale work can masquerade as the requested build. |
| G015 | keep-in-place | A resume whose checkpoint depends on head identity stops when the live head cannot be read, after bounded retries. | `trident/orchestrator.ts:1111`; `trident/launch-preparation.ts:206` | `trident/orchestrator.test.ts:7241`; `trident/orchestrator.test.ts:7264` | Silent: unreadability can be mistaken for absence or unchanged code. |
| G016 | keep-in-place | A fresh launch refuses an existing local branch not contained in the base unless the prior pin and recorded work establish this run's own leftover. | `trident/launch-preparation.ts:573`; `trident/launch-preparation.ts:625`; `trident/launch-preparation.ts:656` | `trident/orchestrator.test.ts:7537`; `trident/orchestrator.test.ts:8336`; `trident/orchestrator.test.ts:8386` | Silent: one run can adopt another run's work. |
| G017 | keep-in-place | Negative ancestry requires repeated exit-1 answers with complete history on both sides; unknown or shallow evidence refuses without alleging divergence. | `trident/launch-preparation.ts:416`; `trident/launch-preparation.ts:452`; `trident/launch-preparation.ts:544` | `trident/orchestrator.test.ts:7588`; `trident/orchestrator.test.ts:7743`; `trident/orchestrator.test.ts:7855` | Silent: a healthy branch can be falsely rejected with destructive advice. |
| G018 | keep-in-place | The dispatch-ID minter must return a nonempty string before launch state advances. | `trident/launch-preparation.ts:694` | `trident/orchestrator.test.ts:353`; `trident/orchestrator.test.ts:356` | Silent: a run can lose its durable dispatch identity. |
| G019 | keep-in-place | A bound-PR review failure terminates the review run instead of entering the build flow. | `trident/bound-review.ts:122`; `trident/bound-review.ts:163` | `trident/review-run.test.ts:492` | Silent: review failure could trigger an unintended build. |

### Planner, builder, and resume

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G020 | re-home-to-TS | An unpinned diff base must be nonempty, unpadded, and not option-shaped before it becomes shell text. | `pre-#845 trident/inner-workflow.mjs:1555`; `pre-#845 trident/inner-workflow.mjs:1580`; `pre-#845 trident/inner-workflow.mjs:1585` | `trident/diff-base-option-shaped.test.ts:288`; `trident/diff-base-option-shaped.test.ts:799` | Silent: malformed operands can yield a plausible wrong diff. |
| G021 | deleted-with-the-mechanism | A CLI build requires the launcher-supplied harness wrapper path rather than resolving a wrapper from the target repository. | `pre-#845 trident/inner-workflow.mjs:2333` | `trident/inner-workflow-gates.test.ts:116` | Silent: a different wrapper can change the build contract. |
| G022 | re-home-to-TS | A CLI build cannot proceed without a complete trailer or a connected result; bounded probes distinguish a recorded exit from an unknown wait. | `pre-#845 trident/inner-workflow.mjs:2366`; `pre-#845 trident/inner-workflow.mjs:2395`; `pre-#845 trident/inner-workflow.mjs:2397` | `trident/inner-workflow-gates.test.ts:81` | Silent: an unfinished build can be treated as complete. |
| G023 | re-home-to-TS | A reported builder branch must agree with the deterministic assigned branch. | `pre-#845 trident/inner-workflow.mjs:2413` | `trident/inner-workflow-gates.test.ts:129` | Silent: subsequent review and cleanup could target different work. |
| G024 | re-home-to-TS | A wave member must receive a plan and its pinned task must still be unchecked; it cannot select another task. | `pre-#845 trident/inner-workflow.mjs:7944`; `pre-#845 trident/inner-workflow.mjs:7950` | `trident/inner-workflow-gates.test.ts:144` | Silent: a child could silently execute the wrong task. |
| G025 | re-home-to-TS | Ralph builds require a non-null planner result before Forge runs. | `pre-#845 trident/inner-workflow.mjs:8122` | `trident/inner-workflow-plan-next.test.ts:1278` | Silent: an agent could improvise work with no execution plan. |
| G026 | re-home-to-TS | The cheap continuation planner requires the clean handoff and a valid positive round outside the periodic full-refresh interval. | `pre-#845 trident/inner-workflow.mjs:8012` | `trident/inner-workflow-plan-next.test.ts:508`; `trident/inner-workflow-plan-next.test.ts:1333` | Silent: an exhausted or deviated plan can be reused indefinitely. |
| G027 | re-home-to-TS | The cheap planner requires a found nonempty committed plan with a positive integer unchecked count. | `pre-#845 trident/inner-workflow.mjs:8064` | `trident/inner-workflow-plan-next.test.ts:868`; `trident/inner-workflow-plan-next.test.ts:881` | Silent: a missing or exhausted plan can select no meaningful work. |
| G028 | re-home-to-TS | The cheap planner requires the plan body's checksum and measured unchecked count to agree with the probe. | `pre-#845 trident/inner-workflow.mjs:1219`; `pre-#845 trident/inner-workflow.mjs:8072` | `trident/inner-workflow-plan-next.test.ts:924`; `trident/inner-workflow-plan-next.test.ts:1058` | Silent: a truncated or changed relay can become the execution plan. |
| G029 | re-home-to-TS | A cheap planner's changed body, task, and remaining count are replaced by the committed measurements before execution. | `pre-#845 trident/inner-workflow.mjs:8145`; `pre-#845 trident/inner-workflow.mjs:8163`; `pre-#845 trident/inner-workflow.mjs:8192` | `trident/inner-workflow-plan-next.test.ts:1112`; `trident/inner-workflow-plan-next.test.ts:1127`; `trident/inner-workflow-plan-next.test.ts:1169` | Silent: model paraphrases could change task identity or end the run early. |
| G030 | re-home-to-TS | Branch-log and branch-brief data are delimiter-neutralized and byte-capped before being used as untrusted prompt material. | `pre-#845 trident/inner-workflow.mjs:2685`; `pre-#845 trident/inner-workflow.mjs:2707`; `trident/gates/result-contract.ts:178`; `trident/build-run.ts:372` | `trident/gates/result-contract.test.ts:27`; `trident/build-run.test.ts:305` | Silent: data could escape its prompt boundary or consume the context. |
| G031 | re-home-to-TS | A null initial Forge result terminates instead of opening review. | `pre-#845 trident/inner-workflow.mjs:8249` | `trident/__tests__/cross-model-dispatch.test.ts:765` — added by #738, after this inventory's baseline. It drives the workflow with a null build and asserts the terminal class, so replacing the guard with `if (false)` turns it red. `trident/infra-retry.test.ts:125` does NOT certify this gate: it injects an outer failure result and stays green under that same mutation. | Loud: downstream artifact/head requirements would also reject an empty build. |
| G032 | re-home-to-TS | Local builds and fixes require a readable full built head, with bounded read attempts, before trusting a reported diff. | `trident/build-run.ts:318`; `trident/build-run.ts:324` | `trident/build-run.test.ts:1052`; `trident/build-run.test.ts:1075` | Silent: an unverified artifact can be reviewed as current work. |
| G033 | re-home-to-TS | Local builder commit claims, when parseable, must prefix-match the measured head; PR claims are deferred to the publisher’s independent object check. | `pre-#845 trident/inner-workflow.mjs:8287`; `pre-#845 trident/inner-workflow.mjs:8854` | `trident/inner-workflow-built-head.test.ts:98`; `trident/inner-workflow-built-head.test.ts:147` | Silent: review can approve a different commit from the one built. |
| G034 | re-home-to-TS | A wave member must produce a full commit before returning its terminal built result without publication or review. | `pre-#845 trident/inner-workflow.mjs:8371` | `trident/inner-workflow-gates.test.ts:169` | Silent: a join can consume an unbuilt member. |
| G035 | re-home-to-TS | A fresh review requires both a branch head and a diff artifact. | `pre-#845 trident/inner-workflow.mjs:8480` | `trident/inner-workflow-built-head.test.ts:229` | Silent: a panel can approve no change. |
| G036 | re-home-to-TS | A confirmed merged PR terminates before re-fire or lost-round handling. | `pre-#845 trident/inner-workflow.mjs:8420`; `pre-#845 trident/inner-workflow.mjs:8941`; `trident/orchestrator.ts:5016` | `trident/orchestrator.test.ts:3611`; `trident/orchestrator.test.ts:3649` | Silent: already-landed work can be rebuilt or reported failed. |
| G037 | re-home-to-TS | Intermediate Task sequence work defers review and publication until the terminal task; the outer handoff clears the old result atomically. | `pre-#845 trident/inner-workflow.mjs:8446`; `trident/orchestrator.ts:2261`; `trident/orchestrator.ts:2378` | `trident/inner-workflow-task-sequence-refire.test.ts:109`; `trident/orchestrator.test.ts:803`; `trident/orchestrator.test.ts:4759` | Silent: partial work can merge or an old result can be harvested repeatedly. |
| G038 | re-home-to-TS | Resume fast paths require the same full recorded and live head; a missing or moved head rebuilds and an unreadable required head stops. | `pre-#845 trident/inner-workflow.mjs:4193`; `pre-#845 trident/inner-workflow.mjs:4208`; `pre-#845 trident/inner-workflow.mjs:4219` | `trident/inner-workflow-resume.test.ts:625`; `trident/inner-workflow-resume.test.ts:662`; `trident/inner-workflow-resume.test.ts:671` | Silent: stale approval can authorize changed code. |
| G039 | re-home-to-TS | A resumed rejection may skip review only when recorded actionable code findings exist; advisory or lane findings require re-review. | `pre-#845 trident/inner-workflow.mjs:4242`; `pre-#845 trident/inner-workflow.mjs:8543` | `trident/inner-workflow-resume.test.ts:413`; `trident/inner-workflow-resume.test.ts:461`; `trident/inner-workflow-resume.test.ts:687` | Silent: Forge can repeat work against stale infrastructure findings. |
| G040 | re-home-to-TS | Resumed review requires a regenerated diff from the pinned OID; an empty result falls back to rebuilding. | `pre-#845 trident/inner-workflow.mjs:6018`; `pre-#845 trident/inner-workflow.mjs:7776` | `trident/inner-workflow-resume.test.ts:317`; `trident/inner-workflow-resume.test.ts:681` | Silent: review can use absent or unrelated content. |
| G041 | re-home-to-TS | A resumed fix inherits its spent round count instead of restarting the budget. | `pre-#845 trident/inner-workflow.mjs:4268`; `pre-#845 trident/inner-workflow.mjs:7927` | `trident/inner-workflow-resume.test.ts:580`; `trident/inner-workflow-resume.test.ts:597` | Silent: crash recovery can make the loop unbounded. |
| G042 | re-home-to-TS | An unchanged or unreadable post-fix head stops the loop as round-lost after checking for an actual merge. | `pre-#845 trident/inner-workflow.mjs:4075`; `pre-#845 trident/inner-workflow.mjs:8948` | `trident/round-landed.test.ts:76`; `trident/round-landed.test.ts:87`; `trident/round-landed.test.ts:168` | Silent: review repeats while no fix lands. |
| G043 | re-home-to-TS | A fix leaving no diff stops before paying for another review. | `pre-#845 trident/inner-workflow.mjs:8971` | `trident/inner-workflow-built-head.test.ts:314` | Silent: a panel can approve a fix that erased the change. |

### Review readiness and verdict gates

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G044 | re-home-to-TS | Review readiness must know the required-check configuration; unknown configuration defers rather than declaring an unprotected base. | `pre-#845 trident/inner-workflow.mjs:5020`; `pre-#845 trident/inner-workflow.mjs:5394` | `trident/gates/review-readiness.test.ts:16`; `trident/gates/review-readiness.test.ts:17` | Silent: unreadable protection can become permission to proceed. |
| G045 | re-home-to-TS | Protection contexts and ruleset checks are combined, and an ambiguous protection 404 requires independent branch evidence. | `pre-#845 trident/inner-workflow.mjs:5192`; `pre-#845 trident/inner-workflow.mjs:5215` | `trident/production-host-effects.test.ts:129`; `trident/production-host-effects.test.ts:141` | Silent: one protection source can vanish from the required set. |
| G046 | re-home-to-TS | Produced-check evidence must include complete check-run and status lists with valid counts; unreadability disables the fast configuration-fault inference. | `pre-#845 trident/inner-workflow.mjs:5262`; `pre-#845 trident/inner-workflow.mjs:5290` | `trident/production-host-effects.test.ts:928`; `trident/production-host-effects.test.ts:951`; `trident/ci-readiness.test.ts:27`; `trident/gates/review-readiness.test.ts:90` | Silent: a truncated producer list can falsely end the run. |
| G047 | re-home-to-TS | Readiness requires parseable PR data and MERGEABLE status; conflicts stop immediately and unknown mergeability waits. | `pre-#845 trident/inner-workflow.mjs:5418`; `pre-#845 trident/inner-workflow.mjs:5432`; `pre-#845 trident/inner-workflow.mjs:5435` | `trident/gates/review-readiness.test.ts:20`; `trident/gates/review-readiness.test.ts:21`; `trident/gates/review-readiness.test.ts:22` | Silent: paid review can run against an unreviewable PR. |
| G048 | re-home-to-TS | An app-bound required name cannot be satisfied by a classic commit-status row; this checks row shape, not actual app identity. | `pre-#845 trident/inner-workflow.mjs:5472`; `pre-#845 trident/inner-workflow.mjs:5505` | `trident/ci-readiness.test.ts:11`; `trident/ci-readiness.test.ts:13` | Silent: the wrong producer can satisfy the requirement. |
| G049 | re-home-to-TS | Every required check must have run and settled; missing, skipped-only, or running checks defer. | `pre-#845 trident/inner-workflow.mjs:5564`; `pre-#845 trident/inner-workflow.mjs:5568`; `pre-#845 trident/inner-workflow.mjs:5571` | `trident/gates/review-readiness.test.ts:26`; `trident/gates/review-readiness.test.ts:28`; `trident/gates/review-readiness.test.ts:29` | Silent: pending or skipped checks can appear complete. |
| G050 | re-home-to-TS | A missing required name becomes a configuration stop only after grace, nonempty base evidence, and a nonempty settled PR rollup. | `pre-#845 trident/inner-workflow.mjs:5493`; `pre-#845 trident/inner-workflow.mjs:5549` | `trident/ci-readiness.test.ts:19`; `trident/ci-readiness.test.ts:20`; `trident/ci-readiness.test.ts:21`; `trident/ci-readiness.test.ts:22` | Silent: a queued or conditional job can be falsely declared impossible. |
| G051 | re-home-to-TS | A fresh resolved configuration reclassifies a tentative configuration error before it is accepted. | `pre-#845 trident/inner-workflow.mjs:5380` | `trident/ci-readiness.test.ts:32`; `trident/ci-readiness.test.ts:34` | Silent: stale configuration can terminate a viable run. |
| G052 | re-home-to-TS | Without named requirements, at least one non-skipped check must run and all participating rows must settle. | `pre-#845 trident/inner-workflow.mjs:5586`; `pre-#845 trident/inner-workflow.mjs:5588` | `trident/gates/review-readiness.test.ts:33`; `trident/gates/review-readiness.test.ts:34` | Silent: zero checks can look green. |
| G053 | re-home-to-TS | Readiness waits have a 900000-ms budget and 30000-ms cadence; exhaustion defers without buying review rounds. | `pre-#845 trident/inner-workflow.mjs:4579`; `pre-#845 trident/inner-workflow.mjs:4628`; `pre-#845 trident/inner-workflow.mjs:5600` | `trident/gates/review-readiness.test.ts:39`; `trident/gates/review-readiness.test.ts:40`; `trident/gates/review-readiness.test.ts:41`; `trident/gates/review-readiness.test.ts:78` | Silent: absent checks can strand the run indefinitely. |
| G054 | re-home-to-TS | Both passed and failed settled checks may enter review; conflicting, unknown, and configuration-error states cannot spend review. | `pre-#845 trident/inner-workflow.mjs:5604`; `pre-#845 trident/inner-workflow.mjs:5609` | `trident/gates/review-readiness.test.ts:47`; `trident/gates/review-readiness.test.ts:50` | Silent: a rewrite may either skip required readiness or prevent review of repairable red CI. |
| G055 | re-home-to-TS | New red CI findings force rejection, while the same failing check measured on the base becomes advisory. | `pre-#845 trident/inner-workflow.mjs:5683`; `pre-#845 trident/inner-workflow.mjs:7343`; `pre-#845 trident/inner-workflow.mjs:7511` | `trident/gates/review-ci.test.ts:11`; `trident/gates/review-ci.test.ts:12`; `trident/gates/review-ci.test.ts:14`; `trident/gates/review-ci.test.ts:15`; `trident/build-run.test.ts:1183`; `trident/build-run.test.ts:1192` | Silent: review can approve newly failing CI or endlessly fix pre-existing failures. |
| G056 | re-home-to-TS | Pending or unreadable CI joins the deferred-peer gate rather than granting approval. | `pre-#845 trident/inner-workflow.mjs:5765`; `pre-#845 trident/inner-workflow.mjs:7546` | `trident/gates/review-ci.test.ts:33`; `trident/gates/review-ci.test.ts:34`; `trident/build-run.test.ts:1202`; `trident/build-run.test.ts:1204` | Silent: unavailable CI can be silently omitted. |
| G057 | re-home-to-TS | Enabled core review seats need usable verdicts; a missing seat contributes a blocker. | `pre-#845 trident/inner-workflow.mjs:6774`; `pre-#845 trident/inner-workflow.mjs:7368` | `trident/inner-workflow.test.ts:1451`; `trident/inner-workflow.test.ts:1480` | Silent: an incomplete panel can approve. |
| G058 | re-home-to-TS | A configured cross-model peer that defers prevents approval, whereas an unconnected or deliberately disabled peer is not required. | `pre-#845 trident/inner-workflow.mjs:6653`; `pre-#845 trident/inner-workflow.mjs:6947`; `pre-#845 trident/inner-workflow.mjs:3320` | `trident/inner-workflow.test.ts:994`; `trident/inner-workflow.test.ts:1284` | Silent: an unavailable required peer can be dropped. |
| G059 | re-home-to-TS | Seat retries are bounded and only retry deferred results, never usable verdicts; a rate-limit answer skips immediate retry. | `pre-#845 trident/inner-workflow.mjs:3369`; `pre-#845 trident/inner-workflow.mjs:3380`; `pre-#845 trident/inner-workflow.mjs:3395` | `trident/lane-retry.test.ts:238`; `trident/lane-retry.test.ts:561`; `trident/lane-retry.test.ts:672` | Silent: a failure can consume unbounded calls or overwrite a real verdict. |
| G060 | re-home-to-TS | Missing or unusable synthesis, including a thrown review round, becomes an infrastructure block rather than approval. | `pre-#845 trident/inner-workflow.mjs:3998`; `pre-#845 trident/inner-workflow.mjs:4040` | `trident/__tests__/synthesis-unavailable.test.ts:311`; `trident/__tests__/synthesis-unavailable.test.ts:656` | Silent: no final judgment can become approval. |
| G061 | re-home-to-TS | Only a nonempty set consisting entirely of nonblocking findings upgrades REQUEST_CHANGES; empty or unknown severity does not earn that upgrade. | `pre-#845 trident/inner-workflow.mjs:3076`; `pre-#845 trident/inner-workflow.mjs:3218` | `trident/inner-workflow.test.ts:2225`; `trident/inner-workflow.test.ts:2194` | Silent: a malformed rejection can be waived or minor findings can waste rounds. |
| G062 | re-home-to-TS | Model-supplied advisory and reserved lane/suite markers are stripped before gating. | `pre-#845 trident/inner-workflow.mjs:3108` | `trident/inner-workflow.test.ts:2362`; `trident/inner-workflow.test.ts:2442` | Silent: a reviewer can mark a blocker as exempt. |
| G063 | re-home-to-TS | When a test strategy is supplied, only a zero exit code observed by the host proves the full suite. A nonzero exit rejects — with one deliberate exception: a `failed-preexisting` claim that carries base-comparison evidence is recorded as an ADVISORY finding for the panel to verify, not a blocker, because a repo that is red at the base for reasons the diff did not cause should not be unmergeable forever; an evidence-free `failed-preexisting` claim is a blocker. **A worker-reported outcome never selects whether a receipt is required — the HOST's scope does (corrected #1153).** On a host-selected `subset` round no full suite is run by design, so an absent receipt is expected and does not block; a `failed-preexisting` claim there still obeys the evidence rules above. On a `full-suite` round an absent, unreadable or non-integer receipt remains unknown. This row previously said a worker-reported `deferred` never satisfies the gate and that every absent receipt stays unknown; both clauses described the `pre-#845` legacy and were already contradicted by the re-homed TS, which honoured a worker-reported `deferred` to return known. The correction removes the worker string from the decision rather than widening what the gate accepts: the receipt is still required on the round that gates merge. | `pre-#845 trident/inner-workflow.mjs:6408`; `pre-#845 trident/inner-workflow.mjs:6432`; `pre-#845 trident/inner-workflow.mjs:6477`; `pre-#845 trident/inner-workflow.mjs:6539` | `trident/gates/review-suite.test.ts:12`; `open/__tests__/project-build-wiring.test.ts:51` | Silent: a worker assertion can stand in for a suite the host never ran; an absent strategy currently bypasses this check. |
| G064 | superseded-by-G063-host-receipt | Worker-reported test booleans and outcomes do not authorize approval; the host-observed exit receipt is the verdict-bearing input. | `pre-#845 trident/inner-workflow.mjs:6410` | `trident/gates/review-suite.test.ts:30`; `open/__tests__/project-build-wiring.test.ts:51` | Silent: contradictory worker fields can distract from the host's measured outcome. |
| G065 | re-home-to-TS | A pre-existing-red suite claim needs nonempty comparison evidence to be advisory; an empty claim remains a blocker. | `pre-#845 trident/inner-workflow.mjs:6434` | `trident/inner-workflow-assembly.test.ts:1233`; `trident/inner-workflow-assembly.test.ts:1254` | Silent: an unsupported excuse can waive testing. |
| G066 | re-home-to-TS | An empty panel rejection padded only by advisory suite findings stops as infra-only without purchasing a Forge round. | `pre-#845 trident/inner-workflow.mjs:6543`; `pre-#845 trident/inner-workflow.mjs:6573` | `trident/inner-workflow.test.ts:2282` | Silent: the loop can spend rounds on no actionable finding. |
| G067 | re-home-to-TS | A completed panel with only advisory findings does not buy code fix rounds; infra-only blocks also stop the fix loop. | `pre-#845 trident/inner-workflow.mjs:3437`; `pre-#845 trident/inner-workflow.mjs:8737`; `pre-#845 trident/inner-workflow.mjs:8738` | `trident/review-round-cap.test.ts:201` | Silent: infrastructure and advisory holds can consume the entire code budget. |

### Escalation and budgets

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G068 | re-home-to-TS | Self-declared escalations must use design-gap or missing-dependency and supply concrete nonempty whatIsMissing text. | `pre-#845 trident/inner-workflow.mjs:3681`; `pre-#845 trident/inner-workflow.mjs:3721`; `pre-#845 trident/inner-workflow.mjs:3725` | `trident/__tests__/escalation-gate.test.ts:486`; `trident/__tests__/escalation-gate.test.ts:500` | Silent: a bare complaint can terminate a build. |
| G069 | re-home-to-TS | APPROVE plus any escalation payload cannot approve and cannot authorize that declaration. | `pre-#845 trident/inner-workflow.mjs:3717`; `pre-#845 trident/inner-workflow.mjs:3744`; `pre-#845 trident/inner-workflow.mjs:7599` | `trident/__tests__/escalation-gate.test.ts:113` | Silent: a contradictory reply can authorize an irreversible merge. |
| G070 | re-home-to-TS | A repeated actionable finding across fix rounds triggers a stop independently of reviewer self-declaration. | `pre-#845 trident/inner-workflow.mjs:3614`; `pre-#845 trident/inner-workflow.mjs:3787`; `pre-#845 trident/inner-workflow.mjs:3829` | `trident/__tests__/escalation-gate.test.ts:388`; `trident/__tests__/escalation-gate.test.ts:525` | Silent: the same defect can consume repeated rounds. |
| G071 | re-home-to-TS | A blocker/major count that fails to decrease across two readable code rounds triggers no-progress escalation. | `pre-#845 trident/inner-workflow.mjs:3659`; `pre-#845 trident/inner-workflow.mjs:3788` | `trident/__tests__/escalation-gate.test.ts:432`; `trident/__tests__/escalation-gate.test.ts:447` | Silent: nonconverging work can spend the rest of its budget. |
| G072 | re-home-to-TS | Missing finding identity or unreadable counts remain undecidable, not evidence of repetition or progress. | `pre-#845 trident/inner-workflow.mjs:3567`; `pre-#845 trident/inner-workflow.mjs:3623`; `pre-#845 trident/inner-workflow.mjs:3660` | `trident/__tests__/escalation-gate.test.ts:406`; `trident/__tests__/escalation-gate.test.ts:456` | Silent: invented arithmetic can stop a healthy run. |
| G073 | re-home-to-TS | One valid design gap may purchase one bounded re-plan; a later design gap or post-re-plan trigger stops. | `pre-#845 trident/inner-workflow.mjs:3817`; `pre-#845 trident/inner-workflow.mjs:8676` | `trident/__tests__/escalation-gate.test.ts:569`; `trident/__tests__/escalation-gate.test.ts:584` | Silent: repeated replanning can evade escalation. |
| G074 | re-home-to-TS | A valid missing dependency stops immediately rather than purchasing a re-plan. | `pre-#845 trident/inner-workflow.mjs:3823`; `pre-#845 trident/inner-workflow.mjs:3829` | `trident/__tests__/escalation-gate.test.ts:592` | Silent: the loop keeps building without a prerequisite. |
| G075 | re-home-to-TS | A failed bounded re-plan escalates instead of silently rebuilding against the old plan. | `pre-#845 trident/inner-workflow.mjs:8802` | `trident/inner-workflow-gates.test.ts:179` | Silent: an invalid plan can survive its attempted replacement. |
| G076 | re-home-to-TS | The review loop requires remaining round budget and increments by exactly one; the configured cap is threaded from the run row. | `trident/inner-loop.ts:602`; `pre-#845 trident/inner-workflow.mjs:8734`; `pre-#845 trident/inner-workflow.mjs:8740` | `trident/review-round-cap.test.ts:93`; `trident/review-round-cap.test.ts:109` | Silent: a rewrite can lose the only general fix ceiling. |
| G077 | re-home-to-TS | A pending re-plan at the final allowed round becomes a design-gap stop. | `pre-#845 trident/inner-workflow.mjs:9034` | `trident/__tests__/escalation-e2e.test.ts:408`; `trident/__tests__/escalation-e2e.test.ts:435` | Silent: a known design gap can be lost at budget exhaustion. |
| G078 | re-home-to-TS | An escalation stops the loop and overrides a severity-upgraded APPROVE in the terminal result. | `pre-#845 trident/inner-workflow.mjs:8733`; `pre-#845 trident/inner-workflow.mjs:9061` | `trident/__tests__/escalation-e2e.test.ts:654` | Silent: a stopped run can be merged as approved. |
| G079 | keep-in-place | Ralph re-fire stops when the next durable task round exceeds max_ralph_rounds. | `trident/orchestrator.ts:4879` | `trident/orchestrator.test.ts:4347`; `trident/orchestrator.test.ts:4374` | Silent: task continuation can run forever. |
| G080 | keep-in-place | Infrastructure retry requires a measured qualifying failure; APPROVE and genuine code review failures are not retryable infrastructure. | `trident/infrastructure-retry.ts:46`; `trident/infrastructure-retry.ts:51`; `trident/infrastructure-retry.ts:61` | `trident/infra-retry.test.ts:178` | Silent: code rejection can be reset into a fresh attempt. |
| G081 | keep-in-place | Infrastructure retries have a durable retry ceiling and a separate launch backoff. | `trident/infrastructure-retry.ts:11`; `trident/infrastructure-retry.ts:93`; `trident/infrastructure-retry.ts:120` | `trident/infra-retry.test.ts:206`; `trident/infra-retry.test.ts:126` | Silent: infrastructure can create an endless retry loop. |
| G082 | keep-in-place | A lost atomic retry claim does not launch another workflow. | `trident/infrastructure-retry.ts:114`; `trident/infrastructure-retry.ts:116` | `trident/infra-retry.test.ts:306`; `trident/store.test.ts:1148` | Silent: concurrent ticks can create duplicate work. |

### Publication and replay

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G083 | keep-in-place | Outer publication requires PR mode and a locally resolved full branch head. | `trident/publication.ts:98`; `trident/publication.ts:106` | `trident/orchestrator.test.ts:391`; `trident/orchestrator.test.ts:396`; `trident/orchestrator.test.ts:398` | Loud: later git/PR operations would fail for missing inputs. |
| G084 | keep-in-place | A fix-round reviewed-head pin must be full width and the produced head must descend from it before publishing. | `trident/publication.ts:130`; `trident/publication.ts:131` | `trident/orchestrator.test.ts:1397`; `trident/orchestrator.test.ts:1408`; `trident/orchestrator.test.ts:1416` | Silent: a fix can abandon reviewed work. |
| G085 | keep-in-place | Publication requires a readable remote branch state before choosing its lease. | `trident/publication.ts:146` | `trident/orchestrator.test.ts:472`; `trident/orchestrator.test.ts:474`; `trident/orchestrator.test.ts:480` | Silent: an unknown branch can be treated as absent. |
| G086 | keep-in-place | First publication with a base pin requires the branch to contain that pinned launch base. | `trident/publication.ts:153`; `trident/publication.ts:158` | `trident/orchestrator.test.ts:2860` | Silent: work built on another lane can be published. |
| G087 | keep-in-place | Replay requires a readable remote base, available base object, and a provable fork point rather than a stale local-base fallback. | `trident/replay.ts:195`; `trident/replay.ts:238`; `trident/replay.ts:286` | `trident/orchestrator.test.ts:1819` | Silent: replay can import unrelated commits. |
| G088 | keep-in-place | Replay requires readable depth and successful unshallowing when needed before ancestry decisions. | `trident/replay.ts:118`; `trident/replay.ts:122`; `trident/replay.ts:142` | `trident/publish-rebase-realgit.test.ts:249` | Silent: shallow history can produce a wrong replay boundary. |
| G089 | keep-in-place | Replay refuses an unreadable or empty patch and an unavailable scratch worktree. | `trident/replay.ts:335`; `trident/replay.ts:353`; `trident/replay.ts:355`; `trident/replay.ts:376` | `trident/publish-rebase-realgit.test.ts:283`; `trident/publish-rebase-realgit.test.ts:284`; `trident/publish-rebase-realgit.test.ts:286`; `trident/publish-rebase-realgit.test.ts:287` | Loud: apply/commit generally fail without these artifacts. |
| G090 | keep-in-place | A failed apply with no unmerged paths is a wholesale failure, not permission to invoke the conflict resolver. | `trident/replay.ts:473` | `trident/orchestrator.test.ts:2687` | Silent: a resolver can fabricate work for the wrong patch. |
| G091 | keep-in-place | Replay conflicts require a configured resolver that actually reports resolution; otherwise they raise the conflict attention outcome. | `trident/replay.ts:484`; `trident/replay.ts:504` | `trident/orchestrator.test.ts:1950`; `trident/orchestrator.test.ts:2108` | Loud: unresolved git state normally prevents commit. |
| G092 | keep-in-place | A claimed resolution must be checked against readable unmerged state and staged content, including residual conflict markers. | `trident/replay.ts:410`; `trident/replay.ts:455`; `trident/replay.ts:506` | `trident/orchestrator.test.ts:2198`; `trident/orchestrator.test.ts:2259`; `trident/orchestrator.test.ts:2469` | Silent: a staged file containing conflict residue can commit successfully. |
| G093 | keep-in-place | Each replay resolution round must shrink the remaining conflict set. | `trident/replay.ts:524` | `trident/orchestrator.test.ts:2150`; `trident/orchestrator.test.ts:2516` | Silent: a resolver can spend repeated calls making no progress. |
| G094 | keep-in-place | Replay conflict resolution cannot exceed MAX_CONFLICT_ROUNDS (12). | `trident/replay.ts:492`; `trident/merge.ts:311` | `trident/publish-rebase-realgit.test.ts:307`; `trident/publish-rebase-realgit.test.ts:310`; `trident/publish-rebase-realgit.test.ts:313` | Silent: partially progressing resolution can become unbounded. |
| G095 | keep-in-place | Replay must read the original commit message before composing its replay commit. | `trident/replay.ts:547` | `trident/publish-rebase-realgit.test.ts:397` | Silent: metadata can replace the original subject and provenance. |
| G096 | keep-in-place | A resolution that leaves nothing to commit is a conflict attention outcome; other commit failures also stop publication. | `trident/replay.ts:572`; `trident/replay.ts:587`; `trident/replay.ts:589` | `trident/orchestrator.test.ts:2610`; `trident/orchestrator.test.ts:2656` | Loud: git rejects the commit, but taxonomy can still be lost. |
| G097 | keep-in-place | Replay advances the branch with compare-and-swap against the old head. | `trident/replay.ts:600` | `trident/publish-rebase-realgit.test.ts:344`; `trident/publish-rebase-realgit.test.ts:346`; `trident/publish-rebase-realgit.test.ts:347` | Silent: another writer's branch advance can be overwritten. |
| G098 | keep-in-place | Push uses the observed remote OID as an explicit lease, including an empty lease for first publication. | `trident/publication.ts:307`; `trident/publication.ts:312` | `trident/orchestrator.test.ts:1487`; `trident/orchestrator.test.ts:1536`; `trident/orchestrator.test.ts:1575` | Silent: a concurrent remote advance can be overwritten. |
| G099 | keep-in-place | Successful push must be followed by an exact remote witness for the head to publish. | `trident/publication.ts:324` | `trident/orchestrator.test.ts:975` | Silent: an unconfirmed push can unlock review. |
| G100 | keep-in-place | A real builder-claim/head disagreement refuses PR creation and review after preserving the branch on origin; an unresolvable claim is absent. The preservation push (`checkBuildClaim`) is unconditional on the claim conflict: the G166 session-trailer scan runs before it and is named in the refusal, but never withholds it (owner decision 2026-09-19). | `trident/publication.ts:54`; `trident/publication.ts:336`; `trident/gates/build-claim.ts:34`; `trident/gates/build-claim.ts:67` | `trident/orchestrator.test.ts:1494`; `trident/orchestrator.test.ts:1525`; `trident/orchestrator.test.ts:1555`; `trident/gates/build-claim.test.ts:33`; `trident/gates/build-claim-realgit.test.ts:96`; `trident/gates/build-claim-realgit.test.ts:114`; `trident/build-host.test.ts:778` | Silent: review can judge the wrong commit or good work can be stranded. |
| G101 | keep-in-place | PR creation must succeed and an open PR must be confirmed before review handoff. | `trident/publication.ts:348`; `trident/publication.ts:351` | `trident/orchestrator.test.ts:1909` | Loud: the review-readiness probe otherwise cannot find a PR. |
| G102 | keep-in-place | Review dispatch requires a readable nonempty base-to-head change and a successfully materialized diff artifact. | `trident/publication.ts:472`; `trident/publication.ts:612`; `trident/publication.ts:636` | `trident/orchestrator.test.ts:2732` | Silent: a panel can approve an empty or stale artifact. |
| G103 | keep-in-place | Diff reordering is allowed only with a valid seen pin and a manageable unquoted, unpadded path list; uncertainty uses the full unordered diff. | `trident/publication.ts:547`; `trident/publication.ts:571`; `trident/publication.ts:599` | `trident/orchestrator.test.ts:836`; `trident/orchestrator.test.ts:882`; `trident/orchestrator.test.ts:928` | Silent: an optimization can drop files from review. |

### Harvest, merge, recovery, and preservation

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G104 | keep-in-place | A result claiming APPROVE must have the row's own argus-approved checkpoint before the outer merge path runs. | `trident/merge-approval.ts:37`; `trident/merge-approval.ts:242` | `trident/orchestrator.test.ts:4335` | Silent: self-asserted approval can merge unreviewed work. |
| G105 | keep-in-place | Post-approval mutation proof must succeed at the reviewed head or earn an explicit exemption before merge. | `trident/merge-approval.ts:106`; `trident/merge-approval.ts:148` | `trident/orchestrator.test.ts:1234`; `trident/mutation-prover.test.ts:5145`; `trident/mutation-prover.test.ts:5390` | Silent: a convincing claim can replace executable evidence. |
| G106 | keep-in-place | A missing inline mutation nomination may be read only from the reviewed commit; malformed or absent artifact input does not become a pass. | `trident/merge-approval.ts:97` | `trident/orchestrator.test.ts:3585`; `trident/mutation-claim-artifact.test.ts:151`; `trident/mutation-claim-artifact.test.ts:181`; `trident/mutation-claim-artifact.test.ts:391` | Silent: an unrelated or uncommitted nomination can authorize merge. |
| G107 | keep-in-place | PR merge requires a PR number and a full reviewed-head pin, and the remote merge command pins that reviewed commit. | `trident/merge.ts:1755`; `trident/merge.ts:1768` | `trident/merge.test.ts:153`; `trident/merge.test.ts:185`; `trident/merge.test.ts:196` | Silent: code changed after review can merge. |
| G108 | keep-in-place | PR drift assessment requires the PR's actual base, a same-repository head, and a resolvable branch; unreadable or material overlapping drift holds. | `trident/merge.ts:1805`; `trident/merge.ts:1822`; `trident/merge.ts:1844`; `trident/merge.ts:1920` | `trident/merge.test.ts:1692`; `trident/merge.test.ts:1717`; `trident/orchestrator.test.ts:3895` | Silent: changed base semantics can bypass review. |
| G109 | keep-in-place | Local merge requires a branch, isolated reusable worktree, and acceptable base drift before landing. | `trident/merge.ts:1644`; `trident/merge.ts:1969`; `trident/merge.ts:2053` | `trident/merge.test.ts:1180` | Silent: unreviewed interaction with the base can land (drift pin). |
| G110 | keep-in-place | Dirty or unverifiable merge worktrees are preserved and cannot be silently reused or force-removed. | `trident/merge.ts:1547`; `trident/merge.ts:1644`; `trident/merge.ts:1713` | `trident/merge.test.ts:2088`; `trident/merge.test.ts:2132`; `trident/merge.test.ts:2155` | Silent: cleanup can destroy uncommitted work. |
| G111 | keep-in-place | A terminal run is a no-op, and a harvestable result is handled before crash/orphan recovery. | `trident/recovery-liveness.ts:81`; `trident/recovery-liveness.ts:137` | `trident/orchestrator.test.ts:414`; `trident/orchestrator.test.ts:417`; `trident/orchestrator.test.ts:432`; `trident/orchestrator.test.ts:435` | Silent: a finished run can relaunch or a valid result can be replaced by failure. |
| G112 | keep-in-place | Crash recovery first recognizes an already merged PR and then requires a winning recovery claim. | `trident/recovery-liveness.ts:177`; `trident/recovery-liveness.ts:179`; `trident/recovery-liveness.ts:261` | `trident/crash-recovery.test.ts:336` | Silent: landed work can be rebuilt or two recoveries can launch. |
| G113 | keep-in-place | Positive run-process activity defers crash relaunch only within the inflight ceiling. | `trident/recovery-liveness.ts:213`; `trident/recovery-liveness.ts:225` | `trident/orchestrator.test.ts:5425`; `trident/orchestrator.test.ts:5492` | Silent: a live detached build can be duplicated. |
| G114 | keep-in-place | Crash recovery has a durable maximum and repeated launch throws have a separate bounded fault counter. | `trident/recovery-liveness.ts:237`; `trident/recovery-liveness.ts:279`; `trident/recovery-liveness.ts:609` | `trident/crash-recovery.test.ts:167`; `trident/crash-recovery.test.ts:227`; `trident/launch-throw-bounded.test.ts:96` | Silent: recovery can retry forever after every fault. |
| G115 | keep-in-place | Completed, failed, or crashed subagents with no parseable terminal result are reaped rather than left running. | `trident/recovery-liveness.ts:310` | `trident/orchestrator.test.ts:6968`; `trident/orchestrator.test.ts:6991` | Silent: an unharvestable run can retain its slot forever. |
| G116 | deleted-with-the-mechanism | An unconfirmed fire is tied to its dispatch and only a late settle or post-dispatch stage evidence can confirm it. | `trident/orchestrator.ts:4626`; `trident/orchestrator.ts:4643`; `trident/orchestrator.ts:4669` | `trident/orchestrator.test.ts:5042`; `trident/orchestrator.test.ts:5082`; `trident/orchestrator.test.ts:5372` | Silent: a stale launch can confirm or kill its replacement. |
| G117 | deleted-with-the-mechanism | At the second fire deadline, positive launched/published evidence is checked before failing and cancelling the unconfirmed launcher. | `trident/orchestrator.ts:4724`; `trident/orchestrator.ts:4732`; `trident/orchestrator.ts:4743` | `trident/orchestrator.test.ts:5213`; `trident/orchestrator.test.ts:5255` | Silent: a live or published build can be falsely killed. |
| G118 | keep-in-place | The no-advance watchdog reaps suspected hangs or measured death unless sufficiently fresh run evidence warrants standing down. | `trident/recovery-liveness.ts:339`; `trident/recovery-liveness.ts:473`; `trident/recovery-liveness.ts:512` | `trident/orchestrator.test.ts:5708`; `trident/orchestrator.test.ts:6334` | Silent: hangs can retain slots or live work can be killed. |
| G119 | deleted-with-the-mechanism | Only run-scoped liveness may restamp progress; a live shared launcher alone cannot renew the run's clock. | `trident/orchestrator.ts:4824` | `trident/orchestrator.test.ts:5873` | Silent: a healthy shared process can make a dead run immortal. |
| G120 | keep-in-place | Unknown run evidence defers a suspected-hang kill, but cannot override positive launcher death or the inflight ceiling. | `trident/recovery-liveness.ts:489` | `trident/orchestrator.test.ts:6369`; `trident/orchestrator.test.ts:6436` | Silent: a blind probe can authorize a kill or hold forever. |
| G121 | keep-in-place | Orphan handling obeys fail/wait/redispatch policy and allows only one redispatch per process. | `trident/recovery-liveness.ts:526`; `trident/recovery-liveness.ts:542`; `trident/recovery-liveness.ts:579` | `trident/orchestrator.test.ts:6510`; `trident/orchestrator.test.ts:6633`; `trident/orchestrator.test.ts:6645` | Silent: restart can create repeated duplicate launches. |
| G122 | keep-in-place | A live branch-holder prevents an orphan redispatch. | `trident/recovery-liveness.ts:551`; `trident/recovery-liveness.ts:563` | `trident/orchestrator.test.ts:6570` | Silent: two processes can build the same branch. |
| G123 | keep-in-place | The inflight ceiling terminates a run even when ordinary liveness reprieves would keep it alive. | `trident/recovery-liveness.ts:473`; `trident/recovery-liveness.ts:633` | `trident/orchestrator.test.ts:5492` | Silent: a run can hold resources indefinitely. |
| G124 | re-home-to-TS | A thrown workflow writes a typed failure with no review verdict and no remaining tasks; failure to write falls back to the outer stall guard. | `pre-#845 trident/inner-workflow.mjs:9215`; `pre-#845 trident/inner-workflow.mjs:9245`; `trident/recovery-liveness.ts:633` | `trident/inner-workflow.test.ts:1847` | Silent: a crashed workflow can remain pending or re-fire as successful progress. |
| G125 | re-home-to-TS | Cleanup runs in finally independently of the builder result and delegates deletion decisions to the deterministic cleanup script. | `pre-#845 trident/inner-workflow.mjs:9250`; `pre-#845 trident/inner-workflow.mjs:9279` | `trident/inner-workflow.test.ts:1638`; `trident/inner-workflow.test.ts:1664` | Silent: a crashed builder can strand or destroy its only work. |
| G126 | re-home-to-TS | Cleanup preserves dirty/unverifiable trees and retains local-mode branches; PR branch deletion needs a matching remote copy. | `pre-#845 trident/inner-workflow.mjs:9278` | `trident/worktree-cleanup-sh.test.ts:126`; `trident/worktree-cleanup-sh.test.ts:200`; `trident/worktree-cleanup-sh.test.ts:589` | Silent: unmerged commits or untracked work can be deleted (delegated script pin). |
| G127 | re-home-to-TS | Cleanup reporting cannot treat missing exit evidence as success or confuse preserved output with successful removal. | `pre-#845 trident/inner-workflow.mjs:4498`; `pre-#845 trident/inner-workflow.mjs:9300` | `trident/inner-workflow.test.ts:1704`; `trident/inner-workflow.test.ts:1769` | Silent: preserved or uninspected work can disappear from the operator's view. |
| G128 | keep-in-place | Failure salvage inspects only an attributable run worktree, excluding shared checkout, mismatched recorded path, and ambiguous candidates. | `trident/failure-salvage.ts:282`; `trident/failure-salvage.ts:295`; `trident/failure-salvage.ts:296`; `trident/failure-salvage.ts:320` | `trident/stranded-salvage-realgit.test.ts:528`; `trident/stranded-salvage-realgit.test.ts:550` | Silent: salvage can snapshot another lane's edits (live-owner pin only). |
| G129 | keep-in-place | A dirty-work snapshot requires successful git staging/tree/commit/ref operations before it is reported recoverable. | `trident/failure-salvage.ts:164`; `trident/failure-salvage.ts:167`; `trident/failure-salvage.ts:170`; `trident/failure-salvage.ts:196`; `trident/failure-salvage.ts:209` | `trident/stranded-salvage-realgit.test.ts:590`; `trident/stranded-salvage-realgit.test.ts:622` | Silent: a false preservation receipt can conceal data loss. |
| G130 | keep-in-place | Board reconciliation may only detach this run into its own terminal lane; escalation payloads cannot reorder the queue. | `trident/board-reconcile.ts:49`; `trident/board-reconcile.ts:106`; `trident/board-reconcile.ts:122` | `trident/escalation-block.test.ts:537`; `trident/escalation-block.test.ts:579` | Silent: model output can reprioritize unrelated work. |
| G131 | keep-in-place | Blocked-board routing requires failed phase, a harvest marker, and a coherent kind/payload pair. | `trident/escalation-block.ts:97`; `trident/escalation-block.ts:100`; `trident/escalation-block.ts:105` | `trident/escalation-block.test.ts:130`; `trident/escalation-block.test.ts:136`; `trident/escalation-block.test.ts:143` | Silent: stale or partial results can relabel unrelated failures as blocked. |

### Prompt requirements and advisory boundaries retained for review

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G132 | re-home-to-TS | Workers are instructed never to ask interactive questions and to abort with a reason instead; this is a prompt requirement, not an independent refusal in this file. | `pre-#845 trident/inner-workflow.mjs:1350`; `trident/inner-loop.ts:787` | **NO TEST** | Silent: a worker can hang waiting for input (prompt pin only). |
| G133 | re-home-to-TS | Workers are instructed to redirect verbose output rather than exhaust their context. | `pre-#845 trident/inner-workflow.mjs:1353` | **NO TEST** | Silent: useful task context can be lost; included as a resource precondition. |
| G134 | re-home-to-TS | Workers are instructed never to kill by pattern and to kill only processes they started and can identify. | `pre-#845 trident/inner-workflow.mjs:1377`; `pre-#845 trident/inner-workflow.mjs:9289` | `trident/inner-workflow-assembly.test.ts:882` | Silent: a worker can terminate sibling lanes; prompt-only restriction. |
| G135 | re-home-to-TS | Every direct commit in a clone with the repository hooks installed refuses when HEAD does not resolve; Forge is also instructed to use the diagnostic wrapper. The wrapper also removes every `Claude-Session:` line (matched by a bash-3.2 bracket pattern, ASCII case-insensitive) from the commit it just created and leaves Co-Authored-By intact (#1133). It acts on the REF it committed on and the OBJECT it created, never on whatever HEAD names afterwards: before the commit it captures the ref (`git symbolic-ref -q HEAD`, or the literal `HEAD` when detached; exit 77 when neither can be determined) and the parent the new commit must have (the probed HEAD, or for an `--amend` that commit's own first parent); after the commit it re-reads that ref (exit 69 failed / 70 named no object, no rewrite, stderr naming the ref and the commit as carrying the trailer if the message had one), reads the commit back as the raw object (`git cat-file commit`; an initial read failure refuses without withdrawing because provenance is unknown) and requires its first parent to be the expected one -- any other first parent is another writer's commit and is refused with exit 76, nothing rewritten and nothing withdrawn. The stripped commit is then BUILT, not amended: the message is filtered byte-for-byte under the C locale (the stored bytes are the first commit's message minus the removed line and, when that line was a paragraph of its own, the one empty line that separated it; a message empty without the trailer is refused as git would, exit 1, unless `--allow-empty-message` was given) and `git commit-tree` re-makes the commit from the object's own tree, parents, author, committer (raw dates, no committer-date bump) and encoding, signed if and only if the object carries a `gpgsig` header (key from the argv scan -- `-S<key>`, `--gpg-sign=<key>`, a signing letter inside a boolean short-flag cluster such as `-aS` / `-sSkey`; option values, attached or as the next element of a `-am`-style cluster, are never read as flags -- else the configured default key); ONE argv scan, run before the commit, decides `--amend`, the signing key and `--allow-empty-message` by the same value-skipping rules, so a `-m` paragraph that is literally `--amend` is a message, not an amend; a header commit-tree cannot carry (`mergetag`, or an unknown one) is refused with exit 75. No hook runs on the rebuild, so a `prepare-commit-msg` hook cannot put the trailer back and a `post-commit` hook cannot land a follow-up on the rewrite. The candidate is verified as an OBJECT before any ref names it (read back raw, the same filter must remove nothing, tree and parents must equal the first commit's; exit 73 otherwise) and published by compare-and-swap in a prepared `git update-ref --stdin` transaction with `no-deref`, checking that the captured ref is direct while its lock is held: a lost swap is exit 74, nothing rewritten, all three objects named. Every withdrawal uses that same locked ref-type check and compare-and-swap against the captured ref (probed HEAD replacing the proven commit, index kept), never a blind reset, so a commit another writer landed on top is refused and reported as the commit remaining, and a branch switch after the commit cannot redirect a withdrawal onto another branch. It prints the final HEAD in full because the pre-strip commit was replaced and any `[branch sha]` summary line naming it is stale; every refusal names the state a withdrawal cannot restore (an in-progress merge, cherry-pick or revert that the first commit concluded is gone). Exit 0 is reached only with no trailer in a provenance-checked commit, or after the swap moved the captured ref from that exact commit to a candidate verified clean. | `.githooks/pre-commit:4`; `pre-#845 trident/inner-workflow.mjs:1686`; `trident/commit-with-resolved-head.sh:19`; `trident/commit-with-resolved-head.sh:108`; `trident/commit-with-resolved-head.sh:131`; `trident/commit-with-resolved-head.sh:228`; `trident/commit-with-resolved-head.sh:248`; `trident/commit-with-resolved-head.sh:267`; `trident/commit-with-resolved-head.sh:355`; `trident/commit-with-resolved-head.sh:374`; `trident/commit-with-resolved-head.sh:501` | `trident/commit-with-resolved-head-realgit.test.ts:66`; `trident/commit-with-resolved-head-realgit.test.ts:78`; `trident/commit-with-resolved-head-realgit.test.ts:119`; `trident/commit-with-resolved-head-realgit.test.ts:141`; `trident/commit-with-resolved-head-realgit.test.ts:210`; `trident/commit-with-resolved-head-realgit.test.ts:241`; `trident/commit-with-resolved-head-realgit.test.ts:259`; `trident/commit-with-resolved-head-realgit.test.ts:422`; `trident/commit-with-resolved-head-realgit.test.ts:445`; `trident/commit-with-resolved-head-realgit.test.ts:473`; `trident/commit-with-resolved-head-realgit.test.ts:483`; `trident/commit-with-resolved-head-realgit.test.ts:512`; `trident/commit-with-resolved-head-realgit.test.ts:536`; `trident/commit-with-resolved-head-realgit.test.ts:556`; `trident/commit-with-resolved-head-realgit.test.ts:577`; `trident/commit-with-resolved-head-realgit.test.ts:610`; `trident/commit-with-resolved-head-realgit.test.ts:624`; `trident/commit-with-resolved-head-realgit.test.ts:667`; `trident/commit-with-resolved-head-realgit.test.ts:696`; `trident/commit-with-resolved-head-realgit.test.ts:777`; `trident/commit-with-resolved-head-realgit.test.ts:792`; `trident/commit-with-resolved-head-realgit.test.ts:817`; `trident/commit-with-resolved-head-realgit.test.ts:838`; `trident/commit-with-resolved-head-realgit.test.ts:853`; `trident/commit-with-resolved-head-realgit.test.ts:872`; `trident/commit-with-resolved-head-realgit.test.ts:889`; `trident/commit-with-resolved-head-realgit.test.ts:919`; `trident/commit-with-resolved-head-realgit.test.ts:944`; `trident/commit-with-resolved-head-realgit.test.ts:977`; `trident/commit-with-resolved-head-realgit.test.ts:999`; `trident/commit-with-resolved-head-realgit.test.ts:1037`; `trident/commit-with-resolved-head-realgit.test.ts:1082`; `trident/commit-with-resolved-head-realgit.test.ts:1099`; `trident/commit-with-resolved-head-realgit.test.ts:1117`; `trident/commit-with-resolved-head-realgit.test.ts:1140`; `trident/commit-with-resolved-head-realgit.test.ts:1164`; `trident/commit-with-resolved-head-realgit.test.ts:1186`; `trident/commit-with-resolved-head-realgit.test.ts:1203`; `trident/commit-with-resolved-head-realgit.test.ts:1226`; `trident/commit-with-resolved-head-realgit.test.ts:1255`; `trident/commit-with-resolved-head-realgit.test.ts:1270`; `trident/inner-workflow-gates.test.ts:126`; `trident/commit-with-resolved-head-realgit.test.ts:1295`; `trident/commit-with-resolved-head-realgit.test.ts:1311`; `trident/commit-with-resolved-head-realgit.test.ts:1351`; `trident/commit-with-resolved-head-realgit.test.ts:1385`; `trident/commit-with-resolved-head-realgit.test.ts:1406`; `trident/commit-with-resolved-head-realgit.test.ts:1436`; `trident/commit-with-resolved-head-realgit.test.ts:1454` | Loud: git refuses before creating the commit; without installed repository hooks, only the wrapper instruction remains. |
| G136 | re-home-to-TS | Forge must stop after committing; member builds cannot publish and PR publication belongs to the outer loop. | `pre-#845 trident/inner-workflow.mjs:1687`; `pre-#845 trident/inner-workflow.mjs:1690` | `trident/inner-workflow.test.ts:590` | Silent: a worker can publish before the outer checks; prompt-only prohibition. |
| G137 | re-home-to-TS | Reflection guidance is restricted to builder context and excluded from independent reviewers and verdict synthesis. | `trident/inner-loop.ts:667`; `pre-#845 trident/inner-workflow.mjs:7403` | `trident/inner-workflow.test.ts:1954`; `trident/inner-workflow.test.ts:1964` | Silent: mutable guidance can influence the review standard; included as a trust boundary. |
| G138 | re-home-to-TS | The Argus rubric requires evidence-backed minority-veto review; its prose requirements remain distinct from deterministic severity arithmetic. | `pre-#845 trident/inner-workflow.mjs:2438`; `pre-#845 trident/inner-workflow.mjs:7429` | `trident/inner-workflow.test.ts:421` | Silent: losing the prompt can weaken what a valid review means. |
| G139 | keep-in-place | Leak preflight must run before opening the PR, but unresolved findings or gate errors are logged and publication continues for CI enforcement. | `trident/publication.ts:212`; `trident/publication.ts:228`; `trident/publication.ts:346` | `trident/orchestrator.test.ts:9895`; `trident/orchestrator.test.ts:9938` | Silent: losing preflight loses early detection; it is NOT a publish veto. |
| G140 | re-home-to-TS | A panel configured entirely within one model family emits a warning but accepts the configuration. | `pre-#845 trident/inner-workflow.mjs:7278` | `trident/gates/review-panel.test.ts:129`; `trident/gates/review-panel.test.ts:130`; `trident/gates/review-panel.test.ts:131` | Silent: losing the warning hides reduced independence; NOT a refusal. |

### Delegated proof and cleanup subgates

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G141 | keep-in-place | Mutation proof requires a readable nonempty diff and a nomination within that diff unless the measured change qualifies for the prose/no-legal-target exemptions. | `trident/mutation-prover.ts:4539`; `trident/mutation-prover.ts:4591`; `trident/mutation-prover.ts:4625`; `trident/mutation-prover.ts:4634`; `trident/mutation-prover.ts:4642` | `trident/mutation-prover.test.ts:5376`; `trident/mutation-prover.test.ts:5761`; `trident/mutation-prover.test.ts:5772` | Silent: a missing claim or unreadable diff can become an exemption. |
| G142 | keep-in-place | Mutation proof validates the branch and requires the pinned proof head to equal the caller's expected reviewed head. | `trident/mutation-prover.ts:4478`; `trident/mutation-prover.ts:4510`; `trident/mutation-prover.ts:4524` | `trident/mutation-prover.test.ts:6069`; `trident/mutation-prover.test.ts:5390` | Silent: proof can refer to a different branch or commit. |
| G143 | keep-in-place | Mutation target and test argv must meet the prover's path, runner, separate-test, and distinct guard/control rules before execution. | `trident/mutation-prover.ts:2921`; `trident/mutation-prover.ts:646`; `trident/mutation-prover.ts:654` | `trident/mutation-prover.test.ts:1543`; `trident/mutation-prover.test.ts:1737`; `trident/mutation-prover.test.ts:1765`; `trident/mutation-prover.test.ts:5267` | Silent: tautological or redirected commands can manufacture evidence; detailed parser subcases stay owned by the prover tests. |
| G144 | keep-in-place | Python module runners must not be shadowed by the branch's modules, pytest configuration, or importable packages. | `trident/mutation-prover.ts:3222`; `trident/mutation-prover.ts:3235`; `trident/mutation-prover.ts:3243`; `trident/mutation-prover.ts:3255` | `trident/mutation-prover.test.ts:638`; `trident/mutation-prover.test.ts:708`; `trident/mutation-prover.test.ts:739`; `trident/mutation-prover.test.ts:812` | Silent: branch code can impersonate the installed runner. |
| G145 | keep-in-place | Runner load hooks and resolution-map values must not be branch-authored, and inherited preloads must not target changed files. | `trident/mutation-prover.ts:3320`; `trident/mutation-prover.ts:3333` | `trident/mutation-prover.test.ts:930`; `trident/mutation-prover.test.ts:1029`; `trident/mutation-prover.test.ts:1192` | Silent: unrelated tests can be made to execute the mutated file before assertions. |
| G146 | keep-in-place | The mutation must change the target bytes. | `trident/mutation-prover.ts:3399`; `trident/mutation-prover.ts:3576` | `trident/mutation-prover.test.ts:1528` | Silent: unchanged input can be reported as a mutation experiment. |
| G147 | keep-in-place | The mutated guard must exit red without timing out. | `trident/mutation-prover.ts:3579`; `trident/mutation-prover.ts:3582` | `trident/mutation-prover.test.ts:282`; `trident/mutation-prover.test.ts:312` | Silent: a timeout or passing guard can be sold as mutation coverage. |
| G148 | keep-in-place | The control must stay green under the mutation without timing out. | `trident/mutation-prover.ts:3596` | `trident/mutation-prover.test.ts:291` | Silent: a broad crash can be sold as a specific behavioral test. |
| G149 | keep-in-place | Restoration must restore the exact original bytes and the guard must return green without timing out. | `trident/mutation-prover.ts:3602`; `trident/mutation-prover.ts:3605` | `trident/mutation-prover.test.ts:298`; `trident/mutation-prover.test.ts:407` | Silent: the experiment can leave broken code or claim recovery that never happened. |
| G150 | keep-in-place | Mutated and restored guard output hashes must differ. | `trident/mutation-prover.ts:3608` | `trident/mutation-prover.test.ts:305` | Silent: identical observations can be presented as opposite results. |
| G151 | keep-in-place | Proof evidence must pass schema, run/head identity, token authentication, and observation checks before the outer gate accepts it. | `trident/mutation-prover.ts:3531`; `trident/mutation-prover.ts:3538`; `trident/mutation-prover.ts:3552`; `trident/mutation-prover.ts:4710` | `trident/mutation-prover.test.ts:5834` | Silent: handwritten or replayed evidence can authorize merge. |
| G152 | keep-in-place | The branch must still equal the proof pin after execution and on exempt paths. | `trident/mutation-prover.ts:4543`; `trident/mutation-prover.ts:4593`; `trident/mutation-prover.ts:4698` | `trident/mutation-prover.test.ts:5350` | Silent: the proved code can differ from the merged code. |
| G153 | keep-in-place | Cleanup refuses invalid arguments, unknown modes, or a non-repository before attempting deletion. | `trident/worktree-cleanup.sh:108`; `trident/worktree-cleanup.sh:117`; `trident/worktree-cleanup.sh:124` | `trident/worktree-cleanup-sh.test.ts:605`; `trident/worktree-cleanup-sh.test.ts:613`; `trident/worktree-cleanup-sh.test.ts:619` | Loud: later git commands generally reject invalid inputs. |
| G154 | keep-in-place | An unreadable worktree enumeration is preservation, not an empty successful cleanup. | `trident/worktree-cleanup.sh:166` | `trident/worktree-cleanup-sh.test.ts:426` | Silent: uninspected work can be reported cleaned. |
| G155 | keep-in-place | Cleanup skips the shared checkout and paths that are no longer the registered worktree root. | `trident/worktree-cleanup.sh:169`; `trident/worktree-cleanup.sh:209` | `trident/worktree-cleanup-sh.test.ts:335`; `trident/worktree-cleanup-sh.test.ts:381` | Silent: another checkout can be removed or falsely attributed to this run. |
| G156 | keep-in-place | Cleanup preserves a PR branch when any worktree was preserved, the remote cannot be read, the branch was never pushed, or remote and local heads disagree. | `trident/worktree-cleanup.sh:265`; `trident/worktree-cleanup.sh:282`; `trident/worktree-cleanup.sh:288`; `trident/worktree-cleanup.sh:292` | `trident/worktree-cleanup-sh.test.ts:552`; `trident/worktree-cleanup-sh.test.ts:563`; `trident/worktree-cleanup-sh.test.ts:578` | Silent: the only ref to built work can be deleted. |
| G157 | keep-in-place | Cleanup bounds remote reads when timeout is available and disables interactive git authentication. | `trident/worktree-cleanup.sh:99` | `trident/worktree-cleanup-sh.test.ts:500`; `trident/worktree-cleanup-sh.test.ts:523` | Silent: cleanup can hang after the run ends; timeout executable availability remains a limitation. |
| G158 | keep-in-place | Merge conflict resolution has a 12-attempt ceiling, and a second-opinion retry requires remaining rounds and the one-arbitration-per-rebase allowance. | `trident/merge.ts:3639`; `trident/merge.ts:3706`; `trident/merge.ts:3707` | `trident/arbiter-wiring.test.ts:532`; `trident/arbiter-wiring.test.ts:587` | Silent: repeated conflict resolution can monopolize the serial sweep. |
| G159 | re-home-to-TS | A truncated or unspecified Codex coverage flag is presented to synthesis as partial coverage, not a whole-change approval. | `pre-#845 trident/inner-workflow.mjs:7029`; `pre-#845 trident/inner-workflow.mjs:7032` | `trident/inner-workflow.test.ts:1132`; `trident/inner-workflow.test.ts:1163` | Silent: a partial review can be interpreted as complete; prompt-level constraint. |
| G160 | re-home-to-TS | Fully base-excused red CI still holds REQUEST_CHANGES without buying a fix round, and a dead synthesis is not assigned a verdict. | `pre-#845 trident/inner-workflow.mjs:7534`; `pre-#845 trident/inner-workflow.mjs:7537` | `trident/gates/review-ci.test.ts:12`; `trident/build-run.test.ts:1192`; `trident/build-run.test.ts:1193`; `trident/gates/review-panel.test.ts:50` | Silent: pre-existing red can be silently merged or cause endless code fixes. |

### Additional boundary checks from the cross-check

| ID | Fate | Refuses or requires | Enforcement | Test pin, or NO TEST | If lost |
| --- | --- | --- | --- | --- | --- |
| G161 | keep-in-place | A proof must re-provision a hook-disabled worktree at the same commit before the restored observation and verify its bytes match the commit. | `trident/mutation-prover.ts:3000`; `trident/mutation-prover.ts:3006`; `trident/mutation-prover.ts:3009` | `trident/mutation-prover.test.ts:430`; `trident/mutation-prover.test.ts:457`; `trident/mutation-prover.test.ts:489`; `trident/mutation-prover.test.ts:507` | Silent: nominated commands can edit the restored test and forge green. |
| G162 | keep-in-place | The mutation find string must occur exactly once and the target must remain within the worktree after symlink resolution. | `trident/mutation-prover.ts:3039`; `trident/mutation-prover.ts:3384` | `trident/mutation-prover.test.ts:1512`; `trident/mutation-prover.test.ts:1520`; `trident/mutation-prover.test.ts:1552` | Silent: a mutation can land on the wrong occurrence or outside its tree. |
| G163 | keep-in-place | The proof shares one wall-clock deadline across mutated guard, control, and restored guard. | `trident/mutation-prover.ts:3019`; `trident/mutation-prover.ts:3412`; `trident/mutation-prover.ts:3505` | `trident/mutation-prover.test.ts:340` | Silent: multiple command budgets can make a proof run indefinitely longer than its allowance. |
| G164 | keep-in-place | A bound review cannot record REQUEST_CHANGES without findings; it becomes REVIEW_NOT_RUN without invoking the build path. | `trident/bound-review.ts:198` | `trident/review-run.test.ts:425`; `trident/review-run.test.ts:462` | Silent: an unwritable or invented rejection can loop forever. |
| G165 | re-home-to-TS | The delta-classification helper permits its cheap path only for readable nonempty documentation paths outside its denylist; production reachability is not established by this audit. | `pre-#845 trident/inner-workflow.mjs:3859`; `pre-#845 trident/inner-workflow.mjs:3899` | `trident/__tests__/delta-classifier.test.ts:43`; `trident/__tests__/delta-classifier.test.ts:66`; `trident/__tests__/delta-classifier.test.ts:90` | Silent: if reused without its predicate, an optimization can skip code review; included as uncertain/helper-only. |
| G166 | keep-in-place | Every push of a build branch to origin scans the launch-base..head range for a commit whose message has a line beginning `Claude-Session:` (ASCII case-insensitive, read from the raw object with replacement objects disabled; the range ignores replacement refs, shallow boundaries, legacy grafts and the commit-graph cache; an empty message is measured clean; the raw read is cross-checked against the object's own `cat-file -s` size and Git OID, using SHA-1 or SHA-256 according to the listed OID width; replacement objects are disabled on both reads; an unauthenticated capture is unmeasured and never clean), and every PR-bound push refuses on a carrier or on missing raw history: the checked publishers through `publicationReadiness`; `publishBuiltCommit` -- EVERY legacy-loop publication (the `publish_requested` push after each build and fix round) as well as the stranded-work salvage push, the path a refused wrapper commit actually takes -- through the same exported scan after the replay and before its lease push. The G100 preservation push (`checkBuildClaim`, the path a wrapper provenance refusal -- Forge claiming the commit it created while the branch names another -- actually takes) runs the same scan before its push and names its result in the refusal (`; preserved range: Publication branch carries a Claude-Session trailer on N commit(s) ... -- strip before any PR`, or `; session-trailer scan unmeasured: <detail>`), but preserves the branch as G100 specifies (owner decision 2026-09-19: G100 is not superseded; this scan observes, it does not veto). The checked publishers and the salvage push fail closed on a range they cannot measure AND on a commit whose raw object was read INCOMPLETELY: `run` is an injected host runner, so completeness is MEASURED, not assumed from the runner -- a capture cut mid-header, cut at the header/message boundary, cut inside the message, or longer than the object is `unknown`, never `allow`, naming the sha and the byte gap, while a real `--allow-empty-message` commit stays allowed through both a trimming runner (`spawnCapture`, exactly 2 bytes short) and an untrimming one (#1133 round 22). Every accepted capture reconstructs the exact listed OID using Git's NUL-terminated `commit <byte length>` header and the captured UTF-8 bytes. A capture with a message boundary permits direct bytes or up to two proposed LFs (#1133 round 30: a plain wrapper commit whose trailing trailer paragraphs were all dropped keeps the separator blank before them, so its raw object ends in two LFs and a trimming runner captures it two bytes short); three or more are never LF-reconstructed; a headers-only empty-message capture permits one or two proposed separator LFs. A short capture the LF proposals cannot explain gets ONE untrimmed read (#1133 round 31: the trimming runner strips EVERY trailing whitespace code point, so a `--cleanup=verbatim`, `commit-tree` or tooling-made message ending in a space, a tab or three-plus LFs was permanently unknown): `git log -1 --format=format:%x00%B%x00` fences the raw message with NULs that no trim removes, and it is accepted only when the capture is a prefix of the reconstruction, everything after the capture is whitespace, the reconstruction weighs the object's own `cat-file -s` size and it reproduces the listed OID -- so it explains a trim and never heals a capture cut inside content. These bytes are authenticated before the message is scanned, so genuine clean `Claude-Session` prefixes followed by LF allow, missing trailer colons and equal-length substitutions are unknown, and authenticated carriers remain blocked and named. Invalid UTF-8 inflation that exactly offsets truncation also stays unknown; a literal valid UTF-8 replacement character is ordinary message data. This replaces the round-23/24 token-prefix heuristic and closes both its false positive and the byte-count false negative. BOTH ENDS OF THE RANGE ARE VALIDATED (#1133 round 24): `head` is checked with the same `fullOid` test as `launchBase` and a value failing it is `unknown` naming the (bounded) value, before any `rev-list`, any raw read and any push. Two defects needed it: git reads `<base>..` as `<base>..HEAD` (exit 0), so an empty head measured the checkout instead of the commit being published; and `publication.ts`'s object-pinned refspec `${headToPublish}:refs/heads/<branch>` becomes git's branch-DELETE form when the head is empty -- measured on git 2.43.0, `git push --force-with-lease=refs/heads/<b>:<observed> origin ':refs/heads/<b>'` prints `- [deleted]` and EXITS 0, satisfying the lease, after which the post-push witness compares '' to '' and agrees. `headToPublish` is `stdout.trim()` of an injected runner's read (`replay.ts:200`, `replay.ts:595`, `leak-preflight.ts:448`, each checking only `.ok`), so the shape is reachable. `publicationReadiness` also `return await`s the scan rather than returning its promise bare, so a throwing injected runner becomes `unknownCause(...)` instead of escaping the gate's `catch` as a rejection. The cross-check authenticates against the OIDs supplied by the range listing; a runner that fabricates that listing remains outside this boundary. Every one of the three pushes the OBJECT it scanned (`<sha>:refs/heads/<branch>`), never the local ref. This scan is a deterministic raw-object check of the loop's own commits, not the leak preflight; G139's advisory preflight semantics are unchanged. The push sites are enumerated by `grep -rn "'push'" trident --include=*.ts` excluding tests, re-measured 2026-09-22 on this head: the recipe returns SEVEN lines across FIVE non-test files, and every one of them is accounted for below so the enumeration and the instrument that checks it agree (earlier revisions of this row said "four pushes" and named only five of the seven lines, omitting `trident/merge.ts:2147` and `trident/publication.ts:318`). THREE are origin-facing pushes that publish objects on a BUILD branch, and all three run this scan before pushing: `trident/production-host-effects.ts:390` (the host's object-pinned lease push), `trident/publication.ts:311` (`publishBuiltCommit`; the sixth line, `trident/publication.ts:318`, is that same push's failure-reason string and not a second site) and `trident/gates/build-claim.ts:74` (the G100 preservation push). The remaining lines publish no build-branch object: `trident/merge.ts:1686` is the local-mode merge push, which pushes the merge commit onto the configured base branch of the local repository under `receive.denyCurrentBranch=updateInstead`; it is not an origin push and publishes no build-branch object; `trident/merge.ts:2147` (`git push origin --delete <branch>`) IS origin-facing and DOES name a build branch, but a delete refspec transfers no object, so there is nothing for this scan to read and no way for that command to carry a trailer to origin; and `trident/wrong-base-remedy.ts:536` (`tok.indexOf('push')`) is a string scan that redacts a destructive push out of a remedy sentence and issues no argv. This enumeration is a documentation claim about reachability, not a scan: no scanning is added to the delete or the local-mode merge. A wrapper refusal Forge ignored cannot reach origin as a PR (#1133); the one push that may carry it to origin is G100's preservation of a refused build, which names it. The range listing is a plain `git` argv -- the graft override travels as the host runner's typed `extraEnv` (`GIT_GRAFT_FILE=/dev/null`, with `advice.graftFileDeprecated=false` so git's graft-file hint never reaches a captured stderr), never as an `env` argv prefix: the salvage path's host doubles admit only `git`/`gh` and throw on anything else, and round 19's prefix red 7 consumer tests (`trident/stranded-salvage-realgit.test.ts`, `gateway/composition/build-core-modules-trident-stranded-sweep.test.ts`). WINDOW (#1133 round 31): the salvage path scans from the observed base tip; the checked publishers start from the run's launch pin, which is carried across resumes and fix rounds while the branch is rebased onto the current base tip, so `pin..head` also listed base-branch commits origin already publishes and one public carrier among them (`0fc6cb83`, #1131's squash) refused the run with nothing the loop could strip. `publicationReadiness` now takes the run's base branch and narrows the window to `merge-base(head, <tip>)` where `<tip>` is what `ls-remote` says ORIGIN holds (never the local `refs/remotes/origin/*` ref), computed over the same raw graph, and only when that merge-base descends from the pin -- so the window is always a subset of `pin..head` and every excluded commit is already public. A failed or malformed `ls-remote`, an absent base branch, an unfetched tip or a merge-base below the pin keeps the pin; nothing is fetched and no ref moves. | `trident/gates/release-readiness.ts:23`; `trident/gates/release-readiness.ts:29`; `trident/gates/release-readiness.ts:30`; `trident/gates/release-readiness.ts:39`; `trident/gates/release-readiness.ts:51`; `trident/gates/release-readiness.ts:56`; `trident/gates/release-readiness.ts:81`; `trident/gates/release-readiness.ts:102`; `trident/gates/release-readiness.ts:124`; `trident/gates/release-readiness.ts:127`; `trident/gates/release-readiness.ts:140`; `trident/gates/release-readiness.ts:148`; `trident/gates/release-readiness.ts:152`; `trident/gates/release-readiness.ts:157`; `trident/gates/release-readiness.ts:159`; `trident/gates/release-readiness.ts:181`; `trident/gates/release-readiness.ts:231`; `trident/gates/release-readiness.ts:304`; `trident/gates/release-readiness.ts:318`; `trident/gates/release-readiness.ts:336`; `trident/gates/release-readiness.ts:345`; `trident/production-host-effects.ts:382`; `trident/build-host.ts:228`; `trident/publication.ts:266`; `trident/publication.ts:314`; `trident/gates/build-claim.ts:65`; `trident/gates/build-claim.ts:71`; `trident/gates/build-claim.ts:79` | `trident/gates/release-readiness.test.ts:85`; `trident/gates/release-readiness.test.ts:116`; `trident/gates/release-readiness.test.ts:136`; `trident/gates/release-readiness.test.ts:148`; `trident/gates/release-readiness.test.ts:172`; `trident/gates/release-readiness.test.ts:184`; `trident/gates/release-readiness.test.ts:192`; `trident/gates/release-readiness.test.ts:201`; `trident/gates/release-readiness.test.ts:215`; `trident/gates/release-readiness.test.ts:227`; `trident/gates/release-readiness.test.ts:246`; `trident/gates/release-readiness.test.ts:262`; `trident/gates/release-readiness.test.ts:274`; `trident/gates/release-readiness.test.ts:316`; `trident/gates/release-readiness.test.ts:337`; `trident/gates/release-readiness.test.ts:348`; `trident/gates/release-readiness.test.ts:367`; `trident/gates/release-readiness.test.ts:384`; `trident/gates/release-readiness.test.ts:404`; `trident/gates/release-readiness.test.ts:435`; `trident/gates/release-readiness.test.ts:453`; `trident/gates/release-readiness.test.ts:471`; `trident/gates/release-readiness.test.ts:495`; `trident/gates/release-readiness.test.ts:540`; `trident/gates/release-readiness.test.ts:572`; `trident/gates/release-readiness.test.ts:603`; `trident/gates/release-readiness.test.ts:636`; `trident/gates/release-readiness.test.ts:665`; `trident/gates/release-readiness.test.ts:698`; `trident/gates/release-readiness.test.ts:725`; `trident/gates/release-readiness.test.ts:754`; `trident/gates/release-readiness.test.ts:759`; `trident/gates/release-readiness.test.ts:764`; `trident/gates/release-readiness.test.ts:774`; `trident/gates/release-readiness.test.ts:804`; `trident/gates/release-readiness.test.ts:826`; `trident/gates/release-readiness.test.ts:855`; `trident/gates/release-readiness.test.ts:876`; `trident/gates/release-readiness.test.ts:903`; `trident/production-host-effects.test.ts:1243`; `trident/build-host.test.ts:858`; `trident/publication-session-trailer-realgit.test.ts:127`; `trident/publication-session-trailer-realgit.test.ts:148`; `trident/publication-session-trailer-realgit.test.ts:179`; `trident/publication-session-trailer-realgit.test.ts:199`; `trident/publication-session-trailer-realgit.test.ts:225`; `trident/publication-session-trailer-realgit.test.ts:243`; `trident/publication-session-trailer-realgit.test.ts:291`; `trident/publication-session-trailer-realgit.test.ts:333`; `trident/gates/build-claim.test.ts:33`; `trident/gates/build-claim.test.ts:134`; `trident/gates/build-claim.test.ts:150`; `trident/stranded-salvage-realgit.test.ts:325`; `gateway/composition/build-core-modules-trident-stranded-sweep.test.ts:160`; `trident/gates/build-claim-realgit.test.ts:96`; `trident/gates/build-claim-realgit.test.ts:114`; `trident/gates/build-claim-realgit.test.ts:137`; `trident/build-host.test.ts:758` | Silent: a trailer-bearing commit is pushed to the public repository. |

## Independent vocabulary cross-check

The terminal vocabulary was enumerated from `pre-#845 trident/inner-workflow.mjs:4712` and checked against `trident/terminal-cause.ts:67`, rather than inferred from successful paths. All 16 entries map into this inventory:

| Vocabulary values | Rows that preserve the transition |
| --- | --- |
| `review-approved`, `resume-approved-unchanged` | G038, G061, G104–G107 |
| `round-budget-exhausted` | G041, G076 |
| `review-advisory-only`, `review-infra-only` | G056–G067, G160 |
| `review-escalated` | G068–G078 |
| `round-lost-work`, `round-lost-no-diff` | G042–G043 |
| `pr-already-merged` | G036, G112 |
| `resume-head-unreadable`, `built-head-unverified` | G015, G032–G033, G038 |
| `wave-member-built` | G009, G024, G034 |
| `handoff-publish`, `ralph-task-built` | G037, G083–G103 |
| `workflow-threw`, `unknown` | G005–G012, G124 |

`parseTerminalCause` rejects unrecognized values to null (`trident/terminal-cause.ts:162`); `unknown` is an explicit member, not the same thing as a missing field. `reviewLoopTerminalCause` selects lost work/diff, escalation, approval, infra/advisory stop, and budget exhaustion, then falls back to unknown (`pre-#845 trident/inner-workflow.mjs:4891`). This is a reporting vocabulary: adding a word alone does not add a gate.

The eight decoded block kinds are `none`, `code`, `infra-only`, `advisory-only`, `round-lost`, `design-gap`, `missing-dependency`, and `not-converging` (`trident/inner-loop.ts:899`). Unknown kinds become null. G011, G067–G078, and G131 pin their authority: code can purchase fixes, infra/advisory cannot, and escalation needs matching evidence. Fire outcomes are `fired`, `failed`, and `unconfirmed` (G003–G004, G116–G117), with an incomplete stream explicitly failed at `trident/inner-loop.ts:1118`.

The outer merge catch distinguishes `TridentBaseDriftHold` and `TridentMergeConflictEscalation`, then treats other errors as generic merge failures while retaining the approval (`trident/orchestrator.ts:5351`, `trident/orchestrator.ts:5362`, `trident/orchestrator.ts:5370`). Replay conflicts use `TridentRebaseConflict` (G090–G096), not a new code-review verdict. Infrastructure classification defaults to genuine and admits retries only on measured qualifying shapes (`trident/orchestrator.ts:1022`). Board routing defaults to failed except done or a derived coherent escalation (`trident/board-reconcile.ts:106`). These defaults are part of preservation: a new outcome that falls through can buy retries, suppress them, or label infrastructure as a code rejection.

## Limits, uncertainties, and continuous enforcement

This is a bounded inventory of the three named loop files and the explicitly followed boundary owners, **not a proof that every transitive dependency is covered**. Compound conditions are grouped by the decision they protect; rows and raw grep hits do not correspond one-to-one. Lexical searches can miss computed dispatch, dynamically constructed commands, a new synonym, an indirect helper, or a condition whose significance is not evident from its name. Test declarations can miss assertions inside generic fixtures or generated cases; this is why NO TEST is an unresolved pin, not an absence theorem. A positive control proves the search works on that spelling, not that the spelling spans every implementation.

The delegated mutation validator (G143) retains ownership of its many argument spellings; this document does not enumerate each runner-option alias. Transitive rebase/arbiter and bound-review checks are not exhaustively expanded here. Host credential resolution, dispatch admission, scheduler concurrency, Workflow runtime token accounting, GitHub branch protection, and CI's own leak scanner are outside the three-file audit boundary. They must not be mistaken for guards implemented by these files. This slice adds no runtime token-budget gate merely because `budget.spent()` appears in logging (`pre-#845 trident/inner-workflow.mjs:7630`).

G139 and G140 are deliberately included even though they do not refuse: preflight and single-family warnings are easy to mistake for hard gates. G132–G134, G136–G138 and G159 include prompt requirements and data boundaries because losing them changes what a review or build can mean. They depend on the agent following its instructions; they are not independent enforcement. G135 is independently enforced only in clones whose versioned hooks are installed; otherwise its wrapper invocation remains prompt-routed (`.githooks/pre-commit:4`, `pre-#845 trident/inner-workflow.mjs:1686`). Cleanup is a deterministic command, but its invocation remains an agent boundary (`pre-#845 trident/inner-workflow.mjs:9291`).

Code-level gates are maintained by the loop's comparisons on every transition. Retry/recovery bounds also rely on persisted counts and atomic claims (G079–G082, G114). The outer watchdog remains a backstop when the inner workflow cannot persist a failure (G123–G124), so that backstop does not require the failed inner agent to cooperate; it still requires the outer ticker and storage to run. Cleanup in finally similarly does not require a valid builder result, but process death can prevent finally from executing (G125). This document introduces no new invariant or enforcement mechanism and makes no broader crash-survival claim.

No code or tests were changed, no new guard was added, and no mutation experiment was performed in this documentation slice. Existing test references are preservation targets for the replacement lane, not permission to weaken tests or a claim that all cited tests were run here. In particular, the existing ten-round default and per-run cap (G076) are what this inventory records; it does not silently substitute a prose review-round policy for the implemented condition.
