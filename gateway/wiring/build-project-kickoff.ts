/**
 * @neutronai/gateway/wiring — C8 re-export SHIM (one release).
 *
 * C8 evicted the one-time agentic per-project KICKOFF product logic out of the
 * composition layer into the product package `onboarding/openings/`. New code
 * should import from `@neutronai/onboarding/openings/kickoff.ts` directly.
 */
export * from '@neutronai/onboarding/openings/kickoff.ts'
