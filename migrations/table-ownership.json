{
  "$comment": [
    "P4 (world-class refactor, 2026-07) \u2014 committed table-ownership map:",
    "'one owning module per table'. For every table listed here, the set of",
    "non-test source files containing a WRITE statement (INSERT [OR ...] INTO /",
    "UPDATE ... SET / DELETE FROM / REPLACE INTO) must EXACTLY equal the",
    "'writers' allowlist below. CI-enforced by",
    "migrations/__tests__/table-ownership-conformance.test.ts, which also",
    "checks each table exists in migrations/expected-schema.txt.",
    "Migrations (*.sql + the runner) and test files are exempt: schema",
    "evolution and fixtures are not runtime writers.",
    "To extend coverage to another table, add an entry with its honest",
    "current writer set \u2014 the test picks it up automatically."
  ],
  "tables": {
    "projects": {
      "owner": "gateway/projects/sqlite-store.ts",
      "writers": [
        "cores/free/agent-settings/src/backend.ts",
        "gateway/projects/sqlite-store.ts",
        "gateway/wiring/project-create.ts"
      ],
      "notes": [
        "gateway/projects/sqlite-store.ts is the owning settings/lifecycle store",
        "(seed, PATCH, archive/restore, activity stamps \u2014 incl.",
        "touchActivityIncludingArchived, moved verbatim from open/composer.ts).",
        "gateway/wiring/project-create.ts is the sanctioned",
        "project-CREATION writer (refactor-plan \u00a7P4: creation converges on",
        "sqlite-store.ts + project-create.ts; the second creation writer at",
        "onboarding/wow-moment/actions/03-project-shells.ts was deleted by",
        "K11b1 before this unit landed, so there is nothing to converge).",
        "cores/free/agent-settings/src/backend.ts is a RETAINED direct writer",
        "(rename/delete/archive/restore/merge/engagement-mode): it lives in the",
        "cores band, where importing the gateway store would violate the",
        "cores-use-sdk-only dependency rule. Folding it in needs a",
        "dependency-inversion seam (inject a lifecycle-writer interface built",
        "in the gateway wiring) \u2014 deliberate follow-up, out of P4 scope.",
        "FOLLOW-UP: touchActivity vs touchActivityIncludingArchived differ only",
        "by 'archived_at IS NULL'; converge deliberately once the archived-row",
        "stamp semantics are decided (byte-identical-move rule kept them apart)."
      ]
    },
    "onboarding_state": {
      "owner": "onboarding/interview/sqlite-state-store.ts",
      "writers": [
        "migrations/scope-rekey.ts",
        "onboarding/interview/sqlite-state-store.ts"
      ],
      "notes": [
        "The telemetry factory's mint-on-miss attempt_id",
        "transaction (onboarding/telemetry/event-emitter.ts) moved verbatim",
        "into SqliteOnboardingStateStore.resolveOrMintAttemptId in P4.",
        "resume-cron's SQL json_remove writer (the plan's second dialect) was",
        "deleted by K11b1 before this unit landed \u2014 the store's JS",
        "shallow-merge is now the only phase_state write dialect.",
        "migrations/scope-rekey.ts is a DELIBERATE second writer (#451): the",
        "boot scope reconciler resolves an old-key vs current-key pair for the",
        "same user_id by DELETING the less authoritative row before the generic",
        "re-key move. It cannot route through the store \u2014 the store's own",
        "rekey() THROWS on exactly this collision (by design; the rename",
        "orchestrator's pre-flight is supposed to prevent it), whereas the",
        "reconciler exists precisely to repair databases where it already",
        "happened. It is also a boot-step in the migrations band, which cannot",
        "import the onboarding band. Scope: DELETE only, only during a re-key."
      ]
    },
    "instance_scope_ledger": {
      "owner": "migrations/scope-rekey.ts",
      "writers": [
        "migrations/scope-rekey.ts"
      ],
      "notes": [
        "Single-writer singleton (#451). Records which scope key this database's",
        "slug-scoped rows are written under. Written ONLY as the last statement",
        "of the reconciler's transaction, so a crash rolls the ledger back",
        "together with the row moves it describes."
      ]
    },
    "gbrain_sync_state": {
      "owner": "gateway/wiring/gbrain-sync-state-store.ts",
      "writers": [
        "gateway/wiring/gbrain-sync-state-store.ts"
      ],
      "notes": [
        "P9 (world-class refactor) \u2014 GBrain sync observability. Single-writer:",
        "the `createGbrainSyncStateStore` sink UPSERTs the one per-scope health",
        "row (status/latch/last-success/deferred-count). It is the concrete",
        "`GbrainSyncStateSink` implementation injected into GBrainSyncHook; the",
        "hook (gbrain-memory, platform band) owns no DB and only calls the port,",
        "so the write stays in the composition band where DB access lives.",
        "Pure side-observation \u2014 this unit adds visibility, not behavior; the",
        "fail-soft sync path is byte-identical."
      ]
    },
    "code_ritual_runs": {
      "owner": "reminders/ritual-runs.ts",
      "writers": [
        "reminders/ritual-runs.ts"
      ],
      "notes": [
        "Executor-mode reminders (plan task 4; migration 0106 PART B). Durable",
        "ritual run history \u2014 one row per fire ATTEMPT (skips included), retained",
        "on its own window (NOT pruned on the subagent liveness prune). Single-",
        "writer: `createRitualRunStore` owns every INSERT (skipped/running/failed)",
        "and the terminal UPDATE (finished/failed/timed_out/crashed + ended_at +",
        "truncated output_summary). The retention prune lands with plan task 5."
      ]
    },
    "project_launcher_entries": {
      "owner": "gateway/http/sqlite-project-launcher-store.ts",
      "writers": [
        "gateway/http/sqlite-project-launcher-store.ts"
      ],
      "notes": [
        "Per-project Apps-launcher CUSTOMISATION only (order, renames, per-project",
        "tile removals). The tile CATALOGUE is read live from the bundled-Cores",
        "registry on every list(), never from this table, so a Core installed after",
        "a project's first write still appears in that project's grid.",
        "Rows are materialised on the first MUTATION, never on a read (ISSUES #412)."
      ]
    },
    "usage_pool_samples": {
      "owner": "persistence/usage-samples-store.ts",
      "writers": [
        "persistence/usage-samples-store.ts"
      ],
      "notes": [
        "Added to the map on creation rather than retrofitted: the table has exactly",
        "ONE writer and enrolling it now is what keeps that true. A usage series with",
        "several writers would make a gap in the data impossible to attribute.",
        "The monitor does not write directly \u2014 it calls an injected sink, so the",
        "60-second tick and the storage stay separable and the tick stays fail-soft."
      ]
    }
  }
}
