## Issue 548 — version-free model classes

### What changed

The four Claude defaults now select `opus`, `fable`, `sonnet`, and `haiku` rather than concrete releases (`runtime/models.ts:45-87`). The independently loaded boot configuration mirrors those values (`config/index.ts:52-57`), and its existing cross-package drift guard compares the two sources (`gateway/__tests__/model-defaults-drift.test.ts:28-45`). Explicit environment values remain authoritative at each runtime declaration (`runtime/models.ts:45-87`).

Pricing now classifies each version-free selector through `MODEL_CLASS_PRICING_TARGETS` before the existing exact and snapshot lookups (`runtime/model-pricing.ts:183-230`). The exported `resolveModelPricingTarget` is the same catalog lookup used by `resolveModelPricing`, so tests for resolved and stamped defaults can follow future catalog releases without copying today's ID (`runtime/model-pricing.ts:188-228`, `cores/free/code-gen/__tests__/substrate-runtime.test.ts:602-614`, `cores/free/email/__tests__/tools.test.ts:475-500`, `cores/free/research/__tests__/sub-agent.test.ts:206-212`, `gateway/tasks/p6/__tests__/nudge-engine.test.ts:457-462`, `runtime/adapters/claude-code/persistent/__tests__/model-floor.test.ts:346-401`). This keeps the existing fail-loud unknown-model behavior at `runtime/model-pricing.ts:241-248` while allowing the four defaults to boot.

The HTTP settings vocabulary now resolves each registry row's selector through that same catalog before exposing `model_id` (`gateway/http/trident-phase-models-surface.ts:155-194`). Previously the row copied the registry's version-free Claude class unchanged (`gateway/http/trident-phase-models-surface.ts:179-183`, `trident/model-tiers.ts:129-150`), so the surface could say `opus` or `haiku` rather than name the release that will run. The tier remains the persisted owner choice; only the rendered explanatory ID is concrete, as specified by the surface test (`gateway/__tests__/trident-phase-models-producer.test.ts:340-355`).

The update watchdog now recognizes the same four-class vocabulary (`runtime/adapters/claude-code/persistent/model-update-watchdog.ts:139-144`). On a first class probe it records a matching concrete ID only as the edge-detection baseline and routes a wrong-family answer to the existing fail-closed `skip-unrecognized` outcome (`runtime/adapters/claude-code/persistent/model-update-watchdog.ts:286-295`). Restart restoration does not replace a class with a persisted version (`runtime/adapters/claude-code/persistent/model-update-watchdog.ts:657-665`), and live wiring retains the class when a newer concrete ID is observed (`runtime/adapters/claude-code/persistent/supervision.ts:938-967`).

### Decisions

Class aliases are the dispatch contract because the Claude CLI already demonstrates class-to-current-release resolution with `--model opus` (`runtime/adapters/claude-code/persistent/model-update-watchdog.ts:83-92`). Concrete IDs remain valid only as explicit overrides and persisted observation baselines.

Every newly reported site still defaulted in production: Research supplies its local Sonnet fallback when no model is passed (`cores/free/research/src/sub-agent.ts:196-200`, `cores/free/research/src/sub-agent.ts:276-281`), nudge supplies its fast-class default before persisting the selected model (`gateway/tasks/p6/nudge-engine.ts:93-94`, `gateway/tasks/p6/nudge-engine.ts:441-445`), and the floor returns the requested or configured selector according to its passthrough, clamp, same-tier, and blank-floor branches (`runtime/adapters/claude-code/persistent/model-floor.ts:337-372`). Therefore these assertions follow resolution; no production default was repaired.

The supplied widened search was re-run across every `*.test.ts*` file and enumerated 15 files. The newly found concurrency-metadata assertion now follows catalog resolution (`cores/free/research/__tests__/orchestrator.test.ts:395-413`). The other matches either already resolve the catalog class, cover pricing and class declarations, mention a selector only in source-shape commentary, or deliberately test exact selector identity. The exact comparisons stay direct because they test alias identity (`runtime/__tests__/models.test.ts:43-46`, `tasks/__tests__/prioritize-llm.test.ts:380-384`), the selector handed to the CLI (`runtime/adapters/claude-code/persistent/__tests__/model-floor.test.ts:770-783`), or the workflow option passed to Claude Code (`trident/__tests__/cross-model-dispatch.test.ts:1256-1265`, `trident/__tests__/cross-model-dispatch.test.ts:1298-1307`). The stale-phrase search returned no matches; its same-invocation positive control found the replacement phrases at `cores/free/research/__tests__/sub-agent.test.ts:208`, `gateway/tasks/p6/__tests__/nudge-engine.test.ts:458`, `cores/free/research/__tests__/orchestrator.test.ts:396`, and `runtime/adapters/claude-code/persistent/__tests__/model-floor.test.ts:349`.

No new watchdog outcome was introduced. Matching resolutions join `ModelUpdateDecision`'s existing `no-change` outcome, while mismatched families join `skip-unrecognized`; the switch persists the former and logs/refuses the latter by default (`runtime/adapters/claude-code/persistent/model-update-watchdog.ts:237-257`, `runtime/adapters/claude-code/persistent/model-update-watchdog.ts:694-727`).

The class invariant is continuously maintained at the default declarations and the boot-config parity test (`runtime/__tests__/models.test.ts:32-78`, `gateway/__tests__/model-defaults-drift.test.ts:28-45`). Watchdog restart and adoption guards do not depend on a stale concrete model remaining callable (`runtime/adapters/claude-code/persistent/model-update-watchdog.ts:657-665`, `runtime/adapters/claude-code/persistent/supervision.ts:938-967`).

### Mutation table

| Guard | Mutation | Red proof | Restored proof |
|---|---|---|---|
| All defaults are version-free classes (`runtime/models.ts:45-87`) | Restored Sonnet's concrete release at `runtime/models.ts:80` in the mutation checkout (the restored declaration is now `runtime/models.ts:80`) | `runtime/__tests__/models.test.ts`: 2 failures | Focused run: green |
| A resolved ID must match its configured class (`runtime/adapters/claude-code/persistent/model-update-watchdog.ts:286-295`) | Inverted the family comparison | `model-class baselines`: 2 failures | Focused run: green |
| A class is never replaced by the observed concrete ID (`runtime/adapters/claude-code/persistent/supervision.ts:938-943`) | Inverted the class-retention condition | wiring test: expected `opus`, received the concrete probe ID | Focused run: green |
| Code-gen's absent-response model follows the Sonnet catalog target (`cores/free/code-gen/__tests__/substrate-runtime.test.ts:602-614`) | Changed the fallback at `cores/free/code-gen/src/substrate-runtime.ts:526` to the prior Sonnet release | Code-gen suite: 1 failure, expected the catalog target and received the prior release | Code-gen suite: 24 passing, 0 failing |
| Email's absent dependency model follows the fast catalog target (`cores/free/email/__tests__/tools.test.ts:475-500`) | Changed the fallback at `cores/free/email/src/tools.ts:190` to the undated Haiku alias | Email suite: 1 failure, expected the catalog snapshot and received the alias | Email suite: 15 passing, 0 failing |
| Research's local fallback follows the Sonnet catalog target (`cores/free/research/__tests__/sub-agent.test.ts:206-212`) | Changed `DEFAULT_SUB_AGENT_MODEL` at `cores/free/research/src/sub-agent.ts:281` to a Haiku snapshot; printed the mutated declaration | Research suite: 1 failure, expected the Sonnet target and received the Haiku snapshot | Restored targeted run: green |
| Research concurrency metadata follows the Sonnet catalog target (`cores/free/research/__tests__/orchestrator.test.ts:395-413`) | Changed the metadata write at `cores/free/research/src/research-orchestrator.ts:371` from `SONNET_MODEL` to `haiku`; printed the mutated write | Targeted T7: 1 failure, expected the Sonnet target and received the Haiku target | Restored orchestrator file: 18 passing, 0 failing |
| Nudge's default follows the fast catalog target (`gateway/tasks/p6/__tests__/nudge-engine.test.ts:457-462`) | Changed `DEFAULT_NUDGE_MODEL` at `gateway/tasks/p6/nudge-engine.ts:94` to the Opus class; printed the mutated declaration | Nudge suite: 1 failure, expected the Haiku target and received the Opus target | Restored targeted run: green |
| An unfloored substrate preserves its fast-tier choice (`runtime/adapters/claude-code/persistent/__tests__/model-floor.test.ts:346-350`) | Returned the configured floor from the disabled branch at `runtime/adapters/claude-code/persistent/model-floor.ts:345-347`; printed the mutated return | Focused floor case: 1 assertion failure, expected Haiku and received Opus | Restored targeted run: green |
| A lower request clamps to the configured Sonnet floor (`runtime/adapters/claude-code/persistent/__tests__/model-floor.test.ts:373-382`) | Returned the request rather than the floor at `runtime/adapters/claude-code/persistent/model-floor.ts:372`; printed the mutated return | Focused floor case: 1 assertion failure, expected Sonnet and received Haiku | Restored targeted run: green |
| A same-tier request remains unclamped (`runtime/adapters/claude-code/persistent/__tests__/model-floor.test.ts:384-393`) | Replaced the rank comparison's `>=` with `>` at `runtime/adapters/claude-code/persistent/model-floor.ts:368`; printed the mutated condition | Focused floor case: 1 assertion failure, expected `clamped=false` and received `true` | Restored targeted run: green |
| A blank configured floor preserves the fast request (`runtime/adapters/claude-code/persistent/__tests__/model-floor.test.ts:395-402`) | Returned `getBestModel()` from the blank-floor branch at `runtime/adapters/claude-code/persistent/model-floor.ts:364`; printed the mutated return | Focused floor case: 1 assertion failure, expected Haiku and received Opus | Restored targeted run: green |
| HTTP tier rows expose the resolved release (`gateway/http/trident-phase-models-surface.ts:179-183`) | Replaced the catalog resolution with `t.model_id`; printed the mutated line at `gateway/http/trident-phase-models-surface.ts:182` | Focused HTTP case: 1 failure, expected a `claude-haiku` prefix and received `haiku` | Restored HTTP suite: 26 passing, 0 failing |

The restored focused command ran the original touched/owning files plus the newly reported Core and gateway files and reported 209 passing, 0 failing. The six earlier corrected assertion cases reported 6 passing, 0 failing, and the added orchestrator file reported 18 passing, 0 failing. `bunx tsc --noEmit`, `bash scripts/ci/lint.sh`, and `git diff --check` completed successfully.

The widened 15-file search set plus the four remaining original files ran together: 424 tests passed and six model-floor integration cases failed because the sandbox refused the local reply-sink listener (`runtime/adapters/claude-code/persistent/__tests__/model-floor.test.ts:618-621`). The resolver-level model-floor cases and the newly corrected orchestrator case passed in that run. No assertion was weakened or skipped.

For this final surface follow-up, the original test set was enumerated from `git log --name-only origin/main..HEAD` as 12 unique test files. The HTTP suite plus the other 11 files passed 252 tests; the separately selected resolver-level model-floor cases passed 33 tests. Running all 13 files together passed 285 tests and reproduced only the six listener-bound model-floor failures above. The requested widened TypeScript search returned 361 matching lines across 82 unique files. `bunx tsc --noEmit`, `bash scripts/ci/lint.sh`, and `git diff --check` passed.

The requested surrounding-directory run enumerated the five owning `__tests__` directories on the command line and completed with 1,922 passing, 9 skipped, and 233 failing across 163 files. The failures reflected sandbox listener/filesystem restrictions and their cascades: even `Bun.serve({ port: 0 })` was refused at `runtime/adapters/claude-code/persistent/__tests__/activity-tap-hook.test.ts:29-53` and `runtime/adapters/claude-code/persistent/__tests__/sink-restart-survival.test.ts:69-75`, while the shared sink then failed closed at `runtime/adapters/claude-code/persistent/pool-state.ts:318-334`. None was an assertion changed by this follow-up; the corrected cases passed separately in the restored targeted run.

### Deliberately not changed

Concrete model IDs in historical records and fixtures remain because they test parsing, pricing, downgrade ordering, or persistence compatibility; they are not live defaults (`runtime/adapters/claude-code/persistent/__tests__/model-update-watchdog.test.ts:94-126`, `runtime/__tests__/model-pricing.test.ts:153-196`). The two local Core fallback implementations were not replaced: runtime evidence showed both still default correctly (`cores/free/code-gen/src/substrate-runtime.ts:516-526`, `cores/free/email/src/tools.ts:188-190`), so only their stale assertions and the shared catalog accessor changed. The filed issue did not change a product decision in `SPEC.md`, so no Decisions Log entry was added. No feature flag or parallel runtime path was introduced.

No outcome vocabulary or continuous invariant changed in the HTTP follow-up: the existing successful GET payload gained a resolved display value, while request validation and error handling remain unchanged (`gateway/http/trident-phase-models-surface.ts:199-230`). No test assertion was loosened, and no client-side resolver was added.
