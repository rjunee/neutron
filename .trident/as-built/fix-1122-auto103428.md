## 2026-09-17 — stopped-run reconciliation for unarmed trailer reservations

### What changed

The reservation protocol distinguishes unarmed and armed bytes at `runtime/workers/trailer-slot.ts:42-46`. A competing worker still returns `unknown` for an unarmed reservation at `runtime/workers/trailer-slot.ts:126-128`; it does not guess that the owner died. The new host reconciliation enumerates only the four reservation filename families, preserves armed files, and removes unarmed files at `runtime/workers/trailer-slot.ts:48-85`.

The host invokes reconciliation while preparing a later driver attempt, before rebuilding the runners, at `open/wiring/project-build.ts:226-238`. If listing, reading, or removal cannot be established, preparation throws the detailed uncertainty instead of dispatching (`runtime/workers/trailer-slot.ts:58-83`; `open/wiring/project-build.ts:231-232`). The existing launcher reservation admits only one in-process driver before preparation at `trident/project-launcher.ts:116-124`; this is what continuously makes host preparation, rather than a competing worker, the safe reconciliation boundary.

The filename families were enumerated from all `reserveTrailerSlot` call sites with `rg -n "reserveTrailerSlot" runtime/workers`: Claude, Codex, Pi, and Codex headless are constructed at `runtime/workers/claude-in-repl.ts:43-48`, `runtime/workers/codex-in-repl.ts:44-49`, `runtime/workers/pi-in-repl.ts:55-60`, and `runtime/workers/codex-headless.ts:135-137`. The same search found the known helper definition at `runtime/workers/trailer-slot.ts:111` as its positive control.

### Outcome vocabulary

This adds no worker outcome. A competing worker's inability to establish dispatch remains `BoundedWorkOutcome.kind = unknown` (`runtime/workers/trailer-slot.ts:35-40`), and the existing switch stops the run on that value (`trident/build-run.ts:357-362`). Host reconciliation failure throws during preparation and the launcher records construction failure through its existing failed path (`trident/project-launcher.ts:165-172`). It never shares a branch or message with `refused`.

### Tests and mutation evidence

The dead-owner test seeds the exact create-before-arm state, first proves it is unknown, reconciles it, then proves one dispatch followed by resume at `runtime/workers/trailer-slot.test.ts:60-68`. The paired non-replay test proves an armed reservation remains a resume at `runtime/workers/trailer-slot.test.ts:71-75`. The host-wiring test proves a later preparation removes the dead unarmed file at `open/__tests__/project-build-wiring.test.ts:133-142`.

| Guard | Compiling mutation printed | Red | Restored green |
| --- | --- | --- | --- |
| Recognised reservation selection | `runtime/workers/trailer-slot.ts:66`: invert the filename condition so recognised files are skipped | Low-level test returned `unknown` instead of `dispatch`; host test observed the file still present | 31/31 runtime tests; targeted host test 1/1 |

The mutation compiled, ran, and returned the old wrong answer. It did not crash. The restored condition was printed again at `runtime/workers/trailer-slot.ts:66` before the green run.

### Validation

- `bun test runtime/workers/trailer-slot.test.ts runtime/workers/project-runners.test.ts`: 31 pass, 0 fail.
- `bun test open/__tests__/project-build-wiring.test.ts -t 'a later host attempt clears a dead unarmed reservation before rebuilding runners'`: 1 pass, 0 fail.
- `bash scripts/ci/typecheck-all.sh`: 51 configurations, all pass.
- `bash scripts/ci/lint.sh`: all lint gates pass.
- `git diff --check`: pass.
- `bash scripts/ci/leak-gate.sh`: zero findings from the rules that ran, but incomplete because the two local denylist inputs are unavailable.

### Citation corrections and limits

The filed issue cites the terminal `unknown` switch as `trident/build-run.ts:340`; it is now at `trident/build-run.ts:358`. Its predecessor-file condition is resolved: `git ls-tree` against the fetched `origin/main` found `runtime/workers/trailer-slot.ts`, with `runtime/workers/claude-in-repl.ts` and `CONTRIBUTING.md` as positive controls.

Deliberately unchanged: armed reservations are never cleared, worker-side takeover is still forbidden, and no lease, clock, feature flag, alternate dispatch path, or spec decision was added. A real process kill was not verified; the deterministic fixture creates the identical durable bytes at the specified death boundary. The full `open/__tests__/project-build-wiring.test.ts` file could not be green in this sandbox because its pre-existing final test cannot bind a loopback port; in that combined run the other 60 tests passed and only that port-binding test failed. The focused changed case is green as recorded above. The private leak denylist rules could not be verified locally; all available leak checks reported zero findings.
