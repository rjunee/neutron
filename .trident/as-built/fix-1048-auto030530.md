## Issue 1048 — rendered worker budgets share the enforced source

### What changed

The four project-build role walls now have one owner, `PROJECT_BUILD_WALL_MS`, at
`trident/project-build-budget.ts:8-13`. The host places that value into every worker
request at `open/wiring/project-build.ts:286`, while the strategy renderer converts the
build and fix values to minutes at `trident/test-strategy.ts:739-743`. The rendered
instruction therefore names both 90-minute builder ceilings without a second numeric
source.

The timeout remains in the existing suite-outcome vocabulary: an exhausted wall is
`not-run` at `trident/test-strategy.ts:680-681`, and the rendered instruction still
requires `testsPassed=false` at `trident/test-strategy.ts:743`. This change adds no new
outcome or default classification.

### Decisions and invariant

I placed the record in Trident because the renderer already lives there and the Open
wiring may import Trident modules. Milliseconds remain the source unit because that is
the worker-request API unit at `open/wiring/project-build.ts:286`; prose alone converts
to minutes at `trident/test-strategy.ts:739-740`.

The maintained invariant is: worker prose and host enforcement read the same role-budget
record. Imports at `open/wiring/project-build.ts:18` and `trident/test-strategy.ts:40`
maintain it continuously, and the focused assertion at
`trident/test-strategy.test.ts:654-666` pins both the 90-minute decision and the derived
render. The mechanism does not rely on a worker noticing drift.

The filed locations had not moved: the stale prose was at
`trident/test-strategy.ts:732-747` on `origin/main`, and the enforced values were at
`open/wiring/project-build.ts:46-50`. No cited line number required correction.

A whole-tree search for the two old distinctive phrases plus the new template found the
new template at `trident/test-strategy.ts:743` and no old rendered phrase. That new
template was the positive control for the search. A separate complete reference search
enumerated the shared record's definition and consumers in `trident/` and `open/`.

### Mutation table and verification

| Guard | Compiling mutation and landed line | Red | Restored green |
|---|---|---|---|
| `trident/test-strategy.test.ts:654-666` pins both builder walls and their rendered prose | Changed build and fix from 90 to 45; printed at `trident/project-build-budget.ts:11-12` | `bun test trident/test-strategy.test.ts`: 85 pass, 1 fail; expected 90, received 45 | Same command: 86 pass, 0 fail |

Additional final-state checks:

- `bun test open/__tests__/project-build-wiring.test.ts`: 18 pass, 0 fail.
- `bash scripts/ci/typecheck-all.sh`: all 51 configurations passed.
- `bash scripts/ci/lint.sh`: every reported gate passed with zero findings.
- `git diff --check`: passed.

### Deliberately not changed and not verified

No feature flag, alternate path, suite-outcome value, spec decision, or historical record
was added or changed. Plan and review remain 15 minutes at
`trident/project-build-budget.ts:9-10`; build and fix remain 90 minutes at
`trident/project-build-budget.ts:11-12`. I did not run the full suite, as the lane brief
forbids it. I could not verify a live dispatched worker or remote CI in this offline build
lane; the renderer and wiring tests cover the local composition boundaries.
The leak gate reported zero findings from every rule that ran, but exited incomplete
because its two owner-PII checks require an external denylist that is unavailable here;
those two checks could not be verified locally.
