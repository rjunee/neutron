## 2026-09-17 — Retry a run on its own published PR

### What changed

This branch records separately durable publication provenance in `migrations/0153_trident_published_pr_provenance.sql:1`. The initial implementation inferred that provenance from a branch lookup; review reproduced a foreign PR appearing during push. The completed correction takes the number from the successful create receipt and verifies that exact PR (`trident/production-host-effects.ts:384-396`). Observation alone never writes new provenance. Existing recorded provenance allows republication without creating another PR (:392-396).

Dispatch carries the separately recorded value (`trident/board-dispatch.ts:1516-1519`), project composition supplies ownership (`trident/project-build-host.ts:133`), and fresh admission checks number, open state, and head (`trident/build-run.ts:267-270`). Launch discovery remains observational (`trident/launch-preparation.ts:201-204`).

### Decisions and corrected invariant

The branch retains reclaim semantics. Publication continuously enforces the receipt boundary at its durable writer (`trident/production-host-effects.ts:396`); the database preserves the value independently of a failed worker. An observed PR without matching existing provenance is refused (:392-393). A crash before the receipt is persisted remains uncertain and cannot be reclaimed through observation alone.

Direct publication refusals use existing `GateResult` vocabulary (:19-20). `requireAllow` throws them (:446-448), and the enclosing build classifies host exceptions as `unknown` (`trident/build-run.ts:659-661`). The earlier claim that these publication refusals necessarily surfaced as a blocked build was too broad.

The current receipt correction, bidirectional mutation evidence, and current checks are recorded in `.trident/as-built/fix/1142-published-pr-provenance.md`. The tables below retain the initial branch's historical test results and historical line numbers; they do not establish receipt authentication.

### Mutation table

| Guard | Compiling mutation | RED | Restored GREEN |
|---|---|---|---|
| Owned-versus-foreign admission, `trident/build-run.ts:262-265` | Removed `!ownsMeasuredPr`, restoring unconditional fresh refusal; printed lines 258-268 | Owned retry received `blocked` instead of `merged` | Focused owned-retry case: 1 pass, 0 fail |
| Observation is not provenance, `trident/board-dispatch.ts:1516-1519` | Substituted `prior.pr` for `prior.published_pr`; printed lines 1514-1522 | Foreign observed PR produced `published_pr: 73` instead of null | Focused foreign-PR case: 1 pass, 0 fail |
| Provenance carry, `trident/board-dispatch.ts:1519` | Removed the carry spread; printed lines 1514-1521 | Owned retry received null instead of PR 7 | Focused unchanged-tip case: 1 pass, 0 fail |
| Publication persistence, `trident/production-host-effects.ts:389` | Removed `published_pr` from the atomic update; printed lines 385-392 | Published row retained null instead of PR 12 | Focused publication case: 1 pass, 0 fail |

### Verification

`bun test trident/build-run.test.ts trident/board-dispatch.test.ts trident/production-host-effects.test.ts trident/store.test.ts trident/project-build-host.test.ts trident/stranded-salvage-realgit.test.ts`: 523 pass, 0 fail. `bash scripts/ci/typecheck-all.sh`: all 51 configurations passed after the shared and real-git fixtures acquired the new required field. `bash scripts/ci/lint.sh`: every reported gate passed. `git diff --check` passed. The local leak scan reported zero findings from rules that ran, but its external PII denylist was unavailable, so that gate was incomplete rather than clean.

The changed-file list was completely enumerated with `git diff --name-only origin/main...HEAD` plus `git diff --name-only` for the uncommitted layer before staging.

### Deliberately not changed and not verified

No cleanup-on-failure behavior was added, so reviewed work is retained. No `SPEC.md` or spec-item decision changed because this implements the issue's selected durable-provenance reclaim behavior without changing the product target. The historical hosted run and its PR could not be replayed locally because this lane intentionally has no network access.

A whole-tree search for the superseded ownership wording used the same expression for old and replacement phrases. Its positive controls found the replacement provenance wording at `trident/board-dispatch.ts:1516-1519` and the matching regression at `trident/board-dispatch.test.ts:2020-2028`; after replacing the rejected prior record it found no remaining assertion that the observational PR itself proves ownership.
