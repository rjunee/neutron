## 2026-09-16 — remove unreachable review-suite deferred acceptance

### What changed

Removed the subset-only `suiteOutcome === 'deferred'` acceptance from
`trident/gates/review-suite.ts:41-49`. Any nonzero host-observed suite receipt now
reaches the existing `FULL SUITE NOT PROVEN` blocker, except for the existing
evidence-backed `failed-preexisting` advisory at `trident/gates/review-suite.ts:44-47`.
The test at `trident/gates/review-suite.test.ts:12-29` covers every admitted
scope/outcome pair and requires `fix` for every nonzero receipt. The G063 inventory
now matches this result at `docs/trident-gates-inventory.md:137`.

### Decision and evidence

The filed issue's cited condition remained at `trident/gates/review-suite.ts:44`
before this change; its required numeric receipt remained at line 41. The project
composition fixes this gate's scope to `full-suite` at
`open/wiring/project-build.ts:321`; its checkpoint reader extracts a full-suite
command and reports null when none exists at `open/wiring/project-build.ts:345-346`.
Therefore the gate's subset-only allowance could not be reached through this
composition, and moving it before the receipt check would not have supplied the
host evidence that G063 requires.

The distinct workflow-only subset behavior remains at
`trident/inner-workflow.mjs:4714-4718`; it does not use this host-receipt gate, so
it was deliberately not changed.

### Mutation proof

| Guard | Mutation | Result |
| --- | --- | --- |
| Every nonzero host receipt rejects | Reinserted `if (report.suiteOutcome === 'deferred' && value.scope === 'subset') return known()` at `trident/gates/review-suite.ts:44` | `bun test trident/gates/review-suite.test.ts` failed: expected `fix`, received `approve`. |
| Restored deletion | Removed that exact line; confirmed `trident/gates/review-suite.ts:44` proceeds to evidence handling | The same focused test passed: 6 pass, 0 fail. |

### Verification and limits

`bunx tsc -p trident/tsconfig.json --pretty false` and
`bunx eslint trident/gates/review-suite.ts trident/gates/review-suite.test.ts` both
exited 0. `git diff --check` passed. The focused test above is the only test file
run; the full suite was deliberately not run under the lane instruction.

The repository-wide `scripts/ci/typecheck-all.sh` and `scripts/ci/lint.sh` did not
produce final statuses before the local command window ended, so their complete
results could not be verified here. No `SPEC.md` decision changed, and no feature
flag, fallback path, or project-wiring change was added.
