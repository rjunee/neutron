## 2026-09-16 — Cover local merge refusal paths

### What changed

`trident/gates/local-merge.test.ts:41-71` now centralizes the real local-worktree, no-PR run fixture. The review-readiness case at `trident/gates/local-merge.test.ts:82-90` proves a blocked gate stops in the review phase before reviewer dispatch and leaves the reviewed commit unmerged. The publication-preflight case at `trident/gates/local-merge.test.ts:93-104` proves an unreadable preflight stops as unknown in the publish phase after approval and before merge.

### Existing outcome vocabulary and decisions

No production outcome was added. `trident/build-run.ts:203-209` maps a blocked gate to the existing blocked outcome and an unknown gate to the separate unknown outcome. The review guard at `trident/build-run.ts:531-533` therefore preserves the explicit readiness refusal; the preflight guard at `trident/build-run.ts:486-495` preserves uncertainty as unknown. Local mode reaches publication after approval through `trident/build-run.ts:595-598`, so both paths are reachable without a PR. The filed default-mode citation remains correct at `trident/store.ts:867`; no cited line required correction.

### Mutation evidence

| Guard | Compiling mutation and observed result | Restored result |
| --- | --- | --- |
| `trident/build-run.ts:533` | Appended `&& false`; the focused review-readiness test was RED, returning unexpected unknown review-progress output. | Focused test GREEN. |
| `trident/build-run.ts:495` | Appended `&& false`; the focused preflight test was RED, returning unexpected blocked merge output. | Focused test GREEN. |

### Verification

`bun test trident/gates/local-merge.test.ts` passed 7 tests; its final paired run with `trident/as-built-log.test.ts` passed 12 tests. `bunx tsc -p trident/tsconfig.json` and `bunx eslint trident/gates/local-merge.test.ts --format stylish` passed. The repository-wide lint and broader typecheck matrix were each started but did not yield a final result in this lane window, so neither is claimed as verified. The full test suite was deliberately not run because this lane permits targeted files only.

### Deliberately not changed

No production code, product decision, spec item, feature flag, or parallel path was added. Existing local-merge success coverage remains at `trident/gates/local-merge.test.ts:74-80`.
