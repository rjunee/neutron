## 2026-09-16 — ritual recovery survives a throwing logger

### What changed

`sweepPendingApprovals` catches a per-row render failure and returns `null` so `reraisePending` can expire that bad grant while the loop continues (`reminders/ritual-registration.ts:750-790`). Its recovery report now catches an exception from the injected logger before returning that same `null` (`reminders/ritual-registration.ts:784-790`). A logger fault therefore cannot replace the render failure, prevent the bad row's expiry, or stop later rows from being processed.

The invariant is maintained continuously at the recovery call site by the local `try`/`catch` (`reminders/ritual-registration.ts:784-789`); it does not depend on the failing logger remaining operational. This introduces no new error, verdict, refusal, or state, so there is no outcome vocabulary or default classification to extend. The existing grant lifecycle remains the vocabulary: `reraisePending` receives `null`, and the regression observes the bad grant as `expired` (`reminders/ritual-registration.test.ts:1196-1199`).

### Test and mutation evidence

The new regression installs a logger that throws only after proposal setup, corrupts one real grant's `args_json` to reach the render recovery, and retains a healthy grant in the same batch (`reminders/ritual-registration.test.ts:1181-1194`). It asserts the sweep resolves, the bad grant expires, the healthy grant remains pending, and the healthy ritual has emitted twice (`reminders/ritual-registration.test.ts:1196-1199`). The non-empty registry and malformed persisted field ensure the fixture reaches the protected call rather than removing the field under test.

| Guard | Compiling, reachable mutation | Red evidence | Restored evidence |
|---|---|---|---|
| Logger containment at `reminders/ritual-registration.ts:784-789` | Removed the local `try`/`catch`, leaving the direct `log(...)` call; printed mutation at line 784 | Focused regression ran and failed because `sweepPendingApprovals()` rejected instead of resolving: 0 pass / 1 fail | Printed restored guard at lines 784-789; focused regression: 1 pass / 0 fail |

Final verification: `bunx tsc --noEmit -p reminders/tsconfig.json` and `bunx tsc --noEmit -p tsconfig.json` both exited successfully; `bash scripts/ci/lint.sh` passed every reported gate; `bun test reminders/ritual-registration.test.ts` passed 44 / 44; `git diff --check` passed.

### Evidence, decisions, and scope

The filed issue's cited declaration remains at `reminders/ritual-registration.ts:378-388`, its recovery call remains within `reminders/ritual-registration.ts:777-790`, and the whole sweep remains at `reminders/ritual-registration.ts:750-797`; no filed line number required correction. The defect was verified on the fetched `origin/main` commit before implementation by reading the same option declaration and unprotected call there.

The smaller call-site guard was chosen over changing the callback's type contract: TypeScript cannot enforce that an arbitrary `() => void` implementation does not throw, while the local runtime boundary directly preserves this recovery path. The whole-tree distinctive-phrase search for `ritual approval render failed` enumerated the production call, its regression, and the prior historical record; the historical record stays because it accurately describes the earlier change that introduced the report.

### Deliberately not done and unverified

Other logging calls were not wrapped because the acceptance criterion is specifically the render-failure recovery report (`reminders/ritual-registration.ts:777-790`). No feature flag, parallel implementation, status taxonomy, spec decision, or product behavior beyond exception containment was added. The full test suite was not run, per the lane instruction; only the touched test file was run.

The leak gate found zero issues in the rules that ran but exited 3 because its two external owner-PII denylist checks were unavailable. Owner-PII cleanliness therefore could not be verified locally, and this is not recorded as a clean leak-gate result. No network, push, PR creation, or merge was attempted.
