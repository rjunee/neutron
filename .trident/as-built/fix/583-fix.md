## Issue 583 — board-add confirmation states the completed action

### What changed

The `card_added` rendering now says `Added to work board` while retaining the resolved board label and card title (`work-board/chat-ack.ts:129-142`). Exact-text coverage pins the normal confirmation (`work-board/chat-ack.test.ts:36-42`), title truncation cases (`work-board/chat-ack.test.ts:59-88`), and every board-label fallback (`work-board/chat-ack.test.ts:287-331`).

### Decisions

This is a wording replacement, not a new outcome. It remains the existing `card_added` member of `WorkBoardChatAckKind` (`work-board/chat-ack.ts:43-55`) and the existing exhaustive rendering switch handles it directly (`work-board/chat-ack.ts:129-143`), so no fallback classification changes. The action-first form matches the neighboring `build_dispatched` and `inline_started` confirmations (`work-board/chat-ack.ts:139-142`) while preserving the board-name disambiguation.

The filed production citation had moved: its cited line 126 is now `work-board/chat-ack.ts:138`. The cited test section still begins at `work-board/chat-ack.test.ts:287`.

### Mutation evidence

| Guard | Mutation | Red evidence | Restored evidence |
|---|---|---|---|
| Exact card-add wording at `work-board/chat-ack.test.ts:40-42` | Changed only `work-board/chat-ack.ts:138` back to `On the Work Board` and printed the landed line | `bun test work-board/chat-ack.test.ts --test-name-pattern 'card_added text'` failed: 0 pass, 1 fail | `bun test work-board/chat-ack.test.ts` passed: 27 pass, 0 fail |

### Validation

- `bun test work-board/chat-ack.test.ts`: 27 pass, 0 fail.
- `bash scripts/ci/lint.sh`: all reported gates passed.
- `bash scripts/ci/typecheck-all.sh`: `work-board/tsconfig.json` passed. The full matrix remained red only in unchanged files under `app`, `gateway`, `logger`, and `onboarding`; the root configuration repeated three of those unchanged-file errors.
- `git diff --check`: passed.

### Deliberately not changed

No acknowledgement kind, board-name behavior, deduplication behavior, or delivery path changed. The historical pre-fix sentence at `work-board/chat-ack.test.ts:280-285` remains because it documents the earlier board-name defect. No `SPEC.md` decision changed.
