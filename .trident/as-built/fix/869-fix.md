## 2026-09-15 — Credential-free task prioritization remains deterministic

### What changed

The per-project provider work made the conversational utility substrate constructible whenever a live provider resolver exists, even before any credential is available (`open/wiring/substrates.ts:174-177`). Open composition subsequently treated the substrate's existence as credential availability and built `proactiveLlm` (`open/composer.ts:3384-3388`). On a credential-free boot that gave task prioritization an async LLM function instead of the null value that selects its real deterministic path.

`proactiveLlm` now requires both a constructed substrate and at least one resolved Anthropic or OpenAI credential pool (`open/composer.ts:3384-3388`). This does not choose a provider and does not collapse provider inheritance: the live project/instance selection still resolves independently at `open/composer.ts:1037-1044`. The task cron remains unconditionally enabled at `open/composer.ts:3452`, with the possibly-null LLM passed as data at `open/composer.ts:3455`.

The no-credential composition fixture now saves and clears both credential environment keys (`open/__tests__/open-tasks-wiring.test.ts:42-54`, `open/__tests__/open-tasks-wiring.test.ts:60-73`). It asserts the cron stays enabled and the prioritizer receives null (`open/__tests__/open-tasks-wiring.test.ts:133-146`).

### Decisions and outcome vocabulary

The fix distinguishes three independent facts: a project's explicit selection, a project inheriting the instance selection, and whether the environment has a usable credential. Only the last fact controls whether this background utility receives an LLM function. No unresolved selection is defaulted to a concrete provider.

The existing task-prioritization outcome vocabulary is `PrioritizedBy`, which records `llm` or `deterministic` (`tasks/prioritize-llm.ts:94-100`). Its no-LLM branch writes `prioritized_by: 'deterministic'` and returns that same outcome (`tasks/prioritize-llm.ts:169-178`). Core composition already registers the handler when the enable switch is true and explicitly accepts a null LLM (`gateway/composition/build-core-modules.ts:1324-1355`), so no new outcome or default classification was introduced.

The regression invariant is continuously maintained by the production-composer test at `open/__tests__/open-tasks-wiring.test.ts:133-146`; it does not depend on a provider or credential service being operational because the fixture removes both credential families before composition (`open/__tests__/open-tasks-wiring.test.ts:69-71`).

### Mutation table

| Guard | Mutation | Red | Restored green |
|---|---|---|---|
| Credential presence is required before constructing `proactiveLlm` (`open/composer.ts:3385-3386`) | Removed the pool condition, leaving only `llmCallSubstrate !== null`; the printed mutation landed at `open/composer.ts:3385` | The named no-credential test failed at `open/__tests__/open-tasks-wiring.test.ts:144`, receiving an async function instead of null | The uniquely anchored restoration returned the named test to 1 pass, 0 fail |

An initial restoration matched the similar onboarding condition at `open/composer.ts:1926`; printing the diff exposed the wrong landing site. That edit was reverted, the intended proactive block was restored with unique context, and the final source lines were printed before the green run.

### Verification

- `bun test open/__tests__/open-tasks-wiring.test.ts`: 8 pass, 0 fail.
- The six test files enumerated from the original commit with `git diff-tree --no-commit-id --name-only -r HEAD | rg '(test\\.ts|__tests__)'`: 161 pass, 0 fail. The earlier commit message recorded 117; the current files contain more tests.
- `bun test open/__tests__`: 784 pass, 91 fail across 117 files. Observed failures include listener creation refused with `EADDRINUSE` even for port 0 and one unrelated memory-index test timing out at 20 seconds. The focused composition suite passes in the same environment.
- `bunx tsc -p open/tsconfig.json --noEmit`: pass.
- `bash scripts/ci/typecheck-all.sh`: 50 of 51 configurations pass, including `open/`, `gateway/`, `runtime/`, and `tasks/`; `app/tsconfig.json` fails because its implicit `@types` definition cannot be found.
- `bash scripts/ci/lint.sh`: pass.

### Deliberately not changed

No provider fallback was added, no cron switch was made credential-dependent, and no project selection or instance-inheritance behavior changed. The surrounding-directory listener and timeout failures are outside this change and their tests were not weakened or skipped. `SPEC.md` and the spec item were not changed because this follow-up restores the already-required deterministic behavior rather than changing a product decision.

### Migration collision follow-up

The branch was rebased onto current `origin/main` and was already up to date. Because the concurrently allocated 0145 must remain untouched, this change's provider migration moved to the next usable ordinal, 0146; its SQL body remains the additive, constrained `projects.model_provider` column (`migrations/0146_project_model_provider.sql:1-5`). The migration directory was enumerated with `find migrations -maxdepth 1 -type f -name '*.sql' -printf '%f\\n' | sort`; its highest base migration was 0144 and the same filename grep matched `0146_project_model_provider.sql` as the positive control while finding no 0145 copy in this branch.

The fresh-install applied-version vocabulary is the exhaustive array in `migrations/runner.test.ts`; it now classifies 146 as applied and expects no skipped migrations (`migrations/runner.test.ts:204-214`). The live-ledger repair fixture's two exhaustive later-migration outcomes also include 146 (`migrations/__tests__/live-ledger-125-repair.test.ts:88-94`, `migrations/__tests__/live-ledger-125-repair.test.ts:172-177`). This introduces no new runtime outcome: the runner continues to report the migration through its existing `applied` array.

The migration changes the schema, so the pinned snapshot was regenerated and now records the nullable constrained column (`migrations/expected-schema.txt:1267-1272`). The migration runner continuously maintains that column constraint in SQLite; it does not depend on the settings writer remaining operational.

| Guard | Mutation | Red | Restored green |
|---|---|---|---|
| Exhaustive fresh-install ordinal list (`migrations/runner.test.ts:204-212`) | Replaced expected 146 with 145; printed landing line 212 | Named fresh-apply test failed with received 146 versus expected 145 | Named test: 1 pass, 0 fail |
| Generated schema snapshot (`migrations/expected-schema.txt:1271-1272`) | Restored the pre-column snapshot text; printed landing lines 1266-1272 | Named snapshot test failed at line 1271 with observed `model_provider` | Named test: 1 pass, 0 fail |

Verification after restoration: migration suites passed 211 tests across 25 files; the seven original test files, enumerated with `git diff --name-only origin/main...HEAD | rg '(test\\.ts|__tests__)'`, passed 169 tests. `bash scripts/ci/lint.sh` passed. The 51-project typecheck matrix passed the changed `gateway`, `migrations`, `open`, and `runtime` projects but remained red solely at `app/tsconfig.json` because the implicit `@types` definition is unavailable, the same environment failure recorded above. The leak gate reported zero findings from the rules it could run, but remained incomplete because its local PII denylist was unavailable.

The other change's migration was not edited. No migration collision tolerance was added. `SPEC.md` and the spec item remain unchanged because ordinal allocation and snapshot repair do not alter a product decision.

### Credential-free production boot follow-up

The reported production suite reproduced with 2 pass and 1 fail: composition returned a Trident object instead of leaving it unset. The cause was the first failure family (availability conflation), specifically the presence of a resolver being treated as readiness before any selection was resolved. The composer supplies the live resolver (`open/composer.ts:1039-1042`), and the former construction condition accepted its existence. The resulting live substrate enables the fire seam (`open/composer.ts:1145-1148`) and the returned Trident surface (`open/composer.ts:7041-7044`). Boot was not throwing a resolution refusal.

The shared construction condition now requires an Anthropic pool or fully wired OpenAI capability (`open/wiring/substrates.ts:108-111`, `open/wiring/substrates.ts:159-165`). The provider option bag continues to pass the live resolver unchanged (`open/wiring/substrates.ts:122-129`). This fixes the construction class rather than adding another caller-specific condition. The existing outcome vocabulary is `Substrate | null` (`open/wiring/substrates.ts:52-56`); the null branch maps to the existing absent fire seam (`open/composer.ts:1145-1148`) and omitted Trident surface (`open/composer.ts:7041-7044`). No new error or classification was introduced.

The construction condition maintains the boot invariant independently of the resolver or provider running. Three fixtures cover credential-free application, instance, and project selections (`open/__tests__/open-wiring-substrates.test.ts:1062-1078`). The production boot fixture now clears and restores the OpenAI key as well (`open/__tests__/open-trident-prod-boot-wiring.test.ts:58`, `open/__tests__/open-trident-prod-boot-wiring.test.ts:85-89`, `open/__tests__/open-trident-prod-boot-wiring.test.ts:1399-1406`). Existing credentialed dispatch checks remain positive controls, including live Responses-to-Claude switching (`open/__tests__/open-wiring-substrates.test.ts:751-772`).

| Guard | Mutation | Red | Restored green |
|---|---|---|---|
| Shared credential availability, `open/wiring/substrates.ts:163` | Restore the prior resolver-or-provider-or-Anthropic condition; printed the mutated line 163 | All three new selection cases and the production credential-free boot case failed: 0 pass, 4 fail | Restored line 163 printed; same filter: 4 pass, 0 fail |

Verification rows:

| Run | Result |
|---|---|
| Original 272 plus three new cases and three production boot tests | 278 pass, 0 fail across 13 files |
| Related task, agent-dispatch, arbiter, and leak-fixer composition suites | 17 pass, 0 fail across 4 files |
| Changed substrate and production boot suites, before mutation | 42 pass, 0 fail |

The original set was enumerated from the two validation lists in `.trident/as-built/issue-869.md`: `open/__tests__/instance-model-provider.test.ts`, `gateway/wiring/__tests__/build-llm-call-substrate-provider.test.ts`, `open/__tests__/open-wiring-substrates.test.ts`, `runtime/adapters/select-substrate.test.ts`, `gateway/__tests__/app-projects-surface.test.ts`, `gateway/projects/__tests__/sqlite-store.test.ts`, `tests/integration/identity-env-readers-registry.test.ts`, `migrations/runner.test.ts`, `migrations/__tests__/live-ledger-125-repair.test.ts`, `migrations/snapshot.test.ts`, `migrations/__tests__/table-ownership-conformance.test.ts`, and `scripts/__tests__/spec-items-index.test.ts`. The added production suite is `open/__tests__/open-trident-prod-boot-wiring.test.ts`.

Corrected prose was searched across TypeScript and Markdown with `rg -n 'SELECTED provider.s|When NOT|Compose is LLM-only — no provider|Credential availability and provider selection' . --glob '*.ts' --glob '*.md'`. The positive control was the new construction comment at `open/wiring/substrates.ts:159`. The other hit at `open/wiring/landing.ts:128` concerns its own provider-specific wiring and stays unchanged.

Deliberately did not change provider resolution, choose a provider for unresolved settings, add flags, alter credential provisioning, or change the spec decision. This restores the required credential-free boot behavior. The build lane uses the requested branch-specific as-built path and local-commit-only delivery.

Static validation: `bun run typecheck` reports no script. `bash scripts/ci/typecheck-all.sh` checked 51 configurations: 50 passed, including `open/` and the root; `app/tsconfig.json` retains the previously recorded TS2688 missing implicit `@types` definition. `bash scripts/ci/lint.sh` and `git diff --check` passed. The leak gate reported zero findings from runnable rules and INCOMPLETE for missing private denylist inputs. The shard retains exactly one level-two heading.
