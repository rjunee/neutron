## Issue 601 — web usage hairline moved to the composer; remaining inputs blocked

### What changed

The shell continues to own and poll the active usage reading at `landing/chat-react/ProjectShell.tsx:367-398`, but now passes that reading into the chat at `landing/chat-react/ProjectShell.tsx:815-820`. The meter is rendered immediately before the composer at `landing/chat-react/ChatApp.tsx:2433-2434`; the previous shell-seam render is absent between the top bar and stage at `landing/chat-react/ProjectShell.tsx:788-802`. The tab band regained its ordinary border and the composer surrendered its old border so the two-line meter remains the only composer hairline (`landing/chat-react.html:273-278`, `landing/chat-react.html:1537-1538`). The component documentation now names its actual placement (`landing/chat-react/UsageMeter.tsx:4-18`).

The new shell assertion checks both directions: the meter is a child of the chat thread and its immediate next sibling is the composer, while no meter follows the top bar (`landing/chat-react/__tests__/project-shell.test.tsx:194-204`). The existing meter suite still establishes the independent rendering semantics: two ordered lines, unknown-without-fill, threshold colors, widths, zero visibility, and clamping (`landing/chat-react/__tests__/usage-meter.test.tsx:44-98`).

### Decisions

Polling remains in `ProjectShell`, because its effect already survives visits to non-chat tabs (`landing/chat-react/ProjectShell.tsx:367-398`); only presentation moved. `ChatApp` accepts an optional reading with the existing unknown value as its default so standalone component harnesses preserve the unavailable state rather than inventing usage (`landing/chat-react/ChatApp.tsx:2749-2783`). This adds no new outcome vocabulary: unavailable usage continues to use `UsagePayload` and `USAGE_UNKNOWN`, whose existing component branch renders tracks without fills (`landing/chat-react/UsageMeter.tsx:45-76`).

The filed web citation was nearly current: the explanatory comment now began at line 799 and the meter at line 802 before this change. No product specification or `SPEC.md` decision changed.

### Mutation evidence

| Guard | Mutation and printed landing line | Red result | Restored result |
|---|---|---|---|
| Meter immediately precedes composer | Swapped the two valid JSX siblings; the print showed composer at `landing/chat-react/ChatApp.tsx:2433` and meter at `landing/chat-react/ChatApp.tsx:2434` | `landing/chat-react/__tests__/project-shell.test.tsx:203` failed: expected the meter's next sibling to carry `car-composer-wrap`, received no next sibling; 10 pass, 1 fail | Meter restored to `landing/chat-react/ChatApp.tsx:2433`, composer to `:2434`; 11 pass, 0 fail |

### Verification

- `bun test landing/chat-react/__tests__/project-shell.test.tsx` — 11 pass, 0 fail after restoration.
- `bun test landing/chat-react/__tests__/usage-meter.test.tsx` — 6 pass, 0 fail.
- `bunx tsc -p landing/chat-react/tsconfig.json --noEmit` — pass. The root package defines no `typecheck` script (`package.json:54-63`), so the touched React leaf's CI command was run directly as identified by `landing/chat-react/tsconfig.json`.
- `bash scripts/ci/lint.sh` — all repository lint gates pass.
- `git diff --check` — clean.

### Deliberately not changed

Three filed items remain unbuilt because their required inputs are unavailable. The in-repo icon research says existing project emoji are owner-chosen and forbids implementation until the owner decides whether they remain or are migrated (`docs/research/bundled-icon-pack-2026-08-10.md:107-113`). The app manifest identifies the current raster assets (`app/app.json:14-37`), but the filed reference image was not supplied. The task requires the planner script to be lifted verbatim, while a search across every local and remote-tracking ref found no matching bridge source. That absence search used the same `planner.?bridge|planner bridge` expression against the supplied brief as a positive control, where it matched line 6.

No substitute icon migration policy, invented app artwork, or hand-recreated planner script was added. No feature flag or parallel implementation path was introduced.
