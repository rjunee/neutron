## 2026-09-17 — Recover publication from durable pre-creation proof

### Change and evidence

Publication now creates and validates a UUID token, persists it before the external create, and places an exact token-bound HTML comment in the PR body (`trident/production-host-effects.ts:19-20`, `:385-404`). The run row carries that pre-creation evidence through migration `migrations/0154_trident_publication_token.sql:1` and the store read/write paths (`trident/store.ts:243-247`, `:516-526`, `:591-600`, `:1676-1685`, `:2249-2255`). The schema snapshot records the installed column at `migrations/expected-schema.txt:666`.

On a later round, an observed PR without final `published_pr` provenance is adopted only when its body contains the exact durable token; a missing, malformed, or different token cannot cross that boundary (`trident/production-host-effects.ts:405-410`). PR observation now requests and validates `body`, while the ordinary build snapshot deliberately retains its prior number/head/state shape (`trident/production-host-effects.ts:184-203`, `:217-218`). The recovery regression simulates a successful create followed by a persistence crash, then proves the next call completes without a second create (`trident/production-host-effects.test.ts:255-274`). The same-branch foreign-PR regressions remain refusals (`trident/production-host-effects.test.ts:242-253`, `:276-286`).

The invariant is maintained continuously by the durable row write before `gh pr create` and by exact proof comparison on recovery (`trident/production-host-effects.ts:388-408`). It does not require the process that created the PR to survive. The existing `GateResult` vocabulary is `allow`, `blocked`, and `unknown` (`trident/build-run.ts:37-38`): a known proof mismatch is `blocked` (`trident/production-host-effects.ts:405-408`), while malformed or unpersisted evidence is `unknown` (`:389-390`). The adapter's default treatment of either non-allow value is to throw rather than publish successfully (`trident/production-host-effects.ts:463-465`).

### Decisions

I selected a random body-bound token rather than adopting from branch/head/base observation. The tuple remains observable and can be shared by a foreign PR; the token is generated and durably recorded before creation, so only the PR created with that prepared body can satisfy recovery (`trident/production-host-effects.ts:388-398`). Existing finalized provenance remains authoritative and does not require the token path (`:405`). This replaces the permanent wedge with one recovery path; it does not introduce a flag or retain a second adoption path.

The earlier immutable as-built record still describes the old gap at `.trident/as-built/fix/1142-published-pr-provenance.md:9`. A whole-tree phrase search found that historical record as the only surviving statement; it stays because merged as-built records are immutable under `docs/process/work-tracking.md:131-137`, and this record supersedes the behavior rather than rewriting history. No `SPEC.md` decision or repository spec item changed.

### Mutation table

| Guard | Compiling mutation and printed line | RED result | Restored GREEN |
|---|---|---|---|
| Token shape, `trident/production-host-effects.ts:19` | Replaced the UUID expression with `/^.*$/`; printed line 19 | malformed-intent test received `allow` instead of `unknown` | focused recovery/refusal set: 5 pass, 0 fail |
| Pre-create durability, `trident/production-host-effects.ts:390` | Inverted `!await store.update` to `await store.update`; printed line 390 | failed-persistence test received `allow` instead of `unknown` | focused recovery/refusal set: 5 pass, 0 fail |
| Exact PR proof, `trident/production-host-effects.ts:406-408` | Inverted `!pr.body.includes` to `pr.body.includes`; printed lines 403-410 | foreign PR received `allow` instead of `blocked` | recovery plus foreign test: 2 pass, 0 fail |
| Readable body evidence, `trident/production-host-effects.ts:199-202` | Removed the string-body clause; printed lines 196-204 | malformed observation became `known` instead of `unknown` | focused recovery/refusal set: 5 pass, 0 fail |

### Verification

`bun test trident/production-host-effects.test.ts migrations/snapshot.test.ts` passed 80 tests with 0 failures. `bash scripts/ci/typecheck-all.sh` passed all 51 TypeScript configurations. The initial matrix run exposed the two complete-row fixture defaults that the new required field needed; those defaults are now explicit at `trident/testing/make-trident-run.ts:32-35` and `trident/stranded-salvage-realgit.test.ts:205-211`, and the full matrix was rerun green.

The filed brief's cited implementation lines `381-396` were still correct before editing; no cited line needed correction. The landing-branch probe used `git grep` against `origin/main`: the defect matched at `trident/production-host-effects.ts:381-396`, with positive controls `publishChecked` at `:363` and the foreign-PR regression at `trident/production-host-effects.test.ts:240`. A post-change combined search for both the old and new refusal strings found the new runtime and tests at `trident/production-host-effects.ts:408` and `trident/production-host-effects.test.ts:249,283`, and no old runtime string; the new string was the positive control for the search shape.

### Deliberately not done and limits

I did not change finalized-provenance carry, merge behavior, dispatch identity, or `SPEC.md`. I did not run the full repository test launcher, as the lane instructions prohibit it. I could not verify a real hosted PR create or crash because this lane has no network access; the host-command fixture exercises the exact body file, create receipt, discovery, and persistence sequence at `trident/production-host-effects.test.ts:23-88`. The leak gate found zero issues in every rule it could run, but its local PII denylist was unavailable, so that portion could not be verified.
