## Issue 1132 — change-facing project PR publication

### What changed

Project publication is now materialized after the reviewed snapshot is known. The production contract accepts an asynchronous publication source at `trident/production-host-effects.ts:22-35`, and the publish effect resolves it before pushing or creating a PR at `trident/production-host-effects.ts:363-384`.

The project composition validates the completed plan and the newest build/fix result matching the reviewed head at `open/wiring/project-build.ts:371-384`. It derives a heading-free title from `branchBrief`, falling back to `topTask`, and writes a body containing the change summary, worker-reported commit, test and mutation evidence, plus the original card in a collapsed section at `open/wiring/project-build.ts:385-396`.

The focused fixture drives a dispatch-instruction card and a change-facing branch brief at `open/__tests__/project-build-wiring.test.ts:132-146`, then asserts the title, commit, evidence, and retained card at `open/__tests__/project-build-wiring.test.ts:148-157`.

### Decisions

Publication remains host-owned and is composed only from schema-validated worker payloads. Forge selection checks the result envelope's head against the reviewed snapshot at `open/wiring/project-build.ts:376-384`; fix is checked before build so the latest repair evidence wins.

Missing, malformed, or stale publication evidence throws inside `publishChecked`; its existing catch maps the failure to the `GateResult` `unknown` vocabulary at `trident/production-host-effects.ts:363-390`. It does not share the `blocked`/refusal path, so inability to find out remains distinct from a known negative answer.

The original card is retained as supporting context rather than copied into the reviewer-facing summary. The `<details>` body section at `open/wiring/project-build.ts:394` keeps the design document reachable while the summary leads with completed work.

### Mutation table

| Guard | Mutation | Red proof | Restored proof |
|---|---|---|---|
| Plan-derived publication summary, `open/wiring/project-build.ts:385` | Replaced it with `run.task`; printed the landed line before running | Focused publication test failed: expected `Omit empty failure reasons from wakeup logs`, received `DISPATCH THIS THROUGH TRIDENT` | Same focused test passed 1/1 after restoration |

`bunx tsc --noEmit` passed. The focused publication test passed 1/1; publication effect tests passed 9/9; the two other affected contract files passed 30/30. A broader combined run reached 118 passing tests before an unrelated existing REPL test could not bind its locally derived sink port.

### Citation corrections

The filed issue cited `open/wiring/project-build.ts` without numeric lines. On `origin/main`, the verbatim body write was at line 371 and the card-first-line title was at line 379; no filed numeric citation required correction.

### Deliberately not done and unverified

`SPEC.md` was not changed because the product decision did not change; this fixes publication content to meet the filed acceptance criteria. The separate persisted-request helper at `trident/project-publication-source.ts:10-31` was not repurposed because whole-tree usage enumeration found it imported only by `trident/project-policy-sources.test.ts:7`; as a positive control, the same search found its declaration at `trident/project-publication-source.ts:12`.

The full suite was not run, as the lane brief prohibits it. The unrelated REPL sink test was not verified green because a local port was already occupied; all publication-specific assertions in the touched test file were run independently and passed.
