## #646 — second-pass CodeQL classification and manifest ReDoS repair

### Result and enumeration

The supplied table has 46 rows. I enumerated it with `jq -r '.[] | [.number,.rule,.path,.start_line] | @tsv' ../briefs/codeql-alerts-table.json`, sorted no rows, and reconciled the 46 alert numbers below against that output. The first pass had already fixed alert 9, classified alerts 10, 11, 12, and 21, and identified alert 47 as real but deferred; its record leaves the other 40 explicit (`.trident/as-built/fix/646-codeql-alert-triage.md:23-64`). This pass resolves those 40 as 32 additional false positives and 8 real deferred defects, and fixes the already-confirmed alert 47. “Real, deferred” is not a dismissal recommendation; it remains separate from false positive.

The fix rule was: repair the exponential expression whose replacement is local, grammar-preserving, and covered through the existing schema suite; retain polynomial sites whose safe repair belongs to a different parser or input-boundary contract. Accordingly alert 47 is fixed. No alert is suppressed or dismissed, because repository code cannot supply the per-alert disposition and dismissal remains the owner's action.

### Per-alert decisions

| Alert | Rule | Decision | Input, caller/control, and bound evidence |
|---:|---|---|---|
| 22 | `js/bad-tag-filter` | false positive | External Gmail HTML reaches `stripHtmlToText`, but its result is explicitly plaintext (`cores/free/email/src/mime.ts:80-92`) and the only production caller assigns it to `body_text` (`cores/free/email/src/mime.ts:180-187`), not an HTML sink. |
| 17 | `js/double-escaping` | false positive | Entity decoding creates literal plaintext after tags are removed (`cores/free/email/src/mime.ts:102-117`); the result remains `body_text` (`cores/free/email/src/mime.ts:180-187`). Double HTML interpretation is not this function's contract. |
| 20 | `js/incomplete-multi-character-sanitization` | false positive | Same plaintext path as 17: remaining tags are removed before entity decoding (`cores/free/email/src/mime.ts:102-117`) and the result is assigned to `body_text` (`cores/free/email/src/mime.ts:180-187`). |
| 19 | `js/incomplete-multi-character-sanitization` | false positive | Same plaintext path as 22: style removal is text extraction (`cores/free/email/src/mime.ts:80-97`), and the result is `body_text` (`cores/free/email/src/mime.ts:180-187`). |
| 18 | `js/incomplete-multi-character-sanitization` | false positive | Same plaintext path as 22: script removal is text extraction (`cores/free/email/src/mime.ts:80-97`), and the result is `body_text` (`cores/free/email/src/mime.ts:180-187`). |
| 21 | `js/incomplete-multi-character-sanitization` | false positive (first pass) | Repository-owned SVG is transformed only inside a paint test (`landing/__tests__/favicon-serving.test.ts:129-145`). |
| 13 | `js/incomplete-sanitization` | false positive | A stored topic is escaped for a Markdown table cell, capped to 60 characters, and placed in `card.body_md` (`cores/free/research/src/chat-commands.ts:272-287`). This is Markdown formatting, not JavaScript-string sanitization. |
| 14 | `js/incomplete-sanitization` | false positive | Owner-entered onboarding values are converted to one prompt line and capped at 240 characters (`onboarding/interview/agent-name-suggester.ts:345-370`); no executable-language sink exists at the reported transform. |
| 15 | `js/incomplete-sanitization` | false positive | Owner corrections are converted to one prompt line and capped at 240 characters (`onboarding/interview/personality-character-suggester.ts:399-413`); no executable-language sink exists at the reported transform. |
| 16 | `js/incomplete-sanitization` | false positive | Owner text is converted to one prompt line and capped at 240 characters (`onboarding/persona-gen/summarize.ts:187-200`); no executable-language sink exists at the reported transform. |
| 9 | `js/incomplete-url-scheme-check` | real, fixed in first pass | URL-bearing attributes now reject every `data:` scheme (`landing/chat-react/HtmlDoc.tsx:73-95`). |
| 1 | `js/insecure-randomness` | false positive | `selectCredential` uses `Math.random` only to choose an already-existing available credential (`runtime/credential-pool.ts:215-248`); the reported import caller then reads that selected credential's existing secret (`gateway/wiring/build-import-substrate.ts:300-345`). No secret, token, or nonce is generated. |
| 2 | `js/insecure-randomness` | false positive | Same import call as 1: the random value is a non-security load-balancing index (`runtime/credential-pool.ts:243-248`); the reported OAuth-kind check merely decides whether to refresh the selected credential (`gateway/wiring/build-import-substrate.ts:327-345`). |
| 3 | `js/insecure-randomness` | false positive | The LLM caller selects an existing pool member (`gateway/wiring/build-llm-call-substrate.ts:177-186`); randomness only implements the pool's documented `random` strategy (`runtime/credential-pool.ts:208-248`), not credential generation. |
| 4 | `js/insecure-randomness` | false positive | Same call as 3: the selected member's kind controls OAuth refresh (`gateway/wiring/build-llm-call-substrate.ts:185-198`); the random value only chose the member (`runtime/credential-pool.ts:243-248`). |
| 5 | `js/insecure-randomness` | false positive | A later OpenAI dispatch reads the selected existing secret (`gateway/wiring/build-llm-call-substrate.ts:1358-1376`); selection randomness is only a pool index (`runtime/credential-pool.ts:243-248`). |
| 6 | `js/insecure-randomness` | false positive | A later CLI dispatch likewise passes the selected existing secret (`gateway/wiring/build-llm-call-substrate.ts:1377-1388`); selection randomness is only a pool index (`runtime/credential-pool.ts:243-248`). |
| 7 | `js/insecure-randomness` | false positive | The pool key contains the selected credential's identifier, explicitly never its secret (`runtime/adapters/claude-code/persistent/pool.ts:275-303`). Random pool selection therefore changes a non-security routing identity; it does not create an access value. |
| 8 | `js/insecure-randomness` | false positive | The current spawn path creates the session identifier with `randomUUID` and the security-sensitive channel name with 16 cryptographic random bytes (`runtime/adapters/claude-code/persistent/spawn.ts:115-136`). The only `Math.random` feeding this dataflow is credential load balancing (`runtime/credential-pool.ts:243-248`), not either identifier. |
| 11 | `js/insufficient-password-hash` | false positive (first pass) | SHA-256 creates a stable credential identity fingerprint, not a password verifier (`runtime/adapters/claude-code/persistent/repl-session.ts:704-714`). |
| 12 | `js/insufficient-password-hash` | false positive (first pass) | SHA-256 hashes the request body before HMAC-SHA-256 authenticates it (`runtime/internal-signature.ts:41-52`); this is request signing, not password storage. |
| 25 | `js/polynomial-redos` | false positive | `GatewayHttpClient` receives `base_url` as constructor configuration and strips trailing slashes without a pre-regex bound (`client-core/index.ts:58-75`). Its enumerated production consumers are the app and web client subclasses found by `rg -n 'extends GatewayHttpClient' app landing`; this is operator/app configuration, not attacker input. |
| 26 | `js/polynomial-redos` | real, deferred | `importSharedProjectMemoryOnJoin` composes `projectId` and host authority, then applies the expression once per imported page/edge (`connect/shared-project-memory-mirror.ts:363-400`, `connect/shared-project-memory-mirror.ts:330-355`). Join metadata can be supplied by the remote project side and has no local length bound before normalization. |
| 27 | `js/polynomial-redos` | false positive | `getUnifiedProjects` receives each workspace `base_url` as caller-provided configuration (`connect/unified-project-list.ts:25-50`) and strips slashes before an authenticated fetch (`connect/unified-project-list.ts:200-226`). There is no pre-regex bound, but the value is configured infrastructure, not attacker input. |
| 28 | `js/polynomial-redos` | false positive | The calendar dispatcher passes the authenticated user's chat body to `parseCalCommand` (`gateway/cores/calendar-wiring.ts:88-104`); the expression has no pre-match length cap (`cores/free/calendar/src/chat-commands.ts:112-122`), but the controller is the owner issuing `/cal`, not an attacker. |
| 29 | `js/polynomial-redos` | real, deferred | A calendar event description from the provider is passed by the pre-meeting dispatcher (`gateway/cores/calendar-wiring.ts:237-250`) to the unbounded per-line agenda expression (`cores/free/calendar/src/backend.ts:934-946`). An external event organizer controls this content. |
| 30 | `js/polynomial-redos` | false positive | Authenticated-owner reminder text reaches the unbounded `tomorrow` expression (`cores/free/reminders/src/chat-commands.ts:457-466`). The owner controls it; no attacker path is established. |
| 31 | `js/polynomial-redos` | false positive | Authenticated-owner reminder text reaches the unbounded `next weekday` expression (`cores/free/reminders/src/chat-commands.ts:469-480`). The owner controls it; no attacker path is established. |
| 32 | `js/polynomial-redos` | false positive | Authenticated-owner reminder text reaches the unbounded month/day expression (`cores/free/reminders/src/chat-commands.ts:483-496`). The owner controls it; no attacker path is established. |
| 33 | `js/polynomial-redos` | false positive | Authenticated-owner reminder text reaches the unbounded `at` expression (`cores/free/reminders/src/chat-commands.ts:499-507`). The owner controls it; no attacker path is established. |
| 34 | `js/polynomial-redos` | real, deferred | The research model's response reaches `extractJson`; its fence regex runs before any local response cap (`cores/free/research/src/backend.ts:436-453`). The response is influenced by caller-provided sources and query embedded in the prompt (`cores/free/research/src/backend.ts:402-418`), so hostile source content can influence unbounded model output. |
| 35 | `js/polynomial-redos` | real, deferred | A third-party Core author's package name reaches `packageNameToSlug` without a pre-normalization bound (`cores/runtime/loader.ts:61-80`). The caller reads that value from the Core's package manifest (`cores/runtime/loader.ts:83-90`); a malicious package author controls it. |
| 36 | `js/polynomial-redos` | false positive | The onboarding owner supplies the non-curated archetype name used as the cache slug (`onboarding/archetypes/library.ts:157-170`); normalization has no pre-regex bound (`onboarding/archetypes/library.ts:274-282`), but no attacker controls this field. |
| 37 | `js/polynomial-redos` | false positive | The authenticated onboarding owner's freeform option reaches `parseBareOptionNumber` through the two engine handlers enumerated by `rg -n 'parseBareOptionNumber\(' onboarding`: `onboarding/interview/engine-agent-name.ts:109` and `onboarding/interview/engine-persona.ts:197`. There is no pre-regex length cap (`onboarding/interview/engine-internals.ts:1465-1472`), but it is owner input. |
| 38 | `js/polynomial-redos` | false positive | The only three patterns supplied to `matchAfterPhrase` are module constants at `onboarding/interview/extract-agent-name.ts:119-151`; the helper runs them against onboarding-owner text (`onboarding/interview/extract-agent-name.ts:100-117`, `onboarding/interview/extract-agent-name.ts:215-229`). Input is unbounded before matching, but not attacker-controlled and the regex argument is not caller-controlled. |
| 40 | `js/polynomial-redos` | real, deferred | `entitySlugify` normalizes the full input before its 80-character output cap (`runtime/entity-slug.ts:24-38`). The gateway indexer passes GBrain page owner/name fields (`gateway/wiring/build-project-page-indexer.ts:53`), which can derive from model-extracted external email or collaborator content; no pre-regex input cap exists. |
| 41 | `js/polynomial-redos` | false positive | `sanitizeToSlug` receives an owner-selected agent name from the slug picker (`runtime/slug-picker-types.ts:76-87`); it has only a post-regex 31-character cap (`runtime/slug-grammar.ts:157-167`). It is unbounded owner input, not attacker input. |
| 42 | `js/polynomial-redos` | real, deferred | `runExtraction` feeds model output into `parseExtraction` (`scribe/extract.ts:205-226`), whose fence regex sees the complete response before any cap (`scribe/extract.ts:309-329`). Scribe inputs include user turns and flattened email/calendar rows (`scribe/index.ts:156-185`), so hostile external content can influence that output. |
| 43 | `js/polynomial-redos` | real, deferred | `stripBoilerplate` applies the H1 expression to every compiled-truth line without a line cap (`scribe/reflect/jaccard.ts:136-151`). Its caller tokenizes candidate pages for deduplication (`scribe/reflect/jaccard.ts:304-319`); compiled truth can include externally sourced scribe facts, so a hostile source can influence an unbounded line. |
| 44 | `js/polynomial-redos` | real, deferred | The same unbounded external compiled-truth line reaches the generated-body expression immediately after the H1 check (`scribe/reflect/jaccard.ts:136-151`), through the deduplication caller (`scribe/reflect/jaccard.ts:304-319`). |
| 45 | `js/polynomial-redos` | false positive | The projection writer reads the owner's existing `STATUS.md` and passes it to `replaceMarkedBlock` (`tasks/projection/write.ts:165-184`); trailing whitespace is unbounded before the expression (`tasks/projection/parse.ts:70-81`) but controlled by the owner, not an attacker. |
| 46 | `js/polynomial-redos` | false positive | `composeBlock` trims the generated task projection body (`tasks/projection/parse.ts:84-86`), called only by `replaceMarkedBlock` (`tasks/projection/parse.ts:70-72`). The production writer constructs that body from the owner's task store (`tasks/projection/write.ts:160-183`); no attacker-controlled input path is established. |
| 47 | `js/redos` | real, fixed | A Core author controls capability strings accepted by the open manifest contract (`cores/sdk/manifest.ts:35-48`). The old nested separator regex could backtrack exponentially. `isCapabilityFormat` now scans each character once and `CapabilitySchema` uses it (`cores/sdk/manifest.ts:51-100`). |
| 23 | `js/stack-trace-exposure` | false positive | `jsonResponse` only serializes the body its caller supplied (`gateway/http/surface-kit.ts:107-117`). The sole production `err.stack` under `gateway/http`, enumerated by `rg -n 'err\.stack' gateway/http --glob '*.ts'`, is sent to the logger (`gateway/http/app-admin-surface.ts:368-378`), not to `jsonResponse`. |
| 24 | `js/stack-trace-exposure` | false positive | The resume handler serializes caught `Error.message`, not `Error.stack` (`gateway/upload/import-resume-handler.ts:123-140`, `gateway/upload/import-resume-handler.ts:218-253`, `gateway/upload/import-resume-handler.ts:273-294`). Its local serializer only stringifies that body (`gateway/upload/import-resume-handler.ts:345-349`). |
| 10 | `js/xss-through-dom` | false positive (first pass) | The DOM is sanitized before import: forbidden elements, handlers, and dangerous URLs are removed (`landing/chat-react/HtmlDoc.tsx:107-127`, `landing/chat-react/HtmlDoc.tsx:157-180`). |

### Caller-set and absence controls

Caller lists above were enumerated with `rg` over TypeScript sources rather than inferred from exports. For the two absence claims, the searches had positive controls in the same result set: `rg -n 'err\.stack|jsonResponse' gateway/http --glob '*.ts'` found both the logger at `gateway/http/app-admin-surface.ts:377` and serializer calls while showing no stack-to-serializer call; `rg -n 'Math\.random|randomBytes|randomUUID'` over the four randomness-alert files plus `runtime/credential-pool.ts` found the `Math.random` source at `runtime/credential-pool.ts:244` and cryptographic generators at `runtime/adapters/claude-code/persistent/spawn.ts:120-135`, while the four reported files contain no other insecure generator.

### Repair design and outcome vocabulary

The manifest grammar remains open: it validates shape without limiting values to `KNOWN_CAPABILITIES` (`cores/sdk/manifest.ts:41-48`, `cores/sdk/manifest.ts:64-74`). The scanner preserves the former accepted character language, including the unusual acceptance of repeated trailing resource separators, while rejecting a lone terminal separator and slash before a resource separator (`cores/sdk/manifest.ts:62-92`). Invalid input joins Zod's existing schema-issue vocabulary through `.refine`; by default `safeParse` returns `success: false`, as the existing malformed-capability tests assert (`cores/sdk/__tests__/manifest.test.ts:145-186`). No new runtime error code or unclassified verdict was added.

### Mutation table

| Guard | Mutation and landing evidence | Red | Restored green |
|---|---|---|---|
| Linear capability grammar | Changed `cores/sdk/manifest.ts:98` from `.refine(isCapabilityFormat, …)` to `.refine(() => true, …)`; printed lines 94-101 and the full diff before running. | `bun test cores/sdk/__tests__/manifest.test.ts`: 3 failures, including the new 100,000-separator case, now at `cores/sdk/__tests__/manifest.test.ts:194`. | Restored line 98; after the parity cases were added, the same suite: 33 pass, 0 fail. |

### Verification

The focused pre-mutation run of `bun test cores/sdk/__tests__/manifest.test.ts` passed 32 tests with 0 failures; after adding explicit grammar-parity cases, the final restored run passed 33 with 0 failures. The mutation run failed in the expected direction. `bash scripts/ci/typecheck-all.sh` checked 51 TypeScript configurations and passed. `bash scripts/ci/lint.sh` passed every reported guard with zero findings. `git diff --check` passed. The local leak gate reported zero findings from the rules it could run but exited 3 because its external PII denylist is unavailable; that is an incomplete result, not a clean claim.

### Deliberately not done

No alert was suppressed or dismissed, no repository setting was changed, and no network call was attempted. The real polynomial sites remain explicit because each needs its own format-preserving rewrite or an independently justified input limit; grouping them behind one generic regex helper would not prove equivalent behavior. `SPEC.md` is unchanged because the open capability language and product behavior did not change; only its validation algorithm did.
