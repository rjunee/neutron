## 2026-09-16 — Cover local merge refusal paths

### What changed

`trident/gates/local-merge.test.ts:41-72` now centralizes the real local-worktree, no-PR run fixture. The review-readiness case at `trident/gates/local-merge.test.ts:85-96` now proves a blocked gate stops before reviewer dispatch by checking actual runner requests at `trident/gates/local-merge.test.ts:92-93`, retains the panel assertion at :95, and leaves the reviewed commit unmerged at :96. The original panel-only assertion did not prove worker ordering. The publication-preflight case at `trident/gates/local-merge.test.ts:99-111` proves an unreadable preflight stops as unknown in the publish phase after approval and before merge.

### Existing outcome vocabulary and decisions

No production outcome was added. `trident/build-run.ts:203-209` maps a blocked gate to the existing blocked outcome and an unknown gate to the separate unknown outcome. The review guard at `trident/build-run.ts:531-533` therefore preserves the explicit readiness refusal; the preflight guard at `trident/build-run.ts:486-495` preserves uncertainty as unknown. Local mode reaches publication after approval through `trident/build-run.ts:595-598`, so both paths are reachable without a PR. The filed default-mode citation remains correct at `trident/store.ts:867`; no cited line required correction.

### Mutation evidence

| Guard | Compiling mutation and observed result | Restored result |
| --- | --- | --- |
| `trident/build-run.ts:533` | Appended `&& false`; the focused review-readiness test was RED, returning unexpected unknown review-progress output. | Focused test GREEN. |
| `trident/build-run.ts:495` | Appended `&& false`; the focused preflight test was RED, returning unexpected blocked merge output. | Focused test GREEN. |

### Original lane verification

The original lane reported: `bun test trident/gates/local-merge.test.ts` passed 7 tests; its final paired run with `trident/as-built-log.test.ts` passed 12 tests. `bunx tsc -p trident/tsconfig.json` and `bunx eslint trident/gates/local-merge.test.ts --format stylish` passed. The repository-wide lint and broader typecheck matrix were each started but did not yield a final result in this lane window, so neither is claimed as verified. The full test suite was deliberately not run because this lane permits targeted files only.

### Deliberately not changed

No production code, product decision, spec item, feature flag, or parallel path was added. Existing local-merge success coverage remains at `trident/gates/local-merge.test.ts:75-82`.

### Review correction — fix1083

The review finding was reproduced before changing the test: moving readiness from `trident/build-run.ts:531-533` to immediately after worker dispatch kept all seven original tests green. The mutated source was printed: worker dispatch at :542, readiness at :543-545. Production already has the correct order; the defect was the test's observation point, since panel invocation occurs later at `trident/build-run.ts:550`.

The helper now returns runner calls alongside the outcome (`trident/gates/local-merge.test.ts:70-72`). The fake runner records requests at `runtime/bounded-work.ts:168-169`; it does not infer dispatch from the panel. The success case observes one review call at `trident/gates/local-merge.test.ts:79`, and the refusal case observes zero at :92 with plan/build as the positive control at :93. These assertions continuously protect the ordering through the focused test, independent of a review worker reporting success. No production invariant or outcome was introduced; the existing blocked/unknown mapping remains `trident/build-run.ts:203-209`.

| Guard | Compiling, executed mutation | Strengthened test RED | Restored GREEN |
| --- | --- | --- | --- |
| Readiness before worker dispatch, `trident/build-run.ts:531-533` | Move the three readiness lines immediately after `work('review', round)`; printed mutated placement :542-545 | 6 pass, 1 fail; `trident/gates/local-merge.test.ts:92` expected 0 review requests, received 1 | 7 pass, 0 fail; 30 assertions |

The original test's 7-pass result under this same mutation confirms the coverage defect. The earlier mutation rows above are retained as the original lane's historical evidence; this follow-up reproduced the ordering mutation directly.

### Citation corrections and scope

All supplied review citations matched the starting tree. Insertions moved helper :41-71 to :41-72, success :74-80 to :75-82, readiness :82-90 to :85-96, and preflight :93-104 to :99-111; the references above are corrected. Production citations did not move after restoration. A whole-tree search using `rg -n --hidden 'before reviewer dispatch|reviewDispatched' --glob '!.git/**'` enumerated the original claim in this record and the three misleading variable references in the test; the record was corrected and the variable renamed to `panelCalled`. The branch's single record was moved to the exact branch-named path requested for this lane.

No product/spec decision changed. Deliberately did not alter production behavior, run the full suite, launch real review workers, access the network, push, open a PR, or merge. Remote CI and fresh upstream state cannot be verified in this offline lane; origin/main is only the supplied local comparison ref.

### Follow-up verification and limits

`bash scripts/ci/typecheck-all.sh` passed all 51 configurations, including Trident. `bash scripts/ci/lint.sh` exited 0; focused ESLint also passed. `bun test trident/gates/local-merge.test.ts trident/as-built-log.test.ts` passed 12 tests with 39 assertions. `git diff --check` passed, and the record has exactly one `## ` heading. Dependency verification reported two unresolved SDK notes but accepted the installed dependency tree; the complete typecheck matrix still passed.

Not verified: remote CI, current remote branch state, real paid-worker execution, the full test suite, and the public-tree leak gate with its private denylist. The ordering regression is verified using the actual build-run path and fake runner requests, not by launching a paid worker.
