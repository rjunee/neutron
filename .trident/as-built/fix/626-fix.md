## 2026-09-15 — #626 repair nine reproducible CodeQL ReDoS findings

### Scope and enumeration boundary

The requested live API read failed to connect in this offline lane. The supplied brief says 30 open alerts, but its six rule counts sum to **33**. The available JSON export has 46 historical rows. This record does **not** claim to have observed the current open set or closed an alert remotely.

I enumerated every row of `../briefs/codeql-alerts-table.json`, then excluded the ten previously repaired IDs (9, 26, 29, 34, 35, 40, 42, 43, 44, 47) and the three rules omitted from the new brief (bad-tag-filter, double-escaping, xss-through-dom: IDs 22, 17, 10). The resulting **33 candidate rows** exactly reproduce the brief's rule counts. Every candidate appears below: nine real findings repaired here, 22 false positives, and two unresolved findings that must remain flagged. The subtraction selects an archival review set; it does not establish live alert state. A current export is still required to reconcile the stated 30.

The excluded repairs are recorded at `.trident/as-built/fix/646-codeql-triage-second-pass.md:23`, `.trident/as-built/fix/646-codeql-triage-second-pass.md:55`, and `.trident/as-built/fix/712-bound-redos-inputs.md:7`. They are historical evidence, not new validation or an assumption that every alert closed on the scanner.

### Per-alert review and pasteable dismissal reasons

Locations below are current source lines read in this session. Each false-positive reason is intended as the dismissal text. Unresolved rows intentionally have no dismissal recommendation.

| Alert | Rule | Current location | Decision and reason |
|---:|---|---|---|
| 1 | `js/insecure-randomness` | `gateway/wiring/build-import-substrate.ts:335` | False positive: the random value selects an existing credential from the available pool; this line reads that credential's existing secret (`runtime/credential-pool.ts:243`). |
| 2 | `js/insecure-randomness` | `gateway/wiring/build-import-substrate.ts:336` | False positive: credential kind selects the OAuth refresh path; the random pool index does not generate authentication material (`runtime/credential-pool.ts:243`). |
| 3 | `js/insecure-randomness` | `gateway/wiring/build-llm-call-substrate.ts:186` | False positive: this reads the existing secret of the pool member selected at `gateway/wiring/build-llm-call-substrate.ts:178`; randomness implements credential load balancing (`runtime/credential-pool.ts:243`). |
| 4 | `js/insecure-randomness` | `gateway/wiring/build-llm-call-substrate.ts:187` | False positive: the selected credential's kind controls refresh, while randomness only chooses an existing pool member (`runtime/credential-pool.ts:243`). |
| 5 | `js/insecure-randomness` | `gateway/wiring/build-llm-call-substrate.ts:1378` | False positive: the API key is an existing credential secret; the random number is only the pool-selection index (`runtime/credential-pool.ts:243`). |
| 6 | `js/insecure-randomness` | `gateway/wiring/build-llm-call-substrate.ts:1397` | False positive: the CLI receives the existing selected secret; selection randomness does not create the API key (`runtime/credential-pool.ts:243`). |
| 7 | `js/insecure-randomness` | `runtime/adapters/claude-code/persistent/pool.ts:305` | False positive: credential identity distinguishes pooled sessions, and is explicitly the existing credential ID, not its secret (`runtime/adapters/claude-code/persistent/pool.ts:282`). |
| 8 | `js/insecure-randomness` | `runtime/adapters/claude-code/persistent/spawn.ts:418` | False positive: the child receives the selected existing credential through the environment (`runtime/adapters/claude-code/persistent/spawn.ts:309`); session/channel identifiers use cryptographic generators (`runtime/adapters/claude-code/persistent/spawn.ts:124`, `runtime/adapters/claude-code/persistent/spawn.ts:129`). |
| 11 | `js/insufficient-password-hash` | `runtime/adapters/claude-code/persistent/repl-session.ts:713` | False positive: SHA-256 fingerprints an existing API/OAuth secret to detect rotation for warm-session reuse; it is not a password verifier (`runtime/adapters/claude-code/persistent/repl-session.ts:699`, `runtime/adapters/claude-code/persistent/spawn.ts:1422`). |
| 12 | `js/insufficient-password-hash` | `runtime/internal-signature.ts:48` | False positive: this hashes the request body before HMAC authenticates the request; it is not password storage (`runtime/internal-signature.ts:51`). |
| 13 | `js/incomplete-sanitization` | `cores/free/research/src/chat-commands.ts:276` | False positive: pipe escaping formats a Markdown table cell in `card.body_md`, rather than escaping an executable-language string (`cores/free/research/src/chat-commands.ts:281`, `cores/free/research/src/chat-commands.ts:287`). |
| 14 | `js/incomplete-sanitization` | `onboarding/interview/agent-name-suggester.ts:366` | False positive: this transforms user content into a length-limited prompt line, not executable source (`onboarding/interview/agent-name-suggester.ts:351`, `onboarding/interview/agent-name-suggester.ts:370`). |
| 15 | `js/incomplete-sanitization` | `onboarding/interview/personality-character-suggester.ts:409` | False positive: escaped corrections are prompt text capped at 240 characters, not an executable-language string (`onboarding/interview/personality-character-suggester.ts:402`, `onboarding/interview/personality-character-suggester.ts:413`). |
| 16 | `js/incomplete-sanitization` | `onboarding/persona-gen/summarize.ts:188` | False positive: the transformation produces a prompt line capped at 240 characters, not executable source (`onboarding/persona-gen/summarize.ts:187`, `onboarding/persona-gen/summarize.ts:192`). |
| 18 | `js/incomplete-multi-character-sanitization` | `cores/free/email/src/mime.ts:96` | False positive: script removal belongs to HTML-to-plaintext extraction, whose result is assigned to `body_text`; it is not an HTML security sanitizer (`cores/free/email/src/mime.ts:184`). |
| 19 | `js/incomplete-multi-character-sanitization` | `cores/free/email/src/mime.ts:97` | False positive: style removal belongs to HTML-to-plaintext extraction, whose result is assigned to `body_text` (`cores/free/email/src/mime.ts:184`). |
| 20 | `js/incomplete-multi-character-sanitization` | `cores/free/email/src/mime.ts:102` | False positive: stripping tags before decoding entities produces literal plaintext for `body_text`, rather than sanitized HTML (`cores/free/email/src/mime.ts:104`, `cores/free/email/src/mime.ts:184`). |
| 21 | `js/incomplete-multi-character-sanitization` | `landing/__tests__/favicon-serving.test.ts:134` | False positive: this removes comments from the repository SVG for a paint assertion inside a test, not from externally supplied HTML for rendering (`landing/__tests__/favicon-serving.test.ts:130`, `landing/__tests__/favicon-serving.test.ts:139`). |
| 23 | `js/stack-trace-exposure` | `gateway/http/surface-kit.ts:114` | Unresolved: the export identifies a generic serializer but supplies no source-to-sink trace. The local stack read inspected is a logger argument (`gateway/http/app-admin-surface.ts:375`); that alone cannot disprove a transitive stack-to-body flow. Keep flagged pending the alert trace. |
| 24 | `js/stack-trace-exposure` | `gateway/upload/import-resume-handler.ts:346` | Unresolved: catches expose `Error.message` or `String(err)` (`gateway/upload/import-resume-handler.ts:201`, `gateway/upload/import-resume-handler.ts:229`, `gateway/upload/import-resume-handler.ts:251`, `gateway/upload/import-resume-handler.ts:292`). This is insufficient to disprove a stack embedded upstream in that string. Keep flagged pending the alert trace. |
| 25 | `js/polynomial-redos` | `client-core/index.ts:73` | False positive under the configured-client boundary: the regex trims constructor-supplied server configuration, not response or chat content (`client-core/index.ts:72`). This reason depends on that configuration remaining trusted. |
| 27 | `js/polynomial-redos` | `connect/unified-project-list.ts:216` | False positive under the configured-infrastructure boundary: workspace URLs come from registry resolution or configured ingress resolution (`gateway/projects/shared-projects-resolver.ts:211`), not response content. |
| 28 | `js/polynomial-redos` | `cores/free/calendar/src/chat-commands.ts:119` | Real, fixed: incoming chat text reaches overlapping verb/rest terms (`gateway/cores/calendar-wiring.ts:103`); a long malformed multiline command blocked the original expression. Require a whitespace separator and a non-whitespace rest start. |
| 30 | `js/polynomial-redos` | `cores/free/reminders/src/chat-commands.ts:458` | Real, fixed: the tomorrow parser repartitioned a long whitespace run between separator and time capture. The capture now starts with a non-whitespace character. |
| 31 | `js/polynomial-redos` | `cores/free/reminders/src/chat-commands.ts:470` | Real, fixed: the next-weekday parser had the same overlapping separator/time capture, independently reproduced and repaired. |
| 32 | `js/polynomial-redos` | `cores/free/reminders/src/chat-commands.ts:484` | Real, fixed: the month/day parser had the same overlapping separator/time capture, independently reproduced and repaired. |
| 33 | `js/polynomial-redos` | `cores/free/reminders/src/chat-commands.ts:500` | Real, fixed: the lazy time capture repeatedly tried a whitespace-plus-today suffix. Remove the fixed-width trailing suffix separately, trim its preceding whitespace, and consume the time once. Removing the suffix first preserves multiline whitespace before today. |
| 36 | `js/polynomial-redos` | `onboarding/archetypes/library.ts:278` | False positive: the preceding replacement collapses every run of non-alphanumeric characters to one hyphen, so the flagged trim cannot encounter a long hyphen run (`onboarding/archetypes/library.ts:277`). |
| 37 | `js/polynomial-redos` | `onboarding/interview/engine-internals.ts:1466` | Real, fixed: optional punctuation allowed adjacent whitespace terms to repartition a long malformed reply. Trim outside the regex and require punctuation in the optional whitespace branch (`onboarding/interview/engine-internals.ts:1467`). |
| 38 | `js/polynomial-redos` | `onboarding/interview/extract-agent-name.ts:149` | Real, fixed: an optional apostrophe joined two whitespace repetitions over user text. Separate apostrophe-present and apostrophe-absent alternatives; a fixed regex argument never made its variable input safe (`onboarding/interview/extract-agent-name.ts:220`). |
| 41 | `js/polynomial-redos` | `runtime/slug-grammar.ts:163` | False positive: hyphen runs are already collapsed to one character before this trailing trim (`runtime/slug-grammar.ts:161`), and the later trim runs on at most 31 characters (`runtime/slug-grammar.ts:164`). |
| 45 | `js/polynomial-redos` | `tasks/projection/parse.ts:79` | Real, fixed: existing project prose is read from disk (`tasks/projection/write.ts:179`) and can contain a long internal whitespace run followed by text. Use `trimEnd` instead of repeated suffix searches. |
| 46 | `js/polynomial-redos` | `tasks/projection/parse.ts:85` | Real, fixed: generated projection text contains variable task titles (`tasks/projection/format.ts:137`); an internal newline run followed by text triggers repeated suffix searches. Scan only the leading and trailing newline boundaries. |

### Design, outcome vocabulary, and continuous enforcement

The repair removes overlapping repetitions at the construction sites, without input truncation, a new size limit, or an alternate implementation. The grammar still returns its existing outcomes: calendar `unrecognized` falls through at `gateway/cores/calendar-wiring.ts:104` (or maps to `malformed` when directly executed at `cores/free/calendar/src/chat-commands.ts:419`); reminder `null` maps to `malformed_time_spec` at `cores/free/reminders/src/chat-commands.ts:252`; option selection returns `null` at `onboarding/interview/engine-internals.ts:1469`; name extraction returns `null` at `onboarding/interview/extract-agent-name.ts:221`. Projection output remains a string (`tasks/projection/parse.ts:81`, `tasks/projection/parse.ts:90`). No new error taxonomy value or product decision was introduced; `SPEC.md` is unchanged.

The parser structure maintains the runtime property on every invocation. Regression tests enforce an independent three-second child-process deadline (`tests/security/codeql-redos.test.ts:66`); a regex blocking the child cannot prevent the parent from terminating it. All nine cases also assert accepted output, with long accepted whitespace controls for the option/name parsers (`tests/security/codeql-redos.test.ts:30`, `tests/security/codeql-redos.test.ts:39`).

### Mutation evidence

Each row was run separately with `bun test tests/security/codeql-redos.test.ts -t 'CodeQL <ID>'`. Each original vulnerable expression was restored from the branch base, its actual changed line printed, and its single test run before restoration. Every RED was `ETIMEDOUT`, not a syntax/type/import error. Each restored case passed; the combined focused suites are recorded below.

| Alert | Mutation and printed landing line | Mutated | Restored |
|---:|---|---|---|
| 28 | Restore overlapping verb/rest regex, `cores/free/calendar/src/chat-commands.ts:119` | RED, 3-second timeout | GREEN |
| 30 | Restore `(.+)` time capture, `cores/free/reminders/src/chat-commands.ts:458` | RED, 3-second timeout | GREEN |
| 31 | Restore `(.+)` time capture, `cores/free/reminders/src/chat-commands.ts:470` | RED, 3-second timeout | GREEN |
| 32 | Restore `(.+)` time capture, `cores/free/reminders/src/chat-commands.ts:484` | RED, 3-second timeout | GREEN |
| 33 | Restore lazy capture and optional repeated-whitespace suffix, `cores/free/reminders/src/chat-commands.ts:500` | RED, 3-second timeout | GREEN |
| 37 | Restore original regex and untrimmed argument, `onboarding/interview/engine-internals.ts:1466` | RED, 3-second timeout | GREEN |
| 38 | Restore optional apostrophe between whitespace repetitions, `onboarding/interview/extract-agent-name.ts:149` | RED, 3-second timeout | GREEN |
| 45 | Restore whitespace suffix regex, `tasks/projection/parse.ts:79` | RED, 3-second timeout | GREEN |
| 46 | Restore newline suffix regex, `tasks/projection/parse.ts:85` | RED, 3-second timeout | GREEN |

### Corrections to prior records and search controls

This evidence supersedes the nine corresponding false-positive rows at `.trident/as-built/fix/646-codeql-triage-second-pass.md:37` and the blanket remaining-false-positive reconciliation at `.trident/as-built/fix/626-triage-the-alerts-with-evidence.md:7`. Earlier records remain untouched because merged as-built shards are immutable (`docs/process/work-tracking.md:135`). The two stack-exposure dismissals at `.trident/as-built/fix/646-codeql-triage-second-pass.md:56` also exceed what a local serializer search establishes, so this review leaves them unresolved.

I searched the whole working tree with `rg -n 'authenticated onboarding owner|no attacker path is established|36 false-positive|36 remaining false-positive|owner controls it' . --glob '*.md' --hidden`. It finds the known earlier reminder rows at `.trident/as-built/fix/646-codeql-triage-second-pass.md:39` and the option row at line 46. Those historical hits remain for the immutability reason above. This is a working-tree search, not a fetched-ref claim.

For randomness, `rg -n 'Math\.random|randomBytes|randomUUID'` across the four flagged files and `runtime/credential-pool.ts` finds the actual `Math.random` source at `runtime/credential-pool.ts:244` and the cryptographic generators at `runtime/adapters/claude-code/persistent/spawn.ts:124`. For stacks, `rg -n 'err\.stack|error\.stack|jsonResponse' gateway/http --glob '*.ts'` finds both the logger stack argument at `gateway/http/app-admin-surface.ts:377` and serializer calls. Neither search is claimed to prove the absence of an indirect dataflow.

### Validation and deliberately unfinished work

The focused run passed 122 tests across `tests/security/codeql-redos.test.ts`, `cores/free/calendar/__tests__/chat-commands.test.ts`, `cores/free/reminders/__tests__/chat-commands.test.ts`, `onboarding/interview/__tests__/extract-agent-name.test.ts`, and `tasks/__tests__/projection.test.ts`. Nine individual original-expression mutations failed, and all nine restored checks passed.

After preserving multiline whitespace before the today suffix (`tests/security/codeql-redos.test.ts:25`), alert 33's original-expression mutation was repeated: RED by timeout, restored GREEN. The final security and reminder suites passed 48 tests, and the reminder TypeScript configuration passed again.

`bash scripts/ci/typecheck-all.sh` checked 51 configurations: 50 passed; `app/tsconfig.json` failed with TS2688, missing the implicit `@types` entry. This matches the pre-existing environment failure recorded at `.trident/as-built/fix/626-triage-the-alerts-with-evidence.md:25`. `bash scripts/ci/lint.sh` passed every reported gate. `git diff --check` passed. The leak gate reported zero findings from runnable rules, but exited 3 because its external identity denylist was unavailable; that result is incomplete, not green.

Current alert enumeration and the two stack source-to-sink traces remain unavailable. No dispositions, network workaround, push, PR, or merge were attempted. Current scanner closure is unverified; the orchestrator must obtain the current export and traces, and let the next scan establish closure. This record is intentionally a partial completion of the live-alert request, with concrete code repairs rather than a claim that all alerts were triaged.
