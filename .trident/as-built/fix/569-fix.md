## Issue 569 — linked plan titles

### Built

Linked active-card titles now open their plan through the same callback as the adjacent document control; cards without an in-app document reference retain inline title editing (`landing/chat-react/WorkBoardTab.tsx:1175`). Linked titles in the Shelved and Done disclosures also open their plans (`landing/chat-react/WorkBoardTab.tsx:939`, `landing/chat-react/WorkBoardTab.tsx:991`).

Successful document writes now fan out through the existing mutation hook to both comment re-anchoring and plan-title synchronization, with `Promise.allSettled` ensuring either consumer still runs if the other fails (`open/composer.ts:3662-3671`). The service reads the written document from its document project, extracts the first ATX H1 after frontmatter, enumerates the target board with `list`, and updates every card whose exact `design_doc_ref` identifies that path (`work-board/spec-doc-service.ts:108-136`, `work-board/spec-doc-service.ts:252-261`). General keeps its distinct document-project and board-scope identifiers (`open/composer.ts:4396-4401`).

### Decisions

The document H1 is authoritative because generated plan documents already place the card title there (`work-board/spec-doc.ts:202-220`). A missing H1, an unrelated document, or a read failure is a no-op; failures join the service's existing structured warning vocabulary and do not turn a successful document save into an HTTP error (`work-board/spec-doc-service.ts:113-135`). No new client/server outcome value was introduced.

The post-write callback maintains the synchronization continuously. It is invoked only after a file mutation lands (`gateway/http/doc-store.ts:431-445`), and independent settled promises prevent the existing comment walker from disabling title synchronization or vice versa (`open/composer.ts:3666-3670`).

### Evidence and tests

The UI test proves a linked title opens `plans/linked.md` without entering edit mode and proves an unlinked title still enters edit mode (`landing/chat-react/__tests__/work-board-tab.test.tsx:157-176`). Service tests prove exact linked-title synchronization, the General scope split, unrelated/no-H1 no-ops, and H1 parsing (`work-board/spec-doc-service.test.ts:145-188`). The production-composer test drives a document PUT and observes the renamed card through the board GET (`open/__tests__/doc-comments-wiring.test.ts:451-472`).

| Guard | Mutation | Red | Restored |
|---|---|---|---|
| Linked title uses document opener | Replaced `onOpenDoc ?? onStartEdit` with `onStartEdit` at `landing/chat-react/WorkBoardTab.tsx:1179` | `opens a linked plan from the card title...` failed: opener received no call | 64 focused tests green |
| Exact document reference selects linked cards | Inverted `=== ref` to `!== ref` at `work-board/spec-doc-service.ts:126` | `a linked plan H1 renames its card...` failed: title remained `Old title` | 64 focused tests green |

Validation: `bun test landing/chat-react/__tests__/work-board-tab.test.tsx work-board/spec-doc-service.test.ts` passed 64 tests; `bunx tsc --noEmit -p work-board/tsconfig.json`, `bunx tsc --noEmit -p landing/chat-react/tsconfig.json`, and `bunx tsc --noEmit -p open/tsconfig.json` passed; targeted ESLint passed; `git diff --check` passed. The requested root `bun run typecheck` cannot run because the root scripts contain no `typecheck` entry (`package.json:7-12`). The production-composer test reached its existing harness setup but the sandbox refused its port-zero listener before assertions with `EADDRINUSE` (`open/__tests__/doc-comments-wiring.test.ts:169-175`).

### Deliberately not changed

No reverse card-title-to-document rewrite was added: linked titles now navigate to the authoritative plan, while unlinked cards keep the existing inline editor (`landing/chat-react/WorkBoardTab.tsx:1179`). No feature flag, alternate path, schema change, or specification decision was introduced.
