## Issue 1077 — Codex pane adoption recognizes interpreter shims

### What changed

`isCodexArgv` now accepts either the exact recorded vector or the interpreter form `<node|nodejs|bun|deno> <codex-script> <recorded-arguments>`. The runtime allowlist, script-slot basename check, and exact remaining-argument comparison are at `runtime/adapters/codex-cli/persistent/project-session.ts:63-69`. The live-pane gate continues to distinguish an unavailable inspection as `unknown` at `runtime/adapters/codex-cli/persistent/project-session.ts:159-163` from a verified identity mismatch as `refused` at `runtime/adapters/codex-cli/persistent/project-session.ts:164-167`.

The existing outcome vocabulary is `CodexSessionRecovery` at `runtime/adapters/codex-cli/persistent/project-session.ts:11`: a successful attach remains `adopted` at `runtime/adapters/codex-cli/persistent/project-session.ts:168-169`. This change adds no outcome value, so there is no new default classification.

The identity invariant is checked on every live recorded-pane open by `inspectHandle` and `isCodexArgv` at `runtime/adapters/codex-cli/persistent/project-session.ts:159-168`. It uses the host's observation rather than cooperation from the pane process.

### Decisions

The implementation copies the established property rather than sharing Claude-specific code: the accepted interpreter set and script-slot identity rule match `runtime/adapters/claude-code/persistent/orphan-adoption.ts:277-290`. Codex additionally compares every argument after the script with the recorded vector at `runtime/adapters/codex-cli/persistent/project-session.ts:67-68`, because its registry already carries the complete launch vector at `runtime/adapters/codex-cli/persistent/project-session.ts:184-188`.

Direct invocations remain exact-vector matches at `runtime/adapters/codex-cli/persistent/project-session.ts:65`; the interpreter exception does not introduce a second launch path, only a second observed representation of the same process.

### Tests and mutation table

The shim-adoption fixture asserts the `adopted` recovery and the attach without spawn at `runtime/adapters/codex-cli/persistent/project-session.test.ts:107-120`. The wrong-script fixture asserts refusal without attach or spawn at `runtime/adapters/codex-cli/persistent/project-session.test.ts:122-134`.

| Guard | Compiling mutation and landed line | RED result | Restored result |
|---|---|---|---|
| Interpreter allowlist | Replaced the allowlist with `not-a-runtime` at `runtime/adapters/codex-cli/persistent/project-session.ts:66` | Shim-adoption test refused at the identity gate | 15 pass, 0 fail |
| Script-slot identity | Removed the basename comparison at `runtime/adapters/codex-cli/persistent/project-session.ts:67` while retaining the argument-tail comparison | Wrong-script test resolved instead of rejecting | 15 pass, 0 fail |

Final local checks:

- `bunx tsc --noEmit -p runtime/tsconfig.json` — green.
- `bunx eslint runtime/adapters/codex-cli/persistent/project-session.ts runtime/adapters/codex-cli/persistent/project-session.test.ts` — green.
- `bash scripts/ci/lint.sh` — green across all reported gates.
- `bun test runtime/adapters/codex-cli/persistent/project-session.test.ts` — 15 pass, 0 fail, 36 assertions.

### Filed-line verification

All numeric citations in the filed issue were enumerated directly from its prose. `project-session.ts:63-65` still identified the original matcher, `project-session.ts:162` still identified the refusal before this edit, and `orphan-adoption.ts:282-289` still identified the interpreter arm. No filed line number required correction. After the edit, the refusal moved to `runtime/adapters/codex-cli/persistent/project-session.ts:166`.

### Deliberately not changed and unverified

No spec item or `SPEC.md` decision changed: the implementation satisfies the filed behavior without changing product intent. No shared Claude adoption helper was extracted, because that would broaden a two-line identity fix across adapter ownership boundaries. No full test suite was run, as the lane brief requires the focused file only.

The real Herdr process-table representation was not reproduced locally; the focused host fixture supplies the reported structured argv at `runtime/adapters/codex-cli/persistent/project-session.test.ts:111-114`. Everything else required by the filed acceptance criteria was verified locally.

The local leak gate reported zero findings from every rule it could run, but its owner-PII file was unavailable, so that portion could not be verified in this build context.
