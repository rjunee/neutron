## 2026-09-16 — Codex pane adoption recognizes interpreter and packaged launchers

### Change and evidence

The original branch added interpreter-form adoption. Review reproduced a production defect: a persisted default `codex` launch rejected the reported live argv `node /usr/lib/node_modules/@openai/codex/bin/codex.js --enable multi_agent_v2`. The regression at `runtime/adapters/codex-cli/persistent/project-session.test.ts:122` failed with the identity refusal before this correction (15 pass, 1 fail).

The matcher accepts an exact recorded vector or an allowed interpreter with matching script basename; it now also recognizes the explicit `codex` to `codex.js` mapping at `runtime/adapters/codex-cli/persistent/project-session.ts:70`. The interpreter check remains at `runtime/adapters/codex-cli/persistent/project-session.ts:66`, and the exact argument-tail check at `runtime/adapters/codex-cli/persistent/project-session.ts:71`. This narrow mapping avoids treating arbitrary custom launchers as Codex. The default launcher and launch arguments are at `runtime/adapters/codex-cli/persistent/project-session.ts:133` and `runtime/adapters/codex-cli/persistent/project-session.ts:149`.

This adds no outcome: the existing `CodexSessionRecovery` vocabulary at `runtime/adapters/codex-cli/persistent/project-session.ts:11` still reports `adopted` on attach at `runtime/adapters/codex-cli/persistent/project-session.ts:172`; there is no new value for a default classifier. Unavailable inspection still throws `unknown` at `runtime/adapters/codex-cli/persistent/project-session.ts:165`, separately from identity refusal at `runtime/adapters/codex-cli/persistent/project-session.ts:169`.

The host inspects and validates each recovered live pane before attachment at `runtime/adapters/codex-cli/persistent/project-session.ts:163`. This is an adoption-time check, not continuous executable attestation; already-open sessions are cached at `runtime/adapters/codex-cli/persistent/project-session.ts:138`. The correction adds no continuous invariant and requires no cooperation from the observed process in the matcher.

### Tests and mutations

The original direct and interpreter adoption tests remain at `runtime/adapters/codex-cli/persistent/project-session.test.ts:95` and `runtime/adapters/codex-cli/persistent/project-session.test.ts:107`. The new positive fixture asserts adoption, attachment, and no spawn at `runtime/adapters/codex-cli/persistent/project-session.test.ts:132`. Four negative fixtures, enumerated by the table at `runtime/adapters/codex-cli/persistent/project-session.test.ts:137`, pin their launch configuration and independently vary launcher, runtime, script, or arguments. They assert identity refusal and no attach or spawn at `runtime/adapters/codex-cli/persistent/project-session.test.ts:148`.

Every mutation below was applied individually, its actual changed source line printed, and the same targeted test file executed. Each compiled and ran to a wrong behavioral answer. Restoring after each mutation gave 20 pass, 0 fail.

| Guard/property | Mutation in runtime/adapters/codex-cli/persistent/project-session.ts | RED | Restored |
|---|---|---|---|
| Packaged alias | Line 70: `return (script === launcher)` | 19 pass, 1 fail: packaged adoption refused | 20 pass |
| Recorded launcher | Line 70: remove `launcher === 'codex' &&` | 19 pass, 1 fail: custom launcher adopted | 20 pass |
| Script identity | Line 70: remove `&& script === 'codex.js'` | 18 pass, 2 fail: unrelated scripts adopted | 20 pass |
| Interpreter allowlist | Line 66: add `python` to runtime regex | 19 pass, 1 fail: wrong runtime adopted | 20 pass |
| Argument equality | Line 71: replace argument-tail equality with `true` | 19 pass, 1 fail: changed arguments adopted | 20 pass |

### Citation corrections and scope

The staged brief has one line stating there is no filed issue; the supplied review finding provided the task. Every supplied code citation was checked against the initial branch: matcher 66–68, default 130, launch 146–147, gate 164–168, and positive test 107–120 were accurate before editing. After this correction, production matcher is 66–71, default 133, launch 149–150, and gate 167–171; the existing positive test remains 107–120. The reproduced refusal moved from 166 to 169. All production and test references here are to project-session.ts and project-session.test.ts under runtime/adapters/codex-cli/persistent/.

A whole-tree Markdown search (including hidden files) for `script-slot basename check|script-slot identity rule match` found the prior unmerged shard at lines 5 and 13 as positive controls. This consolidated record replaces that PR-owned shard at the lane-requested path and corrects its strict-basename description and overly broad continuous-check implication.

No product decision or spec acceptance changed. No unrelated adapter, runtime allowlist, registry format, or launch path was changed. No full test suite or real agent process was run. The local base was inspected using `git grep` on `origin/main` and HEAD for `isCodexArgv|sameArgv`, with `sameArgv` as positive control; it was not fetched, per the offline instruction.

### Verification limits

The real installed launcher and live host process table were not independently verified: the regression uses exactly the structured argv supplied by review. The brief's heading mentions two blockers but supplies only BLOCKER 1; an unspecified second finding cannot be assessed. Remote CI and a current remote base were not checked because this lane is offline.

Local leak gate exited 3 (INCOMPLETE): zero findings from the rules that ran, but the private PII denylist checks for files and messages could not run. This is not a clean leak-gate result.

### Local validation

- `bun test runtime/adapters/codex-cli/persistent/project-session.test.ts`: 20 pass, 0 fail, 51 assertions after each restoration.
- `bunx --no-install eslint runtime/adapters/codex-cli/persistent/project-session.ts runtime/adapters/codex-cli/persistent/project-session.test.ts`: passed.
- `bash scripts/ci/lint.sh`: passed all reported gates.
- `git diff --cached --check`: passed; exactly one `## ` heading verified in this record.
- `bash scripts/ci/typecheck-all.sh`: all 51 configurations passed. Used the repository CI matrix (`scripts/ci/typecheck-all.sh:71`) for typechecking.
