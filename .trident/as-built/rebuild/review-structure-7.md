## 2026-09-15 — Materialized review input certification

### Delivered scope and remainder

**This bounded increment delivers G102. G101 and G124 remain unfinished; this does not close the re-homing.** Read the six predecessor records, the three inventory rows, and the corresponding original audit sections from the audit build worktree. The audit sections enumerate the same three assigned properties. The census record's three additional conflicts were G032/G100/G140 (`.trident/as-built/rebuild/gate-audit-2.md:56`, `.trident/as-built/rebuild/gate-audit-2.md:99`, `.trident/as-built/rebuild/gate-audit-2.md:133`, read in the second audit build worktree); it explicitly described a partial census at line 5. Neither that census nor this increment proves that every other inventory row is preserved.

| Gate | Property in the inventory's own words | Disposition and current evidence |
| --- | --- | --- |
| G102 | Review dispatch requires a readable nonempty base-to-head change and a successfully materialized diff artifact. | Delivered readback gate at `trident/gates/review-artifact.ts:9`; host composition at `trident/build-host.ts:83`; driver refusal before dispatch at `trident/build-run.ts:277`. |
| G101 | PR creation must succeed and an open PR must be confirmed before review handoff. | Unfinished: readiness still precedes publication (`trident/build-run.ts:423`, `trident/build-run.ts:506`). Needs a pre-review publication transition, local-mode handling, remeasurement and confirmation of the open PR at the produced head. Existing mutation/leak/publication gates must remain enforced when publication moves. |
| G124 | A thrown workflow writes a typed failure with no review verdict and no remaining tasks; failure to write falls back to the outer stall guard. | Unfinished: the driver catch remains nonterminal unknown (`trident/build-run.ts:532`). The kept workflow writes null verdict, zero remaining tasks and workflow-threw (`trident/inner-workflow.mjs:6976`, `trident/inner-workflow.mjs:6981`, `trident/inner-workflow.mjs:6994`), with write failure falling back to the outer guard (`trident/inner-workflow.mjs:7002`, `trident/orchestrator.ts:6325`). A durable definite-failure contract and recovery integration still need implementation and certification. |

G101 and G124 are implementation work, not merely missing composition. A blanket conversion of every exception to failure would discard uncertainty after an external write; the existing catch explicitly preserves that uncertainty (`trident/build-run.ts:533`). Pending preparation is also still represented by a pending worker checkpoint before preparation starts (`trident/build-run.ts:275`), and resume preserves that identity (`trident/build-run.ts:202`). This increment does not claim to repair those recovery semantics.

### G102 evidence and decisions

The production observer already generates the pinned base-to-head diff into a temporary file, requires command success, and reads its bytes (`trident/production-host-effects.ts:168`, `trident/production-host-effects.ts:172`, `trident/production-host-effects.ts:173`). It requires a pinned launch base and rechecks the branch and stored pins (`trident/production-host-effects.ts:179`, `trident/production-host-effects.ts:186`, `trident/production-host-effects.ts:188`). Preparation verifies the current snapshot, writes the request and measured context, and renames the temporary file (`trident/production-host-effects.ts:395`, `trident/production-host-effects.ts:399`, `trident/production-host-effects.ts:400`). Those protected implementations were read as the exemplar, not copied or edited.

The new gate certifies the artifact that survives preparation and is referenced by the worker's brief. It requires a full OID and nonempty measured diff (`trident/gates/review-artifact.ts:12`), verifies the brief's integrity and context reference (`trident/gates/review-artifact.ts:16`), and reads the context back from disk (`trident/gates/review-artifact.ts:17`). It checks the host's run/step identity and review role (`trident/gates/review-artifact.ts:18`) and compares the artifact's head, exact diff string and PR observation to the host snapshot (`trident/gates/review-artifact.ts:21`). This uses the existing JSON context artifact rather than introducing a second diff handoff format.

Missing, malformed, stale or unreadable facts join the existing **GateResult** vocabulary as unknown with a specific missing-fact reason (`trident/gates/review-artifact.ts:11`, `trident/gates/review-artifact.ts:25`; union at `trident/build-run.ts:33`). The driver preserves unknown and routes blocked to the orchestrator; neither defaults to permission (`trident/build-run.ts:167`). The new dependency is optional in the type because non-review paths do not need it, but an attempted review without it refuses (`trident/build-run.ts:104`, `trident/build-run.ts:278`). No new terminal-cause value, provider choice, round counter or budget was introduced.

Continuous enforcement is the host's shared review dispatch boundary: after preparation, it awaits readback before calling the runner (`trident/build-run.ts:276`, `trident/build-run.ts:279`, `trident/build-run.ts:284`). It does not ask a reviewer to attest that its input exists. The two-round test checks preparation/check/dispatch order on the first review and the review after a fix (`trident/build-run.test.ts:1299`). This proves the check at dispatch time, not immutable storage throughout review, a timeout for hung filesystem reads, process-death recovery, or retrospective certification of old approved checkpoints. The retained review-only executor remains a separate protected contract (`trident/review-run.ts:244`).

### Certification and fixture accounting

The inventory G102 test pin at `trident/orchestrator.test.ts:2731` now points to a resolver-read refusal, not the review artifact property. The current empty-change certification is `trident/orchestrator.test.ts:2994`. G101's pin at `trident/orchestrator.test.ts:1908` is now inside a first-publication replay test; a current creation-failure test begins at `trident/orchestrator.test.ts:2171`. G124's kept failure-write certification is at `trident/inner-workflow.test.ts:1831`. The prescribed certification files are run unchanged; this is preservation of their current assertions, not historical line-for-line equivalence or evidence that they invoke the new gate.

New direct certification uses real files for successful local/PR artifacts, missing/malformed files, incorrect brief integrity/reference, invalid measured input, stale dispatch identity and mismatched head/diff/PR (`trident/gates/review-artifact.test.ts:30`, `trident/gates/review-artifact.test.ts:39`, `trident/gates/review-artifact.test.ts:50`, `trident/gates/review-artifact.test.ts:59`, `trident/gates/review-artifact.test.ts:70`, `trident/gates/review-artifact.test.ts:78`). Driver tests assert missing/unknown/blocked refusals without dispatch (`trident/build-run.test.ts:1286`). The composed host test reads actual prepared context and rejects a stale rewrite (`trident/build-host.test.ts:726`).

Enumerated `BuildRunDeps` with `rg -l 'BuildRunDeps' --glob '*.ts' --glob '!node_modules/**' .`: **seven files**, not the brief's five: `trident/build-run.ts`, `trident/build-host.ts`, `trident/build-run.test.ts`, `trident/gates/local-merge.test.ts`, `trident/production-host-effects.ts`, `trident/production-host-effects.test.ts`, `runtime/workers/codex-headless.test.ts`. No required property was added. Production effects retain their existing four-effect Pick (`trident/production-host-effects.ts:388`); composition supplies the gate (`trident/build-host.ts:83`). The runtime comparison fixture cannot reach artifact preparation with its deliberately non-OID head (`runtime/workers/codex-headless.test.ts:10`, `trident/build-run.ts:421`), so its earlier refusal remains unchanged.

The driver and local-merge fake hosts compose the real gate over simulated context reads (`trident/build-run.test.ts:58`, `trident/gates/local-merge.test.ts:55`). They do not certify filesystem materialization. The composed host fixtures write physical context files (`trident/build-host.test.ts:41`, `trident/build-host.test.ts:488`); production-effect fixtures use the real gate over production preparation files (`trident/production-host-effects.test.ts:473`, `trident/production-host-effects.test.ts:558`). Existing assertions were preserved. No test was weakened or skipped to obtain green.

### Search controls and unchanged decisions

Whole-tree content search, including hidden files and excluding dependencies/git metadata, used `physical diff-file materialization|complete review-artifact handoff|Review artifact host is missing`. It found the positive implementation control at `trident/build-run.ts:278` and two historical deferrals at `.trident/as-built/rebuild/review-structure.md:35` and `.trident/as-built/rebuild/review-structure-6.md:30`. Those deferrals remain because prior as-built records are immutable descriptions of their increments. Product requirements and spec decisions did not change. Read the work-tracking record rules again before writing; the task explicitly prescribed this branch-specific shard destination.

### Mutation evidence

All 22 mutations below compiled with `bunx tsc -p trident/tsconfig.json --noEmit`, produced an assertion failure from a wrong answer, and passed the same test after restoration. Each replacement was applied alone and its actual changed source line was printed. The driver-dispatch mutation is a stricter wrong-phase control; driver-bypass separately proves that suppressing review checks permits the wrong execution. No parser or typechecker failure is counted as RED.

| Guard | Printed landing and replacement | Test file / filter | Compile | Mutated | Restored |
| --- | --- | --- | --- | --- | --- |
| head | `trident/gates/review-artifact.ts:12` — `false` | `trident/gates/review-artifact.test.ts` / `G102 empty` | GREEN | RED | GREEN |
| empty-diff | `trident/gates/review-artifact.ts:12` — `false` | `trident/gates/review-artifact.test.ts` / `G102 empty` | GREEN | RED | GREEN |
| brief-integrity | `trident/gates/review-artifact.ts:16` — `false` | `trident/gates/review-artifact.test.ts` / `G102 brief` | GREEN | RED | GREEN |
| brief-reference | `trident/gates/review-artifact.ts:16` — `false` | `trident/gates/review-artifact.test.ts` / `G102 brief` | GREEN | RED | GREEN |
| run-identity | `trident/gates/review-artifact.ts:18` — `false` | `trident/gates/review-artifact.test.ts` / `G102 artifact identity` | GREEN | RED | GREEN |
| step-identity | `trident/gates/review-artifact.ts:18` — `false` | `trident/gates/review-artifact.test.ts` / `G102 artifact identity` | GREEN | RED | GREEN |
| role-identity | `trident/gates/review-artifact.ts:19` — `false` | `trident/gates/review-artifact.test.ts` / `G102 artifact identity` | GREEN | RED | GREEN |
| head-equality | `trident/gates/review-artifact.ts:21` — `false` | `trident/gates/review-artifact.test.ts` / `G102 stale` | GREEN | RED | GREEN |
| diff-equality | `trident/gates/review-artifact.ts:21` — `false` | `trident/gates/review-artifact.test.ts` / `G102 stale` | GREEN | RED | GREEN |
| local-pr | `trident/gates/review-artifact.ts:22` — `false` | `trident/gates/review-artifact.test.ts` / `G102 stale` | GREEN | RED | GREEN |
| pr-number | `trident/gates/review-artifact.ts:22` — `false` | `trident/gates/review-artifact.test.ts` / `G102 stale` | GREEN | RED | GREEN |
| pr-head | `trident/gates/review-artifact.ts:23` — `false` | `trident/gates/review-artifact.test.ts` / `G102 stale` | GREEN | RED | GREEN |
| pr-state | `trident/gates/review-artifact.ts:23` — `false` | `trident/gates/review-artifact.test.ts` / `G102 stale` | GREEN | RED | GREEN |
| read-failure | `trident/gates/review-artifact.ts:25` — `return { kind: 'allow' }` | `trident/gates/review-artifact.test.ts` / `G102 unreadable` | GREEN | RED | GREEN |
| success-control | `trident/gates/review-artifact.ts:24` — `return unknown('denied')` | `trident/gates/review-artifact.test.ts` / `G102 materialized` | GREEN | RED | GREEN |
| driver-missing | `trident/build-run.ts:278` — `return { stop: { kind: 'merged', snapshot } }` | `trident/build-run.test.ts` / `G102 driver refuses missing` | GREEN | RED | GREEN |
| driver-stop | `trident/build-run.ts:280` — `if (false) return { stop: artifact! }` | `trident/build-run.test.ts` / `G102 driver refuses` | GREEN | RED | GREEN |
| driver-dispatch | `trident/build-run.ts:277` — `if (role === 'plan') {         if (!deps.reviewArtifact)` | `trident/build-run.test.ts` / `G102 driver checks` | GREEN | RED | GREEN |
| host-composition | `trident/build-host.ts:83` — `reviewArtifact: async () => ({ kind: 'allow' }),` | `trident/build-host.test.ts` / `G102 host composes` | GREEN | RED | GREEN |
| missing-artifact-permissive | `trident/gates/review-artifact.ts:19` — `return { kind: 'allow' }` | `trident/gates/review-artifact.test.ts` / `G102 artifact identity` | GREEN | RED | GREEN |
| missing-snapshot-permissive | `trident/gates/review-artifact.ts:23` — `return { kind: 'allow' }` | `trident/gates/review-artifact.test.ts` / `G102 stale` | GREEN | RED | GREEN |
| driver-bypass | `trident/build-run.ts:277` — `if (role === 'review' && round < 0) {         if (!deps.reviewArtifact)` | `trident/build-run.test.ts` / `G102 driver checks` | GREEN | RED | GREEN |

### Final validation and delivery

- Restored `bun test trident/build-run.test.ts trident/build-host.test.ts trident/gates/ trident/production-host-effects.test.ts runtime/workers/codex-headless.test.ts`: **368 pass, 0 fail, 1490 assertions**, 17 files.
- Retained certification `bun test trident/orchestrator.test.ts trident/inner-workflow.test.ts`: **457 pass, 0 fail, 2112 assertions**, two unchanged files. Both assigned G101/G102 certification pins share the first file; G124 cites the second.
- Restored `bunx tsc -p trident/tsconfig.json --noEmit`: passed. `bash scripts/ci/lint.sh`: passed. All 22 individual mutation compiles also passed. No whole-suite sweep or harness-launching test script was run.
- `bash scripts/ci/leak-gate.sh --tree .`: exit 3, **INCOMPLETE**, zero findings from executed rules; `pii-denylist` and `pii-denylist-msg` unavailable. The scan covered zero commit-message lines and does not certify the subsequent commit message. Local restricted-text checks cover the nine changed files and chosen message, but cannot replace the unavailable denylist.
- `git diff --check`: passed. Changed files were enumerated using `git diff --name-only` plus `git ls-files --others --exclude-standard`: six modified code/test files, the new gate and its test, and this record. The shard has exactly one `## ` heading.
- The scope control `git diff --name-only -- trident/build-run.ts trident/inner-loop.ts trident/review-run.ts trident/inner-workflow.mjs gateway open trident/production-host-effects.ts` printed only `trident/build-run.ts`. This is a positive control over the local diff, not a claim about a fetched remote tree. Protected implementations have no local diff in this change.
- Delivery is a local commit on `rebuild/review-structure-7`. The orchestrator owns review and publication. No push, PR creation or merge was attempted. G101/G124 remain explicitly unfinished above.
