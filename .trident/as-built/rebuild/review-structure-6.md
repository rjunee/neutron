## 2026-09-15 — Review infrastructure blocks and continuous fix ancestry

### Delivered boundary

This bounded increment addresses **G057, G060 and G084**. The other three assigned gates remain explicitly unfinished below. Read the five predecessor records in order, the inventory rows, the original audit's G101/G102/G124 sections, and the census conflict details. The two audit records were read from the audit build worktrees because their requested local refs did not resolve. No remote access was attempted.

| Gate | Property in the inventory's words | Delivered implementation | Certification, green |
| --- | --- | --- | --- |
| G057 | Enabled core review seats need usable verdicts; a missing seat contributes a blocker. | Infrastructure blocks for missing configuration, absent observations, mismatched provenance and unusable verdicts: `trident/gates/review-panel.ts:69`, `trident/gates/review-panel.ts:72`, `trident/gates/review-panel.ts:83`, `trident/gates/review-panel.ts:87`. | Kept `trident/inner-workflow.test.ts:1435`, `trident/inner-workflow.test.ts:1464`; direct taxonomy cases `trident/gates/review-panel.test.ts:26`; driver routing `trident/build-run.test.ts:1266`. |
| G060 | Missing or unusable synthesis, including a thrown review round, becomes an infrastructure block rather than approval. | Invalid trailer, absent/mismatched synthesis, malformed synthesis, missing approval checkpoint and observation exceptions block: `trident/gates/review-panel.ts:68`, `trident/gates/review-panel.ts:91`, `trident/gates/review-panel.ts:93`, `trident/gates/review-panel.ts:115`, `trident/gates/review-panel.ts:117`. A thrown review runner blocks at `trident/build-run.ts:279`. | Kept `trident/__tests__/synthesis-unavailable.test.ts:311`, `trident/__tests__/synthesis-unavailable.test.ts:657`; direct `trident/gates/review-panel.test.ts:26`, `trident/build-run.test.ts:1258`, `trident/build-run.test.ts:1266`. |
| G084 | A fix-round reviewed-head pin must be full width and the produced head must descend from it before publishing. | Every completed fix checks its newly measured head against the pre-fix host snapshot before accepting that snapshot: `trident/build-run.ts:310`. Host composition calls the retained gate with that live pin: `trident/build-host.ts:84`. Width and git ancestry remain at `trident/gates/fix-lineage.ts:10`, `trident/gates/fix-lineage.ts:15`. | Kept `trident/orchestrator.test.ts:1548`, `trident/orchestrator.test.ts:1559`, `trident/orchestrator.test.ts:1567`, `trident/orchestrator.test.ts:1574`; direct multi-fix `trident/build-run.test.ts:1216`, resume `trident/build-run.test.ts:1235`, host composition `trident/build-host.test.ts:707`, real git `trident/gates/fix-lineage.test.ts:51`. |

The G084 inventory pins at lines 1396/1407/1415 now refer to other assertions; current ancestry certification is the section beginning at `trident/orchestrator.test.ts:1530`. The G060 pin at 656 is now a separator before the test at 657. All three retained certification files passed unchanged. This establishes current certification, not historical line-for-line equivalence.

### Vocabulary, decisions and continuous enforcement

The panel joins the existing `ReviewDecision` union (`trident/build-run.ts:34`), including blocked and unknown from `GateResult` (`trident/build-run.ts:33`). Missing panel facts now produce **blocked**, with an `infra-only:` reason (`trident/gates/review-panel.ts:39`). The driver defaults blocked to an orchestrator-addressed outcome and unknown to a nonterminal outcome with phase and step identity (`trident/build-run.ts:162`, `trident/build-run.ts:163`, `trident/build-run.ts:166`). Tests assert this distinction directly (`trident/build-run.test.ts:1275`). No new terminal-cause value or durable block classifier was introduced: the infrastructure category is carried in the existing reason string, and this record does not claim a persisted `blockKind` field.

The kept exemplar actually returns an infrastructure finding and prevents a pointless code fix (`trident/inner-workflow.mjs:2922`, `trident/inner-workflow.mjs:2996`). The rebuilt driver similarly stops on blocked before its fix branch (`trident/build-run.ts:443`). Incomplete observations are inspected by host code, not by asking the missing reviewer to report its own absence (`trident/gates/review-panel.ts:82`). Valid complete panels still approve (`trident/gates/review-panel.test.ts:19`). Existing tests asserting unknown for these facts were incorrect against the assigned inventory; their assertions now require the exact blocked category, rather than accepting either outcome (`trident/gates/review-panel.test.ts:47`, `trident/build-host.test.ts:131`). Missing stable finding identities still belong to the separate uncertainty path (`trident/gates/review-panel.ts:111`).

G084 is maintained by the **driver's shared completed-fix transition**, including fresh and resumed fixes. It passes the prior measured snapshot's head, never a worker trailer or the produced head as its own pin (`trident/build-run.ts:312`). It refuses before another review or publication if ancestry fails, and preserves unknown if ancestry cannot be established (`trident/build-run.ts:313`, `trident/gates/fix-lineage.ts:18`). The next accepted snapshot advances the next round's pin (`trident/build-run.ts:330`). This does not require the fixer to admit it abandoned work. Post-review revision checks still bind publication to the approved head (`trident/build-run.ts:491`, `trident/build-run.ts:498`). The original publication check remains for a supplied persisted pin (`trident/build-host.ts:147`); the live fix path now proves every intervening edge, even when that constructor pin is null (`trident/build-host.test.ts:710`). This is repeated enforcement of the same gate, not a selectable alternative implementation.

The new host capability is optional in the interface because paths that never fix need not invoke it; its absence **refuses** a completed fix as unknown (`trident/build-run.ts:107`, `trident/build-run.ts:311`, `trident/build-run.test.ts:1249`). No required field was added. Enumerating `BuildRunDeps` with `rg -l 'BuildRunDeps' --glob '*.ts' --glob '!node_modules/**' .` found **seven**, rather than the brief's older five: `trident/build-run.ts`, `trident/build-host.ts`, `trident/build-run.test.ts`, `trident/gates/local-merge.test.ts`, `trident/production-host-effects.ts`, `trident/production-host-effects.test.ts`, and `runtime/workers/codex-headless.test.ts`. The two production persistence fixtures that exercise fixes now use the real gate with real git, matching host composition (`trident/production-host-effects.test.ts:445`, `trident/production-host-effects.test.ts:529`). Their existing crash and resume assertions were preserved. The driver's fake fixture also composes the real gate, with a simulated successful git observation (`trident/build-run.test.ts:53`).

### Explicit remainder

Enumerated by subtracting the three delivered IDs from the six assigned IDs:

- **G101:** “PR creation must succeed and an open PR must be confirmed before review handoff.” Review still enters readiness at `trident/build-run.ts:416`, and the publication effect remains at `trident/build-run.ts:499`. This needs a distinct pre-review publication/open-PR handoff with local-mode handling and head remeasurement. It is not delivered here.
- **G102:** “Review dispatch requires a readable nonempty base-to-head change and a successfully materialized diff artifact.” The driver's current check is a full head and nonempty diff string (`trident/build-run.ts:414`). The production observer now writes and reads a pinned git diff, then removes its temporary directory (`trident/production-host-effects.ts:80`); preparation writes the measured context into an artifact (`trident/production-host-effects.ts:312`). These are relevant evidence, but this increment does not certify the complete review-artifact handoff or its failure paths and makes no completion claim.
- **G124:** “A thrown workflow writes a typed failure with no review verdict and no remaining tasks; failure to write falls back to the outer stall guard.” The generic driver catch still preserves uncertainty (`trident/build-run.ts:525`). A durable definite-failure contract and independently maintained outer recovery remain necessary. The kept implementation writes zero remaining tasks and a typed cause, then explicitly relies on the stall guard if the write fails (`trident/inner-workflow.mjs:6981`, `trident/inner-workflow.mjs:6994`, `trident/inner-workflow.mjs:7001`). This increment's narrow review-runner catch does not deliver that workflow-wide contract.

Approved historical resumes, interrupted pending fixes, process death, hung observation sources, and durable infrastructure classification are not newly certified. An awaited gate prevents advancement while unresolved; it is not an independent watchdog (`trident/build-run.ts:312`). Product requirements and spec decisions did not change. Read the work-tracking record rules again before writing; the task explicitly prescribed this branch-specific shard destination.

### Search controls

The whole-tree search for `missing panel evidence stay unknown|missing source, config, malformed trailer and thrown observations stay unknown|missing panel evidence are infrastructure blocks`, including hidden files and excluding dependencies/git metadata, found only the corrected title at `trident/build-host.test.ts:131`. The third alternative is its positive control. A broader panel/synthesis-and-unknown search found historical claims in `.trident/as-built/rebuild/host-admission-review.md:15` and `.trident/as-built/rebuild/host-policies.md:5`; they remain immutable records of earlier increments. Current suite composition's acceptance of both blocked and unknown remains accurate (`.trident/as-built/rebuild/review-structure-2.md:24`).

`git diff --name-only -- trident/build-run.ts trident/inner-loop.ts trident/review-run.ts trident/inner-workflow.mjs gateway open trident/production-host-effects.ts` printed only `trident/build-run.ts`. This positive control concerns the local diff, not a fetched remote tree. Protected implementations have no local diff in this change.

### Mutation evidence

All 19 mutations below compiled with `bunx tsc -p trident/tsconfig.json --noEmit`, returned a wrong answer that caused an assertion failure, and passed the same test after restoration. Each replacement was applied alone and its actual landing line was printed. No parsing or typecheck failure is counted as RED. The broad taxonomy test reaches each changed refusal independently, including a synthesis exception after usable seats (`trident/gates/review-panel.test.ts:41`).

| Guard | Printed landing / mutation | Test file / filter | Compile | Mutated | Restored |
| --- | --- | --- | --- | --- | --- |
| G057 G060 permissive infrastructure | `trident/gates/review-panel.ts:39` — `({ kind: 'approve' })` | `trident/build-run.test.ts` / `G057 G060 driver` | GREEN | RED | GREEN |
| panel taxonomy 1 | `trident/gates/review-panel.ts:68` — `if (!trailer.ok) return unknown(`Review trailer ${trailer.reason} at ${trailer.path}`)` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 2 | `trident/gates/review-panel.ts:69` — `if (!source) return unknown('Review panel observation source is missing')` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 3 | `trident/gates/review-panel.ts:72` — `if (seats.some(seat => !seat.id \|\| !seat.modelId) \|\| !seats.some(seat => seat.role === 'core') \|\| new Set(seats.map(seat => seat.id)).size !== seats.length) return unknown('Review core seat configuration is missing or duplicated')` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 4 | `trident/gates/review-panel.ts:83` — `if (!observed) return unknown(`Review seat ${seat.id} (${seat.provider}) has no recorded observation`)` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 5 | `trident/gates/review-panel.ts:84` — `if (observed.runId !== runId \|\| observed.head !== snapshot.head \|\| observed.round !== round \|\| observed.provider !== seat.provider \|\| observed.modelId !== seat.modelId) return unknown(`Review seat ${seat.id} (${seat.provider}) provenance does not match run, revision, round, provider or model`)` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 6 | `trident/gates/review-panel.ts:87` — `if (!checked.ok) return unknown(`Review seat ${seat.id} (${seat.provider}) verdict is unusable`)` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 7 | `trident/gates/review-panel.ts:91` — `if (!recorded \|\| recorded.runId !== runId \|\| recorded.head !== snapshot.head \|\| recorded.round !== round) return unknown('Review synthesis provenance does not match run, revision and round')` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 8 | `trident/gates/review-panel.ts:93` — `if (!synthesis.ok) return unknown('Review recorded synthesis is unusable')` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 9 | `trident/gates/review-panel.ts:115` — `if (recorded.checkpoint !== 'argus-approved') return unknown('Review recorded approval checkpoint is missing')` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| panel taxonomy 10 | `trident/gates/review-panel.ts:117` — `} catch { return unknown('Review panel host observation failed') }` | `trident/gates/review-panel.test.ts` / `G057 G060` | GREEN | RED | GREEN |
| G060 thrown round | `trident/build-run.ts:279` — `if (role === 'review') return { stop: { kind: 'merged', snapshot } }` | `trident/build-run.test.ts` / `G060 thrown` | GREEN | RED | GREEN |
| G084 missing host | `trident/build-run.ts:311` — `if (!deps.checkFixLineage) return { stop: { kind: 'merged', snapshot } }` | `trident/build-run.test.ts` / `G084 missing` | GREEN | RED | GREEN |
| G084 refusal propagation | `trident/build-run.ts:313` — `if (false) return { stop: lineage! }` | `trident/build-run.test.ts` / `G084 each fix` | GREEN | RED | GREEN |
| G084 prior pin | `trident/build-run.ts:312` — `deps.checkFixLineage(measured, measured.head)` | `trident/build-run.test.ts` / `G084 each fix` | GREEN | RED | GREEN |
| G084 resume refusal | `trident/build-run.ts:313` — `if (false) return { stop: lineage! }` | `trident/build-run.test.ts` / `G084 resumed` | GREEN | RED | GREEN |
| G084 host pin | `trident/build-host.ts:85` — `options.reviewed_head, snapshot.head),` | `trident/build-host.test.ts` / `G084 host composes` | GREEN | RED | GREEN |
| G084 ancestry | `trident/gates/fix-lineage.ts:16` — `if (false) {` | `trident/gates/fix-lineage.test.ts` / `non-descendant` | GREEN | RED | GREEN |
| G084 width | `trident/gates/fix-lineage.ts:10` — `if (false) {` | `trident/gates/fix-lineage.test.ts` / `short and malformed` | GREEN | RED | GREEN |

### Final validation

- Restored `bun test trident/build-run.test.ts trident/build-host.test.ts trident/gates/ trident/production-host-effects.test.ts runtime/workers/codex-headless.test.ts`: **354 pass, 0 fail, 1445 assertions**, 16 files.
- Retained `bun test trident/inner-workflow.test.ts trident/__tests__/synthesis-unavailable.test.ts trident/orchestrator.test.ts`: **512 pass, 0 fail, 2256 assertions**, three unchanged files.
- `bunx tsc -p trident/tsconfig.json --noEmit`: exit 0 on restored sources. `bash scripts/ci/lint.sh`: exit 0. No whole-suite sweep or harness-launching test script was run.
- `bash scripts/ci/leak-gate.sh --tree .`: **INCOMPLETE**, zero findings from executed rules; `pii-denylist` and `pii-denylist-msg` could not run. The scan included 327 pre-existing message lines, so it does not certify this change's subsequent commit message. Local restricted-text checks cover the changed files and chosen message.
- `git diff --check` passed. Changed files were enumerated by `git diff --name-only` plus `git ls-files --others --exclude-standard`: seven code/test files and this record. The shard has exactly one `## ` heading. The temporary mutations to the retained lineage gate were restored; `git diff --name-only -- trident/gates/fix-lineage.ts trident/build-run.ts` printed only the driver as a positive control.
- Delivery is a local commit on `rebuild/review-structure-6`. The orchestrator owns review, push and PR creation; no publication or merge was attempted by this lane.
