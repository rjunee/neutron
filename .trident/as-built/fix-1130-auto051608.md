## 2026-09-17 — Make table-ownership scanning safe against concurrent probe cleanup

### Change and evidence

`collectSourceFiles` snapshots candidate source paths at
`migrations/__tests__/table-ownership-conformance.test.ts:59-80`. The ownership
tests subsequently read each candidate through `findWriters` at
`migrations/__tests__/table-ownership-conformance.test.ts:166-175`. A candidate
that disappears in that interval cannot write a table, so `readSourceFile` now
returns `undefined` only for `ENOENT` at
`migrations/__tests__/table-ownership-conformance.test.ts:157-164`; all other
read failures still throw.

The regression creates, enumerates, and removes a root-source probe at
`migrations/__tests__/table-ownership-conformance.test.ts:234-249`. It asserts
both that the vanished probe reads as `undefined` and that a persistent approved
`projects` writer remains detected. The table verdict continues to calculate
both unapproved and stale writers at
`migrations/__tests__/table-ownership-conformance.test.ts:268-286`.

The seam probe remains in `gateway/`: its dependency-cruiser invocation takes a
root-relative path and executes with the repository root as its current working
directory at `gbrain-memory/__tests__/memory-swap-seam.depcruise.test.ts:82-108`,
then asserts the probe's root-relative source path at :128-147. Moving it into a
test directory would make the ownership scanner exclude it by the established
walk rule at `migrations/__tests__/table-ownership-conformance.test.ts:64-76`.

### Tests and mutation evidence

**Re-measured after a second commit added the non-missing-read guard.** An
earlier draft of this section reported 16 tests / 84 assertions and cited the
verdict at `:268-286` and the mutation target at `:276-286`. Those numbers and
anchors were correct for the first commit and are stale now; the values below are
what the stated command produces on the tip under review, and they supersede
them. Adversarial review caught the drift.

Before the change, starting the ownership test while the real seam PASS probe
existed reproduced `ENOENT` after probe cleanup: the ownership run reported
8 pass / 7 fail. After the change the same probe-coordinated run is green for
ownership. The seam test itself has unrelated dependency-cruiser failures in this
environment and is outside this change's scope.

Current anchors, re-checked against this tip:

- `readSourceFile` — `migrations/__tests__/table-ownership-conformance.test.ts:157`
- deletion regression — `:234`
- non-missing-read regression — `:251`
- table verdict — `:288`

| Guard | Mutation | Mutated | Restored |
| --- | --- | --- | --- |
| Vanished source maps to `undefined` (`:157-164`) | Return an empty string instead | RED: the deletion test expected `undefined`, received an empty string | GREEN |
| ENOENT tolerance is NARROW (`:161-162`) | Swallow every read error, not just ENOENT | RED: `scanner propagates a read error that is NOT a missing file`. Before that test existed this mutation left the suite GREEN at 16/16 — the narrowing was unguarded | GREEN |
| Exact ownership allowlist (`:288`) | Add an unapproved `UPDATE projects` source file | RED: the `projects` verdict named the added source as a new writer | GREEN |

`bun test migrations/__tests__/table-ownership-conformance.test.ts` — **17 pass,
0 fail, 85 expect() calls.**

`bunx eslint
migrations/__tests__/table-ownership-conformance.test.ts`, `bunx tsc --noEmit
--pretty false`, and `git diff --check` passed. The root package has no
`typecheck` or `lint` script; the direct repository TypeScript command and
file-scoped ESLint command were used. `bun install` completed, and the trident
workspace resolution was verified inside this build worktree.

### Scope and decisions

No `SPEC.md` decision changed. This change adds no feature flag or alternate
scanner: it replaces the direct read in the sole ownership scan. It deliberately
does not alter the seam test, dependency-cruiser configuration, unrelated seam
failures, or the full-suite gate.
