{
  "file": "trident/mutation-claim-artifact.ts",
  "find": "32 * 1024",
  "replace": "0",
  "guard": ["bun", "test", "trident/mutation-claim-artifact.test.ts"],
  "control": ["bun", "test", "trident/mutation-prover.test.ts"],
  "rationale": "Round 11 reverted the rounds-9/10 base-resolution rewiring per review, which deleted round 10's find string, so the nomination re-points at the channel's own byte cap. Zeroing the cap makes the reader refuse every committed nomination as oversized: the cap-boundary and realgit round-trip tests in the guard go red, while the prover suite, which never reads the committed artifact, stays green."
}
