- [x] T1: Route production Claude/Codex conversations through the shared strict project-workspace host with the actual per-dispatch scope (General is the manager's null scope, a literal `general` project keeps its id); missing host on Herdr or invalid identity refuses, never falls back to an inherited workspace
- [x] T2: Coordinate credential rotation as a verified Chat handoff at a shared composer scope-lifecycle boundary: stop scope admission, retire the exact old REPL owner via pool.ts, then let the manager replace the positively-gone Chat, so rotation is never blocked
- [x] T3: Safe sleep/wake: scope-lifecycle retirement that reads admission/lease awake evidence read-only, refuses busy/queued/build/approval/uncertain-child/foreign/unverified, retires an idle owned Chat with transcripts kept and only re-verified owned panes closed, wakes on demand, adopts survivors on restart without duplication; spec status note + as-built shard