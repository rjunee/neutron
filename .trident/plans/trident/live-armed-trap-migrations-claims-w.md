# IMPLEMENTATION_PLAN — Disarm the v122 `work_board_items_pr` ledger trap before #269 can merge

Regenerated 2026-08-18 by the planner, against origin/main `854f5f6` (NOTE: the repo-of-record checkout is STALE at `89de752`; branch from fetched origin/main). Card: the live `_migrations` ledger records `work_board_items_pr` (scar row at ordinal 122, `content_sha256` NULL) while `work_board_items` has NO `pr` column — so when #269 (which carries `migrations/0133_work_board_items_pr.sql`) merges and the gateway reboots, `classifyMigration` answers `recorded-by-name` off the scar, the file is silently SKIPPED, and the merged code's reads of `work_board_items.pr` reproduce the "no such column" outage.

Planner forensics, verified read-only against the live DB 2026-08-18 (do NOT re-derive from older runner revisions — `migrations/runner.ts` on current main is ~1700 lines, name-identity based, nothing like pre-08-16 versions):

- Live `_migrations` DDL: `version INTEGER NOT NULL, name TEXT NOT NULL PRIMARY KEY, applied_at REAL NOT NULL` + 3 nullable provenance columns. Name-keyed IS the current canonical shape (`rekeyLedgerOnName` moves the PK off `version` onto `name`); one row per name is enforced by the PK.
- Ledger tail: 120–127, 130, 131, 132 (`work_board_items_dependencies`), 134, 135, 136. Free ordinals: 128, 129, 133, 137+. Scars: (122 `work_board_items_pr`), (124 `dispatch_dependencies_and_claims`), (125 `code_trident_runs_fix_round_contract`) — all NULL sha, all covered by suppress/acknowledge entries in `migrations/repairs.json`.
- `work_board_items` columns: id, project_slug, title, status, sort_order, design_doc_ref, inline_active, linked_run_id, created_at, updated_at, completed_at, task_type, blocked_by, declared_surfaces. NO `pr`, NO `pr_url`.
- HOW the schema and ledger diverged: the 08-14 premature branch application added `pr`/`pr_url` AND wrote the row; `0130_work_board_items_archived_status.sql` (applied 08-17) rebuilt the table (CREATE new → INSERT SELECT with an explicit column list → DROP → RENAME) and silently dropped the stray columns. The row survived; the schema it attests was destroyed. The scar is now a FALSE WITNESS, and because a scar implies the instance also ran 0130, ANY instance carrying this scar has lost the columns — re-applying the ALTERs is safe fleet-wide.
- #269 (`trident/put-the-pr-number-on-the-card-a-cli`, OPEN) carries `migrations/0133_work_board_items_pr.sql` (`ALTER TABLE work_board_items ADD COLUMN pr INTEGER; ALTER TABLE work_board_items ADD COLUMN pr_url TEXT;`) plus `migrations/__tests__/live-ledger-122-work-board-pr.test.ts` whose fixture models the live state WRONG (hand-adds the columns before boot, then asserts the by-name skip is benign). It does NOT touch `runner.ts` or `repairs.json`. Green branch, broken deploy — exactly the card's claim.
- The current repairs mechanism can only SUPPRESS (an entry's `file_name` names a migration already applied by hand, so it must not run) or acknowledge orphan rows. NOTHING can force a re-apply, and a re-applied file could never record itself in `_migrations` anyway — the name PK already holds the scar row, and doctrine (#575, repairs notes) forbids rewriting or deleting it. Therefore the fix is a new, explicit, hand-verified repair KIND (`reapply`) whose firing is recorded in `_migration_repairs` (in-transaction), leaving row 122 untouched forever.

## Tasks

- [x] Forensics: verify the live trap end-to-end (ledger DDL, scar row, missing columns, 0130 rebuild as the destroyer, #269 contents, current runner semantics). Done by the planner 2026-08-18; recorded above.
- [x] Add the `reapply` repair kind to the migration runner, ship the hand-verified `repairs.json` entry for the v122 `work_board_items_pr` scar, and pin the whole class with a live-replica boot harness (RED control: today's silent skip leaves `pr` absent; GREEN: with the entry the column lands, boot is clean, row 122 is byte-identical, and a second boot is idempotent). Includes README/INVARIANTS/AS_BUILT updates and a PR body that carries the merge-order constraint below.

## Coordination (not code — for the harvester / owner, carried in the PR body and AS_BUILT)

- **#269 must NOT merge until this PR is merged AND deployed** (the entry must be live in the deployed checkout's `repairs.json` before the first post-#269 boot). Cross-reference on card `01M00S2MW24QWP2N0M3W044N19` (which currently overstates #269 as mergeable) and note on `01M095E3F8YN3N9XV5AK4S9XJW` (0133 ordinal). The build lane cannot edit board cards; the PR body + AS_BUILT entry are the hand-off.
- After this lands, #269 must rebase and FIX its `live-ledger-122-work-board-pr.test.ts`: its columns-present fixture is falsified by measurement (0130 dropped them), and with the reapply entry active that fixture now correctly THROWS `duplicate column name: pr` (pinned by this PR's strictness test).
- Deploy order on the live instance is self-sequencing: this fix deploys first and is inert (no file named `work_board_items_pr` in the tree ⇒ the entry does not fire and MUST NOT write its acknowledgment); the first boot after #269 merges applies 0133's ALTERs via the repair.
- Commit .trident/plans/trident/live-armed-trap-migrations-claims-w.md together with your code + tests.
