# IMPLEMENTATION_PLAN.md — Build dispatch refused: the publisher auth probe gets no credential

Plan card: the probe asked a bare `gh` about ambient state instead of the publisher's own credential, and the refusal did not name its cause. Regenerated 2026-08-16 by diffing the card against the code at `fe4b9f40`.

- [x] Find the measured root cause of the 22:19Z→00:05Z flip on a live gateway (do not guess). FOUND AND FIXED (#307 / commit 34c8f952, AS_BUILT 2026-08-15): unscoped `gh auth status` exits 1 when ANY account on ANY host is broken — a stale second (default-keyring) account went invalid under the running process, so a VALID publisher token was reported unauthenticated. `PUBLISHER_AUTH_COMMAND` is now scoped `gh auth status --hostname github.com --active` (`trident/git-mode.ts`), with `unknown flag` on old gh classified `publisher_cli_unavailable`, never as a bad token.
- [x] The probe uses the same credential path as the publisher it probes. `defaultGitModeProbe` REQUIRES a `PublisherCredentialSource` (no default runner/credential; `trident/git-mode.ts:895`), resolved per call via `load()` so a token connected after boot takes effect on the next probe. All three call sites supply it: `open/composer.ts:2068` (store-backed source, one shared probe for `/code` + HTTP + board seam), `trident/board-dispatch.ts` (resolver REQUIRED, no fallback), `onboarding/overnight/register.ts` (`publisher_credential` REQUIRED). `unwiredPublisherCredential()` exists only as the greppable negative control; no production site constructs it.
- [x] The refusal names its cause and the owner handle consulted. `PublisherAuthFailureCause` (9 members, exhaustiveness enforced by a `satisfies Record<…>` map) + `describePublisherAuthFailure` render a distinct, action-correct sentence per cause including `owner handle "<handle>" (<source>)`; multi-line `gh` output flattened, length-capped, never line-selected.
- [x] Guard: a valid stored token with no ambient `gh` auth returns `authenticated: true`, asserted through the token-store seam. `trident/git-mode-credential-seam.test.ts` drives a REAL `SecretsStore` on a temp db through the composer's exact construction (`githubProcessEnv(await readGitHubToken(...))`); `process.env` never mutated. Boot wiring proven BY ROUND-TRIP + identity in `open/__tests__/open-trident-prod-boot-wiring.test.ts`.
- [x] Ambiguity resolved by measurement, not regex: `gh auth status` prints byte-identical output for a bad token and three transport failures, so the ambiguous branch asks a second question (`gh api --hostname github.com /zen`, 15s budget) and classifies refusal / SSO / rate-limit / unreachable / reachable / inconclusive; `spawnCapture` watchdog records `timed_out`; mutation harness (`trident/git-mode-mutation.test.ts`, 11 mutations + positive control) checked in; AS_BUILT Rounds 1–5 documented.
- [x] Fix the one remaining environment-sensitive guard: `trident/gh-authed.test.ts` "with no stored token: no GH_TOKEN, no git config injection at all" asserts ABSOLUTE ABSENCE from a child env that by design inherits `process.env` (gh-authed invariant 4), so it reddens on any host whose shell carries an ambient `GH_TOKEN`/`GIT_CONFIG_*` — verified red in this checkout under the build-lane env and green under `env -u GH_TOKEN -u GIT_CONFIG_KEY_0 -u GIT_CONFIG_VALUE_0`. Make the assertion inheritance-relative (the built env IS `{...process.env}` with nothing injected), so the guard asserts what gh-authed adds, not what the test host's shell contains — the same "seam, not environment" discipline the card mandates.

Non-tasks, noted so they are not duplicated: the `open-trident-prod-boot-wiring.test.ts` error `Cannot find module '@neutronai/calendar-core'` is a stale `node_modules` in the repo-of-record checkout (the workspace package exists at `cores/free/calendar` and `gateway/package.json` declares it) — run `bun install` in the build worktree, do not change code. Cards `01M00K3ZC9E2E32HR3YGMNB4CV` and `01M00WKJ4BYC4D33ETE0K9NWWH` cover WHERE the credential row lives and are out of scope here.
