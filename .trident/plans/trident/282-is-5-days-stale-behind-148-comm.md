# IMPLEMENTATION_PLAN — #282 re-spec: port the 245-line residual onto a fresh branch from main

Regenerated 2026-08-19 against origin/main `5895e268`. Card: PR #282 (`trident/a-build-with-no-pr-number-must-reso`, merge base `39e3b6b2`, 5 branch commits vs 149 on main) carries real un-landed work but is the thinner, older copy of 6 heavily-diverged files — merging or rebasing it deletes ~7,000 lines of main. Plan: rebuild its residual on a fresh branch cut from current main (this lane's branch IS that branch), then close #282 with a pointer. Reference the old branch READ-ONLY via `git show f48ba46d:<path>` (tip `f48ba46d`).

## Measured against main `5895e268` (planner-verified 2026-08-19)

- `isInfraDeath` / `infraDeathSentence` / `findings_present` / `unpublishedCommitAlarm*` / `detectUnpublishedCommit`: 0 hits anywhere on main — T4's behaviour is absent, T3's symbols are absent.
- `parseInnerResult` (`trident/inner-loop.ts` ~627) still decodes `prNumber` with `Number.isFinite` — the pr=0 sentinel clobber is live. All three `trident/inner-workflow.mjs` sites are still old-shape (~2185 `checkpoint()`, ~2243 `writeTerminalResult()`, ~5982 forge adoption). Render-side `pr > 0` guards exist (`composeTerminalDelivery`) but the ROW can still be erased 267 → 0.
- Both terminal failed transitions in `trident/orchestrator.ts` still write `inner_verdict: 'REQUEST_CHANGES'` for infrastructure deaths — the catch-all (~3070) and the #389 infra-retry-budget-exhausted site (~2945). `run-progress.ts:211` surfaces that field to the owner.
- **T3 IS SUPERSEDED**: #389 (`ec3bcc56`) + #446 (`5895e268`) added `reconcile_stranded` at the exact choke point the branch's T3 wanted (`step` wraps `stepCore`, probes newly-failed pr-mode runs, orchestrator.ts ~3419-3438) and does MORE than alarm: it PUBLISHES a stranded ahead-of-base commit as a salvage PR (`TRIDENT_SALVAGE_MARKER`) and anchors dirty-worktree snapshots at `refs/tags/trident-salvage/<run.id>`.
- Main machinery the T4 port must ADAPT to (all newer than #282's base): `classifyInnerFailure` (#389, retry-side, fail-closed to `'genuine'`); `trident/infra-block.ts` `deriveInfraBlock` (structured infra-only delivery with the 🚧 deferral copy, checked FIRST in `interpretFailure`); `innerTerminalFailureReason`'s widened measured-cause branch (`block_kind === 'infra-only' || null` with a non-empty redacted cause); the .mjs catch path now emits `terminalCause: infraCause(...)` and writes NO `findings` key at all (absent still decodes `findings_present: false` fail-closed).

## Tasks

- [x] 1. **Port the prNumber-0 sentinel fix** (#282 commit `639399d6`): `parseInnerResult` decodes a non-positive / non-integer `prNumber` to `null`; `inner-workflow.mjs` `checkpoint()` / `writeTerminalResult()` / forge-adoption write/adopt only a positive-integer PR; the wrapper's `PR_NUMBER=0` trailer contract (FORGE_PR_LINE) stays byte-identical. Tests: sentinel decode test in `trident/inner-loop.test.ts`, 4-test SRC describe in `trident/inner-workflow.test.ts`, harvest regression ("an inner-error harvest carrying prNumber 0 keeps the known PR on the failed row") in `trident/orchestrator.test.ts`.
- [ ] 2. **Port T4 — an infrastructure death is never a verdict** (#282 commit `4dcba892`), adapted to main's evolved shape: add `findings_present` (fail-closed: only a non-empty `findings` array decodes true) to `InnerResult` + `parseInnerResult`; add `isInfraDeath` to `orchestrator.ts` and `infraDeathSentence(round, ceiling)` to `delivery.ts` (the matched strings are authored in exactly two places and move together); widen `innerTerminalFailureReason`'s result Pick with `'ok' | 'verdict' | 'findings_present'` and add the `isInfraDeath` branch AFTER main's measured-cause branch (it catches only the NO-measured-cause shapes: inner-error with no findings, infra-only with no cause); null the verdict at BOTH failed transitions (`inner_verdict: isInfraDeath(result) ? null : 'REQUEST_CHANGES'` at the catch-all ~3070 AND — adaptation, site is newer than the branch — the #389 budget-exhausted site ~2945); route the authored infra sentences to `klass: 'infra'` in `interpretFailure` EARLY (after the crash-recovery-budget branch, before the hang branch — their embedded measured words like 'stalled'/'exhausted' are not ours to keyword-proof): `'build infrastructure failed'`, `'review never ran (infra-only)'`, plus (adaptation for main's 2026-08-15 thrown-with-cause sentence, today falling to the non-echoing unknown fallback) `'inner workflow failed at round'`; add `blockKind` + `findings` passthrough to `SimResult`/`simResultJson`; docstring the relationship `isInfraDeath` (verdict honesty, fail-closed false) vs `classifyInnerFailure` (retry eligibility, fail-closed genuine) — two deciders, deliberately different, do not merge them. Adapt the branch's tests (`git show f48ba46d:trident/orchestrator.test.ts`, `...terminal-failure-reason.test.ts`, `...delivery.test.ts`, `...inner-loop.test.ts`) to main's suites — never restore branch copies; update `terminal-failure-reason.test.ts` pins for the shapes that flip to the infra sentence (~lines 74-180, 361, 420, 443) and add the three new Pick fields at every `innerTerminalFailureReason` call-site literal (the compiler will enumerate them).
- [x] 3. **T3 unpublished-commit alarm — superseded on main, not ported.** `reconcile_stranded` (#389/#446) publishes the stranded commit instead of alarming; same choke point, strictly stronger. Do NOT port `detectUnpublishedCommit` / `unpublishedCommitAlarmSentence` / `unpublishedCommitAlarm` / the `step`→`stepInner` rename (main already has `step` wrapping `stepCore` with the salvage hook installed).
- [ ] 4. **Wire-check + close #282.** Verify the sentinel fix is wired: `git grep -n "isInfraDeath\|infraDeathSentence" trident/ | grep -v test` must show non-test callers (orchestrator.ts terminal transitions + `innerTerminalFailureReason`; delivery.ts authors + reads back). Then close #282 UNMERGED with a comment naming this lane's replacement PR (resolve it via `gh pr list --head <this lane's branch>`); if `gh` is unauthenticated/unavailable in the build env, leave this task unchecked and say so plainly in the report — never guess a PR number.

## Do not

- Do NOT check out, merge, rebase, or resolve conflicts on `trident/a-build-with-no-pr-number-must-reso`. Every large conflict resolves toward main; the branch contributes nothing in those regions.
- Do NOT restore the branch's copies of `orchestrator.test.ts` (2,629 lines behind main) / `terminal-failure-reason.test.ts` / `inner-workflow.mjs` — only its ADDITIONS port, rewritten against main's current files.
- Do NOT close #282 as "superseded" (the behaviour was not on main when this card was cut) and do NOT leave it open past task 4.
