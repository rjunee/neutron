# IMPLEMENTATION_PLAN — #282 re-spec: port the 245-line residual onto a fresh branch from main

Round 3, regenerated 2026-08-19 against origin/main `5895e268` (unmoved since rounds 1–2 — no drift to reconcile). Card: PR #282 (`trident/a-build-with-no-pr-number-must-reso`, merge base `39e3b6b2`, 5 branch commits vs 148 on main) carries real un-landed work but is the thinner, older copy of 6 heavily-diverged files — merging or rebasing it deletes ~7,000 lines of main. Plan: rebuild its residual on a fresh branch cut from current main (this lane's branch IS that branch), then close #282 with a pointer. Reference the old branch READ-ONLY (`git show f48ba46d:<path>`); never check it out, merge it, or resolve its conflicts.

## Measured state (planner re-verified this round, 2026-08-19)

- Branch tip `e93486fa` = origin/main `5895e268` + task 1 (`260782b0`) + task 2 (`e93486fa`). All seven touched suites pass at tip: 473 pass / 0 fail (`terminal-failure-reason`, `delivery`, `inner-loop`, `inner-workflow`, `infra-retry`, `inner-workflow-built-head` = 324; `orchestrator` = 149).
- Wire-check ALREADY PASSES on this branch: non-test callers exist — `trident/orchestrator.ts` imports `infraDeathSentence` (line 83), defines `isInfraDeath` (1705), calls it at 1796–1797 (`innerTerminalFailureReason`), 2992 (budget-exhausted failed transition), 3119 + 3125 (catch-all failed transition); `trident/delivery.ts:384` authors `infraDeathSentence`; `trident/inner-loop.ts:686` decodes `findings_present` fail-closed. Task 4 re-runs the greps as its gate.
- gh reads are authenticated; #282 is still OPEN (head `trident/a-build-with-no-pr-number-must-reso`); NO PR exists yet for this lane's branch (`gh pr list --head trident/282-is-5-days-stale-behind-148-comm --state all` → empty; the branch is local-only — the lane's publish step pushes it and opens the PR).
- Gap found this round: commit `260782b0` (task 1) carries no `docs/AS_BUILT.md` entry — task 2's commit added only its own. Task 4 adds the missing prNumber-sentinel entry so the changelog matches what merges.

## Tasks

- [x] 1. **Port the prNumber-0 sentinel fix** (#282 commit `639399d6`) — DONE, commit `260782b0`: `parseInnerResult` decodes a non-positive / non-integer `prNumber` to `null`; `inner-workflow.mjs` `checkpoint()` / `writeTerminalResult()` / forge-adoption write or adopt only a positive-integer PR; the wrapper's `PR_NUMBER=0` trailer contract stays byte-identical; sentinel-decode test, 4-test SRC describe, and the harvest regression ("prNumber 0 keeps the known PR on the failed row") all in place.
- [x] 2. **Port T4 — an infrastructure death is never a verdict** (#282 commit `4dcba892`), adapted to main's evolved shape — DONE, commit `e93486fa`: `findings_present` decoded fail-closed (only a non-empty `findings` array is true) in `InnerResult` + `parseInnerResult`; `isInfraDeath` (orchestrator.ts) + `infraDeathSentence(round, ceiling)` (delivery.ts) — the matched strings authored in exactly two places that move together; verdict nulled at BOTH failed transitions (catch-all and #389 budget-exhausted); authored infra sentences routed to `klass: 'infra'` in `interpretFailure`; `blockKind`/`findings` passthrough on `SimResult`; tests adapted onto main's suites, never restored from branch copies; AS_BUILT entry for this change; docstring separates `isInfraDeath` (verdict honesty) from `classifyInnerFailure` (retry eligibility).
- [x] 3. **T3 unpublished-commit alarm — superseded on main, not ported.** `reconcile_stranded` (#389 `ec3bcc56` / #446 `5895e268`) publishes the stranded commit instead of alarming, at the same choke point; strictly stronger. `detectUnpublishedCommit` / `unpublishedCommitAlarmSentence` / `unpublishedCommitAlarm` / the `step`→`stepInner` rename stay unported.
- [ ] 4. **Close out the card.** Re-run the wire-check greps (must show non-test callers of `isInfraDeath` / `infraDeathSentence` — fail the task if empty); add the missing `docs/AS_BUILT.md` entry for the prNumber sentinel (below the infra-death entry, keeping newest-first honest); close #282 UNMERGED with the authored pointer comment — resolve the replacement PR at execution time via `gh pr list --head trident/282-is-5-days-stale-behind-148-comm --state all`, and if none exists yet name this branch and its two ported commits instead; never pass `--delete-branch`; persist this plan with the box checked and evidence. If `gh` cannot close (auth/permission), commit the docs honestly, leave this box UNCHECKED with the exact error quoted, and say so plainly — never guess a PR number or fake the closure. — NOT DONE: harness override: "Do NOT run `gh` at all." GitHub closure and the replacement pointer are delegated to the outer wrapper after this local commit exits.

## Do not

- Do NOT check out, merge, rebase, or resolve conflicts on `trident/a-build-with-no-pr-number-must-reso`. Every large conflict resolves toward main; the branch contributes nothing in those regions.
- Do NOT restore the branch's copies of `orchestrator.test.ts` (2,629 lines behind main) / `terminal-failure-reason.test.ts` / `inner-workflow.mjs` — only its ADDITIONS port, rewritten against main's current files.
- Do NOT close #282 as "superseded" (the behaviour was not on main when this card was cut) and do NOT leave it open past task 4.
- Do NOT touch production code in task 4 — it is a docs + GitHub close-out; the ported code and tests are already committed and green.
- Commit `.trident/plans/trident/282-is-5-days-stale-behind-148-comm.md` together with the AS_BUILT change.
- Commit .trident/plans/trident/282-is-5-days-stale-behind-148-comm.md together with your code + tests.
