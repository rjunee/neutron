# IMPLEMENTATION_PLAN — #282 re-spec: port the 245-line residual onto a fresh branch from main

Round 2, regenerated 2026-08-19 against origin/main `5895e268` (unmoved since round 1 — no drift to reconcile). Card: PR #282 (`trident/a-build-with-no-pr-number-must-reso`, merge base `39e3b6b2`, 5 branch commits vs 148 on main) carries real un-landed work but is the thinner, older copy of 6 heavily-diverged files — merging or rebasing it deletes ~7,000 lines of main. Plan: rebuild its residual on a fresh branch cut from current main (this lane's branch IS that branch, tip `260782b0` = main + task 1), then close #282 with a pointer. Reference the old branch READ-ONLY: `git fetch origin trident/a-build-with-no-pr-number-must-reso` first, then `git show f48ba46d:<path>` (tip) / `git show 4dcba892 -- <path>` (the T4 commit's own diff). Never check it out, merge it, or resolve its conflicts.

## Measured state (planner re-verified this round)

- Task 1 landed on this branch as commit `260782b0` (parseInnerResult prNumber-0 sentinel decode; all three `inner-workflow.mjs` sites positive-integer-guarded; tests in `inner-loop.test.ts` / `inner-workflow.test.ts` / `orchestrator.test.ts`; per-branch plan file created). Do NOT redo it.
- `isInfraDeath` / `infraDeathSentence` / `findings_present`: 0 hits on main and on this branch — task 2's behaviour is still absent.
- Both terminal failed transitions in `trident/orchestrator.ts` still hard-code `inner_verdict: 'REQUEST_CHANGES'`: the no-APPROVE catch-all (~line 3070) and the #389 infra-retry budget-exhausted site (~line 2945). `run-progress.ts` surfaces that field to the owner, so an infrastructure death still reads as a reviewer rejection.
- Main machinery task 2 must ADAPT to (all newer than #282's base): `classifyInnerFailure` (retry-side, fail-closed `'genuine'` — a different decider, do not merge them); `deriveInfraBlock` structured infra delivery checked FIRST in `interpretFailure`; `innerTerminalFailureReason`'s widened measured-cause branch (`block_kind === 'infra-only' || null` + non-empty redacted cause → two measured sentences, including main's `inner workflow failed at round …: <cause>`); the .mjs catch path writes NO `findings` key at all (absent decodes `findings_present: false` fail-closed — NO .mjs change needed for task 2).
- gh is authenticated in this environment; #282 is still OPEN (head `trident/a-build-with-no-pr-number-must-reso`); no PR exists yet for this lane's branch (`gh pr list --head` returned empty), so task 4 must resolve the replacement PR at execution time or name the branch.

## Tasks

- [x] 1. **Port the prNumber-0 sentinel fix** (#282 commit `639399d6`) — DONE, commit `260782b0` on this branch: `parseInnerResult` decodes a non-positive / non-integer `prNumber` to `null`; `inner-workflow.mjs` `checkpoint()` / `writeTerminalResult()` / forge-adoption write/adopt only a positive-integer PR; the wrapper's `PR_NUMBER=0` trailer contract stays byte-identical; sentinel decode test, 4-test SRC describe, and the harvest regression ("prNumber 0 keeps the known PR on the failed row") all in place.
- [x] 2. **Port T4 — an infrastructure death is never a verdict** (#282 commit `4dcba892`), adapted to main's evolved shape: add `findings_present` (fail-closed: only a non-empty `findings` array decodes true) to `InnerResult` + `parseInnerResult`; add `isInfraDeath` to `orchestrator.ts` and `infraDeathSentence(round, ceiling)` to `delivery.ts` (the matched strings are authored in exactly two places and move together); widen `innerTerminalFailureReason`'s result Pick with `'ok' | 'verdict' | 'findings_present'` and add the `isInfraDeath` branch AFTER main's measured-cause branch (it catches only the NO-measured-cause shapes: inner-error with no findings, infra-only with no/over-redacted cause); null the verdict at BOTH failed transitions (`inner_verdict: isInfraDeath(result) ? null : 'REQUEST_CHANGES'` at the catch-all ~3070 AND the #389 budget-exhausted site ~2945); route the authored infra sentences to `klass: 'infra'` in `interpretFailure` EARLY (after the crash-recovery-budget branch, before the hang branch — embedded measured words like 'stalled'/'exhausted'/'git ' are not ours to keyword-proof): `'build infrastructure failed'`, `'review never ran (infra-only)'`, plus (adaptation for main's thrown-with-cause sentence) `'inner workflow failed at round'`; add `blockKind` + `findings` passthrough to `SimResult`/`simResultJson`; docstring the relationship `isInfraDeath` (verdict honesty, fail-closed false) vs `classifyInnerFailure` (retry eligibility, fail-closed genuine). Adapt the branch's tests (`git show 4dcba892 -- trident/terminal-failure-reason.test.ts` etc.) onto main's suites — never restore branch copies; the compiler (`scripts/ci/typecheck-all.sh`) enumerates every call-site literal the widened Pick touches. AS_BUILT entry + this plan file updated in the same commit.
- [x] 3. **T3 unpublished-commit alarm — superseded on main, not ported.** `reconcile_stranded` (#389 `ec3bcc56` / #446 `5895e268`) publishes the stranded commit instead of alarming, at the same choke point; strictly stronger. Do NOT port `detectUnpublishedCommit` / `unpublishedCommitAlarmSentence` / `unpublishedCommitAlarm` / the `step`→`stepInner` rename.
- [ ] 4. **Wire-check + close #282.** Verify the fix is wired: `git grep -n "isInfraDeath\|infraDeathSentence" -- trident/ | grep -v test` must show non-test callers (orchestrator.ts terminal transitions + `innerTerminalFailureReason`; delivery.ts authors + reads back). Then close #282 UNMERGED with a comment naming the replacement (resolve via `gh pr list --head trident/282-is-5-days-stale-behind-148-comm`; if no PR exists yet, name that branch and its ported commit subjects); if `gh` is unauthenticated/unavailable at execution time, leave this unchecked and say so plainly — never guess a PR number.

## Do not

- Do NOT check out, merge, rebase, or resolve conflicts on `trident/a-build-with-no-pr-number-must-reso`. Every large conflict resolves toward main; the branch contributes nothing in those regions.
- Do NOT restore the branch's copies of `orchestrator.test.ts` (2,629 lines behind main) / `terminal-failure-reason.test.ts` / `inner-workflow.mjs` — only its ADDITIONS port, rewritten against main's current files.
- Do NOT close #282 as "superseded" (the behaviour was not on main when this card was cut) and do NOT leave it open past task 4.
- Commit .trident/plans/trident/282-is-5-days-stale-behind-148-comm.md together with your code + tests.
