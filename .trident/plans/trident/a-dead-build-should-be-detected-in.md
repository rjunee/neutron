# IMPLEMENTATION_PLAN — A dead build is detected in seconds (external liveness, not self-report)

Regenerated 2026-08-16 (resume pass) against branch `trident/a-dead-build-should-be-detected-in` @ f019149 (rebased onto main @ f99d6d4). T1+T2 are COMMITTED on that branch — do not redo them. Continue on the SAME branch.

WHAT ALREADY EXISTS (credited, do not rebuild):
- [x] Event-pushed launcher-death detection: the supervision watchdog + in-pool exit handling fire `onChildCrash` → `TridentRunStore.crashRunningByLauncher(generationKey, reason)` which latches `trident_launcher_crashes`, stamps every `running` run on that generation `subagent_status='crashed'`, and re-stamps `last_advanced_at` (`open/wiring/substrates.ts` ~429, `runtime/adapters/claude-code/persistent/supervision.ts`, durable registry pid-probe + sink retry, cross-restart).
- [x] Crash → recovery-not-reap (#267): `step()` routes `subagent_status==='crashed'` through `beginCrashRecovery` (atomic claim, releases the sub-agent slot, budget `crash_recoveries`), relaunching as a continuation; budget spent → terminal with a reason carrying the latched crash text (`trident/orchestrator.ts`).
- [x] Seconds-scale reaction to a LATCHED crash: the 2 s `trident-watch` change-signature watcher wakes the 90 s sweep the moment `crashRunningByLauncher` re-stamps rows (`trident/tick.ts`).
- [x] Backstops retained: `NO_ADVANCE_HANG_MS` (90 min) and `DEFAULT_MAX_INFLIGHT_MS` (2 h) in `trident/liveness.ts` + `orchestrator.ts` — this card must not remove them.

THE GAP: detection is push-only. If the crash EVENT is missed — gateway down at death time, sink write failure past its retry window, a latch-predicate race — nothing ever ASKS "is the recorded launcher generation still a live process?", and the lane sits until the 90-minute reaper with a reason that does not say it died.

- [x] T1 — trident-owned POLLED liveness loop in `trident/tick.ts` (COMMITTED f019149): injectable seams `probe_launcher_alive(run) → 'alive'|'dead'|'unknown'` + `latch_launcher_dead(session_key, reason)` + `liveness_interval_ms` (default `LIVENESS_PROBE_INTERVAL_MS = 15_000` in `trident/liveness.ts`); a third SupervisedLoop `trident-liveness` probes every non-terminal `subagent_status==='running'` run with a recorded `workflow_run_id`, deduped per generation; a POSITIVE 'dead' latches through the injected latch with a reason prefixed `inner workflow child crashed:` (never containing "exhausted"), then wakes the sweep directly. 'alive'/'unknown'/throw → NO action. Seams absent → tick.ts byte-identical (`describeAll()` shows no third timer). Pinned by `trident/tick-liveness.test.ts` (432 lines) + `trident/liveness.test.ts`.
- [x] T2 — the production probe (COMMITTED f019149): `probeLauncherGenerationAlive(generationKey, replRegistryPath): 'alive'|'dead'|'unknown'` exported from `runtime/adapters/claude-code/persistent/supervision.ts` — pool session whose `childGeneration` matches → alive iff `!hasChildExited()`; else registry record with matching `child_generation` → `isPidAlive(record.pid)`; found NOWHERE / unreadable registry / any throw → 'unknown'. Pinned by `runtime/adapters/claude-code/persistent/__tests__/launcher-liveness-probe.test.ts`.
- [x] T3 — wiring: add `probe_launcher_alive?` to the `trident` input type (`gateway/composition/input/misc-input.ts`), thread it in `gateway/composition/build-core-modules.ts` into `TridentTickLoop` with an additive `store.failRunningByLauncher` terminal latch; produce the probe in `open/wiring/trident-launcher-liveness.ts` and wire it in `open/composer.ts`. Ordinary pushed crashes retain the existing recovery latch.
- [x] T4 — end-to-end pin + docs: integration test (real store + real orchestrator step) proving a hard-dead generation goes terminal on detection, clears its lane claim, and carries a death-specific reason; a slow-but-alive run remains untouched. The existing long reaper remains the ambiguity backstop.

DESIGN DECISION (round-5 correction): the acceptance criterion controls — a positive external death goes terminal immediately, releases its lane, and invokes the normal terminal delivery/board hooks. It does not consume the ordinary pushed-crash recovery budget.

OUT OF SCOPE (guard-railed): `trident/inner-workflow.mjs` and `trident/board-dispatch.ts` untouched (other in-flight builds own those). The 90-min/2-h backstops stay exactly as they are.
