# IMPLEMENTATION_PLAN — the brief-parts writer must emit receipts the persisted bytes provably match, and a receipt refusal must surface on the run record

Regenerated 2026-08-18 against main (89de7523). Card: run `f784c03a` refused brief part `r1.brief.a1` at 27893:ff41febe vs receipt 28462:9f34d3b0 (569 bytes short). `codex-build.sh` fail-closed correctly and MUST NOT be weakened; the work is (1) harden the writer so a receipt is computed from bytes actually persisted, (2) a truncated-write regression test that is RED without the fix, (3) surface a brief-part receipt refusal on the run record and the board instead of only `/tmp/trident-codex-build-<run>-r1.err`. DISTINCT from cards 01M0274598 (parts env never arrives) and 01M07AMS6Z (arrival/forge:build emission) — do not touch those paths. The run's terminal publish failure belongs to 01M088BV9V — out of scope. No false-success fix is claimed: the run record shows phase=failed.

PLANNER SURVEY CORRECTION (2026-08-18, read from main): `r1.brief.a1` (`/tmp/trident-codex-build-<uniq>-r1.brief.a1`) is the WORKFLOW-COMPOSED head segment written by the BRIDGE MODEL executing rendered heredoc/printf chunk calls (`inner-workflow.mjs` `codexBuildPrompt`/`renderChunks`), not a file `trident/brief-parts.ts` writes; its receipt (`a1Integrity`) is computed in-process by the `.mjs` from the composed string, and the chunk-reassembly property is test-pinned. The defect that IS in `trident/brief-parts.ts`: `writeBriefParts` writes the host-held task/reflection parts with `writeFileSync` and returns receipts computed from the IN-MEMORY string — nothing ever verifies the bytes on disk, and every error is swallowed to a silent `null`. That is the card's named class ("receipt computed from the in-memory buffer") and it is hardened below; the bridge-copy class stays receipt-guarded by the unchanged wrapper check and becomes VISIBLE via tasks 2–3.

- [x] Fail-closed per-part receipt verification in `trident/codex-build.sh` (`CODEX_BUILD_BRIEF_PART_CORRUPT`, exit 3, names file + both measurements) — the verifier that caught run `f784c03a`. It worked; keep it byte-unchanged in task 1, and any task-3 change must be justified as not weakening fail-closed behaviour.
- [x] By-path manifest authority + per-part receipts (landed 2026-08-16, AS_BUILT): `buildWorkflowFirer` (`trident/inner-loop.ts` ~709) is the non-test production caller of `writeBriefParts` — the caller falsification criterion 2 requires; hardening flows through it with no wiring change.
- [x] `briefIntegrity` TS/JS parity pinned across the module boundary (`trident/brief-parts.test.ts` scrapes the `.mjs` twin), including the lone-surrogate → U+FFFD contract.
- [x] 1. Harden `writeBriefParts` (`trident/brief-parts.ts`): read back every part after writing and compute the receipt from the PERSISTED bytes (byte-domain FNV twin `bufferIntegrity`, parity-pinned against `briefIntegrity`); on mismatch rewrite ONCE and re-verify; still mismatched → report loudly through a `warn` seam (default `console.error`; measurements only, never brief text) and return `null` — NEVER a manifest whose receipts disagree with disk, and NEVER a degraded task-only manifest when the reflection part fails verification. Injectable `io` seam (`write_file`/`read_file`) so a truncated write is simulable; the currently-silent `catch { return null }` also becomes loud. Regression tests: 569-byte truncation → null + warn + retry observed (RED if the read-back verify is removed, GREEN with it); truncate-once-then-faithful → manifest returned with receipts matching on-disk bytes; reflection-only truncation → null. `codex-build.sh` untouched.
- [x] 2. Carry the wrapper's measured refusal into the terminal reason: add a required `wrapperErrTail` string to `CODEX_FORGE_SCHEMA` (`inner-workflow.mjs`), instruct the codex-build/collect/wait bridges to copy the bounded tail of `<run>-r<N>.err` verbatim whenever `codexStatus !== 'connected'`, and include it in `forgeAgent`'s deferral throw — so `CODEX_BUILD_BRIEF_PART_CORRUPT: ... measures X but its receipt is Y` lands in `failure_reason` via the existing thrown-workflow path (`infraCause` cap 500) and is readable on the run record and board without opening `/tmp`. Keep the word `deferred` in the thrown sentence so `classifyInnerFailure` (`trident/orchestrator.ts`) still classifies it infrastructure; tests pin prompt text, throw content, and unchanged classification.
- [ ] 3. Host-recorded refusal alert (covers the SURVIVED case, where the bridge's one retry clears the corruption and nothing throws): migration 0132 adds nullable `brief_alert TEXT` to `code_trident_runs` (+ `migrations/expected-schema.txt`); `trident/checkpoint.sh` whitelists a `brief_alert` field (sql-escaped, NOT liveness-frozen, mirroring `branch`); `trident/codex-build.sh` writes the SAME stderr sentence via the already-threaded `NEUTRON_CODEX_BUILD_CHECKPOINT_*` env on the three brief-integrity refusals (`_PART_MISSING`, `_PART_CORRUPT`, `_CORRUPT`) with a `|| echo ... >&2` guard, THEN still exits 3 unchanged — justification: a best-effort recording before the same exit does not weaken fail-closed behaviour; stderr tags and exit codes are byte-identical. Store/row mapping in `trident/store.ts`. Tests: `codex-build.test.ts` (corrupt part → alert row written AND exit 3 + stderr unchanged; no checkpoint env → behaviour unchanged), `checkpoint-sh.test.ts` (field whitelisting, escaping, terminal-row write allowed).
- [ ] 4. Board visibility of `brief_alert`: thread it through `RunProgress` (`trident/run-progress.ts` `deriveRunProgress`), the work-board surface/wire types, and render on `landing/chat-react/WorkBoardTab.tsx` + `app/lib/work-board-helpers.ts` so a run whose brief part failed its receipt shows the reason on the card even when the run proceeded; tests at derive + render layers. AS_BUILT entry per task; INVARIANTS line for "brief-part receipts are measured from persisted bytes" with task 1.
- Commit .trident/plans/trident/the-brief-parts-writer-emits-a-segm.md together with your code + tests.
