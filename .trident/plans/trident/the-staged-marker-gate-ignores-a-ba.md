# IMPLEMENTATION_PLAN — the staged-marker gate catches a bare `=======` separator

Regenerated 2026-08-16 against origin/main c5054a4 (⚠️ the local checkout at 4eb3adc is BEHIND origin — always `git fetch origin` and branch from `origin/main`, never local main). The gate under this card is `CONFLICT_MARKER_ADDED` + `stagedMarkerFiles` in `trident/orchestrator.ts` (landed via PR #295); `scripts/ci/` contains NO marker gate (verified by search), and the local-merge path (`merge.ts` `rebaseBranchOntoBase`) has no staged-byte scan at all — the publish-path scan is the single gate this card names. No open PR exists for this card (checked `gh pr list`, searched "separator"/"marker").

- [x] Outer markers are caught at any width: `CONFLICT_MARKER_ADDED = /^\+(?:<{7,}|>{7,})(?: |\t|$)/` (orchestrator.ts:450 on origin/main) — `{7,}` covers `conflict-marker-size`, proven by a real-git test (`publish-rebase-realgit.test.ts` "a LONGER marker under conflict-marker-size").
- [x] The scan is candidate-scoped and fail-closed: `stagedMarkerFiles` (orchestrator.ts ~758) scans only paths that were ever unmerged in THIS replay, reads `git diff --cached -U0`, and throws `unreadableConflictState` when git cannot be asked (orchestrator.test.ts pins both fail-closed reads).
- [x] A resolver staging a file with outer markers still inside is refused (stub-host test at orchestrator.test.ts ~1020, real-git test at publish-rebase-realgit.test.ts ~366) and a clean resolution publishes (the happy-path resolved test reaching phase 'done').
- [x] Extend the gate to catch a bare `=======` separator line — a partially hand-resolved conflict (outer markers deleted, separator left) currently passes `stagedMarkerFiles` and lands on the shared branch. Rule: an ADDED line that is EXACTLY `={7,}` (optional trailing `\r`, nothing else — git's separator never carries a label) is residue, EXCEPT in markdown files (`.md`/`.markdown`), where it is a legitimate setext H1 underline and `-U0` output provides no context to distinguish. Tests: bare separator in a code file REFUSED (stub-host + real-git); setext underline in a resolved `.md` ALLOWED (publish proceeds); suffixed/indented `=` runs in code ALLOWED; existing outer-marker and clean-file tests still green. Update the `CONFLICT_MARKER_ADDED` docblock, append one entry to docs/AS_BUILT.md, state the rule and its residual gap in the PR body.
- [ ] FOLLOW-UP (separate card, do NOT build here): the gate still ignores a bare `|||||||` diff3 base marker — a repo with `merge.conflictStyle=diff3` configured can leave the same class of residue. Same exact-line + width rule would apply; needs its own real-git proof under diff3.
