# IMPLEMENTATION_PLAN — 🐤 CANARY: persist the real round number (`code_trident_runs.round` is 1 on all 195 rows while `inner_checkpoint` records fix-round-2..7)

Regenerated 2026-08-18 against main (89de7523). Card: one pure parser mapping a checkpoint string to its round, wired monotonically into the store's persistence of `inner_checkpoint`, plus the tests the card's falsification criteria demand. The PRIMARY deliverable is the end-to-end traversal (build → review → publish → record); the code change is real but deliberately small.

Survey findings (2026-08-18, read-only, main @ 89de7523):

- No `checkpointRound` or equivalent exists anywhere in `trident/` — nothing in TS derives a round from a checkpoint string, and `round` is only ever written from `patch.round`/`run.round`, which no production caller raises. The column is dead, exactly as the card measured.
- `inner_checkpoint` is persisted by TS at THREE SQL writes, all inside `TridentRunStore` (`trident/store.ts`): `update()` (~line 721 — the orchestrator's `persistRefireReset` writes `outer-published:…` through it, orchestrator.ts:2416-2425), and the full-snapshot `save()` (~line 866) / `saveIfActive()` (~line 914) — the tick's harvest commit, the path that stamps terminal rows with `result.checkpoint` (e.g. `fix-round-7` on exhaustion, orchestrator.ts:2626). The card's "SINGLE existing site" is measured as this one store class; deriving only in `update()` would leave every plain (non-ralph) exhausted run at round=1 — the exact false finding the card opens with. A fourth writer, `trident/checkpoint.sh`, is out-of-process bash and cannot call the TS parser: deliberately out of scope (mid-run rows lag until the next TS-side persist; terminal + refire records become correct going forward). The PR body must state this measured deviation.
- FIELD-ORDER TRAP: the card's `outer-published:<oid>:R:T → R` notation is TRANSPOSED relative to the code. The publisher builds `outer-published:<head>:<remaining_tasks>:<round>[:deviated]` (orchestrator.ts:2415) and the production resume parse reads `round = Number(publishedResume[3])` (inner-workflow.mjs:5403). The round is the LAST numeric field (regex group 3), never the first.
- `round INTEGER NOT NULL DEFAULT 1` (migrations/0077_code_trident_runs.sql:96); no CHECK constraint involves it; no migration is needed and none is permitted.

- [x] The whole canary in ONE branch (indivisible by design — a parser-only or wiring-only slice fails the card's own falsification gates: `tools/lane_review.sh` rejects a new export with no non-test production caller, and the mutant criterion rejects tests that only exercise the parser):
      (1) NEW `trident/checkpoint-round.ts` — pure exported `checkpointRound(checkpoint: string | null): number | null`: `fix-round-N` → N; `outer-published:<40-hex>:<remaining>:<round>[:deviated]` → round (the LAST numeric, group 3); anything else — including `argus-request-changes-round-N` — → null (do NOT guess).
      (2) Wire it into every `TridentRunStore` SQL write of `inner_checkpoint`, monotonic IN SQL against the STORED value (never lowered): `update()` adds `round = MAX(round, ?)` when a checkpoint patch derives a round and no explicit `patch.round` was given; `save()`/`saveIfActive()` change `round = ?` to `round = MAX(round, ?, ?)` binding `run.round` and `checkpointRound(run.inner_checkpoint) ?? 0`.
      (3) Tests: the parser over every shape (including the group-3 transposition pin and the unparseable/do-not-guess cases); store tests proving `update({inner_checkpoint:'fix-round-3'})` leaves round=3 on the row, that EACH of the three write paths derives (deleting any one turns a named test red), monotonicity (`fix-round-2` after `fix-round-3` keeps 3; a stale full-row snapshot never lowers a stored round), and the outer-published round=group-3 pin at the SQL level.
      (4) `bash tools/lane_review.sh <branch>` exits 0 (`checkpointRound` has a non-test production caller: `trident/store.ts`). NO backfill of the 195 historical rows; NO phase-column, CHECK-constraint, or `checkpoint.sh` changes; NO store refactor.
