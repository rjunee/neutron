# IMPLEMENTATION_PLAN — Dependency-aware dispatch: cards declare blockers + surfaces; trident never fans out on an unmet dependency or a surface collision

Regenerated 2026-08-17 against main (89de7523). Card spec: the Plan-card doc "Dependency-aware dispatch" incl. the 2026-08-16 UPDATE (file-level contention now IN scope). Survey result: nothing of this card exists yet — `work_board_items` (0130) has neither `blocked_by` nor `declared_surfaces`; `dispatchBoardBoundBuild` (`trident/board-dispatch.ts`) has no dependency or contention gate; no surface renders a blocked state. Sibling coupling: card `01M03X6FG9JCHBRZFADS29ZNDH` (remove the universal AS_BUILT/IMPLEMENTATION_PLAN contention) is a SEPARATE card that must land in the same window, or the no-surface default serialises the whole board — noted in task 6, not built here. Auto-start-on-unblock is deliberately NOT built (default per spec: the card becomes startable and the board says so).

- [x] Sequencing precondition: run `bb3c8c8e` / `01KZSVZ1NTDT0P224BZX148HKE` (Work Board row state) landed — the card is startable (discharged per the 2026-08-16 update).
- [x] **Data model — migration 0132 + store.** Add nullable TEXT JSON-array columns `blocked_by` + `declared_surfaces` to `work_board_items` (plain ADD COLUMN, STRICT-legal, 0105 pattern); expose both on `WorkBoardItem`; validate every write INSIDE the write transaction: an unknown blocker id, a self-edge, and any cycle are REFUSED with a typed `WorkBoardValidationError` (a cycle is refused, not stored); regenerate `migrations/expected-schema.txt`; store tests incl. the diamond-DAG-accepted control.
- [ ] **Surfaces write/read the new fields.** `work_board_add`/`work_board_update` agent-tool schemas + handlers and HTTP POST/PATCH (`gateway/http/work-board-surface.ts`) accept `blocked_by` and `declared_surfaces`; cycle/unknown-blocker refusals map to clean 400 / `{ok:false,error}` (never a 500); item echoes, `work_board_list`, and the GET snapshot carry both fields; tests for accept/refuse on each surface.
- [ ] **Dependency dispatch gate.** `dispatchBoardBoundBuild` gains rejection code `blocked_by_dependency`: REFUSE when any `blocked_by` card is not `done`, with a typed reason naming the specific unmet blocker (id + title + current status) and instructing the agent to tell the owner in its own words, never raw refusal text (same shape as `underspecified`); `work_board_dispatch_build`, `work_board_start`, and the HTTP ▶ route (409, code `blocked_by_dependency`) all surface it; test proves refusal, then AUTOMATIC admission after the blocker's terminal reconcile (`detachRun` outcome 'done') flips it done — unblock-on-merge with no auto-dispatch.
- [ ] **Contention gate.** `dispatchBoardBoundBuild` gains rejection code `blocked_by_contention`: enumerate LIVE (non-terminal) runs for this project (`TridentRunStore.listNonTerminal` → `board.getByRunId`) and REFUSE when the candidate's declared surfaces intersect an in-flight card's (segment-aware literal-prefix overlap rule — over-blocks, never under-blocks); a card with NO declared surface is universal and serialises against everything; the refusal names the other card AND the overlapping path; it clears automatically because the gate reads live runs per dispatch — asserted by the required test: dispatch two contending cards, second refused, first goes terminal, second admitted with no human re-dispatch. Contention is modelled separately from `blocked_by` (simultaneity vs order — distinct codes, distinct checks).
- [ ] **Blocked is a visible, two-flavoured state.** One shared pure derivation (new `work-board/blocked-state.ts`) computes per-card `blocked: {kind:'dependency', on:[{id,title}]} | {kind:'contention', with:{id,title}, path} | null`, consumed by the HTTP GET + `work_board_changed` snapshot, the `<work_board>` per-turn fragment, and `work_board_list`; web `landing/chat-react/WorkBoardTab.tsx` + the app board render DISTINCT badges — "waiting for #N to land" (dependency) vs "waiting for #N to finish" (contention) — and a blocked card never renders a bare startable ▶ in `upcoming`; fragment + component tests.
- [ ] **Docs + decision record.** Record in `docs/SYSTEM-OVERVIEW.md` (trident/work-board sections): the base branch stays `main` ALWAYS, with the reasoning (a non-default base buys parallelism at the price of rebase churn, re-review after every rebase, and hand-maintained merge order — dependency-aware scheduling makes stacking unnecessary), so the request is not re-opened uninformed; the dependency-vs-contention model; the no-surface-serialises default and its coupling to sibling card `01M03X6FG9JCHBRZFADS29ZNDH` (must land in the same window or fan-out is dead); auto-start deliberately not built. Add `docs/INVARIANTS.md` entries for both gates; append the AS_BUILT entry.
