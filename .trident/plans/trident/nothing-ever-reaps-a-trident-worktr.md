# IMPLEMENTATION_PLAN — Nothing ever reaps a trident worktree (proactive reaper + teardown on every terminal path)

Regenerated 2026-08-17 against the repo of record (HEAD 89de7523; branch `trident/nothing-ever-reaps-a-trident-worktr` = origin/main @ #391, no card commits yet). Card: 62 dead `wf_*` holders caused 42% of run failures; teardown exists but only fires on the merge path (`trident/merge.ts:575`). Fix = a proactive reaper on a timer + at startup: /proc-proven liveness, detach dead `trident/*` holders, remove process-free worktrees past a 24h retention, never touch the main checkout, never force, never kill. Live re-measure this iteration: 161 worktrees, 84 `wf_*`, 11 holding `trident/*` branches — the leak is still growing.

SEQUENCING JUDGMENT (unattended call, noted per protocol): the card says land AFTER the git-truth-reconciliation card (01M07YTEDDMDRQRZRSD16ADBTQ). Verified that card is NOT on main (no reconcile-after-failure code, no AS_BUILT entry, no landed PR). Proceeding anyway because this reaper as specified cannot destroy the copy reconciliation would rescue: it NEVER deletes a branch, removal is the plain-`git worktree remove` preservation primitive (dirty → PRESERVED, refused → PRESERVED, never `--force`), and a non-terminal run's worktree is never removed. A failed run's committed work stays on its `trident/*` branch; its uncommitted work blocks removal. Every reaper action leaves the rescueable material in place.

Already satisfied on main (verified in code — do not redo):
- [x] Success-path teardown with preservation semantics: `removeWorktree`/`removeWorktreePath` (`trident/merge.ts:757`/`:345`), plain remove only, dirty → PRESERVED, refused → PRESERVED, called from the merge path (`merge.ts:575`).
- [x] Inner-loop cleanup on every INNER path: `trident/worktree-cleanup.sh` (exit-code contract, #541 preserve-not-force) driven from `inner-workflow.mjs`'s `cleanup:worktree` step + `worktree-cleanup-sh.test.ts`.
- [x] Reactive bind-time branch reclaim: `codex-build.sh` `branch_holder`/`holder_is_live` (/proc-cwd, lsof fallback, unverifiable → refuse) + `CODEX_BUILD_BRANCH_RECLAIMED` detach-in-place; refuses the shared main checkout and live holders.
- [x] One-time manual unblock of 2026-08-17 (62 dead holders detached by hand; operational, already done per the card).

Queued (the card's actual gap — no proactive reaper exists anywhere on main; grep confirms no non-test caller and no module):
- [x] T1 — Build the reaper: new `trident/worktree-reaper.ts` (`sweepTridentWorktrees` + `buildWorktreeReaperLoop` on `SupervisedLoop`), narrow store seam + `TridentRunStore.listRepoPaths()`, export `removeWorktreePath` from `merge.ts` for reuse; real-git tests covering falsification criteria 1–4 plus retention/dirty-preservation and the no-/proc skip (never guess), plus never-force/never-branch-delete source assertions.
- [ ] T2 — Production registration (falsification criterion 5) + terminal-path teardown: compose `buildWorktreeReaperLoop` in the trident module of `gateway/composition/build-core-modules.ts` (register-before-start in the LoopRegistry, `immediate: true` for the startup sweep, `stop()` in module shutdown), append a terminal observer that `wake()`s the reaper so teardown also runs at every failed/stopped transition; pin `trident-worktree-reaper` in BOTH exact-set loop-inventory tests (`gateway/__tests__/loop-inventory-production-composer.test.ts`, `open/__tests__/loop-inventory-open-composer.test.ts`) so deleting the registration turns tests RED; export the module surface from `trident/index.ts`; record in docs per repo conventions.

Deliberate boundaries: the reaper never deletes branches (rescue property; branch teardown stays owned by merge/worktree-cleanup.sh); `.trident-worktrees/rebase-*` publisher worktrees are out of scope (card names `wf_*` only); no new env knobs — defaults as exported constants, options injectable for tests.
