# IMPLEMENTATION_PLAN — mobile: the first tap is eaten while the keyboard is open (key-p1z70d)

Card: docs/plans/mobile-the-first-tap-is-eaten-while-the-keyboard-is-open-key-p1z70d.md (the spec for this build; honour its `## Do not` section).
Root cause (established, do not re-derive): RN scrollables default `keyboardShouldPersistTaps="never"`, so with the keyboard up the first tap outside the input is consumed by keyboard dismissal. Fix value is ALWAYS `"handled"`, NEVER `"always"`.
Verified during planning: FlashList 2.3.2 accepts and forwards the prop (`FlashListProps extends Omit<ScrollViewProps,...>` in dist/FlashListProps.d.ts:31; `{...rest}` spread onto CompatScrollView at dist/recyclerview/RecyclerView.js:357).
Scope fence: do NOT touch composer focus/blur or keyboard-inset arithmetic (`app/lib/keyboard-inset.ts`, `useKeyboardInset` usage in ChatSyncSurface ~543-560) — PR #21 lives there.
Repo rules: never edit docs/AS_BUILT.md on a branch (as-built-write-guard fails the PR) — stage the entry at `.trident/as-built/<branch>.md`. Never put tenant filesystem paths in files OR commit messages — the purity leak gate blocks them. Leave the root IMPLEMENTATION_PLAN.md alone (it is main's #313 record, inherited from base, not this card's plan).
Resume note (2026-08-31, round 4): T1 landed as 2312030c, T2 as ce3eff73, T3 as cf663f4f. Planner re-verified the tip independently with a prototype of the T4 AST matcher: 45 JSX scrollable sites under app/ (excluding tests) = 43 carrying `keyboardShouldPersistTaps="handled"` + 2 carrying an in-tag `// KEYBOARD-TAPS-EXEMPT:` with justification, 0 misses — so the T4 gate ships GREEN against this tree. A raw grep for `<(ScrollView|FlashList|FlatList)\b` counts 49 because 4 hits are TYPE positions (`useRef<ScrollView | null>` at app/components/TaskList.tsx:73, app/components/ReminderList.tsx:95, app/app/login.tsx:374, + one more non-element match): exactly the false-positive class the AST matcher exists to be immune to — lock them as tests. FlatList has ZERO current JSX usages in app/ (gate still covers the tag for the future; the real-tree floor test must not require one). Remaining work: T4 (gate), then T5 (as-built staging + PR verification statement).

- [x] T0 — reference implementation exists: app/app/login.tsx:391 already sets `keyboardShouldPersistTaps="handled"` (the pattern to copy verbatim). Nothing to build.
- [x] T1 — CHAT PATH (owner hits daily; card priority 1): `keyboardShouldPersistTaps="handled"` on app/components/ChatSyncSurface.tsx:876 (FlashList transcript), app/components/ProjectRail.tsx:338, app/components/ProjectTabBar.tsx:122; plus harness test app/__tests__/chat-keyboard-taps-ask.test.tsx asserting the FlashList ASK via `lastFlashListProps()` (the honest boundary: the prop the surface passes is assertable; native touch delivery is a device claim). DONE in 2312030c.
- [x] T2 — MODALS/DRAWERS (card priority 2): same prop on app/components/ReminderEditModal.tsx:101, app/components/TaskEditModal.tsx:102, app/components/ProjectSettingsDrawer.tsx:361, app/components/CommentsSidePane.tsx:268, app/components/ActivityInspectorDrawer.tsx:451. No new test: the harness resolves ScrollView to react-native-web, which swallows the prop; these sites get pinned at source level by T4's CI gate. DONE in ce3eff73.
- [x] T3 — SWEEP THE REMAINING 36 SITES: 34 sites took `"handled"` (22 single-line tags, 12 multi-line tags, across 20 files — full site list in the round-3 plan revision and the cf663f4f diff); 2 tappable-free sites took the in-tag exempt comment instead, per the card's Do-not (app/features/admin/MemoryPane.tsx:45→46, app/app/projects/[id]/backups.tsx:651→652, both `// KEYBOARD-TAPS-EXEMPT: <reason>` with reason ≥12 chars inside the opening tag). Acceptance held at tip: prop occurrences outside __tests__ = 43, `KEYBOARD-TAPS-EXEMPT:` = 2, `bun test app/__tests__` green. DONE in cf663f4f.
- [x] T4 — CI GATE so the 46th scrollable cannot regress this (THIS ITERATION): new scripts/ci/keyboard-taps-check.mjs (+ keyboard-taps-check.d.mts + keyboard-taps-check.test.ts, mirroring wall-clock-bound-check.* structure and mode 100644), wired as CHECK 7 in scripts/ci/lint.sh before `exit "$fail"` (CI already runs lint.sh at .github/workflows/ci.yml:272). Matcher: TS AST (`ts.createSourceFile`, ScriptKind.TSX, setParentNodes true), visit JsxOpeningElement/JsxSelfClosingElement whose tag is ScrollView|FlashList|FlatList (bare Identifier or PropertyAccess tail, e.g. `Animated.ScrollView`); site passes iff (a) attributes include a JsxAttribute named `keyboardShouldPersistTaps`, or (b) the source slice of the opening tag (getStart()..getEnd()) contains `KEYBOARD-TAPS-EXEMPT` followed by ≥12 chars of justification — a bare marker is its own offense class (like wall-clock's 'bare'); marker placement is INSIDE the opening tag only (a leading comment above the element or a JSX child comment does NOT exempt). AST means type refs (`useRef<ScrollView | null>`, `ScrollViewProps`), comments, and strings can never be hits — lock each as a test. Walk app/**/*.tsx excluding node_modules, __tests__, *.test.tsx, .expo, dist, .git, coverage, vendor. MANDATORY tripwires in main(), per the card ("an extraction that finds nothing must not read as a pass"): (1) runtime positive control — hard-coded offending sample must yield exactly its known offenses or exit 1 "positive control failed — matcher broken"; (2) runtime negative control (prop set; justified in-tag marker) must yield 0 offenses or exit 1; (3) zero .tsx files walked ⇒ exit 1; (4) zero scrollable sites matched ⇒ exit 1. Companion test additionally pins a real-tree floor (≥20 sites found under the repo's app/, 0 offenses) and covers scanTree on scratch dirs (offense detected; empty dir reports zero files). Acceptance: gate exits 0 on this tree printing 45 sites / 2 exempt; `bun test scripts/ci/keyboard-taps-check.test.ts` and `bun test app/__tests__` green; `bash scripts/ci/lint.sh` fully green (proves T1–T3 covered every site); typecheck green.
- [ ] T5 — stage the as-built entry at `.trident/as-built/trident/fix-the-mobile-first-tap-is-eaten-w.md` (format: `## 2026-08-31 — <unique heading>` + prose body, cf. the staged example in that dir) and write the PR verification statement per the card's bar: VERIFIED = prop present at source on every one of the 45 sites (now gate-enforced), FlashList pass-through cited from vendored library source, chat FlashList ask asserted in harness; NOT VERIFIED = actual first-tap delivery with a real keyboard — unit tests do not exercise touch delivery; needs a device/simulator check with the keyboard up, stated plainly, not claimed from a green suite.
