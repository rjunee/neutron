# IMPLEMENTATION_PLAN — mobile: the first tap is eaten while the keyboard is open (key-p1z70d)

Card: docs/plans/mobile-the-first-tap-is-eaten-while-the-keyboard-is-open-key-p1z70d.md (the spec for this build; honour its `## Do not` section).
Root cause (established, do not re-derive): RN scrollables default `keyboardShouldPersistTaps="never"`, so with the keyboard up the first tap outside the input is consumed by keyboard dismissal. Fix value is ALWAYS `"handled"`, NEVER `"always"`.
Verified during planning: FlashList 2.3.2 accepts and forwards the prop (`FlashListProps extends Omit<ScrollViewProps,...>` in dist/FlashListProps.d.ts:31; `{...rest}` spread onto CompatScrollView at dist/recyclerview/RecyclerView.js:357 — re-confirmed round 5 against the installed package).
Scope fence: do NOT touch composer focus/blur or keyboard-inset arithmetic (`app/lib/keyboard-inset.ts`, `useKeyboardInset` usage in ChatSyncSurface) — PR #21 lives there.
Repo rules: never edit docs/AS_BUILT.md on a branch (as-built-write-guard fails the PR) — stage the entry at `.trident/as-built/<branch>.md`. Never put absolute host filesystem paths in files OR commit messages — the purity leak gate blocks them. Leave the root IMPLEMENTATION_PLAN.md alone (it is main's #313 record, inherited from base, not this card's plan). Never push or run `gh` from the build sandbox — the outer loop publishes and opens the PR (`gh pr create --fill`, so commit messages ARE the PR body).
Resume note (2026-08-31, round 6 — review fixes): T1–T5 landed; this round closes the review findings on the GATE itself, not the sweep. (a) BLOCKER: `keyboardShouldPersistTaps={false}` / `{true}` / a shorthand attribute / `{'never' as const}` all read as "declared the prop" and passed — RN maps the booleans to "never"/"always" and TYPES the prop `boolean | 'always' | 'never' | 'handled'`, so `{false}` is the bug itself and typechecks clean. `forbiddenValueOf` now unwraps `as const`/parens and rejects every literal spelling, and a duplicated attribute is judged on ALL its occurrences (React applies the last). (b) An ALIASED import (`import { ScrollView as NativeScroll }`) was invisible to both the prefilter and the matcher; import bindings are now resolved per file and the prefilter admits the alias form. (c) Real-tree floor tightened from >=44 to >=45 (the swept count) so losing a site cannot stay green; `scanTree` uses `relative()` so a rootDir with a trailing slash no longer chops the first character off every rel path. Verified at this tip: `bun scripts/ci/keyboard-taps-check.mjs` exits 0 printing "45 sites checked (2 exempt)"; the same gate against a scratch tree of the five reported bypasses (bool, shorthand, as-const, duplicate, alias) exits 1 and names all five; `bun test scripts/ci/keyboard-taps-check.test.ts` 53 pass / 0 fail. Chat-path props unchanged at source (ChatSyncSurface.tsx:881, ProjectRail.tsx:340, ProjectTabBar.tsx:125); both exemptions still argued (app/features/admin/MemoryPane.tsx:46, app/app/projects/[id]/backups.tsx:652); CHECK 7 still wired in scripts/ci/lint.sh. STILL NOT VERIFIED, and not verifiable here: actual first-tap delivery with a real keyboard — that needs a device/simulator.

- [x] T0 — reference implementation exists: app/app/login.tsx:391 already sets `keyboardShouldPersistTaps="handled"` (the pattern copied verbatim). Nothing to build.
- [x] T1 — CHAT PATH (owner hits daily; card priority 1): `keyboardShouldPersistTaps="handled"` on app/components/ChatSyncSurface.tsx (FlashList transcript), app/components/ProjectRail.tsx, app/components/ProjectTabBar.tsx; plus harness test app/__tests__/chat-keyboard-taps-ask.test.tsx asserting the FlashList ASK (the honest boundary: the prop the surface passes is assertable; native touch delivery is a device claim). DONE in 2312030c.
- [x] T2 — MODALS/DRAWERS (card priority 2): same prop on ReminderEditModal, TaskEditModal, ProjectSettingsDrawer, CommentsSidePane, ActivityInspectorDrawer. Pinned at source level by T4's CI gate (harness resolves ScrollView to react-native-web, which swallows the prop, so no runtime test). DONE in ce3eff73.
- [x] T3 — SWEEP THE REMAINING 36 SITES: 34 sites took `"handled"` across 20 files; 2 tappable-free sites took the in-tag `// KEYBOARD-TAPS-EXEMPT: <reason>` comment instead per the card's Do-not (MemoryPane, backups diff body; reasons ≥12 chars inside the opening tag). Prop occurrences outside __tests__ = 43, exemptions = 2, app tests green. DONE in cf663f4f.
- [x] T4 — CI GATE: scripts/ci/keyboard-taps-check.mjs (+ .d.mts + .test.ts), wired as CHECK 7 in scripts/ci/lint.sh. TS-AST matcher (JsxOpeningElement/JsxSelfClosingElement, tag ScrollView|FlashList|FlatList incl. PropertyAccess tail) — type refs/comments/strings can never be hits; site passes iff the opening tag declares `keyboardShouldPersistTaps` or carries in-tag `KEYBOARD-TAPS-EXEMPT` + ≥12 chars justification (bare marker = its own offense class). Mandatory tripwires all present and verified: runtime positive control, runtime negative control, zero-.tsx-files ⇒ exit 1, zero-sites ⇒ exit 1. Real-tree floor pinned in the companion test. DONE in 9332bd1c; re-verified green at tip round 6 (gate exit 0 "45 sites checked (2 exempt)"; 53/53 tests, incl. the boolean/alias/duplicate bypasses closed in review round 3).
- [x] T5 — stage the as-built entry at `.trident/as-built/trident/fix-the-mobile-first-tap-is-eaten-w.md` (exact drafted content in the execution spec; contract: NOTHING before the `## ` heading, EXACTLY one `## ` heading in the file, heading matches /^## \d{4}-\d{2}-\d{2} — .+/ with a real em dash — enforced by trident/as-built-staged-entries.test.ts via foldEntryIntoLog) and carry the PR verification statement in the T5 commit message body (outer loop opens the PR with `gh pr create --fill`): VERIFIED = prop at source on all 45 sites (gate-enforced), FlashList pass-through cited from installed package source, chat FlashList ask pinned in harness, PR #21 inset tests green; NOT VERIFIED = actual first-tap delivery with a real keyboard — unit tests do not exercise native touch dispatch; needs a device/simulator check, stated plainly, never claimed from a green suite. Acceptance: `bun test trident/as-built-staged-entries.test.ts` green; `bun scripts/ci/keyboard-taps-check.mjs` exit 0; commit touches ONLY the new staged file (+ this plan file); docs/AS_BUILT.md, root IMPLEMENTATION_PLAN.md, and all app/ code untouched; no absolute filesystem paths in the new file or the commit message (purity gate).
