# IMPLEMENTATION_PLAN — mobile: the first tap is eaten while the keyboard is open (key-p1z70d)

Card: docs/plans/mobile-the-first-tap-is-eaten-while-the-keyboard-is-open-key-p1z70d.md (the spec for this build; honour its `## Do not` section).
Root cause (established, do not re-derive): RN scrollables default `keyboardShouldPersistTaps="never"`, so with the keyboard up the first tap outside the input is consumed by keyboard dismissal. Fix value is ALWAYS `"handled"`, NEVER `"always"`.
Verified during planning: FlashList 2.3.2 accepts and forwards the prop (`FlashListProps extends Omit<ScrollViewProps,...>` in dist/FlashListProps.d.ts:31; `{...rest}` spread onto CompatScrollView at dist/recyclerview/RecyclerView.js:357).
Scope fence: do NOT touch composer focus/blur or keyboard-inset arithmetic (`app/lib/keyboard-inset.ts`, `useKeyboardInset` usage in ChatSyncSurface ~543-560) — PR #21 lives there.
Repo rules: never edit docs/AS_BUILT.md on a branch (as-built-write-guard fails the PR) — stage the entry at `.trident/as-built/<branch>.md`. Never put tenant filesystem paths in files OR commit messages — the purity leak gate blocks them.
Resume note (2026-08-31, round 3): T1 landed as 2312030c (chat path; harness test `app/__tests__/chat-keyboard-taps-ask.test.tsx` green). T2 landed as ce3eff73 (all five modal/drawer sites). 36 bare JSX scrollable sites remain at the branch tip. Every former "inspect" flag was resolved this round by reading each subtree at the tip, so T3's verdicts are FINAL: 34 sites take `"handled"`; exactly 2 are tappable-free and take the exempt comment instead. The markdown preview scrollers (PersonalityPane:446/:466, docs-panes:355/:379, and the docs viewer/history scrollers) all render `RenderMarkdown` or Pressable rows — `app/lib/markdown-render.tsx` ships a Pressable copy button (line 144) and `Linking.openURL` link handling (line 69), so markdown content IS tappable and those sites take the prop. Line numbers below are exact at branch tip ce3eff73.

- [x] T0 — reference implementation exists: app/app/login.tsx:391 already sets `keyboardShouldPersistTaps="handled"` (the pattern to copy verbatim). Nothing to build.
- [x] T1 — CHAT PATH (owner hits daily; card priority 1): `keyboardShouldPersistTaps="handled"` on app/components/ChatSyncSurface.tsx:876 (FlashList transcript), app/components/ProjectRail.tsx:338, app/components/ProjectTabBar.tsx:122; plus harness test app/__tests__/chat-keyboard-taps-ask.test.tsx asserting the FlashList ASK via `lastFlashListProps()` (the honest boundary: the prop the surface passes is assertable; native touch delivery is a device claim). DONE in 2312030c.
- [x] T2 — MODALS/DRAWERS (card priority 2): same prop on app/components/ReminderEditModal.tsx:101, app/components/TaskEditModal.tsx:102, app/components/ProjectSettingsDrawer.tsx:361, app/components/CommentsSidePane.tsx:268, app/components/ActivityInspectorDrawer.tsx:451. No new test: the harness resolves ScrollView to react-native-web, which swallows the prop — nothing observable to assert; these sites get pinned at source level by T4's CI gate. DONE in ce3eff73.
- [x] T3 — SWEEP THE REMAINING 36 SITES (verdicts final; one commit).
  HANDLED, single-line opening tag (22 sites — insert the prop immediately after the tag name, cf. ProjectSettingsDrawer.tsx:361): app/app/codegen.tsx:154, app/app/cores/[slug].tsx:228, app/app/integrations.tsx:598, app/app/projects/[id]/backups.tsx:253, app/app/projects/[id]/backups.tsx:539, app/app/projects/[id]/cores/dtc-analytics.tsx:201, app/app/projects/[id]/docs.tsx:373, app/app/projects/[id]/workboard.tsx:395, app/app/settings.tsx:305, app/app/usage.tsx:417, app/components/DiagnosticsErrorBoundary.tsx:61, app/components/DocsDrillList.tsx:101, app/features/admin/BackupPane.tsx:117, app/features/admin/BackupPane.tsx:129, app/features/admin/CoresPane.tsx:90, app/features/admin/DiagnosticsPane.tsx:110, app/features/admin/GatewayPane.tsx:40, app/features/admin/MaxAccountPane.tsx:80, app/features/admin/PersonalityPane.tsx:324, app/features/docs/docs-panes.tsx:355, app/features/docs/docs-panes.tsx:379, app/features/docs/docs-panes.tsx:442.
  HANDLED, multi-line opening tag (12 sites — insert the prop as its own attribute line directly below the `<ScrollView` line, matching attribute indentation, cf. ProjectRail.tsx:338-341): app/app/admin.tsx:136, app/app/projects/[id]/launcher.tsx:226, app/app/projects/[id]/settings.tsx:297, app/components/FocusList.tsx:162, app/components/FocusList.tsx:209, app/components/ReminderList.tsx:133, app/components/TaskList.tsx:109, app/features/admin/PersonalityPane.tsx:446, app/features/admin/PersonalityPane.tsx:466, app/features/docs/docs-panes.tsx:229, app/features/docs/docs-panes.tsx:277, app/lib/citation-chip-row.tsx:60.
  EXEMPT (2 sites — zero tappables in subtree; per the card's Do-not, add the opt-out comment INSIDE the opening tag, NOT the prop; break the single-line tag into a multi-line tag to carry it): app/features/admin/MemoryPane.tsx:45 (read-only text/stats, 0 Pressable/Touchable/onPress in file), app/app/projects/[id]/backups.tsx:651 (DiffView — plain selectable Text diff lines only). Comment format the T4 gate will require: `// KEYBOARD-TAPS-EXEMPT: <reason ≥12 chars>` inside the opening-tag span.
  Acceptance: `keyboardShouldPersistTaps="handled"` occurrences in app/ outside __tests__ go 9 → 43; `KEYBOARD-TAPS-EXEMPT:` occurrences go 0 → 2; `bun test app/__tests__` fully green; diff touches exactly the 20 files above.
- [ ] T4 — CI GATE so the 46th scrollable cannot regress this: new scripts/ci/keyboard-taps-check.mjs (+ keyboard-taps-check.d.mts + keyboard-taps-check.test.ts, mirroring wall-clock-bound-check.* exactly), wired as an additional CHECK in scripts/ci/lint.sh before `exit "$fail"`. Matcher: TS AST (ts.createSourceFile, filename *.tsx ⇒ JSX), walk for JsxOpeningElement/JsxSelfClosingElement whose tag is ScrollView|FlashList|FlatList (bare or `X.ScrollView` property tails); offense unless (a) attributes include a JsxAttribute named `keyboardShouldPersistTaps`, or (b) the source slice of the opening tag (getStart()..end) contains `KEYBOARD-TAPS-EXEMPT:` with ≥12 chars of justification (bare marker = its own offense, like wall-clock's 'bare'). AST means comments/strings/type-refs (`useRef<ScrollView|null>`, `ScrollViewProps`) can never be hits — lock each as a test. Walk app/ **/*.tsx excluding node_modules, __tests__, *.test.tsx, .expo. MANDATORY tripwires in main(), per the card ("an extraction that finds nothing must not read as a pass"): (1) runtime positive control — hard-coded offending sample must yield exactly its known offenses or exit 1 "positive control failed — matcher broken"; (2) negative control sample (prop set; justified marker) must yield 0; (3) zero-files-walked ⇒ exit 1; (4) zero-sites-matched ⇒ exit 1. Companion test additionally pins a real-tree floor (≥ 20 sites found under app/). Acceptance: `bash scripts/ci/lint.sh` fully green (proves T1–T3 covered every site).
- [ ] T5 — stage the as-built entry at `.trident/as-built/<current-branch>.md` (format: `## 2026-08-31 — <unique heading>` + body, cf. the staged example in that dir) and write the PR verification statement per the card's bar: VERIFIED = prop present at source on every site (gate-enforced), FlashList pass-through cited from vendored library source, chat FlashList ask asserted in harness; NOT VERIFIED = actual first-tap delivery with a real keyboard — unit tests do not exercise touch delivery; needs a device/simulator check, stated plainly, not claimed from a green suite.
