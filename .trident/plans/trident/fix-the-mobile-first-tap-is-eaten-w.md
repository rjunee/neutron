# IMPLEMENTATION_PLAN — mobile: the first tap is eaten while the keyboard is open (key-p1z70d)

Card: docs/plans/mobile-the-first-tap-is-eaten-while-the-keyboard-is-open-key-p1z70d.md (the spec for this build; honour its `## Do not` section).
Root cause (established, do not re-derive): RN scrollables default `keyboardShouldPersistTaps="never"`, so with the keyboard up the first tap outside the input is consumed by keyboard dismissal. Fix value is ALWAYS `"handled"`, NEVER `"always"`.
Verified during planning: FlashList 2.3.2 accepts and forwards the prop (`FlashListProps extends Omit<ScrollViewProps,...>` in dist/FlashListProps.d.ts:31; `{...rest}` spread onto CompatScrollView at dist/recyclerview/RecyclerView.js:357). Full JSX-site enumeration below is exact (45 sites; the card's ~48 grep included 3 type-refs/comments).
Scope fence: do NOT touch composer focus/blur or keyboard-inset arithmetic (`app/lib/keyboard-inset.ts`, `useKeyboardInset` usage in ChatSyncSurface ~543-560) — PR #21 lives there.
Repo rule: never edit docs/AS_BUILT.md on a branch (as-built-write-guard fails the PR); stage the entry at `.trident/as-built/<branch>.md`.

- [x] T0 — reference implementation exists: app/app/login.tsx:391 already sets `keyboardShouldPersistTaps="handled"` (the pattern to copy verbatim). Nothing to build.
- [x] T1 — CHAT PATH (owner hits daily; card priority 1): add `keyboardShouldPersistTaps="handled"` to app/components/ChatSyncSurface.tsx:876 (FlashList transcript), app/components/ProjectRail.tsx:338, app/components/ProjectTabBar.tsx:122; plus new harness test app/__tests__/chat-keyboard-taps-ask.test.tsx asserting the FlashList ASK via `lastFlashListProps()` (the honest boundary: the prop the surface passes is assertable; native touch delivery is a device claim).
- [ ] T2 — MODALS/DRAWERS WITH A TextInput (card priority 2): same one-line prop on app/components/ReminderEditModal.tsx:101, app/components/TaskEditModal.tsx:102, app/components/ProjectSettingsDrawer.tsx:361, app/components/CommentsSidePane.tsx:268, app/components/ActivityInspectorDrawer.tsx:451. All five opening tags verified plain `<ScrollView` multi-line tags. Run app/__tests__/comments-side-pane.test.tsx + full app/__tests__ as regression net.
- [ ] T3 — SWEEP THE REMAINING 36 SITES: set `"handled"` where the scrollable's subtree contains any tappable (Pressable/Touchable/onPress/markdown links); where it genuinely has NONE, do NOT add the prop (card Do-not) — instead put `{/* comment */}`-style line comment INSIDE the opening tag: `// KEYBOARD-TAPS-EXEMPT: <reason ≥12 chars>`. Sites: features/admin: MaxAccountPane:80, MemoryPane:45 (exempt candidate — static text/stats only, 0 tappables in file), CoresPane:90, BackupPane:117, BackupPane:129, GatewayPane:40, PersonalityPane:324, PersonalityPane:446 (inspect subtree), PersonalityPane:466 (inspect), DiagnosticsPane:110; features/docs/docs-panes.tsx: 229, 277, 355 (inspect — markdown scroller), 379 (inspect), 442; lib/citation-chip-row.tsx:60 (tappable chips — handled); components: DiagnosticsErrorBoundary:61 (inspect), TaskList:109, DocsDrillList:101, FocusList:162, FocusList:209, ReminderList:133; app routes: codegen:154, usage:417, cores/[slug]:228, projects/[id]/docs.tsx:373, projects/[id]/workboard.tsx:395, projects/[id]/backups.tsx:253, backups:539 (inspect), backups:651 (inspect — diff text scroller), projects/[id]/cores/dtc-analytics.tsx:201, projects/[id]/launcher.tsx:226, projects/[id]/settings.tsx:297, integrations.tsx:598, admin.tsx:136, settings.tsx:305. File-level tappables counts measured at plan time: only MemoryPane is 0.
- [ ] T4 — CI GATE so the 46th scrollable cannot regress this: new scripts/ci/keyboard-taps-check.mjs (+ keyboard-taps-check.d.mts + keyboard-taps-check.test.ts, mirroring wall-clock-bound-check.* exactly), wired as CHECK 7 in scripts/ci/lint.sh before `exit "$fail"`. Matcher: TS AST (ts.createSourceFile, filename *.tsx ⇒ JSX), walk for JsxOpeningElement/JsxSelfClosingElement whose tag is ScrollView|FlashList|FlatList (bare or `X.ScrollView` property tails); offense unless (a) attributes include a JsxAttribute named `keyboardShouldPersistTaps`, or (b) the source slice of the opening tag (getStart()..end) contains `KEYBOARD-TAPS-EXEMPT:` with ≥12 chars of justification (bare marker = its own offense, like wall-clock's 'bare'). AST means comments/strings/type-refs (`useRef<ScrollView|null>`, `ScrollViewProps`) can never be hits — lock each as a test. Walk app/ **/*.tsx excluding node_modules, __tests__, *.test.tsx, .expo. MANDATORY tripwires in main(), per the card ("an extraction that finds nothing must not read as a pass"): (1) runtime positive control — hard-coded offending sample must yield exactly its known offenses or exit 1 "positive control failed — matcher broken"; (2) negative control sample (prop set; justified marker) must yield 0; (3) zero-files-walked ⇒ exit 1; (4) zero-sites-matched ⇒ exit 1. Companion test additionally pins a real-tree floor (≥ 20 sites found under app/). Acceptance: `bash scripts/ci/lint.sh` fully green (proves T1–T3 covered every site).
- [ ] T5 — stage the as-built entry at `.trident/as-built/<current-branch>.md` (format: `## 2026-08-31 — <unique heading>` + body, cf. the staged example in that dir) and write the PR verification statement per the card's bar: VERIFIED = prop present at source on every site (gate-enforced), FlashList pass-through cited from vendored library source, chat FlashList ask asserted in harness; NOT VERIFIED = actual first-tap delivery with a real keyboard — unit tests do not exercise touch delivery; needs a device/simulator check, stated plainly, not claimed from a green suite.
