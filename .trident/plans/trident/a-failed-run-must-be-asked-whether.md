# IMPLEMENTATION_PLAN.md — a failed run must be asked whether it built something, before it is recorded as failed

Regenerated 2026-08-18 (RESUME) by the planner from the card + the committed state of branch `trident/a-failed-run-must-be-asked-whether` @ 966fca0f, diffed against true main (origin/main @ 5968cfed). Scope is the OUTER loop only (`trident/orchestrator.ts`, `trident/store.ts`, `gateway/composition/build-core-modules.ts`). Explicitly OUT of scope, per the card: any push from `codex-build.sh` or the inner loop, the merge gate, the review panel, the liveness veto.

- [x] Prerequisite (verified on main, not built): `publishBuiltCommit(run, claimedHead)` takes `claimedHead: string | null` and resolves the head to publish from git itself; the salvage reuses it verbatim — no new credential, no inner-loop push.
- [x] 1. IN-STEP GIT-TRUTH SALVAGE ON THE TERMINAL-FAILURE PATH (commit e71b1d9d). `buildTridentOrchestrator` wraps `step()`: every outcome about to be committed `phase:'failed'` on a `pr`-mode run first asks git whether `refs/heads/<run.branch ?? trident/<slug>>` exists and is ahead of base; if yes and not already fully on origin with a recorded PR, calls the existing `publishBuiltCommit(run, null)`. Phase STAYS `failed`; the original `failure_reason` is preserved with the appended `TRIDENT_SALVAGE_MARKER` ("build survived the failure", delivery-classifier-safe) plus branch + PR number. Branch absent / not ahead / already published → silent no-op. A failed rescue leaves the row byte-identical (diagnostic goes only into the step note via a WeakMap).
- [x] 2. REAL-GIT FALSIFICATION TESTS 1–5 (commit e71b1d9d): `trident/stranded-salvage-realgit.test.ts` — (1) a committed branch survives a fire failure: pushed to a file:// origin, `gh pr create` invoked, row still `failed` with reason preserved and marker appended after it; (2) a missing branch leaves the failure untouched, zero push/create; (3) a zero-commits-ahead branch is silently ignored; (4) reason-survival + not-an-unqualified-success asserted in (1) and in the sweep unit tests; (5) a failed rescue leaves the row byte-identical and origin untouched.
- [x] 3. STARTUP SWEEP (commits 1eb8f52d + 5cbd6847): `TridentRunStore.listFailedPrRuns(limit=50)` (phase='failed' AND merge_mode='pr', newest-advanced first) + store tests; exported `sweepStrandedFailures({ store, reconcile })` that never rejects per-row and persists only `{ pr, failure_reason }`; `buildTridentOrchestrator` returns third member `reconcile_stranded`; fire-and-forget call after `loop.start()` in the wired trident module init in `gateway/composition/build-core-modules.ts`, promise exposed as `stranded_sweep` on the module instance. This sweeps the eleven already-stranded branches at next boot.
- [x] 4. FALSIFICATION CRITERION 6 — NON-TEST PRODUCTION CALLER (commit 1eb8f52d): `gateway/composition/build-core-modules-trident-stranded-sweep.test.ts` drives the REAL `buildCoreModules(...).tridentModule.init` against a real git fixture with a seeded failed run and awaits `instance.stranded_sweep`; deleting the production call site removes `stranded_sweep`/the publish and turns the test RED.
- [x] 5. `docs/AS_BUILT.md` entry appended; per-card plan committed at `.trident/plans/trident/a-failed-run-must-be-asked-whether.md`; full-suite verification recorded (196ce6c0) under the pre-#402 harness; two subsequent clean merges of origin/main with one typecheck fix (exactOptionalPropertyTypes conditional spread, 5cbd6847).
- [x] 6. CONVERGE ON TRUE MAIN AND RE-VERIFY GREEN UNDER THE NEW HARNESS. Merge origin/main @ 5968cfed (#397 work-board dependency metadata + migration 0132, #402 eight-shard cost-split CI harness, #395 never-migrate-the-live replay) into the branch — dry-run `git merge-tree --write-tree` is verified CLEAN (result tree d56c0e40, zero conflicts; the only overlap file across sides is `docs/AS_BUILT.md` and git auto-merges it). During the merge commit, restore newest-first order in `docs/AS_BUILT.md` by moving this card's entry block back to the top (branch precedent from its previous main-merge). Re-run typecheck-all, lint, leak gate, the four card test files, and the FULL suite via the NEW `scripts/run-tests.sh`; regenerate the per-card plan file with this task ticked; commit. NO push — publishing and the PR remain the outer loop's job.
- Commit .trident/plans/trident/a-failed-run-must-be-asked-whether.md together with your code + tests.
- Report `deviatedFromSpec: true` in your structured result ONLY if you materially deviated from the EXECUTION SPEC above (different target files, a different design, or the task as built is not the task as specced) — a true here forces the next iteration to re-derive the whole plan, so do not set it for cosmetic drift. Otherwise report false or omit it.
