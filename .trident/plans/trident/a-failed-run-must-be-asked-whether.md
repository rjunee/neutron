# IMPLEMENTATION_PLAN.md — a failed run must be asked whether it built something, before it is recorded as failed

Regenerated 2026-08-17 by the planner from the card + a survey of `trident/` on main. Scope is the OUTER loop only (`trident/orchestrator.ts`, `trident/store.ts`, `gateway/composition/build-core-modules.ts`). Explicitly OUT of scope, per the card: any push from `codex-build.sh` or the inner loop, the merge gate, the review panel, the liveness veto.

- [x] Prerequisite (already true on main — verify, do not build): `publishBuiltCommit(run, claimedHead)` takes `claimedHead: string | null` and resolves the head to publish from git itself (`trident/orchestrator.ts:1679`; call-site comment at 2324: "A build that reported no OID is still published"). The reconciliation reuses it verbatim; no new credential, no inner-loop push.
- [x] 1. IN-STEP GIT-TRUTH SALVAGE ON THE TERMINAL-FAILURE PATH. Inside `buildTridentOrchestrator`, wrap `step()` so every outcome about to be committed as `phase:'failed'` on a `pr`-mode run first asks git: does `refs/heads/<run.branch ?? trident/<slug>>` exist locally, and is it ahead of base (`rev-list --count <base>..<head>` > 0)? If yes and not already fully on origin with a recorded PR, call the existing `publishBuiltCommit(run, null)`; on success record failed-but-published honestly (phase STAYS 'failed', `pr` set, original `failure_reason` preserved with an appended salvage note — never overwritten, never dressed up as success). Branch absent / not ahead / already published → silent no-op. A failed rescue (push/PR error) leaves the row byte-identical to today. Covers every step-produced failure mode the card measured (fire-failed, 90-min hang, orphan reap, stalled, garbled, launch-throw cap, crash-recovery budget). Real-git tests for falsification criteria 1–5.
- [ ] 2. STARTUP SWEEP + PRODUCTION REGISTRATION (falsification criterion 6) + AS_BUILT entry. Add `TridentRunStore.listFailedPrRuns(limit=50)` (phase='failed' AND merge_mode='pr', newest first); return a third member `reconcile_stranded(run): Promise<TridentRun | null>` from `buildTridentOrchestrator` (the same salvage function; null = untouched); add exported `sweepStrandedFailures({ store, reconcile })` that persists a salvage via `store.update(id, { pr, failure_reason })` (update() has no terminal-phase predicate, so it lands on failed rows) and never throws per-row; call it FIRE-AND-FORGET from the trident module init in `gateway/composition/build-core-modules.ts` (wired path only, after `loop.start()`), exposing the promise as `stranded_sweep` on the module instance so the wiring test can await it. Wiring test `gateway/composition/build-core-modules-trident-stranded-sweep.test.ts` drives the REAL `buildCoreModules(...).tridentModule.init` against a real git fixture with a seeded failed run — deleting the production call site removes `stranded_sweep`/the publish and turns the test RED. This is what sweeps up the eleven already-stranded branches at next boot. Append the `docs/AS_BUILT.md` entry.
