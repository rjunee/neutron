# IMPLEMENTATION_PLAN — a codex-routed build cannot transmit a mutation nomination, so the merge gate refuses its PRs

Card: `mutationClaim` is structurally unreachable on the codex build route — the schema field is filled by the codex BRIDGE, which never sees the build's reasoning; the wrapper trailer is six lines of repository facts with no nomination line; `forgeBuildContract`/`codexBuildPrompt` never ask for one — so every codex-routed APPROVE reaches the post-APPROVE mutation gate with `claim: null` and is refused ("mutation proof required but the build nominated no mutation to run"). Fix: give the nomination a channel from the process that did the work to the value the gate reads; keep it untrusted; keep absence meaning null.

Resume state (branch `trident/a-codex-routed-build-cannot-transmi`): ONE squashed commit past its base plus THIS round's fix commit. Do NOT cite commit shas here — the branch is replayed onto a moving base every round and any sha written down is stale by the time it is read; `git log --oneline main..HEAD` is the only honest answer. Keep committing on this branch. A worktree WITHOUT its own `node_modules` fails trident/orchestrator.test.ts en masse with "table code_trident_runs has no column named claimed_paths" (workspace imports resolving into another checkout); `bun install --frozen-lockfile` in the worktree fixes it — environment artifact, not a regression. The ROOT `IMPLEMENTATION_PLAN.md` must be persisted with THIS body; mirror it to `.trident/plans/trident/a-codex-routed-build-cannot-transmi.md`.

## DESIGN DECISION (locked in the committed card plan — do not re-litigate) — Option A, refined: the nomination is a COMMITTED artifact, read back out of git at the gate call site

The build writes `.trident/mutation-claims/<branch>.json` — PER-BRANCH, mirroring `.trident/plans/<branch>.md` — and COMMITS it with its work; the orchestrator falls back to `readCommittedMutationClaim(opts.run_host, run.repo_path, { expected_head, branch, base_branch })` ONLY when `result.mutation_claim` is null. Committed-and-read-from-git rather than a loose worktree file or Option B's seventh trailer line because: (1) in pr mode the workflow process ENDS at every publish handoff and re-enters with `mutationClaim = null` — the schema field only survives to the gate in local mode, so ANY in-process carry dies at that boundary; a committed blob is the one channel that already crosses it (prior art: `.trident/plans/`, `.trident/as-built/`; not gitignored). (2) The build worktree belongs to a dead process at harvest time; the reviewed OID is durable and binds the nomination to the very commit the gate pins — tip movement already collapses into the gate's existing expected-vs-pinned refusal. (3) No wrapper change, no bridge change; the trailer stays wrapper-measured facts, which a model claim is not. Untrust preserved: the artifact is branch-controlled input, shape-decoded by the SAME `parseMutationClaim` the agent route uses, then validated and actually RUN by the unchanged gate; every absence/failure decodes to null (today's refusal); a schema-supplied claim always wins and the reader is not even invoked then (`??` short-circuit). `trident/mutation-prover.ts` IS modified, minimally and only where the channel cannot be made honest anywhere else — the self-nomination rejection and the prose dispensation (Task 6(b)/(c)), plus the one-line base-name rejection at the `git diff` boundary that review round 7 required (Task 10(c)); the base PINNING rounds 5 and 6 added there is REVERTED (Task 10(a)) and everything else is imported from its existing exports. The card's "do not conflict with PR #490" note still governs the shape of those edits: they are additions in `validateClaim`, `isProseOnlyChange` and `changedFilesOnBranch`, away from the prover, the token and the evidence schema.

## Tasks

- [x] Survey: the card's four measurements reproduced at the base; the pr-mode publish-handoff drop confirmed (why the artifact must be committed and read at the gate, not relayed in-process).
- [x] 1. Artifact reader module + unit suite: `trident/mutation-claim-artifact.ts` — `mutationClaimArtifactPath(branch)` = `.trident/mutation-claims/<branch>.json` (superseded by Task 4(a) from the round-1 fixed path), `MUTATION_CLAIM_ARTIFACT_MAX_BYTES = 32 * 1024`, `readCommittedMutationClaim(run_host, repo_path, { expected_head, branch, base_branch })`: 40-hex OID else `refs/heads/<branch>` then `refs/remotes/origin/<branch>`, each past `isPlainBranchName`, else NO git command at all; diff-membership, then `git cat-file -s` size preflight, then `git show`; decode is `parseMutationClaim` and nothing more (agent-route parity — an absolute-path `file` survives the read for the gate to refuse); never throws, every failure is null. `trident/mutation-claim-artifact.test.ts` pins the exact argv, empty-argv-log refusals for unsafe refs, throw-swallowing, the oversize cutoff, decode parity, with paired positive/negative controls.
- [x] 2. Gate call-site fallback + production-seam tests: trident/orchestrator.ts hoists `const expectedHead = reviewedHeadOid(run)` and reads the committed artifact ONLY when `result.mutation_claim` is null, at the single `proveMutation` call; `SimResult` gained opt-in `mutationClaim`. Three orchestrator.test.ts seam tests prove at the production call site: artifact reaches the spy gate (deep-equal, exact `show <reviewed-oid>:<per-branch path>` argv pinned) and the run merges; a missing artifact still means null and a required proof still refuses; a schema-supplied claim wins with NO host command naming the artifact path.
- [x] 3. Ask in the contract + real-gate parity + terminal (THIS ITERATION — see execution spec). (a) Insert a MUTATION NOMINATION block into `forgeBuildContract` (trident/inner-workflow.mjs ~:1305), between the test-strategy interpolation line and the `CONTRACT` heading — anchor verified unique; no step renumbering; the codex route embeds this brief verbatim (`forgeAgent` → `codexBuildPrompt`), so no wrapper/bridge edit. Block states: write ONE JSON object (`file`, `find`, `replace`, `guard`, `control`, optional `rationale`) to the per-branch artifact path and COMMIT it with the work; `file` repo-relative (no leading `/`, no `..`), in THIS branch's diff against the base, PRODUCTION code (never a test file, never documentation); `find` occurs EXACTLY ONCE; `replace` differs; `guard`/`control` two DIFFERENT argv ARRAYS (never shell strings) on the allowlisted runner shapes; guard RED under the mutation, control GREEN; nominate nothing ONLY when the whole diff is documentation — then do NOT write the file at all; a fix round that moves/deletes the line updates the file; schema routes also set `mutationClaim` to the same object (or null). Block must NOT contain the phrase "TEST EXECUTION" (assembly test asserts its absence on the legacy path). (b) Prompt-seam tests: extend `Captured` in trident/inner-workflow-mutation-claim.test.ts with the prompt argument; assert `forge:build` AND a `forge:fix-round-*` brief each contain `mutationClaimArtifactPath(branch)` (production helper — drift-proof), 'EXACTLY ONCE', 'do NOT write the file at all', above `\nCONTRACT\n`; positive controls: forge.length > 1, non-empty briefs. (c) Real-gate parity test in trident/mutation-claim-artifact.test.ts: an absolute-path `file` claim SURVIVES the read non-null, then the REAL `runMutationProofGate` (scripted host per the gateDeps pattern in trident/mutation-prover.test.ts — read, never modify) refuses it with 'repo-relative' and ZERO guard runs, even against a colluding diff listing the absolute path. (d) Terminal: suites green (inner-workflow-mutation-claim, inner-workflow-assembly, inner-workflow, mutation-claim-artifact, orchestrator; mutation-prover as untouched control); persist this plan (root + card mirror) with Task 3 checked; commit this branch's OWN nomination artifact (file `trident/mutation-claim-artifact.ts`, find `32 * 1024` — occurs exactly once, verified; replace `0`; guard `["bun","test","trident/mutation-claim-artifact.test.ts"]`; control `["bun","test","trident/mutation-prover.test.ts"]`) and set the SAME object as `mutationClaim` in the schema report; commit message + PR body state the Option A choice, the committed-artifact refinement, and the pr-mode handoff finding.

Constraints honored throughout: nothing under `.github/workflows/`; `TEST_COMMAND_SHAPES`, the general-shell ban, the documentation rejection, the diff-membership requirement, the proof token/HMAC/evidence schema/`prover_version` all untouched; trident/mutation-prover.ts modified ONLY by the additions rounds 4 and 7 proved necessary (see Task 6(b) and Task 10(c)) — the card asked for no change there and round 3 had none, but the self-nomination bypass is not closable anywhere else; no absolute host filesystem paths and no per-customer-instance word in any committed text. Expected per the card: the DEPLOYED gate governing the run that builds this branch predates the fix, so this branch's own merge may still be refused with a null claim on the codex route — deployment lag, not a diagnosis to re-open.

- [x] 4. Review round 2 — close the inheritance, conflict, cap and coverage findings.
  - (a) **The path is PER-BRANCH** (`.trident/mutation-claims/<branch>.json`, derived by `mutationClaimArtifactPath`), **and the blob must appear in `git diff --name-only <base>...<reviewed-oid>`.** A single fixed tracked path is inherited: once one branch merges it, every branch cut afterwards is born holding a nomination it never wrote, and "this build nominated nothing" silently became "reuse the last PR's claim". The per-branch path also removes the add/add conflict every concurrent lane would have hit on one shared file; the diff-membership check still covers the case a branch NAME is reused after its predecessor merged. `trident/inner-workflow.mjs` derives the identical path from `forgeBranch`, and the tests assert the brief against the production helper's output rather than a literal, so the two halves cannot drift.
  - (b) **The byte cap now bounds the READ**, via `git cat-file -s <rev>:<path>` BEFORE the body is fetched. Measuring `Buffer.byteLength(stdout)` bounded nothing — the host buffers the whole child output first and trims it, so a whitespace-padded 128 KiB blob trimmed down to a few hundred bytes and was accepted. The boundary is pinned inclusively (at-cap resolves, one byte over is null).
  - (c) **The refusal says WHICH absence it was.** The reader returns `{ claim, note }` and the orchestrator appends the note to the gate's reason when — and only when — the artifact channel was consulted and came back empty. The undifferentiated "nominated no mutation to run" is exactly what this card records as misdiagnosed for days as an agent omission.
  - (d) **The codex route is covered at the prompt seam.** `trident/inner-workflow-mutation-claim.test.ts` now runs the workflow with the build phase pinned to a cli-transport tier, decodes the base64 transport blocks, and asserts the nomination block survives INTO the wrapper brief — plus that the bridge is told `mutationClaim` is always null on that route and must never fabricate one (a fabricated object would short-circuit the fallback and shadow the real committed nomination). The contract's load-bearing clauses (`COMMIT it with your work`, `EXACTLY ONCE`, the prose opt-out) are each asserted, so deleting one reddens.
  - (e) **The prose opt-out LEADS the block**, so a documentation-only branch that reads the first imperative and stops does not write a `.json` that would destroy its own prose-only exemption.
  - (f) The reader also falls back to `refs/remotes/origin/<branch>` after `refs/heads/<branch>`, matching `resolveMergeHeadSha` — the worktree holding the local branch is routinely cleaned before the gate runs.


- [x] 5. Review round 3 — restore the round-2 content the replay dropped, and finish the prose story.
  - (a) **The published head shipped round-1 code under round-2's commit message.** The replay that moved this branch onto the new base kept the message and dropped the content: the tree still carried the fixed path `.trident/mutation-claim.json`, the unbounded cap, the `refs/heads`-only resolution and the un-instructed bridge. All of Task 4 is re-applied here as a real tree change (a 3-way replay of the round-2 commit onto the current head), and the round-1 root artifact is gone: the only nomination on the branch is `.trident/mutation-claims/trident/a-codex-routed-build-cannot-transmi.json`.
  - (b) **The opt-out says INERT documentation, and names what is not.** `isProseOnlyChange` refuses the exemption for harness-driving markdown (`SPEC.md`, `IMPLEMENTATION_PLAN.md`, `CLAUDE.md`, `AGENTS.md`, `SKILL.md` by basename; anything under `skills/`, `prompts/`, `.claude/`, `agent-dispatch/`, `.github/`), so a branch whose whole diff is one of those is proof-REQUIRED. The old wording told such a build both that it was documentation (opt out) and that documentation is never a legal target — a contradiction with no exit. The block now says those paths get no exemption AND are themselves legal targets, and the prompt-seam test asserts each name against the production `isProseOnlyChange` (false for each, with an inert `.md` as the true control) rather than a literal list, so the brief cannot drift from the classifier. The ordering assertion also gained the presence control it was missing — `indexOf` returns -1 for an absent needle and -1 is less than every index, so the round-2 assertion would have passed on a deleted phrase.


- [x] 6. Review round 4 — the typecheck gate, the self-nomination bypass, and the mutable-ref read.
  - (a) **The branch failed the TypeScript gate, which failed the whole `test` check.** `mutationClaimArtifactPath` returns `string | null` and the round-3 prompt-seam test fed that straight into `toContain`/`indexOf` (TS2769/TS2345 at four sites). CI's `test` job is a pure aggregator over `typecheck`/`lint`/`purity`/`layering`/`shard`, so one red tsc reddened both blocker checks. Fixed by a narrowing helper in the test — it THROWS on null rather than defaulting to `''`, because an empty-string default makes every `toContain` below it pass vacuously, which is the silent pass that suite exists to prevent.
  - (b) **A nomination can no longer nominate ITSELF.** `validateClaim` rejected test files and prose but never the nomination path, and the gate's diff-binding check (`files.includes(claim.file)`) is satisfied by that file on every nominating branch BY CONSTRUCTION — so one boilerplate self-nomination plus a test that reads the JSON proved red-then-green while the real production change shipped unguarded. `MUTATION_CLAIM_ARTIFACT_DIR` therefore moves INTO `trident/mutation-prover.ts` (re-exported by the reader, so the writer's path cannot drift from the rules policing it) and `validateClaim` refuses any path under it, compared segment-by-segment with `.` segments dropped so `./.trident/…` is not a second spelling. Proved against a COLLUDING diff that lists every spelling, with zero guard runs and a production-file positive control that does run one. This is the only edit to `mutation-prover.ts` the card's "do not conflict with PR #490" note did not anticipate; it is three additions in two functions plus one exported constant, deliberately kept away from the prover, the token and the evidence schema.
  - (c) **The same knowledge fixes the prose exemption.** `.json` is not a prose suffix, so a documentation-only branch that also wrote its nomination lost the exemption, owed a proof, and had no legal target — permanently unmergeable, mitigated only by prompt wording. `isProseOnlyChange` now treats a nomination path as inert (it is the gate's own bookkeeping, not code the harness runs); code in the same diff still requires the proof.
  - (d) **A mutable fallback ref is PINNED to an OID before the read's three legs run.** `readAtRevision` resolved its operand independently for the diff, for `cat-file -s` and for `git show`; a ref that moved between the size and the show meant the cap sized one object and the reader read another. `pinRevision` resolves `refs/heads/<b>` / `refs/remotes/origin/<b>` once via `rev-parse --verify --quiet <ref>^{commit}` and passes the OID; a pin that answers with anything that is not 40 hex is a refusal, not a substitution. The already-pinned `expected_head` path issues no extra command.
  - (e) **The reader's note is appended ONLY to the refusal it explains.** The gate also refuses a rejected branch name, an unresolvable head and a moved tip; suffixing those with "no committed nomination" pointed at the wrong failure. The refusal string is now the exported `NO_NOMINATION_REFUSAL`, matched exactly. Also fixed: the duplicated word in two reader notes ("no committed nomination committed at").
  - (f) **The codex prompt-seam assertions now cover fix rounds, not just `forge:build`.** Fix rounds go through the same bridge seam, and a round that dropped the ask would leave the gate reading whatever the first round committed.
  - Knowingly NOT done, both nits: the branch diff is read twice on a null-claim path (once by the reader, once inside `proveMutation`) — threading the file list through would widen the `mutation-prover.ts` surface this card is told to leave alone, for one extra `git diff`; and `.trident/mutation-claims/` accumulates one tracked file per merged branch with no reaping, exactly as `.trident/plans/` and `.trident/as-built/` already do — reaping is its own card across all three.


- [x] 7. Review round 5 — the base of the diff is pinned (**(a) REVERTED in round 8 — see Task 10(a)**), and the brief stops promising a target the gate refuses.
  - (a) **BLOCKER: a stale local base defeated both the inheritance protection and the gate's diff-binding.** Every branch-side ref in this gate is resolved to an OID before it decides anything (`resolveMergeHeadSha`, the reviewed `expected_head`, the reader's `pinRevision`) — the BASE arrived as a bare NAME, and `git diff <name>...<ref>` resolves it against the LOCAL ref. Reproduced in this repo: local `main` trailed `origin/main` by 19 merges, so `git diff main...<this branch>` listed **170 files where the branch changed 13**, including `trident/mutation-prover.ts` and merged `.trident/` artifacts the branch never touched. A trailing base drags the three-dot merge-base backwards, and both rules riding on that diff then read too permissively: `claim.file` counts as "in the branch diff" when the branch never touched it, and a committed nomination counts as "this build's" when it arrived from a merged predecessor of the same name — invariant (a) defeated by the one unpinned operand. `resolveBaseRevisions` now pins the base, preferring THE TIP THAT CONTAINS THE OTHER (a stale ref is an ancestor of the fresh one), and on divergence returning BOTH tips for the caller to intersect (round 6, Task 8(a) — the single-tip answer was merge-mode-blind); nothing is fetched (the shared repo's symbolic-ref and configured-refspec traps are the reason `resolveMergeHeadSha` documents its own fetch so carefully), and an unresolvable base falls back to the bare name so a local-only base still works. It lives in `changedFilesOnBranch`, which is the single seam both consumers go through — the reader's diff-membership check and the gate's `files.includes(claim.file)` binding — so neither can drift from the other. Tests: the containment rule both ways plus divergence; the pins' exact fully-qualified argv; an unsafe base reaching git NOT AT ALL; and, end to end at the reader, a stale base whose over-wide diff carries a predecessor's nomination reading NULL, proved by the argv (origin's tip diffed, the stale name absent, no body fetched) with an in-diff positive control.
  - (b) **The brief no longer promises a legal target where the gate has none.** Round 3's wording said harness-driving markdown is itself a legal nomination target; `TEST_FILE` refuses any path with a `test/`, `tests/` or `__tests__/` segment, so at the intersection (`skills/tests/SKILL.md`) a branch would be proof-required with nothing nominable and no exemption — unmergeable. No such path exists in the tree today, so this is latent rather than a regression, and the honest fix is the brief: it now names the exception and tells the build to nominate a non-test file the change also needs, or to say so, rather than shipping a diff that cannot merge.
  - (c) **`..` is refused, not dropped.** `isMutationClaimArtifact` normalised `.` segments so `./x` and `x` are one path; dropping `..` as well would be the same spelling trick from the other side (`.trident/mutation-claims/../../src/a.json` reading as the gate's own bookkeeping and going inert). Unreachable on both call paths today — `validateClaim` rejects `..` first and git emits normal paths — so this closes the spelling, not an exploit.
  - (d) **The nomination's prose dispensation runs LAST**, after the denylist and executable-prose refusals rather than before them, so a segment added to either list later polices this directory too instead of being skipped by an escape hatch that ran first. Accepted cost, asserted as behaviour: a branch NAME carrying `skills`, `prompts`, `.claude`, `agent-dispatch` or `.github` as a path segment forfeits the dispensation; trident slugs are `[a-z0-9-]`.
  - (e) The orchestrator seam test now imports `NO_NOMINATION_REFUSAL` instead of copying its sentence — the orchestrator matches that refusal EXACTLY before appending the reader's note, so a literal could drift silently.
  - Knowingly NOT done, with reasons. The dotted-branch path duality (`trident/foo` is a file where `trident/foo.json/bar` needs a directory) is fail-closed — the second branch simply cannot commit its artifact — and the obvious guard (refusing any `.json` path segment in a branch name) would reject ordinary names like `fix/package.json-bump` to close a case trident's `[a-z0-9-]` slugs cannot reach. A fix round that updates the committed artifact but reports NO schema claim is still gated on the earlier in-result claim: the "last round that nominates wins, a silent round leaves the previous nomination standing" precedence is stated and tested, and the case needs a round-1 agent claim followed by an artifact-only fix round — a mixed route that does not occur, since the route is per run. The double read of the branch diff on a null-claim APPROVE and the unreaped `.trident/mutation-claims/` stand as recorded in Task 6.

- [x] 8. Review round 6 — the two blockers: the base pin was merge-mode-blind (**(a) REVERTED in round 8 — see Task 10(a)**), and the reader could answer with a stale origin commit's nomination.
  - (a) **BLOCKER: picking ONE base tip on diverged refs certified files outside the branch delta.** Round 5 pinned the base to origin whenever local and origin had diverged. That is blind to the merge MODE: pr mode lands the branch on `origin/<base>`, local mode merges into the LOCAL `<base>` (`merge.ts` checks out `opts.base_branch ?? detectBaseBranch` in the local checkout). With the tips diverged, whichever one is picked the OTHER mode reads a three-dot diff whose merge-base sits back at the fork — so a file changed only on the picked tip's missing side counts as "in the branch diff", and `validateClaim`'s diff-membership binding accepts a nomination pointing at code the branch never touched. `resolveBaseRevision` becomes `resolveBaseRevisions` and returns BOTH tips when neither contains the other; `changedFilesOnBranch` INTERSECTS the diffs. A file the branch changed is in both; a file that arrived from either base's own commits is in at most one. The intersection is the branch's real delta under either merge target and is narrower than both, so this can only refuse where a single tip would have accepted — never the reverse. Containment still collapses to one tip and one diff (the common case: unchanged cost), and an unanswerable containment question now falls through to the diverged case rather than to origin, because "git could not tell me" is not a containment proof. Tests: the diverged case returns both tips with a one-tip control; the intersection at `changedFilesOnBranch` with both diffs' argv pinned and a containment control that keeps its full listing; a failed diff leg on either base still null; and a REAL-GIT case in `trident/mutation-prover-realgit.test.ts` building the exact divergence (branch cut from a local-only commit, origin advanced from the fork point) — real git lists `local-base-only.ts` from origin's tip and the gate's reader lists neither base-only file, with a second branch-changed file as the positive control.
  - (b) **BLOCKER: the reader fell through to `origin/<branch>` after a local ref that RESOLVED but carried no artifact.** The two refs are different commits whenever the local branch was reset, amended or rebuilt, so a local tip that nominated nothing was answered by a stale origin commit's nomination — a claim about work that is not the work the gate proves, which is exactly the binding this reader exists for. The fallback is now over REF EXISTENCE ONLY (its stated reason: the worktree holding the local ref is routinely cleaned before the gate runs): the first revision that names a commit is the one read, claim or no claim. Test asserts the null AND, by argv, that origin's tip was never read from, with two controls — the artifact on the local tip reads back, and an absent local REF still falls through to origin.
  - (c) Minor/nit sweep: `pinRevision` gained `--end-of-options`, matching its sibling `pinBaseRef` (two spellings of one hardening rule is how one of them rots); the reader's notes slice branch-supplied names to 80 characters like the gate's own branch rejection, and the orchestrator caps the appended note at 300 characters before it reaches the verbatim-stored `failure_reason`.
  - Knowingly NOT done. `mutation-prover.ts` still carries the base resolution and the two nomination-path rules: the resolution has to live where `changedFilesOnBranch` does (both consumers go through that one seam), and moving it to a third module would either duplicate `isPlainBranchName` or make the prover import from a module that imports the prover. Correctness over conflict surface — the edits stay three additions in `validateClaim`/`isProseOnlyChange`/`changedFilesOnBranch` plus the resolver, away from the prover, the token and the evidence schema. The reader/gate disagreement over a base that fails `isPlainBranchName` (the reader aborts, the gate falls back to the bare name) is unreachable: `detectBaseBranch` always yields a non-empty name defaulting to `main`. The double read of the branch diff and the unreaped `.trident/mutation-claims/` stand as recorded in Task 6. The PR body still describes the round-1 mechanism (`.trident/mutation-claim.json`, a `MUTATION_CLAIM_ARTIFACT_PATH` constant); the build lane may not run `gh`, so refreshing it before the squash lands is the publishing loop's to do — this plan, which reviewers read, is current.

- [x] 9. Review round 7 — the blocker: the codex route's null nomination was guarded by PROSE, not by code.
  - (a) **BLOCKER: a bridge response carrying a non-null `mutationClaim` was schema-valid and reached the gate.** `mutationClaim` rides into `CODEX_FORGE_SCHEMA` in the `...FORGE_SCHEMA.properties` spread as type `['object','null']`, so a fabricated object passed validation; `forgeAgent` returned the bridge's object untouched (it checked only `trailerComplete`/`codexStatus`/`branch`); `mutationClaim = forge.mutationClaim ?? null` and the fix-round twin carried it into the terminal result; and the gate prefers an in-result claim over the committed artifact — so an invented nomination would have SHADOWED the real committed one and the gate would have proved a mutation nobody measured. The only guard was the sentence in `codexBuildPrompt` telling the bridge not to invent it, and an instruction to an LLM is not a guard. `forgeAgent` now returns `{ ...res, mutationClaim: null }` on the cli route: null becomes a property of the ROUTE, on round 1 and every fix round alike, because both call sites come through that one function. NORMALISING rather than narrowing the schema to `{type:'null'}` is deliberate and fail-safe — a fabricating bridge loses the fabrication instead of failing schema validation and killing the run, and the committed artifact answers for this route either way. Tests (both proved to fail with the normalisation reverted): an adversarial bridge fabricating a structurally VALID claim on the build round yields a null terminal claim, with two controls — `parseMutationClaim` accepts the same object (so the null is the route discarding it, not the decoder refusing it) and the identical object on the CLAUDE route reaches the terminal result intact; and the same fabrication on a FIX round, where the `if (fix && fix.mutationClaim)` assignment is a second door onto the same value, with a positive control that a fix round really ran.
  - (b) Nit: the prose dispensation's comment claimed it covered "the build's OWN nomination" while `isMutationClaimArtifact` tests only the directory prefix and the `.json` suffix. The CODE is right and stays — threading a branch through `isProseOnlyChange` would change its signature and both call sites for no behaviour (the reader only reads the per-branch path; `validateClaim` refuses every spelling under that directory as a TARGET, so a stray sibling blob can buy an exemption but never a provable claim). The comment now says what the code does. Trailing whitespace on the Task-2 bullet stripped from both plan copies.
  - Knowingly NOT done, with reasons. `changedFilesOnBranch` still collapses "empty two-base intersection" and "diff unreadable" into one null, and the gate's refusal then names the unreadable case; distinguishing them means changing that function's return type and every caller inside `trident/mutation-prover.ts` — exactly the widening the card forbids while PR #490 rewrites those regions — and both arms are fail-closed, so the cost is a misdirecting message on a run that was going to refuse anyway. The reader's `refs/remotes/origin/<branch>` fallback still does not fetch, unlike `resolveMergeHeadSha`: adding a fetch to the reader would put the shared repo's symbolic-ref and refspec traps on a path whose failure direction is already a confusing REFUSAL rather than a false pass. Both are single-reviewer, unverified, explicitly "surface, do not block". The double read of the branch diff, the unreaped `.trident/mutation-claims/`, and the `mutation-prover.ts` overlap with PR #490 (a conflict to resolve at #490's rebase — the pin is load-bearing and fail-closed) all stand as recorded in Tasks 6 and 8.

- [x] 10. Review round 8 — the base resolution comes OUT, and the findings that rode on it go with it.
  - (a) **BLOCKER × 2, one root: `resolveBaseRevisions` and the two-base intersection are REMOVED, and `changedFilesOnBranch` is back to the single-base one-liner it was.** The intersection corrupted the gate in BOTH directions — the narrowed list fed `isProseOnlyChange`, where narrower is MORE permissive, so a branch that reverted code and edited docs could manufacture a prose-only exemption and skip the proof entirely; and an empty intersection returned null, refusing a branch whose diff reads perfectly well with the false reason "branch diff could not be read". The module doc's claim that the intersection "cannot admit a file that picking a single tip would have refused" was true of diff MEMBERSHIP and false of the prose EXEMPTION. The second blocker has the same root and no in-scope fix: the resolver deliberately does not FETCH, so it cannot tell a fresh pair of refs from a pair that are equal and both stale — and a stale base OID widens the diff and lets a nomination against a base-only file certify. Adding a fetch means putting the shared repo's symbolic-ref and configured-refspec traps on this path, which is its own card. Both reviewers who raised the scope finding reached the same disposition independently: **the self-nomination guard is defensible and stays; the base pinning is an independent fix for a PRE-EXISTING defect and belongs on its own card** — which is also what the card's own "do NOT modify `trident/mutation-prover.ts` beyond what this change strictly requires; PR #490 is actively changing that file" instruction has said since round 1. Removing it deletes the largest hunk in that file, both blockers, and most of the conflict surface with #490 in one move. The stale-base widening it was meant to close is a pre-existing repository condition that predates this branch and is unchanged by it.
  - (b) The tests that pinned the removed behaviour go with it: the `resolveBaseRevisions` describe block in `trident/mutation-prover.test.ts`, the real-git diverged-bases case, the reader's stale-base case, and the base-pin argv in the reader's scripted host. The gate's own diff-range assertion is back to `main...<pinned head>` — the BRANCH side is still pinned, which is the property that was never in question.
  - (c) **A base name that fails `isPlainBranchName` is now REFUSED inside `changedFilesOnBranch`, instead of being handed to git.** `git diff --name-only <base>...<ref>` takes its range as a bare operand and admits no end-of-options marker, so a base spelled `--output=<path>` is parsed as an OPTION: real git 2.43.0 exits 0 and CREATES the file. The name reaches this function from an operator flag or from `origin/HEAD` via `detectBaseBranch`, and `git check-ref-format` accepts a ref of that spelling — so the source cannot be assumed clean and the filter belongs at the consumer. Two lines; the refusal reads as "the diff could not be read", which makes the gate REQUIRE the proof and can never become a pass. Tested both ways: a mocked case proving the hostile names leave the argv log EMPTY (with a slash-bearing plain name as the positive control), and a REAL-GIT case that first demonstrates raw git writing the file and then shows the production reader writing nothing and returning null, with an ordinary base listing the branch's file as the control.
  - (d) **The contract now NAMES the allowlisted runner shapes and the blob cap.** A build nominating `["npx","vitest",...]` learned the rule only from a post-APPROVE refusal — the one-round-per-lesson loop this card exists to end. The block lists every shape `TEST_COMMAND_SHAPES` accepts, names `npx vitest` as refused, and states the 32 KiB ceiling that otherwise silently nulls an oversized nomination. The prompt-seam test extracts the allowlisted PROGRAMS from the production source and asserts each has a documented spelling in the brief, so a runner added to the gate reddens here rather than being discovered by a refused build.
  - (e) **`readCommittedMutationClaim` gains real-git coverage**: its `cat-file -s <oid>:<path>` and `show <oid>:<path>` legs were only ever exercised against a scripted host. Three cases against a real repository — a committed nomination round-tripping, the same repo WITHOUT the blob reading null, and a blob git itself sizes over the cap being refused before its body is shown.
  - (f) **Invariant (a) in the reader's module doc now states what it does NOT catch**: a nomination going stale WITHIN one branch. A fix round, or a later lane member on the same pinned branch, that changes code without rewriting the file is proved against the earlier round's nomination — the blob is in that branch's own diff by construction, so no diff-membership test can see it. Parity with the agent route, not a regression, and bounded by the rules that do hold every round: the target must be production code in the branch diff, and the gate RUNS the mutation, so a stale nomination fails to redden its guard and refuses.
  - Knowingly NOT done, with reasons. The stale-base widening (Task 7(a)'s original subject) is left to its own card, as every reviewer in round 7 recommended; it is pre-existing, it is not made worse here, and closing it honestly needs the fetch this file is right to avoid. The nomination on this branch is unchanged and NOT stale — it names `trident/inner-workflow.mjs`'s route-level normalisation, whose line this round does not touch and which remains the load-bearing behaviour of the card. The double read of the branch diff, the unreaped `.trident/mutation-claims/`, and the PR body predating the mechanism all stand as recorded in Tasks 6, 8 and the PR BODY section.

- [x] 11. Review round 9 — the inherited-nomination fail-open closes INSIDE the reader, and the allowlist comes off the shared gate leg.
  - (a) **BLOCKER: a stale local base let a reused branch name inherit its predecessor's nomination.** Diff membership is only as honest as the base it is measured from, and the reader was handing `changedFilesOnBranch` a local branch NAME with no freshness check. Reproduced against real git: local `main` reset behind `origin/main`, the branch name reused after its predecessor merged its claim, a new commit touching only unrelated production code — the correct diff is that one file, yet the reader returned the inherited round-1 nomination, so round-2 work shipped proved by round-1's mutation. Closed where it belongs, in `trident/mutation-claim-artifact.ts`: `resolveBaseCommits` pins BOTH spellings of the base (`refs/remotes/origin/<base>` and `refs/heads/<base>`) to OIDs, and the artifact must be in the diff against EVERY commit they name. A blob the branch really wrote is in both diffs; one inherited from either base is missing from that base's own. Neither ref resolving is a null, not a waiver. This is contained to the reader — `isProseOnlyChange` is nowhere near it, so round 8's reason for ripping the earlier intersection out (a narrowed list is MORE permissive at the exemption) cannot recur, and no fetch is added: the reader reads the remote-tracking ref this repository already has. Stated honestly, that means a pair of refs that are BOTH stale is still not detectable here, which is the pre-existing condition round 8 left to its own card; what closes is the asymmetric case, which is the one that actually fires, because a gate host fetches origin and rarely fast-forwards the local base. Tested both ways in `trident/mutation-claim-artifact.test.ts`: a scripted case where the blob is in the stale base's range and not the fresh one, and a REAL-GIT clone reproducing the whole scenario — each with a positive control where the branch really did write the file and it reads back.
  - (b) **MAJOR + the scope-creep MINOR, one edit: the ASCII allowlist on the base comes off `changedFilesOnBranch`, replaced by the one refusal the vector needs.** Round 8(c) filtered the base with `isPlainBranchName`, which has no `@` — so `release@v1`, a name `git check-ref-format --branch` accepts and `detectBaseBranch` forwards unmodified, made every non-exempt merge in such a repository refuse with "branch diff could not be read", permanently; the same line also refused the `HEAD~1` an operator flag passes on a PRE-EXISTING call path the card fenced off. The vector is option parsing, not name shape: argv is an array, there is no shell, so a leading `-` is the whole of it. `if (base.length === 0 || base.startsWith('-')) return null`, and everything else is git's to judge — a base git cannot resolve fails the diff, which still reads as "could not be read" and still makes the gate REQUIRE the proof. The real-git hostile-base case is unchanged and green; a new case pins the regression the other way, asserting `release@v1`, `HEAD~1` and friends reach git as the left side of the range, with git's own "no" as the control. The reader's own base is untouched by this: it is validated by `isPlainBranchName` there and reaches the diff as an OID, so no NAME crosses that range at all.
  - (c) **MINOR (accuracy): the `.mjs` comment stops overstating per-branch isolation.** Members of one wave share the pinned lane branch, hence one nomination file, so a later member's gate can be satisfied by an earlier member's nomination. The comment now says so and points at the same bound invariant (a) carries: the gate RUNS the mutation, so a nomination that no longer describes the code fails to redden its guard.
  - This branch's own nomination is RE-POINTED at round 9's load-bearing line (`const bases = await resolveBaseCommits(...)` → `const bases = [base]`), verified by hand: the guard suite goes 24 pass / 11 fail under the mutation and back to 35 pass / 0 fail restored, while the control (`trident/mutation-prover.test.ts`, which never reads a committed nomination) stays 75 pass / 0 fail.
  - Knowingly NOT done, with reasons. The fail-CLOSED sibling nit — a byte-identical nomination on a reused branch name producing no diff entry — is left alone: it refuses rather than admits, its probability is low (the free-text rationale differs every round), and the base fix above already removes the case where the two bases disagree. The double read of the branch diff (reader and gate each run `git diff --name-only`) is now three subprocesses rather than two on the codex path and stays accepted: it is paid once per APPROVE and buys a fail-open closure. The unreaped `.trident/mutation-claims/` blobs and within-branch staleness stand as recorded in Tasks 6, 8 and 10(f).

- [x] 12. Review round 10 — the pinned base reaches the GATE's own binding, and the two modules stop enforcing different base policies.
  - (a) **BLOCKER: a stale local base could validate a `claim.file` the branch never changed.** Round 9 pinned the base for ARTIFACT membership but the gate went on binding the nomination through the bare base NAME: `changedFilesOnBranch(run_host, repo_path, input.base_branch, pinnedSha)` runs `git diff --name-only <base>...<ref>`, and a local `main` sitting behind `origin/main` puts the predecessor's merged files inside that range. A nomination pointing at one of them passed `files.includes(claim.file)` and satisfied the gate's own stated invariant ("the proof must break a line THIS branch changed") while being entirely diff-INDEPENDENT — the one thing that binding exists to make impossible. Closed with `branchDiffAgainstEveryBase`, which resolves the base to every commit it names and returns BOTH readings of the branch diff: `any` (the union) and `every` (the intersection).
  - (b) **Why the intersection is safe THIS time — round 8's removal reason is answered, not ignored.** Round 8 ripped an earlier intersection out because it fed `isProseOnlyChange`, where a NARROWER list is MORE permissive: a branch that reverted code and edited docs could manufacture an exemption. The two consumers fail closed in OPPOSITE directions, so they now get opposite readings — the exemption takes the UNION (code in either range forfeits it) and the binding takes the INTERSECTION (a file must be in every range to carry a proof). One list cannot serve both, which is exactly what round 8 discovered. Round 8's second objection is answered too: an empty intersection is returned as an empty LIST, not as null, so the refusal reads "the nominated file X is not in this branch's diff against every commit main names" and never the false "the branch diff could not be read". Only a genuinely unreadable range nulls both readings, and null is still "require the proof". Tested scripted (union/intersection shape; a stale-only file refused with no worktree provisioned; the branch's own file still opening the gate as the positive control; code in either range forfeiting the exemption, with a prose-in-both control that still exempts; an unreadable range nulling both) and against REAL git end to end: a real clone with a real stale local `main`, the vector demonstrated with raw `git diff`, the gate refusing the inherited file and getting PAST the binding on the branch's own.
  - (c) **MAJOR: the reader's `isPlainBranchName(base)` refusal is gone, and there is now ONE base policy.** The two halves of one channel enforced contradictory rules: `changedFilesOnBranch` deliberately leaves a base to git ("a base git accepts is git's to judge" — its tests name `release@v1` and `HEAD~1`) while the reader nulled every base that failed an ASCII allowlist with no `@`. Fail-closed but permanent: an operator-supplied base of a legal name would read every nomination as absent and block every codex-routed non-prose PR forever. The reader now refuses only an EMPTY base and shares the gate's resolution — `resolveBaseCommits` moved into `trident/mutation-prover.ts` next to `changedFilesOnBranch` (the seam both consumers already go through; keeping a copy in the reader would have meant the prover importing from a module that imports the prover) and the reader's duplicate deleted. A hostile base stays inert by construction rather than by allowlist: every command it appears in prefixes it with `refs/…` and passes `--end-of-options`, so git can only read it as a ref path, and when neither spelling resolves the bare name is refused by `changedFilesOnBranch`'s leading-`-` guard before the diff. Both are tested, including an argv-level assertion that no `diff` command ever carries the hostile string.
  - (d) **DECLARED, per the review: `isProseOnlyChange` grants the whole-PR exemption to ANY `.trident/mutation-claims/**/*.json`, not only the branch's own.** That widening landed in round 5 and was not named in the PR body; it is stated here and in the PR BODY section below. It is deliberate — the function is not told which branch it is judging, the content is inert bookkeeping, the two denylist refusals still run first, and `validateClaim` refuses the same paths as mutation TARGETS — so a stray sibling blob can buy a branch an exemption but never a provable claim.
  - (e) Nit: the load-bearing comment that claimed `validateClaim` refuses "every spelling" under the nomination directory is corrected. `isMutationClaimArtifact` requires a literal lowercase `.json`, so `junk.JSON` and a suffix-less file are refused for the ORDINARY reasons (nothing executes them; they are not in the diff), not as self-nominations. The comment now says that the two rules are one predicate and therefore cannot drift.
  - (f) Minor: the within-branch staleness residual gains the half it was missing — the PRECEDENCE. The in-result claim wins and the artifact is not read at all when one arrived, so on the AGENT route a fix round that rewrites the blob but reports no claim of its own is still proved against the round the inner loop carries forward. Recorded in the reader's invariant (a) and named in the orchestrator precedence test, whose subject that composed case is. On the codex route — the one this module exists for — the in-result claim is structurally null every round, so there is nothing to shadow. The bound is unchanged and still real: the gate RUNS the mutation, so a nomination whose line moved fails to redden its guard.
  - This branch's own nomination is RE-POINTED at round 10's load-bearing line (`const bases = await resolveBaseCommits(run_host, repo_path, base_branch)` → `const bases: string[] = []`), verified by hand: the guard (`bun test trident/mutation-prover.test.ts`) goes 76 pass / 5 fail under the mutation and back to 81 pass / 0 fail restored, while the control (`bun test trident/inner-workflow-mutation-claim.test.ts`, which never reads a branch diff) stays 12 pass / 0 fail.
  - Knowingly NOT done, with reasons. The reader's OID leg still does not FETCH: it is fail-CLOSED, the reviewer who raised it could not construct a live repro, and adding a fetch puts the shared repo's symbolic-ref and refspec traps on a read path whose failure direction is already a refusal. `readCommittedMutationClaim` still runs before the prose-only exemption is decided (4-5 wasted subprocesses on a documentation-only APPROVE) — the orchestrator cannot know the diff is prose-only without reading it, and duplicating that read to save these would cost more than it saves. The `mutation-prover.ts` overlap with PR #490 grows by one function pair and one call-site line and stays a conflict to resolve at #490's rebase; the resolution has to live beside `changedFilesOnBranch` for the reason in (c). The unreaped `.trident/mutation-claims/` blobs stand as recorded in Task 6.

## PR BODY (authoritative — the published body predates the mechanism this branch ships)

The build lane may not run `gh`, so the published body on the PR still describes the ROUND-1 mechanism (`.trident/mutation-claim.json`, an imported `MUTATION_CLAIM_ARTIFACT_PATH` constant); neither exists in this tree — `git grep MUTATION_CLAIM_ARTIFACT_PATH` matches only prose. Refreshing it is the publishing loop's to do; this is the text it should carry, and it is what reviewers should read as the body:

> **Option A, refined: the nomination is a COMMITTED artifact, read back out of git at the gate call site.** The build writes `.trident/mutation-claims/<branch>.json` — per-branch, mirroring `.trident/plans/<branch>.md` — and commits it with its work; `trident/orchestrator.ts` calls `readCommittedMutationClaim` at the single `proveMutation` call site ONLY when `result.mutation_claim` is null. Chosen over a loose worktree file and over Option B's seventh trailer line because in pr mode the workflow process ENDS at every publish handoff and re-enters with `mutationClaim = null`, so any in-process carry dies at that boundary; a committed blob is the one channel that already crosses it, and the reviewed OID binds the nomination to the very commit the gate pins. The trailer stays what it is — facts the wrapper measured with git and gh — because a nomination is not that kind of value. The nomination remains UNTRUSTED: same `parseMutationClaim` as the agent route, then the unchanged gate validates it and actually runs it; `TEST_COMMAND_SHAPES`, the general-shell ban, the documentation rejection, the diff-membership requirement, the proof token/HMAC/evidence schema and `prover_version` are untouched. A missing artifact still decodes to null and a required proof still refuses. One exemption surface IS widened and is declared here: `isProseOnlyChange` treats ANY `.trident/mutation-claims/**/*.json` as inert, not only the branch's own — the function is not told which branch it is judging, the content is bookkeeping the harness never executes, the denylist refusals still run first, and `validateClaim` refuses those same paths as mutation targets, so a stray sibling blob can buy an exemption but never a provable claim. The gate's diff binding is also now measured against EVERY commit the base names: the prose exemption takes the union of those ranges and the nomination binding their intersection, so a stale local base can neither buy an exemption nor lend a branch a file it never changed. The agent route is unaffected — a schema-supplied claim wins and the reader is not invoked at all — and on the codex route `forgeAgent` forces `mutationClaim` to null in code, so a bridge cannot fabricate one that shadows the committed nomination.
