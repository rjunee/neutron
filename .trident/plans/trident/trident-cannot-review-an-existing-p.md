# IMPLEMENTATION_PLAN — Trident review-only dispatch mode (`bound_pr`): a "review PR #N" dispatch must review THAT PR, never build a docs PR about it

Regenerated 2026-08-18 against main @ 89de7523. Card: "TRIDENT CANNOT REVIEW AN EXISTING PR — bound_pr is 0 of 190 runs; blocking all enterprise merges." Survey verified against this tree before regenerating: `code_trident_runs.bound_pr` exists (migration 0124) and is plumbed through `trident/store.ts` (types, COLS, `create()`, row map) but is POPULATED BY NOTHING and READ BY NOTHING outside tests; the dispatch surface (`trident/board-dispatch.ts` `dispatchBoardBoundBuild`, `trident/work-board-build-tool.ts` `work_board_dispatch_build`) accepts only free text, so every dispatch takes the build path; `trident/orchestrator.ts:1933` throws away `created.stderr` on `gh pr create` failure (the push site directly above already uses `publishFailureReason(step, branch, stderr)` — the helper exists); the codex deferral reason lives only in `/tmp/trident-codex-build-*.err` (GC'd per round; `inner-workflow.mjs:1815` throws "see the codex-build wrapper stderr" without including it); `started_at`/`last_advanced_at` are ISO-8601 TEXT (`migrations/expected-schema.txt:585-586`) so any numeric window filter silently matches every row; the 90-min `NO_ADVANCE_HANG_MS` reaper (`orchestrator.ts` no-advance reap) still fires on the stale-by-construction clock with no positive-liveness requirement. No `review-evidence` machinery exists anywhere in the repo.

CROSS-LANE HAZARD (do not resolve silently): `.trident/plans/trident/a-fix-round-that-abandons-the-revie.md` (a different lane, its tasks 2/4 unbuilt) plans `bound_pr` as a fix-round PUBLISH-TARGET pin — a run that commits and pushes to exactly PR #N. THIS card locks the opposite semantic: `bound_pr` set ⇒ review-only, the run must never create a branch, commit, or PR, enforced fail-closed at launch. This card's semantic ships now (owner: launch after deploy; Fix 1 unblocks enterprise); if the fix-round lane later needs commit-capable bound runs it must add its own discriminator and update the launch guard deliberately — record the collision in the code comment at the guard.

Card do-nots, binding on every task: do NOT weaken the brief-part receipt check; do NOT satisfy the publisher by committing an empty file from a review lane; do NOT close the card on Fix 3/4 alone; `CODEX_BUILD_BRANCH_RECLAIMED` is a different, addressed defect — not evidence here. Each merged task appends its own `docs/AS_BUILT.md` entry per repo convention.

Already satisfied in this tree (verified, do not rebuild):

- [x] `bound_pr INTEGER` column + full store plumbing — migration `0124`, `trident/store.ts` (`TridentRun`, `CreateTridentRunInput.bound_pr`, COLS, `create()` write at :394, row map at :1020), schema regenerated. Landed via the fix-round-contract lane's task 1.
- [x] Push-failure stderr carried into the stored reason with the credential-disclosure guard (`publishFailureReason('push', branch, pushed.stderr)` + `classifyPublishFailure`, PR #259, pinned by `trident/terminal-failure-reason.test.ts:525+`). Only the `gh pr create` sibling at `orchestrator.ts:1933` remains (task 3).
- [x] Infrastructure auto-retry for `codexStatus=deferred` with a MEASURED terminal cause — `classifyInnerFailure` + `TridentRunStore.beginInfraRetry` (migration 0126), bounded + separate from fix rounds, pinned by `trident/infra-retry.test.ts`. The deferral CLASS retries; the wrapper's own `CODEX_BUILD_*` reason still dies with `/tmp` (task 4).
- [x] Binary pid-death liveness probe (15 s, `LIVENESS_PROBE_INTERVAL_MS`, `trident/tick.ts`) + `trident/checkpoint.sh:196` re-stamping `last_advanced_at` at every inner phase boundary — partial heartbeat. The no-advance reaper still lacks a positive-liveness requirement (task 6).

Tasks, in priority order (Fix 1 first — it is what unblocks enterprise landing):

- [x] 1. FIX 1, DISPATCH HALF — `bound_pr` on the dispatch surface + the dispatch-time REFUSAL + the fail-closed build-path guard. `BoardBoundBuildInput`/`dispatchBoardBoundBuild` (`trident/board-dispatch.ts`) gain a validated `bound_pr` (positive integer; malformed refused with zero state) threaded into `store.create`; a pure exported `detectReviewIntent(task)` refuses any review-shaped free-text dispatch that lacks `bound_pr` with a message NAMING `bound_pr` (never silently converted into a build); a `bound_pr` dispatch skips the ask-before-acting gate (a review round is specified by its PR — the gate protects underspecified BUILDS; non-review dispatches keep it, pinned by a control test); the tool schema (`trident/work-board-build-tool.ts`, auto-exposed over MCP via the generic `mcp/surfaces/neutron-tools.ts` registry bridge — verify, no second change site) advertises `bound_pr`; and `launch()` (`trident/orchestrator.ts` ~1985) fails CLOSED before any host command: a run with `bound_pr !== null` never fires the build workflow, never touches git, and terminal-fails with an honest reason naming the bound PR — until task 2 replaces that branch with the real review executor. Tests: falsification criteria 2 (refusal names `bound_pr`; deleting the guard goes red) and 3 (`count(*) where bound_pr is not null and != 0` > 0 after a bound dispatch), the must/must-not intent corpus, zero-state on refusal, and zero host commands + zero fires for a bound run.
- [ ] 2. FIX 1, EXECUTION HALF — review-only run for a bound PR. When `run.bound_pr` is set the orchestrator resolves the bound PR's HEAD sha via credentialed `gh pr view` (host boundary — the agent never holds the credential, per the standing doctrine), produces the PR's own diff, runs the existing review panel lanes against that head in a DETACHED read-only worktree, posts the fenced ```review-evidence``` comment (verdict + findings + reviewed sha) to THAT PR via `gh pr comment`, best-effort re-triggers the `review-gate` check (tolerate its absence in Open repos; record what happened), and lands terminal SUCCESS with no commit — replacing task 1's fail-closed stop. A review run must be structurally unable to reach `publishBuiltCommit` or any branch/commit/push path (falsification 1: afterwards no new branch, no new commit, no new PR exists; the evidence comment exists on the target). Non-test caller required for every new entry point (falsification 6).
- [ ] 3. FIX 2a — `trident/orchestrator.ts:1933`: carry `created.stderr` into the thrown reason via the existing `publishFailureReason('open a PR for', branch, created.stderr)`; assert a forced `gh pr create` failure persists a `failure_reason` containing the underlying stderr (falsification 4) and that the disclosure guard still holds (no credential material; positive control).
- [ ] 4. FIX 2b — persist the codex deferral reason to the run record: thread the `CODEX_BUILD_*` token/tail of the wrapper `.err` file into the launch-bridge structured result and the thrown terminal cause (`inner-workflow.mjs` ~1765/1770/1815) so `failure_reason`/`terminal_cause` names WHICH of the 22 deferral paths fired after `/tmp` is gone; never a cause that was not measured (#240 rule); test with a seeded `.err` fixture.
- [ ] 5. FIX 3 — the ISO-timestamp window-filter trap: add the store-level window query (correct ISO-string comparison over the TEXT column) plus the seeded 2-year-table test proving a 24h filter returns ONLY recent rows, with the naive `strftime('%s')` numeric form as the red control that matches 100% of rows against today's TEXT column (falsification 5); document the trap at the column and in `docs/INVARIANTS.md`. (No in-repo query does numeric filtering today — the wrong analyses were ad-hoc; the helper + red-pin is the card's offered alternative to an epoch-ms migration.)
- [ ] 6. FIX 4 — hang watchdog positive liveness: the `NO_ADVANCE_HANG_MS` reap consults positive evidence (live launcher process via the existing 15 s probe, or a growing artifact) before reaping; a run with positive liveness is never reaped by the no-advance clock (the 2h `DEFAULT_MAX_INFLIGHT_MS` ceiling still bounds); a dead pid is still reaped fast. Tests both directions; killing the liveness writer must turn a test red.
- [ ] 7. DOCS sweep — `docs/SYSTEM-OVERVIEW.md` review-only dispatch section, `docs/INVARIANTS.md` entries (bound run never reaches the publisher; refusal-not-conversion at dispatch), and the card's falsification checklist recorded in `docs/AS_BUILT.md`.

## Do not (copied verbatim from the card's plan doc)

_Source: `docs/plans/trident-cannot-review-an-existing-pr-handed-review-pr-n-it-b-q284gz.md`. Do not paraphrase this section._

- Do not weaken the brief-part receipt check — it worked.
- Do not "fix" this by having the review lane commit an empty file to satisfy the publisher.
- Do not close this card on Fix 3 or 4 alone; **Fix 1 is what unblocks enterprise.**
- Do not treat `CODEX_BUILD_BRANCH_RECLAIMED` as evidence this is solved — that class (formerly `BRANCH_UNBOUND`) appears addressed since the 00:12Z restart, and is a different defect.
