# IMPLEMENTATION_PLAN — Trident review-only dispatch mode (`bound_pr`): a "review PR #N" dispatch must review THAT PR, never build a docs PR about it

Regenerated 2026-08-18 against origin/main @ 92f82085, resumed on branch `trident/trident-cannot-review-an-existing-p` @ 711b26e2 (exactly one commit ahead of origin/main). Card: "TRIDENT CANNOT REVIEW AN EXISTING PR — bound_pr is 0 of 190 runs; blocking all enterprise merges." Survey re-verified against this tree before regenerating: the dispatch half (task 1) has MERGED to main (#415 — `bound_pr` validated on `dispatchBoardBoundBuild`, `detectReviewIntent` refusal naming `bound_pr`, tool schema in `trident/work-board-build-tool.ts:58-61`, store plumbing); the execution half (task 2) is COMMITTED on this branch (`trident/review-run.ts` `executeBoundReview` + `formatReviewEvidence`, orchestrator `launch()` dispatches every non-null `bound_pr` to it and returns before base resolution/build-fire/publisher, `trident/review-run.test.ts` 25 bound-mode tests, AS_BUILT entry; full-suite audit at merge-base proved the 44 failing files pre-existing). Still true in this tree: `trident/orchestrator.ts:1982` throws away `created.stderr` on `gh pr create` failure while the push sibling directly above already routes through `publishFailureReason(step, branch, stderr)` (defined/exported same file :483, redaction + 600-char bound built in); `started_at`/`last_advanced_at` are ISO-8601 TEXT so any numeric window filter silently matches every row (no store-level window helper, no INVARIANTS entry); the (1b) no-advance hang watchdog still reaps on `last_advanced_at` alone with no positive-liveness consult (`trident/liveness.ts` documents the defect; the 15 s pid-death probe exists but the reaper does not require positive evidence). NEW since the last regeneration: the codex deferral reason now survives `/tmp` GC — the brief-parts lane (#422, on main) added required `wrapperErrTail` to the launch-bridge schema (`inner-workflow.mjs:921/941`), `wrapperErrTailInstruction` in all three bridge prompts, and `codexDeferralMessage(...)` in the thrown terminal cause (:1861), pinned by `trident/inner-workflow.test.ts:81-257` — task 4 is satisfied by that lane and must NOT be rebuilt.

CROSS-LANE HAZARD (do not resolve silently): `.trident/plans/trident/a-fix-round-that-abandons-the-revie.md` plans `bound_pr` as a fix-round PUBLISH-TARGET pin — the opposite semantic. THIS card's semantic is live: `bound_pr` set ⇒ review-only, enforced at `launch()`; the collision is recorded in the code comment at the guard. A future commit-capable interpretation must add its own discriminator rather than weakening the review-only return.

Card do-nots, binding on every task: do NOT weaken the brief-part receipt check; do NOT satisfy the publisher by committing an empty file from a review lane; do NOT close the card on Fix 3/4 alone; `CODEX_BUILD_BRANCH_RECLAIMED` is a different, addressed defect — not evidence here. Each merged task appends its own `docs/AS_BUILT.md` entry per repo convention.

Already satisfied in this tree (verified, do not rebuild):

- [x] `bound_pr INTEGER` column + full store plumbing — migration `0124`, `trident/store.ts` (types, COLS, `create()` write, row map), schema regenerated.
- [x] Push-failure stderr carried into the stored reason with the credential-disclosure guard (`publishFailureReason('push', branch, pushed.stderr)` + `classifyPublishFailure` + `redactPushError`, pinned by `trident/terminal-failure-reason.test.ts:500+`). Only the `gh pr create` sibling at `orchestrator.ts:1982` remains (task 3).
- [x] Infrastructure auto-retry for `codexStatus=deferred` with a MEASURED terminal cause — `classifyInnerFailure` + `beginInfraRetry`, pinned by `trident/infra-retry.test.ts`.
- [x] Binary pid-death liveness probe (15 s, `LIVENESS_PROBE_INTERVAL_MS`, `trident/tick.ts`) + `trident/checkpoint.sh` re-stamping `last_advanced_at` at every inner phase boundary — partial heartbeat. The no-advance reaper still lacks a positive-liveness requirement (task 6).

Tasks, in priority order (Fix 1 first — it is what unblocks enterprise landing):

- [x] 1. FIX 1, DISPATCH HALF — `bound_pr` on the dispatch surface + the dispatch-time REFUSAL + the fail-closed build-path guard. LANDED on main (#415): validated `bound_pr` threaded into `store.create`; pure `detectReviewIntent(task)` refuses review-shaped free text lacking `bound_pr` with a message NAMING `bound_pr`; tool schema advertises `bound_pr` (auto-exposed over MCP); falsification 2 and 3 pinned by tests.
- [x] 2. FIX 1, EXECUTION HALF — review-only run for a bound PR. ON THIS BRANCH (711b26e2): `executeBoundReview` resolves the bound PR's head via host-boundary `gh pr view`, persists `gh pr diff` as the panel artifact, reviews the exact head in a detached read-only worktree removed in `finally`, imports no publisher/branch/commit/push helper, posts the fenced ```review-evidence``` comment (verdict + findings + reviewed sha + run id) to THAT PR via `gh pr comment`, best-effort re-triggers `review-gate` recording `triggered`/`absent`/`failed`, and lands terminal `done` with no commit; `launch()` returns from the `bound_pr` guard before base resolution, the build-workflow fire, and every publisher path. Falsification 1 + 6 pinned by `trident/review-run.test.ts` (25 tests) + orchestrator tests.
- [x] 3. FIX 2a — `trident/orchestrator.ts:1982`: carry `created.stderr` into the thrown reason via the existing `publishFailureReason('open a PR for', branch, created.stderr)`; red-first end-to-end test forcing `gh pr create` to fail and asserting the persisted `failure_reason` contains the underlying stderr (falsification 4), plus pure formatter + disclosure-guard positive-control tests. Do NOT touch the `:1985` sibling (`could not confirm an open PR`) — `detectExistingPr` swallows its probe stderr by design.
- [x] 4. FIX 2b — persist the codex deferral reason to the run record. SATISFIED BY THE BRIEF-PARTS LANE (#422, on main): required `wrapperErrTail` in the launch-bridge structured result (`inner-workflow.mjs:921/941/944`), `wrapperErrTailInstruction(errFile)` in all three bridge prompts (:1710/:1742/:1771), and `codexDeferralMessage(label, codexStatus, wrapperErrTail)` in the thrown terminal cause (:1861) so `failure_reason` names WHICH `CODEX_BUILD_*` path fired after `/tmp` is gone; empty tail stated honestly ("wrapper stderr was empty."), never invented (#240 rule). Pinned by `trident/inner-workflow.test.ts:81-257`. Verified — do not rebuild.
- [ ] 5. FIX 3 — the ISO-timestamp window-filter trap: add the store-level window query (correct ISO-string comparison over the TEXT `last_advanced_at` column) plus the seeded 2-year-table test proving a 24h filter returns ONLY recent rows, with the naive `strftime('%s')` numeric form as the red control that matches 100% of rows against today's TEXT column (falsification 5); document the trap at the column definition and in `docs/INVARIANTS.md`. (No in-repo query does numeric filtering today — the wrong analyses were ad-hoc; the helper + red-pin is the card's offered alternative to an epoch-ms migration.)
- [ ] 6. FIX 4 — hang watchdog positive liveness: the (1b) no-advance reap consults positive evidence (live launcher process via the existing 15 s probe, or a growing artifact) before reaping; a run with positive liveness is never reaped by the no-advance clock (the 2h `DEFAULT_MAX_INFLIGHT_MS` ceiling still bounds); a dead pid is still reaped fast. Tests both directions; killing the liveness writer must turn a test red.
- [ ] 7. DOCS sweep — `docs/SYSTEM-OVERVIEW.md` review-only dispatch section, `docs/INVARIANTS.md` entries (bound run never reaches the publisher; refusal-not-conversion at dispatch; the ISO-TEXT window trap if task 5 has not already recorded it), and the card's falsification checklist recorded in `docs/AS_BUILT.md`.

## Do not (copied verbatim from the card's plan doc)

_Source: `docs/plans/trident-cannot-review-an-existing-pr-handed-review-pr-n-it-b-q284gz.md`. Do not paraphrase this section._

- Do not weaken the brief-part receipt check — it worked.
- Do not "fix" this by having the review lane commit an empty file to satisfy the publisher.
- Do not close this card on Fix 3 or 4 alone; **Fix 1 is what unblocks enterprise.**
- Do not treat `CODEX_BUILD_BRANCH_RECLAIMED` as evidence this is solved — that class (formerly `BRANCH_UNBOUND`) appears addressed since the 00:12Z restart, and is a different defect.
