# IMPLEMENTATION_PLAN.md — one factory for TridentRun test fixtures

Governing card: "Every branch that adds a TridentRun field breaks up to 14 fixtures across the repo — one factory, not twenty literals", base `main`.

Survey findings (2026-08-17):
- No factory exists (`makeTridentRun` matches nothing). Each fixture file carries its own full ~36-field literal (`makeRun`/`run`/`fakeRun`/`runWith`/`doneRun`/`localRun`).
- Exactly 15 test files contain a full hand-written `TridentRun` literal, identified empirically as the only files containing all three of `crash_recoveries:`, `started_at:`, `last_advanced_at:` as object keys (these fields are required on `TridentRun` but deliberately absent from `CreateTridentRunInput` and `TridentRunUpdate`, so only a full literal spells them).
- The card's `arbiter` fixture (PR #367, `infra_retries`) is NOT in this checkout — no `infra_retries` anywhere, no arbiter files. Out of scope until that PR lands; the T3 guard will catch it at merge time if it arrives as a literal.
- Cast-based partial fixtures (`as unknown as TridentRun` in `work-board/complete-refuses-live-run.test.ts`, `trident/publish-rebase-realgit.test.ts`, `trident/terminal-failure-reason.test.ts`, `open/wiring/__tests__/trident-launcher-liveness.test.ts`) do NOT break on field additions and do NOT trip the sentinel trio — leave them alone.
- `gateway` and `open` both already declare `"@neutronai/trident": "workspace:*"`, so a factory under `trident/` is importable from every fixture site (`work-board` cannot import trident — cycle — but has no full literal, only a cast).

- [x] **T1 — create the shared factory.** New `trident/testing/make-trident-run.ts` exporting `makeTridentRun(over: Partial<TridentRun> = {}): TridentRun`, defaults taken VERBATIM from `trident/state-machine.test.ts`'s local `makeRun` (the canonical neutral fixture). New unit test `trident/testing/make-trident-run.test.ts`. Migrate `trident/state-machine.test.ts` as the first consumer (delete its local `makeRun`, rename call sites) — zero-diff by construction since the defaults are copied from it. `trident/tsconfig.json` includes `./**/*.ts`, so no config change.
- [ ] **T2a — migrate the 11 remaining trident-package fixture files** to `makeTridentRun` (relative import `./testing/make-trident-run.ts`): `trident/merge.test.ts` (its `makeRun`; the derived `localRun` at ~line 389 already delegates and needs no change beyond the base), `trident/ported-fixes.test.ts`, `trident/git-mode.test.ts`, `trident/inner-loop.test.ts`, `trident/conflict-resolver.test.ts`, `trident/run-progress.test.ts`, `trident/run-driving.test.ts`, `trident/delivery.test.ts`, `trident/merge-realgit.test.ts` (its `localRun(repo,id,branch)` becomes a thin wrapper over the factory), `trident/terminal-observer.test.ts`, `trident/terminate.test.ts`. Pure refactor rule: for each file, every field whose hand-written value differs from a factory default is preserved as an explicit override; each file may keep a one-line local alias (e.g. `const run = (over: Partial<TridentRun> = {}) => makeTridentRun({ <this file's defaults>, ...over })`) so per-test call sites don't change at all. No assertion changes; per-file pass count identical before/after.
- [ ] **T2b — migrate the 3 cross-package fixture files** via `import { makeTridentRun } from '@neutronai/trident/testing/make-trident-run.ts'`: `gateway/proactive/__tests__/work-wakeup-selection.test.ts` (`run`), `gateway/http/work-board-surface.test.ts` (`fakeRun`), `open/__tests__/open-skill-forge-wiring.test.ts` (`doneRun`). Same pure-refactor rule as T2a. Both packages already depend on `@neutronai/trident`.
- [ ] **T3 — guard against literals returning.** New `trident/__tests__/no-handrolled-trident-run-fixtures.test.ts`: enumerate tracked test files (`git ls-files '*.test.ts'` from the repo root, root = two dirs up from the test), fail any file (other than an in-test `ALLOWLIST: string[]` that starts EMPTY) containing all three of `\bcrash_recoveries\s*:`, `\bstarted_at\s*:`, `\blast_advanced_at\s*:` (note: `\b` correctly rejects `max_crash_recoveries:` since `_` is a word char — verified). Failure message names the offending files, points at `trident/testing/make-trident-run.ts`, and explains the allowlist escape hatch for a legitimate future file that overrides all three via the factory. The factory file itself is not a `.test.ts` and `trident/store.ts` is production code — both automatically exempt. Sentinel validated against the whole repo today: exactly the 15 fixture files trip it, nothing else; after T2a/T2b, zero files trip.
- [x] **T4 — schema-surface treatment.** Already satisfied: `migrations/expected-schema.txt` IS generated from the applied schema by `migrations/regen-snapshot.ts` and byte-compared by `migrations/snapshot.test.ts` (its failure message names the refresh command) — no transcription step exists to fix. Per the card, the deliberate 36-column literal in `trident/store.test.ts` (~line 680) and `COLS` in `trident/store.ts` stay exactly as they are. No action.

Out of scope / notes for the record: `trident/terminal-failure-reason.test.ts`'s cast fixture carries `ralph: 1` (a number where the type says boolean) — pre-existing, hidden by its cast, deliberately NOT touched (this card is a refactor, not a review). Do not weaken any `TridentRun` required field. Do not migrate `trident/orchestrator.test.ts` / `trident/store.test.ts` / `trident/crash-recovery.test.ts` — they build runs through `store.create()`, not literals.
