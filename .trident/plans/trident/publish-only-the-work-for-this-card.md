# IMPLEMENTATION_PLAN — publish the severity-gate round-economy salvage (tag `sevgate-wip-merged`)

RESUME NOTE (PUBLISH-ONLY AS CARDED; NO LONGER TRUE OF THE BRANCH — see FIX ROUND 5, item 5). The card's work was already built, tested and committed, and the publish itself was a clean fast-forward. What the branch carries now is that publish PLUS the Argus fix rounds recorded below (FIX ROUND 2 through FIX ROUND 12), each answering a named finding, because a review contract outranks a publish-only prohibition. The salvage's commits live in the repo of record (Projects/neutron-open/code), reachable from the canonical repo: tag `sevgate-wip-merged` = 1662fa61, three commits in order cut from bee6dfd9 (origin/main at the time, and STILL origin/main as of 2026-09-01 — main has not moved): 8799a926 → 17c72201 → 1662fa61. They touch six files, all under trident/: `inner-workflow.mjs`, `inner-workflow.test.ts`, `lane-retry.test.ts`, `__tests__/severity-gate.test.ts`, `__tests__/synthesis-unavailable.test.ts`, `__tests__/ci-gate.test.ts` (all mode 100644). The only remaining work is to land those commits on this branch, re-verify, and hand the diff to the reviewers.

BUILT — the severity/advisory round economy (8799a926 + 17c72201). A finding the file already classifies non-blocking no longer costs a fix round, and an advisory-only rejection no longer buys one. DELIBERATE, RECORDED DEVIATION from acceptance criterion 1: an advisory-only rejection terminates REQUEST_CHANGES + blockKind `'advisory-only'` — the fix loop exits and no round is bought — rather than being laundered into an APPROVE, because this file's standing rule is that a rejection carrying no stated reason is malformed, not benign. (This line said `'infra-only'` until FIX ROUND 11: FIX ROUND 6 split the exit into a third kind, and `classifyBlock` returns `noReviewRan ? 'infra-only' : 'advisory-only'` — `'infra-only'` is now reserved for a stop NO panel judged. The loop exits on both; only the recorded reason differs.)

BUILT — CI-red provenance (1662fa61). A CI red that predates the branch may not force a code round: `inner-workflow.mjs` now tracks prior reds (`priorRed`) and only a NEW red buys a round. Guard record (acceptance criterion 6): the mutation site `if (!priorRed.has(f.name)) {` occurs exactly once in `trident/inner-workflow.mjs` (the only production file that carries it; the string is also quoted inside this plan doc, so a repo-wide grep returns those quotations too); `trident/__tests__/ci-gate.test.ts` imports inner-workflow and pins the prior-red behavior, so it is the guard that must fail under mutation. Suite record at the salvage tip: the six-file run (the six touched files plus `__tests__/dead-core-seat-e2e.test.ts`) is 381 pass / 0 fail, 1283 expect() calls on an unmoved main.

PUBLISH RECORD (this iteration). `git merge --ff-only` from a branch cut at bee6dfd9 fast-forwarded cleanly to 1662fa61 — no conflicts, nothing resolved by hand. Fidelity gate passed: `git diff 1662fa61 HEAD --stat -- <six files>` is empty, and `if (!priorRed.has(f.name)) {` occurs exactly once in `trident/inner-workflow.mjs`. Six-file re-run reproduced the expected counts exactly: 381 pass / 0 fail / 1283 expect() calls. The full stage-2 suite surfaced pre-existing, environment-dependent failures unrelated to this diff (landing/chat-bundle, GitHub-credential composer, install.sh Codex/GBrain self-install, gbrain-memory resolver/build — all reproduced failing identically on an unmodified BASE checkout, i.e. present before this merge) plus five mobile `app/__tests__/*` device-lane tests that were red on this branch's device lane but did not reproduce failing in a file-scoped BASE re-run (the runner's own isolation note applies: a file-scoped run does not reproduce the device lane, so that comparison is inconclusive, not proof either way). One NEW failure was found and is NOT masked: `trident/__tests__/phase-model-coverage.test.ts` ("every workflow label is claimed by a phase OR listed as unrouted") now fails because the salvage's CI-red-provenance commit (1662fa61) added a probe label, `ci-base-probe-round-<n>`, to `inner-workflow.mjs` that this coverage test does not yet know about; the same test is confirmed green on an unmodified BASE checkout, so this regression is real and is caused by the salvage content itself, not by anything done in this publish step. It is FIXED in round 2, below — a red mandatory guard is not something a publish step may leave behind.

FIX ROUND 2 (Argus round-1 blockers). The publish-only prohibition gave way to the review contract for four confirmed blockers; nothing else was touched, and the salvage's own six files keep their salvaged content.
1. ROUTING REGRESSION (blocker). The salvage's new probe seat `ci-base-probe-round-<n>` was unrouted: `routeModel` prefix-matches `ci-probe-round-`, which is NOT a prefix of `ci-base-probe-round-`, so a one-command `gh api` transcription landed on the terminal Opus/high fallback — the exact `head-probe-round-N` incident `phase-models.ts` documents. Fixed at both seams: a `routeModel` arm mapping it to `ROLE_MODEL['ci-probe']`, and the label added to the `bookkeeping` phase in `trident/phase-models.ts`. `trident/__tests__/phase-model-coverage.test.ts` — the guard that was RED at the branch tip and green at base — is green again (44 pass / 0 fail).
2. SHELL INJECTION (blocker). `probeCiBase` interpolated `${ref}` (the pinned base sha, else `baseBranch` verbatim from the workflow args) bare into a `gh api` URL inside a bash command; `git check-ref-format --branch 'main$(id)'` accepts such a name. Now `encodeURIComponent`d and single-quoted, exactly as the hardened sibling `probeRequiredChecks` already does. The `per_page=100` cap is left as it is: truncation can only HIDE a base red, which un-excuses a check — the fail-closed direction.
3. THE PRE-EXISTING HATCH'S SAFETY NET WAS UNREACHABLE (blocker). The advisory finding told its reader to "treat it as a BLOCKER in your own words", but `ciFindings` were built AFTER every panel prompt and after synthesis, so no seat ever saw them. The base probe and `ciFindings` now run ABOVE the synthesis prompt and are interpolated into it as `ciFindingsPrompt` (the same shape as the existing `suiteFindingsPrompt`), so the hatch is addressed to the seat that actually sets the verdict — and a REQUEST_CHANGES it returns survives the non-forcing path. The core reviewers still cannot see it: they are dispatched before CI is probed, deliberately and unchanged. Same-name matching is also narrowed — `classifyCi`'s `'unnamed check'` placeholder is now the named const `CI_UNNAMED_CHECK` and is excluded from `ciPreexistingNames`, so two unreadable rows can no longer excuse each other.
4. NULL-SPREAD ON THE NON-FORCING PATH (minor, from the same finding set). `{ ...severityGated, … }` with a dead synthesis seat dropped the CI advisories; it is now `{ ...(severityGated ?? {}), … }`. The direction was already fail-closed; the findings are no longer lost from the report.
Two nits from the same review are fixed: the misplaced `#184 false claim` docblock in `__tests__/severity-gate.test.ts` now sits above `gateDocblock()` again, and the mutation-site claim below is stated file-scoped rather than repo-wide.

5. CI TYPECHECK (blocker). `bun x tsc -p trident/tsconfig.json` was FAILING on the salvaged `inner-workflow.test.ts`: `stripAdvisoryMarkers` is declared `(s: unknown) => { findings?: unknown[] } | null`, and `toBe({ verdict: 'REQUEST_CHANGES' })` is a weak-type mismatch against it. The dead-seat fixture now declares the optional `findings` it was always standing in for. `trident/tsconfig.json` PASSES. The typecheck matrix still reds five OTHER projects (`app`, `gateway`, `logger`, `onboarding`, root) on `Cannot find type definition file for '@types'`, `node:zlib` `crc32`, and two unrelated overload errors — none in any file this branch touches.

ROUND-2 EVIDENCE. `bun run test` (NEUTRON_TEST_JOBS=4) completed: `run-tests: FAIL — 5/16 lane(s)`, 41 failures across 14 files, NONE under `trident/`. Every one of those 14 files was re-run at BASE content (the nine branch files checked out at bee6dfd9) with the identical invocation: the general group is 129 pass / 14 fail at head AND at base, fail lists byte-identical; the device lane (all 38 `installNativeHarness` files, the lane's own shape) is 322 pass / 10 fail at head AND at base, fail lists byte-identical. So suiteOutcome is `failed-preexisting`, proven both directions. `bun test trident/` is 2975 pass / 0 fail across 104 files; the six-file set is 385 pass / 0 fail / 1300 expect() (was 381/1283 before this round's four added guards).

FIX ROUND 3 (Argus round-2 blockers + majors). Same rule as round 2: the publish-only prohibition gives way to the review contract, and nothing outside the findings was touched.
1. A DEAD SYNTHESIS DROPPED THE CI ADVISORY (blocker). Round 2's `?? {}` kept the findings only as far as `withCi`; `synthesisOrInfraBlock` then replaced the WHOLE verdict-less object with the shared `SYNTHESIS_UNAVAILABLE` constant, so the report named the dead seat and said nothing about the red build. The guard round 2 wrote greps the source and never executed the substitution, which is why it stayed green. `synthesisOrInfraBlock` now CARRIES non-lane findings onto the block (lane blocker still first, verdict and `blockKind` still the constant's, a fresh frozen object so the shared instance is untouched), and the new guard in `__tests__/synthesis-unavailable.test.ts` RUNS the tail and reads the merged list.
2. AN ADVISORY-ONLY REJECTION STILL BOUGHT A ROUND (blocker). `classifyBlock` returned 'code' the instant `deferredPeers` was empty — WITHOUT reading the findings — so the whole advisory economy above it only applied when a lane had ALSO failed. On the ordinary healthy-panel path a rejection whose only findings were nits, or a red that predates the branch, re-Forged a full round with nothing to change. The early return is gone: code findings decide first, a down peer is 'infra-only' second, and an EMPTY findings list still re-Forges — a rejection with no stated reason is malformed, not benign, and 'infra-only' would exit the loop on it.
3. THE ADVISORY EXIT WAS AUTO-RETRIED AS INFRASTRUCTURE (blocker, same finding). `infraTerminalCause` fell back to ANY titled finding, so an advisory-only stop reported the advisory's own title as its measured cause — and `classifyInnerFailure` (trident/orchestrator.ts:876-880) reads 'infra-only' PLUS a non-empty cause as `infrastructure`, relaunching the whole run up to three times straight back into the same fix round. The fallback now skips findings this file has already declared non-blocking (`isNonBlockingFinding`, the one predicate), so such a stop measures `''` and classifies `genuine` — terminal, not retried. A LANE finding, or any real blocker, still names itself exactly as before. The orchestrator is NOT edited: the cause is measured where it is known, which is this file's stated contract for that field.
4. THE BASE PROBE COULD NOT SEE STATUS-CONTEXT CI (major). `gh pr checks` on the PR side reports check runs AND classic commit statuses; the base probe asked only `/check-runs`, so a Buildkite/Travis/webhook red failing identically on both sides could never enter `ciPreexistingNames` and was charged as new every round — fail-closed, but the hatch was silently inert for every repository whose CI is a status. The probe now reads BOTH endpoints in its one seat, marked `___SECTION=RUNS` / `___SECTION=STATUSES` (the shape the hardened sibling `probeRequiredChecks` already uses), reshaping `{context,state}` into the same `{name,state}` rows so there is still ONE parser. `ciSection` splits them (`classifyCi` reads first `[` to last `]`, so two arrays in one string parse as neither) and `mergeBaseCi` combines them: reds add up, an UNREADABLE half makes the whole measurement `unknown` — half a base is not a base — and only two readable clean halves report a clean base.
5. A SLASH IN THE BASE BRANCH DISABLED THE HATCH (minor). `encodeURIComponent('release/1.x')` is `release%2F1.x`, which the commits endpoint does not resolve; the probe answered 'unknown' (safe) while burning a seat every red round to learn nothing. `encodeRefPath` encodes each SEGMENT, keeping the separator and still leaving no `$`, backtick or space in the string — the injection is closed by `shSingleQuote`, which is unchanged and still pinned. The 40-hex `pinnedBase` path is unaffected. `probeRequiredChecks` has the same whole-string encoding and is NOT touched: it is main's code, outside this branch's diff.
6. THE MUTATION NOMINATION'S CONTROL (major). Already re-nominated in round 2 and repeated below; the transmitted nomination for this round carries `trident/__tests__/severity-gate.test.ts`, never `trident/ascii-trim.test.ts`.

ROUND-3 FINDINGS ACCEPTED AND NOT CHANGED, stated here because a reviewer is owed the reason.
- "The hatch removes the merge block, not just the forced round" (major). That is what the hatch IS, and it is the same shape the full-suite `failed-preexisting` hatch has carried in this file for months: a check red for reasons that are red at the base too is not evidence about this diff. The excuse is name-matched, so the synthesis seat is shown the advisory and told in the prompt to return REQUEST_CHANGES in its own words if the diff touches what that check exercises; and branch protection remains the deterministic backstop — an APPROVE over a genuinely required red check fails at `gh pr merge` as a `TridentMergeError`. Adding a second CI re-read inside `trident/merge.ts` is a merge-path design change, not a fix to this diff, and it is not made here.
- "A complete healthy panel's rejection is recorded as REVIEW_NOT_RUN" (minor, single reviewer). Round 3 makes this REACHABLE more often (item 2), and it is the honest-reporting cost of the exit: `writeTerminalResult` reserves REQUEST_CHANGES for `blockKind: 'code'` with findings. Widening that discriminator changes what every downstream consumer of `recordedTerminalVerdict` reads and is out of this branch's scope; the terminal cause is now `''` for these stops, so the run is classified `genuine` rather than retried, which was the harmful half.
- "enforceSeverityGate's advisory arm is unreachable in production" (minor, single reviewer). True as stated: `stripAdvisoryMarkers` runs at the binding above the gate, and the only producers of `advisory: true` attach after it. The acceptance-test case is therefore a specification of the gate's contract rather than a reproduction of a shipped path, and it is left in deliberately — the arm is what keeps the contract true if a future producer attaches earlier.

FIX ROUND 4 (Argus round-3). The single BLOCKER was the transmitted mutation nomination, not the code: the CONTROL it carried named `trident/ascii-trim.test.ts`, a file that has never existed in this repo, so `mutation-prover.ts:791` would have failed a valid proof on a control that is red before any mutation is applied. This round's reported nomination carries the re-nominated control `trident/__tests__/severity-gate.test.ts` (verified by two reviewers under the same mutation: guard `trident/__tests__/ci-gate.test.ts` goes 143 pass / 3 fail, control stays 18 pass / 0 fail). Three code-side majors from the same review are fixed; the rest are recorded below with their reasons.
1. `classifyBlock` COULD THROW ON A MALFORMED FINDINGS LIST (major). `(synthesis && synthesis.findings) || []` accepted any truthy value and then called `.filter` on it, so a synthesis carrying `findings: 'oops'` — model-authored JSON — threw a TypeError out of `runReviewRound` and killed the round. Round 2's removal of the `deferredPeers` early return took away the shield that had hidden this path. It now asks `Array.isArray` first, exactly as every sibling gate in the file does, and reads an unreadable list as NO findings — which lands on the 'code' arm, the direction that re-Forges rather than exiting the loop.
2. `kind: 'lane'` WAS A LAUNDERING HATCH (major). `stripAdvisoryMarkers` removed the model's `advisory` marker and left its `kind`, but `classifyBlock` reads `kind === LANE_FINDING_KIND` as "a lane failed, there is nothing here for Forge to change" and 'infra-only' EXITS the fix loop. Both markers are workflow facts this file stamps when it has measured something; neither is the model's to set. The strip now removes a lane kind for the same reason and in the same place — before either gate reads it, and before this file attaches its own lane findings downstream. A `kind` the model invented for itself means nothing to any gate here and is left alone. This was never a merge hazard (the verdict stays REQUEST_CHANGES) but it bought a laundered code blocker its way out of the round.
3. THREE COMMENTS DESCRIBED CODE THAT NO LONGER BEHAVES THAT WAY (minor/nit; in this file comments are the spec). `withSuiteBlocker`'s advisory-only arm promised the stop "is reported with the suite finding's own title rather than silently" — round 3 made `infraTerminalCause` exclude non-blocking findings on purpose, so the cause is `''` and the findings ride out on the terminal result instead; the comment now says that, and why. `probeCiBase` credited `encodeURIComponent` with closing the shell injection — the closure is `shSingleQuote`, and the encoding is about URL correctness. `ciPreexistingNames` now states what a MIXED base (some rows red, others still running) means: the set is built from `failing` only, so a base row that never settled contributes no name and excuses nothing, which is exactly what "pending excuses nothing" means per check. And in `inner-workflow.test.ts`, the rationale citing `classifyBlock`'s deleted early return is corrected (the assertion was always right).
Both behaviour fixes are covered by new tests in the existing severity-gate block of `trident/inner-workflow.test.ts`: a model-supplied lane kind is stripped and the blocker reads as 'code' again, a non-lane kind survives, and a non-array `findings` classifies 'code' instead of throwing.

ROUND-4 FINDINGS RECORDED AS ACCEPTED AT THE TIME — THE FIRST TWO WERE OVERRULED IN ROUND 5 AND ARE NOW FIXED; READ THIS SECTION AS THE HISTORY OF THAT ARGUMENT, NOT AS CURRENT BEHAVIOUR — Argus's own stated alternative for the first of these is "an explicit recorded acceptance", and this is it.
- "The pre-existing-CI hatch matches by check NAME only, not by cause" (major, all three reviewers). ACCEPTED, deliberately and with the trade stated. Name matching is the same shape the full-suite `failed-preexisting` hatch has carried in this file for months, and cause-aware matching would mean parsing arbitrary CI log output to decide whether two reds are "the same failure" — a heuristic that fails OPEN when it guesses wrong, which is a worse failure direction than the one it replaces. The compensations are real and are all in the diff: the base is MEASURED, not claimed; the sentinel `CI_UNNAMED_CHECK` cannot match itself; the advisory is interpolated into the synthesis prompt and tells the seat, in words, to return REQUEST_CHANGES if the check's base failure looks different or the diff touches what it exercises; and on a protected repository `gh pr merge` remains the deterministic backstop. On an unprotected repository the synthesis seat's judgement IS the remaining gate, and that is the accepted cost. Making it cause-aware is a design change to the hatch, not a fix to this diff.
- "An advisory-only reviewed outcome is recorded as REVIEW_NOT_RUN downstream" (major, single reviewer; carried from round 3 where the same point was raised as a minor). ACCEPTED, unchanged, for the reason given then and re-checked now: `recordedTerminalVerdict` (trident/orchestrator.ts) reserves REQUEST_CHANGES for `block_kind: 'code'` with findings, and every downstream consumer of that discriminator reads it. Fixing the label honestly needs a third block kind for "the panel judged the code and found only non-blocking things", which changes the fix loop's exit condition, the orchestrator contract and the outer loop's failure path — outside a publish card's scope, and not a change to make while the branch is under review. The harmful half was fixed in round 3: the terminal cause is `''`, so such a run classifies `genuine` and is not auto-retried three times into the same round. The findings are preserved on the row either way.
- "The branch is not the pure 3-commit republish the task describes" (nit). True and stated plainly here so the PR body does not claim byte-fidelity: the branch carries the three salvage commits plus this plan and the round-2/3/4 fixes above, each of which is a recorded Argus finding rather than an improvement made while resolving. The six salvaged files' salvaged CONTENT is unchanged except where a named blocker required otherwise, and every such change is itemised in the round sections above.

MUTATION GATE (re-nominated). The round-1 nomination's CONTROL named `trident/ascii-trim.test.ts`, which does not exist — a control that is red with no mutation applied. Control is now `trident/__tests__/severity-gate.test.ts` (18 pass / 0 fail under the same mutation); guard stays `trident/__tests__/ci-gate.test.ts`.

- [x] Build the three fixes (committed as 8799a926, 17c72201, 1662fa61; tagged `sevgate-wip-merged` in the repo of record)
- [x] Record the deviation from acceptance criterion 1 and the criterion-6 guard results (above, and in the PR body for reviewers)

FIX ROUND 5 (Argus round-4). Two confirmed BLOCKERS were the two findings this plan had twice recorded as ACCEPTED-and-unchanged; a third was the transmitted mutation nomination again. Argus's ruling is the one that stands, and both are fixed here. The scope note below is corrected at the same time, because the honest description of this branch is part of what round 4 asked for.
1. AN EXCUSED CI RED PASSED AN APPROVE TO THE MERGE (blocker). The non-forcing arm of `withCi` returned the panel's verdict untouched, so a red PR whose every failing check name also fails at the base went APPROVE -> `gh pr merge`, with branch protection — repository CONFIGURATION, the thing this gate exists to stop relying on — as the only backstop. And the excuse is a NAME match: `.github/workflows/ci.yml` names its jobs `shard N/8`, so a test THIS branch broke, landing inside a shard that was already red for another reason, is invisible to it. The two questions that arm was answering with one match are now answered separately, both in code: "is there code work here?" stays NO (`ciFindingsBlock` is false, `classifyBlock` reads the advisories as `advisory-only`, the fix loop exits without re-Forging — the salvage commit's whole claim, intact), and "may this merge over a red PR?" is now unconditionally NO (the arm sets `verdict: 'REQUEST_CHANGES'`). The excuse buys a ROUND, never a MERGE. Nothing is delegated to the synthesis seat: the closing sentence of `ciFindingsPrompt` that told it to "return REQUEST_CHANGES in your own words" is gone, because a gate whose contract is "every answer is derived in code, never read by a model" may not keep a model as its only guard. The seat can still ADD a blocker; it can no longer be the thing that removes the hold.
2. A COMPLETED ADVISORY-ONLY REVIEW WAS RECORDED AS REVIEW_NOT_RUN (blocker). `classifyBlock` returned `'infra-only'` for a HEALTHY panel whose findings were all non-blocking, and `'infra-only'` means, in the outer contract's own words, that NO REVIEW SEAT EVER JUDGED THE CODE. It is now a distinct kind, `'advisory-only'`: the same loop exit (no round is bought) with a true statement attached. Threaded end to end — `classifyBlock` and `withSuiteBlocker` emit it, the fix-loop condition exits on it, `writeTerminalResult` and `recordedTerminalVerdict` (trident/orchestrator.ts) both count it as a reviewer that spoke, `parseInnerResult` decodes it rather than failing it to null, and `isInfraDeath` deliberately does NOT list it. The resume seam is the other half: `resumeMode === 'fix'` asserted `blockKind: 'code'` over the recorded findings unconditionally, so a checkpoint written by an advisory-only round bought at resume the very fix round the round itself had refused to buy. It now derives the kind from the recorded list with `classifyBlock`'s own predicate (`isNonBlockingFinding`), so the two cannot drift. `infra-only` is untouched, and so is every consumer that keys on it (`infra-block.ts`, `delivery.ts`, the terminal-cause path).
3. `ciSection` SPLIT ON THE FIRST MARKER MENTION (major). It used `indexOf` where the sibling reader `probeSections` uses a whole-line, last-occurrence match — and that sibling uses it because a payload containing the marker text once moved a boundary past the real content. With the probe's command ECHOED into the reply, both boundaries were found in the echo line, both sections came back empty, and the base read 'unknown' — fail-shut, so nothing was excused, but the entire hatch was INERT every round with no signal anywhere that it never engaged. Same discipline as the sibling now: the opening boundary is the LAST line that is exactly the marker, the closing boundary is the next line that STARTS with `___SECTION=`. A check name carrying the marker text is payload, not a boundary.
4. THE CI FINDINGS REACHED THE SYNTHESIS PROMPT UN-REDACTED (minor). A check's name comes from `.github/workflows/*.yml`, a file Forge writes, so round 3's interpolation opened a build-authored-text path into the seat that sets the verdict. It now goes through `redactProbeText` and a length cap, exactly as every sibling probe text in this file does.
5. SCOPE, STATED HONESTLY (minor). This branch is NOT the pure three-commit republish the card describes, and the lead note above now says so rather than being read as a claim of byte-fidelity. It carries the three salvage commits (8799a926, 17c72201, 1662fa61) plus this plan and the round-2/3/4/5 fixes, each of which answers a named Argus finding. Files under `trident/` touched across the whole branch, measured with `git diff --name-only bee6dfd9..HEAD` at FIX ROUND 12: `inner-workflow.mjs`, `inner-workflow.test.ts`, `inner-workflow-resume.test.ts`, `lane-retry.test.ts`, `phase-models.ts`, `inner-loop.ts`, `review-run.ts`, `inner-loop-sim.ts`, `orchestrator.ts`, `orchestrator.test.ts`, `review-round-cap.test.ts`, `__tests__/severity-gate.test.ts`, `__tests__/synthesis-unavailable.test.ts`, `__tests__/ci-gate.test.ts` (plus `SPEC.md`, `IMPLEMENTATION_PLAN.md` and this plan copy outside `trident/`). The six-file target set's count moves with each round's added guards and is reported per round, not as the card's original 381/1283.
6. THE MUTATION NOMINATION. Round 4 confirmed the transmitted CONTROL still named `trident/ascii-trim.test.ts`, which has never existed in this repo — a control that is red before any mutation is applied, so `mutation-prover.ts:791` fails a valid proof closed. This round's reported nomination carries `trident/__tests__/severity-gate.test.ts`, and the FIND string's uniqueness is stated file-scoped (`mutation-prover.ts:656` counts occurrences in `claim.file` only), not repo-wide — the same string is quoted in this plan doc.

ROUND-5 FINDINGS ACCEPTED AND NOT CHANGED.
- "`bun x tsc -p trident/tsconfig.json` exits 0 is not independently reproducible" (minor, both reviewers). Accepted as stated: both reviewers measured a head-vs-base delta of ZERO, and the errors they saw are in files this diff does not touch (`cores/sdk/manifest.ts`, `git-mode.test.ts`) and depend on how `node_modules` was materialised. No claim of a clean repo-wide typecheck is made here; the claim is the delta.

- [x] PUBLISH: land tag `sevgate-wip-merged` on this branch (`git merge --ff-only`; cherry-pick 8799a926 17c72201 1662fa61 only if ff refuses, resolving any conflict in favour of the salvage ref for the six files and saying so in the PR body), verify byte-fidelity to 1662fa61 for the six files, re-run the six-file suite (expect 381 pass / 0 fail / 1283 expect()) and the full stage-2 suite, persist this plan, write the branch diff for the reviewers, and stop. Write NO new code.

FIX ROUND 6 (Argus round-5). Two blockers, both introduced by round 5's own fix, and the three guards that let them through. Full detail in `IMPLEMENTATION_PLAN.md` (FIX ROUND 6); in short:
1. The non-forcing CI arm set `verdict: 'REQUEST_CHANGES'` over `{ ...(severityGated ?? {}) }`, and `severityGated` is null when the synthesis seat dies — so a dead seat plus a fully-excused red carried a verdict past `synthesisOrInfraBlock` (which returns anything with a usable verdict untouched) and landed as an ordinary rejection: no lane finding, no infra retry, no seat named. The verdict is now forced only over a seat that spoke; the dead-seat arm is verdict-less and fail-closed, and still carries the CI advisories onto the infra block.
2. `'advisory-only'` asserts a healthy panel judged the code, and was emissible when none had: `withSuiteBlocker`'s reason-less arm (the panel stated NO finding of its own — the only entry is one this file prepended) is `'infra-only'` again, which is what the salvage commit 17c72201 shipped and what the terminal writer's REVIEW_NOT_RUN honestly reflects; and `classifyBlock` now takes "no seat ran" as an argument, so a result whose `reviewRecord` reads 'NO REVIEW RAN' can no longer carry 'advisory-only' in the same object. The healthy-panel arm of `classifyBlock` is untouched.
3. Guards: `__tests__/synthesis-unavailable.test.ts` no longer hand-copies the `withCi` arm — it slices the real one out of the source and runs it; `__tests__/ci-gate.test.ts`'s excused-red block runs the arm instead of grepping it for the verdict string; `inner-loop-sim.ts`'s terminal writer uses production's exact discriminator, driven by a new simulator-based orchestrator test.

MUTATION GATE (round 6). FIND `if (!priorRed.has(f.name)) {` -> `if (true) {` in `trident/inner-workflow.mjs`: guard `trident/__tests__/ci-gate.test.ts` 159 pass/0 fail -> 151 pass/8 fail; control `trident/__tests__/severity-gate.test.ts` stays 18 pass/0 fail. Applied and reverted in this worktree; tree verified clean before the commit.

TARGET SET (round 6): `bun test trident/__tests__/ci-gate.test.ts trident/inner-workflow.test.ts trident/lane-retry.test.ts trident/__tests__/severity-gate.test.ts trident/__tests__/synthesis-unavailable.test.ts trident/__tests__/dead-core-seat-e2e.test.ts` = 416 pass / 0 fail / 1414 expect(). The card's 381/1283 is the pre-fix-round count and has moved with every round's added guards. `bun test trident/` = 3010 pass / 0 fail across 104 files; `bun x tsc -p trident/tsconfig.json` exits 0.

FIX ROUND 7 (Argus round-6). ONE confirmed blocker, and it is transmission rather than code: the dispatched nomination STILL carried control `trident/ascii-trim.test.ts`, a file that has never existed here, so the control is red before any mutation is applied and the prover reads that as NOT PROVED. This round re-verified the whole mutation cycle in this worktree and reports control `trident/__tests__/severity-gate.test.ts`. NO production code changed this round, deliberately: PR #499 is OPEN / MERGEABLE / CLEAN with every check green at this head, every remaining confirmed finding is fail-closed (the verdict is held at REQUEST_CHANGES on any red PR, so nothing merges through them) and was rated major-not-blocker, and rounds 5 and 6 each had to fix a blocker the previous CODE round introduced. The declined findings, each with its reason — including the dead-seat CI arm, whose forcing half is a documented and behaviourally tested choice (`synthesisOrInfraBlock`'s comment plus 'an unexcused red still forces the verdict, dead seat or live'), and whose malformed-seat half wants `usableStatus` plus an `Array.isArray` guard AND a widening of the sliced-arm harnesses — are recorded in `IMPLEMENTATION_PLAN.md`, FIX ROUND 7. Doc drift corrected there too: the round count in the lead note, and `review-round-cap.test.ts` missing from the file inventory.

MUTATION GATE (round 7, re-verified at this head). FIND `if (!priorRed.has(f.name)) {` -> `if (true) {` in `trident/inner-workflow.mjs` (one occurrence in that FILE — the string is also quoted in the two plan docs, so the uniqueness claim is file-scoped, which is the only scope the prover counts). Guard `trident/__tests__/ci-gate.test.ts`; control `trident/__tests__/severity-gate.test.ts`. Measured this round: guard 159 pass / 0 fail -> 151 pass / 8 fail; control 18 pass / 0 fail both with and without the mutation. Applied and reverted in this worktree; tree verified clean before the commit.

TARGET SET (round 7): the six-file set = 416 pass / 0 fail / 1414 expect(). The full stage-2 suite executed all 1409 files and is red in 5 lanes for pre-existing reasons — 41 failures across 14 files, none under `trident/`. Proven, not transcribed: those 14 files re-run file-scoped at HEAD (22 fail) and at base bee6dfd9 in the SAME worktree (23 fail); the single differing line is a failure the BASE has and this head does not.

FIX ROUND 8 (Argus round-7). ONE BLOCKER, and this one is CODE, not transmission: a findings-free `REQUEST_CHANGES` was being laundered into a recorded `advisory-only` REVIEWER verdict by the workflow's own CI advisories. The CI seam prepends this file's advisories to the seat's reply BEFORE `classifyBlock` reads it, so a rejection carrying no stated reason was no longer an "empty" list, skipped this file's malformed-rejection arm, and came out as a kind that asserts the panel judged the code — which `writeTerminalResult` records as a durable REQUEST_CHANGES. FIXED: `classifyBlock` takes `panelRejectedWithoutReason` and answers `'infra-only'` for it, measured at the caller from `severityGated` (the last place the seat's own reply is separable from the merged one). It is the exact mirror of the arm `withSuiteBlocker` already runs at the other injection seam, and it downgrades nothing: the verdict stays REQUEST_CHANGES and a red PR still holds the merge. Guarded by three EXECUTED tests in `trident/__tests__/ci-gate.test.ts` that slice the real `withCi` arm AND the real tail out of the source (the old fixture asserted only the verdict, which was Argus's point); `__tests__/synthesis-unavailable.test.ts`'s hand-rebuilt tail passes the same argument so it cannot drift. `SPEC.md:832` corrected to name both loop-exit kinds. Every other finding Argus raised is DECLINED with its reason recorded in `IMPLEMENTATION_PLAN.md`, FIX ROUND 8 — all of them UNVERIFIED single-reviewer minors/nits or, in the red->green resume case, new liveness behaviour that is fail-shut today.

MUTATION GATE (round 8, re-verified at this head). FIND `if (!priorRed.has(f.name)) {` -> `if (true) {` in `trident/inner-workflow.mjs` (one occurrence in that file). Guard `trident/__tests__/ci-gate.test.ts`; control `trident/__tests__/severity-gate.test.ts` — never `trident/ascii-trim.test.ts`, which does not exist here.

FIX ROUND 9 (run 4f28c9e0's round-4 review — the finding the 90-minute watchdog reaped before its verdict could be recorded; preserved by the relaunch dispatch). ONE BLOCKER, reproduced by the reviewer against the real resume harness: an advisory-only checkpoint was permanently unrecoverable on an unchanged head. The resume seam reconstructed all-non-blocking recorded findings as a paid review with blockKind 'advisory-only' (inner-workflow.mjs:6978), `runReviewRound` returned that paid review immediately — no CI probe, no reviewer (the `paidReview !== null` early return) — and the fix loop excludes 'advisory-only' (:7031), so once a round terminated advisory-only, a rerun on the same head replayed the terminal hold forever and could never observe that CI had gone green. Strictly worse than the round-buying 17c72201 prevented. FIXED at the resume seam alone: the paid-review shortcut is now reserved for recorded CODE work (`resumeHasCodeWork`, hoisted, same predicate as `classifyBlock`); a recorded hold whose findings are all non-blocking falls through to the REAL round-1 review path — CI re-probed, panel re-run, checkpoint re-recorded — so a red-that-became-green reaches a verdict. The loop's 'advisory-only' exit is untouched: no Forge fix round runs unless the FRESH round states code work, so the round economy 17c72201 bought is intact. Guarded by two EXECUTED tests in `trident/inner-workflow-resume.test.ts`, both proven RED against the unfixed workflow before the fix was applied: a same-head resume of an advisory-only hold re-reviews and reaches APPROVE when the world went green, and hands FRESH blocker findings (never the stale recorded advisories) to the next fix round when it did not; the source-pin test in `trident/lane-retry.test.ts` is updated to the new wiring and defers the behaviour to that executed pair. Doc drift from round 8 corrected: both plan headers were bumped to name every recorded round (round 9 at the time; round 10 bumped them again). Full record: `IMPLEMENTATION_PLAN.md`, FIX ROUND 9.

MUTATION GATE (round 9, re-verify at this head before reporting). FIND `if (!priorRed.has(f.name)) {` -> `if (true) {` in `trident/inner-workflow.mjs` (must occur exactly once in that FILE — the string is also quoted in the two plan docs, so the uniqueness claim is file-scoped, the only scope the prover counts). Guard `trident/__tests__/ci-gate.test.ts` (must go red under the mutation, green without); control `trident/__tests__/severity-gate.test.ts` (must stay green both ways) — NEVER `trident/ascii-trim.test.ts`, which has never existed in this repo and which the prover reads as a control that failed to stay green.

- [x] FIX ROUND 9 — break the advisory-only resume livelock: in `trident/inner-workflow.mjs` reserve the resume paid-review shortcut for recorded CODE work and route a same-head resume of an all-non-blocking recorded REQUEST_CHANGES into the real round-1 review path (CI re-probed, panel re-run) instead of fabricating the recorded 'advisory-only' result; add the two behavioural same-head red-to-green resume tests to `trident/inner-workflow-resume.test.ts` (both proven RED against the unfixed workflow first); update the source-pin test in `trident/lane-retry.test.ts`; bump both plan headers to name FIX ROUND 2 through FIX ROUND 9 and append this round's record to `IMPLEMENTATION_PLAN.md`; re-run the target set + `bun test trident/` + `bun x tsc -p trident/tsconfig.json`, prove any full-suite red pre-existing at bee6dfd9, and re-verify the mutation nomination (control `trident/__tests__/severity-gate.test.ts`).

FIX ROUND 10 (Argus round-9). ONE BLOCKER: round 9's resume gate claimed parity with `classifyBlock` and had only half of it — it asked `isNonBlockingFinding` while the classifier ALSO drops `kind: 'lane'` first, so a persisted lane blocker (what a dead review seat records, and what the orchestrator's infra-only auto-retry replays) asserted recorded CODE work at resume and bought a Forge round to fix a dead seat with code. FIXED by making it one function: `isCodeWorkFinding` carries both exclusions, `classifyBlock` and the resume gate both call it, and the four source-lifting harnesses lift it instead of restating it. A lane-only recorded hold now falls through to the same round-1 re-review as an advisory-only one — a dead seat is a stale measurement — and no round is bought if it is still down. Guarded BEHAVIOURALLY in `trident/inner-workflow-resume.test.ts` (a recorded lane blocker on an unchanged head dispatches NO `forge:` label, runs the panel once, reaches APPROVE on the recorded head) plus an executed predicate test in `trident/lane-retry.test.ts`; both proven red against the unfixed seam first. ALSO TIDIED at the same seam, per Argus: the fix-mode fall-through dropped the recorded SUITE advisory, so `fullSuiteFindings` now stamps `kind: 'suite'` and `resumeSuiteFindings` carries exactly those entries (never the stale CI advisory, which the fall-through re-measures). Two findings declined with reasons in `IMPLEMENTATION_PLAN.md`, FIX ROUND 10.

MUTATION GATE (round 10, re-verify at this head before reporting). FIND `if (!priorRed.has(f.name)) {` -> `if (true) {` in `trident/inner-workflow.mjs` (exactly once in that FILE). Guard `trident/__tests__/ci-gate.test.ts`; control `trident/__tests__/severity-gate.test.ts` — NEVER `trident/ascii-trim.test.ts`, which has never existed in this repo.

- [x] FIX ROUND 10 — share ONE code-work predicate between `classifyBlock` and the resume gate so a recorded lane blocker cannot buy a Forge round; carry the recorded suite advisory through the fix-mode fall-through by `kind`; add the behavioural lane-resume and suite-advisory tests (proven red first); re-run the target set + `bun test trident/` + `tsc`, prove any full-suite red pre-existing at bee6dfd9, and re-verify the mutation nomination.

FIX ROUND 11 (Argus round-10). ONE CONFIRMED MAJOR that round 10 itself introduced, plus the cheap hardening Argus asked to be folded in at the same seam, plus the as-built line both plan copies had wrong.

1. `kind: 'suite'` WAS A WORKFLOW MARKER NOTHING STRIPPED (major, confirmed by two reviewers, reproduced through the real workflow body). Round 10 stamped the full-suite gate's own findings with `kind: SUITE_FINDING_KIND` so `resumeSuiteFindings` could find them again inside a panel's recorded list — but `stripAdvisoryMarkers` tested only `Object.hasOwn(f, 'advisory') || f.kind === LANE_FINDING_KIND`. The suite finding is quoted VERBATIM into the synthesis prompt, so a model that copied the kind back would have had model-authored text recorded on an `argus-request-changes-round-N` row, carried across a resume, and re-presented to every core reviewer under the workflow's own header (`FULL-SUITE GATE FINDINGS (generated by the workflow…)`). That is the identical laundering the file's own docblock forbids and that FIX ROUND 4 rated MAJOR for `kind: 'lane'`. FIXED in the two places the other two markers are already handled: `marked` now recognises `SUITE_FINDING_KIND`, and the delete arm drops it — the gate's OWN finding is built in this file and never round-trips a model, so it keeps the kind the resume reads it by. Fail-closed in the same direction: a stripped finding can only block harder.

2. PARITY FOR WHAT THAT PROMPT QUOTES (same finding's second half). `suiteFindingsPrompt` interpolated the build's `suiteEvidence` transcription into every core reviewer's prompt raw — no `redactProbeText`, no cap — while its sibling `ciFindingsPrompt` had both. A base-comparison log tail is exactly where an echoed remote URL or token surfaces. FIXED: the same `redactProbeText` and a `SUITE_FINDING_PROMPT_MAX` cap. The cap is 6000, not the CI block's 2000, and deliberately so — `fullSuiteFindings` already clamps the build's transcription to 4000 characters inside ~1000 characters of the gate's own framing, and a 2000-character cap would have deleted the base-branch comparison the finding exists to have verified. The test pins that: the widest finding the gate can compose still fits under the cap.

3. ONE MARKER, ONE QUESTION (minor, downgraded from a Verdict-C blocker and folded in here because it is one line at the same seam). `isNonBlockingFinding` read `f['advisory'] === true` through the PROTOTYPE CHAIN while `stripAdvisoryMarkers` asked `Object.hasOwn` — so an object whose prototype carried the marker read as non-blocking and had nothing for the strip to remove. Argus's own verification says no live seam produces that object (every finding reaching these gates is a `JSON.parse` product, and `JSON.parse` cannot set a prototype), so this pins a shape rather than closing a live hole; the cost of the asymmetry is that the two readers of one marker disagree about what "has the marker" means, which is the drift the shared predicate exists to prevent. FIXED: `Object.hasOwn(f, ADVISORY_FINDING_KEY) && f[ADVISORY_FINDING_KEY] === true`.

4. AS-BUILT DRIFT (minor, raised by A). Both plan headers described the advisory-only stop as `blockKind 'infra-only'`, which FIX ROUND 6 stopped being true when it introduced the third kind. Corrected in both copies, with the reason, rather than deleted.

GUARDS — four tests in `trident/inner-workflow.test.ts`, three of them PROVEN RED against the unfixed `inner-workflow.mjs` before the fix was applied (148 pass / 3 fail unfixed; 151 pass / 0 fail fixed): a model-supplied `kind: 'suite'` is stripped and then dropped by the resume carry-through predicate spelled exactly as the source spells it; the gate's own finding keeps that kind; an INHERITED advisory marker leaves a blocker blocking (with the own-property positive control beside it); and the suite prompt block goes through `redactProbeText` with a cap wide enough for the gate's widest composed finding.

DECLINED THIS ROUND, with reasons.
- "Suite-advisory carry-through is inoperative for checkpoint rows written before 0769478f" (minor, single reviewer, unverified). True and BOUNDED: only rows written before the kind existed, and the entry is non-blocking, so no merge decision changes. The proposed title fallback would re-introduce exactly the contract `SUITE_FINDING_KIND`'s docblock exists to remove — two sites agreeing on a message format that nothing enforces — to buy back a transitional case that ages out with the next checkpoint. Not worth the seam.
- "The advisory-only-resume tests never observe a CI probe" (minor, recorded not blocking). Accurate: those tests run `mergeMode:'local'`, where `probeCi` returns `{status:'none'}` by construction. Argus verified the behaviour holds structurally (`paidReview=null` bypasses the ONLY short-circuit that skips the probe) and verified the cost of the PR-mode alternative (a 30s readiness deferral per attempt). The re-probe's red-to-green consequence is already asserted behaviourally by the round-9 pair; adding a minutes-long PR-mode fixture to re-assert the same thing through a slower path is not a trade this suite should make. — OVERTURNED BY ROUND 12. Argus round 11 re-raised it as a CONFIRMED BLOCKER (3/3 panelists, minority-veto applied) and that ruling stands over this one: a test whose NAME asserts a red-to-green CI re-probe, in a mode where CI cannot exist, measures nothing. The predicted cost was also wrong — the readiness gate defers only on a check that has not SETTLED, and a finished check (red or green) is ready, so the PR-mode pair below adds no wait at all. Fixed in FIX ROUND 12.
- The full-suite red. Pre-existing at bee6dfd9, re-proved this round; see the suite record below.

MUTATION GATE (round 11, re-verified at this head). FIND `if (!priorRed.has(f.name)) {` -> `if (true) {` in `trident/inner-workflow.mjs` (exactly once in that FILE — the string is also quoted in the two plan docs, so the uniqueness claim is file-scoped, which is the only scope the prover counts). Guard `trident/__tests__/ci-gate.test.ts`; control `trident/__tests__/severity-gate.test.ts` — NEVER `trident/ascii-trim.test.ts`, which has never existed in this repo and which the prover reads as a control that failed to stay green.

- [x] FIX ROUND 11 — strip `kind: 'suite'` in `stripAdvisoryMarkers` so a model cannot ride a resume as the full-suite gate's own measurement; redact and cap `suiteFindingsPrompt` to parity with `ciFindingsPrompt`; make `isNonBlockingFinding` ask `Object.hasOwn` like the strip does; correct the `blockKind` line in both plan headers; add the four guards to `trident/inner-workflow.test.ts` (three proven red first); re-run the target set + `bun test trident/` + `bun x tsc -p trident/tsconfig.json`, prove any full-suite red pre-existing at bee6dfd9, and re-verify the mutation nomination (control `trident/__tests__/severity-gate.test.ts`).

SUITE RECORD (round 11, measured in this worktree after a real `bun install` — 2503 packages; without it bun resolves node_modules upward to the main checkout and @neutronai/migrations is a different tree). Full stage-2 suite (`NEUTRON_TEST_JOBS=4 NEUTRON_TEST_CONCURRENCY=4 bun run test`) completed: declared 1409 / executed 1409 files, `run-tests: FAIL — 5/16 lane(s) contained failing tests`, 41 failing tests across 14 files, NONE under `trident/`. BASE COMPARISON PERFORMED, not transcribed: those 14 files re-run file-scoped at this head = 143 pass / 22 fail / 347 expect() (165 tests); then `git stash push`, `git switch --detach bee6dfd9` (the merge-base) in the SAME worktree with the SAME node_modules and the identical invocation = 142 pass / 23 fail / 346 expect(). Sorted failure-name lists with timings stripped: 22 at head, 23 at base, and the head list is a strict SUBSET of the base list (the one differing line, `buildGBrainMemory > per-connect key coherence…`, fails at BASE and not at head). The branch adds no failure. Under `trident/` at this head: `bun test trident/` = 3022 pass / 0 fail / 11225 expect() across 104 files; the target set (ci-gate, inner-workflow, lane-retry, severity-gate, synthesis-unavailable, dead-core-seat-e2e, inner-workflow-resume) = 467 pass / 0 fail / 1701 expect(); `bun x tsc -p trident/tsconfig.json` exits 0.

FIX ROUND 12 (Argus round-11). ONE CONFIRMED BLOCKER (3/3 panelists, minority-veto applied): the red-to-green CI RE-PROBE was asserted in `mergeMode: 'local'`, where `probeCi` early-returns `{status:'none'}` without a seat — so the test named for that property executed it nowhere, and round 11's decision to decline the same finding rested on a cost (a readiness deferral per attempt) that does not occur, because the gate waits only on a check that has not SETTLED. FIXED IN THE TEST, not the code: `runResume` now takes `pr: true` (mergeMode 'pr' + PR #7) and scripts the PR-mode probe seats, and two EXECUTED tests run in sequence — CI still red at the PR and at the base (both probes run; the panel approves; the red holds the merge as `advisory-only`; NO `forge:` round; the advisory is recorded on the checkpoint row) and then the SAME recorded advisory on the SAME unchanged head with CI GREEN (`ci-probe-round-2` dispatched — impossible in the replayed shape — the panel runs once, and the run reaches APPROVE). Both proven RED against the pre-round-9 seam first. The green case also pins the stale-advisory filter behaviourally: with the suite gate armed, the recorded `CI RED FOR PRE-EXISTING REASONS` title reaches neither `argus:claude` nor `argus:synthesis`, and dropping the `kind` filter turns exactly that test red. Two minors and two nits fixed with it (the `infraTerminalCause` comment in `trident/inner-workflow.test.ts`, the `SUITE_FINDING_PROMPT_MAX` absolute claim, and the file inventory + round header in both plan copies); one nit declined with its reason. Full record: `IMPLEMENTATION_PLAN.md`, FIX ROUND 12.

- [x] FIX ROUND 12 — add the PR-mode resume harness and the two executed red-to-green boundary tests (proven red first), pin the stale-CI-advisory filter behaviourally, correct the `infraTerminalCause` comment, correct the inventory and header in both plan copies, soften the `SUITE_FINDING_PROMPT_MAX` claim, and re-run the target set + `bun test trident/` + `tsc` with any full-suite red proved pre-existing at bee6dfd9.
