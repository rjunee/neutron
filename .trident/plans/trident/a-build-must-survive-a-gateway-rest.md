# IMPLEMENTATION_PLAN — A build must survive a gateway restart

Regenerated 2026-08-17 (planner: Fable) against main `47144a2a`, with a read-only forensic read of the production DB for the 2026-08-16 23:21:01 incident. MEASURED CORRECTION to the card's T1 framing: the deployed recovery (#312 probe latch + #329/#267 §1a-crash claim/relaunch, wired at `gateway/composition/build-core-modules.ts:658`) DID run — lanes on the dead generations `a310c6fe…`/`79fba6c9…` were atomically claimed (`crash_recoveries=1`) and relaunched as continuations on the new generation `f71ace47…` within ~6 min, and the same path worked again at the 02:02:35 latch. What failed is ADOPTION (T3): the relaunch was blind to finished work — runs `dd2061df` (PR #336) and `48cb602e` (PR #337, checkpoint `forge-done`) were relaunched although their PRs were green, and the owner cancelled both by hand and merged the PRs as-is. Secondary measured failure: at 01:01:29 seven claimed runs went terminal with the unmeasured reason "fire turn raised an error before settling", discarding open PRs #335/#343/#332/#346/#518 rather than adopting them.

GIT TRUTH: branch `trident/a-build-must-survive-a-gateway-rest` in the repo of record already carries commit `2ace3511` ("trident: adopt merged PRs during crash recovery"), rebased onto current main. Per this card's own T3 discipline, that commit is ADOPTED, never rewritten.

- [x] #312 probe semantics — positive-dead-only; `alive`/`unknown` do nothing (`trident/tick.ts` `livenessBody`; `trident/liveness-death-e2e.test.ts`). FROZEN — no task below may touch it.
- [x] Launcher death → durable latch → atomic claim → bounded relaunch continuation (#267/#329): `step()` §1a-crash (`trident/orchestrator.ts`) + `TridentRunStore.beginCrashRecovery` (`trident/store.ts:601`) + durable `crash_recoveries` budget (cap 3), production-wired at `gateway/composition/build-core-modules.ts:658`. Measured working in both the 23:21 and 02:02 incidents.
- [x] T2 — resume-from-checkpoint on the new generation: `launch()` folds `inner_checkpoint`/`inner_checkpoint_head`/`inner_checkpoint_findings` + `resume_live_head` + open-PR fold (`detectExistingPr`).
- [x] Harvest-first ordering — a workflow that wrote its terminal result and only then lost its launcher harvests, never re-fires (`trident/crash-recovery.test.ts`).
- [x] T3 — MERGED-PR ADOPTION AT RECOVERY: `detectMergedPr` probes (read-only, no-evidence-never-adopts) before relaunch; merged → claim then terminal `done` adopting the PR, zero fires, `harvested_at` untouched. Built as commit `2ace3511` on this card's branch with tests in `trident/crash-recovery.test.ts` — ADOPT this commit into the workspace if absent; do not rewrite it.
- [x] **Close the launch-throw zombie wedge (TOP TASK).** An uncaught throw inside `launch()` (first candidate: `resolveBase` at its first line) escapes `step()` to the tick loop's log-only per-run catch (`trident/tick.ts` `advance_failed`) and is retried silently every tick — after a `beginCrashRecovery` claim this is the one remaining TRUE zombie: non-terminal, no process, invisible. Bound it in `step()` at both `launch()` call sites: visible bounded retry (in-memory, 3 attempts) then terminal `failedRun` carrying the LAST thrown text, with branch/checkpoint columns preserved. Reason must never contain the token `exhausted` (`trident/delivery.ts` classifies that token into the review-unresolved class — a confident lie).
- [ ] T1 — zombie-impossibility assertions. Pin the defined behaviour of a latched-dead non-terminal run at both board gates: `work_board_start` answers 409 `already_running` (`gateway/http/work-board-surface.ts:389` / `trident/work-board-build-tool.ts:306`) and complete refuses via `WorkBoardRunStillLiveError` (`work-board/store.ts`) — and extend `trident/liveness-death-e2e.test.ts` with the sweep guarantee: after the latch + N sweeps the run is NEVER still latched; it is either running on a new generation or terminal (bounded-transient, not a wedge). Honest only once the top task lands — sequence after it.
- [ ] Recovery visibility. `beginCrashRecovery` leaves the dead generation's crash text in `failure_reason` on a now-RUNNING recovered row (measured live: runs `707b93d2`/`68b1026b`/`7d8bc997` running at 02:04–02:06 still carrying the 02:02:35 latch sentence). Clear or re-label it at claim time — e.g. `recovered from launcher death (attempt N of 3)` — so the board never shows a crash sentence beside `running`.
