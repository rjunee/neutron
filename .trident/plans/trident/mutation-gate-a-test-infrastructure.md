# IMPLEMENTATION_PLAN — mutation gate: a test-infrastructure diff gets a legal outcome (PR #490)

Card: the mutation-proof gate had no reachable outcome for a diff whose only changed .ts files are declared tests — PR #489 (APPROVED, all checks green) could never merge because every changed file was rejected as a mutation target by the path-based test-file rule while the gate still demanded a proof, and the refusal blamed the build for an omission it could not avoid. Fix, all on this branch: classify targets by what DECLARES a test rather than a path prefix; state the anti-tautology rule directly (a guard may not run the mutated file as its own test); give the no-production-file diff an explicit recorded exemption distinct from prose-only; split the required-but-missing refusal into "a legal target existed and none was named" vs "no legal target existed", naming a disqualified file.

State (2026-09-02, re-verified live at the BRANCH TIP each round rather than pinned to a sha — the previous pin, 9edd9554, was two commits stale by the time it was read): PR #490 OPEN / MERGEABLE / CLEAN, CI 17 of 17 SUCCESS (CodeQL green since cfc38542). Round 6 returned APPROVE with zero blockers and the merge was refused only for a missing nomination; round 7 emitted the claim and closed the npm/pnpm/yarn positional-forwarding hole; round 8 closes the same hole under `make` (below). The inner workflow captures the claim from the FIRST forge output of a round (a fix pass overwrites it only when truthy), so the claim must come out of forge, on the first pass, every round.

- [x] D1 — declared-test classifier in trident/mutation-prover.ts: basename `\.(test|spec)\.(?:[cm][jt]s|[jt]sx?)$` / `_test\.(go|py)$` and direct `__tests__/` children are tests (rejected as before); other files under a top-level `tests/`/`test/` are support libraries and LEGAL targets; documentation rejection unchanged.
- [x] D2 — anti-tautology check: a claim whose guard argv reaches `claim.file` is refused, with the refusal naming the tautology (every operand the runner reads is considered, not only recognised selectors).
- [x] D3 — explicit exemption when git says no production file changed, with its OWN reason string distinct from the prose-only one, recorded in the run record rather than silent.
- [x] D4 — the refusal distinguishes "the build nominated no mutation (a legal target existed)" from "no legal target existed", naming at least one considered-and-disqualified file.
- [x] Tests — red-then-green proof against a `tests/` support library with an unrelated control; classification table over the card's paths; tautology refusal; exempt reason is NOT the prose-only string; the load-bearing negative (a production file in the diff plus no claim is still REFUSED); positive controls against silently-empty extractions.
- [x] Wiring — trident/orchestrator.ts, trident/inner-workflow.mjs, trident/inner-loop-sim.ts carry the new outcomes; four production files changed on this branch.
- [x] CodeQL ReDoS on the loadable-scheme regex fixed (cfc38542), confirmed green by GitHub.
- [x] Round-7 BLOCKER (Argus, reproduced on npm 10.9.8): a package-script runner FORWARDS a bare positional to the script's own command line without `--`, so a branch-authored `"test-all": "bun test #"` swallowed the lexically-checked selector and turned a "targeted" guard into whole-suite discovery that ran the mutated file as its own test. `pathArgs` and `whyNoSelection` now drop every positional for npm/pnpm/yarn — the same reasoning the `--` arm already applied, applied to the spelling with no separator — and the test that pinned that shape as LEGAL now pins it as refused, with a bun-spelled positive control beside it.
- [x] Round-8 BLOCKER (Argus, reproduced against the round-7 head): the wrapper hole was closed for npm/pnpm/yarn and left OPEN under `make`. With a branch-authored `Makefile` whose `test-all` recipe is `bun test`, the guard `make test-all <a real test path>` came back `proved: true` while the same repo refused `make test-all`, `npm run test-all <path>` and `bun test`. Make does not forward the positional into the recipe — it reads it as a second GOAL, so the recipe's whole-suite discovery runs and the named file, which is on disk, is reported up to date. `forwardsPositionalsToAScript` now covers `make` too, `whyNoSelection` gains a make-shaped sentence (a second GOAL, and the `Makefile` this gate cannot read — not a package.json the argv never mentioned), and the WRAPPER-OPACITY residual (a `package.json` script body or a `Makefile` recipe is a command line the branch wrote and the argv does not show — e.g. a script that preloads the mutated PRODUCTION file beside an unrelated test) is now written into the KNOWN RESIDUAL note beside bunfig preload / jest setupFiles / conftest.py. Bounding it lexically is what this gate can do; parsing package.json and every Makefile dialect is what it cannot. Regression tests: the unit loop pins the make spellings as refused with the make-shaped sentence and no package.json in it, and a real-git end-to-end test commits the Makefile, refuses the guard before anything is executed, and keeps a bun-spelled positive control that still proves red-then-green in the same repo.
- [x] Round-9 BLOCKER (confirmed by two reviewers on two routes, both reproduced end to end): the guard-tautology invariant was bypassable, so a forged `proved: true` was obtainable for a PRODUCTION target. Route 1, the SEPARATOR: `--preload ./src` was accepted while the identical `--preload=./src` was refused, because `carriedValue` reads only the `=`-joined and attached-short spellings and a space-separated value was one more positional to every arm. `carriedValueReaching` now pairs each operand with the bare option before it (`isBareOption`: starts with `-`, carries nothing itself, is not `--`) and reads it as that option's value; which options take a value is not knowable from argv, so this over-refuses `bun test --coverage src/` for a target under `src/` — it fails CLOSED and is spellable around by writing the path before the option. The refusal quotes both elements. Route 2, the WRAPPER: `npm run test:unit`, whose branch-authored body is `bun test --preload=./src/limit.ts tests/other.test.ts`, loads the mutated production module into a process running an unrelated test, and the argv says nothing about it. It is not closable by reading argv and would not be closable by parsing package.json and every Makefile dialect either, so it is closed by REFUSING the shape: a wrapper (npm/pnpm/yarn/make) is no longer a legal GUARD for any target — a collectible one already refused every wrapper, and the production half is now refused at the `aRunnerMayCollect` gate. The allowlist is untouched and a wrapper stays a legal CONTROL. The WRAPPER-OPACITY entry in the KNOWN RESIDUAL note is retired accordingly. Tests: unit loops for both routes with positive controls (an option value that is not a path, a directory that does not hold the target, `node --test x.js`, the recommended bun spelling, a wrapper as the control), plus one real-git/real-bun end-to-end test that commits the hostile package.json, refuses all three forging guards before anything executes, and still proves red-then-green in the same repo with `bun test tests/limit.test.ts`. Both arms were verified load-bearing: reverting either reddens exactly the new tests and nothing else.
- [x] Round-9 major (single reviewer, reproduced): the percent-decode inside `argvEscapesTheWorktree` was load-bearing and unpinned — deleting it left both prover suites green while `--import=%2E%2E/x/lib.ts` and `-r%2Fetc%2Fx.js` were accepted and RUN. A killing test now pins all three spellings as escapes, with a `%2D` positive control so it does not read as "refuse every `%`".
- [x] `TICK_NOTE_CEILING`'s docblock claimed a test pinned the cut and none did — every assertion held at 241. The tick-note test now asserts the truncated note EXACTLY, so the ceiling is pinned to 240.
- [x] EMIT THE MUTATION-CLAIM NOMINATION in the forge structured output, every round. The claim, legality re-measured at this round's tree: file `trident/mutation-prover.ts`; find `const search = searchesReaching(guard, target)` (occurs exactly once in that file — uniqueness is counted within claim.file alone, so copies of the string in this plan cost nothing); replace `const search = null`; guard `bun test trident/mutation-prover.test.ts` (the prover's own suite, re-measured at THIS round's tree: 132 pass / 1 fail under the break, 133 / 0 restored); control `bun test trident/git-mode.test.ts` (100 / 0 throughout; zero references to the prover). A round that returns mutationClaim null fails regardless of every other output.
- [x] Round-10 BLOCKER (Argus, independently reproduced end to end against the real prover): an INDIRECT LOAD HOOK forged a `proved: true` for a production target. The branch commits `tests/setup.ts` whose whole body is `import '../src/limit.ts'` and nominates guard `bun test --preload=./tests/setup.ts tests/other.test.ts`, where the guard file asserts only `1 + 1`; every arm compared the hook's VALUE against the target, saw a different file, and passed it — while bun loaded the mutated PRODUCTION module into the guard's own process, so the syntax-shaped mutation reddened the guard with nothing asserting the behaviour and the control stayed green because only the guard carried the hook (`ok: true, exempt: false, proved: true`, guard_mutated 1 / control_mutated 0 / guard_restored 0). The hook's file is branch-authored and this gate reads argv, never file bodies — the WRAPPER's opacity in another spelling, and it is closed the same way: `loadHookCarrying` refuses the SHAPE (`--preload`, `--require`/`-r`, `--import`, `--loader`, `--experimental-loader`, in the `=`, attached-short and space-separated spellings) whatever file it names, for EVERY target, since a preload loads a support library under `tests/` exactly as it loads `src/`. It is asked AFTER the arms that name the mutated file, so a hook that names the target keeps its more specific refusal, and BEFORE the collectible gate, because a preload is a load and not a discovery. The short letter is read narrowly — `-r` counts only when what it carries looks like a path, or go's `-run`/`-race` would lose every Go guard there is. Over-refusal, failing CLOSED, spellable around: name the runner and the separate test, which is what the hook would have run anyway. The KNOWN RESIDUAL note keeps its argv-invisible half (bunfig `[test].preload`, jest `setupFiles`, `conftest.py`) and now says its argv-visible half is refused; pytest's `-p plugin` is named as a deliberate residual (its letter collides with `go test -p 4`). Tests: the forging shape is pinned as REFUSED in six spellings including a collectible target, with the reason naming the opacity and nothing written or run, plus four positive controls (`--grep=` carrying a third file, `go test -run`, `go test -race`, a plain runner); the six positive controls that previously pinned a load hook as LEGAL now carry the same values under a non-hook option, so the seams they guard (extension completion, loader rewrite, resolution, the space separator) are exercised unchanged.
- [x] Round-10 major (Argus): FORGE_SCHEMA's guard description never stated the wrapper ban this branch introduced, so a build following the schema literally on a wrapper repo earned a post-review refusal it could not have foreseen. The description now names both opaque shapes — every wrapper `forwardsPositionalsToAScript` covers, and every load-hook option `LOAD_HOOK_OPTION` covers — and tells the build to spell the runner out instead. A new test in `inner-workflow-mutation-claim.test.ts` reads both lists OUT OF THE PROVER'S SOURCE and requires the description to name each one, so the two lists can only agree by being the same list; it reddens the moment a fifth wrapper or a sixth hook is added on one side only.

Deferred to follow-up cards (accepted at round 6; every one latent and failing closed): `node <script> --test` as a guard; fixtures, snapshots and golden files under `tests/` classifying `production` and so nominatable (a real red-then-green, merely an uninteresting one — whether the nominated mutation was the interesting one is Argus's call by design); the direct-`__tests__`-child rule declaring 13 support libraries tests; unittest `test*.py` and `tests/*.rs` classifying production yet un-nominatable; deletion-only production diffs unclearable; the sim's `{ok:false, exempt:true}` shape; the exempt stamp written before the merge attempt. The full-repo suite red is pre-existing and environmental: 142 pass / 23 fail with byte-identical failure line-sets at base and head after timing-stripping, none of it under trident/.
