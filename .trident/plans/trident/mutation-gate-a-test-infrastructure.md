# IMPLEMENTATION_PLAN — mutation gate: a test-infrastructure diff gets a legal outcome (PR #490)

Card: the mutation-proof gate had no reachable outcome for a diff whose only changed .ts files are declared tests — PR #489 (APPROVED, all checks green) could never merge because every changed file was rejected as a mutation target by the path-based test-file rule while the gate still demanded a proof, and the refusal blamed the build for an omission it could not avoid. Fix, all on this branch: classify targets by what DECLARES a test rather than a path prefix; state the anti-tautology rule directly (a guard may not run the mutated file as its own test); give the no-production-file diff an explicit recorded exemption distinct from prose-only; split the required-but-missing refusal into "a legal target existed and none was named" vs "no legal target existed", naming a disqualified file.

State (2026-09-02, re-verified live at THIS round's own head 41f1dd1f, not copied forward): PR #490 OPEN / MERGEABLE / CLEAN, CI 17 of 17 SUCCESS. Run da9b550b (round 6) returned verdict APPROVE, blockKind none, remainingTasks 0, reviewedHead 41f1dd1f — and mutationClaim: null, so the merge gate refused AGAIN. The cause is now MEASURED, and it is this card's own thesis one level up: NO AGENT-FACING PROMPT EVER ASKS FOR THE NOMINATION. In this branch's trident/inner-workflow.mjs, every occurrence of "mutation" sits in FORGE_SCHEMA's field and its descriptions or in comments; forgeBuildContract — the build's instructions, reused verbatim by every fix round, and handed to the codex executor too (forgeAgent gives both executors the SAME brief, the codex route merely appending a reporting coda) — contains ZERO mentions. The only thing the build ever sees is a schema field of type object-or-null, so null is a schema-valid answer to a question nothing ever asked. That explains every null on this card and its siblings, with the nomination written verbatim in the round briefs throughout. The inner workflow captures the claim from the FIRST forge output of a round (a fix pass overwrites it only when truthy), so the claim must come out of forge, on the first pass, every round; a fresh run starts with no standing claim, so a run whose forge:build returns null fails at the gate regardless of every other output. The seven-spelling forged-proof family remains closed exactly as recorded below.

THE PLAN LIVES AT THE REPO-ROOT IMPLEMENTATION_PLAN.md ONLY. The file at trident/IMPLEMENTATION_PLAN.md is NOT this card's: at this branch's head it is byte-identical to origin/main's copy (an EARLIER MERGED card's plan, "codex review: empty output must fail closed"), and zero commits on this branch touch that path — the round-12 "byte-identical copies" rule pointed at a file this branch never owned. Leave it alone: editing a foreign card's merged file would add an unrelated hunk to an approved diff.

- [x] D1 — declared-test classifier in trident/mutation-prover.ts: basename `\.(test|spec)\.(?:[cm][jt]s|[jt]sx?)$` / `_test\.(go|py)$` and direct `__tests__/` children are tests (rejected as before); other files under a top-level `tests/`/`test/` are support libraries and LEGAL targets; documentation rejection unchanged.
- [x] D2 — anti-tautology check: a claim whose guard argv reaches `claim.file` is refused, with the refusal naming the tautology (every operand the runner reads is considered, not only recognised selectors).
- [x] D3 — explicit exemption when git says no production file changed, with its OWN reason string distinct from the prose-only one, recorded in the run record rather than silent.
- [x] D4 — the refusal distinguishes "the build nominated no mutation (a legal target existed)" from "no legal target existed", naming at least one considered-and-disqualified file.
- [x] Tests — red-then-green proof against a `tests/` support library with an unrelated control; classification table over the card's paths; tautology refusal; exempt reason is NOT the prose-only string; the load-bearing negative (a production file in the diff plus no claim is still REFUSED); positive controls against silently-empty extractions.
- [x] Wiring — trident/orchestrator.ts, trident/inner-workflow.mjs, trident/inner-loop-sim.ts carry the new outcomes; four production files changed on this branch.
- [x] CodeQL ReDoS on the loadable-scheme regex fixed (cfc38542), confirmed green by GitHub.
- [x] Round-7 BLOCKER (Argus, reproduced on npm 10.9.8): a package-script runner FORWARDS a bare positional to the script's own command line without `--`, so a branch-authored `"test-all": "bun test #"` swallowed the lexically-checked selector and turned a "targeted" guard into whole-suite discovery that ran the mutated file as its own test. `pathArgs` and `whyNoSelection` now drop every positional for npm/pnpm/yarn — the same reasoning the `--` arm already applied, applied to the spelling with no separator — and the test that pinned that shape as LEGAL now pins it as refused, with a bun-spelled positive control beside it.
- [x] Round-8 BLOCKER (Argus, reproduced against the round-7 head): the wrapper hole was closed for npm/pnpm/yarn and left OPEN under `make`. With a branch-authored `Makefile` whose `test-all` recipe is `bun test`, the guard `make test-all <a real test path>` came back `proved: true` while the same repo refused `make test-all`, `npm run test-all <path>` and `bun test`. Make does not forward the positional into the recipe — it reads it as a second GOAL, so the recipe's whole-suite discovery runs and the named file, which is on disk, is reported up to date. `forwardsPositionalsToAScript` now covers `make` too, `whyNoSelection` gains a make-shaped sentence (a second GOAL, and the `Makefile` this gate cannot read — not a package.json the argv never mentioned), and the wrapper-opacity residual was written into the KNOWN RESIDUAL note (since retired by round 9's wrapper ban). Regression tests: the unit loop pins the make spellings as refused with the make-shaped sentence and no package.json in it, and a real-git end-to-end test commits the Makefile, refuses the guard before anything is executed, and keeps a bun-spelled positive control that still proves red-then-green in the same repo.
- [x] Round-9 BLOCKER (confirmed by two reviewers on two routes, both reproduced end to end): the guard-tautology invariant was bypassable, so a forged `proved: true` was obtainable for a PRODUCTION target. Route 1, the SEPARATOR: `--preload ./src` was accepted while the identical `--preload=./src` was refused, because `carriedValue` reads only the `=`-joined and attached-short spellings and a space-separated value was one more positional to every arm. `carriedValueReaching` now pairs each operand with the bare option before it (`isBareOption`: starts with `-`, carries nothing itself, is not `--`) and reads it as that option's value; which options take a value is not knowable from argv, so this over-refuses `bun test --coverage src/` for a target under `src/` — it fails CLOSED and is spellable around by writing the path before the option. The refusal quotes both elements. Route 2, the WRAPPER: `npm run test:unit`, whose branch-authored body is `bun test --preload=./src/limit.ts tests/other.test.ts`, loads the mutated production module into a process running an unrelated test, and the argv says nothing about it. It is not closable by reading argv and would not be closable by parsing package.json and every Makefile dialect either, so it is closed by REFUSING the shape: a wrapper (npm/pnpm/yarn/make) is no longer a legal GUARD for any target — a collectible one already refused every wrapper, and the production half is now refused at the `aRunnerMayCollect` gate. The allowlist is untouched and a wrapper stays a legal CONTROL. Tests: unit loops for both routes with positive controls, plus one real-git/real-bun end-to-end test that commits the hostile package.json, refuses all three forging guards before anything executes, and still proves red-then-green in the same repo with `bun test tests/limit.test.ts`. Both arms were verified load-bearing: reverting either reddens exactly the new tests and nothing else.
- [x] Round-9 major (single reviewer, reproduced): the percent-decode inside `argvEscapesTheWorktree` was load-bearing and unpinned — deleting it left both prover suites green while `--import=%2E%2E/x/lib.ts` and `-r%2Fetc%2Fx.js` were accepted and RUN. A killing test now pins all three spellings as escapes, with a `%2D` positive control so it does not read as "refuse every `%`".
- [x] `TICK_NOTE_CEILING`'s docblock claimed a test pinned the cut and none did — every assertion held at 241. The tick-note test now asserts the truncated note EXACTLY, so the ceiling is pinned to 240.
- [x] EMIT THE MUTATION-CLAIM NOMINATION in the forge structured output, every round. The claim, legality re-verified at each round's own head (at this round's head the find string sits at mutation-prover.ts:814 and occurs exactly once — uniqueness is counted within claim.file alone, so copies of the string in this plan cost nothing): file `trident/mutation-prover.ts`; find `const search = searchesReaching(guard, target)`; replace `const search = null`; guard `bun test trident/mutation-prover.test.ts` (measured 138 pass / 1 fail under the break at this round's head, 139/0 restored — re-measure at your tree); control `bun test trident/git-mode.test.ts` (green throughout; zero references to the prover). A round that returns mutationClaim null fails regardless of every other output.
- [x] Round-10 BLOCKER (Argus, independently reproduced end to end against the real prover): an INDIRECT LOAD HOOK forged a `proved: true` for a production target. The branch commits `tests/setup.ts` whose whole body is `import '../src/limit.ts'` and nominates guard `bun test --preload=./tests/setup.ts tests/other.test.ts`, where the guard file asserts only `1 + 1`; every arm compared the hook's VALUE against the target, saw a different file, and passed it — while bun loaded the mutated PRODUCTION module into the guard's own process. The hook's file is branch-authored and this gate reads argv, never file bodies — the WRAPPER's opacity in another spelling, closed the same way: `loadHookCarrying` refuses the SHAPE (`--preload`, `--require`/`-r`, `--import`, `--loader`, `--experimental-loader`, in the `=`, attached-short and space-separated spellings) whatever file it names, for EVERY target. Asked AFTER the arms that name the mutated file and BEFORE the collectible gate. The short letter is read narrowly — `-r` counts only when what it carries looks like a path. Over-refusal, failing CLOSED, spellable around: name the runner and the separate test. Tests: the forging shape pinned as REFUSED in six spellings including a collectible target, plus four positive controls; the six controls that previously pinned a load hook as LEGAL now carry the same values under a non-hook option.
- [x] Round-10 major (Argus): FORGE_SCHEMA's guard description never stated the wrapper ban this branch introduced, so a build following the schema literally on a wrapper repo earned a post-review refusal it could not have foreseen. The description now names both opaque shapes — every wrapper `forwardsPositionalsToAScript` covers, and every load-hook option `LOAD_HOOK_OPTION` covers — and tells the build to spell the runner out instead. A new test in `inner-workflow-mutation-claim.test.ts` reads both lists OUT OF THE PROVER'S SOURCE and requires the description to name each one, so the two lists can only agree by being the same list.
- [x] Round-11 BLOCKER (codex seat, the FIFTH escape of the same family): a REPORTER OPTION is a third opaque load hook. Node v22 loads an arbitrary module from `--test-reporter=<path>` (mocha and vitest from `--reporter`, jest from `--reporters`), so a branch can commit `tests/reporter.mjs` whose body imports the production target and nominate `node --test --test-reporter=./tests/reporter.mjs tests/other.test.mjs`: the reporter drags the mutated file into the guard's process, a syntax break reddens an unrelated guard, restoring goes green — the same false proof `loadHookCarrying` already refuses for preload/require/import/loader. Closed as a FAMILY, not one spelling: `test-reporter|reporter|reporters` joined `LOAD_HOOK_OPTION` (anchored, so `--test-reporter-destination` and `--reporter-outfile` stay legal — they name a file the runner WRITES, not one it loads); the FORGE_SCHEMA guard description names all three; the agreement test now pins hook-list CONTAINMENT of all eight names so a two-sided deletion reddens; six refused unit spellings (including `--test-reporter=spec` — telling a built-in name from a bare module specifier is a vocabulary bet, so the shape is refused whatever the value: over-refusing, failing CLOSED, spellable around by dropping the option, since the prover reads exit codes and never report output) sit beside a `--test-reporter-destination` positive control; and one real-git end-to-end test COMMITS a reporter module importing the target, refuses the guard before anything executes, and still proves red-then-green bun-spelled in the same repo. Mocha's short `-R` joined pytest's `-p` in the KNOWN RESIDUAL note rather than the regex — the short-letter narrowing stays untouched. Verified load-bearing: reverting only the regex edit reddens exactly the new unit rows, the new realgit refusal and the schema-agreement test, and nothing else.

- [x] Round-12 BLOCKER (two reviewers, same family as rounds 8-11): CONFIG SPELLINGS THAT ARE VISIBLE IN THE ARGV still forged `proved: true`, which falsified this branch's own contract text ("refused in every spelling that PUTS the config on the command line"). Three were open. Node's `--experimental-config-file=<file>` names a branch-authored `node.config.json` whose options carry the same `--import` the dotenv route carries; node's `--experimental-default-config-file` takes NO VALUE AT ALL (it reads `node.config.json` from the cwd), so `loadHookCarrying` — which fired only on a carried value or on a following non-option — could not see it however it was written; and bun's and jest's short `-c` is `--config` in two characters less, deferred a round earlier on the ground that `go test -c` spells it too. Closed on the shape, as the family always is: both node options joined `LOAD_HOOK_OPTION`; a LONG hook name is now refused on its NAME, value or none (the only arm that can see a valueless option, and costless because every other option here is malformed without its value); `-c` joined `-r` in the short-letter arm, where an ATTACHED value counts only when it looks like a path — `go test -c` is a valueless flag, so it can never wear that form, and the separated `go test -c ./pkg` is over-refused on purpose, failing closed and spellable around by dropping `-c` since the prover reads exit codes and never a compiled binary. FORGE_SCHEMA's guard description names all of it (a build must be able to foresee the refusal), and the agreement test pins containment of thirteen hook names plus the three spellings its name-loops structurally cannot see (`npm test`, `-c…`, `-r…`). Unit rows cover both node spellings attached and separated, the valueless one written LAST in the argv (the row only the name-only arm can refuse), and `-c` attached and separated; positive controls keep `go test -cover ./cmd/` and a trailing `go test ./cmd/ -c` legal and RUNNING. Both new arms verified load-bearing: neutralising the name-only arm, or dropping `-c` from the short letters, reddens exactly the new rows and nothing else.
- [x] Round-12 minors: three pre-existing rows that pinned the no-selection arm were re-spelled off `--reporter`, which the round-11 change now refuses on sight — they were passing for a reason unrelated to the operand they claim to be about (`--bail 3` and `--reporter-outfile`, which names a file the runner WRITES, replace them). The tick-note and stage-reason docblocks in trident/orchestrator.ts now say the ceiling is on the PREFIX KEPT and not on the result, which is what the pinning tests have always asserted.
- [x] Round-13 BLOCKER (two reviewers, two spellings, verified in source by the synthesizer): the closed wrapper/load-hook family was still open on the argv in two places, which falsified this branch's own contract text ("every spelling that puts such a load ON the argv is refused", "a wrapper is no longer a legal guard for any target"). (1) `node --run <script>` executes a package.json script — a command line the BRANCH wrote — and never enters test-runner mode, but the node shape asked only whether `--test` appeared ANYWHERE in the argv, so a trailing `--test` node forwards to the script satisfied it; `forwardsPositionalsToAScript` is keyed on `argv[0]` and node is a legal runner under its other spelling, so this is refused one step earlier, at the SHAPE (`isNodeRunOption`, both `--run x` and `--run=x`), where "a recognised program in a shape that is not its test subcommand" already belongs. (2) go carries a branch-written body on ONE dash: `-exec` (runs the compiled test binary through a branch program), `-toolexec` (runs one per compile step) and `-overlay` (a JSON map that REPLACES the contents of any source file, the mutated one included). `LOAD_HOOK_OPTION` is anchored `^--` and could never see them, and `SHORT_OPTION_WITH_ATTACHED_VALUE` read `-exec` as `-e` carrying `xec`, so all three were skipped; they get a second regex, `GO_TOOLCHAIN_HOOK_OPTION`, matched WHOLE and asked BEFORE the short-option reading so nothing else changes meaning — a valueless one is still the hook, exactly as a valueless long name is. FORGE_SCHEMA's guard description names all four spellings, and the agreement test reads the go names out of the prover's source, pins containment of the three, and pins `node --run` literally (no name-loop can see a shape refusal). Unit rows: three `node --run` spellings including `--test` written first, plus the CONTROL side; seven go rows across attached, separated and valueless; positive controls keep `node --test tests/other.test.mjs`, `go test -count=1`, `go test -execute-nothing=1` and plain `go test ./pkg/other` legal and RUNNING.
- [x] Round-13 minors: the carried-value arm's two refusal fragments carried their own object and composed into a garbled sentence with the caller's template ("loads it via -rsrc, which names the directory holding it the mutated file src/limit.ts as its own test") — the same wording-class bug already fixed on the wrapper arm; they now read "loads via <spelling>" and "loads, via <spelling> naming the directory holding it,". The same new test pins `carriedValueReaching` and `extensionCompletions` as load-bearing: a reviewer's mutant battery neutralised each and the whole trident suite stayed green, because the hook arm re-refuses the same spellings with a vaguer reason and the resolved-path seam catches the rest at proof time — the two reasons are now told apart by name, so the redundancy cannot be deleted silently.
- [x] Round-14 BLOCKERS (two reviewers, both reproduced end to end, both falsifying this branch's own contract text): (a) `--tsconfig-override` is a RESOLUTION MAP the argv names and `LOAD_HOOK_OPTION` did not list — with `compilerOptions.paths` of `{"mylib": ["../../src/limit.ts"]}` a bare `import 'mylib'` inside an otherwise unrelated test IS the mutated file, so on bun 1.3.13 the guard cannot even resolve the specifier without the flag, exits 0 healthy with it, exits 1 once the target is syntax-broken and exits 0 on restore — a full red-then-green with nothing asserting the mutated behaviour, driven to `proved: true` through the real prover. The option now sits in `LOAD_HOOK_OPTION` beside `--config`, the FORGE_SCHEMA guard description names it (the schema-agreement test reads the regex out of the prover, so the two lists cannot drift), and the docblock says why `--project`/`--tsconfig` are deliberately NOT added: a long hook name is refused on the name alone, and vitest's `--project <name>` selects a workspace project by name, so listing it on suspicion would refuse an honest guard. The argv-invisible half grew one entry in the KNOWN RESIDUAL note: a committed `tests/tsconfig.json` needs no flag at all, because bun walks up from the test file and applies its `paths`. Regression rows: both spellings for a production target, one for a collectible target, plus a positive control that `--project unit` stays legal and really runs. (b) `node <script> --test <unrelated>` passed the node SHAPE check, because it asked only whether `--test` appeared ANYWHERE. Node enters test-runner mode only when `--test` is one of its OWN options, before the entry script; written after one it is an ordinary argument node forwards to that BRANCH-AUTHORED script (verified on v22: the script prints `argv=["--test"]`), which is the `node --run` wrapper again without an option to name it. The shape now requires the leading spelling (`argv[1] === '--test'`), which is exactly what `runnerPrefixLength` already assumed, and the shape string says `--test FIRST` so the refusal tells the next nomination where the word goes. Over-refusal is the cost — `node --loader=… --test <path>` is honest and is now refused — it fails CLOSED, and node accepts the same options written after the mode word, so it costs nothing honest; the one pre-existing test row spelled that way was re-spelled. Regression tests: three wrapper spellings refused before anything is written or run with the refusal naming `--test FIRST`, the control held to the same shape, a positive control that `node --test --test-concurrency=1 <path>` still runs, and a literal pin in the schema-agreement test (no name-loop can see a shape rule) requiring both the schema sentence and the prover's predicate.
- [x] Round-15 BLOCKER (Argus, reproduced on node v22.23.1): `--conditions` is the resolution map with the FILE LEFT IMPLICIT, and it was unlisted. A condition-keyed `package.json` `imports` entry makes a bare `import '#lib'` in an unrelated test mean the MUTATED file only while `--conditions=proof` is on the argv (healthy 0, mutated 1, restored 0 with the flag; 0 while mutated without it), so the FLAG forges the red-then-green exactly as `--tsconfig-override`'s config does, and the prover failed OPEN. `conditions` joined `LOAD_HOOK_OPTION`; node's short `-C` is refused in `loadHookCarrying` OUTSIDE the path-filtered `-r`/`-c` pair, since a condition value is a NAME and that filter would wave `-Cproof` through. FORGE_SCHEMA names both spellings; the agreement test pins fifteen hook names plus `-C…`. Six refused unit rows with positive controls keeping `--conditions-file=…` and `go test -cover ./cmd` legal and RUNNING; both arms verified load-bearing. Minors: the KNOWN RESIDUAL docblock's spliced sentence states its own rule again, and names the attached bare-specifier `-rmylib`, which escapes the narrowed letter into that same committed-config class.
- [x] Round-16 — REQUIREMENT 5, the measured cause of every null claim: the forge contract must ASK for the nomination it gates merges on. In trident/inner-workflow.mjs forgeBuildContract: (a) the report step's field list gains mutationClaim (the unique anchor "diffFile, testsPassed" becomes "diffFile, testsPassed, mutationClaim (see MUTATION NOMINATION below)"); (b) a titled MUTATION NOMINATION block is appended inside the template after its last line (the unique "WORKTREE=<your worktree pwd>" line), stating: the post-APPROVE merge gate RUNS the one nominated mutation, and when the branch diff contains a legal target a run carrying no nomination CANNOT MERGE however green everything else is; what makes a target legal (a production file the branch diff changes — the mutationClaim.file schema description states the exact rule, and the guard description lists every refused opaque shape, so neither list is duplicated into a third copy that can drift); verify the find string occurs EXACTLY ONCE with grep -cF before reporting; and null is an answer ONLY when no legal target exists in the branch diff or when an earlier round's nomination of THIS run still holds (a truthy fix-round claim replaces the standing one, null leaves it standing; the first build pass of a run has nothing standing). One edit covers both executors, because forgeAgent hands codex the same brief plus a coda. Test, in trident/inner-workflow-mutation-claim.test.ts: Captured grows a prompt field; a new test filters the captured build prompts (forge:build plus every forge:fix-round-*), requires MORE THAN ONE captured prompt (the positive control against a silently-empty extraction), and pins each load-bearing sentence so deleting any one reddens. AND EMIT THE NOMINATION in this round's structured output, find-string re-verified at this round's own head (at 41f1dd1f: mutation-prover.ts:816, exactly once; this round's edits touch neither that file nor its tests, so it cannot move — verify anyway).

Deferred to follow-up cards (accepted at round 6; every one latent and failing closed): the `.trident` prose-directory question — a plan doc drives an AGENT, not the harness, and listing `.trident` beside `skills`/`prompts`/`.claude` would classify a plan-doc-only commit (a real shape here) as production with no legal target to nominate, which is this card's own defect re-created, so it is left off with the escape it leaves named in the code; OPAQUE SCRIPT RUNNERS (accepted at round 5 and dropped from this list by the round-12 rewrite — recorded again here, because a record that says a class is closed while it is open is worse than no record: the argv-visible spellings are refused one by one as the panel finds them, the family is not closed by construction) — but NOT `node <script> --test`, which round 14 closed at the shape and which is struck from this list; fixtures, snapshots and golden files under `tests/` classifying `production` and so nominatable (a real red-then-green, merely an uninteresting one — whether the nominated mutation was the interesting one is Argus's call by design); the direct-`__tests__`-child rule declaring 13 support libraries tests; unittest `test*.py` and `tests/*.rs` classifying production yet un-nominatable; deletion-only production diffs unclearable; the sim's `{ok:false, exempt:true}` shape; the exempt stamp written before the merge attempt; mocha's short `-R` reporter letter (recorded beside pytest's `-p`); and the codex build route's reporting coda, which REPLACES the schema report outright ("nothing reads a report from you" — the wrapper measures six repo facts and no trailer field carries a claim), so a codex-routed build still cannot nominate even once asked: latent, fails closed at the same gate, and belongs on a follow-up card beside the others. The full-repo suite red is pre-existing and environmental: 142 pass / 23 fail with byte-identical failure line-sets at base and head after timing-stripping, none of it under `trident/`.
