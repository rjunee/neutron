# IMPLEMENTATION_PLAN — mutation gate: a test-infrastructure diff has no legal nomination (branch trident/mutation-gate-a-test-infrastructure, PR #490)

Card: the mutation-proof gate had no reachable outcome for a test-infrastructure diff (PR #489's class). D1 — the path-based TEST_FILE classifier rejected support libraries under `tests/` that are legal red-then-green targets; D2 — a diff of only declared tests plus prose had no legal target and fell through to a refusal blaming the build. Build items: (1) classify by what DECLARES a test; (2) anti-tautology — refuse a claim whose guard argv reaches `claim.file`; (3) explicit exempt outcome with its OWN reason when git says no production file changed, recorded like the prose-only exemption; (4) refusals distinguish "legal target existed, none named" from "no legal target existed". All original Do-nots still bind: no weakening of required proofs; keep the documentation rejection and the claim-file-in-diff requirement; no `.github/workflows/` changes; no proof-token/HMAC/evidence-schema/TEST_COMMAND_SHAPES changes; do not fix #489 here; never use the word for a per-customer isolated instance — say "absolute host filesystem paths". KEEP THIS FILE COMPACT (<20 KB): an oversized plan doc makes the launcher brief exceed its settle budget and the card stops firing (measured on the sibling card #487).

State of record (this round, verified 2026-09-02): the branch is ONE commit on `bee6dfd9` (main) after a replay; head carries T1-T12's content. PR #490 OPEN, base main. The previous Argus round returned 14 findings, and the two that needed code both live in `trident/mutation-prover.ts`: (a) **CodeQL `js/polynomial-redos` HIGH, alert 64** — `LOADABLE_SCHEME`'s any-scheme alternative sat OUTSIDE the leading token boundary (`|` binds across the whole pattern), so a long run of scheme-characters with no `:/` was rescanned from every offset: measured on this box, 80k characters = ~7s with 4x per doubling, and argv arrives from an unbounded agent-authored nomination. Fixed by bringing both alternatives inside the one boundary — anchor it, do not weaken the refusal, the same shape that cleared this alert class on sibling card #487. (b) two reviewer-reproduced bypasses: `TEST_BASENAME`'s `[cm]?[jt]sx?` reads as eight extensions and admits TWELVE, so a behaviour-bearing `src/payments.test.cjsx` — a name bun does not collect — DECLARED a test and bought the no-production-file exemption; and node's package-imports alias (`--import=#target`) resolved through package.json to the mutated file while `normalizeArg` read the leading `#` as a fragment delimiter and reduced the specifier to `.`, so the tautology check never saw it. Also refiled correctly this round: the suite report (the previous round filed `testsPassed=true` beside `suiteOutcome=failed-preexisting`, which the gate reads as no proof at all) and this plan file, which shipped stale. Findings needing no code: the deletion-only-diff deadlock and the `node <script> --test` shape are PRE-EXISTING on main and out of scope by the card's Do-nots (follow-up cards, recorded below); the `--preload:path` colon-separated spelling is a defence-in-depth gap no allowlisted runner accepts; the reviewer diff-coverage observation is process.

- [x] T1 — Declaration-based classifier: `TEST_BASENAME` / direct-`__tests__/` membership; a support library under top-level `tests/`/`test/` is a LEGAL target; documentation rejection unchanged; classifier table test over the card's six paths. (Rounds 1-2, in PR #490.)
- [x] T2 — Anti-tautology guard analysis: refuse a guard that names `claim.file` (element, `=`-carried value, operand) or collects it (no-path discovery arm, directory arm); `argvEscapesTheWorktree` (absolute, `..`, URL-scheme spellings); symlink/realpath resolution seam. (Commits `8d2417e7`→`1e77a58b`.)
- [x] T3 — D2 exemption: exempt with its own reason string naming every exempting file, distinct from the prose-only string, recorded beside the prose-only exemption. (Landed.)
- [x] T4 — Distinguished refusals when a proof is required and no claim came. (Landed.)
- [x] T5 — Card test battery incl. the realgit red-then-green proof for a support library under `tests/`, the classifier table, THE LOAD-BEARING NEGATIVE (a diff still containing a production file and carrying no claim is REFUSED), positive controls. (Landed.)
- [x] T6/T7 — First review-round fix (`namesASearch`, two-filter `pathArgs`, attached short options) derived at a salvage tag and LANDED as `3b549ff5`.
- [x] T8 — Next round: resolution seam `guardSelectsNothingOnDisk`; docblock honesty. (Landed as `884ab48e`.)
- [x] T9 — Next round: lexical filter arm + operand drop; byte-exact `--name-status` diff reading; deletion exclusion from `legalMutationTargets` (deliberately NOT an exemption — the `git mv` rename pass). (Landed as `73533663`.)
- [x] T10 — Next round: `argumentOperands` for the reach arms; `modulePathsOf` dotted modules; `node --test` invocation; `--coverage src/`; numeric carried values; empty-vs-unreadable diff split; `truncateNote` tick-note cap. (Landed as `f1368154`.)
- [x] T11 — Next round: `SPECIFIER_SUFFIX` (query/fragment/`::` name the same file); numeric positionals are live filters; the no-selection refusal says why. (Landed as `6a166821`.)
- [x] T12 — ANSWER THE 12-FINDING ROUND (this iteration; see execution spec). Recover the salvage EXACTLY (`git checkout refs/tags/trident-salvage/025c31ff-7565-4d34-b0c2-e50368971e33 -- trident/mutation-prover.ts`; verify `git diff --stat` shows exactly +183/−24 on that one file). Add finding 10's durable record: in `trident/orchestrator.ts`, beside the existing `mutation_proof_exempt` log line, stamp the run's stage ledger best-effort via the existing `opts.record_stage` seam (`'mutation-proof-exempt'`, meta = FULL `proof.reason`, try/catch so a stamp can never fail a merge) — the tick drops `outcome.note`, so the ledger row is what makes the exemption visible in the run record. Write the test battery: mixed-selector search refusal (`go test ./cmd/ ./...` vs `tests/support/helper.go`) with the elsewhere-rooted-search positive control and the option-carried-search case; percent-encoded carried value refusal with the malformed-`%` control; `data:`-URL and scheme-free embedded-absolute escape refusals; `missingClaimRefusalReason` prefers an executable source and caveats a non-executable one; query-suffixed element no longer misdescribed as a search; orchestrator exempt-stamp assertions (full reason, negative control, throwing-stamp control). Refresh this plan file, run the trident suites + typecheck, commit ONE commit, push to origin only after asserting the remote still sits at `6a166821` (never force), and emit the mutation nomination (file `trident/mutation-prover.ts`, find `const search = searchesReaching(guard, target)`, guard `bun test trident/mutation-prover.test.ts`, control `bun test trident/git-mode.test.ts`).
- [x] T13 — ANSWER THE 14-FINDING ROUND (this iteration). `LOADABLE_SCHEME`: both alternatives inside one left token boundary, semantics table-checked (every pinned refusal and allowance identical; `-rfile:///abs` keeps its refusal through the attached-short-option re-read and the embedded-absolute arm), plus a 200k-character linearity pin that is red if the boundary comes back off. `TEST_BASENAME`: the two REAL extension families spelled out (`[cm][jt]s` takes no `x`; the bare form takes an optional one), so `.cjsx`/`.mjsx`/`.ctsx`/`.mtsx` stop DECLARING a test and stop buying the exemption — table rows and predicate pins both ways. `elementEscapes`: a specifier that BEGINS with `#` (element head or straight after an `=`, and through the attached short option) is refused as a package-imports alias, with the refusal naming it, and a mid-specifier fragment (`./src/other.test.ts#case`) pinned STILL legal so the boundary is load-bearing. Plan file refreshed to the tree it ships in; the suite report refiled honestly (full suite run, outcome named by the one vocabulary the gate accepts).

NOT taken, deliberately: the deletion-only production diff deadlock (pre-existing, fails closed, needs its own card — recorded in T9). FOLLOW-UP card, out of scope by the card's Do-not: `TEST_COMMAND_SHAPES`'s `node` entry accepts `node check.js --test` (a general shell); identical on main; fix is requiring `argv[1] === '--test'`. Out of scope forever on this branch: fixing or relaunching #489 (after this merges AND deploys — the gate runs from the deployed checkout, not from the merge). This plan file rides the branch as ~100 sibling plan files do: it classifies as prose for the gate and is absent from main, so it clobbers no other lane.
