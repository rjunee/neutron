# IMPLEMENTATION_PLAN — mutation gate: a test-infrastructure diff gets a legal outcome (PR #490)

Card: the mutation-proof gate had no reachable outcome for a diff whose only changed .ts files are declared tests — an APPROVED PR could never merge. Fix, ALL ON THIS BRANCH at head 8f57c727 (PR #490 OPEN / MERGEABLE / CLEAN, 17 of 17 checks pass at that head, re-verified live this round): declared-test classification, the direct anti-tautology rule plus ten closed forged-proof escapes, the recorded no-production-file exemption, the split refusal, and the forge contract's MUTATION NOMINATION ask (8f57c727).

THE CODE IS COMPLETE. Nine consecutive rounds ended APPROVE with mutationClaim null and the merge gate refused every one of them. THE ONLY REMAINING WORK IS EMITTING THE NOMINATION IN THE STRUCTURED OUTPUT. A round that edits any source file has failed this round.

THE PLAN LIVES AT THE REPO-ROOT IMPLEMENTATION_PLAN.md ONLY. trident/IMPLEMENTATION_PLAN.md belongs to an earlier merged card; zero commits on this branch touch it. Leave it alone.

- [x] D1 — declared-test classifier in trident/mutation-prover.ts: basename test/spec/_test patterns and direct __tests__/ children are tests (rejected as before); other files under a top-level tests/ or test/ are support libraries and LEGAL targets; documentation rejection unchanged.
- [x] D2 — anti-tautology stated directly: a guard whose argv reaches claim.file is refused, and the whole opacity family is closed across ten commits — package-script positionals (26884ed6), Makefile recipes (f87a4405), separators and wrappers (296f4479), third-file preloads (6038cdcb), reporter options (470701f0), configs and env files (22e9865a), command-line configs in every spelling (2c4f5d7d), node --run and go one-dash hooks (6fe44901), resolution maps and only-a-leading --test (d72c5fb6), implicit-file conditions (41f1dd1f).
- [x] D3 — explicit recorded exemption when git says no production file changed, with its OWN reason string distinct from the prose-only one, visible in the run record.
- [x] D4 — the required-but-missing refusal distinguishes "a legal target existed and none was named" from "no legal target existed", naming at least one considered-and-disqualified file.
- [x] Requirement 5 — the forge contract asks for the nomination it gates merges on: MUTATION NOMINATION block in forgeBuildContract, report-field wiring, and pinning tests (8f57c727, plus 26 lines of test).
- [x] Tests — red-then-green proof against a tests/ support library with an unrelated control; classification table over the card's paths; tautology refusal naming the tautology; test-only-plus-markdown exemption whose reason is NOT the prose-only string; the load-bearing negative (a production file in the diff plus no claim is still REFUSED); positive controls against silently-empty extractions. CodeQL ReDoS on the loadable-scheme regex fixed and confirmed green by GitHub.
- [x] EMIT THE MUTATION NOMINATION in the mutationClaim structured-output field, verbatim: file `trident/mutation-prover.ts`; find `const search = searchesReaching(guard, target)`; replace `const search = null`; guard `bun test trident/mutation-prover.test.ts`; control `bun test trident/git-mode.test.ts`. Verified at 8f57c727: the find string occurs exactly once, at mutation-prover.ts:816 (uniqueness is counted within claim.file alone, so this plan's copies cost nothing); unmutated guard 140 pass / 0 fail, mutated guard 139 pass / 1 fail; control 100 pass / 0 fail mutated and not. Re-run `grep -cF 'const search = searchesReaching(guard, target)' trident/mutation-prover.ts` at your own head before reporting; whatever the count, DO NOT alter the nomination and DO NOT edit any file — record anomalies in final text only.

Do not re-derive (settled): the full-repo suite red is pre-existing and environmental (142 pass / 23 fail, byte-identical failure line-sets at base and head after timing-stripping, none under trident/); a fresh worktree needs `bun install --frozen-lockfile` before believing any red; guard/control measurements must run inside the repo's own .trident-worktrees/ so workspace aliases resolve as the prover's own proof will; the salvage tag for run 025c31ff is SPENT and its content is on the branch.

Deferred to follow-up cards (accepted; every one latent and failing closed): opaque script runners as a family; the direct-__tests__-child rule declaring 13 support libraries tests; unittest test*.py and tests/*.rs classifying production yet un-nominatable; deletion-only production diffs unclearable; the sim's ok-false-exempt-true shape; the exempt stamp written before the merge attempt; mocha's -R and pytest's -p short reporter letters; the codex build route's reporting coda replacing the schema report so a codex-routed build cannot nominate even once asked.
