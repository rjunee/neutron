# IMPLEMENTATION_PLAN — the wrong-base guard resolves its remedy instead of guessing it (PR #494)

Card: the wrong-base refusal printed an unconditional `git branch -D` remedy that would have destroyed a live lane's work (and, in the case the guard actually fires on, often could not even run). Fix: before composing the refusal, resolve the HOLDER (live worktree / dead-owner worktree / none / unknown) and the publication state (local sha vs `refs/remotes/origin/<branch>` after a one-ref fetch), then emit exactly ONE evidence-naming refusal, chosen by that resolution. The card names four required arms and the as-built ships all four — live holder (no `branch -D` anywhere in the message), dead holder (unlock-then-remove), unheld-and-published (the delete, with both shas as the reason it is safe), unheld-and-unpublished (salvage, never delete) — WITH ONE STATED PRECONDITION ON THE SECOND, because two reviewers measured it and the acceptance claim has to match what runs (Argus round 9 major): the DEAD-holder arm is gated behind an occupancy cross-check, and `probeTreeOccupancy` answers UNKNOWN if ANY `/proc/<pid>/cwd` on the box is unreadable — so off euid 0, on a host carrying other users' processes, a dead lock pid always degrades to the UNKNOWN arm and the DEAD arm is reached only where this process may read every /proc entry (root, or a single-uid box) or from injected fixtures. That degradation is deliberate and in the SAFE direction — an unreadable /proc entry may be a process standing in the tree, and `clear` is the answer that would print `worktree remove` — and the UNKNOWN arm it degrades to names the dead pid, says why occupancy could not be established, prints the read that settles it and authorises nothing. So: three arms fire in production at non-root euid, four where /proc is fully readable, and nothing about that is hidden from the reader of the refusal — plus the shapes the four do not cover, each with its own arm rather than being folded into a neighbour it would misdescribe: the holder is the repo's OWN shared checkout (no other lane to wait for), the holder is a PRUNABLE entry whose directory is gone, a worktree mid-rebase/mid-bisect holds the branch without the listing saying so, and every UNKNOWN (holder, publication, or the ancestry the outer guard itself could not establish). UNKNOWN never authorises an irreversible act. The refusal itself is never weakened.

State this round: PR #494 OPEN, previous head `2fadb68c` (== origin). Argus round 10 returned ONE blocker and two majors on the refusal's WRITE PROSE, all three in `trident/delivery.ts`, plus one stale-doc minor. Blocker: the opening write-safety sentence composed into EVERY arm is an unconditional absolute that a `reference-transaction` hook falsifies — the round-9 fix scoped the launcher's sentence alone and missed this one and the failed-fetch arm's sibling absolute, reproduced on the exact fetch form this path runs. Majors: the successful-fetch arm guarantees a tracking-ref refresh and a reflog append that a fetch finding origin unchanged never makes; and every arm ends with `re-dispatch once the branch is free`, including the unheld arms whose required next step is a verified delete or a salvage. T15 answers all three, plus a bound on the one attacker-shaped refusal field that had none. The full-suite red this branch keeps meeting is PRE-EXISTING and unchanged: the failing files are landing/, tests/integration/, app/, gateway/ and gbrain-memory/, none of which this diff touches, and they fail identically at the merge-base.

- [x] T1 — four-arm remedy resolver + orchestrator wiring (`trident/wrong-base-remedy.ts`, on origin at `f3e365fb`): holders enumerated via `git worktree list --porcelain -z`; lock pid probed as alive / dead / unknown, three distinct answers, by specific pid never by name pattern; publication compared after a one-ref fetch; live-holder refusal names worktree path + pid with no `branch -D`; dead-holder names unlock-then-remove; safe arm prints the re-proving compare-and-delete chain; unpublished arm names the salvage mechanism; UNKNOWN refuses with no destructive command.
- [x] T2 — table tests for all four arms + UNKNOWN + positive control, load-bearing substrings not whole-message equality; delivery classifies the refusal as its own `branch-held` class.
- [x] T3 — GH013 push-protection fix: fictional-vendor credential fixture with both load-bearing assertions kept.
- [x] T4 — published and green at `f3e365fb`: CI 17/17; PR #494 body names the stale-ref-sweep hazard its four-arm table answers.
- [x] T5 — Argus round-1 review obtained (13 findings) and summarised.
- [x] T6 — round-1 review-fix recovered VERBATIM from its salvage and committed as `8adead74`: answers round-1 blockers 1-2, majors 3-7, minors 8-9, nit 11 and parts of 12.
- [x] T7 — finding 10 ENFORCED: `TOTAL_BUDGET_MS = 30_000` prices all five spawns through a `budget(perCall)` clamp with injectable `now`; exhaustion degrades to UNKNOWN; a timed-out origin probe answers UNKNOWN instead of a false claim; watchdog details stop naming per-attempt numbers the clamp can falsify (`051877d6`, `42567cc8`). Published; Argus round-2 returned 15 findings.
- [x] T8 — recover salvage `f3957c3a` VERBATIM as commit 1 (clears CI blockers 1-2 by deleting the wall-clock assertion for a deterministic discriminant — also major 3; control-char fold in `scrub()` + forge test — major 4; truncated-listing UNKNOWN — nit 12; `GIT_TERMINAL_PROMPT=0` — nit 13), then commit 2: nit 14 rider (the banned watchdog figure derived from the measured grant, not the literal `'15s'`) + persist this plan. Verify wall-clock gate 0 unmarked, scoped 383/383, `bun test trident/` 3016/3016, `tsc --noEmit` clean. Never push — the harness publishes at checkpoint, then Argus re-reviews the new head; with every round-2 finding answered, refuted or decided, that review should clear the PR to merge.
- [x] T9 — THIS ROUND (Argus round 3, one blocker + three majors + three minors): the OUTER ancestry guard in `orchestrator.ts` reads `merge-base --is-ancestor`'s THREE exits (0/1/error) instead of only `.ok`, for BOTH probes — containment in the base, and descent from this run's prior base — so a probe error, a watchdog kill or a failed spawn no longer composes a refusal asserting divergence nobody measured; UNKNOWN still refuses (fail-closed, no fire) but as itself, with no destructive command (blocker). `defang()` matches git's COMBINED short options (`-Dr`, `-fd`, `-dr`, `-f -d`), which were real deletes rendering verbatim — and put the literal `branch -D` back into the live arm whose contract forbids it (major). A table runs the REAL composer over all twelve reachable arms into `interpretFailure`, so the composer/classifier seam is enforced by the seam and not by duplicated literals (major). `wrongBaseWrites` names the fetch's REFLOG write and gives the post-fetch throw arm its own honest attribution instead of the fall-through's "refused before the fetch" (two minors). This plan's opening now describes the decision model as built rather than promising "exactly four" arms (minor).
- [x] T10 — THIS ROUND (Argus round 4, two code blockers + one major + two minors + four nits): the branch NAME is folded for prose on both sides of the seam — the exemption rested on "git's ref rules exclude control characters", which is true of ASCII controls and false of U+2028/U+202E, both of which `git branch` accepts and `rev-parse --verify` resolves (measured on git 2.43) — and `sh()` ENCODES the same set with ANSI-C `\uHHHH` so a quoted argument stays runnable without reordering the line (blocker). The rebase/bisect arm discloses that `git bisect reset` and `git rebase --abort` are CHECKOUTS that silently replace a file ignored in that state and tracked on the branch they restore, names the `--ignored` read it already prints as the preflight, and says BEFORE rather than after (blocker). The two pre-launch UNKNOWN refusals count the base fetch's writes — tracking ref, reflog, FETCH_HEAD, objects — conditionally on whether that fetch actually ran, so neither under- nor over-counts (major). `defang()` reads the verb's arguments as a bounded window of TOKENS instead of spelling the option run in the regex, closing `branch -v -q -D`, `branch -Dvvvvv`, `branch -vvvvvD`, `push -f origin :feat`, `push --force origin :feat`, `push origin -d feat` and `push -d origin feat`, and the docblock stops claiming more than the code does and states that the rule is git-verb-scoped (minor + nit). The as-built records that the DEAD-holder arm is FIXTURE-ONLY in production at non-root euid, and that an erroring ancestry probe now refuses before the own-crash-leftover rescue (minor + nit). A test pins that a worktree PATH carrying the banned literal only ever reaches the treat-as-live arms inside `sh()` quoting, with a positive control that the helper can see the safe arm's own unquoted delete (nit).
- [x] T11 — THIS ROUND (Argus round 6, four blockers + three majors + two minors + one nit): the dropped commit `5c73cfa6` is recovered verbatim, restoring the token-window `defang()` rewrite, the `SH_ENCODE` set that makes `sh()` and `defang()` agree on which codepoints are forgery vectors, the folded `branchProse` in every composer arm, and the bisect/rebase checkout disclosure — blockers 1, 2 and 3 and the two majors that rode on them. Blocker 4 (write attribution ignored the launcher's own base fetch, which precedes the composer on a fresh PR build): `delivery.ts` scopes its per-arm claim to "the guard itself" and adds one shared sentence attributing the launcher's base refresh to the launcher — unenumerated on purpose, because this module reads a reason STRING and cannot know whether that arm ran, and naming a write that may not have happened is the overcounting the per-arm conditional exists to prevent. Major: both enumerations stop closing with "and nothing else", which `fetch.writeCommitGraph` falsifies, and bound themselves by the class instead — everything a one-ref fetch touches lives under `.git`; `NO_DESTRUCTIVE_WRITE` and the orchestrator's `noWrites` scope "file" to the TREE, so neither says no file changed one clause before naming FETCH_HEAD. Minor: a lock reason whose digits are not canonical decimal (`pid 0000123`) now names both what was written and what was probed, since this arm's contract is naming the evidence it measured. Nit: `TOTAL_BUDGET_MS`'s docblock states that it prices SPAWNED commands and that `probeTreeOccupancy`'s synchronous /proc walk is bounded by something else. Tests: the pid-rendering disagreement with an ordinary-decimal positive control and the oversized shape the raw rendering exists for, and a seam table asserting the scoped attribution over four arms.

- [x] T12 — THIS ROUND (Argus round 7, one blocker + three majors + one minor): `foldRefName` folds the two NAME fields — branch and base — to a single token instead of folding their forgery codepoints to an ASCII SPACE, which is the character `delivery.ts`'s `WRONG_BASE_PREFIX` anchors on (`[^ \n]+`, because it is the one character git's ref rules forbid); the old fold put a space inside a name field, missed the anchor, and let a git-legal branch name turn a branch-held stand-down back into "Reply to retry the build" (blocker). All six `unknownHolder` call sites now pass the folded name — three passed the raw one, so the arms with the least evidence rendered a forged line break and a verbatim `branch -D` (major). The launcher folds `base` in every refusal that quotes it, on the same terms as branch and repo path (major). The launcher's base fetch names its destination refspec, because the shorthand leaves `refs/remotes/origin/<base>` STALE under a narrowed `remote.origin.fetch` (reproduced, git 2.43) while the refusal claimed that ref was written — the build was pinning a stale base and the message was overclaiming (major). `defang`'s window becomes the leading option RUN plus four tokens, closing six-option delete spellings that rendered verbatim (minor). Tests: the composer's real output over four git-legal hostile names through the real `interpretFailure` (the seam that broke), the three previously-raw UNKNOWN arms, a hostile base through the launcher, the refspec in both the argv and the sentence, and the long option runs with their benign controls.

- [x] T13 — THIS ROUND (Argus round 8, one blocker + three minors + four nits): the launcher declares `repoProse` and `gitDetail` ABOVE the base fetch, so the fetch-failure and tip-resolution refusals fold the repo path and git's stderr on exactly the terms the ancestry arms already did — a legal path (`git init` and `git worktree add` both accept a newline; store.ts persists it verbatim) or hostile fetch stderr could otherwise forge a line carrying `branch -D` inside a message delivery routes through the same `the build was NOT started` classifier, and neither boundary had a test (blocker). `defang`'s verb is no longer anchored on `\b`: one word character in front of it — `Xbranch -D victim` — made the whole rewrite miss and put the literal back into the ALIVE arm whose pinned contract forbids it. The anchor is dropped rather than widened, because the verbs are only rewritten when a delete option is found in the bounded window after them, so prose that merely contains the letters is untouched (minor). The `invariant 122` citations name their section — the numbers live inside §12, so a reviewer looking for a top-level 122 finds sections 1-13 and reads the citation as dangling; the rule exists at docs/INVARIANTS.md:916 and the card's "121" is the off-by-one (nit). This card-local plan is refreshed from IMPLEMENTATION_PLAN.md, which had drifted a round behind (nit). Tests: both PRE-FETCH arms driven through the real launcher with a hostile path AND hostile stderr at once, asserting one line, no `branch -D`, `<command removed>` present, the readable path and git's own words still quoted, and the delivery seam still reading it as a launch that never happened; plus the word-character forgeries at the helper and through the real composer, with a benign-prose control and the safe arm's own delete as the positive control.

- [x] T14 — THIS ROUND (Argus round 9, one CI blocker + four majors + three minors + two nits): `defang` stops being a regex over the whole string and becomes a TOKEN SCAN — one split, one right-to-left pass recording the nearest delete option at or after each index, one left-to-right pass rewriting — which removes the `(?:\s+-\S+)*(?:\s+\S+){0,4}` shape CodeQL flags as `js/polynomial-redos` (blocker) and, with it, the last bounded window: the option may now sit ANYWHERE after the verb, closing `git branch w x y z -D victim` and every longer permutation of it, because git permutes argv and no token count closes a class whose next member has one more word in it (major). The short-cluster test loses its own `[A-Za-z]*[Dd][A-Za-z]*` ambiguity for a `startsWith` plus one anchored `^[A-Za-z]+$`, and `foldEvidence` gains the 64k INPUT cap `scrub` already had, so the "uncontrolled data into a rewrite loop" premise is answered in the code rather than in a comment about the callers (nit). `foldRefName` marks a LEADING DASH, because the git-legal name `-D-victim` spelled the banned literal out of the NAME field where `defang` cannot reach it (minor). The DEAD arm discloses that `worktree remove` does not check occupancy at ALL — reproduced on git 2.43, a non-force remove exited 0 while a process held the tree as its cwd and left it in a deleted directory — so the re-check is named as the reader's own job rather than implied (major). This plan's acceptance claim now states the DEAD arm's production precondition instead of leaving it to the as-built (major). The fetch write-safety prose in `delivery.ts` and `orchestrator.ts` is scoped to what the fetch ITSELF writes, since a `reference-transaction` hook is arbitrary local code that runs inside that ref update (minor). The docblock's perf figure is re-measured against the current code instead of carrying the superseded regex's number (minor). Tests: the argv-permutation table through the real composer with two benign controls and the safe arm's own delete as the positive control, the leading-dash name through the live-holder arm, and the `remove`-is-blind-to-occupancy disclosure pinned in the DEAD arm.


- [x] T15 — THIS ROUND (Argus round 10, one blocker + two majors + one minor): the write-safety sentence every arm OPENS with is ATTRIBUTED (`… by git itself on this path`) and the hook that falsifies an absolute is named ONCE, after the enumeration, covering every write sentence in the message — the failed-fetch and threw arms' `nothing else can have been written either way` is attributed on the same terms, because a hook is arbitrary local code firing inside the very ref update the fetch makes and no sentence here can bound it (blocker). The successful arm's enumeration becomes per-item conditional: the refresh, the reflog append and the downloaded objects are hedged on origin having moved the ref — an identical fetch against an unchanged remote leaves that reflog at one line — while FETCH_HEAD stays unconditional, and the same correction lands on `orchestrator.ts`'s `noWrites` for the base fetch (major). The delivery's closing next step is chosen per arm from the same evidence sentence the write attribution reads: held keeps `re-dispatch once the branch is free`, unheld says the reason's own step (verified delete, or salvage) comes first and re-dispatch only after it, and UNKNOWN authorises nothing until the holder is established — three answers, because an unknown holder is not an unheld branch (major). The rendered lock pid is capped at 32 digits with the truncation MARKED: it was the one attacker-shaped field with no bound, and a forged reason composed a ~200KB persisted refusal; the parse and the `probed as` comparison still run on the raw digits (minor). Tests: the caveat and both retired absolutes pinned across every arm the composer can emit with a once-only count, the reflog hedge and the one item that is not hedged (the guarding test previously asserted only the substring `reflog`, which is how the overclaim survived a round), a three-group table over held / unheld / unknown tails with the held group as the positive control against an over-broad tail deletion, and a 200,000-digit forged pid asserting the marked cut and an unweakened refusal.

Guardrails binding every round: the refusal itself is never weakened or removed; nothing is killed, signalled or reaped; no worktree is unlocked/removed as a side effect of composing a message; liveness is probed by the specific pid a lock names, never by process-name patterns; nothing under `.github/workflows/` changes; `trident/mutation-prover.ts` is untouched (a sibling card owns it); no real-vendor secret-shaped fixtures; no absolute host filesystem paths in files, commit messages or PR bodies; never force-push, rebase or reset this branch.
