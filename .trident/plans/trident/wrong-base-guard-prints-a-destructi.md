# IMPLEMENTATION_PLAN — the wrong-base guard resolves its remedy instead of guessing it (PR #494)

Card: the wrong-base refusal printed an unconditional `git branch -D` remedy that would have destroyed a live lane's work (and, in the case the guard actually fires on, often could not even run). Fix: before composing the refusal, resolve the HOLDER (live worktree / dead-owner worktree / none / unknown) and the publication state (local sha vs `refs/remotes/origin/<branch>` after a one-ref fetch), then emit exactly ONE evidence-naming refusal, chosen by that resolution. The card names four required arms and the as-built ships them all — live holder (no `branch -D` anywhere in the message), dead holder (unlock-then-remove), unheld-and-published (the delete, with both shas as the reason it is safe), unheld-and-unpublished (salvage, never delete) — plus the shapes the four do not cover, each with its own arm rather than being folded into a neighbour it would misdescribe: the holder is the repo's OWN shared checkout (no other lane to wait for), the holder is a PRUNABLE entry whose directory is gone, a worktree mid-rebase/mid-bisect holds the branch without the listing saying so, and every UNKNOWN (holder, publication, or the ancestry the outer guard itself could not establish). UNKNOWN never authorises an irreversible act. The refusal itself is never weakened.

State this round: PR #494 OPEN at head `7936fae2` (== origin). Argus round 3 returned 9 findings: ONE blocker (the outer ancestry guard reads only `.ok`, so exit 128 / a watchdog kill / a spawn failure composes the divergence refusal — verified fail-closed, so no unsafe delete results, but a transient probe error still produces a false terminal refusal asserting evidence never established), three majors (the composer/classifier seam enforced only by duplicated frozen literals, with no test feeding real composed output through `interpretFailure`; `defang()` missing git's combined short-option deletes `-Dr`/`-fd`/`-dr`; and the whole-repo suite red, VERIFIED pre-existing by the round's own base comparison and independently corroborated — does not block), three minors (the post-fetch throw arm inheriting the "refused before the fetch" attribution; "and nothing else" undercounting the fetch's reflog write; this plan still promising "exactly four" refusals against an as-built that ships more arms) and one nit of unverified single-reviewer residue kept non-blocking. Every blocker, major and minor above is answered by T9.

- [x] T1 — four-arm remedy resolver + orchestrator wiring (`trident/wrong-base-remedy.ts`, on origin at `f3e365fb`): holders enumerated via `git worktree list --porcelain -z`; lock pid probed as alive / dead / unknown, three distinct answers, by specific pid never by name pattern; publication compared after a one-ref fetch; live-holder refusal names worktree path + pid with no `branch -D`; dead-holder names unlock-then-remove; safe arm prints the re-proving compare-and-delete chain; unpublished arm names the salvage mechanism; UNKNOWN refuses with no destructive command.
- [x] T2 — table tests for all four arms + UNKNOWN + positive control, load-bearing substrings not whole-message equality; delivery classifies the refusal as its own `branch-held` class.
- [x] T3 — GH013 push-protection fix: fictional-vendor credential fixture with both load-bearing assertions kept.
- [x] T4 — published and green at `f3e365fb`: CI 17/17; PR #494 body names the stale-ref-sweep hazard its four-arm table answers.
- [x] T5 — Argus round-1 review obtained (13 findings) and summarised.
- [x] T6 — round-1 review-fix recovered VERBATIM from its salvage and committed as `8adead74`: answers round-1 blockers 1-2, majors 3-7, minors 8-9, nit 11 and parts of 12.
- [x] T7 — finding 10 ENFORCED: `TOTAL_BUDGET_MS = 30_000` prices all five spawns through a `budget(perCall)` clamp with injectable `now`; exhaustion degrades to UNKNOWN; a timed-out origin probe answers UNKNOWN instead of a false claim; watchdog details stop naming per-attempt numbers the clamp can falsify (`051877d6`, `42567cc8`). Published; Argus round-2 returned 15 findings.
- [x] T8 — recover salvage `f3957c3a` VERBATIM as commit 1 (clears CI blockers 1-2 by deleting the wall-clock assertion for a deterministic discriminant — also major 3; control-char fold in `scrub()` + forge test — major 4; truncated-listing UNKNOWN — nit 12; `GIT_TERMINAL_PROMPT=0` — nit 13), then commit 2: nit 14 rider (the banned watchdog figure derived from the measured grant, not the literal `'15s'`) + persist this plan. Verify wall-clock gate 0 unmarked, scoped 383/383, `bun test trident/` 3016/3016, `tsc --noEmit` clean. Never push — the harness publishes at checkpoint, then Argus re-reviews the new head; with every round-2 finding answered, refuted or decided, that review should clear the PR to merge.
- [x] T9 — THIS ROUND (Argus round 3, one blocker + three majors + three minors): the OUTER ancestry guard in `orchestrator.ts` reads `merge-base --is-ancestor`'s THREE exits (0/1/error) instead of only `.ok`, for BOTH probes — containment in the base, and descent from this run's prior base — so a probe error, a watchdog kill or a failed spawn no longer composes a refusal asserting divergence nobody measured; UNKNOWN still refuses (fail-closed, no fire) but as itself, with no destructive command (blocker). `defang()` matches git's COMBINED short options (`-Dr`, `-fd`, `-dr`, `-f -d`), which were real deletes rendering verbatim — and put the literal `branch -D` back into the live arm whose contract forbids it (major). A table runs the REAL composer over all twelve reachable arms into `interpretFailure`, so the composer/classifier seam is enforced by the seam and not by duplicated literals (major). `wrongBaseWrites` names the fetch's REFLOG write and gives the post-fetch throw arm its own honest attribution instead of the fall-through's "refused before the fetch" (two minors). This plan's opening now describes the decision model as built rather than promising "exactly four" arms (minor).

Guardrails binding every round: the refusal itself is never weakened or removed; nothing is killed, signalled or reaped; no worktree is unlocked/removed as a side effect of composing a message; liveness is probed by the specific pid a lock names, never by process-name patterns; nothing under `.github/workflows/` changes; `trident/mutation-prover.ts` is untouched (a sibling card owns it); no real-vendor secret-shaped fixtures; no absolute host filesystem paths in files, commit messages or PR bodies; never force-push, rebase or reset this branch.
