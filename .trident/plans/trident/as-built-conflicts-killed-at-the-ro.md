# IMPLEMENTATION_PLAN — AS_BUILT conflicts killed at the root: no branch writes docs/AS_BUILT.md; the outer loop appends on main after the merge lands

Regenerated 2026-08-17 against main (89de7523). Card: builds stop touching `docs/AS_BUILT.md` and instead stage their entry as a run-scoped artifact committed on the branch — `.trident/as-built/<branch-slug>.md`, a unique path per branch, the exact shape `.trident/plans/<branch>.md` already uses, so two concurrent PRs cannot conflict by construction. After a merge lands, the OUTER loop (the one owner of GitHub operations, already serialised by merging) folds every staged entry into `docs/AS_BUILT.md` directly on main and removes the consumed staging file in the same commit. A CI guard turns the old habit into a red X. The union/merge-driver machinery — which exists only to survive branch-side writes, and which GitHub never consults server-side (measured: 26 of 33 open PRs CONFLICTING with the union floor in place) — is retired once nothing needs it.

Locked constraints (from the card; violating any is a refused design):
- `docs/AS_BUILT.md` stays the ONE canonical place to READ the log (owner lock 2026-08-16). The staging dir is a consumed queue — every file in it is deleted by the fold that lands it — never a second log. No `docs/as-built/` revival.
- The inner loop never pushes to GitHub and never commits to main; the appender belongs to the outer loop only.
- KEEP `scripts/git/as-built-heading-uniqueness` (the shape contract) and the `## YYYY-MM-DD — title` newest-first form.
- The 31 historical prose cross-references are not rewritten.

Survey 2026-08-17: nothing of the new path exists. CONTRIBUTING.md still teaches branch-side prepending (§"Merge driver for the AS_BUILT log"), `.gitattributes`' only content is the union floor, ci.yml (layering job, ~line 193) still runs `check-governed-repo-attributes.ts`, and `trident/orchestrator.ts` still installs the entry-aware driver before every replay ((e2), ~line 1180). All tasks unchecked.

- [x] 1. **The shared entry model + pure fold, in trident.** NEW `trident/as-built-log.ts`: move `parseLog`/`serializeLog`/`LogEntry`/`ParsedLog` (+ `FENCE`/`HEADING`/`DATE_IN_HEADING`) verbatim out of `scripts/git/as-built-log-merge.ts`, and `findDuplicateEntryHeadings`/`explainDuplicateEntryHeadings`/`DuplicateEntryHeading` out of `scripts/git/as-built-heading-uniqueness.ts`; both scripts/git modules become import+re-export shims over `@neutronai/trident/as-built-log.ts` (scripts→trident is the sanctioned direction — precedent `scripts/ci/check-governed-repo-attributes.ts`; trident must never import scripts). Add `foldEntryIntoLog`/`foldEntriesIntoLog`: insert a staged entry directly under the preamble (newest-first), byte-preserve every existing log byte, validate the `## YYYY-MM-DD — title` heading form, retitle a colliding heading with the first free ` (n)` suffix (the driver's own rule, preserving heading uniqueness), refuse malformed input with a reason naming the defect. NEW `trident/as-built-log.test.ts` incl. round-trip + fold over the REAL `docs/AS_BUILT.md`; the three existing scripts/git test files must pass UNMODIFIED.
- [x] 2. **The fold pass — the one writer.** NEW `trident/as-built-appender.ts`: `foldStagedAsBuiltEntries(run_host, repo_path, merge_mode, base)` — fetch the base tip (pr mode), enumerate `.trident/as-built/**/*.md` in that tree (no-op when empty), order by the commit time each file landed on the base (oldest folded first so the newest ends topmost), fold via task 1, `git rm` every consumed file IN THE SAME COMMIT as the log append, commit on the base in a scratch worktree (never the shared checkout) under a pinned neutral identity (`-c user.name`/`-c user.email` — never the ambient checkout identity, which is on the PII denylist per the SPEC backlog), push in pr mode with a PLAIN push (never `--force`: a non-fast-forward rejection means main moved; leave the queue intact and let the next pass retry off a fresh fetch). Idempotent by construction — the staged file's presence on the base IS the durable queue. Every failure reason carries git's own stderr (#259 doctrine) and never credential material; a malformed staged file is refused by name and left in place, never guessed at. Realgit tests for: fold-after-merge, nothing-staged no-op, malformed-staged refusal, non-FF rejection surfacing stderr with the queue intact, stale-local-state immunity via the scratch worktree.
- [x] 3. **Wire the writer into the outer loop.** Call the fold pass after every successful `cleanupAfterMerge` in `trident/orchestrator.ts` `applyResult` (~line 2572; both merge modes), and add a bounded per-repo catch-up in `trident/tick.ts` for staged entries whose landing the post-merge pass missed (merge-queue-delayed pr landings, credential blinks, restarts) — the catch-up is what makes a missed window self-heal instead of accreting. A fold failure must NOT fail the already-merged run (the merge landed; the entry is safely queued) — surface it in the advance note. Tests: APPROVE path runs the fold; fold failure leaves the run done; the catch-up folds an entry the post-merge pass missed.
- [ ] 4. **Teach the convention where builders learn it, and dogfood it.** CONTRIBUTING.md: replace §"Merge driver for the AS_BUILT log" with "The as-built log has ONE writer" — no branch touches `docs/AS_BUILT.md`; stage exactly one entry as `.trident/as-built/<branch-slug>.md` with the `## YYYY-MM-DD — title` heading; the outer loop folds it into the log on main after the merge lands; the log remains the only place to READ. Stage THIS card's own as-built entry through the new path — this branch must never touch `docs/AS_BUILT.md` (it is the guard's first subject). Only touch `scripts/ci/leak-gate-allowlist.txt` if a staged path actually trips a literal-path rule (INVARIANTS #33/#83/#99: allowlist moves ride the same PR); new entries must not need retired-vocab exemptions.
- [ ] 5. **T3 — the CI guard that makes the old habit impossible.** NEW `scripts/ci/as-built-write-guard.sh`: exit 1 when `git diff --name-only "$GUARD_BASE_SHA"..."$GUARD_HEAD_SHA"` names `docs/AS_BUILT.md`, with a message naming the rule and the staging path; exit 2 — never a silent skip — when either sha is missing or unresolvable, so the verdict is DETERMINISTIC for a given head (the leak gate's per-event base window is the named counterexample in SPEC). ci.yml: run it in the `purity` job for `pull_request` (`base.sha`/`head.sha` — the three-dot form diffs merge-base→head, so a fold commit on main can never false-red a PR) and `merge_group` (`base_sha`/`head_sha`); NOT on push to main (the appender is the legitimate writer there). Pin the wiring in `scripts/ci/ci-workflow.test.ts` — a gate with no workflow step ran nowhere (the governed-attributes gate's own history, #315).
- [ ] 6. **T4 — prove it with two concurrent branches, real git.** New realgit test: cut two branches from one base, each staging its entry through the new path; merge both; assert (a) neither merge conflicts, (b) both entries present in `docs/AS_BUILT.md` on main after the fold passes, (c) newest-first ordering + heading form hold, (d) the staging queue is empty. Mutant: a third branch writing `docs/AS_BUILT.md` directly makes the guard exit non-zero naming the file, with a clean-branch control passing — exercising the REAL `scripts/ci/as-built-write-guard.sh`, not a reimplementation, so an appender-only test can no longer pass with the guard unwired.
- [ ] 7. **T5 — retire what the one-writer design makes dead, in one change.** Delete: the `.gitattributes` union block (the file's only content — delete the file), `scripts/git/as-built-merge-driver.ts` + `as-built-merge-realgit.test.ts`, the `mergeAsBuiltLog` half of `scripts/git/as-built-log-merge.ts` (+ its merge-behavior tests; the parser lives in trident since task 1), `scripts/install-merge-drivers.sh` + its CONTRIBUTING section, `trident/orchestrator.ts` `ensureAsBuiltMergeDriver`/`asBuiltDriverCommand`/`AS_BUILT_DRIVER_NAME` + the (e2) pre-replay install + `trident/as-built-publish-wiring-realgit.test.ts`, `trident/as-built-union-attribute.ts`(+test), `scripts/ci/check-governed-repo-attributes.ts`(+test) + its ci.yml step + its `ci-workflow.test.ts` describe; update the `docs/INVARIANTS.md` entries that pin the union floor. KEEP `scripts/git/as-built-heading-uniqueness.ts`(+its unmodified test) — the shape contract outlives the merge machinery.

Order rationale: the new path must exist end-to-end (1–4) before the guard (5) forbids the old one — forbidding first would strand every in-flight build's provenance; the union floor stays until (7) so still-open old-convention branches keep merging locally in the interim; retirement lands only after the proof (6) shows the one-writer path carrying the load.

## MEASURED 2026-08-18 — the currently-red driver tests must be DELETED by task 7, not fixed

Three independent findings, each verified on this branch. Do not spend a round trying to
make `as-built-merge-realgit.test.ts` green; it cannot be, and task 7 removes it.

1. **The driver cannot import the shared parser at all, by construction.**
   `as-built-merge-realgit.test.ts:93-95` copies only `install-merge-drivers.sh`,
   `as-built-merge-driver.ts` and `as-built-log-merge.ts` into a bare temp repo with no
   `node_modules`. So `@neutronai/trident/as-built-log.ts` cannot resolve there, the driver
   fails to load, `install-merge-drivers.sh` returns non-ok, and six tests go red. This is
   not a repo-wide resolution bug — 32 other `@neutronai/trident/*` subpaths resolve fine.

2. **The obvious fix is forbidden.** Rewriting it as `../../trident/as-built-log.ts` DOES
   fix five of the six (measured: 111 pass / 1 fail locally) but `scripts/ci/lint.sh`
   refuses it: *"Relative import from another package is not allowed. Use
   `@neutronai/trident/as-built-log.ts`"*. Attempted and reverted (`4a9275c4` → `2eaeecfa`).
   The parser also cannot simply move to `scripts/git/`, because `trident/as-built-appender.ts`
   imports it. Lint forbids one form, standalone execution forbids the other: while the
   driver exists there is no legal import.

3. **The entry-loss test can never pass, even with the import fixed.**
   `realgit.test.ts:345` expects conflict markers when one side is missing an entry. Run
   alone it emits NO driver stderr — git never invokes the driver, because a one-sided
   delete is a clean merge, not a content conflict. The refusal path
   (`as-built-merge-driver.ts:174-176`) is correct and simply unreachable.

**Consequence for sequencing:** a merge driver structurally cannot enforce an append-only
log, because git decides whether to call it. That is positive evidence for T1/T2, and it
means task 7 is not merely tidy-up — it is what makes the red disappear. Tasks 1–6 must
land together with 7 rather than leaving the driver behind in a state no change can fix.
