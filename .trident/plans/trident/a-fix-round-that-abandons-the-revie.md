# IMPLEMENTATION_PLAN — A fix round that abandons the reviewed branch must be refused (enforce ancestry, not trust)

Regenerated 2026-08-16 against main @ 4eb3adc. Verified by direct code survey before regenerating: NONE of the card's three gates exist today — `reviewed_head` / `bound_pr` / `fenced_paths` appear nowhere in `trident/` or `migrations/`; the only `merge-base --is-ancestor` in production code is `rebaseOntoObservedBase`'s base-containment probe (orchestrator.ts ~475), which answers a different question; `publishBuiltCommit` (orchestrator.ts ~1029) will happily push any branch and `gh pr create` a brand-new PR with no notion of a pinned PR or a reviewed head. The dispatch chokepoint (`dispatchBoardBoundBuild`, board-dispatch.ts) carries constraints only as PROSE inside `task`. This plan makes the fix-round contract machine-readable at dispatch and machine-enforced at publish.

Standing context the gates build ON (already true, do not re-build):
- [x] The publish path already reads the head from git (`rev-parse --verify refs/heads/<branch>`), treats a relayed sha as a check only, and refuses zero-commits-ahead — the credentialed outer boundary where the new gates belong exists and is loud (a thrown Error surfaces as `publish failed: <reason>` on `failure_reason`, applyResult ~1557).
- [x] The rebase-replay (`rebaseOntoObservedBase`) rewrites shas onto the observed base tip AFTER the local tip is read — which is exactly why every new gate must evaluate the PRE-rebase produced head (`resolvedHead`), where the reviewed head is still in history.
- [x] `git merge-base --is-ancestor X X` exits 0 (a commit is its own ancestor), so a legitimate resume/continuation of the reviewed branch passes an ancestry gate with no special-casing — the recovery-card interaction (01M04C6PKB71R3F87A1FWRS0KW) is satisfied by git semantics, to be pinned by test, not by an exemption.

Tasks, in priority order:

- [x] 1. ANCESTRY GATE + the contract columns (the load-bearing half). Migration `0124` adds nullable `reviewed_head TEXT`, `bound_pr INTEGER`, `fenced_paths TEXT` (JSON array) to `code_trident_runs` (STRICT-safe single ADD COLUMNs, forward-only, style of 0123); plumb through `trident/store.ts` (`TridentRun`, `CreateTridentRunInput`, `TridentRunDbRow`, `COLS`, `create()`, row mapping; COLS-pin test 31→34); regenerate `migrations/expected-schema.txt`. In `publishBuiltCommit`, when `run.reviewed_head` is set: `git merge-base --is-ancestor <reviewedHead> <resolvedHead>` must hold BEFORE the rebase replay; failure REFUSES the publish with a message naming both shas verbatim; an unverifiable check (bad object, malformed pin) also refuses, fail-closed, naming both values. Equality/descent passes (resume-safe). Tests pin refusal wording, the pass cases, and fail-closed.
- [ ] 2. PR IDENTITY GATE. When `run.bound_pr` is set, the PR probed for the branch (`prBefore`) must BE that PR: a different PR refuses naming both numbers; NO PR refuses too — the `gh pr create` fallback is unreachable for a bound run (opening a different PR is a refusal, not an outcome). Test pins both refusals and asserts no `pr create` command was ever issued; the bound-and-matching path publishes to exactly #N.
- [ ] 3. FENCED-PATHS GATE. When `run.fenced_paths` is a non-empty list, enforce it against `git diff --name-only` at publish: over `<reviewed_head>..<resolvedHead>` (pre-rebase) when a reviewed head is pinned — the fence governs THIS round's own work — else over the observed-base review diff already computed. Directory-prefix match (equal, or `entry + '/'` prefix). A touched fenced path refuses, naming the offending paths and the fence entries; unparseable `fenced_paths` JSON refuses loudly (fail closed, never silently unfenced). Tests: touched fence refuses; untouched fence + fenced-paths-only-in-reviewed-history passes; garbage JSON refuses.
- [ ] 4. DISPATCH SURFACE. Thread the constraints in machine-readably at the chokepoint: `BoardBoundBuildInput` gains optional `bound_pr` / `reviewed_head` / `fenced_paths` / `branch` (a fix round must continue the reviewed branch, not mint `trident/<new-slug>`); `dispatchBoardBoundBuild` validates shape (40-hex sha, positive int PR, non-empty relative paths) and REJECTS a malformed pin with zero state written; expose through both entries — the `work_board_dispatch_build` tool schema and the `/code` command — and surface the constraints in the build brief (brief-parts.ts) so the builder is TOLD the contract it will be held to, not just refused after the fact.
- [ ] 5. RESUME/CONTINUATION REGRESSION PIN. One test that runs a legitimate continuation through ALL THREE gates at once — produced head a strict descendant of the reviewed head, the bound PR probed and matched, fence untouched — and publishes; plus the equality case (republish of the reviewed head itself, the crash-recovery shape). This is the card's fourth acceptance criterion and guards the recovery card's resume path against the new gates.
- [ ] 6. DOCS. `docs/AS_BUILT.md` entry for the fix-round contract; a line in `docs/SYSTEM-OVERVIEW.md` (and `docs/INVARIANTS.md` if the gate earns invariant status): a fix-round dispatch carries reviewed head, bound PR, and fence machine-readably, and `publishBuiltCommit` refuses — never launders — a run that abandons them. Note explicitly that local-merge-mode runs have no outer publish and the contract currently binds `pr` mode only (the card's scope; state it rather than hide it).
