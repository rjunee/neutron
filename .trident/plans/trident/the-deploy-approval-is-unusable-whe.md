# IMPLEMENTATION_PLAN — the deploy approval is unusable where it lands (T5+T6 remainder)

Regenerated 2026-08-17 by the planner against `origin/main` @ `477671d7`, after clean-room validation. Governing spec: the Plan card. T1–T4 are DONE on main (PR #287, squash `8dbfcdb2`) — do not rebuild them. The old branch `trident/the-deploy-approval-is-unusable-whe` is retired (its head `e72eaf78` only moves the plan file; its T5 commit is `eeecad9d`); work happens on a FRESH branch off current main, and this plan persists to `.trident/plans/<branch-slug>.md` (the per-branch convention on main — the root `IMPLEMENTATION_PLAN.md` currently belongs to a different card and must be left alone).

**MEASURED CORRECTION (2026-08-17, planner clean-room): the card's PART 2 claim that `eeecad9d` "fails three of its own tests" is FALSE of the code.** At the un-rebased head `eeecad9d`, `bun test open/__tests__/host-deploy.test.ts` = 78 pass / 0 fail in a clean environment (checkout's own complete node_modules). The reported failures (`args_json.prompt_id` undefined, `h.retired` empty) reproduce ONLY in a mixed environment where `@neutronai/tools` resolves to a checkout that lacks `recordPromptLink` — the `(d1)` best-effort try/catch in `open/host-deploy.ts` swallows the resulting TypeError, which yields exactly those three reds and nothing else. A planner probe against the real `ApprovalManager` + `ProjectDb` further confirmed the link is race-free as written: the un-awaited `requestApproval`'s INSERT is enqueued on the ProjectDb write mutex synchronously at call time and flushes in its microtask before the emit await resumes, so `recordPromptLink`'s sync `get(id)` guard sees the row. NOTHING in `eeecad9d`'s code hunks needs changing; the remaining T5 work is the PORT onto current main.

**Cherry-pick validation (planner clean-room, `origin/main` @ `477671d7` + `eeecad9d`):** conflicts ONLY in `IMPLEMENTATION_PLAN.md`, `open/__tests__/loop-inventory-open-composer.test.ts`, `open/__tests__/loop-inventory-boot-shell.test.ts`; `docs/AS_BUILT.md` and every code file merge clean. With the count resolutions in T5 below, all six affected test files pass — 115 pass / 0 fail — and `tsc -p open/tsconfig.json` + `tsc -p tools/tsconfig.json` are green.

- [x] **T1 — the Approve/Deny prompt is raised on the topic that requested the deploy.** Merged to main in PR #287.
- [x] **T2 — a dead-grant tap re-raises a fresh approval instead of dead-ending.** Merged in PR #287.
- [x] **T3 — the typed fallback token is printed IN the approval body.** Merged in PR #287.
- [x] **T4 — an evicted prompt's tap explains itself and re-raises.** Merged in PR #287.
- [x] **T5 — LAND the host-deploy approval sweeper (`eeecad9d`) on current main.** Fresh branch off `origin/main`; `git cherry-pick -x eeecad9d` VERBATIM (do not edit its code hunks — they are green as-is). Resolve exactly: `IMPLEMENTATION_PLAN.md` → keep main's root file (`--ours`) and write THIS regenerated plan to `.trident/plans/<branch-slug>.md` instead; `loop-inventory-open-composer.test.ts` → HEAD side with "eleven running loops" → "twelve running loops" and `'12 loop(s) running'` → `'13 loop(s) running'` (the merged `EXPECTED_RUNNING_LOOPS` list merges clean and has 13 entries incl. `host-deploy-approval-sweeper`); `loop-inventory-boot-shell.test.ts` → "12 composer/graph loops" → "13 composer/graph loops" and `'13 loop(s) running'` → `'14 loop(s) running'`. Append a dated `docs/AS_BUILT.md` addendum recording the port AND the falsified-red correction (the mixed-node_modules mechanism), in a second docs commit. VERIFICATION DISCIPLINE: run `bun install --frozen-lockfile` in the worktree first — a stale or borrowed node_modules missing new workspace links (`@neutronai/mcp`, `@neutronai/calendar-core`, `@neutronai/reminders-core`, …) reproduces exactly the false-red class this card fell for ("Cannot find module @neutronai/…" is the signature). Then the six test files + package typechecks + lint + leak gate (the new plan doc must carry NO private absolute paths).
- [ ] **T6 — the approval banner must be dismissible and must not outlive its grant.** Seams (measured on the ported tree): `open/wiring/approval-notifier.ts` broadcasts ONE `agent_message` with `message_id: approval:<row.id>` to every live topic and (post-T5) refuses to announce a grant already past `ttl_ms`, but still sends no retraction; `landing/chat-react/controller.ts` holds ephemeral notices on `this.notices` (~line 450), composes `tail = [...liveStreams, ...this.notices]` sorted by `createdAt` but appended AFTER the durable transcript unconditionally (~line 1477), and clears only on reset (~line 704) — no dismissal path. Work: (a) a retraction frame keyed by the existing `message_id`, fired on the decided/expired/swept edges (T5's `sweepExpiredGrants`, `respondApproval`, `cancelPending` are the edges; either extend the `ApprovalNotifier` seam or derive the banner from the row); (b) the client removes the matching notice on retraction, gives every ephemeral notice a dismissal affordance, and stops pinning notices below messages that arrived after them; (c) a retracted/expired banner states it expired rather than vanishing silently where a durable surface exists. Envelope change lands in `channels/adapters/app-ws/envelope.ts` (+ `wire-types/` if the outbound union is pinned there). Assert: send a notice, then a user message — the notice does NOT render last; a decided/expired grant's banner disappears without a page reload; dismissal removes it; a swept grant's banner is gone after the sweep tick.
