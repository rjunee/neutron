# IMPLEMENTATION_PLAN — a gateway restart must not strand a claimed build: the recovery counter may only move when a relaunch is really attempted

Regenerated 2026-08-17 (planner: Fable) against **origin/main `477671d7`**. ⚠️ WORKSPACE BASE WARNING: the repo-of-record checkout sits DETACHED at `89de7523`, a stale lineage that LACKS #392 (`remoteAlreadyAtPublishHead`) and #311 — cut the build branch from `origin/main`, and treat orchestrator.ts line numbers as approximate there (§ markers are stable).

MEASURED DIAGNOSIS (2026-08-17T19:42:56Z incident, both lanes explained by one wedge): the recovery chain (probe → latch → `beginCrashRecovery` → §1a-crash relaunch) is present and wired (`gateway/composition/build-core-modules.ts:668`) and worked in the 08-16 23:21 / 08-17 02:02 incidents. The 19:42 end-state — run `44ad20cd` claimed once (`crash_recoveries=1`) then frozen non-terminal with no process, run `ec90f361` left latched `crashed` forever — matches a SINGLE-FLIGHT TICK WEDGE inside the first lane's post-claim `launch()`: every host git/gh command is 60s-bounded (`spawnCapture` watchdog, `trident/git-mode.ts:1186`), but the fire's settle budget (`buildSubstrateWorkflowFire`, `trident/inner-loop.ts:797-833`) only fires `handle.cancel()` and then KEEPS AWAITING the event stream — a fire turn that neither completes nor errors and whose cancel is ineffective (a warm pool mid-boot) holds the fire promise, the step, the tick body, and (single-flight, overlap-skipped) every later sweep forever. Lane A stays claimed-but-unlaunched; lane B is never stepped. The counter recorded a death observed, not work resumed. Compounding it, the shipped e2e assertion (`trident/liveness-death-e2e.test.ts:195-203`) is `recoveredOnNewGeneration || isTerminalPhase(...)` — green even when the relaunch is deleted and the run is reaped — the exact fake-pass T4 condemns; and no test pins the composition wiring line 668.

- [x] Detection half — FROZEN. External launcher-liveness probe acts only on positive `'dead'`; `alive`/`unknown` do nothing (`trident/tick.ts` `livenessBody`; latch = `TridentRunStore.crashRunningByLauncher`; push sink `open/wiring/substrates.ts` `onChildCrash`). No task below may touch it.
- [x] Atomic claim + durable budget: `TridentRunStore.beginCrashRecovery` (`trident/store.ts:628`) — one conditional UPDATE clears the latch, releases slots, nulls the dead generation, spends one unit of `crash_recoveries` (cap `DEFAULT_MAX_CRASH_RECOVERIES = 3`, `trident/orchestrator.ts:337`); sole writer of the counter.
- [x] §1a-crash recover-don't-reap in `step()` (`trident/orchestrator.ts`, "(1a-crash)" marker): harvest-first ordering, merged-PR adoption via `detectMergedPr` before any relaunch, budget-spent → terminal with the latched cause and never the token `exhausted`, bounded launch-throw retries (`MAX_LAUNCH_FAULTS = 3`).
- [x] T2 continuation primitives: `launch()` folds `inner_checkpoint`/`inner_checkpoint_head`/`inner_checkpoint_findings` + `resume_live_head` + existing-PR fold, and #392's `remoteAlreadyAtPublishHead` makes an already-fully-pushed branch a publish NO-OP instead of a failure (origin/main only — see base warning).
- [x] Production wiring exists: `orchestratorOpts.begin_crash_recovery = (id) => store.beginCrashRecovery(id)` at `gateway/composition/build-core-modules.ts:668` (currently unpinned by any test — closed by task 1).
- [x] 1. **TOP — make the relaunch provable and un-wedgeable on the production path (T1 + T4).** (a) Bound the fire settle UNCONDITIONALLY in `buildSubstrateWorkflowFire` (`trident/inner-loop.ts`): race the stream consumption against the settle timer so the budget resolves `{status:'failed', error:'fire turn did not settle within the budget'}` even when `handle.cancel()` does not end the stream (cancel still fires best-effort; exact existing outcome strings preserved; behavior byte-identical when the stream settles/errors first). (b) Replace the tautological `|| isTerminalPhase` assertion in `trident/liveness-death-e2e.test.ts` with decisive relaunch assertions: a NEW builder launch REQUESTED (fires ≥ 1), run non-terminal + `running` on the new generation, `crash_recoveries === 1`, and the fire input carrying the continuation (checkpoint/branch/PR) — deleting the §1a-crash `launch(claimed)` call turns it red with zero fires. (c) Add the two-lane wedge regression: lane A's relaunch fire never settles and ignores cancel (through the REAL `buildWorkflowFirer` + `buildSubstrateWorkflowFire`), lane B latched dead behind it — the sweep must RETURN, lane B must still be claimed AND relaunched, lane A must go terminal with the measured settle reason, never sit non-terminal with no process (pre-fix this test hangs = red). (d) Pin the composition wiring line 668 with a stripped-source assertion (pattern: `gateway/__tests__/trident-active-runs-wiring.test.ts`). Out of scope, deliberately: routing fire failures into `beginInfraRetry` (the adjacent infrastructure-retry card owns the shared classifier — sequence, don't merge).
- [ ] 2. **T3 visibility — the row must distinguish "observed dead" from "recovery attempted".** `beginCrashRecovery` currently leaves the dead generation's crash sentence in `failure_reason` on a now-recovering row (the open item inherited from `.trident/plans/trident/a-build-must-survive-a-gateway-rest.md`). Re-label at claim time inside the same atomic UPDATE — `failure_reason = 'recovering from launcher crash (attempt ' || (COALESCE(crash_recoveries,0)+1) || '): ' || COALESCE(failure_reason,'inner workflow child crashed')` — PRESERVING the latched measured cause (gateway boot timestamps) after the colon; never introduce the token `exhausted`. Update `trident/store.test.ts` (`beginCrashRecovery` describe) and the two `crash-recovery.test.ts` expectations that read post-claim `failure_reason` (lines ~185/~203); confirm the budget-exhausted reason still carries the LATCH sentence (a fresh latch overwrites the relabel on each death, so no nesting).
- [ ] 3. **Operational verification + AS_BUILT.** The card forbids marking done on symbol presence: on the deployed gateway, kill a live builder's launcher against a live run (restart the gateway service or SIGKILL only the specific `cc-trident-fire-*` child pid of that run's own generation — NEVER pkill/pattern kills) and observe the relaunch on the row (`crash_recoveries` +1, new `workflow_run_id`, non-terminal, builder process returns). Owner/live-box step; the executor's share is the `docs/AS_BUILT.md` entry recording what was built and what the live check must show.
- Commit .trident/plans/trident/a-gateway-restart-still-kills-every.md together with your code + tests.
