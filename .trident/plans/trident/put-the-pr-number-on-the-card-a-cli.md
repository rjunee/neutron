# IMPLEMENTATION_PLAN — PR number on the card: durable #NNN tag, clickable, + title-opens-doc (owner-requested 2026-08-14)

Governing spec: this Plan card (parts 1–4 + Part 1b, acceptance (a)–(i)). Re-verified 2026-08-17 against LOCAL branch `trident/put-the-pr-number-on-the-card-a-cli` (head b011eac, 5 commits, NOT yet pushed — origin's copy is the stale pre-rebase 1d105e2) vs `origin/main` (head 477671d):

- CORRECTION TO THE PREVIOUS PLAN: T3 IS DONE. The prior plan re-opened it against head 1d105e2; commit 8122846b then landed it and the tree proves it (`cwb-pr` anchor with `target="_blank" rel="noopener noreferrer"` + plain-span fallback, tag first on the meta line, `Merged #NNN · date` completed row, `.cwb-pr` CSS, 201 lines of tab tests). Checked below. The tree wins over the checkbox, in both directions.
- NEW BLOCKER (recurrence of Part 1b): the first renumber (T2.5, commit b011eac) took 0123 when main topped out at 0122 — but main has since merged `0123_code_trident_runs_crash_recoveries.sql` through `0131_code_trident_runs_base_sha_repair.sql` (128/129 are deliberate gaps). **The branch's `0123_work_board_items_pr.sql` collides AGAIN.** Next free ordinal on origin/main is 0132. The branch is 107 commits behind; 15 files overlap both sides (composer.ts drifted +838 lines on main, store.ts +185, WorkBoardTab.tsx +103 via inline-activity #263).
- SUPERSEDED MECHANISM (verified three ways: `migrations/README.md`, `runner.ts` `classifyMigration`, AS_BUILT 2026-08-17 "an ordinal is not an identity"): main's runner now reconciles by NAME — `if (ledger.names.has(migration.name)) return 'recorded-by-name'` — and `migrations/repairs.json` entry 1 already acknowledges the live v122 `work_board_items_pr` row. So when the renamed migration merges, the owner's live instance SKIPS it by name; **the Part 1b "one-time bookkeeping row" operator step is no longer required.** Per Part 1b's own rule (flag, don't silently handle), this must be PROVEN by a test modeling the live ledger and STATED in the PR description — not assumed.
- ⚠️ Unchanged prohibitions: never touch the live `_migrations` v122 `work_board_items_pr` row (#575 keeps it as the incident record); never edit `migrations/repairs.json`; NEVER run the migration runner against a real DB (no `bun run migrate`, run every bun command with `NEUTRON_HOME`/`NEUTRON_DB_PATH` unset — inheriting them is the exact #575 incident).

- [x] **T1 — durable PR on the item, written by the real detach path.** `pr INTEGER` + `pr_url TEXT` on `work_board_items`; `detachRun` writes both in the SAME transaction as status/completed_at for BOTH outcomes; `attachRun` clears; hand-reopen clears (e58880a0); `trident/repo-web-url.ts` resolver; reconcile observer resolves `pr_url`; columns on the fan literal, `AppWsWorkBoardItem`, web client type. Acceptance (c) guard drives the REAL observer → `detachRun` → store re-read.
- [x] **T2 — live `pr_url` on `run_progress`.** `pr_url: string | null` on server + wire + web mirror; `deriveRunProgress` composes `<url>/pull/<pr>`; ONE `makeRepoWebUrlCache` in `open/composer.ts` feeding both `withRunProgress` and `fanWorkBoardChanged`.
- [x] **T2.5 — first rebase + renumber 0122 → 0123 (commit b011eac).** Correct when made; ordinal 0123 has since been consumed on main. Superseded by T2.6 — the disposition itself (rebase to head, renumber to the FIRST FREE ordinal read off origin/main AT REBASE TIME, never "highest on branch + 1") is unchanged, only the number moved.
- [x] **T3 — render the tag (parts 1–3 in `WorkBoardTab.tsx`, commit 8122846b).** Meta line: `#NNN` FIRST then step tag then round; number `run_progress?.pr ?? item.pr`, URL `run_progress?.pr_url ?? item.pr_url`; anchor `target="_blank" rel="noopener noreferrer"` when a URL resolved, plain span when not, NOTHING when no PR; completed row `Merged #265 · Aug 14` from durable item fields; PR-less completed row byte-identical to today; `.cwb-pr` CSS. Tests cover acceptance (a), (b), (d) + render half of (c).
- [ ] **T2.6 — RE-rebase onto current origin/main + renumber 0123 → 0132 + prove the live-ledger name-skip (Part 1b recurrence, BLOCKING, acceptance i).** Rebase the 5-commit branch onto origin/main (@477671d or later), resolving the 15 overlapping files so BOTH sides survive (main's inline-activity #263 + refuse-done-on-write-path #363 + archived-status 0130 + the ~840 new composer.ts lines KEPT; the branch's pr/pr_url plumbing and tag rendering RE-APPLIED additively). `git mv` `0123_work_board_items_pr.sql` → `0132_work_board_items_pr.sql` (header comment line only; the two `ALTER TABLE` statements byte-unchanged; 0132 verified absent from origin/main; re-verify at push time and bump if taken). `migrations/runner.test.ts` applied list becomes `…, 126, 127, 130, 131, 132`. Regenerate `migrations/expected-schema.txt` via `bun run migrations/regen-snapshot.ts` (in-memory, safe) — must show `work_board_items` with BOTH main's archived-status shape AND `pr`/`pr_url`. NEW test `migrations/__tests__/live-ledger-122-work-board-pr.test.ts` (fixture style of `live-ledger-125-repair.test.ts`): model the live instance — full real migrations minus 0132 applied, then the incident simulated by hand (the two ALTERs + a pre-provenance `_migrations` row `(122, 'work_board_items_pr')`) — then apply the FULL real dir: must NOT throw `duplicate column`, must NOT apply 132, columns still present, still exactly one row named `work_board_items_pr` at version 122. Full suite green with scrubbed env; regenerate this plan file with T2.6 checked; `git push --force-with-lease` the rewritten branch. PR description states: exactly one migration (0132), why the ordinal moved twice, and that the merge-time operator step is superseded by name-keyed reconciliation (cite `classifyMigration` + the new test) — no row insertion, no touching the live v122 row.
- [ ] **T4 — title opens the doc; the doc-link line dies (part 4 UI, acceptance e, g, h).** In rebased `WorkBoardTab.tsx`: delete the `cwb-doc-link` element; card WITH a resolvable doc: title click calls the existing `onOpenDoc`/`openDoc` path — NO inline rename on that card at all, no new control in `cwb-actions`; card WITHOUT a doc: title click keeps today's `onStartEdit` rename exactly. No link styling on the title (cursor change on hover only). Tests: doc-card click opens doc and never enters edit; docless card still renames; no rename affordance anywhere on a doc card.
- [ ] **T5 — doc H1 → card title sync (part 4 mechanics, acceptance f).** New gateway module hooked off `DocStore.onMutationSuccess` (COMPOSED with the existing handler, not replacing it): on a `write` to `plans/*.md`, parse the first `# ` H1 — H1 wins over frontmatter; no readable H1 → NO-OP. Find the card whose `design_doc_ref` resolves to that path; update its title when different; rewrite frontmatter `title:` to match (fixpoint, no loop). Tests drive the REAL `writeDoc` path and assert: card renamed, frontmatter rewritten, filename unchanged, `design_doc_ref` still resolves, H1-less save leaves the title untouched.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
- Commit .trident/plans/trident/put-the-pr-number-on-the-card-a-cli.md together with your code + tests.
