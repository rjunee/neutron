# IMPLEMENTATION PLAN — resolve the build wrapper from the HARNESS install, not the target repo

Card: `codex-build.sh` is resolved inside the repo being BUILT (`trident/inner-workflow.mjs:1509`), so only neutron-open works (it IS the harness repo); every other project exits 127 or runs a drifted deployed copy through a hand-made symlink. The harness copy must be authoritative for every project INCLUDING Open.

- [x] T0 — Pattern already established for three sibling scripts: `checkpoint.sh`, `gh-authed.ts`, `worktree-cleanup.sh` are resolved beside `inner-loop.ts` via `import.meta.url` and threaded through `buildWorkflowArgs` (`trident/inner-loop.ts:299-327,393-402`; `trident/inner-loop.test.ts:473,489`) because the workflow script has no module resolution. This card generalizes that pattern to the codex build wrapper.
- [x] T1 — **codex-build.sh resolves from the harness install.** Export `CODEX_BUILD_SCRIPT_PATH` beside `inner-loop.ts`, thread it as `codexBuildScript` in `buildWorkflowArgs`, consume it in `inner-workflow.mjs` with NO repoPath fallback (a cli-routed build with no threaded path fails closed by name). Update the `'/repo/trident/codex-build.sh'` assertions in `trident/__tests__/cross-model-dispatch.test.ts` (lines 661, 862, 909, 930, 1024, 1028, 1041) to assert the harness path. Add the acceptance tests: a repo with NO `trident/` directory resolves AND runs the wrapper (exit 10 NOT_CONNECTED, never 127). Remove the hand-made enterprise symlink `neutron-enterprise/code/trident/codex-build.sh` + the `trident/` line in that repo's `.git/info/exclude`, and prove the wrapper still runs from the enterprise repo with no symlink. Record the change + the deploy-window caveat in docs/AS_BUILT.md.
- [ ] T2 — **codex-review.sh has the identical defect** (`inner-workflow.mjs:4679`: `const script = \`${repoPath}/trident/codex-review.sh\``) and NO symlink covers it anywhere — a configured codex review seat in any non-Open project fails today. Apply the same pattern: `CODEX_REVIEW_SCRIPT_PATH` exported from `inner-loop.ts`, threaded as `codexReviewScript`, consumed with no repoPath fallback, harness-path assertions in the review-dispatch tests.
- [ ] T3 — **kimi-review-cli.ts has the identical defect** (`inner-workflow.mjs:4715`: `const cli = \`${repoPath}/trident/kimi-review-cli.ts\``). Same pattern: `KIMI_REVIEW_CLI_PATH` threaded as `kimiReviewCliScript`; note `bunBin` is already threaded for exactly this class of reason.
- [ ] T4 — **Retire the legacy repoPath fallbacks for `checkpointScript` and `worktreeCleanupScript`** (`inner-workflow.mjs:274,278`) so NO dispatch path can ever resolve a harness script from the target repo; update the SRC-pinning tests that encode the fallback shape (`trident/inner-workflow.test.ts:244-245,1277-1279`) and any hand-built-args tests that rely on it (`inner-workflow-resume.test.ts:173-174`, `inner-workflow-built-head.test.ts:66`).
