# IMPLEMENTATION_PLAN — Forge commits to the worktree's auto branch instead of run.branch (card 01M03Y9H1KXRF8J0CWB5PBS6G1 blocker)

Root cause (measured in the repo of record): the CC Workflow tool's `isolation:'worktree'` creates the build worktree on an auto-named `worktree-wf_<id>` branch, and the ONLY thing binding it to `run.branch` is a prompt instruction (`git switch -c ${forgeBranch}`, trident/inner-workflow.mjs:1169). `refs/heads/trident/build-dispatch-is-refused-the-publi` already exists locally (tip f9425cc7, left by prior failed run 52393b74), so the relaunched fresh round's `git switch -c` collides with "branch already exists", the model commits on the worktree's auto branch, and the (correct) guard at inner-workflow.mjs:1677-1681 refuses. NOT a regression from #327 (bce629e2 touched only the orchestrator publish/shallow-heal path, not checkout creation). The fix binds the worktree to run.branch deterministically on the host side; the guard stays byte-identical.

- [x] The branch-binding guard exists and refuses a diverged build with the measured message ("committed on branch '…' but the run builds '…'. Refusing to continue: …") — trident/inner-workflow.mjs:1676-1681. KEEP VERBATIM; never weaken or special-case `worktree-wf_*`.
- [x] The wrapper measures the branch and blanks the head when the commit landed off the run's branch, naming where it ended up — trident/codex-build.sh `emit_trailer` branch gate, proven against real git at trident/codex-build.test.ts:1233 ("a commit made on the WRONG BRANCH reports an empty sha, and names the branch").
- [x] **Bind the build worktree to `run.branch` deterministically in `trident/codex-build.sh` BEFORE `codex exec` launches** (re-enter an existing local branch; create at the remote tip in pr mode when only origin has it; create fresh otherwise; DEFER exit 3 with a named `CODEX_BUILD_BRANCH_UNBOUND` reason when the bind cannot be made — e.g. branch checked out in another worktree), keep the pre-bind parked HEAD in `PRE_EXISTING_HEADS`, and update `codexBuildCoda()` in trident/inner-workflow.mjs to tell the codex build STEP 1 is already done (do not `git switch -c`). Real-git tests in trident/codex-build.test.ts prove: a build that never switches still lands its commit on run.branch; the measured leftover-local-branch incident now binds instead of colliding; a bind failure defers pre-token; the existing wrong-branch divergence test still passes unchanged.
- [ ] Make the FRESH-round Claude forge instruction tolerant of a leftover local branch: `forgeStep1(false)` (trident/inner-workflow.mjs:1169) becomes create-or-re-enter (`git switch -c ${forgeBranch} 2>/dev/null || git switch ${forgeBranch}`), so a relaunched card whose earlier run left `refs/heads/trident/<slug>` behind re-enters it instead of dying on "branch already exists"; update/keep the source-pin tests in trident/inner-workflow.test.ts (the reenter pin at :478-490 must still hold).
- [ ] Add a guard-not-dead test for the inner-workflow refusal: a source-pin in trident/inner-workflow.test.ts asserting the `reportedBranch !== forgeBranch` comparison and the exact "committed on branch … Refusing to continue" throw survive in inner-workflow.mjs (today NO test pins that guard — deleting it would go green).
