# Implementation Plan — PR #289 fix round (`trident/the-build-re-runs-1-273-test-files` @ 4523107b)

Scope: the three outstanding Argus findings on PR #289. Stay inside `trident/inner-workflow.mjs`, `trident/test-strategy.ts` and their tests. Never touch `trident/tick.ts`, `trident/board-dispatch.ts`, `trident/delivery.ts`, `trident/orchestrator.ts` (owned by other lanes). Push onto the existing branch — NO force-push, NO new PR, NO re-rebase.

- [x] Re-rebase onto current main — DONE at 03:52Z. Verified 2026-08-16: `git merge-tree --write-tree origin/main <branch>` is conflict-free, `reviewedHead` still comes only from the recorded `git rev-parse` OID (`inner-workflow.mjs:4461-4490`, `:5268`), and the branch's `fixSuiteFindings` wiring survived. Do not redo.
- [x] Finding 1 (BLOCKER) + Finding 2 (MAJOR) — make the full-suite gate fail closed: (a) a `failed-preexisting` claim without a non-empty `suiteEvidence` transcription is a BLOCKER, not a `major`; (b) when evidence exists, the finding (with the build's own transcription embedded) is rendered into the core reviewer and synthesis prompts BEFORE the panel returns, not attached after; (c) `fullSuiteFindings` reads `suiteOutcome` before honouring `testsPassed === true`, so `{testsPassed: true, suiteOutcome: 'failed-new' | 'not-run' | 'failed-preexisting'}` is a contradiction BLOCKER. Pin with tests that the old `inner-workflow-assembly.test.ts` fail-open case (bare `failed-preexisting` + mocked approvals → APPROVE) can no longer reach APPROVE, and that BOTH contradictory combinations fail closed.
- [ ] Finding 3 (MAJOR) — package-manager semantics for the tier-1 runner: `resolveTestCommand` must not hand back `scripts.test` verbatim for direct shell execution (`vitest run` only exists on PATH under the package manager's `node_modules/.bin` injection). Detect the manager (`packageManager` field, else lockfile: `bun.lock`/`bun.lockb` → `bun run test`, `pnpm-lock.yaml` → `pnpm test`, `yarn.lock` → `yarn test`, else `npm test`), render THAT as the stage-2 command, and keep probing the knobs/pin against the raw script text so `bash scripts/run-tests.sh` projects (this repo) still discover `NEUTRON_TEST_JOBS`. Reproduce the defect with a fixture whose runner exists ONLY under `node_modules/.bin`: executing the raw script string from the fixture root exits 127, executing the resolved package-manager command succeeds.
- [ ] Final verification and delivery: full suite via `bash scripts/run-tests.sh` (redirected to a log, tail only) no worse than 1525 pass / 1 pre-existing fail (`gh-authed.test.ts` fails identically on clean main); never set `NEUTRON_TEST_SHARD` (cross-runner CI slicing — it silently skips tests; `NEUTRON_TEST_JOBS` is the intra-machine knob, see `scripts/run-tests.sh:74-87`); append the round to the single canonical `docs/AS_BUILT.md` (merge=union, never re-create `docs/as-built/`); plan lives at `.trident/plans/the-build-re-runs-1-273-test-files.md` (never a root `IMPLEMENTATION_PLAN.md`); push to `trident/the-build-re-runs-1-273-test-files` without force so #289 updates in place.
