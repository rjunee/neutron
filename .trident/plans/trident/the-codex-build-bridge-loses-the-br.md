# IMPLEMENTATION_PLAN.md — codex build bridge: the brief must ARRIVE, the run id must correlate

Card: "The codex build bridge loses the brief — chunked parts are written but the PARTS env never arrives, and the run id gets mistyped."

Planner survey (2026-08-16, read-only against main @ 4eb3adc): the by-path transport shipped by #261 is complete on main — `trident/inner-loop.ts` (`buildWorkflowFirer`) writes the parts via `trident/brief-parts.ts` `writeBriefParts`, threads the manifest through `buildWorkflowArgs.briefParts`, `inner-workflow.mjs` `codexBuildPrompt` emits `NEUTRON_CODEX_BUILD_BRIEF_PARTS=` on the run command (line 1543/1575), and `trident/codex-build.sh` assembles + receipt-checks the parts (lines 652–697). The planner EXECUTED the workflow-emitted run command verbatim against the real wrapper with a stub `codex` and an exec seam that dumps the child's env: PARTS ARRIVED in the child environment, the assembled brief reached the codex seam byte-identical to the prompt's own receipt, `CODEX_BUILD_MODEL` arrived, `GH_TOKEN` was scrubbed, and the exit/trailer files landed at the exact run-id-keyed paths. Neither reported symptom reproduces on main. What the repo LACKS is exactly what the card demands: an ARRIVAL test on the BUILD dispatch — every existing suite stops at prompt-text assertions (`inner-workflow.test.ts`), hand-built env into the wrapper (`codex-build.test.ts`), or chunk-block file assembly that never executes the run command (`inner-workflow-assembly.test.ts` lockstep). A regression that drops `${partsEnv}` from the emitted command, breaks `shSingleQuote` on the newline-joined list, or drifts the run id between the artifact paths would ship green today. PR #223 owns the code-gen SELECTOR only; no open PR covers this card.

- [x] Launcher writes the brief parts by path (`trident/brief-parts.ts` `writeBriefParts`, run-id-sanitized filenames, integrity receipts) — shipped in #261, covered by `brief-parts.test.ts`
- [x] Manifest threaded into the workflow args (`trident/inner-loop.ts` `buildWorkflowArgs` `briefParts`, omitted when absent) — covered by `inner-workflow-assembly.test.ts`
- [x] Workflow emits `NEUTRON_CODEX_BUILD_BRIEF_PARTS` + one whole-file receipt on the run command; malformed manifest falls back byte-identical to full chunking — covered by the by-path suite in `inner-workflow-assembly.test.ts`
- [x] Wrapper assembles parts in order, fails closed on a missing/empty part (exit 3 `CODEX_BUILD_BRIEF_PART_MISSING`) and on a receipt mismatch (exit 3 `CODEX_BUILD_BRIEF_CORRUPT`) — covered by `codex-build.test.ts`
- [x] Prompt→receipt lockstep: emitted chunk blocks run through real bash assemble to the prompt's own receipt — 2026-08-14 AS_BUILT entry
- [x] Run id is a string end-to-end on the launcher path (`TridentRun.id: string` → `buildWorkflowArgs.runId` → part filenames) — no numeric mistype found; typed at `trident/brief-parts.ts:71`
- [x] ARRIVAL proof for the codex build dispatch (`trident/codex-build-arrival.test.ts`, NEW): execute the workflow-EMITTED chunk blocks AND run command with real bash/sh against the REAL `trident/codex-build.sh`, stub `codex`, exec seam dumping the CHILD's env + stdin; assert child-side that PARTS arrives, the brief matches the prompt's own receipt, the run id correlates through the exit/trailer artifact paths, and the negative control (PARTS assignment stripped) is REFUSED before the seam runs. Append the AS_BUILT entry recording that the two reported symptoms were re-measured and do not reproduce, and that arrival is now pinned.
- [ ] Hardening follow-up (only after the arrival suite is merged): teach `trident/codex-build.sh` an alternative `NEUTRON_CODEX_BUILD_BRIEF_PARTS_FILE` input (ordered part list read from a launcher-written manifest FILE, inline PARTS still honored) so a future workflow change can stop passing a newline-embedded env value through a model-retyped command; contract + tests in `codex-build.test.ts` only — the emitting side stays owned by the inner-workflow.mjs lane.
