# IMPLEMENTATION_PLAN — The shallow clone is back: three failures, one cause; kill the writer, self-heal at dispatch, name shallowness loudly

Regenerated 2026-08-19 against main @ 89de7523. Card: `.git/shallow` was recreated in the repo of record at 05:21 on 2026-08-19 (recurrence of 01M03CH91WA6X87XG8CS5K4H84) and produced three unrelated-looking failures. Working read-only from the repo of record, this planning round CLOSED T1 with proof:

**T1 — the writer is identified and the mechanism is reproduced.** The re-shallowing line is the "best-effort freshen" stanza duplicated verbatim in the three ratchet guards — `git fetch --depth=1 origin main >/dev/null 2>&1 || true` at `scripts/ci/depcruise-ratchet-guard.sh:56`, `scripts/ci/route-slot-ratchet-guard.sh:57`, `scripts/ci/composition-field-ratchet-guard.sh:58` (it fires whenever `MAIN_REF` is the default `origin/main`). Evidence: (1) mechanism reproduced in a scratch fixture on this box — `git fetch --depth=1 origin main` into a FULL clone writes `.git/shallow` and truncates local `origin/main` to 1 commit; (2) the incident arithmetic matches exactly — origin/main sat at 8a2a51c8 from the 03:03 deploy pull ("root … equal the deployed sha"), the 05:21 depth-1 fetch truncated there, and the 05:47 (bb976caa) + 05:55 (f2f65ddc) fetches stacked one commit each = the observed 3; (3) the reflog has NO origin/main entry at 05:21 — a `--depth` fetch of an unchanged tip moves no ref, and `>/dev/null 2>&1 || true` hid the rest, so only `.git/shallow`'s ctime testified; (4) repo-wide grep: these are the only `--depth` fetches that can reach the real origin with default env — the guard test files use fixture ROOTs and `MAIN_REF='main'` which skips the stanza entirely, and the realgit tests depth-clone only their own scratch origins; (5) the amplifier is that ~140 `.claude/worktrees/wf_*` worktrees share the ONE `/code/.git` common dir, so ANY lane pre-verifying the CI guard scripts locally (they are ci.yml steps) shallows the whole repository for every other lane. The stanza's comment says "on a shallow checkout" — it was written for CI's depth-1 `actions/checkout`, where `--depth=1` is correct; run against the full shared checkout the same line is destructive. The specific 05:21 invoker is unrecoverable by design of the `>/dev/null` and does not matter: the line is the defect.

- [x] (prior card 01M03CH91WA6X87XG8CS5K4H84 — verified still in place, do NOT rebuild) install.sh provisioning: clone has no `--depth` (pinned by tests/integration/install-clone-origin.test.ts:159) and the existing-checkout update path heals shallowness before pulling (install.sh:1374–1375).
- [x] (prior card — verified still in place, do NOT rebuild) `healShallowCheckout` runs at the top of `rebaseOntoObservedBase` (trident/orchestrator.ts:934–1001) and throws loudly with depth + boundary when it cannot heal; realgit shallow fixtures assert the anti-fake `.git/shallow`-exists precondition.
- [x] (in flight on PR #401's branch `trident/as-built-conflicts-killed-at-the-ro`, commit ec6832e2 — that lane owns `scripts/ci/as-built-write-guard.sh`; this card MUST NOT touch it) CI-side self-heal for sites 2+3: `ensure_commit` fetches the named sha, `ensure_history` unshallow-or-deepens before the three-dot diff.
- [x] **TOP — T2(kill the writer) + T4(test that fails without the fix) + T1 write-up:** make the freshen stanza in all THREE ratchet guards incapable of introducing shallowness — `--depth=1` ONLY when `git rev-parse --is-shallow-repository` already says `true` (preserves the CI arm ci-workflow.test.ts documents), plain `git fetch origin main` otherwise, still best-effort `|| true`; add to EACH of the three guard test files (a) the regression test proving a FULL fixture clone stays non-shallow across a guard run with `MAIN_REF` explicitly `'origin/main'` (RED on the current stanza — red proof must actually be performed and reported), and (b) a shallow-checkout test with the anti-fake `.git/shallow`-exists assert proving origin/main becomes resolvable (the CI arm still works); update the stale header comment at depcruise-ratchet-guard.sh:16; append the dated T1 root-cause entry to docs/AS_BUILT.md.
- [ ] **T2(self-heal where the checkout is prepared):** in `ensureProjectBuildWorkspace` (trident/build-workspace.ts — the dispatch chokepoint every board-bound build resolves its workspace through, i.e. "once, before any lane uses it"), on the healthy-repo path probe `git rev-parse --is-shallow-repository`; on `true` run `git fetch --no-tags --unshallow origin`, re-probe, and THROW (fail-closed → `backend_error`, no run row) naming the measured depth and `.git/shallow` if still shallow; a healthy checkout pays one probe. Update stub probes in trident/build-workspace.test.ts to answer the new call and add heal/refuse/no-op tests. Defense in depth is required even after the TOP task: stale worktrees carry OLD copies of the guard scripts until their branches rebase, so the writer can transiently return.
- [ ] **T3(loud diagnosis):** in trident/merge.ts, when an ancestry-bearing host step fails (the local `git merge`/checkout/fetch steps flowing through `must()` at merge.ts:221), probe shallowness and append a one-line diagnosis to the `TridentMergeError` message — "the checkout is SHALLOW (`.git/shallow` exists); ancestry results are invalid — run `git fetch --unshallow`" — so tonight's failure (1) ("refusing to merge unrelated histories") names its cause; realgit test: shallow fixture, drive the local-merge failure, assert the message contains "SHALLOW"; full-clone control asserts no such suffix.

Guard rails: do NOT loosen any guard (a guard that skips on an unresolvable sha is the rubber-stamp failure of 01M09YXYZA9CVE9ZC4RDNJ4V12); do NOT touch `.github/workflows/` (no workflow scope — CI self-heals from inside scripts, as ec6832e2 does); do NOT touch `scripts/ci/as-built-write-guard.sh` (PR #401's lane owns it); do NOT re-shallow anything in tests outside fixture-local scratch repos; keep every fetch in the guards best-effort (`|| true`) so offline/fork runs still skip rather than block.

## Do not (copied verbatim from the card's plan doc)

_Source: `docs/plans/the-shallow-clone-is-back-and-it-bit-three-different-places-qk2cdf.md`. Do not paraphrase this section._

- Do not "fix" this by making guards lenient. `as-built-write-guard.sh` refusing was CORRECT;
  the checkout was wrong. A guard that skips when it cannot resolve a sha is the rubber-stamp
  failure this board already carries a card about (`01M09YXYZA9CVE9ZC4RDNJ4V12`).
- Do not assume CI and this box need the same fix. `actions/checkout` depth is set in
  `.github/workflows/`, which no agent here can write (no `workflow` scope, tested); the CI
  side must self-heal from inside the script, as `ec6832e2` does.
