# IMPLEMENTATION_PLAN — REQUEST_CHANGES must mean a reviewer spoke: the REVIEW_NOT_RUN terminal state, the empty-findings write guard, and the dead `round` column

Regenerated 2026-08-19 (round 3) against branch `trident/root-cause-of-non-autonomy-a-run-th` @ 36920352 (base = origin/main @ 5c4bb201). Card: a crashed run and a rejected build are indistinguishable — 95 of 146 REQUEST_CHANGES rows carry zero findings; the value is written whenever a run dies before the reviewer speaks. Reserve `REQUEST_CHANGES` for a reviewer that actually produced a verdict; record everything else as the new `REVIEW_NOT_RUN`; make the store refuse a findings-free rejection; make `round` tell the truth.

NOTE (new this round): main advanced one commit to ec4eb39f (#440, planner-select observability + `Number(ralphRound)` coercion). It edits trident/inner-workflow.mjs around line ~5729 — a DIFFERENT region from every T3 site — and coerces the OUTER re-fire counter `ralphRound`, not the inner `round` column, so T6's scope is unchanged. Do not rebase mid-task; the publish path reconciles.

LOCKED DECISIONS (hold across iterations):
- New verdict value is `REVIEW_NOT_RUN` (one value, not two — the infra/genuine split already exists in `block_kind`/`terminal_cause`/`classifyInnerFailure`).
- ONE discriminator, used by every writer: recorded terminal verdict = `APPROVE` iff server-gated APPROVE; `REQUEST_CHANGES` iff `result.verdict === 'REQUEST_CHANGES'` AND `block_kind === 'code'` AND recorded findings parse to a non-empty array; else `REVIEW_NOT_RUN`. (Shipped orchestrator-side in T1 as `recordedTerminalVerdict`, trident/orchestrator.ts:587.)
- Checkpoint VOCABULARY untouched (`argus-approved`/`argus-request-changes-round-N`/`fix-round-N` drive the resume matrix). The orchestrator no longer FABRICATES `'argus-request-changes'` as a checkpoint fallback (shipped in T1) — null is honest.
- NO backfill of the 195 historical rows — they are the measurement evidence; migration 0137 is structural only (CHECK widened via STRICT-table rebuild; landed in T1).
- `InnerResult.verdict` stays `'APPROVE'|'REQUEST_CHANGES'|null` (the reviewer's own word; null = never spoke). Only the RECORDED `inner_verdict` gains the third value. `parseInnerResult`'s `normalizeVerdict` already decodes anything non-APPROVE/non-REQUEST_CHANGES to null — no TS decoder change needed for T3.
- A REQUEST_CHANGES whose findings are EMPTY records `REVIEW_NOT_RUN` by design (an empty finding set is approval or infrastructure failure, never a rejection) — the nit-gate's findings-free REQUEST_CHANGES included. Do not "fix" this asymmetry.
- `round` is FIXED, not dropped. Main's #416 canary landed the store-side fix (`checkpointRound` wired monotonically into `update()`/`save()`/`saveIfActive()`); T6 is only the remainder: checkpoint.sh + .mjs threading + the terminal fold of `result.round`.
- Auto-retry stays keyed on `classifyInnerFailure` (budget/backoff, #367). The null-verdict stops still classify `infrastructure` via `block_kind='infra-only'` + measured cause (classifyInnerFailure never keys on the fabricated verdict), so T3 changes no retry behavior — only the record.
- Test-side sims of the DETACHED writer (checkpoint.sh) model it OUT-OF-BAND: the simulated `inner_verdict` lands via raw SQL, never through the guarded store API. After T3 the sim mirrors the FIXED writer's discriminator (same raw-SQL seam).

Already satisfied (verified in tree, do not redo):
- [x] Infrastructure auto-retry with a durable budget + backoff, fired BEFORE the terminal stamp (`classifyInnerFailure`, `beginInfraRetry`, migration 0126, #367).
- [x] Launcher-crash recovery budget separate from fix rounds (`beginCrashRecovery`, migration 0123).
- [x] Measured-cause plumbing: `block_kind` + `terminal_cause` travel in the terminal result; `innerTerminalFailureReason` quotes only measured causes.
- [x] A REQUEST_CHANGES with genuine findings does NOT auto-retry (`classifyInnerFailure` fail-closed to 'genuine'; pinned in infra-retry tests).
- [x] Genuine-review findings are recorded (`argus-request-changes-round-N` checkpoint writes `inner_checkpoint_findings` atomically via checkpoint.sh).
- [x] Store-side `round` derivation from checkpoints (main's #416 canary: `trident/checkpoint-round.ts` + monotonic `MAX(round, …)` in all three store writers).

Build queue, priority order:
- [x] T1 — `REVIEW_NOT_RUN` end-to-end on the ORCHESTRATOR side (committed e49fa766): migration 0137 rebuilds the STRICT table and widens the `inner_verdict` CHECK to `('APPROVE','REQUEST_CHANGES','REVIEW_NOT_RUN')`; `recordedTerminalVerdict` used at every orchestrator fabrication site (catch-all applyResult, provenance gate, infra-budget exhaustion, Ralph cap, `failedRun` default); fabricated checkpoint fallback dropped; nexus `decision` event gate excludes `REVIEW_NOT_RUN` while the handoff event carries it; verdict unions widened in run-progress.ts + wire-types; schema/snapshot/orchestrator/store/run-progress/crash-recovery/infra-retry/nexus tests updated.
- [x] T2 — The STORE refuses a findings-free rejection (committed 36920352): `update`/`save`/`saveIfActive` throw typed `TridentEmptyFindingsRejectionError` when the effective post-write state would be `inner_verdict='REQUEST_CHANGES'` with NULL/`[]`/unparseable findings; `parseCheckpointFindings` extracted to leaf `trident/checkpoint-findings.ts`; sims re-pointed at raw SQL (`writeSimulatedResult`, `harvestRawResult`); FALSIFICATION-2 RED tests named per method; infra-retry claim clears stale raw verdicts atomically.
- [x] T3 — Stop the WORKFLOW fabricating the verdict at source (trident/inner-workflow.mjs). `writeTerminalResult` line 2235 (`result.verdict === 'APPROVE' ? 'APPROVE' : 'REQUEST_CHANGES'`) maps the `inner_verdict` column write by the ONE discriminator (APPROVE / REQUEST_CHANGES only when `result.blockKind==='code'` with non-empty `result.findings` / else REVIEW_NOT_RUN); `terminalResult` (~6439) carries `synthesis.findings` on the genuine non-round-lost REQUEST_CHANGES path so the writer has its evidence; the resume bounded stop `stopResult` (~5488), `builtHeadStop`'s stop (~5639) and the thrown-path `failureResult` (~6470) carry `verdict: null` (a throw is not a review verdict — the file already says so; the one-failure-class comment at ~5612 updated to match); publish/refire handoff JSON verdicts stay `'REQUEST_CHANGES'` (belt-and-suspenders — but their COLUMN write now lands REVIEW_NOT_RUN via the discriminator); APPROVE/merged/resume-approved paths byte-stable. `writeSimulatedResult` + `SimResult` (inner-loop-sim.ts) model the FIXED writer (optional `blockKind`/`findings`, same discriminator in the raw UPDATE). Tests: inner-workflow.test.ts pin at 1620 flips to `verdict: null` + a named RED-on-revert discriminator source test; inner-workflow-built-head.test.ts stop assertion (~415) flips to null + terminal-result prompt asserts `inner_verdict 'REVIEW_NOT_RUN'`; new falsification-3 behavioral test (maxRounds=1 genuine rejection) proves the honest path still writes `inner_verdict 'REQUEST_CHANGES'` with findings; stale comment at orchestrator.test.ts ~1072 reworded.
- [ ] T4 — Honest READERS (remainder after T1): trident/delivery.ts gains a review-never-ran failure class keyed on the RECORDED verdict (stop classifying never-reviewed exits by failure-reason substrings); widen the verdict normalizers in landing/chat-react/work-board-client.ts:113,422 and app/lib/work-board-client.ts:92,385 (both currently collapse `REVIEW_NOT_RUN` to null) so the board renders "review never ran" rather than a blank verdict.
- [ ] T5 — Retry-policy coherence tests: pin that every `classifyInnerFailure='infrastructure'` terminal (budget exhausted) records `REVIEW_NOT_RUN` with a reason naming the budget; pin the mutant that a genuine `REQUEST_CHANGES` (block 'code', findings non-empty) NEVER enters the auto-retry claim; document that a terminal `REVIEW_NOT_RUN` card is safe to re-dispatch without code changes (resume via `detectExistingPr` already carries the PR).
- [ ] T6 — Make `round` tell the truth for LIVE and crash-terminal rows (falsification 4; the remainder #416 deliberately excluded): add `round` to trident/checkpoint.sh's field whitelist (integer-validated like `pr`, monotonic `MAX(round, N)` like the store) and thread it from the .mjs fix-round checkpoints so a LIVE multi-round run shows round > 1 mid-flight; `applyResult` folds the workflow-reported `result.round` into every terminal run it commits; update the `round` doc comment; assert a simulated multi-round run commits round > 1 even when the terminal checkpoint carries no round.
- [ ] T7 — Record the invariant: docs/INVARIANTS.md line for "REQUEST_CHANGES requires non-empty recorded findings; REVIEW_NOT_RUN is the no-reviewer terminal" with file:line + test anchors; docs/SYSTEM-OVERVIEW.md trident section updated; AS_BUILT entry (executor's normal per-merge append).
- Commit .trident/plans/trident/root-cause-of-non-autonomy-a-run-th.md together with your code + tests.
