# IMPLEMENTATION_PLAN — salvage grows a working-tree leg: the commit-scoped sweep must see uncommitted work (card: port the lane shepherd's snapshot into trident)

Card: `sweepStrandedFailures` / `reconcileStranded` (PR #389, merged as `ec3bcc56`, now HEAD of origin/main) decide "did this failed run build anything" from `git rev-list --count base..HEAD` — commit-scoped only. The one measured loss (run `9d115490`, 292 lines, ZERO commits, working tree only) is invisible to it. Port the shepherd's mechanics INSIDE `reconcile_stranded`: a `git stash create` snapshot of a dirty worktree, anchored by a ref in the shared store and named on the run row, plus `git stash list` consultation before any built-nothing conclusion.

Planner survey 2026-08-19 against origin/main `ec3bcc56` (verified by grep + read, not by presence): `trident/orchestrator.ts` contains ZERO occurrences of `stash` or `status --porcelain` on the salvage path; `reconcile_stranded` (line 2123) gates on `rev-list --count` alone and returns null (silently, unannotated) for the 0-commit case; both call sites are live (terminal choke point `step()` line 3265, boot sweep wired at `gateway/composition/build-core-modules.ts` init). Sandbox-verified mechanics: `git stash create` returns EMPTY for untracked-only changes (they must be counted by porcelain but cannot be object-captured); with tracked changes it returns an OID and leaves `rev-parse HEAD` + `status --porcelain` byte-identical; an OID created in a linked worktree is anchorable from the main checkout via `update-ref refs/tags/...`; `git stash list` is shared across worktrees and entry lines carry `On <branch>:` / `WIP on <branch>:`; `git worktree list --porcelain` locates a run's worktree by `branch refs/heads/<name>` stanza.

- [x] PREREQUISITE — #389 (`sweepStrandedFailures`/`reconcile_stranded`) merged to main as `ec3bcc56` and invoked on the real composition path (verified end to end per the card, 2026-08-19T12:17Z). Build against current origin/main; extend, do not reimplement.
- [x] THE CARD (Findings 1+2): extend `reconcile_stranded` in `trident/orchestrator.ts` with a working-tree leg — locate the run's worktree by branch via `worktree list --porcelain`, snapshot a dirty tree with `git stash create`, anchor the OID at `refs/tags/trident-salvage/<run_id>` in the shared ref store, and report BOTH legs in the terminal record ("0 commits, N uncommitted line(s) across M file(s)" is never rendered as built-nothing); when the tree is clean and 0 commits, consult `git stash list` for branch-scoped entries before concluding nothing, and annotate "work parked in stash" instead. New reason text is delivery-classifier-safe (no forbidden tokens, no raw stash messages, no branch names in new suffixes). Pinned by realgit falsification tests in `trident/stranded-salvage-realgit.test.ts` (uncommitted-only → recorded, leg deleted → red; stashed → not built-nothing; stash create proven non-mutating byte-for-byte; no push/no gh in the new legs). AS_BUILT entry. `tools/lane_review.sh <branch>` exits 0.

Out of scope, by the card's own text — do not build here:
- FINDING 3 (dirty-file count as positive liveness for the hang watchdog) belongs to card `01M08S2HFG`; #442 already landed the stage-event stand-down seam it feeds.
- Retention/GC of salvage snapshots is a separate card; v1 never deletes them — losing work is the failure being fixed.
- No pushes from the capture legs: snapshot capture is LOCAL (tag + reason text). Publishing commits stays the existing outer-loop `publishBuiltCommit` path.

Known limitation, recorded deliberately: `git stash create` cannot capture untracked-only files (sandbox-verified). The disposition string therefore counts untracked files by name (`N untracked file(s) counted by name only`) so the record is honest about what the snapshot holds; full untracked capture would need a temp-index `write-tree`, which the `RunHostCommand` seam (no env threading) cannot express — a follow-up card if ever wanted, not this one.

remaining after top task: 0 — single-card, single-iteration change.
