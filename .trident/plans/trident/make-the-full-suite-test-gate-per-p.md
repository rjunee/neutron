# IMPLEMENTATION_PLAN — Make the FULL-SUITE test gate PER PLAN, not PER TASK

Regenerated 2026-08-18 against main (89de752). Card: intermediate Ralph tasks (remaining_tasks > 0) get the stage-1 blast-radius gate; ONLY the terminal task (remaining_tasks == 0) — and every non-Ralph run — carries the full-suite requirement, byte-identical to today. Fail CLOSED to the full suite whenever the discriminator or the intermediate block is missing. Measured cost being removed: 3,511s / 1,728s of duplicate suite time per round (runs 9bece714, 6827425d). Scope: test-strategy.ts + the inner-workflow contract only — no orchestrator Ralph-loop refactor, no publish/rebase changes, no touching the brief-part receipt, liveness dead-only policy, or migration ownership refusal. The fullSuiteFindings gate in inner-workflow.mjs is deliberately UNCHANGED (its recording at intermediate forge-done checkpoints is the fail-closed net for the crash-resume-into-review edge).

- [x] 1. `trident/test-strategy.ts`: add the INTERMEDIATE (subset) render scope — `scope?: 'full-suite' | 'subset'` on `TestStrategyInput` + `buildTestStrategy`, pinned `INTERMEDIATE_SUITE_DEFERRED` / `INTERMEDIATE_REPORT_RULE` exports (testsPassed keeps ONE meaning: full suite only; intermediates report testsPassed=false, suiteOutcome='not-run'), stage-1 section shared verbatim between scopes, `buildTestStrategyDetail` returns a new `intermediate_block`; default/full-suite output BYTE-IDENTICAL (unit-pinned); zero edits to existing assertions in `test-strategy.test.ts`.
- [ ] 2. Thread the intermediate block launcher→workflow: `trident/orchestrator.ts` fire path derives `test_strategy_intermediate = detail.intermediate_block` beside `test_strategy` (both null on the catch path); `trident/inner-loop.ts` `FireWorkflowInput.test_strategy_intermediate?: string | null` and `buildWorkflowArgs` maps it to `testStrategyIntermediate` ('' when absent/null, mirroring `testStrategy` at line 476); threading tests in `orchestrator.test.ts` (fire-input carries it, pattern at lines 2752-2789) and `inner-loop.test.ts` (verbatim + '' default, pattern at lines 611-620).
- [ ] 3. `trident/inner-workflow.mjs`: per-iteration suite-scope selection — destructure `testStrategyIntermediate = ''`; `forgeBuildContract(reenter, checkpointName, suiteScope = 'full-suite')` splices the intermediate block IFF suiteScope==='subset' AND testStrategyIntermediate !== '' (else the full block — fail closed); r1 call site passes 'subset' IFF `ralph === true && ralphRemaining > 0`; fix-round call site untouched (always full); step-3/report text three-way branch with the '' and full-suite branches byte-identical to today; one `log()` line naming the chosen scope and the reason (intermediate N remain / terminal / non-ralph / fail-closed: no intermediate block).
- [ ] 4. Acceptance matrix over the REAL `.mjs` body (new prompt-capture harness file cloning `runRalph` from `inner-workflow-ralph-refire.test.ts` + the capture pattern of `inner-workflow-assembly.test.ts`, distinct marker strings per block): (a) remainingTasks=2 → forge:build prompt carries the SUBSET marker, not the FULL marker; (b) remainingTasks=0 → FULL marker, not SUBSET; (c) a 1-task plan (remainingTasks=0 on its only task) → FULL; (d) plan relay OMITS remainingTasks (undefined/non-finite) → FULL; (e) remainingTasks=2 but no testStrategyIntermediate threaded → FULL (fail-closed on the missing block); (f) fix-round prompt always carries the FULL block; (g) terminal step-3 contract text unchanged. Verify falsifiability: (a) goes RED with the production change reverted; (b)/(c)/(d) go RED against the `> 0` → `>= 0` selection mutant. Record which in the PR body.
- [ ] 5. Wiring proof + record: run `bash tools/lane_review.sh <branch>` and confirm every new exported symbol has a NON-TEST production caller (orchestrator.ts consumes `intermediate_block`; inner-loop.ts maps it; inner-workflow.mjs selects it); put the caller path + the verified-red test in the PR body; append the AS_BUILT entry with the measured duplicate-suite numbers this removes.
