# IMPLEMENTATION_PLAN — a finished build must not be thrown away (trailer read/write race)

Card: run `8620a7d1` / `01M04GAEN7WX9M5D93CTWPFBR0`. The wrapper writes the trailer non-atomically (`printf ... > "$TRAILER_FILE"` in `emit_trailer`, trident/codex-build.sh); the bridge's wait loop fires on `test -s` at the first byte (trident/inner-workflow.mjs:1614); the orchestrator then classifies exit-0 work as DEFERRED with three unmeasured claims (inner-workflow.mjs:1654-1657 — the exact incident message). Close the race at the writer (atomic rename), at the reader (exit-code consultation), salvage stranded commits, and make the terminal message quote only what was read. Per the card: do NOT fix this by extending a timeout.

- [x] Precondition (already in place): the detached supervisor records the wrapper's exit status to the `.exit` file on every run (`printf "%s\n" "$rc" > "$status"` in the launch command, inner-workflow.mjs:1611) — the exit-code file the reader-side fix consults exists.
- [x] Precondition (already in place): the trailer has its own file separate from stdout (`NEUTRON_CODEX_BUILD_TRAILER_FILE`), so atomic publication is a rename of that one known path.
- [x] T1 — WRITER closes the race at the source (card item 1): `emit_trailer` publishes the trailer atomically. Write the six measured lines to `"$TRAILER_FILE.tmp.$$"` and `mv -f` (rename(2), same directory/filesystem) into place; prove pre-launch writability on the temp path and `rm -f` any stale final trailer before codex launches, so a reader sees either no trailer or a complete one — never a partial or stale one. Tests in trident/codex-build.test.ts: concurrent-observer proves the first non-empty read is the complete six-line trailer; a build that plants garbage at the final path is atomically replaced; no `.tmp.*` residue on the ok or failed path; exit-3 CODEX_BUILD_TRAILER_UNWRITABLE preserved.
- [ ] T2 — READER consults measurements before classifying (card items 2 + 4): in `forgeAgent` (inner-workflow.mjs), when `trailerComplete !== true` and `wrapperExitCode` is not in {3,10,11}, do NOT throw immediately — dispatch a fixed verification probe (same raw/exit_code single-Bash-command probe pattern as `classifyPrMerged`) that cats the exit file, counts the trailer's bytes (`wc -c`), and cats the trailer between markers; a pure exported classifier decides: exit-file content `0` plus a trailer parseable as the six `NEUTRON_CODEX_BUILD_*` lines = SUCCESS — adopt the probe-read trailer values (branch/commitSha/prNumber/diffFile/worktreePath) and continue the run. Only when verification fails does the run stop, and the replacement terminal message asserts ONLY measured facts and QUOTES the exit-file content and trailer byte count actually read — the phrases "killed before it could report", "empty or missing", and "holds uncommitted work" may appear only when the quoted measurement shows them. Acceptance tests: a trailer written slowly (partial, then complete) is not classified DEFERRED; exit=0 outranks a missing/partial trailer; the terminal message quotes the exit code and trailer bytes read.
- [ ] T3 — SALVAGE a built-but-stranded commit (card item 3): when the trailer is genuinely absent/unusable but the probe shows the forge branch has commits ahead of base (`git rev-list --count <base>..<branch>` > 0), adopt the measured branch head (`git rev-parse <branch>`), regenerate the diff file from `<base>..<branch>`, and let the run proceed to the outer publish path instead of discarding the commit. Acceptance test: a committed-but-unpushed worktree is published, not discarded.
