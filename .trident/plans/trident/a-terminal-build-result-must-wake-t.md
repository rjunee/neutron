# IMPLEMENTATION_PLAN.md — terminal build result must wake the agent

Card: a terminal `code_trident_runs` row must produce (a) the existing chat card AND (b) exactly one agent turn bound to its board item, idempotent across redelivery/retry/gateway restart. Plus: `PR #0` (the no-PR sentinel) must never render.

Verified current state (repo of record @ main):
- `trident/delivery.ts` posts the terminal card to the originating chat topic via `OutboundSink` — nothing in the repo starts an agent turn on a terminal run (no wake path exists).
- Both PR render sites (`composeTerminalDelivery` `done` case ~line 334 and `failed` trail ~line 345) guard only on `run.pr !== null`, so `pr = 0` (the no-PR sentinel) renders "PR #0". Card `01M01HGAWHA1KBK7CXXHC4R6RH` has NOT landed this fix — no reference to it exists in the tree.
- Wake precedent exists and is proven: `gateway/proactive/work-wakeup.ts` composes an `AgentSpec` on the owner's warm chat session via `buildSubstrateReminderLlm(liveAgentSubstrate)` (keyed by `metering_context.project_id`), posts the reply through `deliver`, and grants `LIVE_AGENT_TOOL_NAMES` verbatim (a narrower `--tools` surface evicts the warm REPL).
- Idempotency substrate precedent exists: `crash_recoveries` (migration 0123) — a nullable column with ONE atomic-claim writer on `TridentRunStore`, deliberately absent from `TridentRunUpdate`/`update`/`save`/`saveIfActive`.
- Terminal observers run at THREE assembly sites: the tick loop (`gateway/composition/build-core-modules.ts` `composeTerminalHook(delivery, [boardReconcile, on_run_terminal])`) and the two `buildTridentTerminator` assemblies in `open/composer.ts` (~5557, ~5578). Board reconcile clears `linked_run_id` on `done` but preserves it on failure — so a wake observer must run BEFORE reconcile to resolve the board item for done runs.

Checklist (prioritized):

- [x] Terminal chat-card delivery to the originating chat topic, per-run `channel_kind` routing, failure interpretation (#352/#361) — `trident/delivery.ts`, wired at all three terminal assemblies. Acceptance criterion 2 already holds; do not touch the card path.
- [x] A run with no originating chat context no-ops cleanly (`topicForRun` null guard + tests) — acceptance criterion 4 holds for the chat path; the wake observer must replicate the same `chat_id` gate.
- [x] Board reconcile on terminal runs (`work-board/store.ts detachRun`; `getByRunId(project_slug, run_id)` resolves the bound item).
- [x] T1 — Durable at-most-once wake claim + PR #0 sentinel guard: migration `0125` adds nullable `agent_waked_at INTEGER` to `code_trident_runs`; `TridentRunStore.claimAgentWake(id)` is its ONLY writer (atomic `UPDATE … WHERE agent_waked_at IS NULL AND phase IN <terminal>`, returns won/lost); regen `migrations/expected-schema.txt`; add `run.pr > 0` to BOTH `composeTerminalDelivery` PR render sites; unit tests for claim semantics and for "PR #0 never appears" (acceptance criterion 5).
- [x] T2 — Wake observer module `gateway/proactive/terminal-build-wake.ts`: exported `buildTerminalBuildWakePrompt` (run id, board item id or "none", terminal phase, `failure_reason` VERBATIM, branch, PR only when `> 0`, task title, imperative act-now instructions modeled on `buildWakeupPrompt`) + `buildTerminalBuildWakeObserver(deps)` returning `(run: TridentRun) => Promise<void>`: gate on `isTerminalPhase` and `run.chat_id` (no chat → no-op, no claim — acceptance criterion 4), gate on substrate availability, CLAIM FIRST via injected `claimWake(run_id)` (lost claim → silent no-op — this is what makes redelivery/retry/boot-replay produce zero duplicate turns), resolve board item via injected `boardItemIdForRun`, compose one turn on the `WakeupLlm`-shaped seam with `metering_context.project_id` = the run's project chat scope, `LIVE_AGENT_TOOL_NAMES`, `getBestModel`; post the agent's reply via injected `post` (buzz-suppressed for `done`, loud for `failed`). Unit tests MUST include: re-running the observer over an already-claimed terminal row produces no second turn (acceptance criterion 3), no-chat no-op, prompt carries all required facts, compose failure after claim is logged not thrown.
- [x] T3 — Wiring: construct the observer in `open/composer.ts` (after `deliver` + `workBoardStore` + `liveAgentSubstrate` exist, near the work-wakeup wiring; production deps: `boardRunStore.claimAgentWake`, `workBoardStore.getByRunId(run.project_slug, run.id)`, `buildSubstrateReminderLlm(liveAgentSubstrate)`, `deliver(resolveAppWsReminderTopic(...))`, `workBoardProjectIdForKey`), late-bind via a holder (the `boardTerminatorHolder` pattern) and thread it into ALL THREE terminal assemblies ORDERED BEFORE board reconcile: a new optional `trident` wiring field consumed by `gateway/composition/build-core-modules.ts` (observers list `[wake, boardReconcile, runTerminalObserver]`) and first in the observer arrays of both `buildTridentTerminator` assemblies. Wiring test: one terminal transition → exactly one agent turn + the unchanged chat card; re-delivery over the same row → card logic unchanged, zero additional turns (acceptance criteria 1–3).
  LANDED AS (#385, on main): the wiring field is `on_terminal_wake`, the observer is constructed ONCE in `open/composer.ts` and registered at all three sites, and it is ordered LAST in each chain (not first) so a multi-minute wake compose never delays board reconcile or skill-forge — `detachRun` keeps `linked_run_id` on both outcomes (#340), so the board item still resolves after reconcile.
- [ ] T4 — Docs: `docs/AS_BUILT.md` entry (newest-first) + update the terminal-delivery section of `docs/SYSTEM-OVERVIEW.md` to name the agent recipient; note that the PR #0 guard landed here so card `01M01HGAWHA1KBK7CXXHC4R6RH` must not re-do it.
- Commit .trident/plans/trident/a-terminal-build-result-must-wake-t.md together with your code + tests.
