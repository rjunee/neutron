# IMPLEMENTATION_PLAN — A stalled build must restart itself (checkpoint durability · local-ref recovery · stall restart · cancel authority)

Regenerated 2026-08-16 against origin/main `6f9df1e7`. ⚠️ The shared checkout's local `main` (4eb3adc) is BEHIND origin — fetch and branch from origin/main; never plan or diff from local main. Evidence base: run 6f794290 (branch ref 03:09:16Z, review diff 03:10:27Z, checkpoint EMPTY until 03:41:11Z → cancelled as dead with finished work on a local branch) and run e7e87cb3 (`codegen_cancel` → `{cancelled:true, phase:"stopped"}`, then the run rebased #289's branch and opened PR #316).

Already satisfied on origin/main (verified in code, not assumed):

- [x] DETECTION (#312, commit f66dbf91): the 15 s `trident-liveness` loop probes each in-flight run's launcher generation via the REPL supervision registry (`open/wiring/trident-launcher-liveness.ts`, `trident/tick.ts` livenessBody); a POSITIVE dead answer latches the run `crashed` in seconds; 'unknown' never reaps.
- [x] HARD-DEATH RESTART (#267, wired to #312's latch): `orchestrator.ts` step() §1a-crash claims a `crashed` row via `beginCrashRecovery` and RELAUNCHES it as a continuation, bounded by the durable `crash_recoveries` column against `DEFAULT_MAX_CRASH_RECOVERIES = 3`; budget exhaustion goes terminal naming the last crash.
- [x] CANCELLED-ROW LIVENESS FREEZE (#175/#177): `trident/checkpoint.sh` freezes `subagent_status`/`last_advanced_at` on a terminal row while still recording branch/pr/checkpoint/result, so an orphaned workflow cannot fake liveness after a cancel — but note #177's own comment concedes the orphan KEEPS RUNNING, which is the unfixed half T5 below closes.

Queued, in priority order:

- [x] **T1 — ARTIFACT-TIME CHECKPOINT (defect 1, the root).** The `forge-done`/`fix-round-N` checkpoints are written by `agent()` steps only AFTER the build seat returns (`inner-workflow.mjs` ~4600/~5226), so a run whose branch + diff are already on disk is indistinguishable from a hard death for the whole tail of the phase (31 min on 6f794290). Extend `forgeBuildContract(reenter)` with an artifact-time step: immediately after the commit lands and the diff file is written, the forge/fix agent runs ONE fully-composed `checkpoint.sh` command whose head is shell-substituted (`"$(git rev-parse --verify HEAD)"`, never model-relayed), writing the SAME semantic checkpoint name the post-return code writes ('forge-done' round 1, `fix-round-N` fix rounds). Gated on dbPath && runId; must-not-fail-the-build; post-return `checkpoint()` calls stay as the authoritative re-stamp. No new checkpoint names, no `classifyResume` edits.
- [x] **T2 — ARTIFACT-TIME CHECKPOINT ON THE CODEX LANE.** The GPT-executor path replaces the forge contract's steps (`codex-build.sh`, dispatched via nohup/setsid) — thread `NEUTRON_CODEX_BUILD_CHECKPOINT_*` env (script path, db, run id, checkpoint name) and have the wrapper invoke `checkpoint.sh` itself right after the commit + diff write, so defect 1 is closed on BOTH executors, not just the Claude one.
- [ ] **T3 — RECOVERY READS THE LOCAL REFS (defect 2).** Tonight both the operator and the relaunch treated "not on origin" as "no work exists" while the work was one `git log` away. Before any relaunch/dispatch path decides to rebuild from zero (empty `inner_checkpoint`, `detectExistingPr` miss, `resolveResumeLiveHead` 'absent' in pr mode), the launcher must probe the shared checkout's OWN refs (`git for-each-ref refs/heads/<run.branch ?? trident/<slug>>` in `run.repo_path`/`run.worktree`); a local branch with commits ahead of the merge-base with the base branch is recovered — pushed via the credentialed outer publisher so it becomes durable and the pr-mode probes can see it — and the run SAYS so (log + a note on the row), never silently rebuilding work that is already on disk.
- [ ] **T4 — THE STALL RESTART HALF (defect 3).** #312 detects hard DEATH only; a stall (process alive, no progress) still ends at the `NO_ADVANCE_HANG_MS` reap → `failed` (`orchestrator.ts` ~1900) and a human has to ask. On a confirmed stall the tick loop must instead HARVEST (T3's local-ref probe + any checkpoint/diff already recorded), push what exists, and relaunch as a continuation through the SAME bounded `beginCrashRecovery`/`crash_recoveries` budget the death path uses — so it cannot loop — and the terminal message when the budget is spent names stall-restarts distinctly from crashes. The owner is told a restart happened, not handed silence.
- [ ] **T5 — CANCEL IS AUTHORITATIVE (in-scope observation).** `codegen_cancel` flips the row terminal but nothing stops the detached workflow (e7e87cb3 → PR #316 AFTER cancel). Two layers, both needed because the warm fire REPL is SHARED across runs (a generation-wide pid kill would murder bystander lanes — rejected): (a) OUTER GATE — every credentialed side effect (`publishBuiltCommit` push/PR-create, merge, re-fire in `applyResult`) re-reads the row and REFUSES when it is terminal; (b) INNER FENCE — the workflow probes the row's phase at each phase boundary (before forge, each review round, each fix round; `checkpoint.sh` already computes the 'terminal' answer) and aborts when terminal. Test pins the card's acceptance: after a won cancel, a late-arriving inner result produces NO push, NO PR, NO merge, NO re-fire.
