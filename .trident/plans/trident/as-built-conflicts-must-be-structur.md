# IMPLEMENTATION_PLAN.md — AS_BUILT conflicts must be structurally impossible

Regenerated 2026-08-16 by diffing the plan-card spec against the tree. Verified-existing layers are checked; the four defects (T1–T4) are ordered so each task's mechanism exists before the task that depends on it (T2's union is what T3's local merge applies; T3's resolver is what T4 surfaces at review time).

- [x] `.gitattributes` carries `docs/AS_BUILT.md merge=union` as the tracked fallback floor (#326) — verified present; `scripts/ci/check-governed-repo-attributes.ts` gates it. DO NOT REMOVE.
- [x] Entry-aware merge driver `as-built-log` exists and unions whole entries with distinct headings (`scripts/git/as-built-merge-driver.ts` + `scripts/git/as-built-log-merge.ts`, #323), with history-loss refusals terminated by the driver rather than delegated to git.
- [x] The driver is installed per-repo at publish/rebase time: `ensureAsBuiltMergeDriver` in `trident/orchestrator.ts` writes `merge.as-built-log.driver` config + the `$GIT_COMMON_DIR/info/attributes` binding, called from `rebaseOntoObservedBase` step (e2).
- [x] Precedence verified with real git and pinned by tests: `info/attributes` (driver) outranks tracked `.gitattributes` (union) — `trident/as-built-union-attribute.test.ts`, `trident/as-built-publish-wiring-realgit.test.ts`.
- [x] Heading-uniqueness gate shares the driver's parser so gate and driver cannot disagree about entry boundaries (`scripts/git/as-built-heading-uniqueness.ts`).
- [x] **T2 — the driver must UNION two different entries added under the same heading instead of declining.** Tonight's measured common-case failure (`both sides added a different entry under the same heading: ## 2026-08-15 — …`). Remove the refusal at `scripts/git/as-built-log-merge.ts` step (2); keep both entries intact, retitling theirs' copy with a numeric disambiguator so the merged log still satisfies the heading-uniqueness gate (a verbatim duplicate would fail every subsequent PR against that gate). Real-git acceptance test: two branches each adding a different entry under the same heading merge clean, both bodies intact, zero conflict markers, zero duplicate headings.
- [ ] **T1 — install the driver at clone/checkout/workspace-provision time, not publish time.** Call `ensureAsBuiltMergeDriver` (idempotent, best-effort) when the build workspace is resolved and at run launch — `trident/board-dispatch.ts` after `ensureProjectBuildWorkspace`, and `trident/orchestrator.ts` `launch` before the inner workflow fires — so the binding exists in `$GIT_COMMON_DIR/info/attributes` (covering every linked worktree) before any merge can occur. Acceptance: a freshly provisioned checkout answers `git check-attr merge -- docs/AS_BUILT.md` → `as-built-log` before any build/merge step runs; union remains the answer when the driver is absent (fallback preserved).
- [ ] **T3 — auto-resolve a PR whose ONLY conflict is `docs/AS_BUILT.md`.** GitHub ignores gitattributes server-side, so a driver-resolvable conflict still shows CONFLICTING. New bounded step (orchestrator-side, no model turn needed): scratch worktree on the PR branch, driver installed, `git merge --no-edit origin/<base>`; if the merge completes cleanly (the driver resolves the log) push with `--force-with-lease`; if any OTHER path conflicts, abort the merge and fall through to the existing conflict surfacing untouched. Assert: never pushes a tree containing conflict markers; a moved-underneath remote is refused by the lease.
- [ ] **T4 — surface an AS_BUILT-only conflict at review time as a mechanical auto-fix, never as REQUEST_CHANGES or an infra-only defer.** In `trident/inner-workflow.mjs`, when `classifyReviewReadiness` reports `conflicting`, invoke/require the T3 resolution before `reviewPreconditionDeferred` emits its infra-only `REQUEST_CHANGES`; only a conflict that is NOT AS_BUILT-only may still defer. Assert both directions: AS_BUILT-only conflict → resolved + review proceeds; a real code conflict → still defers with the existing wording.

## Do not (copied verbatim from the card's plan doc)

_Source: `docs/plans/as-built-conflicts-must-be-structurally-impossible-install-t-whdsfb.md`. Do not paraphrase this section._

- Do not remove `merge=union` from `.gitattributes` — it is the deliberate fallback.
- Do not restructure `docs/AS_BUILT.md` itself; a rewrite of that file needs real review, and union/driver semantics are only safe because it is append-only.
