# IMPLEMENTATION PLAN — fix round on PR #289 (branch `trident/the-build-re-runs-1-273-test-files`)

Card: derive the TEST EXECUTION strategy — discover the runner, probe its knobs, budget the jobs.
The card is BUILT and CI was green; this round is a re-rebase plus three review findings. Do NOT rebuild from scratch.

- [x] T1 — `trident/test-strategy.ts` + `test-strategy.test.ts`: discover the runner, probe knobs, budget jobs/concurrency (on PR #289)
- [x] T2 — thread the TEST EXECUTION block into both build contracts (`inner-workflow.mjs`, `inner-loop.ts`, `orchestrator.ts`, gateway wiring) (on PR #289)
- [x] T3 — measure the parallel suite on the real box; `docs/testing-runner.md` + AS_BUILT entries (on PR #289)
- [x] Argus rounds 1–3 fixes (jobs budget survives staggered launches; gate reaches PR mode; evidenced escape hatch) (on PR #289)
- [x] **Task 1 — re-rebase the PR #289 branch onto current main.** `git rebase origin/main` in a throwaway worktree; expected conflicts in `trident/orchestrator.test.ts` (replay of daeb6ff) and `docs/SYSTEM-OVERVIEW.md` (replay of 5142071); possible intermediate conflicts in `trident/inner-workflow.mjs`/`orchestrator.ts`/`docs/AS_BUILT.md`. SILENT-REVERT TRAP: every head recorded or reviewed must keep #290's git-truth (`readBuiltHead`/`rev-parse`), never a model-reported `commitSha` except through the existing `normalizeOid` claim fallback. Verify post-rebase with grep gates, run the touched test files, force-push with lease, confirm PR #289 reports MERGEABLE.
- [ ] Task 2 — BLOCKER: the `failed-preexisting` hatch can self-approve past a red suite. Thread the suite findings INTO the panel (`runReviewRound` → `reviewAndSynthesize` reviewer + synthesis prompts) and make an unvalidated hatch FAIL CLOSED: the synthesis must affirmatively report the claim validated (new optional `suiteClaimValidated` boolean in VERDICT_SCHEMA, enforced in `withSuiteBlocker` only when the hatch finding is present). Re-pin `inner-workflow-assembly.test.ts:903` so unsubstantiated `failed-preexisting` + mocked approvals now yields REQUEST_CHANGES, plus a validated-claim case that still APPROVEs.
- [ ] Task 3 — MAJOR: contradictory claims fail open. `fullSuiteFindings()` returns `[]` at `testsPassed === true` before reading `suiteOutcome`; make `{testsPassed: true, suiteOutcome !== 'passed' && present}` a blocker finding. Tests for both `failed-new` and `failed-preexisting` (and `not-run`) alongside `testsPassed: true`; absent `suiteOutcome` with `testsPassed: true` stays legacy-clean.
- [ ] Task 4 — MAJOR: the runner string is executed without package-manager semantics. `resolveTestCommand` tier 1 must return the package-manager invocation (`npm test` / `pnpm test` / `yarn test` / `bun run test`, detected from `packageManager` field then lockfiles) instead of `scripts.test` verbatim, while `probeParallelKnobs` and the subset/placeholder/pinned guards keep operating on the RAW script text (new `script` field on `TestCommandResolution`). Fixture test with a runner that exists ONLY under `node_modules/.bin`: the raw script fails (127), the resolved command passes.
- [ ] Close-out — full suite (`NEUTRON_TEST_JOBS` per `scripts/run-tests.sh:74-87`, NEVER `NEUTRON_TEST_SHARD`) no worse than 1525 pass / 1 pre-existing fail (`gh-authed.test.ts` is pre-existing on clean main); append the fix-round entry to the ONE canonical `docs/AS_BUILT.md` (never recreate `docs/as-built/`); update `.trident/plans/the-build-re-runs-1-273-test-files.md`; force-push; PR #289 MERGEABLE.
