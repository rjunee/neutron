# IMPLEMENTATION_PLAN — Salvage #239: the buffered-pipeline guard + the long-silent-turn window, ported fresh onto main

Regenerated 2026-08-19 (round 3) against origin/main (`5895e268`); branch `trident/salvage-239-227-lines-187-commits-s` = origin/main + `86b591e5` (round 1, pipeline-guard half) + `9afe677d` (round 2, chat-window half). Card: salvage PR #239 (`fix(chat): preserve long silent turns`, branch `trident/turn-lifecycle`, head `19673841`, merge-base `6dfe3388`, 187 commits stale) by PORTING its residual onto this fresh branch — never merging or rebasing the stale branch (conflicts resolve toward main; the stale branch is the thinner copy).

Round-3 measurements (all against branch tip `9afe677d`): the PR's `docs/SYSTEM-OVERVIEW.md` diff (3 hunks, +21/−6, all inside the turn-lifecycle block now at ~L2315–2345) applies CLEAN — re-verified this round via `git apply --check` against the branch-tip file. Exactly TWO stale-citation sites remain in docs on the tip: L2336–2337 (the "snappy 90s / larger 180s" composer sentences — covered verbatim by the PR hunks) and the `stuck_agent` Scope block at L6988–6997, which cites the now-DELETED `TURN_INACTIVITY_MS` at a stale line anchor (`build-live-agent-turn.ts:95` — the only line-anchored citation of that file in the whole doc) and derives its "band it uniquely covers" arithmetic from the dead 90 s chat window. Current symbols on tip: `CHAT_TURN_INACTIVITY_MS = 30 * 60_000` (`gateway/wiring/build-live-agent-turn.ts:96`), `TURN_ABSOLUTE_CEILING_MS = 45 * 60_000` (`:106`), `DEFAULT_TURN_INACTIVITY_MS = 90_000` (`runtime/adapters/claude-code/persistent/signatures.ts:60`), stuck_agent default 15 minutes (`watchdog/detectors.ts`, `inactivity_threshold_ms`). `docs/AS_BUILT.md` on tip (25,501 lines) is append-only newest-LAST (recent headers `## 2026-08-17 — …`, last at L25358); the PR's own +41 AS_BUILT hunk targets the old mid-file position and stays dropped — a fresh entry is appended at the END instead. `docs/INVARIANTS.md` carries no turn-timeout citations (verified by grep). Source objects `19673841` and `6dfe3388` are both present in the repo of record.

- [x] Measure the residual and the drift (planning, 2026-08-19): PR #239 = +283/−45 over 15 files; functional residual = (a) the pipeline-guard half, (b) the chat-window half (`CHAT_TURN_INACTIVITY_MS = 30min` replacing the 90s/180s split + the `completedUserText` stale-Retry guard + 2 gateway test files + the `signatures.ts` doc hunk). NOT ported, by decision: `docs/AS_BUILT.md` (append a fresh entry instead), the `substrate-profiles.ts`/`.test.ts` cosmetic hunks, the `index.ts`/`types.ts` "(30min)" doc edits (FALSE on the PR itself — `DEFAULT_TURN_INACTIVITY_MS` is 90_000 on both sides; note the deliberate correction in the replacement PR body), `build-repl-argv.*` (net zero across the PR).
- [x] T1a+T2+T3 — pipeline-guard half, ported and PROVEN registered (commit `86b591e5`): `hooks/pipeline-guard.ts` (+73); `PIPELINE_GUARD_HOOK_PATH` + `pipelineGuard?: { hookPath?: string }` wired in `build-settings.ts` (guard's `PreToolUse` `matcher: 'Bash'` entry emitted FIRST; activityTap switched from replacing to APPENDING via a `...pre` spread); `pipelineGuard: {}` activated in `spawn.ts` inside the `enableToolBridge` gate ONLY (owner chat + nudge lane guarded; disposable Trident build REPLs exempt — builds legitimately run post-exit `| tail`); 3 test files incl. the named controls — spawned-hook `producer | tail -20` goes RED (exit 2), `producer | tee build.log` stays green; mutant check (emptied `FULL_BUFFERING_CONSUMERS`) flipped 4/6 guard tests incl. the positive control; focused 29 pass / stage-1 404 pass / ESLint clean.
- [x] T1b — chat-window half, ported via the pre-verified clean `git apply` of the four-file subset of `6dfe3388..19673841` (commit `9afe677d`): exported `CHAT_TURN_INACTIVITY_MS = 30 * 60_000` replaces both deleted constants, `buildSpec`'s `turn_timeout_ms` simplified to it (absolute ceiling untouched); `completedUserText` map beside `lastUserText` + the button-tap stale-Retry guard (log `retry_tap_already_completed`, reply "That turn already finished.", return replied) + delete-on-new-real-turn + set-on-successful-completion for non-seed turns; `signatures.ts` doc-comment-only hunk (`DEFAULT_TURN_INACTIVITY_MS` stays 90_000); both gateway test files ported incl. the NEW delayed-Retry test. Two mutant checks (m1 90_000 window → RED ×3; m2 removed set-on-success → RED on the delayed-Retry test) then exact restore; focused 816 pass / stage-1 422 pass / ESLint clean. Full suite deferred to the terminal task.
- [x] Docs (THIS iteration): (a) apply the PR's three `docs/SYSTEM-OVERVIEW.md` turn-lifecycle hunks VERBATIM via `git apply` (pre-verified clean): "(updated 2026-08-14)" header kept as-is, 30-minute-window + delayed-Retry-suppression + profile-less-90 s prose replacing the 90s/180s composer sentences, and the new "Chat pipelines cannot hide activity" bullet after the `isFreezeTimeout` bullet; (b) hand-rewrite the `stuck_agent` Scope block (L6988–6997): cite by SYMBOL NAME only (`CHAT_TURN_INACTIVITY_MS` 30 min, `DEFAULT_TURN_INACTIVITY_MS` 90 s profile-less default, `TURN_ABSOLUTE_CEILING_MS` 45 min, 15-min `stuck_agent` default in `watchdog/detectors.ts`) and re-derive the band honestly — for profile-less internal calls the driver watchdog stays an order of magnitude faster and `stuck_agent` uniquely covers emitting-but-never-settling turns 15–45 min; for CHAT turns the ordering INVERTS (15-min in-flight threshold fires before the 30-min silence trip or 45-min ceiling, silent or emitting, and may flag a legitimately long healthy turn — alert-only, nothing abandoned); (c) append `## 2026-08-19 — salvage #239 …` at the absolute END of `docs/AS_BUILT.md` recording provenance (head `19673841`, merge-base, 187 stale, port-not-rebase, symbols-absent-from-main check), T2 registration evidence, T3 positive/negative controls + mutant flips, the chat-window mutants, and the dropped-hunks corrections — evidence sourced from the two commit messages, never invented. ONE commit incl. this plan file; focused two-file bun test as no-regression control; full suite stays deferred. Evidence: three doc hunks applied cleanly, the Scope ordering was re-derived by symbol, and the append-only salvage record was added at the file end.
- [ ] T4 — Terminal: full `bun test` + ESLint (redirect to a log file, read only the tail), `tools/lane_review.sh trident/salvage-239-227-lines-187-commits-s` exits 0, publish the branch / open the replacement PR (body carries the T2 non-test-caller grep, the T3 controls, and the dropped-hunks note incl. the deliberate "(30min)" correction), then `gh pr close 239 --comment "…"` naming the replacement + rationale (187 commits stale; residual ported fresh; symbols verified absent from main before the port). Do NOT delete `trident/turn-lifecycle` (it is the measured artifact). If the lane's token cannot close #239, record the requested close in the replacement PR body instead of failing the build.
- Commit .trident/plans/trident/salvage-239-227-lines-187-commits-s.md together with your code + tests.
