# IMPLEMENTATION_PLAN — Salvage #239: the buffered-pipeline guard + the long-silent-turn window, ported fresh onto main

Regenerated 2026-08-19 (round 2) against origin/main (5895e268); branch `trident/salvage-239-227-lines-187-commits-s` = origin/main + `86b591e5` (round 1). Card: salvage PR #239 (`fix(chat): preserve long silent turns`, branch `trident/turn-lifecycle`, head `19673841`, merge-base `6dfe3388`, 187 commits stale; verified still OPEN via `gh pr view 239` this round) by PORTING its residual onto this fresh branch — never merging or rebasing the stale branch (conflicts resolve toward main; the branch is the thinner copy).

Round-2 measurements (all against the branch tip `86b591e5`): the four-file chat-window subset of the PR diff (217 patch lines) applies CLEAN via `git apply --check` — main-side drift in those files since `6dfe3388` (+16/−9: the DEFAULT_TOOL_NAMES comment ~L371 of `build-live-agent-turn.ts`, the `defaultIsPidAlive` DI seam ~L420 of `signatures.ts`, and `applyMigrations`→`seedMigratedDb` in both test `beforeEach` blocks) is disjoint from every residual hunk. `effectiveCold`/`onboardingActive` retain 17 uses after the ternary simplification (no unused-var lint risk). No test outside the two ported files asserts chat `turn_timeout_ms` values (`work-wakeup.ts` has its own constant). PR-internal `build-repl-argv` churn is net zero over `6dfe3388..19673841` — correctly dropped. NEW finding: `docs/SYSTEM-OVERVIEW.md` grew a `stuck_agent` scope block (~L6985–6996) AFTER the PR that cites `TURN_INACTIVITY_MS` at `build-live-agent-turn.ts:95` and the 90s/180s split — the PR's docs hunks do not cover it; the Docs task must rewrite it too.

- [x] Measure the residual and the drift (planning, 2026-08-19): PR #239 = +283/−45 over 15 files; functional residual = (a) the pipeline-guard half, (b) the chat-window half (`CHAT_TURN_INACTIVITY_MS = 30min` replacing the 90s/180s split + the `completedUserText` stale-Retry guard + 2 gateway test files + the `signatures.ts` doc hunk). NOT ported, by decision: `docs/AS_BUILT.md` (append a fresh entry instead), the `substrate-profiles.ts`/`.test.ts` cosmetic hunks, the `index.ts`/`types.ts` "(30min)" doc edits (FALSE on the PR itself — `DEFAULT_TURN_INACTIVITY_MS` is 90_000 on both sides; note the deliberate correction in the replacement PR body), `build-repl-argv.*` (net zero across the PR).
- [x] T1a+T2+T3 — pipeline-guard half, ported and PROVEN registered (commit `86b591e5`): `hooks/pipeline-guard.ts` (+73); `PIPELINE_GUARD_HOOK_PATH` + `pipelineGuard?: { hookPath?: string }` wired in `build-settings.ts` (guard's `PreToolUse` `matcher: 'Bash'` entry emitted FIRST; activityTap switched from replacing to APPENDING via a `...pre` spread); `pipelineGuard: {}` activated in `spawn.ts` inside the `enableToolBridge` gate ONLY (owner chat + nudge lane guarded; disposable Trident build REPLs exempt — builds legitimately run post-exit `| tail`); 3 test files incl. the named controls — spawned-hook `producer | tail -20` goes RED (exit 2), `producer | tee build.log` stays green; mutant check (emptied `FULL_BUFFERING_CONSUMERS`) flipped 4/6 guard tests incl. the positive control; focused 29 pass / stage-1 404 pass / ESLint clean. Full suite deferred to the terminal task.
- [x] T1b — Port the chat-window half via the pre-verified clean `git apply` of the four-file subset of `6dfe3388..19673841` (THIS iteration; see execution spec): exported `CHAT_TURN_INACTIVITY_MS = 30 * 60_000` replaces both deleted constants, `buildSpec`'s `turn_timeout_ms` simplified to it (absolute ceiling `turn_absolute_ceiling_ms` untouched); `completedUserText` map beside `lastUserText` + the button-tap stale-Retry guard (log `retry_tap_already_completed`, reply "That turn already finished.", return replied) + delete-on-new-real-turn + set-on-successful-completion for non-seed turns; `signatures.ts` doc-comment-only hunk (`DEFAULT_TURN_INACTIVITY_MS` stays 90_000); both gateway test files ported (onboarding-scope mirror constant → 30 * 60_000 with 3 reshaped assertions; timeout-retry: retry-after-failure reshape to a 3-spec sequence + NEW "a delayed Retry after a completed long turn does not redo completed work" test). Two mutant sanity checks then exact restore; focused tests + ESLint green; ONE commit incl. this plan file updated.
- [ ] Docs — adapt `docs/SYSTEM-OVERVIEW.md`: (a) the PR's turn-lifecycle hunks (~L2277–2340: "updated 2026-08-14" header, 30-minute-window prose, delayed-Retry suppression sentence, the "Chat pipelines cannot hide activity" bullet — Bash `PreToolUse` guard, offender named, streaming/`tail -f` allowed, redirect-to-log-and-inspect-separately guidance); (b) the NEWER `stuck_agent` scope block ~L6985–6996 (postdates the PR): rewrite its 90s/180s citations for the 30-minute chat window (profile-less internal calls keep the 90 s substrate default) and re-derive its "band it uniquely covers" arithmetic honestly; cite by symbol name, not line number. Append a dated entry at the END of `docs/AS_BUILT.md` (append-only, newest last) recording the salvage, source sha `19673841`, the port-vs-rebase decision, the T2 registration + T3 positive-control evidence, and the dropped-hunks corrections.
- [ ] T4 — Terminal: full `bun test` + ESLint (redirect to a log file, read only the tail), `tools/lane_review.sh trident/salvage-239-227-lines-187-commits-s` exits 0, publish the branch / open the replacement PR (body carries the T2 non-test-caller grep, the T3 controls, and the dropped-hunks note), then `gh pr close 239 --comment "…"` naming the replacement + rationale (187 commits stale; residual ported fresh; symbols verified absent from main before the port). Do NOT delete `trident/turn-lifecycle` (it is the measured artifact). If the lane's token cannot close #239, record the requested close in the replacement PR body instead of failing the build.
