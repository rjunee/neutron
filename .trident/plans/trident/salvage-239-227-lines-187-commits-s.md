# IMPLEMENTATION_PLAN — Salvage #239: the buffered-pipeline guard + the long-silent-turn window, ported fresh onto main

Regenerated 2026-08-19 (round 4) against origin/main (`5895e268`, unmoved since round 3); branch `trident/salvage-239-227-lines-187-commits-s` = origin/main + `86b591e5` (round 1, pipeline-guard half) + `9afe677d` (round 2, chat-window half) + `d6a02f7a` (round 3, docs + append-only AS_BUILT entry). Card: salvage PR #239 (`fix(chat): preserve long silent turns`, branch `trident/turn-lifecycle`, head `19673841`, merge-base `6dfe3388`, 187 commits stale) by PORTING its residual onto this fresh branch — never merging or rebasing the stale branch (conflicts resolve toward main; the stale branch is the thinner copy, and it is deliberately NOT deleted).

Round-4 planner verification (by grep + read against tip `d6a02f7a`, not by trust): the three signature symbols (`CHAT_TURN_INACTIVITY_MS`, `PIPELINE_GUARD_HOOK_PATH`, `findBufferedPipelineConsumer`) resolve on the branch and to ZERO files on origin/main. T2 registration is real, not decorative: `runtime/adapters/claude-code/persistent/build-settings.ts:125` consumes `PIPELINE_GUARD_HOOK_PATH` in production code (`PreToolUse` `matcher: 'Bash'`, guard entry emitted FIRST, activityTap switched from replacing to APPENDING via a `...pre` spread), and `spawn.ts` activates `pipelineGuard: {}` inside the `enableToolBridge` gate ONLY (owner chat + nudge lane guarded; disposable Trident build REPLs exempt — post-exit `| tail` is legitimate there), with `tool-bridge.test.ts` asserting both presence when enabled and ABSENCE when opted out. T3 controls exist by read in `__tests__/pipeline-guard.test.ts`: 'positive control: a live producer piped into tail -20 goes RED' (spawned hook, exit 2, offender named) and 'negative control: streaming through tee stays green' (exit 0), plus the bypass battery (paths, `env`, backslash, quotes, `|&`, `wc`/`less`/`jq -s`/`column -t`/`tac`/`sponge`) and the `tail -f` / quoted-mention exemptions. AS_BUILT salvage entry confirmed appended at the absolute END of `docs/AS_BUILT.md` (file now 25,515 lines). PR #239 confirmed still OPEN on rjunee/neutron. `tools/lane_review.sh` resolves a LOCAL ref before trying `origin/` (local wins), so the review leg runs BEFORE any push. Full-suite command is `bash scripts/run-tests.sh` (the bounded-memory chunked runner with the pglite retry lane); the other locally runnable CI gates are `bash scripts/ci/typecheck-all.sh` and `bash scripts/ci/lint.sh`.

- [x] Measure the residual and the drift (planning, 2026-08-19): PR #239 = +283/−45 over 15 files; functional residual = (a) the pipeline-guard half, (b) the chat-window half (`CHAT_TURN_INACTIVITY_MS = 30min` replacing the 90s/180s split + the `completedUserText` stale-Retry guard + 2 gateway test files + the `signatures.ts` doc hunk). NOT ported, by decision: `docs/AS_BUILT.md` (append a fresh entry instead), the `substrate-profiles.ts`/`.test.ts` cosmetic hunks, the `index.ts`/`types.ts` "(30min)" doc edits (FALSE on the PR itself — `DEFAULT_TURN_INACTIVITY_MS` is 90_000 on both sides; note the deliberate correction in the replacement PR body), `build-repl-argv.*` (net zero across the PR).
- [x] T1a+T2+T3 — pipeline-guard half, ported and PROVEN registered (commit `86b591e5`): `hooks/pipeline-guard.ts` (+73); `PIPELINE_GUARD_HOOK_PATH` + `pipelineGuard?: { hookPath?: string }` wired in `build-settings.ts` (guard's `PreToolUse` `matcher: 'Bash'` entry emitted FIRST; activityTap switched from replacing to APPENDING via a `...pre` spread); `pipelineGuard: {}` activated in `spawn.ts` inside the `enableToolBridge` gate ONLY (owner chat + nudge lane guarded; disposable Trident build REPLs exempt — builds legitimately run post-exit `| tail`); 3 test files incl. the named controls — spawned-hook `producer | tail -20` goes RED (exit 2), `producer | tee build.log` stays green; mutant check (emptied `FULL_BUFFERING_CONSUMERS`) flipped 4/6 guard tests incl. the positive control; focused 29 pass / stage-1 404 pass / ESLint clean.
- [x] T1b — chat-window half, ported via the pre-verified clean `git apply` of the four-file subset of `6dfe3388..19673841` (commit `9afe677d`): exported `CHAT_TURN_INACTIVITY_MS = 30 * 60_000` replaces both deleted constants, `buildSpec`'s `turn_timeout_ms` simplified to it (absolute ceiling untouched); `completedUserText` map beside `lastUserText` + the button-tap stale-Retry guard (log `retry_tap_already_completed`, reply "That turn already finished.", return replied) + delete-on-new-real-turn + set-on-successful-completion for non-seed turns; `signatures.ts` doc-comment-only hunk (`DEFAULT_TURN_INACTIVITY_MS` stays 90_000); both gateway test files ported incl. the NEW delayed-Retry test. Two mutant checks (m1 90_000 window → RED ×3; m2 removed set-on-success → RED on the delayed-Retry test) then exact restore; focused 816 pass / stage-1 422 pass / ESLint clean. Full suite deferred to the terminal task.
- [x] Docs (commit `d6a02f7a`): the PR's three `docs/SYSTEM-OVERVIEW.md` turn-lifecycle hunks applied verbatim via `git apply` (30-minute-window + delayed-Retry-suppression + profile-less-90s prose + the "Chat pipelines cannot hide activity" bullet); the `stuck_agent` Scope block hand-rewritten to cite by SYMBOL NAME only with the band re-derived honestly (chat ordering INVERTS: 15-min in-flight alert precedes the 30-min silence trip and 45-min ceiling, alert-only); `## 2026-08-19 — salvage #239 …` appended at the absolute END of `docs/AS_BUILT.md` recording provenance, T2 registration evidence, T3 controls + mutant flips, the chat-window mutants, and the dropped-hunks corrections. Round-4 planner re-verified the entry sits at file end and the Scope block cites `CHAT_TURN_INACTIVITY_MS`.
- [x] T4 — Terminal: full verification + publish + close #239. (1) `bash scripts/run-tests.sh`, `bash scripts/ci/typecheck-all.sh`, `bash scripts/ci/lint.sh` — each redirected to a log, read only the tail, all green (a red in a file this branch does not touch that is green on a solo re-run is a pre-existing flake: record it and proceed; a red in a touched file is yours: fix first). (2) `tools/lane_review.sh trident/salvage-239-227-lines-187-commits-s` exits 0 (resolves the local ref; base defaults to origin/main). (3) Commit the plan file with T4 checked + evidence tails, then `git push -u origin trident/salvage-239-227-lines-187-commits-s`. (4) Open the replacement PR (idempotent: check `gh pr list --head` first); body carries the T2 non-test-caller evidence, the T3 positive/negative controls + mutant flips, the provenance block (head `19673841`, merge-base `6dfe3388`, 187 stale, port-not-rebase, symbols-absent-from-main), and the dropped-hunks note incl. the deliberate "(30min)" correction. (5) `gh pr close 239 --comment "…"` naming the replacement + rationale; if the lane's token cannot close #239, fall back to `gh pr comment 239` and record the requested close in the replacement PR body instead of failing the build. Do NOT delete or push `trident/turn-lifecycle` (it is the measured artifact); do NOT rebase or force-push anything; do NOT touch the root `IMPLEMENTATION_PLAN.md` (it belongs to main's last-merged card — this lane's plan lives at `.trident/plans/trident/salvage-239-227-lines-187-commits-s.md`).

Evidence (round 4): `bun install --frozen-lockfile` installed 2,503 packages; cumulative stage 1 finished `422 pass / 0 fail`; the required post-fix `bun run test` completed its coverage audit (`declared files: 1371`, `files executed: 1371`) and reported `FAIL — 7/16 lane(s)` only in untouched files, so suite outcome is `failed-preexisting`, not green: the exact 43 files were `gateway/wiring/__tests__/persona-loader.test.ts`, `gbrain-memory/__tests__/resolve-gbrain-command.test.ts`, `open/__tests__/activity-inspector-served.test.ts`, `open/__tests__/app-surfaces-served.test.ts`, `open/__tests__/doc-comments-wiring.test.ts`, `open/__tests__/nudge-floor-notice-composer-wiring.test.ts`, `open/__tests__/open-app-ws-scribe-wiring.test.ts`, `open/__tests__/open-app-ws-verbatim-originals-wiring.test.ts`, `open/__tests__/open-board-terminate-wiring.test.ts`, `open/__tests__/open-claim-redirect-reconnect.test.ts`, `open/__tests__/open-code-command-wiring.test.ts`, `open/__tests__/open-connect-served.test.ts`, `open/__tests__/open-diagnostics-wiring.test.ts`, `open/__tests__/open-project-opening-recovery.test.ts`, `open/__tests__/open-projects-changed-wiring.test.ts`, `open/__tests__/open-reminder-appws-live-delivery.test.ts`, `open/__tests__/open-task-command-wiring.test.ts`, `open/__tests__/open-tasks-wiring.test.ts`, `open/__tests__/reachability.test.ts`, `open/__tests__/usage-dashboard-base-url-wiring.test.ts`, `open/__tests__/usage-dashboard-lapsed-wiring.test.ts`, `open/__tests__/usage-dashboard-unreadable-wiring.test.ts`, `open/__tests__/usage-dashboard-wiring.test.ts`, `tests/integration/claim-redirect-once.open.test.ts`, `tests/integration/cores-slug-provenance-wired.open.test.ts`, `tests/integration/identity-jwks-bearer.open.test.ts`, `tests/integration/import-finalize-after-fields-complete.open.test.ts`, `tests/integration/import-watch-rearm-on-reconnect.open.test.ts`, `tests/integration/install-codex.test.ts`, `tests/integration/install-gbrain.test.ts`, `tests/integration/launcher-served.open.test.ts`, `tests/integration/onboarding-import-step-guard.open.test.ts`, `tests/integration/onboarding-interests-deadlock.open.test.ts`, `tests/integration/onboarding-welcome-seed-once.open.test.ts`, `tests/integration/reminders-tab-and-push.open.test.ts`, `tests/integration/tasks-tab-served.open.test.ts`, `tests/integration/wide-bind-dev-owner-rejected.open.test.ts`, `gateway/wiring/__tests__/build-gbrain-memory.test.ts`, `app/__tests__/general-tab-set.test.tsx`, `app/__tests__/project-switch-is-instant.test.tsx`, `app/__tests__/project-switch-reaches-the-wire.test.tsx`, `app/__tests__/rail-tap-lands-on-the-tapped-project.test.tsx`, and `app/__tests__/reachability.test.tsx`; solo replay on this branch and detached base `5895e268` produced identical status for all 43 (38 failed identically from ambient identity settings / machine-global `gbrain` and `codex`; the five `app` files passed solo on both), proving the red predates this diff. The branch-owned `tool-bridge.test.ts` type mismatch found by the first typecheck was fixed minimally; a clean detached worktree carrying that exact diff ended `typecheck matrix: 51 tsconfig(s) checked` / `TYPECHECK MATRIX: ALL PASS` (the primary worktree alone sees an ancestor checkout's malformed self-referential `node_modules/@types/@types` symlink). Lint ended with all seven gates green, including `EAS PREFLIGHT: every declared app dependency is installed ✅`. `tools/lane_review.sh trident/salvage-239-227-lines-187-commits-s` exited 0 and ended `=== delivers behaviour: yes`, confirming production callers for all three exported symbols. Replacement-PR URL and #239 close evidence are pending the credentialed outer wrapper by executor contract; this lane intentionally ran neither `git push` nor `gh`.
