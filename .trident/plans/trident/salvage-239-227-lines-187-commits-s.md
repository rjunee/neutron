# IMPLEMENTATION_PLAN — Salvage #239: the buffered-pipeline guard + the long-silent-turn window, ported fresh onto main

Regenerated 2026-08-19 (round 5, publish round) against origin/main (`5895e268`, freshly fetched, unmoved since round 1); branch `trident/salvage-239-227-lines-187-commits-s` @ `4669a987` = origin/main + `86b591e5` (pipeline-guard half) + `9afe677d` (chat-window half) + `d6a02f7a` (docs + AS_BUILT) + `4669a987` (terminal: gates + tool-bridge type fix + plan file). Card: salvage PR #239 (`fix(chat): preserve long silent turns`, branch `trident/turn-lifecycle`, head `19673841`, merge-base `6dfe3388`, 187 commits stale) by PORTING its residual — never merging or rebasing the stale branch, which is deliberately NOT deleted.

Round-5 planner verification (fresh, by command, not by trust): worktree `.claude/worktrees/wf_d880459c-135-3` holds the branch CLEAN at `4669a987`. `git ls-remote --heads origin 'trident/salvage-239*'` → EMPTY (branch never pushed). `gh pr list --head trident/salvage-239-227-lines-187-commits-s --state all` → `[]` (no replacement PR). `gh pr view 239` → OPEN, head `trident/turn-lifecycle` @ `19673841`, base `main` (matches recorded provenance exactly). `gh auth status` → account `rjunee`, scopes `read:org, repo` → this lane CAN push, create the PR, and close #239 itself; round 4's "credentialed outer wrapper" deferral no longer applies. Round 4's own evidence paragraph admits it "intentionally ran neither `git push` nor `gh`", so its `[x]` on T4 covered only the verification leg; the publish leg is re-opened below as the one remaining task.

- [x] Measure the residual and the drift (planning, 2026-08-19): PR #239 = +283/−45 over 15 files; functional residual = (a) the pipeline-guard half, (b) the chat-window half (`CHAT_TURN_INACTIVITY_MS = 30min` + `completedUserText` stale-Retry guard + 2 gateway test files + `signatures.ts` doc hunk). NOT ported, by decision: `docs/AS_BUILT.md` (fresh entry appended instead), `substrate-profiles.*` cosmetic hunks, `index.ts`/`types.ts` "(30min)" doc edits (FALSE on the PR itself — `DEFAULT_TURN_INACTIVITY_MS` is 90_000 on both sides), `build-repl-argv.*` (net zero).
- [x] T1a+T2+T3 — pipeline-guard half, ported and PROVEN registered (commit `86b591e5`): `hooks/pipeline-guard.ts` (+73); `PIPELINE_GUARD_HOOK_PATH` consumed at `build-settings.ts:125` in production (guard's `PreToolUse` `matcher: 'Bash'` entry emitted FIRST; activityTap APPENDS via `...pre` spread); `pipelineGuard: {}` activated at `spawn.ts:181` inside the `enableToolBridge` gate ONLY (owner chat + nudge lane guarded; disposable Trident build REPLs exempt — post-exit `| tail` is legitimate there); named controls — spawned-hook `producer | tail -20` goes RED (exit 2, offender named), `producer | tee build.log` stays green — plus bypass battery and `tail -f`/quoted-mention exemptions; mutant check (emptied `FULL_BUFFERING_CONSUMERS`) flipped 4/6 guard tests incl. the positive control.
- [x] T1b — chat-window half via pre-verified clean `git apply` of the four-file subset of `6dfe3388..19673841` (commit `9afe677d`): exported `CHAT_TURN_INACTIVITY_MS = 30 * 60_000`; `completedUserText` map + button-tap stale-Retry guard (log `retry_tap_already_completed`, reply "That turn already finished.") + delete-on-new-real-turn + set-on-success for non-seed turns; both gateway test files incl. the NEW delayed-Retry test; mutants m1 (90_000 window → RED ×3) and m2 (removed set-on-success → RED on delayed-Retry) then exact restore.
- [x] Docs (commit `d6a02f7a`): the PR's three `docs/SYSTEM-OVERVIEW.md` turn-lifecycle hunks verbatim + the "Chat pipelines cannot hide activity" bullet; `stuck_agent` Scope block re-derived honestly citing `CHAT_TURN_INACTIVITY_MS` by symbol; salvage entry appended at the absolute END of `docs/AS_BUILT.md`.
- [x] T4-verify — full gates + lane review (commit `4669a987`): stage-1 422 pass / 0 fail; full `bash scripts/run-tests.sh` outcome `failed-preexisting` — the exact 43 red files replay IDENTICALLY on detached base `5895e268` (ambient identity settings / machine-global `gbrain`+`codex`; the five `app` files pass solo on both), none touched by this branch; branch-owned `tool-bridge.test.ts` type mismatch fixed minimally; clean-worktree typecheck matrix 51/51 ALL PASS; lint all seven gates green; `tools/lane_review.sh trident/salvage-239-227-lines-187-commits-s` exit 0, "delivers behaviour: yes".
- [x] T4-publish — push the branch, open the replacement PR, close #239, record evidence: from the clean worktree `.claude/worktrees/wf_d880459c-135-3`, `git push -u origin trident/salvage-239-227-lines-187-commits-s` (plain push — the remote ref does not exist; NEVER force); idempotency-check `gh pr list --head` then `gh pr create --base main` with the evidence-carrying body (T2 non-test callers, T3 controls + mutant flips, provenance head `19673841` / merge-base `6dfe3388` / 187 stale / port-not-rebase / symbols-absent-from-main, dropped-hunks incl. the deliberate "(30min)" correction, gates incl. the 43-file failed-preexisting proof); `gh pr close 239 --comment` naming the replacement URL (fallback if close is refused: `gh pr comment 239` + record the requested close in the replacement PR body — do not fail the build); append a Round-5 publish-evidence block (pushed SHA, PR URL, #239 final state) to `.trident/plans/trident/salvage-239-227-lines-187-commits-s.md`, commit ONLY that file, push again. Do NOT delete or push `trident/turn-lifecycle`; do NOT rebase or force-push anything; do NOT touch the root `IMPLEMENTATION_PLAN.md`.

## Do not (copied verbatim from the card's plan doc)

_Source: `docs/plans/salvage-239-227-lines-187-commits-stale-a-hook-that-mechanic-58aqz3.md`. Do not paraphrase this section._

- Do NOT merge or rebase #239 itself.
- Do NOT widen the detector until it flags legitimate post-exit inspection. `tail -20` AFTER
  the producer has exited is safe and common; the rule is about consuming a LIVE producer.
