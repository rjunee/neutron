# IMPLEMENTATION_PLAN — Salvage #239: the buffered-pipeline guard + the long-silent-turn window, ported fresh onto main

Regenerated 2026-08-19 against origin/main (5895e268). Card: salvage PR #239 (`fix(chat): preserve long silent turns`, branch `trident/turn-lifecycle`, head `19673841`, merge-base `6dfe3388`, 187 commits stale) by PORTING its residual onto a fresh branch cut from current origin/main — never merging or rebasing the stale branch (T1 rule: conflicts resolve toward main; the branch is the thinner copy).

Measured this planning round: `PIPELINE_GUARD_HOOK_PATH`, `findBufferedPipelineConsumer`, `CHAT_TURN_INACTIVITY_MS`, `pipeline-guard` → 0 files each on origin/main (the salvage is genuinely unlanded). Main's wiring anchor points are unchanged since the PR base, so the port applies nearly clean: `build-settings.ts` hook assembly (Stop → todoSync → activityTap, activityTap still REPLACES `PreToolUse`), `spawn.ts` `enableToolBridge === true` settings block, `tool-bridge.test.ts` anchors at main lines 152/178, `build-live-agent-turn.ts` constants at main lines 96/108 with the ternary at 1545–1546.

- [x] Measure the residual and the drift (planning, 2026-08-19): PR #239 = +283/−45 over 15 files. The functional residual is (a) the pipeline-guard half — `hooks/pipeline-guard.ts` +73, `build-settings.ts` +12, `spawn.ts` +1, plus 3 test files — and (b) the chat-window half — `CHAT_TURN_INACTIVITY_MS = 30min` replacing the 90s/180s split, the `completedUserText` stale-Retry guard, plus 2 gateway test files. NOT ported: `docs/AS_BUILT.md` (append a fresh entry instead), the `substrate-profiles.ts` cosmetic hunk, and the `index.ts`/`types.ts` doc edits claiming `DEFAULT_TURN_INACTIVITY_MS` is "(30min)" — those are FALSE on the PR itself (the constant is 90_000 on both sides); dropping them is a deliberate correction, note it in the PR body.
- [x] T1a+T2+T3 — Port the pipeline-guard half and PROVE it is registered: create `runtime/adapters/claude-code/persistent/hooks/pipeline-guard.ts` verbatim from `19673841`; wire `PIPELINE_GUARD_HOOK_PATH` + `pipelineGuard?: { hookPath?: string }` into `build-settings.ts` (guard's `PreToolUse` `matcher: 'Bash'` entry emitted FIRST; the activityTap block switches from replacing `hooks['PreToolUse']` to APPENDING via a `...pre` spread, mirroring its existing PostToolUse append); activate `pipelineGuard: {}` in `spawn.ts` inside the `enableToolBridge` gate ONLY (owner chat + nudge lane get the guard; disposable Trident build REPLs never do — builds legitimately run post-exit `| tail`); port the `pipeline-guard.test.ts` / `build-settings.test.ts` / `tool-bridge.test.ts` additions; ADD the card's named controls — `producer | tail -20` goes RED (exit 2 through the SPAWNED hook, bare numeric flag form) and `producer | tee build.log` stays green; registration proof = non-test-caller grep recorded in the PR body + the bridge-spawn settings assertion + the opted-out-substrate negative; one-off mutant check (empty `FULL_BUFFERING_CONSUMERS` → RED tests fail) proves the positive controls bite.
- [ ] T1b — Port the chat-window half: in `gateway/wiring/build-live-agent-turn.ts` replace `TURN_INACTIVITY_MS`/`COLD_TURN_INACTIVITY_MS` with exported `CHAT_TURN_INACTIVITY_MS = 30 * 60_000`, simplify `buildSpec`'s `turn_timeout_ms` to it (absolute ceiling unchanged), and add the `completedUserText` stale-Retry guard (map declaration beside `lastUserText`; the button_prompt_id + already-completed check answering "That turn already finished."; delete-on-new-real-turn; set-on-successful-completion for non-seed turns); port the `build-live-agent-turn-onboarding-scope-timeout.test.ts` and `build-live-agent-turn-timeout-retry.test.ts` updates from `19673841`; carry the `signatures.ts` doc hunk ("Kept modest (~90s) for profile-less internal calls; chat and warm-fire callers opt into larger windows"); DROP the `index.ts`/`types.ts` "(30min)" doc edits and the `substrate-profiles.ts` hunk.
- [ ] Docs — adapt the `docs/SYSTEM-OVERVIEW.md` turn-lifecycle block (main ~2277–2340) to describe the 30-minute chat inactivity window (deliberately below the 45-min absolute ceiling), the suppressed stale Retry, and the new "chat pipelines cannot hide activity" bullet (Bash `PreToolUse` guard, offender named, streaming/`tail -f` allowed, redirect-to-log-and-inspect-separately guidance); append a dated entry at the END of `docs/AS_BUILT.md` (append-only, newest last) recording the salvage, the source sha `19673841`, the port-vs-rebase decision, and the T2/T3 evidence.
- [ ] T4 — Close #239 pointing at the replacement: `gh pr close 239 --comment "…"` naming the replacement branch/PR and this card's rationale (187 commits stale; residual ported fresh; symbols verified absent from main before the port). Do NOT delete the `trident/turn-lifecycle` branch (it is the measured artifact). If the lane's token cannot close the PR, record the requested close in the replacement PR body instead of failing the build.
