# IMPLEMENTATION_PLAN — purity preflight: the build loop runs the leak gate on its own branch before opening a PR

Measured 2026-08-31: 3 of 4 trident PRs were red on exactly one gate — `scripts/ci/leak-gate.sh` — and every finding was in the branch's OWN plan doc under `.trident/plans/`, prose the builder regenerates each run. A wording fix cannot hold; only a mechanical loop can: the outer publisher runs the gate deterministically on the branch tree before `gh pr create`, feeds findings to a bounded fix round, and after at most 2 attempts opens the PR anyway with the findings named (rule + file + line, never the excerpt). A tier that could not run is reported as skipped, never as passing.

WORD DISCIPLINE FOR EVERY COMMIT ON THIS BRANCH (including this file): the two vocabulary rules at `scripts/ci/leak-gate.sh:367` (vocab-word) and `:387` (vocab-purged) match a six-letter word (the retired multi-org vocabulary root) anywhere in a committed file. Never write it literally — in prose say "the banned word"; in code and tests assemble it from fragments (`'ten' + 'ant'`) exactly as `scripts/ci/leak-gate-selftest.test.ts` does. The gate's vocabulary RULE IDS contain that root, so they may never be written literally either — not in code, not in a commit message, not in a PR body; anything derived from gate output that lands on a scanned surface must pass the sanitizer (T2). Also: no absolute host filesystem paths in any committed byte, and never quote a finding excerpt anywhere.

Planning round 2026-08-31 (resume, tip 7213ce65): re-verified in a scratch clone — `trident/leak-preflight.test.ts` + `trident/leak-fixer.test.ts` + `open/__tests__/trident-leak-fixer-wiring.test.ts` 30 pass / 0 fail; `trident/orchestrator.test.ts` 188 pass / 0 fail. The T4 fixture recipe was validated end-to-end against the REAL gate and the REAL `runLeakGatePreflight`: seeded plan doc → FAIL exit 1 with both vocab findings at line 3 of the doc; clean doc without a denylist → INCOMPLETE exit 3 skipping `pii-denylist, pii-denylist-msg`; clean doc + a denylist file → SILENT exit 0; seeded doc + a staging fixer → status `fixed`, attempts 1, branch ref advanced by compare-and-swap. Every T4 assertion value below is measured, not guessed.

Verification round 2026-08-31 (tip `b4d22401`): T3 re-verified in a fresh worktree together with everything under it — `trident/leak-fixer.test.ts` + `trident/leak-preflight.test.ts` + `open/__tests__/trident-leak-fixer-wiring.test.ts` + `trident/conflict-resolver.test.ts` 39 pass / 0 fail; `trident/orchestrator.test.ts` + `trident/leak-preflight-realgit.test.ts` 192 pass / 0 fail; `tsc --noEmit -p` clean for both `trident/` and `open/`. The REAL gate run over this working tree returns exactly ONE finding — the linked worktree's own untracked `.git` pointer file, the artifact commit `7213ce65` already drops by exact path — so zero findings in any committed byte, and the whole branch diff greps to 0 occurrences of the banned root. Only T5 remains.

Planning round 2026-08-31 (resume, tip 15382fe9): T1–T4 verified committed on the branch; the tip commit records the fresh-worktree measurements (39 + 192 tests, tsc clean, zero committed findings). T5 is the LAST task — docs-only, two files, one commit, no production code. Its shape contract is `trident/as-built-staged-entries.test.ts`: the staged file must be exactly one entry, nothing before a `## YYYY-MM-DD — title` heading (em dash), folding cleanly into `docs/AS_BUILT.md`; the exact entry text, plan edits and commit message come verbatim from the execution spec, and the fold was pre-proven against the real log during planning (ok: true, no retitle needed).

Close-out round 2026-08-31 (T5, this commit): staged the branch's one as-built entry and ticked T5. Measured before commit: `bun test trident/as-built-staged-entries.test.ts` 1 pass / 0 fail (the staged entry folds cleanly into the real log); a fragment-assembled grep over the two touched files and the commit message counts 0 occurrences of the contiguous banned root; the real gate over the worktree names nothing beyond the linked worktree's own untracked git pointer file, which the preflight already drops by exact path.

- [x] P0 (done outside this plan, 2026-08-31): the three red branches were reworded to "absolute host filesystem paths" and pushed — heads 6759c925a69a (#481), 56e6a5868211 (#482), 7579d5aeb305 (#483). Do NOT touch them again.
- [x] T1 — `trident/leak-preflight.ts` + `trident/leak-preflight.test.ts` (commit 9b638409): the deterministic preflight core. Classifies a real `leak-gate.sh --tree` run by exit code AND verdict sentinel (no sentinel = 'gate-error', never 'clean'), runs in a throwaway detached worktree via the injected host runner with an explicit 15-min budget, loops a bounded (max 2) fixer seam over findings with commit + compare-and-swap back onto the branch ref, always removes the worktree, never throws. Findings carry rule/file/line only — no excerpt field exists.
- [x] T2 — preflight wired into `publishBuiltCommit` (`trident/orchestrator.ts`, commit 4c359ba7): invoked between the rebase replay and the lease push; the post-preflight head propagates to push, confirm, review diff, returned head and the outer-published checkpoint. `leak_preflight` opts seam DEFAULTS to the real runner; on 'findings-unresolved' the PR still opens and a best-effort, sanitizer-passed `[rule] file:line` annotation lands on the PR body/comment; annotation failure never fails the publish. All pinned in `trident/orchestrator.test.ts`.
- [x] T3 — `buildLeakPreflightFixer` (`trident/leak-fixer.ts` + test, commits 5fa6d598/1e594c43/f29490ea): ONE bounded ephemeral CC turn modeled on `trident/conflict-resolver.ts` (same tool grant, timeout, terminal-marker protocol, conservative no-marker fallback; contract: REWORD flagged text in place, never delete files, never touch the gate or its allowlist, never drop a plan doc's "Do not" section, then `git add` only), wired in `open/composer.ts` as the orchestrator's `fix_leak_findings` with boundary tests (`open/__tests__/trident-leak-fixer-wiring.test.ts`).
- [x] T3b — commit 7213ce65: the scan worktree's own `.git` pointer FILE (whose single line is an absolute path by construction) is dropped from findings by exact path so it can never burn the fix budget or annotate a PR; a pointer-only FAIL with a skipped tier reads INCOMPLETE, never clean — unit-pinned.
- [x] T4 (commit b4d22401) — `trident/leak-preflight-realgit.test.ts`, a NEW test file and no production edits: prove the preflight against the REAL `scripts/ci/leak-gate.sh` over a real throwaway git repo. One fixture repo commits the real gate + `scripts/ci/extract-comment-prose.awk` + the real Apache `LICENSE` + a minimal 14-entry fixture allowlist (the committed gate self-matches its own vocab patterns; the repo's real allowlist must NOT be copied — its entries would be stale there, exit 2) + a plan doc under `.trident/plans/trident/`. Four cases, all through `runLeakGatePreflight` with a test-owned env-REPLACING host runner (scrub `GITHUB_*`/`LEAK_GATE_*`, pin `LEAK_GATE_PII_DENYLIST_FILE` to a nonexistent path): (A) doc seeded with the fragment-assembled banned word → 'findings-unresolved', both vocab rules named at the doc's line 3, head unmoved, scratch worktree removed; (B) clean doc → 'incomplete' with skipped tiers exactly `pii-denylist, pii-denylist-msg` — a tier that could not run never reads as passing; (C) clean doc + denylist file in the runner env → 'clean'; (D) seeded doc + a scripted staging fixer → 'fixed', attempts 1, new head is a child of the old, branch ref moved by compare-and-swap, reworded doc committed. Word discipline throughout; grep self-check must count 0 occurrences of the contiguous root in the new file and the diff.
- [x] T5 (this commit) — close-out: stage the branch's ONE as-built entry at `.trident/as-built/trident/throughput-blocker-trident-s-own-pl.md` (`## 2026-08-31 — <title>` + prose: what was built, why a mechanical loop rather than a prompt fix, the sanitizer rationale — word-disciplined), and tick the finished boxes in `.trident/plans/trident/throughput-blocker-trident-s-own-pl.md`. Exact file contents and commit message are provided by the execution spec — write them verbatim; measure every check before committing.

Do not: weaken or edit `scripts/ci/leak-gate.sh` or `scripts/ci/leak-gate-allowlist.txt`; stop committing `.trident/plans/**`; reword the three P0 branches; let any preflight status block the PR (every status proceeds — only the fix loop is bounded work); amend or rewrite existing branch commits (three earlier-lane commits also ride this branch below this card's work — the publisher owns rebase at publish time); touch root `IMPLEMENTATION_PLAN.md` (it belongs to an older card on main — this plan persists at `.trident/plans/trident/throughput-blocker-trident-s-own-pl.md`); write the banned word, a vocab rule id, or an absolute host path into any committed byte.
