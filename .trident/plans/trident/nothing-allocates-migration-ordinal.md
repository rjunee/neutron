# IMPLEMENTATION_PLAN — migration ordinal allocation (parallel branches must not collide on an integer)

Base: origin/main (now owns ordinals through 0127 — 0127 merged while T1a was running; 0128 and 0129 remain reserved, so the next unreserved ordinal is 0130).

- [x] Boot REFUSES on a duplicate/colliding ordinal instead of silently skipping — already on main (`migrations/runner.ts`, pinned by `migrations/__tests__/ordinal-collision-guard.test.ts` + `ordinal-collision-mutation.test.ts`). MUST NOT be weakened by any task below.
- [x] Gaps in the ordinal sequence are allowed — already on main (runner derives versions from file prefixes, no contiguity requirement; gaps 59 and 64–68 pass; documented in `migrations/runner.test.ts:68-76`).
- [x] The live 0122 incident is acknowledged via `migrations/repairs.json` (version 122, recorded_name `work_board_items_pr`) and pinned by test — do not touch repairs.json in any task below.
- [x] T1a — renumber PR #268's migration 0122→0130: 0127 landed on main while this task was running, and 0128/0129 are reserved for T1b/T1c. On branch `trident/the-agent-must-be-able-to-remove-a`, merge origin/main, `git mv migrations/0122_work_board_items_archived_status.sql migrations/0130_work_board_items_archived_status.sql`, update the applied-version list in `migrations/runner.test.ts` (append 130 after main's `...126, 127`), regen `migrations/expected-schema.txt` via `bun run migrations/regen-snapshot.ts`, push to the PR head (no force-push).
- [ ] T1b — renumber PR #269's migration 0122→0128: same recipe on branch `trident/put-the-pr-number-on-the-card-a-cli` for `0122_work_board_items_pr.sql` → `0128_work_board_items_pr.sql`. EXTRA CARE: a live DB already recorded version 122 = `work_board_items_pr` (see repairs.json); the renumbered 0128 will re-apply there, so verify every statement in the file is idempotent (`IF NOT EXISTS` / `OR IGNORE`) before pushing. Do NOT touch repairs.json.
- [ ] T1c — renumber PR #314's migration 0124→0129: same recipe on branch `trident/dependency-aware-dispatch-a-card-de` for `0124_dispatch_dependencies_and_claims.sql` → `0129_dispatch_dependencies_and_claims.sql` (this PR also touches `migrations/scope-rekey.ts` — carry that through the merge).
- [ ] T2 — branch-time ordinal allocator: new `migrations/next-ordinal.ts` with a PURE core `nextFreeOrdinal(baseOrdinals: number[], claimedOrdinals: number[]): number` (max of all + 1; gaps never reused) and a CLI wrapper that reads (a) `git ls-tree origin/main migrations/` and (b) migration filenames from every OPEN PR via `gh pr list --state open --json number,files` — the decision consults the world, not the local checkout. Update `migrations/README.md` step 1 and `migrations/AGENTS.md` to say "run `bun run migrations/next-ordinal.ts`" instead of "pick the next unused version". Fail loudly (nonzero exit) if `gh` is unavailable rather than silently falling back to local-only.
- [ ] T3 — pre-merge collision gate: new `scripts/ci/migration-ordinal-gate.sh` (or .ts) that, on a PR, fails when any `migrations/NNNN_*.sql` added by the PR uses an ordinal that (a) already exists on the base branch or (b) is claimed by another open PR (via `gh` with the workflow token; the base-branch half must work even if the PR-scan half is unavailable, and say which half ran). Wire it as a new job in `.github/workflows/ci.yml`, add it to the `test` aggregator's `needs`/assert list (lines 226-254 — the aggregator is load-bearing and must fail when the gate fails), and extend `scripts/ci/ci-workflow.test.ts` to pin the new job's presence. Do NOT rename the `test` job.
- [ ] T4 — pin the recurrence: unit test (e.g. `migrations/__tests__/next-ordinal.test.ts`) simulating two concurrent branches: branch A calls `nextFreeOrdinal(base, [])` → N and publishes its claim; branch B calls `nextFreeOrdinal(base, [N])` → must get N+1 ≠ N. Also assert gaps are never reused (base=[...,126] with claim 128 → allocator returns 129, not 127) and that the collision-gate script flags a synthetic duplicate.
- [ ] Record the fix in `docs/AS_BUILT.md` (follows the existing ordinal-124 crash-loop entry) once T1–T4 land.

## Do not (from spec — binding on every task)
- Do not restore silent skipping of a duplicate ordinal in any form; boot refusal stays.
- Do not require contiguous ordinals; a gap is safe and preferred over a collision.
- Do not renumber ordinals already applied to a live database (main's 0122/0124 files stay untouched; only the UNMERGED PR-branch files move).
- Commit .trident/plans/trident/nothing-allocates-migration-ordinal.md together with your code + tests.
