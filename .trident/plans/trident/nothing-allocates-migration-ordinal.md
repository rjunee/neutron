# IMPLEMENTATION_PLAN — migration ordinal allocation (parallel branches must not collide on an integer)

Regenerated 2026-08-17 against origin/main @ 477671d7. World state: main owns ordinals through 0131 with PERMANENT gaps at 0128/0129 (a gap is safe by design — never backfill one); open-PR migration claims: #269 → 0132 (its own lane renumbered off re-rebased main), #314 → 0124 (still colliding with main), #386 (THIS lane) → a stale 0128 that must be dropped; zombie claims 0120 (#172), 0121 (#213), 0122 (#268) belong to stale PRs. Next free ordinal for any new claim: **0133**.

- [x] Boot REFUSES on a duplicate/colliding ordinal instead of silently skipping — already on main (`migrations/runner.ts`, pinned by `migrations/__tests__/ordinal-collision-guard.test.ts` + `ordinal-collision-mutation.test.ts`). MUST NOT be weakened by any task below.
- [x] Gaps in the ordinal sequence are allowed — already on main (runner derives versions from file prefixes, no contiguity requirement; documented in `migrations/runner.test.ts`).
- [x] The live 0122 incident is acknowledged via `migrations/repairs.json` (version 122, recorded_name `work_board_items_pr`) and pinned by test — do not touch repairs.json in any task below.
- [x] T1a (content) — #268's archived-status migration renumbered 0122→0130 and LANDED ON MAIN via merged PR #381 together with the removal-chokepoint feature. PR #268 itself is now a superseded zombie still waving its old 0122 file; closing it is a task below.
- [x] T1b — #269 carries a unique ordinal: #269's OWN lane renumbered `work_board_items_pr` to **0132** off re-rebased main (head commits `34b1a7e4` renumber→0123, then `e9e454bc` renumber→0132), with `runner.test.ts`, `expected-schema.txt` and a dedicated `migrations/__tests__/live-ledger-122-work-board-pr.test.ts` updated on that head. This plan's old 0128 reservation is DEAD — 0128 stays a permanent gap; nothing may ever claim it.
- [x] **W1 (TOP) — purge the stale absorbed #269 snapshot from THIS branch** (`trident/nothing-allocates-migration-ordinal`, PR #386, tip 99bff351). The branch still carries an old copy of #269's feature at ordinal 0128 AND rewrites the already-applied `migrations/0130_work_board_items_archived_status.sql` in place. If #386 merged, main would apply the same work_board_items schema change twice (our 0128 + #269's 0132) — this lane would itself re-create the defect class it exists to kill — and rewriting an applied migration violates this card's own "do not". Bring the branch to origin/main parity except `.trident/plans/trident/nothing-allocates-migration-ordinal.md`: delete `migrations/0128_work_board_items_pr.sql` + `trident/repo-web-url.ts` + `trident/repo-web-url.test.ts`, restore the other 17 modified files from origin/main, commit this regenerated plan, plain push (no force).
- [ ] T1a (PR hygiene) — close #268 as superseded: its content (removal chokepoint + archived status at 0130) is on main via #381, so closing releases its stale `0122_work_board_items_archived_status.sql` claim without any renumber (renumbering would double-apply content main already has). `gh pr close 268 --comment` naming #381 as the landing. Fallback if gh lacks write perms: push a converge-to-main commit to its head that deletes the 0122 file — and report which path was taken.
- [ ] T1c — renumber #314's migration 0124→**0133** on its head `trident/dependency-aware-dispatch-a-card-de` (NOT the old 0129 reservation: #269 took 0132, so max(world)=0132, and the allocator discipline below is max+1 / never reuse a gap — this also avoids the below-max out-of-order apply hazard). Recipe on that branch: `git mv migrations/0124_dispatch_dependencies_and_claims.sql migrations/0133_dispatch_dependencies_and_claims.sql`, fix the ordinal in the file's header comments, update THAT branch's `migrations/runner.test.ts` applied-version list (124→133), regen its `expected-schema.txt` via the branch's own `migrations/regen-snapshot.ts` (present on that base), keep its `migrations/scope-rekey.ts` change intact; plain push, no force. The branch is ~339 files behind main — the full rebase remains ITS lane's job; this card owes only the non-colliding ordinal.
- [ ] T2 — branch-time ordinal allocator: new `migrations/next-ordinal.ts` with a PURE core `nextFreeOrdinal(baseOrdinals: number[], claimedOrdinals: number[]): number` (max of ALL inputs + 1; gaps never reused; stale LOW claims from zombie PRs — 0120/0121/0122 are live examples today — must not drag the answer down) and a CLI wrapper reading (a) `git ls-tree origin/main migrations/` and (b) migration filenames from every OPEN PR via `gh pr list --state open --json number,files` — the decision consults the world, not the local checkout. Nonzero exit if `gh` is unavailable; no silent local-only fallback. Update `migrations/README.md` step 1 and `migrations/AGENTS.md` to say "run `bun run migrations/next-ordinal.ts`" instead of "pick the next unused version".
- [ ] T3 — pre-merge collision gate: new `scripts/ci/migration-ordinal-gate.sh` (or .ts) that fails when a PR-added `migrations/NNNN_*.sql` uses an ordinal that (a) already exists on the base branch or (b) is claimed by another open PR (via `gh` with the workflow token; the base-branch half must work even when the PR-scan half is unavailable, and print which half ran). Wire as a new job in `.github/workflows/ci.yml`, add it to the load-bearing `test` aggregator's `needs` list AND its assert loop (~lines 225-255), extend `scripts/ci/ci-workflow.test.ts` to pin the new job. Do NOT rename the `test` job.
- [ ] T4 — pin the recurrence: `migrations/__tests__/next-ordinal.test.ts` simulating two concurrent branches — branch A calls `nextFreeOrdinal(base, [])` → N and publishes its claim; branch B calls `nextFreeOrdinal(base, [N])` → must get N+1 ≠ N. Also assert gaps are never reused (base ending 0131 with claim 0132 → 0133, never 0128/0129) and that the collision-gate script flags a synthetic duplicate.
- [ ] Record the fix in `docs/AS_BUILT.md` (alongside the existing 2026-08-17 ordinal entries) once T1–T4 land.

## Do not (from spec — binding on every task)
- Do not restore silent skipping of a duplicate ordinal in any form; boot refusal stays.
- Do not require contiguous ordinals; a gap is safe and preferred over a collision (0128/0129 are now permanent gaps — leave them).
- Do not renumber — or rewrite in place — any ordinal already applied to a live database (main's 0122/0124/0130 files stay untouched; only UNMERGED PR-branch files move).
- Commit `.trident/plans/trident/nothing-allocates-migration-ordinal.md` together with your code + tests.
- Commit .trident/plans/trident/nothing-allocates-migration-ordinal.md together with your code + tests.
