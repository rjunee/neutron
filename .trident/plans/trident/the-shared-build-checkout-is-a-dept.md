# IMPLEMENTATION_PLAN — The shared build checkout is a depth-1 shallow clone, so every publish-time rebase fails

Regenerated 2026-08-16 against main @ 4eb3adc. Card: heal shallow build checkouts before publish-time replay (five builds died 2026-08-15 on `(paths unreadable)`). Verified in-tree before writing: `install.sh:1381` still clones `--depth 1`; `trident/orchestrator.ts` `rebaseOntoObservedBase` (line 475) has NO shallow guard at entry (only the bounded deepen inside `localForkPoint`, line 578, which the PR path never reaches); `trident/publish-rebase-realgit.test.ts` `seedWorld` still seeds a ONE-commit origin (line 123) so no object is ever missing and the shallow fixture cannot fail for the card's reason.

- [x] Already landed, do NOT rebuild (card says so explicitly): the replay diff is taken from the branch's FORK POINT (`localForkPoint`, orchestrator.ts ~571) and FAILS CLOSED when the fork point cannot be established — no fallback to the stale local `refs/heads/<base>` (landed with #290/#292 lineage).
- [x] Already landed, do NOT rebuild: patch bytes never round-trip a JS string — `git diff --output=` on the local path AND `gh pr diff > file` via `sh -c` on the PR path (orchestrator.ts ~625–639, landed #292/#296); guarded by the trailing-blank-line fixture in publish-rebase-realgit.test.ts.
- [x] Acceptance criterion 2 — an `apply --3way` failure with ZERO unmerged paths is reported as an apply failure carrying git's own stderr ("the apply failed WHOLESALE and left nothing unmerged, so this is NOT a merge conflict"), never `TridentRebaseConflict`/"(paths unreadable)" (orchestrator.ts 679–687, landed #296). KEEP: `TridentRebaseConflict` remains reserved for ≥1 genuinely unmerged file.
- [x] **TOP — Part 2 (the part that makes it permanent) + Part 3 + Part 4, one coherent change:** heal-on-use shallow guard `healShallowCheckout(run_host, repoPath)` called at the TOP of `rebaseOntoObservedBase` before any git surgery — probe `git rev-parse --is-shallow-repository`; on `true` run `git fetch --no-tags --unshallow origin`; on fetch failure THROW naming the measured depth (`rev-list --count HEAD`) and the shallow boundary shas plus git's stderr (acceptance criterion 1); on `false` do nothing further (a healthy checkout pays one probe). Part 3 in the same change because it is the guard's test: rework `seedWorld` so real multi-commit history exists BEFORE the depth-1 clone, assert the fork-point blob is genuinely ABSENT from the shallow checkout (`git cat-file -e` fails — the anti-fake guard), and add a PR-path test (gh-intercept `run_host` wrapper writing a real full-history diff) that FAILS without the guard because `--3way` lacks the blob. Part 4 in the same change because the guard falsifies the comments: update the `rebaseOntoObservedBase` header (~457 "THE SHARED BUILD CHECKOUT IS SHALLOW … is present"), step (d) (~517 "NOT `--unshallow`"), the `localForkPoint` deepen comment (~576, which falsely calls `--unshallow` on a complete repo "a cheap no-op" — modern git ERRORS), and the `publishBuiltCommit` SHALLOW paragraph (~1073) — shallowness becomes a defect healed on entry, while the never-run-`git rebase`-in-the-shared-working-tree rule is PRESERVED verbatim in spirit (it survives full history: other lanes share the tree). Append a dated entry to docs/AS_BUILT.md. Update `orchestrator.test.ts` stub hosts to answer the new probe.
- [x] Part 1 + acceptance criterion 3 — provisioning: drop `--depth 1` from the `git clone` at install.sh:1381 (Open is ~236 commits; the saved download is worth nothing); on the EXISTING-checkout update path (the `git pull --ff-only` branch just above, ~line 1374), heal a previously-shallow install with `git fetch --unshallow` when `git rev-parse --is-shallow-repository` says true, before the pull; assert by test, not hope — extend the tests/integration/install-*.test.ts family (pattern: install-clone-origin.test.ts seams) or add a ratchet-style guard asserting no `--depth` reaches the `$NEUTRON_REPO` clone.
- [ ] Acceptance criterion 4 — a branch whose commits are real but whose publish failed leaves its work PUSHED, not stranded local. OVERLAPS card 01KZZS9ZKVJM658ZYE6ZRBBJJ2 — COORDINATE, do not duplicate: before building anything, check that card's state; if it is in flight, this item reduces to verifying its coverage of the publish-rebase failure path (a `healShallowCheckout`/replay throw currently exits `publishBuiltCommit` before any push). If built here: on replay failure, best-effort plain push of the UN-rebased branch tip under its own lease so the work survives remotely, without masking the original error.

Guard rails: do NOT loosen `publishBuiltCommit`; do NOT reintroduce `git rebase` in the shared working tree (full history does not make that safe — other lanes share the tree); do NOT touch the landed fork-point fail-closed path or the `--output` patch plumbing; `TridentRebaseConflict` stays reserved for ≥1 genuinely unmerged file.
