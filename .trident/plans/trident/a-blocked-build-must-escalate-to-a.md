# IMPLEMENTATION_PLAN.md — Arbiter escalation tier (blocked builds escalate to Fable, not to the owner)

Governing card: "A blocked build must escalate to a Fable arbiter, not to the owner — only owner-only decisions reach chat."
State surveyed 2026-08-17 against `trident/` + `open/composer.ts` + `migrations/`. No arbiter tier exists anywhere in the tree today; both conflict escalation sites still terminate with prose telling the owner to do git by hand (`merge.ts:705` "needs a manual rebase", `orchestrator.ts:583` "needs a human (or a fresh build) to reconcile it"). Note: the card cites `merge.ts:686`/`:692` — those throws now live at `merge.ts:704`/`:712`/`:725`; the card's `orchestrator.ts:428` message is at `orchestrator.ts:583` (`TridentRebaseConflict`).

- [x] Prereq (card 01M0360N7V5WE1MBWF0Q4SRZWF): bounded Forge conflict resolver wired at BOTH conflict sites — local-merge rebase (`merge.ts` `rebaseBranchOntoBase`) and autonomous publish replay (`orchestrator.ts` ~1220–1260, `mode:'replay'`), built in `open/composer.ts:5629` over `makeEphemeralSubstrate`. The arbiter is the tier ABOVE this.
- [x] Terminal failures already interpreted into plain language, never raw stderr (#352 `interpretFailure`, `delivery.ts`) — groundwork for acceptance 6, but the authored escalation prose still directs the owner to manual git work.
- [x] 1. `trident/arbiter.ts` + `trident/arbiter.test.ts`: the bounded Fable-tier arbiter module — types (`ArbitrationInput`/`ArbitrationOption`/`ArbitrationOutcome`/`ArbitrationRecord`/`TridentArbiter`), read-only tool surface (no Edit/Write), `DECISION:`/`OWNER_ONLY:` terminal-marker protocol with conservative `unavailable` fallback, deterministic owner-only pre-guard (spend/deploy/publish/send questions never reach the model), forbidden-option guard (approve/merge/skip-review are unofferable), per-run invocation cap. Modeled on `conflict-resolver.ts`. Everything below consumes this contract. ← THIS ITERATION
- [ ] 2. Persistence: migration `0125_code_trident_runs_arbiter_log.sql` adding `arbiter_log TEXT` (JSON array of `ArbitrationRecord`) to `code_trident_runs`; `trident/store.ts` reads it onto `TridentRun` and gains a single-writer `appendArbitration(id, record)` (same ownership discipline as `harvested_at`/`crash_recoveries` — absent from `update`/`save`); update `migrations/expected-schema.txt`; store tests.
- [ ] 3. Route the PUBLISH path through the arbiter (acceptance 1): in `orchestrator.ts`, when the replay resolver is absent/declines/exhausts (`TridentRebaseConflict` at lines 1222/1230/1242/1253/1293), consult a configured `arbiter` with options `retry-resolution` (one extra bounded resolver round carrying the arbiter's guidance) / `rebuild` / `stop`; apply the decision, append the `ArbitrationRecord` via the store, and only fall through to terminal failure on `stop`, `owner-only`, or `unavailable`. Enforce the per-run cap here too (acceptance 5). Tests against the existing publish-rebase fixtures.
- [ ] 4. Route the LOCAL-MERGE path through the arbiter: same treatment for the three `TridentMergeConflictEscalation` throws in `merge.ts` (`rebaseBranchOntoBase`, lines 704/712/725) — arbiter decides before the escalation reaches the `applyResult` catch (`orchestrator.ts:2368`) and chat.
- [ ] 5. Route the `inner-error` terminal paths: before `failedRun` finalizes a run whose reason no one reads (`innerTerminalFailureReason` catch-all, `orchestrator.ts:~2402`), let the arbiter judge rebuild-vs-genuinely-stop for arbitrable failure classes; owner-only and unavailable still terminate as today.
- [ ] 6. Composer wiring: build the arbiter in `open/composer.ts` beside the conflict resolver (~line 5629) over `makeEphemeralSubstrate('cc-trident-arbiter')` at `FABLE_MODEL`, thread through `gateway/composition/input/misc-input.ts` + `build-core-modules.ts` into orchestrator opts + merge deps, gated on the same live-credential predicate as `resolve_conflict`.
- [ ] 7. Surfacing + prose (acceptances 2 and 6): `trident/delivery.ts` terminal message appends a "Decided in my absence" section rendering the run's `arbiter_log` (question → decision → reasoning) beside the verdict; rewrite the residual manual-git escalation prose (`merge.ts:705`/`:713`, `orchestrator.ts:583`) into well-formed questions with the options worked out — never "do a manual rebase"; keep `isAuthoredConflictQuestion` in sync.
- [ ] 8. End-to-end acceptance tests: (1) a publish-path conflict the resolver cannot fix reaches the arbiter before chat; (3) a spend/deploy/publish question is NOT auto-arbitrated and still reaches chat; (4) arbiter cannot approve/merge/skip review (structural: option guard + no path from an arbiter outcome to `inner_verdict='APPROVE'`/`phase='done'`); (5) invocation cap holds across a pathological loop.
- [ ] 9. Docs: `docs/AS_BUILT.md` entry + `docs/SYSTEM-OVERVIEW.md` section on the escalation ladder (resolver → arbiter → chat, with the owner-only test verbatim).
