# IMPLEMENTATION_PLAN — a run that never reviewed must not be recorded as REQUEST_CHANGES (Plan card #1)

Card: 97 of 160 REQUEST_CHANGES rows in 30 days carried NO findings (33 of them at forge-done — built work discarded and replayed). Fix: a rejection with no findings is refused AT THE WRITE SITE and the true state (`REVIEW_NOT_RUN`) recorded instead; distinguish died-before-build vs built-never-reviewed vs reviewed-rejected; a built-but-never-reviewed commit seeds the next dispatch instead of being rebuilt WHERE THE BRANCH TIP CAN BE PROVED (see the scope line below — that is `outer-published:*` in pr mode and every salvageable checkpoint in local mode, and it covers 0 of the 33 measured pr-mode `forge-done` rows); genuine findings still record REQUEST_CHANGES (positive control). Never default to APPROVE; never rewrite historical rows; the check lives at the write site, not only in callers.

Resume state (round 26): the branch head is round 26's fix commit on `trident/1-measured-cost-97-of-160-rejection`; PR #487 OPEN. The record moves with the tree — this paragraph is written IN THE SAME COMMIT as the fix. Round 26 fixes the two blockers the review of `a3352760` reproduced. (1) BLOCKER — the prod-boot wiring guard passed with the wiring COMMENTED OUT. Its comment strip knew only a comment's first line, so wrapping the live `hostRunner: tridentHostRunner,` in `/* … */` dropped the two delimiter lines and KEPT the property between them: every count and every site scan read the site as wired while the runtime object carried no `hostRunner`, and the field is optional so tsc said nothing. The strip now carries block state across lines, and the mutant is applied to the real `composer.ts` at the bottom of the test (executed here: 3 pass / 0 fail clean, 2 pass / 1 fail on the mutant). (2) BLOCKER — valid non-ASCII findings walked past the ASCII cost gate into the quadratic SUBSTR scan on the TERMINAL-WRITE path, with no size bound. The gate's premise ("every payload this system writes is all-ASCII, `JSON.stringify` escaping the rest") is false — `JSON.stringify` emits raw non-ASCII bytes and LLM findings carry em dashes. Measured on this box: 0.18 s at 8 K characters, 0.73 s at 16 K, 3.0 s at 32 K, 12.4 s at 64 K. The literal an invocation BRINGS is now decided in bash in one linear pass (`utf8_verdict`, `iconv -f UTF-8 -t UTF-32LE` — verified to be exactly the scan's boundary, rejecting orphan/overlong/surrogate/truncated/out-of-range and accepting U+FFFE/U+FFFF, which `-t UTF-8` alone does not), so a constant reaches SQL at any size; the old row's STORED column still needs SQL and keeps the scan under a 16,384-character ceiling whose above-the-ceiling answer is per-question fail-closed (1 for "must I refuse to erase", 0 for "may I record a rejection"). Pinned by a 64 K-character well-formed non-ASCII rejection that lands REQUEST_CHANGES in bounded time, with the same payload plus one orphan continuation byte as the positive control (still REVIEW_NOT_RUN). (3) MINOR — `run-disposition.ts`'s header now scopes its trustworthy-count claim to rows the fixed write site produced and points at the AS_BUILT statement for history (over the 30-day base this classifier answers `reviewed-rejected` 160 times, not 63). (4) MINOR — `sql_quote` falls back to the pure-bash substitution when `sed` fails, so a subprocess failure under `set -euo pipefail` can no longer abort the UPDATE and leave the run's row stale. No reviewer decision, severity or gate changed.

CARRY-FORWARD (verified; do not re-derive, do not re-litigate):
- BOTH salvage tags are SPENT. `trident-salvage/39f43bd0-…` was applied across rounds 4–8; `trident-salvage/9e124906-…` is a FALSE POSITIVE byte-identical to `add7d0f1` whose recovery would delete `read_file_literal` from trident/checkpoint.sh and revert the round-8 fix. NEVER recover either.
- If a relaunch is refused with "already carries N commit(s) not on origin/main": compare `git rev-parse <branch>` vs `origin/<branch>` FIRST — equal shas plus no worktree holder means the local ref is stale cleanup residue; delete the local ref and relaunch (Forge re-cuts it from origin).
- REVIEW COVERAGE: no completed review has ever covered more than the first 3,000 diff lines (r7: 3,000 of 4,594; r9/r11: 3,000 of 5,506; r13: 3,000 of 6,107; r16: 3,000 of 6,692), and in git default file order that window has NEVER reached checkpoint.sh, inner-workflow, orchestrator, run-disposition or store. Round 12 CLAIMED never-reviewed-first ordering and shipped none: the producer was untouched. Round 17 implemented it IN THE PRODUCER (`publishBuiltCommit`, trident/orchestrator.ts), so the artifact is ordered by construction rather than by a promise — VERIFY it on the artifact anyway, and state in the PR body which portion the review actually covered rather than letting a clean round read as a reviewed diff.
- Keep this plan body compact and REPLACE the resume-state section each round instead of appending; an oversized plan body is what stopped the card from running at all.

- [x] Write-site precondition (trident/store.ts `update()`): refuses `inner_verdict='REQUEST_CHANGES'` when the effective findings decode empty — `TridentEmptyFindingsRejectionError`, inside the same transaction as the UPDATE; falsification tests in store.test.ts.
- [x] Out-of-process write site (trident/checkpoint.sh): one atomic SQL CASE (`json_valid` + `json_type='array'` + `json_array_length>0`) demotes a findings-free REQUEST_CHANGES to `REVIEW_NOT_RUN` inside the same UPDATE — REFUSED-not-FAILED, the rest of the write still lands; `read_file_literal` reads each findings file exactly ONCE (defeats the alternating-file race, reproduced at iteration 132/300 pre-fix) and round-trips bytes verbatim, trailing newline included; artifact bodies reach sqlite3 via STDIN, never argv (a 33 KB findings array once died `Argument list too long` — Linux caps one argv element at 128 KiB); an orphan `[]` write cannot ERASE a settled rejection (it may still ADD real findings; live rows still demote; terminal APPROVE/`REVIEW_NOT_RUN` rows untouched). Pinned in checkpoint-sh.test.ts + review-run.test.ts.
- [x] Distinct no-review terminal state: `REVIEW_NOT_RUN` verdict + schema CHECK constraint (migration 0138); clearing writes are SQL NULL.
- [x] Disposition taxonomy (trident/run-disposition.ts): reviewed-rejected / built-never-reviewed / died-before-build; legacy findings-free REQUEST_CHANGES rows stay countable and are never rewritten.
- [x] A built-but-never-reviewed run seeds the next dispatch instead of being rebuilt (trident/board-dispatch.ts `builtButNeverReviewedSeed`) — **SCOPE, STATED RATHER THAN IMPLIED (Argus r20 blocker):** the seed proves the tip through the ref the launch will read, which is `ls-remote --heads origin` in pr mode, and Forge is told not to push in pr mode. So what it covers TODAY is `outer-published:*` in pr mode plus every salvageable checkpoint in local mode; of the 33 findings-free `REQUEST_CHANGES` rows measured at `forge-done`, **33 are pr mode and 0 are local**, so this box covers NONE of them. Proving them against the LOCAL ref would seed a row `classifyResume` then rebuilds anyway (`resolveResumeLiveHead` reads the same origin ref one process later), so closing that last gap means a publish-then-review recovery in the resume classifier, which is deliberately NOT in this card. Pinned by trident/board-dispatch.test.ts 'PR MODE: a forge-done prior with NO origin branch seeds nothing — the workflow would rebuild it', and stated at length in docs/AS_BUILT.md: checkpoint/head/findings/base-pin carried onto the new row only after proving the branch tip is still the recorded commit; seeding requires the card's OWN `linked_run_id` and FAILS CLOSED when the link is absent/null/whitespace-only; task-text comparison defeats the 35-char slug collision; a `stopped` prior never seeds; the tip and landed-PR probes are INVOKED inside try/catch so a synchronous thrower degrades to no-evidence instead of escaping `dispatchBoardBoundBuild`.
- [x] launch() revalidates the seed against the live head (trident/orchestrator.ts "SEEDED RESUME — REVALIDATED AT LAUNCH"); recovered rows (`crash_recoveries`/`infra_retries` > 0) never seed; the leftover-branch ownership check is ARMED for seeded rows (`freshLaunch || seeded_resume`) with the containment/own-crash-leftover exemptions.
- [x] Trustworthy counting: canonical SQL in docs/AS_BUILT.md ("2026-08-31 — a run that never reviewed is no longer recorded as a rejection"), executed row-for-row against the classifier by trident/as-built-disposition-sql.test.ts with mutant guards; the "frozen legacy count" comment corrected to a claim about the two known writers, not a database constraint.
- [x] Positive controls: a genuine review with real findings still records REQUEST_CHANGES with those findings (checkpoint-sh.test.ts, corpus real-rejection rows, orchestrator terminal-path tests).
- [x] Review-driven hardening, fixed in branch CONTENT (rounds 7, 11, 13): linear two-pointer ASCII trim in trident/ascii-trim.ts (CodeQL HIGH js/polynomial-redos reports `fixed` on the PR head); findings validators aligned at `SQLITE_JSON_MAX_DEPTH = 1000` with a linear depth scan, bound documented at the checkpoint.sh write site; `parseCheckpointFindings` rejects a leading U+FEFF (round 16 re-measured the claim that the two SQLite builds disagree about it: they do not — see the resume state); `save()`/`saveIfActive()` are PATCH-WINS exactly like `update()` — the stored row is consulted only when the incoming column is NULL, so a `'[]'` snapshot can no longer clear the guard on evidence its own COALESCE was about to erase (FALSIFICATION 3, plus the NULL-snapshot positive control); trident/delivery.ts no longer tells a never-reviewed terminal row's operator it "reached max_rounds without Argus APPROVE".
- [x] Rounds 2/4 salvage recovered and replayed (9a10bb68, 0afd77bf); rounds 10/12 truth-synced both committed plan bodies and PR #487's title/body from the stale "docs only" draft to as-built state.
- [x] ROUND 15 — handed `88f699e2` to review, plan bodies only.
- [x] ROUND 16 — fix the review of `88f699e2`: BOM pinned in the canonical SQL (both statements), in `checkpoint.sh`'s `findings_case` and re-measured in `checkpoint-findings.ts`, with a BOM corpus row that reaches the table AS BYTES (a bound parameter loses the mark) and a textual guard that the doc keeps the pin; the erasure guard armed on the row's CLAIM, pinned by "a LEGACY terminal REQUEST_CHANGES -- `[]` or NULL findings -- is not demoted either" (mutation-checked: restoring the stored-findings clause reds it) with the disposition assertion that says why it matters; the seed's sixth precondition enumerated in both docs; the terminal-result duplication decision recorded at the seat. No build behaviour beyond those two fixes changed.
- [x] ROUND 17 — fix the review of `cb0264b1`: the credentialed host runner WIRED from the composition root to all four production dispatch sites (the blocker: the salvage was inert against a private origin); the published reviewer diff ordered never-reviewed-first in the producer itself; the erasure guard extended to verdict-carrying orphan writes and to terminal rows holding real findings; the bash BOM clause pinned; two doc corrections (`create` is not a throwing guard site; INVARIANTS #118 pins). No reviewer decision, severity or gate was changed.
- [x] ROUND 18 — fix the review of `8df3cbb2`: `work_board_start` forwards `host_runner` (blocker, mutation-proven per entry); the diff ordering reads the pin production actually writes (blocker); the disposition SQL guards embedded NULs (blocker); the path-universe listing carries `--no-renames` (major) and stands down on a C-quoted path; the composer wiring guard stops passing on comments (major). No reviewer decision, severity or gate was changed.
- [x] ROUND 19 — fix the review of `311b3bca`: the trident typecheck leg made green (blocker, the whole tsconfig matrix run, not one leg); the publish handoff EMITS the pin the ordering reads, proven through the real workflow and the real reader (blocker); the composer wiring count anchored to real properties so the reviewer's mutant reds it (major); the erasure guard extended to the bare verdict-only write (minor); the NUL guard made symmetric across both rejection counts (minor); two docblock corrections. No reviewer decision, severity or gate was changed.
- [x] ROUND 20 — fix the review of `0c0d1852`: the composer wiring guard made per-object-literal so the reviewer's decoy mutant reds it (blocker); the plan's headline salvage claim truth-synced to the scope `AS_BUILT` already recorded, 0 of the 33 measured rows included (blocker); the reorder's fix-round-only reach stated where it is documented (major); the erasure guard extended to every verdict that brings no findings (minor); `save()`'s non-null direction pinned (minor); split-diff pathspecs made literal (nit). No reviewer decision, severity or gate was changed.
- [x] ROUND 21 — fix the review of `4bd85ef0`: the wiring guard's site set derived from the chokepoint's required fields rather than from the property under test (blocker); `checkpoint.sh`'s CLEARING write (`inner_verdict ''`) frozen against a settled terminal rejection on the same terms as its `REVIEW_NOT_RUN` sibling (blocker); the publisher stands the reorder down on a whitespace-padded path instead of dropping that file (minor); the `hostRunner` docblock and the SYSTEM-OVERVIEW backfill wording corrected (minors). LOST BY THE REBASE REPLAY, RESTORED IN ROUND 22.
- [x] ROUND 22 — restore every round-21 fix the replay dropped (blocker, diffed commit-against-commit rather than trusted to a green suite); the wiring guard reads what the literal BINDS, not what it mentions, so codex's delete-nothing override mutant reds it (blocker); one card per shape in the non-qualifying-prior table plus a qualifying positive control (major); the NUL clause added to `findings_case` for parity with the counting SQL (nit); three false behavioural comments retracted with measurements (nits). No reviewer decision, severity or gate was changed.
- [x] ROUND 23 — fix the review of `36295647`/`839e6e4b`: the dispatch-site wiring scan reports a literal it cannot statically READ as UNWIRED, so a computed-key override (`...{ ['host' + 'Runner']: undefined }`) can no longer bind `hostRunner` to undefined behind a green guard (blocker), with the hold sweep's conditional spreads kept readable by a positive control; the seed DOMAIN enforced in `TridentRunStore.create` via the shared `reviewCapableCheckpoint` rather than only in `builtButNeverReviewedSeed` (major); SYSTEM-OVERVIEW's recovery list given the pr/local boundary AS_BUILT already states (major, docs); `LC_ALL=C` around `checkpoint.sh`'s two round tests, because a locale-collated `[0-9]` matched a non-ASCII digit and the `10#` arithmetic aborted the whole UPDATE under `set -e` (minor). Committed with a round-22 plan body — see round 24.
- [x] ROUND 24 — fix the review of `106c6dcf`: both plan copies truth-synced to the tree (blocker — see the resume state); the seed guard extended from the checkpoint NAME to the two columns that say WHICH commit, `TridentIncompleteSeedError` on a seeded row without a 40-hex `inner_checkpoint_head` and `base_sha` (major); the guarded values persisted instead of the raw arguments (minor); `sql_quote` made linear (minor); the documented real/legacy counts pinned row by row instead of by total (minor); the reorder's file-count guard described as what it is (nit). No reviewer decision, severity or gate was changed.
- [x] ROUND 25 — fix the review of `a00e2c45`: malformed UTF-8 no longer reads as a real rejection to the counting SQL while the classifier reads it as no findings at all (blocker) — a re-encoding scan in both documented statements and in `checkpoint.sh`'s `findings_case`, behind an ASCII cost gate, executed by raw-byte corpus rows and by real script writes, verified against TextDecoder over 231,008 sequences on both engines; `create` refuses an UNSEEDED row carrying a resume pin (`TridentUnseededPinError`) so the write-site precondition is symmetric (minor); `LC_ALL=C` around `phase_for_checkpoint`'s digit tests so the bash mirror and `phaseForCheckpoint` cannot disagree under the ambient locale (minor); the codex-only wiring-scan bypass re-run and refuted in place. No reviewer decision, severity or gate was changed.
