# IMPLEMENTATION_PLAN — fetch and branch from origin/main (stale-base elimination)

Regenerated 2026-08-16 against the card spec, from a survey of the repo of record (local main 4eb3adc — itself measurably behind origin, which is the defect in vivo: `.gitattributes` (#308) and the per-branch plans dir are on origin/main but absent from this checkout). Diff of spec vs code: the publish-time repair machinery exists and is healthy; NOTHING on the LAUNCH path fetches or pins a base. `launch()` (trident/orchestrator.ts) resolves only a base branch NAME; `forgeStep1` (trident/inner-workflow.mjs ~1137) tells Forge `git switch -c <branch>` from a worktree forked off stale local HEAD; the run row (code_trident_runs, migrations/expected-schema.txt) has no base-sha or behind-count columns.

- [x] Publish-time rebase replay onto the observed base exists and keeps its patch bytes (`rebaseOntoObservedBase` in trident/orchestrator.ts; #292/#296/#318 lineage). NOT to be touched by this card — the goal is to start current, not to repair better.
- [x] The publish-time review diff is taken from the ls-remote-OBSERVED base sha, not the stale local ref (round-5 fix, shipped).
- [x] `docs/AS_BUILT.md merge=union` in `.gitattributes` (#308) is on origin/main, defusing the append-collision half (with the documented union caveat: a genuine rewrite is silently doubled, not flagged).
- [x] Per-branch plan files (`.trident/plans/<branch>.md`) replaced the shared root `IMPLEMENTATION_PLAN.md` scratch file (on origin/main per the card; the stale root copy in this checkout is itself a stale-base artifact).
- [x] **P1 — Fresh-base pin at launch (spec items 1+2, end-to-end for the Claude forge path).** In `launch()` (trident/orchestrator.ts), on a FRESH pr-mode fire (no checkpoint, no branch): `git fetch --no-tags origin <base>` via `opts.run_host` in the shared checkout, resolve `refs/remotes/origin/<base>` to a 40-hex `base_sha`, measure `base_behind` (`rev-list --count refs/heads/<base>..refs/remotes/origin/<base>`), record BOTH on the run row (migration 0124 + store.ts), thread `baseSha` through `buildWorkflowArgs` into inner-workflow.mjs so `forgeStep1` cuts the branch with `git switch -c <branch> <baseSha>` and the forge diff (contract step 5) is `git diff <baseSha>..HEAD`. Fetch failure = loud named pre-fire infra terminal (no fire, never a silent fallback to the stale local ref). Resume/reenter launches stay byte-identical.
- [ ] P2 — Codex build path parity: pass the pinned base sha (when present) as `trident/codex-build.sh`'s diff base (arg 2 at the inner-workflow.mjs call site, used at the trailer-diff regeneration sites) so a codex build's diff matches the pinned base instead of stale local `<base>`; the brief's contract text itself inherits P1 via `forgeBuildContract`.
- [ ] P3 — Surface the staleness (rest of spec item 2): include `base_sha` (short) and `base_behind` in the run's handoff/delivery text and progress/board detail so a stale-base failure is tellable from a real one without hand-diffing.
- [ ] P4 — Distinguish "could not merge" (infra) from "review said no" (verdict) on the board and in the handoff (spec item 3). They currently share the `argus-request-changes` checkpoint and the same red card. OVERLAPS card 01M010V2R4CB50ZKYXBGNSP2C7 — coordinate rather than duplicate; scope here is at most a distinct named terminal cause for merge/publish infra stops, deferring board rendering to that card if it owns it.
- [ ] P5 — Follow-up (same defect class, resume path): `writeResumeDiff` (inner-workflow.mjs ~3287) diffs `<stale local base>..<oid>`, so a resumed review can carry already-merged upstream noise; take it from the pinned/observed base sha when available.

Guard rails (from the card): do NOT weaken the merge gate or the publish-time rebase to make conflicts disappear; do NOT touch `rebaseOntoObservedBase`/`publishBuiltCommit` semantics; the reenter (crash-resume / fix-round) branch path is untouched.
