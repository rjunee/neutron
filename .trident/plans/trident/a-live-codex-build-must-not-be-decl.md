# IMPLEMENTATION PLAN — a live codex build must not be declared DEFERRED while the builder is still working

THE MECHANISM, NAMED (work item 1, done): the wait is the BRIDGE seat's bounded poll in `codexBuildPrompt` (trident/inner-workflow.mjs:1643-1644 — `for i in $(seq 1 108) … sleep 5`, ≤5 repeats = 45 minutes, "matching the fire session's absolute ceiling"). When it expires — or the bridge seat itself dies — the bridge reports `trailerComplete=false`, and `forgeAgent` (inner-workflow.mjs:1689-1692) throws an UNCONDITIONAL "the build wrapper was killed before it could report", which the top-level catch (line 6077) records as `checkpoint: 'inner-error'`. Nothing between "trailer absent" and "declared dead" checks whether the wrapper is alive; line 1646 of the bridge prompt literally instructs the false kill claim. The wording is pinned at trident/__tests__/cross-model-dispatch.test.ts:674, and docs/SYSTEM-OVERVIEW.md:3026-3027 repeats the false doctrine.

- [x] Trailer written atomically (temp file + rename) so a partially-written trailer cannot read as "empty or missing" — trident/codex-build.sh:619-633 (prior card, run 8620a7d1).
- [x] Wrapper detached from the Bash tool's 600 s per-call ceiling (`nohup setsid` + exit-file supervisor) so the wrapper itself is not killed by the caller's bound — codexBuildPrompt run command, inner-workflow.mjs:1641.
- [x] 1. TRUTH-ORDER TRAILER WAIT in `forgeAgent` (work items 1, 2, 4-in-run, 5): before any DEFERRED throw on `trailerComplete !== true`, dispatch a deterministic liveness/collection probe (fixed Bash via `agent()` + `seatAttempt`, same shape as `readBuiltHead`): supervisor pid alive (new `.pid` file written by the supervisor's own `$$`), stderr growth across a bounded in-Bash wait, exit-file contents, then the trailer. A live supervisor or a growing `.err` VETOES the deferral and extends the wait (bounded probe loop); a trailer that landed late is parsed IN CODE (`parseBuildTrailer`) and the run continues as a connected build — published and reviewed, `testsPassed=false`/`suiteOutcome='not-run'` (the existing full-suite gate makes that a blocker finding, not a strand). The terminal cause says "killed" ONLY when the exit file records a signal death (rc ≥ 128); otherwise it says the trailer had not appeared within the wait. The bridge prompt's line-1646 instruction to claim a kill is removed. Pinned-wording tests updated; new tests for the growth veto and the late trailer.
- [ ] 2. `awaiting-trailer` RECOVERABLE CHECKPOINT (work items 3, 4-after-terminal): when the extended wait exhausts with the wrapper still observably alive, write checkpoint `awaiting-trailer` via `checkpoint()` BEFORE the throw — distinct from `inner-error` — and preserve the worktree; teach `classifyResume`/`resumeOnUnchangedHead` (inner-workflow.mjs:2568-2633) plus the launcher's fast-exit mirror `resumeHeadDecides` (orchestrator.ts ~1441-1460, kept in lockstep per inner-workflow-resume.test.ts:583-590) a new mode that, on relaunch, probes the trailer file first: trailer landed → reuse the existing commit (review path, `writeResumeDiff` from the trailer head, outer publisher pushes it) instead of rebuilding; trailer still absent and wrapper dead → rebuild. A test proves a resumed `awaiting-trailer` run reuses the existing commit and never re-dispatches forge:build.
- [ ] 3. DOCS: correct docs/SYSTEM-OVERVIEW.md:3026-3027 (currently: an empty/missing trailer "is a hard DEFERRED failure meaning the wrapper was killed before it could report") to describe the truth-order wait and the `awaiting-trailer` checkpoint, and add the docs/AS_BUILT.md entry recording the 2026-08-17 four-lane incident (runs 7a2d1fec, 3953261a, 68b1026b) and this repair.
