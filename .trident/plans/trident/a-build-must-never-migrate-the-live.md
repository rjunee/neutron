# IMPLEMENTATION_PLAN.md — migration governance card (tracker #575, P0)

Card: a build must never migrate the live database; an ordinal collision must not silently skip a real migration. Four branches claimed ordinal 0122; the unmerged one ran against the LIVE DB; the runner (`migrations/runner.ts`) keys only on the version integer so the other three were silently skipped. Live `_migrations` row 122 deliberately reads `work_board_items_pr` and MUST NEVER be rewritten (binding from #575).

Ground truth surveyed 2026-08-16 on main @ fe4b9f40: no name comparison in the runner, no repairs mechanism, no CI ordinal gate in `.github/workflows/ci.yml`, no `NEUTRON_HOME` scoping or refusal in `trident/`. Main now carries `0122_trident_checkpoint_head.sql` and a renumbered `0123_code_trident_runs_crash_recoveries.sql` (#267 landed renumbered). `migrations/snapshot.test.ts` + `expected-schema.txt` exist (relevant to a3). Mutation-test prior art: `trident/git-mode-mutation.test.ts`.

- [x] **T2 + (b)(d)(a4) — ordinal-collision guard in the migration runner.** `applyMigrations` pre-scans: (1) two files in the loaded dir claiming the same ordinal → throw naming both files; (2) a version recorded in `_migrations` under a DIFFERENT name than the file now claiming it → throw naming both names + the version, applying NOTHING. Explicit repair acknowledgments live in a checked-in `migrations/repairs.json` (ships the 2026-08-14 incident entry for version 122 `work_board_items_pr` vs `trident_checkpoint_head`, so the live DB still boots); an acknowledged mismatch is skipped WITHOUT rewriting the `_migrations` row, and an audit row lands in a new `_migration_repairs` table. No auto-apply, no auto-rename, ever. MUTATION-TESTED per #575: disabling the name comparison in a mutant copy of runner.ts must turn the guard test red (subprocess `bun test`, green control on the unmutated copy). Tests pin: both names in the error; nothing applied on refusal; row 122's recorded name never rewritten; the repairs.json incident entry exists (guards against a future "tidy-up").
- [ ] **T1 + (a)(a5) — a migration run whose home is not the build's own is REFUSED, fail-closed.** Mechanism per #575's preference (refusal over env-sanitising): the data home records which source checkout owns it (a marker file, e.g. `<NEUTRON_HOME>/.migrate-owner`, claimed on first migrate / written by install.sh); `applyMigrations` refuses when the runner's own module location (`HERE`) is not the recorded owner checkout — so a trident worktree inheriting the live `NEUTRON_HOME` cannot touch the live `_migrations` or schema no matter what env it inherits. Trident build children get their own home inside the build workspace. The GUARD-AGAINST-A-FAKE-TEST clause is binding: the test must reproduce the real `NEUTRON_HOME`-inheritance condition against the path the live gateway resolves (`resolveOpenDbPath`), assert the REFUSAL itself (not just absence of writes), and assert live `_migrations` + schema are byte-identical after the attempt. This mechanism is the shared closure for the credential-scope incident ("two incidents, one hole") — note that linkage in docs; do not build a second mechanism there.
- [ ] **T4 + (a2) — CI fails a PR whose migration ordinal already exists on main.** New CI step diffs `migrations/NNNN_*.sql` on the PR head against `origin/main`: any ordinal present on main under a different filename → fail, naming both files. Backstopped in-tree by the T2 duplicate-ordinal throw. Assert with a fixture that duplicates a real ordinal (e.g. a temp file colliding with 0122) proving the check goes red, plus a green control.
- [ ] **T3 + (c) — the #269-shape merge must not break boot on a DB that already carries `work_board_items.pr`.** Add a declarative guarded-migration convention to the runner (e.g. a structured `-- @skip-if:` precondition header that, when satisfied, records the version in `_migrations` with a skip note instead of executing — declared in the reviewed file, so nothing is AUTO-applied or auto-resolved). Test seeds a DB with the `pr`/`pr_url` columns already present out-of-band (exactly the live state — a fresh DB proves nothing, per the fake-test guard) and asserts boot-time migrate succeeds without duplicate-column errors and without rewriting row 122.
- [ ] **(a3) — a table-rebuild migration is verified against CURRENT main's schema, not the schema its branch was cut from.** Static check (CI or test): a migration whose body rebuilds an existing table (`CREATE TABLE <x>_new` / rename-swap) must carry every column the table has after applying the full current chain — catching #268's shape, which would DROP `pr`/`pr_url` if merged after #269. Verify what `snapshot.test.ts` already catches post-rebase and close only the real gap.
- [ ] **Docs + carry-back.** AS_BUILT entry; INVARIANTS lines for: the collision guard, the never-rewrite-row-122 rule, and the home-ownership refusal; note in the card/plan doc the fact #575 lacks (FOUR files claimed 0122, not two; #267/#268 were built against DBs missing their own columns; #268's rebuild is a data-loss ordering hazard).
