# IMPLEMENTATION_PLAN.md — migration governance card (tracker #575, P0) — RE-OPENED 2026-08-17

Card: a build must never migrate the live database; an ordinal collision must not silently skip a real migration. Re-opened 2026-08-17: T2 (the collision guard) landed via PR #336 and is confirmed present on main; T1 (a build inheriting the live `NEUTRON_HOME` must be REFUSED, not sanitised) was never built, and the same defect recurred at ordinals 124 and 125 with the guard already merged — the guard converts silent skew into loud refusal but does not stop a build writing the live instance. The `_migrations` row 122 reading `work_board_items_pr` must NEVER be rewritten (binding, #575).

Ground truth re-surveyed 2026-08-17 on main @ 89de7523: `migrations/runner.ts` carries the name-mismatch refusal, `repairs.json` acknowledgements (122, 124, 125), untracked-file refusal, provenance columns, and duplicate-ordinal throw — all mutation-tested (`migrations/__tests__/ordinal-collision-mutation.test.ts`, `untracked-migration-mutation.test.ts`, `provenance-ownership-mutation.test.ts`). NO ownership marker or home-refusal exists: `git grep` for `migrate-owner|migrateOwner|MIGRATE_OWNER` matches only the plan card and an unrelated string in `gateway/index.ts:1224`. Production migrate entries: `gateway/index.ts:295` (`applyMigrationsToProjectDb`), the runner CLI (`runner.ts:776-807`, resolving via `resolveOpenDbPath`), sidecars via `applyProjectScopedMigrations`. `install.sh` migrates `$NEUTRON_DB_TARGET` from checkout `$SRC_DIR` (~line 1455-1490). Trident build children inherit the gateway's env (no `NEUTRON_HOME` override anywhere in `trident/`).

- [x] **T2 + (b)(d)(a4) — ordinal-collision guard in the migration runner.** LANDED (PR #336, confirmed on main): recorded-name vs file-name comparison with loud fail-closed refusal, `migrations/repairs.json` hand-verified acknowledgements audited into `_migration_repairs`, duplicate-ordinal throw naming both files, untracked-stray refusal with tree provenance, no auto-apply/auto-rename ever, and the #575-required mutation tests (disabling the name comparison turns a test red).
- [x] **T1 core + (a)(a5) — home-ownership marker + fail-closed refusal in `applyMigrations`.** The data home records its owning checkout in `<dirname(db)>/.migrate-owner` (claimed on first migrate; written/refreshed by `install.sh` as the explicit ownership-transfer ceremony). `applyMigrations` REFUSES — before any read of the ledger and before any write — when the runner's own module location (`HERE`, `runner.ts:15`) is not the recorded owner, so a trident worktree inheriting the live `NEUTRON_HOME` cannot touch the live `_migrations` or schema no matter what env it inherits. Refusal, never sanitise-and-continue; the runner never rewrites an existing marker. Binding falsification tests: reproduce the real `NEUTRON_HOME`-inheritance condition through `resolveOpenDbPath` (runner CLI subprocess with inherited env, no argv path); assert the REFUSAL itself; assert `_migrations` + `sqlite_master` + marker byte-identical after the attempt; NEGATIVE control (mutant with the ownership comparison disabled goes red, unmutated control green); production call-site pin (`gateway/index.ts` calling `applyMigrationsToProjectDb(db)` — deleting it turns a test red). Implementation constrained to `runner.ts` itself (a new relative import would break the cpSync lists of the three existing mutation tests).
- [ ] **T1 part 3 — trident build children get their own data home inside the build workspace.** The spawn/dispatch seam that composes a build child's environment sets `NEUTRON_HOME` (and clears `OWNER_HOME` / `NEUTRON_DB_PATH`) to a per-run dir inside the build workspace (e.g. `<worktree>/.neutron-home`), created at dispatch — so a build that legitimately needs a database migrates its OWN and the T1 refusal never fires for it. Unit-test the composed child env; the refusal from the previous task remains the fail-closed backstop regardless of this seam.
- [ ] **Docs + shared closure.** `docs/AS_BUILT.md` entry; `docs/INVARIANTS.md` lines for the home-ownership refusal (alongside the existing collision-guard and never-rewrite-row-122 rules); record the #575 "two incidents, one hole" linkage — this mechanism is the shared closure for the credential-scope incident too, and no second, different guard may be built on the credential path; note the claim-window caveat (a pre-marker home is sealed by the first legitimate migrate or by install.sh) and the backup/restore + intentional-move remedy (re-run install.sh).

Out of scope — re-scoped 2026-08-17 per the card ("What remains to build — T1 only"): renumbering the still-open colliding PRs #268/#269/#314, the branch-time ordinal allocator, and the CI duplicate-ordinal gate (T4/a2/a3) belong to card 01M07AKY0X3JNJXZX31A8DNKN2; repairing the damage already done to the live database, including the #269/(c) guarded-migration disposition for the out-of-band `work_board_items.pr` columns (T3), belongs to card 01M083BXK2A5PTWWYFVB5G6DR1.
