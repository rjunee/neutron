# IMPLEMENTATION_PLAN — Branch-state brief: the continuation planner hands FORGE a warm, bounded brief instead of a cold start

Regenerated 2026-08-18 against main (567dbb52). Card: every Ralph task restarts FORGE cold — give the CONTINUATION PLANNER (`plan:next`, trident/inner-workflow.mjs) one additional bounded output, a BRANCH-STATE BRIEF (BUILT / SEAMS / REJECTED / SUITES), REGENERATED each round from the branch + committed plan, hard-capped in code, and handed to Forge in `ralphExecuteNote`. The transcript still starts clean; only derived facts travel. Sequencing note: the colliding card `01M09HVMZ9` (full-suite gate) is ALREADY on main (`fullSuiteFindings` in inner-workflow.mjs) — no constraint remains; this card builds alone. Honesty note per falsification criterion 4: the BEFORE number is measured (codex build 30m01 on the 2026-08-18 continuation round of `trident-cannot-review-an-existing-p`); the AFTER can only be measured on the next real multi-task card post-merge — T2 records the protocol so it will be quoted, and if it does not drop the card says so rather than keeping the feature. State check at this regeneration: T1 is committed on this branch (commit 4a6e6710; `bun test trident/inner-workflow-plan-next.test.ts` 51 pass / 0 fail on the branch tree; `tools/lane_review.sh` exit 0 vs origin/main) and its tests already cover falsification criteria 1–3 plus the null-branchLog fail-open; T2 closes the remaining edges (empty-string and omitted-key variants, plan:fable byte-preservation extensions, whitespace/absent brief) and writes the AS_BUILT measurement record.

- [x] T1 — Produce and transport the brief end-to-end in `trident/inner-workflow.mjs`: the `plan:probe` seat relays a bounded branch log (new optional `branchLog` field, `git log --name-only <base>..<probeRef> | head -c 12288`); `planNextPrompt` gains the BRANCH LOG section and asks for `branchBrief` (new optional `PLAN_SCHEMA` property, four labelled sections BUILT/SEAMS/REJECTED/SUITES, evidence-only, regenerated this round); new `clampBranchBrief()` + `BRANCH_BRIEF_MAX_BYTES=4096` enforce the cap in code at the one consumption point; `ralphExecuteNote` appends the clamped brief to the Forge prompt (byte-identical output when absent/empty); `usePlanNext` never gates on `branchLog`. Named tests for falsification criteria 1–3 in `trident/inner-workflow-plan-next.test.ts`: the producer-deletion canary, the cap at the byte boundary (unit, extracted function, plus an oversized-brief E2E), and round-5-carries-round-5-not-round-2 (brief absent from the persisted plan body and from every checkpoint prompt — no accumulation channel exists).
- [x] T2 — Fail-open edges, docs, and the measurement record: tests that a probe with no/empty `branchLog` still takes the cheap `plan:next` path and Forge gets no brief section; that iteration 1 / `plan:fable` and every genuine crash-resume are byte-untouched (extend the existing byte-preservation canaries); that an absent/null/whitespace `branchBrief` emits no header; append the `docs/AS_BUILT.md` entry quoting the measured BEFORE (30m01) and the exact AFTER protocol (which run-row timestamps/log lines to read on the next multi-task card, and the commitment to remove the brief if the build time does not drop); run `bash tools/lane_review.sh <branch>` and the mandated test gates, recording any proven base-identical environment failures honestly.
- [x] T3 — Argus round-1 hardening: authorize `branchBrief` only when `usePlanNext` selected the continuation producer (with a non-vacuous full-planner sentinel test); clamp relayed `branchLog` again in code at 12,288 UTF-8 bytes with exact/over/multibyte coverage; read `origin/<base>` in both merge modes; fence commit material as untrusted data; derive the brief marker from its constant; add durable `codex-exec-end` instrumentation and stage attribution; record the honest pending AFTER plus a Trident-owner removal deadline of 2026-08-25 instead of claiming an unmeasurable result.
- [x] T4 — Argus round-2 hardening: neutralize closing fence tokens in commit bodies; gate Forge brief transport on non-empty branch material with adversarial missing-log tests; split Forge/base fetches and execute the missing-remote-branch regression against a real Git remote; preserve valid UTF-8 at the shell truncation boundary; pair retry timing from the latest start before the first end; make the 2026-08-25 removal gate executable; and document that the historical 30m01 proxy is not instrument-matched to the new exact pair.
- Commit .trident/plans/trident/b-every-ralph-task-restarts-forge-c.md together with your code + tests.

## Do not (copied verbatim from the card's plan doc)

_Source: `docs/plans/b-every-ralph-task-restarts-forge-cold-the-continuation-plan-fv6745.md`. Do not paraphrase this section._

- Do not carry the raw transcript across tasks. That is the thing the discipline forbids
  and it is right to forbid it.
- Do not let the brief grow monotonically.
- SURFACE: `trident/inner-workflow.mjs` (continuation planner seat). COLLIDES with card
  `01M09HVMZ9` — build these two in sequence on the same branch, or land A first.
</spec>
<parameter name="status">upcoming
