# IMPLEMENTATION_PLAN — Stop per-task publish + rebase-replay; stop the replay destroying the commit subject

Regenerated 2026-08-18 against main (89de7523). Card: `trident/orchestrator.ts` outer publish/replay path ONLY. Two measured defects, one code path: (1) the rebase-replay commit at `rebaseOntoObservedBase` (~line 1347) writes `rebase <branch> onto <base> @ <sha> (replayed from <sha>)` as the commit SUBJECT — four squash-merges on main carry it today (#327 bce629e2, #328 d2680a09, #348 47144a2a, #354 e6d4610d); (2) `applyResult` (~2404) runs the FULL `publishBuiltCommit` rebase+push+PR cycle on EVERY Ralph task boundary because every pr-mode build handoff arrives `publish_requested: true`, so an 11-task card rebases one branch eleven times. HARD CONSTRAINTS: the inner loop NEVER pushes (outer loop owns all GitHub ops); no window where a built commit can be dropped; do not change WHAT merges, only WHEN it publishes and what the message says. OFF LIMITS: `trident/inner-workflow.mjs`, `trident/test-strategy.ts` (parallel lanes are editing them RIGHT NOW), plan format (card C).

- [x] **A. The replay carries the original commit's subject and body; the rebase note becomes body-only metadata.** In `rebaseOntoObservedBase`, read `%B` of `oldHead` (fail CLOSED via `publishFailureReason` on a failed read — never fabricate the old destroyed-subject message) and commit `<original message>\n\n<replay note>`; note string byte-identical to today's. Real-git tests in `publish-rebase-realgit.test.ts`: subject+body carried, note never the subject, replay-of-a-replay still keeps the subject; test written FIRST so the pre-change failure (subject = `rebase … onto …`) is captured and BOTH subjects are quoted in the PR body.
- [ ] **B. Publish once per wave, not once per task.** In `applyResult`, a `publish_requested` result with `run.ralph && (result.remaining_tasks ?? 0) > 0` DEFERS: skip `publishBuiltCommit` entirely and route through `refireNextRalphTask` with a new optional atomic checkpoint override `ralph-task-built` / `ralph-task-built-deviated` (from `result.deviated_from_spec`) in the same `persistRefireReset` patch — `inner_checkpoint_head` untouched (the row already holds the forge-done head, so the (name, head) pair matches what the workflow's own conversion fire writes today). Terminal handoffs (`remaining_tasks` 0/null, all non-ralph runs, fix rounds) publish exactly as today. Rewrite the deviated-handoff test (`orchestrator.test.ts` ~220–265) to the new contract (deviation now crosses the process boundary as the checkpoint NAME, not the `outer-published:` suffix). Tests: an intermediate task produces NO push/`gh pr create` host calls and the refire patch renames the checkpoint; the terminal task (remaining 0) pushes with the lease and opens the PR (existing test ~165 stays green as the positive control).
- [ ] **B2. Keep the cheap continuation planner alive mid-wave.** In `launch()`, when the resume checkpoint is exactly `ralph-task-built` or `ralph-task-built-deviated`, resolve `resume_live_head` against the LOCAL ref (call `resolveResumeLiveHead` with `merge_mode: 'local'`), because a deferred wave's remote is EXPECTEDLY stale and for these names the head never decides rebuild-vs-not (`resumeHeadDecides` is false) — it only selects WHICH planner: the workflow's `cleanContinuation` gate needs recorded==live to open plan:next (inner-workflow.mjs ~5546). Without this, every mid-wave re-fire silently pays the full ~5-min plan:fable survey — re-introducing the waste this card removes. Test: a deferred re-fire passes the local branch tip as `resume_live_head`; reconcile the resume-live-head tests (`orchestrator.test.ts` ~3183–3369) that stub `ls-remote` for these names.
- [ ] **C. A failed/terminal run is still ASKED whether it built something (constraint 2).** New helper: pr-mode + non-null branch, local `rev-parse` resolves 40-hex, and local tip differs from `ls-remote` origin tip (or remote absent) → call `publishBuiltCommit(run, null)`; wire it into the three harvested terminal-failure paths that can now hold deferred work: the REQUEST_CHANGES/null catch-all (~2612), the `max_ralph_rounds` failure in `refireNextRalphTask` (~2318), and the infra-retry-budget failure (~2490). Success sets `pr` on the failed row and reports in the `note`; any salvage failure (incl. `TridentRebaseConflict`) is swallowed into the `note` — NEVER appended to `failure_reason` (delivery.ts classifies reasons by `.includes()`; appending words like "rebase"/"conflict" would reclassify the owner-facing message) and never turns a failure into a success. Test: a mid-wave failure with unpublished local commits pushes the branch, records the PR, and still fails with the byte-identical original reason. Deliberately unchanged (state in PR body): crash/stall reaps in `step()` (recovery relaunches from the local branch; work stays in the shared ref store) and the provenance-gate failure (unreachable mid-wave by construction).
- [ ] **D. Reconcile and prove.** Full `trident/` suite green (output to a log file, read the tail only); `tools/lane_review.sh <branch>` — every new exported symbol has a non-test production caller, paths named in the PR body; AS_BUILT.md entry per repo convention; PR body quotes the pre/post replay subjects (acceptance), names the intermediate-no-publish/terminal-publish/failure-publish tests, and lists the deliberately-unchanged paths with reasons.
