# IMPLEMENTATION_PLAN.md — one factory for TridentRun test fixtures

Governing card: "Fixture factory: #379 landed makeTridentRun but migrated only 1 of 15 files — 14 still carry hand-written literals and no guard stops new ones, so the tax is untouched", base `main`.

Survey refresh (2026-08-17, against `main` post-#379):
- `trident/testing/make-trident-run.ts` + `trident/testing/make-trident-run.test.ts` exist; defaults are complete for the CURRENT `TridentRun` (including `infra_retries` from the arbiter work and `base_sha`/`base_behind`). `trident/state-machine.test.ts` is the one migrated consumer — the exemplar to imitate.
- The guard the factory's own docblock cites (`trident/__tests__/no-handrolled-trident-run-fixtures.test.ts`) DOES NOT EXIST — the docblock is currently a promise, not a fact.
- The sentinel trio (all three of `\bcrash_recoveries\s*:`, `\bstarted_at\s*:`, `\blast_advanced_at\s*:` as object keys in one file) now trips **16** test files, not 14: the original 14 PLUS `trident/arbiter.test.ts` and `gateway/proactive/__tests__/terminal-build-wake.test.ts`, both landed AFTER #379's survey with fresh full literals — the exact failure mode the old plan predicted ("the T3 guard will catch it at merge time"), except the guard was never built, so nothing caught them. Two regrowths in days is the measured cost of shipping the factory without the guard.
- Sentinel validated against the whole tree today: exactly those 16 files trip it; `trident/store.test.ts` (deliberate 36-column literal + `COLS` — stays untouched per the card), `trident/state-machine.test.ts`, `trident/testing/make-trident-run.test.ts`, and the cast-based partial fixtures (`work-board/complete-refuses-live-run.test.ts`, `trident/publish-rebase-realgit.test.ts`, `trident/terminal-failure-reason.test.ts`, `open/wiring/__tests__/trident-launcher-liveness.test.ts`) do NOT trip and are out of scope.
- Post-migration the empty-allowlist guard holds: the only in-test trio-field override call sites (`run-driving` L155, `run-progress` L143, `work-wakeup-selection` L112/161/225) override `last_advanced_at` alone, never all three — zero files trip after T2.
- Cross-package import path verified: deep subpath imports (`@neutronai/trident/<file>.ts`) are the established pattern from `gateway/` and `open/`, and both packages already declare `"@neutronai/trident": "workspace:*"`.

- [x] **T1 — shared factory + first consumer.** Landed in #379: `trident/testing/make-trident-run.ts` (`makeTridentRun(over: Partial<TridentRun> = {}): TridentRun`), its unit test, and `trident/state-machine.test.ts` migrated.
- [x] **T2 — migrate ALL 16 remaining full-literal fixture files to `makeTridentRun`** (pure refactor; per-file pass counts identical before/after). 12 in `trident/` via relative import `./testing/make-trident-run.ts`: `arbiter.test.ts` (`run`), `conflict-resolver.test.ts` (`run`), `delivery.test.ts` (`runWith`), `git-mode.test.ts` (`makeRun`), `inner-loop.test.ts` (`makeRun`), `merge-realgit.test.ts` (`localRun(repo, id, branch)` — keep the positional signature as a thin wrapper), `merge.test.ts` (`makeRun`; its derived `localRun` ~L389 already delegates and needs no change), `ported-fixes.test.ts` (`makeRun`), `run-driving.test.ts` (`run`), `run-progress.test.ts` (`run`), `terminal-observer.test.ts` (`run()` — keep the no-arg signature), `terminate.test.ts` (`fakeRun`; also the default param in `fakeStore`). 4 cross-package via `@neutronai/trident/testing/make-trident-run.ts`: `gateway/http/work-board-surface.test.ts` (`fakeRun`), `gateway/proactive/__tests__/terminal-build-wake.test.ts` (`run`), `gateway/proactive/__tests__/work-wakeup-selection.test.ts` (`run`), `open/__tests__/open-skill-forge-wiring.test.ts` (`doneRun`). Rule: keep each file's fixture NAME and SIGNATURE (zero call-site churn); the body becomes one `makeTridentRun({ …overrides, …over })` call where overrides = exactly the fields whose hand-written value differs from the factory default — never normalize a differing value to the default, never invent a value. No assertion changes.
- [ ] **T3 — land the guard so literals cannot return.** New `trident/__tests__/no-handrolled-trident-run-fixtures.test.ts` (the exact path the factory docblock already cites): enumerate tracked test files (`git ls-files '*.test.ts'` from the repo root — two dirs up from the test's dir), fail any file (other than an in-test `ALLOWLIST: string[]` that starts EMPTY) whose content matches all three of `/\bcrash_recoveries\s*:/`, `/\bstarted_at\s*:/`, `/\blast_advanced_at\s*:/` (`\b` correctly rejects `max_crash_recoveries:` since `_` is a word char). Failure message names the offending files, points at `trident/testing/make-trident-run.ts`, and explains the allowlist escape hatch for a legitimate future file that overrides all three via the factory. Include a self-test that the sentinel regexes match a synthetic literal (positive control) so the guard cannot rot into always-green.
- [x] **T4 — schema-surface treatment.** Already satisfied (verified in #379): `migrations/expected-schema.txt` is generated by `migrations/regen-snapshot.ts` and byte-compared by `migrations/snapshot.test.ts`. The deliberate literal in `trident/store.test.ts` and `COLS` in `trident/store.ts` stay as they are. No action.

Out of scope / notes: do not migrate `trident/orchestrator.test.ts` / `trident/store.test.ts` / `trident/crash-recovery.test.ts` (they build runs through `store.create()`, not literals). `trident/terminal-failure-reason.test.ts`'s cast fixture carries `ralph: 1` — pre-existing, hidden by its cast, deliberately untouched. Do not weaken any `TridentRun` required field.
