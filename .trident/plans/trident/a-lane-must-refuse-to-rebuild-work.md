# IMPLEMENTATION_PLAN — A lane must refuse to rebuild work that has already landed

Regenerated 2026-08-18 against main @ 89de7523. Card: the three-occurrence 2026-08-17 waste (a merged PR read as this run's APPROVE; a branch carrying another card's merged commits; a card built on another card's branch). Three refusal guards (T1 dispatch-time, T2 branch-cut, T3 verdict-time binding), every refusal actionable (T5), every guard's test red when the guard is deleted (T4). Constraints: guards REFUSE (never advisory); GitHub reads stay OUTER-loop (the forge sandbox has no credential); never auto-complete a card off a merged PR; do not weaken #392's own-work no-op-publish refusal.

Measured against the code (why each task exists — do not re-derive):
- NO dispatch-time merged-PR check: `dispatchBoardBoundBuild` (trident/board-dispatch.ts) creates the run after only the item/readiness/workspace gates. A `failed` card whose PR merged is indistinguishable from one that never built. Retries reuse the same slug → same `trident/<slug>` branch (slug-retry-after-terminal), so the card's branch is resolvable at dispatch.
- The fresh-launch base pin is DEAD in production: `launch()` (trident/orchestrator.ts ~2072) gates pinning on `launchRun.branch === null`, but board dispatch names `branch: trident/<slug>` at create — so `base_sha` is never pinned for real runs, `pinnedBase` is null in inner-workflow.mjs, and Forge gets the UNPINNED `git switch -c <branch>` instruction: the branch is cut from the worktree's default HEAD. That is the mechanical opening for occurrences 2 and 3. (The existing "FRESH pr launch fetches and pins" test passes `branch: null` — a shape production never creates.)
- `bound_pr` (store.ts:193, migration 0124) is read by NOTHING in trident/orchestrator.ts, despite store.ts's own comment claiming "enforced by publishBuiltCommit".
- `detectMergedPr` (orchestrator ~1730) attributes a merged PR to the run BY BRANCH NAME when `run.pr` is null, and the crash-recovery path (~2708) adopts it as `phase='done'`, `inner_verdict='APPROVE'`, `inner_checkpoint='pr-merged'` — occurrence 1's exact mechanism (run ec90f361 / stale merged PR #336).
- `detectExistingPr` (~1710) relies on gh's implicit default state filter instead of an explicit `--state open`; its result is silently adopted onto the run at launch (~2051) with no bound_pr cross-check.

- [x] (pre-existing — KEEP, card constraint) `publishBuiltCommit`'s `expected === resolvedHead` refusal ("branch is already at <head> on origin — no new commits") is the #392 own-work no-op path. Do not weaken or reuse it for these guards.
- [x] T1 — dispatch refuses a card whose work already landed: in `dispatchBoardBoundBuild`, after merge-mode resolution and BEFORE `store.create` (zero state on reject), probe `gh pr list --head trident/<slug> --state merged` with the outer credentialed runner; on a MERGED PR, corroborate its `headRefOid` against fetched `origin/<base>` (`merge-base --is-ancestor`) and REFUSE with new rejection code `already_landed`, message containing the literal "already merged as #<n>" + "verify the card instead of rebuilding". New injectable `landedProbe` seam + `makeDispatchLandedProbe(EnvCapableHostRunner)`; wire ONE shared instance (built over `tridentHostRunner`) at all three production dispatch sites in open/composer.ts (code-command ctx ~2145, `boardStartBuild` ▶ route ~4233, `trident_build_dispatch` tool deps ~6694) and thread through trident/code-command.ts + trident/work-board-build-tool.ts. No-evidence (gh non-ok/empty) proceeds as today; local merge-mode never probes. Tests in board-dispatch.test.ts drive the REAL function via a fake runner: refusal + zero rows + attachRun never called; happy path; probe-failure degrade; local-mode no-gh-call; squash case (is-ancestor exit 1 still refuses).
- [x] T2a — make the base pin real for production runs: redefine `launch()`'s fresh predicate from `branch === null` to "no inner_checkpoint AND run.base_sha === null" so board-dispatched round-1 runs fetch origin/<base>, pin `base_sha`/`base_behind`, and hand `pinnedBase` to the workflow's `forgeStep1` (branch provably cut from the fetched origin/main tip). Preserve resume semantics (checkpoint non-null unchanged); never overwrite an existing row pin (crash relaunch keeps the original). Rewrite the existing fresh-pin orchestrator test to a board-shaped run (branch pre-named at create) so the test proves the production shape pins.
- [x] T2b — pre-fire leftover-branch guard (stops occurrences 2 and 3 outright): in `launch()`'s fresh path after the pin, resolve local `refs/heads/<branch>`; if it exists and is NOT contained in the pinned base (`merge-base --is-ancestor <branch-tip> <base_sha>` false), allow only when the row's own PRIOR `base_sha` proves descent (this run's own crash leftover); otherwise FAIL the run WITHOUT firing: "branch <b> already carries N commit(s) not on origin/<base> — it was not cut from origin/<base>; refusing to build on another lane's work. Verify or delete the branch (git -C <repo> branch -D <b>), then re-dispatch." Tests: refusal fires no workflow (h.inputs empty) and names the action; an ancestor-only (stale/equal) leftover proceeds; deleting the guard turns the refusal test red.
- [x] T2c — publish-time cut assertion (defense-in-depth behind T2b): in `publishBuiltCommit`, on the FIRST publish of a branch (observed remote ref absent) with `run.base_sha !== null`, require `merge-base --is-ancestor <base_sha> <resolvedHead>`; on failure throw "branch <b> does not contain the origin/<base> tip pinned at launch (<sha7>) — not cut from origin/<base>; refusing to publish work built on another lane's branch. Verify the card instead of rebuilding." Placed before the rebase replay so a wrong-base branch never reaches conflict resolution. Test: wrong-base first publish refused; correct-base first publish unaffected; #392 no-op path byte-identical.
- [ ] T3a — merged-PR adoption requires binding (kills occurrence 1): extend `detectMergedPr` to return binding evidence — bound iff the PR number equals `run.pr` or `run.bound_pr`, or its `headRefOid` equals this run's recorded head (`outer-published:<oid>` checkpoint or `inner_checkpoint_head`). Crash-recovery (~2708): bound+merged adopts exactly as today; UNBOUND merged → FAIL the run (never adopt, never relaunch): "PR #<n> on branch <b> is already MERGED but is not bound to this run (run.pr=<...>, bound_pr=<...>, no recorded head match) — refusing to adopt it as this run's result or to rebuild over it; verify the card instead." Test reproduces ec90f361: run.pr null, stale merged PR by branch name → failed with that reason, not done/APPROVE; deleting the binding check turns it red.
- [ ] T3b — verdict/publish PR binding: (i) `publishBuiltCommit`: after PR resolution, `run.bound_pr !== null && pr !== run.bound_pr` → throw naming both numbers (makes store.ts's claimed enforcement true); (ii) launch adoption (~2051) and `detectExistingPr`: explicit `--state open`, and a bound_pr/resolved-PR disagreement fails loudly instead of silently adopting; (iii) `applyResult`'s `result.pr_merged` acceptance: corroborate outer-side via `gh pr view <n> --json state,headRefOid` — accept done/APPROVE only when state=MERGED AND the PR is bound to this run (run.pr/bound_pr/recorded head), else fail loudly with the verification message. One test per check; each red when its check is deleted.
- [ ] Closeout — docs/INVARIANTS.md entry for the three refusals (dispatch, branch-cut, verdict binding) + docs/AS_BUILT.md sections per landing; spot-check T4 by commenting out each guard locally and confirming its test goes red before finishing.
