# IMPLEMENTATION_PLAN.md — ordinal-125 deploy blocker (gates deploy `xGkufirIQQKW1L`, `e474cf1f` → `de09d6a6`)

Spec: the Plan card "the pending deploy will not boot: ordinal 125 mismatches with no repairs entry, and base_sha/base_behind/agent_waked_at are missing from the live DB". Base branch: `main` (verified at `de09d6a6`).

Planner verification record (2026-08-17, all read-only):
- Live DB (`<NEUTRON_HOME>/project.db`) `_migrations`: 122=`work_board_items_pr`, 123=`code_trident_runs_crash_recoveries`, 124=`dispatch_dependencies_and_claims`, 125=`code_trident_runs_fix_round_contract`, 126=`code_trident_runs_infra_retries`; no rows 127+.
- Live `code_trident_runs` LACKS `base_sha`, `base_behind`, `agent_waked_at`; HAS `infra_retries` plus stray branch column `claimed_paths` (referenced nowhere on main — safe to shed).
- Live `work_board_items` has all 12 canonical columns plus strays `pr`, `pr_url`, `blockers`; statuses are within 0130's widened CHECK — 0127 and 0130 apply cleanly once boot passes 125.
- Deployed `e474cf1f` migrations stop at 0124; `0125_code_trident_runs_base_sha.sql` arrives in `a8f98914` — the deploy is what arms the refusal. `repairs.json` on main covers only 122 and 124.
- `trident/store.ts` COLS on `de09d6a6` selects `base_sha, base_behind` — confirmed second failure mode.
- No FK in any schema references `code_trident_runs`; `migrations/0130` sets the STRICT-table rebuild precedent; snapshot refresh is `bun run migrations/regen-snapshot.ts`.

- [x] Diagnosis verified against the live DB and both shas (see record above) — the card's table is byte-accurate.
- [x] Repairs machinery + entries for 122 and 124 already shipped (`migrations/repairs.json`, runner acknowledgment via `_migration_repairs`, pin test for 122 in `migrations/__tests__/ordinal-collision-guard.test.ts`).
- [x] Prevention lanes exist elsewhere and are NOT this card: untracked-migration/provenance guard already on main; PR-renumbering card `01M07AKY0X3JNJXZX31A8DNKN2`; the #575 root-cause card.
- [x] **TASK 1 — the deploy-gating repair.** Add the version-125 `repairs.json` acknowledgment (recorded_name=`code_trident_runs_fix_round_contract`, file_name=`code_trident_runs_base_sha` — file_name is the SLUG per `repairsEntryFor` in runner.ts) AND new migration `0131_code_trident_runs_base_sha_repair.sql`: a full `code_trident_runs` STRICT rebuild (0130 pattern) that creates `base_sha`/`base_behind` on BOTH the repaired-live path (0125 skipped) and the fresh-install path (0125 applied) — plain ALTERs are impossible (duplicate-column on every fresh install). Ship with the live-replica falsification tests (criteria 1–4: full-tree apply completes, applies exactly [127,130,131], columns exist after, row 125 byte-identical, negative control without the entry throws and writes nothing), append `131` to runner.test.ts's expected applied array, regen `expected-schema.txt`, add the pin test for the 125 entry, and append the AS_BUILT.md entry.
- [ ] **TASK 2 — the CI guard that would have caught this (fix item 3 / criterion 5).** Commit `migrations/live-ledger.json` — a snapshot of the live `_migrations` (version, name) rows captured 2026-08-17 (planner-verified; recapture read-only ONLY via `sqlite3 "file:<NEUTRON_HOME>/project.db?mode=ro" -json "SELECT version, name FROM _migrations ORDER BY version"`; live ledger runs 1..126 with gaps 59, 64–68; every row matches its file's slug except 122/124/125 as recorded above). Add `migrations/__tests__/live-ledger-compat.test.ts`: for every file `loadMigrations()` returns, assert (ordinal absent from the fixture) OR (fixture name === file slug) OR (a `repairs.json` entry matches the exact triple version\0recorded\0slug). Runs under `bun test` so the sharded CI `test` job enforces it with zero workflow changes. Verify criterion 5: with the version-125 repairs entry temporarily removed the test FAILS on the 125 row; with it, PASSES.
- [ ] **TASK 3 — verify and release.** Run the full gate (`bash scripts/run-tests.sh` → log file, read tail only), confirm the branch is green, and state in the PR body that this repairs the third #575-class incident and gates deploy `xGkufirIQQKW1L` — the deploy must be re-cut from a main sha that includes this fix; the owner approves the deploy, not the build.
