# IMPLEMENTATION_PLAN — the terminal-build wake must ACT, and its once-only claim must survive failure

Regenerated 2026-08-17 against main (89de7523). Card: the #335 wake NOTIFIES but cannot ACT — the compose turn is budgeted like a one-line nudge (90 s seam default, no explicit `timeout_ms`), the prompt orders actions without naming a single executable lever, nothing pins the wake to the executable (tool-bridge) substrate, and any post-claim failure (LLM error, post failure, gateway restart) permanently consumes the run's one wake. Targets: T1 real acting turn · T2 recoverable claim · T3 a test that fails if the tools are stubs. Constraints: keep `agent_waked_at` single-writer discipline (out of COLS/TridentRun/update/save/saveIfActive); the wake never pushes to GitHub (retry = ask the outer loop via the board tools); a posted reply alone is NOT done; the enterprise 10-min sweep stays as backstop.

- [x] #335 mechanism: `gateway/proactive/terminal-build-wake.ts` observer (claim-first, quiet-on-done, verbatim failure reason), migration 0127 `agent_waked_at`, single-writer `TridentRunStore.claimAgentWake` — merged #335, module-tested.
- [x] Registration at every composition site: both `buildTridentTerminator` binds + the tick loop's `on_terminal_wake`, one constructed observer — merged #385, pinned by `open/__tests__/open-terminal-build-wake-wiring.test.ts`.
- [x] The executable surface EXISTS in Open's wiring: the wake composes on the background `cc-nudge-*` substrate (`open/wiring/substrates.ts` — `PROFILE_WARM_CHAT`, `enableToolBridge: true`, cwd = owner home, same grants as a channel turn) via `buildSubstrateReminderLlm`; names-only ToolDefs are the REPL `--tools` contract (`build-live-agent-turn.ts:957`). What is missing is the act budget, the lever-naming prompt, and any test pinning this — an unpinned rewiring to a toolless composer would pass today's tests.
- [x] T1+T3 — make the wake turn an ACTING agent turn and pin the executable surface: add `TERMINAL_BUILD_WAKE_TURN_TIMEOUT_MS` (4 min act budget — the 90 s nudge default aborts an investigate-and-act turn mid-work and burns the claim) threaded as `compose(spec, { timeout_ms })`; rewrite the prompt to name the real levers (`work_board_list`/`work_board_update` on the bound item; retry = `work_board_start` / `work_board_dispatch_build` so the OUTER loop re-dispatches on the existing branch/PR) and the prohibitions (never `git push`/`gh` mutations — the outer loop owns GitHub; never a bare "reply to retry" when a lever exists); tests that fail on a stub surface: module (act timeout threaded, exact `LIVE_AGENT_TOOL_NAMES` parity, lever + prohibition phrases) and composition (`substrateFactory` opts capture proves the wake spec dispatches on `substrate_instance_id` `cc-nudge-*` with `enableToolBridge: true` and the full tool surface).
- [ ] T2a — recoverable-claim substrate in `trident/store.ts` + migration 0132 `agent_wake_done_at`: narrow single-writer-family methods `completeAgentWake(id)` (only after a successful post), `releaseAgentWake(id)` (only while undelivered), `reclaimStaleAgentWake(id, stale_before)` (CAS re-claim of a claimed-but-never-completed wake), `listWakeRedriveCandidates({window_ms, stale_ms})` (terminal + chat_id, bounded by `last_advanced_at` window so pre-0127 NULL rows are never mass-woken); BOTH columns stay out of COLS/TridentRun/update/save/saveIfActive; store tests incl. mutants proving a DELIVERED wake can never be released or reclaimed and a released claim can be won exactly once more.
- [ ] T2b — consume it: observer marks `completeAgentWake` after the post resolves true and calls `releaseAgentWake` in the catch (keep the `terminal_build_wake_failed_after_claim` log event, add `released: true`); extract the shared compose+post core and add `buildTerminalBuildWakeRedrive` (claim-or-reclaim → core) run ONCE at boot in `open/composer.ts` (fire-and-forget, logged) so a wake lost to an in-process error or a gateway restart is re-attempted on the next boot; module + wiring tests (compose-throw → claim released; stale claim + unclaimed recent terminal redriven, delivered + ancient runs untouched); document that the enterprise 10-min sweep remains the timely backstop.
- Commit .trident/plans/trident/the-335-terminal-build-wake-notifie.md together with your code + tests.
- Report `deviatedFromSpec: true` in your structured result ONLY if you materially deviated from the EXECUTION SPEC above (different target files, a different design, or the task as built is not the task as specced) — a true here forces the next iteration to re-derive the whole plan, so do not set it for cosmetic drift. Otherwise report false or omit it.
