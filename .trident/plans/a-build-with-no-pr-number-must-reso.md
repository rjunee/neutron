# IMPLEMENTATION_PLAN — A build with no PR number must resolve one from its branch, not die — and must never leave a finished commit unpushed (run f384460d)

Governing spec: this Plan card. Regenerated 2026-08-17 against branch `trident/a-build-with-no-pr-number-must-reso`. State measured, not inferred: LOCAL head `a814c28d` (carries the finished, tested T4 commit) has DIVERGED from ORIGIN head `225fdf74` (carries a plan-relocation commit that renamed root `IMPLEMENTATION_PLAN.md` → `.trident/plans/a-build-with-no-pr-number-must-reso.md`, per the repo's per-branch plan convention). Both descend from `fe8f39ba`. PR #282 is OPEN (base `main`, head this branch). Main (`89de7523`) has no `isInfraDeath`/`infraDeathSentence` — the sibling card "an infra-only block must be delivered as infrastructure" has NOT landed elsewhere, so this branch's T4 is the single implementation, as the card requires.

- [x] **T2 — publish BEFORE reporting terminal.** A pr-mode forge-done returns a publish handoff (`publishRequested`+`publishHead`, `trident/inner-workflow.mjs`); the orchestrator's `publishBuiltCommit` (`trident/orchestrator.ts`) pushes with an OBSERVED lease (`--force-with-lease=refs/heads/<b>:<observed>`), confirms the head on origin, resolves the PR from the branch, materializes the review diff, and re-fires review via the `outer-published:<head>:<remaining>:<round>` checkpoint. Tested in `trident/orchestrator.test.ts`. No verdict is written before the commit is published on this path.
- [x] **T1a — a branch with a discoverable PR is resolved from GitHub; a branch with none gets one OPENED, never a failed build** (`detectExistingPr` → `gh pr list --head`, then `gh pr create`, inside `publishBuiltCommit`; publish failures carry git's own redacted stderr).
- [x] **T1b — `prNumber: 0` is a sentinel, never a PR number** (commit `639399d6`, on origin). Positive-integer guards at all four readers: forge adoption in `inner-workflow.mjs`, `checkpoint()`, `writeTerminalResult()`, and `parseInnerResult` (non-positive/fractional → null so every `result.pr_number ?? run.pr` site retains the known PR). Includes the card's "Test the real trace" requirement: the run-f384460d journal replayed RED-first (lease push, PR resolved from branch, review re-fired, pr never reads 0) in `trident/orchestrator.test.ts` and `trident/terminal-failure-reason.test.ts`.
- [x] **T3 — an unpushed finished commit is an ALARM in words, not an exit code** (commit `fe8f39ba`, on origin). Every terminal-failed pr-mode transition probes the run's branch (`detectUnpublishedCommit`: local `rev-parse` vs `ls-remote --heads origin`) and appends the exact sentence "a finished commit was not published: <sha>" to `failure_reason`; `composeTerminalDelivery` surfaces it verbatim; single sentence source (`unpublishedCommitAlarmSentence`/`unpublishedCommitAlarm`) in `trident/delivery.ts`.
- [x] **T4 — never report infra as a verdict** (commit `a814c28d` — finished and tested, currently LOCAL-ONLY). `parseInnerResult` decodes `findings_present` FAIL-CLOSED (`trident/inner-loop.ts:567`); `isInfraDeath` reads only measured signals — the wrapper's own catch-path checkpoint `inner-error` with no findings, or `block_kind:'infra-only'` (`trident/orchestrator.ts:622-627`); the applyResult catch-all writes `inner_verdict: null` for an infra death (`orchestrator.ts:1356`) so `run-progress.ts` never shows a phantom REQUEST_CHANGES; `infraDeathSentence` is the one sentence source (`trident/delivery.ts:357`) and `interpretFailure` routes both authored infra reasons to the `infra` class EARLY so an embedded cause's stray 'stalled'/'git ' tokens cannot misroute (`delivery.ts:189`). Positive control kept: genuine review exhaustion still reports REQUEST_CHANGES with review copy. This IS the landing of the sibling item "an infra-only block must be delivered as infrastructure" — implemented once, here.
- [x] **PUBLISH the finished T4 commit — the card's own lesson, applied to this branch.** Rebase `a814c28d` onto the observed origin head `225fdf74` (the only overlap is the root-plan rename: the final tree must have NO root `IMPLEMENTATION_PLAN.md`, and the regenerated plan lives at `.trident/plans/a-build-with-no-pr-number-must-reso.md`), run the trident test suite green in the rebased worktree, push with an observed lease (`--force-with-lease=refs/heads/trident/a-build-with-no-pr-number-must-reso:225fdf74…`), confirm origin and PR #282 carry the new head, then fast-forward the stale local branch ref. Never discard `a814c28d` until origin is confirmed to hold its rebased equivalent; if the push cannot complete, the report must say the stranded sha out loud ("a finished commit was not published: <sha>").
- Commit .trident/plans/trident/a-build-with-no-pr-number-must-reso.md together with your code + tests.
- Report `deviatedFromSpec: true` in your structured result ONLY if you materially deviated from the EXECUTION SPEC above (different target files, a different design, or the task as built is not the task as specced) — a true here forces the next iteration to re-derive the whole plan, so do not set it for cosmetic drift. Otherwise report false or omit it.
