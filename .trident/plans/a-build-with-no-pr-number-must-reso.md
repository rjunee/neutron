# IMPLEMENTATION_PLAN — A build with no PR number must resolve one from its branch, not die — and must never leave a finished commit unpushed (run f384460d)

Governing spec: this Plan card. Verified against branch `trident/a-build-with-no-pr-number-must-reso` (head `dc9d92b`, 2026-08-15):

- **T2 is LANDED.** The outer-publisher design is in: a pr-mode forge-done returns a publish handoff (`publishRequested`+`publishHead`, `trident/inner-workflow.mjs`), and the orchestrator's `publishBuiltCommit` (`trident/orchestrator.ts:493-581`) pushes with an OBSERVED lease (`--force-with-lease=refs/heads/<b>:<observed>`), confirms the head on origin, resolves the PR from the branch, materializes the review diff, and re-fires review via the `outer-published:<head>:<remaining>:<round>` checkpoint. Tested at `trident/orchestrator.test.ts:141-373`. No verdict is written before the commit is published on this path.
- **T1a is LANDED.** `publishBuiltCommit` asks GitHub which PR has this head (`detectExistingPr` → `gh pr list --head`, `orchestrator.ts:475-491,555-564`) and, when a branch genuinely has no PR, OPENS one (`gh pr create`) rather than failing the build. Publish failures carry git's own redacted stderr (`publishFailureReason`/`redactPushError`).
- **T1b is LANDED** (commit `dc9d92b`, "a prNumber of 0 is a sentinel, never a PR number"). All four readers of the pr-mode `PR_NUMBER=0` sentinel now adopt/persist/decode only a POSITIVE INTEGER: forge adoption in `inner-workflow.mjs`, `checkpoint()`, `writeTerminalResult()`, and `parseInnerResult` (maps non-positive/fractional to null so every `result.pr_number ?? run.pr` site retains the known PR). RED-first replay of the run-f384460d trace plus the inner-error variant (pr stays 267 on the failed row) is in `orchestrator.test.ts`; mutation-checked per guard. The wrapper's sentinel contract is deliberately untouched.
- **T3 is LANDED.** `worktree-cleanup.sh` detected the stranded commit precisely (`PRESERVED branch <b> reason=unpushed`, exit 3) and the signal died in the inner workflow's `log()`. Now every terminal-failed pr-mode transition flows through one wrapper that probes the run's branch (`detectUnpublishedCommit`: local `rev-parse refs/heads/<b>` vs `ls-remote --heads origin`) and appends the exact sentence "a finished commit was not published: <sha>" to `failure_reason`; `composeTerminalDelivery` surfaces that sentence to the owner verbatim on its own line. One sentence source (`unpublishedCommitAlarmSentence`/`unpublishedCommitAlarm` in `delivery.ts`) so the writer and the reader cannot drift.
- **T4 is NOT implemented, though its substrate half exists.** `parseInnerResult` already decodes `ok`/`blockKind`/`terminalCause` (`inner-loop.ts:514,542-553`) and `innerTerminalFailureReason` has the one permitted specific message for `infra-only`+cause (`orchestrator.ts:425-427`). But an `inner-error`/`ok:false` harvest with no findings still writes `inner_verdict='REQUEST_CHANGES'` (`orchestrator.ts:1019`) and delivers via the generic "ended without an approved review" class (`delivery.ts:195-201`) — a verdict-shaped message for an infrastructure death. Shares its shape with "an infra-only block must be delivered as infrastructure"; implement once here.

- [x] **T2 — publish BEFORE reporting terminal** (outer publisher: lease push pinned to the observed remote sha, origin confirmation, PR resolve-or-create, diff materialization, review re-fire; `orchestrator.ts` + `inner-workflow.mjs`, tests `orchestrator.test.ts:141-373`).
- [x] **T1a — a branch with a discoverable PR is resolved from GitHub, and a branch with none gets one OPENED, never a failed build** (`detectExistingPr` + `gh pr create` inside `publishBuiltCommit`).
- [x] **T1b — `prNumber: 0` must never erase a known PR number (the measured 267→0 zeroing)** — landed as `dc9d92b`: positive-integer guards at forge adoption, `checkpoint()`, `writeTerminalResult()`, and `parseInnerResult`; run-f384460d trace replayed RED-first (lease push, PR resolved from branch, review re-fired, pr never reads 0; inner-error variant keeps pr=267).
- [x] **T3 — an unpushed finished commit is an ALARM in words, not an exit code.** When a run goes terminal-failed in pr mode, the orchestrator probes the run's branch (local `rev-parse refs/heads/<b>` vs `ls-remote --heads origin`); a local head origin does not hold appends the exact sentence "a finished commit was not published: <sha>" to `failure_reason`, and `composeTerminalDelivery` surfaces that sentence to the owner verbatim (never inferable only from cleanup stdout in a journal). Single sentence source (`unpublishedCommitAlarmSentence` in `delivery.ts`) so the writer and the reader can never drift. Tested RED-first: a failed pr-mode run whose branch is ahead of origin carries the sha in `failure_reason` and in the composed delivery text; a pushed-branch failure and a local-mode failure do not; the publish-failed strand (the measured f384460d shape) carries it.
- [ ] **T4 — never report infra as a verdict.** An `inner-error`/`ok:false` or `block_kind='infra-only'` harvest must not write `inner_verdict='REQUEST_CHANGES'` nor deliver review-flavoured copy: author an infrastructure `failure_reason` (the substrate — `ok`, `block_kind`, `terminal_cause` — is already parsed in `inner-loop.ts`) and route it through `interpretFailure`'s existing `infra` class ("internal error", retry; `delivery.ts:257-271`). This IS the landing of the sibling item "an infra-only block must be delivered as infrastructure" — implement once, here.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
