# IMPLEMENTATION_PLAN — A build with no PR number must resolve one from its branch, not die — and must never leave a finished commit unpushed (run f384460d)

Governing spec: this Plan card. Verified against branch `trident/a-build-with-no-pr-number-must-reso` (ORIGIN head `fe8f39b`, 2026-08-15 03:45Z — origin is newer than, and a superset of, the stale local head `dea3358`; continue from origin):

- **T2 is LANDED.** A pr-mode forge-done returns a publish handoff (`publishRequested`+`publishHead`, `trident/inner-workflow.mjs`), and the orchestrator's `publishBuiltCommit` (`trident/orchestrator.ts`) pushes with an OBSERVED lease (`--force-with-lease=refs/heads/<b>:<observed>`), confirms the head on origin, resolves the PR from the branch, materializes the review diff, and re-fires review via the `outer-published:<head>:<remaining>:<round>` checkpoint. Tested in `trident/orchestrator.test.ts`. No verdict is written before the commit is published on this path.
- **T1a is LANDED.** `publishBuiltCommit` asks GitHub which PR has this head (`detectExistingPr` → `gh pr list --head`) and, when a branch genuinely has no PR, OPENS one (`gh pr create`) rather than failing the build. Publish failures carry git's own redacted stderr.
- **T1b is LANDED** ("a prNumber of 0 is a sentinel, never a PR number"). All four readers of the pr-mode `PR_NUMBER=0` sentinel adopt/persist/decode only a POSITIVE INTEGER: forge adoption in `inner-workflow.mjs`, `checkpoint()`, `writeTerminalResult()`, and `parseInnerResult` (`inner-loop.ts:523-526` maps non-positive/fractional to null so every `result.pr_number ?? run.pr` site retains the known PR). Run-f384460d trace replayed RED-first in `orchestrator.test.ts`.
- **T3 is LANDED** ("an unpushed finished commit is an ALARM in words, not an exit code"). Every terminal-failed pr-mode transition probes the run's branch (`detectUnpublishedCommit`: local `rev-parse` vs `ls-remote --heads origin`) and appends the exact sentence "a finished commit was not published: <sha>" to `failure_reason`; `composeTerminalDelivery` surfaces it verbatim on its own line. Single sentence source (`unpublishedCommitAlarmSentence`/`unpublishedCommitAlarm` in `delivery.ts:316-325`) so writer and reader cannot drift.
- **T4 is NOT implemented; its substrate is.** `parseInnerResult` decodes `ok`/`block_kind`/`terminal_cause` (`inner-loop.ts:514,542-554`) and `innerTerminalFailureReason` has the one permitted specific infra-only message (`orchestrator.ts:706-708`). But the final catch-all (`orchestrator.ts:1374-1391`) still writes `inner_verdict='REQUEST_CHANGES'` for an `inner-error`/`ok:false` harvest with no findings, `run-progress.ts:198` surfaces that verdict to the owner, and `interpretFailure` (`delivery.ts:212-218`) delivers the review-flavoured "ended without an approved review" class — a verdict-shaped message for an infrastructure death. This is the measured f384460d delivery defect and shares its shape with "an infra-only block must be delivered as infrastructure"; implement ONCE, here.

- [x] **T2 — publish BEFORE reporting terminal** (outer publisher: lease push pinned to the observed remote sha, origin confirmation, PR resolve-or-create, diff materialization, review re-fire; `orchestrator.ts` + `inner-workflow.mjs`, tests in `orchestrator.test.ts`).
- [x] **T1a — a branch with a discoverable PR is resolved from GitHub, and a branch with none gets one OPENED, never a failed build** (`detectExistingPr` + `gh pr create` inside `publishBuiltCommit`).
- [x] **T1b — `prNumber: 0` must never erase a known PR number (the measured 267→0 zeroing)** — positive-integer guards at forge adoption, `checkpoint()`, `writeTerminalResult()`, and `parseInnerResult`; run-f384460d trace replayed RED-first (lease push, PR resolved from branch, review re-fired, pr never reads 0; inner-error variant keeps pr=267).
- [x] **T3 — an unpushed finished commit is an ALARM in words, not an exit code** — terminal-failed pr-mode transitions probe the branch and append "a finished commit was not published: <sha>" to `failure_reason`; `composeTerminalDelivery` surfaces it verbatim; one sentence source in `delivery.ts`; RED-first tests for ahead-of-origin / pushed / local-mode / publish-failed shapes.
- [x] **T4 — never report infra as a verdict.** An `inner-error`/`ok:false` harvest with no findings, or a `block_kind='infra-only'` harvest, must not write `inner_verdict='REQUEST_CHANGES'` (write null — `run-progress.ts` surfaces this field to the owner) nor deliver review-flavoured copy: decode `findings_present` fail-closed in `parseInnerResult`, add an `isInfraDeath` predicate, author an infrastructure `failure_reason` (one sentence source `infraDeathSentence` in `delivery.ts`, keeping the existing measured-cause "review never ran (infra-only)…" message when a cause exists), and route both authored infra reasons through `interpretFailure`'s existing `infra` class — placed EARLY so an embedded cause's stray 'stalled'/'git ' tokens cannot misroute it. This IS the landing of the sibling item "an infra-only block must be delivered as infrastructure" — implement once, here. RED-first replay: the f384460d inner-error harvest (pr=267) must produce `inner_verdict=null`, pr intact, an infra reason, and an `infra`-class delivery.
