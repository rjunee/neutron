# IMPLEMENTATION_PLAN — Readiness gate resolves required checks from the repo, not a constant

Regenerated 2026-08-15 against main d08555e. Card: neutron-enterprise run `a6da50ea` / PR #515 — `REVIEW_REQUIRED_CHECKS = ['test','lint','typecheck']` hardcodes THIS repo's job names into a gate that runs against two repos, so review can never run in neutron-enterprise (its checks are `check`, `frontend`, `license-gate`, …). The gate burns the full 15-minute budget and defers with "required check test has not run" — a queue-delay sentence for a config error.

Already satisfied in the tree (verified, do not redo):

- [x] The readiness budget is measured and generous (15 min / 30 s retry, derived attempt count) — `REVIEW_READINESS_BUDGET_MS` / `REVIEW_READINESS_RETRY_MS` / `REVIEW_READINESS_ATTEMPTS`, guarded by ci-gate.test.ts budget tests. The budget itself is NOT this card's defect; keep it untouched.
- [x] Unreadable probes quote their own words (`probeCause` + `redactProbeText`), redacted and capped; 'unknown' is never green.
- [x] The probe seat is a verbatim transcriber (`CI_PROBE_SCHEMA`), every judgement made in pure JS; probes carry the instance credential via `ghReadCommand`.
- [x] Local mode (`!isPr`) bypasses readiness entirely (`probeReviewReadiness` returns 'passed') — unchanged by this card.
- [x] Deferral wording states how long the gate waited (`readinessBudgetLabel`), and 'conflicting'/'unknown' break the retry loop immediately.

The card's work — ONE cohesive change (the classifier, its feed, and the test harness that string-slices the classifier out of the source must move in the same commit, or the tree is broken between iterations):

- [x] Resolve required checks from the PR's base branch at probe time; delete `REVIEW_REQUIRED_CHECKS`. New once-per-round seat reads branch protection `required_status_checks` + rulesets for `baseBranch` and the check-run names observed on the base branch head; a new pure `classifyRequiredChecksProbe` turns that into `{mode:'resolved', required, produced}` (404 on protection/rules = definitively unprotected, other read failures = 'unknown' → defer quoting the cause; unreadable check-run list = `produced: null`, which disables ONLY the config-error fast-fail, never the wait). `classifyReviewReadiness(probe, requiredConfig)`: protected base → today's per-name absent/pending/failed logic over the RESOLVED names, with a new fail-fast 'config-error' status ("required check X is not produced by any workflow in this repository") when a required name is in neither the rollup nor the produced list; a declared-but-unreported name still waits as 'absent'. Unprotected base → at least one non-SKIPPED check exists and ALL are COMPLETED + SUCCESS/NEUTRAL ⇒ 'passed'; zero checks ⇒ 'absent' (waits, then defers — never passes); this preserves the empty-rollup/CodeQL property the constant existed for, without borrowing another repo's job names. `reviewWithPreconditions` breaks immediately on 'config-error' (no budget consumed); `reviewPreconditionDeferred` gains the config-error evidence branch (a repair instruction, not "re-run"). Update `loadReadiness` in ci-gate.test.ts to new named-error-guarded anchors, keep all preamble additions in `//` comments (a `/**` there truncates the classifyCi slice — documented at inner-workflow.mjs:2819), port existing tests to pass a required-config, and add the guard: a `check`/`frontend`-only neutron-enterprise rollup on an unprotected base PASSES readiness, and with a protection demanding `test` it names the config error immediately — asserted, never a silent defer. Add the new seat to `gh-read-command.test.ts`'s probe list and stub it in `dying-reviewer-e2e.test.ts`. Assert no repo-specific job name remains in inner-workflow.mjs. Append the docs/AS_BUILT.md entry.

Guard rails: do NOT change the budget constants or their tests; do NOT touch `classifyCi` (the merge-time CI gate — separate concern, already name-agnostic); do NOT add any `/**` doc-block between `const CI_FAILED_STATES` and `classifyReviewReadiness` (both test slices truncate at `/**` openers); keep the 'absent' wording `required check ${name} has not run` verbatim (mutant tests anchor on it); local mode behaviour is unchanged.
