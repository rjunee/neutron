# IMPLEMENTATION_PLAN — Parallelise the build's test suite by ASKING the runner, per-project, budget-aware (Plan card 2026-08-15)

Governing spec: this Plan card. Verified against `origin/main` (`cd8215e`) and the resume branch `trident/the-build-re-runs-1-273-test-files` (tip `1526441`; T1 committed in `999aa04`, T2 in `1526441`), 2026-08-15:

- MEASURED FACTS: `scripts/run-tests.sh` (500 lines) ALREADY implements chunking, `NEUTRON_TEST_JOBS` (default 1 = sequential), `NEUTRON_TEST_CHUNK_SIZE` (100), cross-runner `NEUTRON_TEST_SHARD`, the declared/discovered/assigned/executed coverage audit (drift = fatal), the serial PGLite quarantine lane and the serial device-harness lane. `docs/testing-runner.md` documents all knobs and the RSS model (peak ≈ JOBS × CHUNK_SIZE × per-file working set). **Do NOT build a scheduler — this card is wiring, not sharding.**
- The build side previously set NOTHING: the canary's `timeout 590 bash scripts/run-tests.sh` was the agent hedging its Bash cap against a ~20-minute suite, so complete runs got killed mid-flight and read as failures (criterion 7).
- Threading precedent, copied exactly: `reflectionGuidance` — derived in testable TS launcher-side, carried as an ALREADY-RENDERED string in `buildWorkflowArgs`, destructured `= ''` in the `.mjs` so an absent arg is byte-identical legacy behaviour, spliced into FORGE ONLY, never argus.
- Box, measured 2026-08-15: 8 cores, 30 GB total, 25 GB available (load avg ~1.9 — this box is SHARED with other build lanes). Suite: 1,273 files ≈ 20 min sequential. `computeTestJobs` as committed (headroom 0.8, chunk 100, 24 MiB/file) yields 8/4/2 jobs for 1/2/4 active runs on these numbers.
- PART 2 (enterprise): trident deploys to every project, so ALL logic lives trident-side and the repo's runner + knobs are DISCOVERED (`package.json` `scripts.test` → agent docs) by T1's `buildTestStrategy`, never hardcoded; a repo with no knobs runs unchanged, sequentially, with one log line (`NO_KNOBS_LINE`) — never an error.

- [x] **T0 — runner-side sharding, audit, and lane isolation.** Already shipped in `scripts/run-tests.sh` + `docs/testing-runner.md`. Nothing to build; the file is NOT to be modified by this card (criteria 3 and 6 are satisfied by leaving it alone and proving the audit line still prints under `JOBS>1` in T3's measurement).
- [x] **T1 — `trident/test-strategy.ts`: discover the project's test command, probe its parallel knobs statically, compute a concurrency-aware jobs budget, and render the TEST EXECUTION prompt block (two-stage gate + full-suite-required + no-`timeout`-wrapper rules).** Committed (`999aa04`): `resolveTestCommand` (package.json → fenced agent-doc invocations, closed pattern list), `probeParallelKnobs` (static text scan only — never spawns), `computeTestJobs` (cores ÷ `max(DEFAULT_BUILD_FANOUT=4, active runs)`, RAM-capped by the same divisor; on 8 cores/25 GiB that is jobs=2 at 1–4 active runs and 1 at ≥8 — superseding this task's first cut, which divided by the LIVE count and produced 8/4/2/1; see T4 and `docs/AS_BUILT.md`), `renderTestStrategy` + never-throwing `buildTestStrategy`, with pinned constants `NO_KNOBS_LINE`, `STAGE_1_REJECT_ONLY`, `FULL_SUITE_REQUIRED`, `NO_TIMEOUT_WRAPPER`. 300+ lines of tests alongside (`trident/test-strategy.test.ts`).
- [x] **T2 — thread the strategy into both build contracts.** Committed (`1526441`): `readHostBudget()` (cores via `os.availableParallelism()` with fallbacks, MemAvailable from `/proc/meminfo` with `os.freemem()` fallback, injectable read seam); `InnerLoopInput.test_strategy` carried through `buildWorkflowArgs` beside `reflectionGuidance`; orchestrator option `resolve_active_runs` (best-effort, throw → 1) composed into `buildTestStrategy(...)` at launch; the LIVE wiring in `gateway/composition/build-core-modules.ts` (`resolve_active_runs = () => store.listNonTerminal(200).filter(phase === 'forge-init' || 'forge-fix').length`, narrowed to BUILD phases in T4 — the phase-models lesson); `inner-workflow.mjs` destructures `testStrategy = ''` and splices the block into `forgeBuildContract` (reaching forge:build, every fix round, and the codex brief) with `''` → byte-identical legacy contract. Executed-prompt-capture tests prove forge-only reach and argus exclusion.
- [x] **T3 — measure on the real box, reconcile the timeout in writing, and document.** DONE, on the resume branch at `1526441` (8 cores, 25.65 GiB MemAvailable, shared box, both runs back to back in the same worktree): **sequential baseline 1322 s = 22.0 min (load 2.61 → 4.90); `NEUTRON_TEST_JOBS=8` (an idle-box ceiling, NOT the shipped budget — see T4) 670 s = 11.2 min (load 3.57 → 4.91) — 1.97x; the SHIPPED `jobs=2 concurrency=4` budget re-measured in T4 at 823 s = 13.7 min — 1.61x, 8.3 min saved per suite run**, and the general chunks alone went 915 s → 293 s (3.12x), the remainder being the ~6.3 min serial floor of the PGLite + device lanes. `computeTestJobs` returns 8 / 4 / 2 / 1 jobs at divisor 1 / 2 / 4 / 8 (T4 makes that divisor the CONSTANT fan-out) — never more than 8 chunk processes on 8 cores (criterion 2 at N=1 and N=4). Under `JOBS=8` the audit still prints `files executed: 1273 (1219 general + 20 PGLite + 34 device)` (criterion 3) and both lanes still print `max-concurrency=1` / `own process` after the last general chunk, retry budget intact (criterion 6). Both runs exited 1 with a **byte-identical 125-failure set** (environment-shaped, pre-existing, none inside this card's diff; the card's own 188 tests are green) — so no green-run speedup is claimed, only that the identical failing set completed in half the time. Criterion 7 in writing: `timeout 590` is 9.8 min, shorter than the suite BEFORE (22.0) and still shorter AFTER (11.2), so it is banned by `NO_TIMEOUT_WRAPPER` and replaced by a background run polled via log tail with a 25-minute hang budget (reconciled in round 4 against the codex build bridge's 45-minute polling ceiling — see `docs/AS_BUILT.md`). `docs/AS_BUILT.md` carries all of it plus the PART-2 statement (no-knob repo runs unchanged + one `NO_KNOBS_LINE`; nothing measured on enterprise, no speedup claimed there); `docs/testing-runner.md` carries the derivation note.
- [x] **T4 — close Argus round 1: the budget must survive STAGGERED launches, and `testsPassed` must be READ.** DONE. (a) TS2532 in `firstFencedTestInvocation` (the typecheck blocker) fixed; (b) the divisor is now the CONSTANT `DEFAULT_BUILD_FANOUT = 4` with the live count as a raise-only term — a launch-time snapshot is frozen into the prompt for the whole run, so run 1 (alone → 8) + run 2 (→ 4) + run 3 (→ 2) totalled 14 processes on 8 cores; the constant makes 4 × 2 = 8 hold however launches are staggered; (c) `computeTestConcurrency` budgets `NEUTRON_TEST_CONCURRENCY = cores ÷ jobs` — the first cut ran `jobs=8 concurrency=8` = 64 interleaved files on 8 cores, +28% CPU, and the shipped `jobs=2 concurrency=4` measures −4%; (d) the live count counts BUILD phases only (`forge-init`/`forge-fix`), so runs parked in planning/review no longer starve the build that is running tests; (e) the knob is rendered as `export` lines above the command, and a command that PINS the knob itself is run unchanged with its own line (`A=1 A=2 cmd` → the command sees 2, and a prefix reaches only the first command of `tsc && bun test`); (f) stage 1 diffs `<base>` vs the WORKING TREE plus untracked files (the contract commits AFTER the tests, so `<base>..HEAD` was empty) and is capped at 40 files in batches of 20, dropping the `grep -l <basename>` tier whole rather than running 642 files in one un-chunked process; (g) tier-2 agent-doc invocations are rejected if they contain shell chaining/substitution/redirection (every alternative ends in `.*`, which admitted `bun test ; curl … | sh`), and `npm init`'s `no test specified` placeholder is not a test command; (h) `fullSuiteBlock` in `inner-workflow.mjs` gives `testsPassed` a real consumer — a build that did not prove the full suite gets NO review panel and a synthetic REQUEST_CHANGES straight into the fix loop (local mode only; **superseded by T5**, which found this shape both unreachable in PR mode and wasteful in local mode); (i) the TEST EXECUTION block is hoisted ABOVE the numbered CONTRACT, out of step 6's verbatim last-lines list; (j) the AS_BUILT receipt names its normalisation (`sed 's/ \[[0-9.]*ms\]$//' | sort -u` → 120 unique failures, zero-line diff across all three runs) and the 24 MiB constant is described as a safety factor rather than attributed to a measurement it is not in.
- [x] **T5 — close Argus round 2: make the full-suite gate REACHABLE, make it ADD to the panel instead of replacing it, and bound stage 1 for real.** DONE. (a) **PR mode.** The round-1 gate sat AFTER the `isPr` publish return, and the re-fired run re-enters as `mode:'review'` with no build report — so in the DEFAULT git mode the gate never ran at all, and a PR-mode project without CI had the full-suite requirement enforced by nothing. The claim now travels on the checkpoint the publisher re-fires from: `checkpoint('forge-done' | 'fix-round-N')` records the blocker in `inner_checkpoint_findings` (the publish reset patch leaves that column alone), the resumed run reads it back as `resumeFindings` and injects it. The same wire closes the local-mode crash-resume hole. (b) **The panel runs.** Replacing the panel with a synthetic verdict meant a run against a pre-existing red suite burned every round on a ~14-minute suite and returned ZERO review signal; the blocker is now PREPENDED to the panel's findings and the verdict FORCED to REQUEST_CHANGES (`withSuiteBlocker`), so criterion 5 holds by override rather than by starvation, and `blockKind` is forced to `code` so an infra-only panel cannot exit the fix loop on it. (c) **Stage 1 is bounded as a WHOLE** — the cap applied to tier (c) only, so (a)+(b) were unbounded (54 files for a one-file edit, 589 for this card's own diff, growing every fix round off the cumulative `git diff <base>`). (d) An agent-doc **file-scoped example** (`bun test packages/api/src/one.test.ts`) is no longer adoptable as the project's full suite — the PART-2 path where a one-file run could satisfy the gate. (e) The RAM cap divides by the same fan-out the cores do (`{32 cores, 16 GiB}` budgeted 5 jobs per build = a claimed 48 GiB across 4 builds); inert on the reference box. (f) `computeTestConcurrency` no longer returns Infinity for a fractional jobs count; the dead `|\|\|` alternative is gone from `SHELL_METACHARACTERS`; the unresolved-command branch stops reporting on a runner it never probed; a jobs-only runner is told its per-process concurrency is NOT bounded here. (g) AS_BUILT's coverage claim is corrected (it said the requirement held in both modes; it did not) and the device-lane effect of the exported concurrency knob is stated.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
