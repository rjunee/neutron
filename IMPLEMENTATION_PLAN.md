# IMPLEMENTATION_PLAN — A build must rebase onto main before review (stale branch ≠ rejected build)

Governing spec: this Plan card. Verified against origin/main 2026-08-15 (`51d17b7`); resumed from branch `trident/a-build-must-rebase-onto-main-befor` (tip `fb56bfc`), which is based exactly on current origin/main with three commits on top (feat `9be4a27`, tests `916989e`, docs `fb56bfc`). T1+T2 previously re-verified green: 59/59 tests across `trident/orchestrator.test.ts` + `trident/publish-rebase-realgit.test.ts` from a clean checkout of the branch tip.

- The readiness probe is repaired and honest: `classifyReviewReadiness` (`trident/inner-workflow.mjs:2123`) reads `mergeable: CONFLICTING` and defers the review round. Correct behavior, now exposing that NOTHING rebased a pr-mode branch — fixed by T1; the only other rebase machinery in the tree is local-mode merge-time (`merge.ts` + `conflict-resolver.ts`).
- The lease push exists and is correct: `publishBuiltCommit` (`orchestrator.ts`) pins `--force-with-lease=refs/heads/<b>:<sha>` to an `ls-remote`-OBSERVED sha, never the remote-tracking ref; third-party-advance refusal is asserted at the fake-host level AND against a real remote (T2).
- DECIDED — WHEN: the rebase lives in the OUTER publisher, between the local-tip verification and the lease observation/push. Only the outer loop holds a push credential (the Forge sandbox strips `*TOKEN*`), so "rebase + re-push before the readiness probe" can only happen there; and because the publisher fires after EVERY build/fix round and before EVERY review re-fire, every fix round re-enters a branch already based on current main — post-round-1 code IS written against the tree it merges into, the closest to "before build" the credential boundary allows.
- DECIDED — MECHANISM (shallow-safe, governance #574/#571): never `git rebase` in the shared checkout (`.git/shallow` boundary makes merge-base lie). Replay the branch's own diff onto the OBSERVED base tip in a throwaway detached worktree: PR exists → `gh pr diff <n>` (server-side merge-base, shallow-immune); first publish → two-dot `git diff <base>..<branch>`. `git apply --3way`, commit, move `refs/heads/<branch>` by compare-and-swap (`update-ref <new> <old>`). The replay squashes the branch into one commit — acceptable, the PR merge is `--squash` anyway.
- DECIDED — ON CONFLICT: a failed 3way apply is an ATTENTION state, never `REQUEST_CHANGES` and never auto-resolved: fail the run with `TridentRebaseConflict` whose reason starts `REBASE CONFLICT — needs attention:` naming branch, base, and the conflicting paths, stating no reviewer judged the code. The `resolve_conflict` Forge resolver is NEVER invoked on this path.

- [x] **P0 — lease-pinned publish**: `--force-with-lease` pinned to an observed sha, empty-lease first push, third-party refusal, git's own words in the failure (merged; `orchestrator.ts`, tests `orchestrator.test.ts`).
- [x] **P0b — honest readiness probe**: `CONFLICTING` → deferred review, no seat dispatched, no round consumed (merged; `inner-workflow.mjs:2123-2187`, `reviewPreconditionDeferred`). The condition it reports is what this plan removes.
- [x] **T1 — shallow-safe rebase-onto-observed-main in the outer publisher (+ conflict = attention state), with fake-host unit tests.** DONE (commit `9be4a27`): exported `rebaseOntoObservedBase(...)` + `TridentRebaseConflict` in `orchestrator.ts`; wired into `publishBuiltCommit` after the local-tip check and before the lease observation; rebased head flows into the push, the post-push confirm, the review diff, and the re-fire checkpoint; PR probe hoisted so the replay can use `gh pr diff`; scratch worktree always torn down. 56/56 tests pass in `orchestrator.test.ts`.
- [x] **T2 — REAL-git + REAL-shallow integration test (`trident/publish-rebase-realgit.test.ts`) guarding acceptance (a)/(b)/(c)/(d) against fake tests.** DONE (commit `916989e`): real origin whose `main` moved by a genuine non-conflicting commit after the branch was cut, with behind-ness PROVED in a FULL clone (`merge-base --is-ancestor` refusing); build checkout is `git clone --depth=1 file://…` and the test ASSERTS `.git/shallow` exists before exercising anything; replay onto main's tip with no false conflict, both sides present in the tree, post-push MERGEABLE fact proved in a full clone; conflicting seed throws `TridentRebaseConflict` naming the path, ref unmoved, never `REQUEST_CHANGES`; third-party advance makes the pinned lease push REFUSE against the real remote. 3/3 tests pass.
- [x] **T3 — docs: record the decision + as-built.** DONE (commit `fb56bfc`): dated entry in `docs/AS_BUILT.md`; "THE PUBLISH BOUNDARY" section of `docs/SYSTEM-OVERVIEW.md` extended with rebase-before-lease ordering, the shallow workaround (#574), and conflict-as-attention. (Residual ordering defect in AS_BUILT — see T4.)
- [x] **T4 — close-out: restore AS_BUILT newest-first ordering and re-verify green at tip.** The `fb56bfc` docs commit inserted the `## 2026-08-15 — a pr-mode build rebases onto observed main before review` entry BELOW the `## 2026-08-14 — the by-path build brief…` entry (line 31 of `docs/AS_BUILT.md`), violating the file's stated newest-first invariant, and the entry header abuts the previous entry's last line with no blank separator. Move the whole 2026-08-15 section to the top (immediately under the intro line), with exactly one blank line at each seam; persist this regenerated IMPLEMENTATION_PLAN.md; re-run both suites at the tip and confirm 59/59 green; commit on the branch.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
