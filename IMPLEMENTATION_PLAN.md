# IMPLEMENTATION_PLAN — A build with no PR number must resolve one from its branch, not die — and must never leave a finished commit unpushed (run f384460d)

Governing spec: this Plan card. Verified against the tree 2026-08-15 (single-commit graft d8324cc):

- **T2 is LANDED.** The outer-publisher design is in: a pr-mode forge-done returns a publish handoff (`publishRequested`+`publishHead`, `trident/inner-workflow.mjs:3172-3190`, fix rounds `:3355-3377`); the orchestrator's `publishBuiltCommit` (`trident/orchestrator.ts:465-553`) pushes with an OBSERVED lease (`--force-with-lease=refs/heads/<b>:<observed>`), confirms the head on origin, resolves the PR from the branch, materializes the review diff, and re-fires review via the `outer-published:<head>:<remaining>:<round>` checkpoint. Tested at `trident/orchestrator.test.ts:141-265` (rebase publishes AND a moved remote is refused). No verdict is written before the commit is published on this path.
- **T1's branch-resolution half is LANDED.** `publishBuiltCommit` asks GitHub which PR has this head (`detectExistingPr` → `gh pr list --head`, `orchestrator.ts:447-463,527-536`) and, when a branch genuinely has no PR, OPENS one (`gh pr create`) rather than failing the build. Publish failures carry git's own redacted stderr (`publishFailureReason`/`redactPushError`, `orchestrator.ts:344-369`).
- **T1's zeroing half is NOT fixed — the measured 267→0 mechanism is live at three sites.** The codex wrapper's pr-mode trailer is `PR_NUMBER=0` BY DESIGN (`inner-workflow.mjs:818` FORGE_PR_LINE: "the outer loop publishes after this build exits"), so `forge.prNumber` arrives as `0`; `inner-workflow.mjs:3167` (`!== null && !== undefined`) adopts it, clobbering the pr=267 threaded in at launch; `checkpoint('forge-done', { pr })` (`:1321`) and `writeTerminalResult` (`:1375`) persist the 0 onto the run row; and on harvest `parseInnerResult` (`trident/inner-loop.ts:429-430`) accepts any finite number, so `result.pr_number ?? run.pr` at `orchestrator.ts:892` (0 is not nullish) writes pr=0 onto the failed row at `:967`.
- **T3 is NOT implemented.** `worktree-cleanup.sh` detects the stranded commit precisely (`PRESERVED branch <b> reason=unpushed`, exit 3) but the signal dies in `log()` at `inner-workflow.mjs:3564-3574` — it reaches neither the run row nor the owner. `trident/delivery.ts` has no wording for an unpublished finished commit.
- **T4 is NOT implemented, and the sibling item has NOT landed first.** The inner workflow classifies `blockKind: 'infra-only'` (`inner-workflow.mjs:3475-3480`) but `parseInnerResult` never parses it, so nothing outer can distinguish infra from a code rejection; an `inner-error` catch result (`:3503-3513`, `findings` absent) harvests into the generic "ended without an approved review" delivery (`delivery.ts:195-201`) — a verdict-shaped message for an infrastructure death. Implement once here; the delivery module's existing `infra` class (`delivery.ts:257-271`) is the reuse point.

- [x] **T2 — publish BEFORE reporting terminal** (outer publisher: lease push pinned to the observed remote sha, origin confirmation, PR resolve-or-create, diff materialization, review re-fire; `orchestrator.ts:465-553` + `inner-workflow.mjs:3172-3190`, tests `orchestrator.test.ts:141-265`).
- [x] **T1a — a branch with a discoverable PR is resolved from GitHub, and a branch with none gets one OPENED, never a failed build** (`detectExistingPr` + `gh pr create` inside `publishBuiltCommit`, `orchestrator.ts:527-536`).
- [x] **T1b — `prNumber: 0` must never erase a known PR number (the measured 267→0 zeroing).** Adopt a forge-reported PR only when it is a positive integer (`inner-workflow.mjs:3167`); refuse to persist a non-positive pr in `checkpoint()` (`:1321`) and `writeTerminalResult()` (`:1375`); `parseInnerResult` maps non-positive/non-integer `prNumber` to `null` (`inner-loop.ts:429-430`) so every `result.pr_number ?? run.pr` site retains the known PR. Replay the run-f384460d trace as a test: a forge result with a valid 40-hex `commitSha`, `testsPassed: true`, `prNumber: 0`, origin one commit behind → the lease push runs, the PR is resolved from the branch, review is dispatched, and the run row's pr never reads 0; plus the inner-error variant (checkpoint `'inner-error'`, `prNumber: 0`) keeps pr=267 on the failed row — RED today at `orchestrator.ts:967`.
- [ ] **T3 — an unpushed finished commit is an ALARM in words, not an exit code.** When a pr-mode run goes terminal-failed, the orchestrator probes the run's branch (local `rev-parse` vs `ls-remote origin`); a local head origin does not hold appends "a finished commit was not published: <sha>" to `failure_reason`, and `delivery.ts` surfaces that sentence to the owner verbatim (never inferable only from cleanup stdout in a journal). Test: a failed run whose branch is ahead of origin carries the sha in `failure_reason` and in the composed delivery text; a pushed-branch failure does not.
- [ ] **T4 — never report infra as a verdict.** Parse `blockKind` (and `ok`) in `parseInnerResult`; an `inner-error`/`ok:false` or `infra-only` harvest authors an infrastructure `failure_reason` and `interpretFailure` routes it to the existing `infra` class ("internal error", retry) — never the review-flavoured "changes requested" framing. Shares its shape with "an infra-only block must be delivered as infrastructure" (not yet landed anywhere — verified nothing outer reads `blockKind`): implement once here, as that item's landing.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
