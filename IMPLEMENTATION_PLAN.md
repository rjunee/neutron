# IMPLEMENTATION_PLAN.md — M2 P0 parity: input modalities (documents + voice) + minimal chat commands (Neutron Open)

Supersedes the executor-mode-reminders plan (landed via the PR #426 lineage; any residue is tracked in `docs/plans/executor-mode-reminders-2026-07-20.md`, not here). Scope source: `docs/research/vajra-neutron-parity-audit-2026-07-19.md` blocker 3 + Ryan 2026-07-21 ("only the chat commands that make sense for Neutron" → `/status` + `/reset` ONLY; the Telegram topic-lifecycle vocabulary is NOT ported). NO FEATURE FLAGS — build the real default. One task per Ralph iteration. THIS SEQUENCE CONTINUES ON THE EXISTING BRANCH `trident/input-modalities-commands` + PR #428 (main is protected — reuse the branch/PR, do NOT open new ones); update `docs/AS_BUILT.md` + `docs/SYSTEM-OVERVIEW.md` inside each task's commits.

Planner ground-truth (original cites re-verified 2026-07-21 against 63fe4119; iteration-3 findings verified against branch HEAD 141d2c1c — every claim file:line):
- The audit UNDERSTATES the modality gap: attachments (INCLUDING images) never reach the agent. `open/wiring/app-ws.ts:756-770` reads `adapter_metadata.attachments` then drops them; an attachment-only send dispatches the placeholder "Sent an attachment.". Task 1 built the attachment→turn threading as the foundation, then widened accepted types.
- Server-side type machinery already EXISTS: `gateway/storage/binary-types.ts` magic-byte sniffs `%PDF-` (:184-193) and audio RIFF/ID3/ftyp (:181-224); `BINARY_MIME_WHITELIST` (:47-58) already lists `application/pdf` + `audio/{mpeg,mp4,wav}`. Only `gateway/http/app-upload-surface.ts` (`IMAGE_MIME_WHITELIST`) restricted chat uploads pre-task-1.
- No ASR/transcription code exists anywhere (grep `whisper|transcrib` → only docs/comments; re-verified on branch HEAD). An OpenAI credential path exists: `OPENAI_API_KEY` via `resolveOpenOpenAiPool` (`open/composer.ts:461-474`) + `config/index.ts:244,267-269,447-449`.
- Chat-command chain: ONE composer-level chain at `open/composer.ts:1403-1406` — `[coresWiring.chatCommandFilter, buildSkillForgeChatCommandFilter(...)]`, where the cores inner chain is `gateway/cores/mount-open-cores.ts:341-364` (`/cal /email /remind /research`); shared by web AND app-ws (comment :1396-1402). Builders live in `gateway/boot-chat-command-filters.ts`; the contract is `contracts/chat-command-filter.ts:25-44`. NO `/status` or `/reset` chat handler exists anywhere (re-verified on branch HEAD: grep-zero; `gateway/http/app-connect-auth.ts:151,215` '/status' is an unrelated HTTP sub-route). `gateway/composition.ts` chains no filters.
- Clients: web file input `landing/chat-react/ChatApp.tsx` (accept list + chip render updated by task 1), allow-list `landing/chat-react/uploads.ts`. Expo picker is ALREADY `type:'*/*'` (`app/components/ChatSyncSurface.tsx`); `app/lib/upload-client.ts` mime→ext map extended by task 1.
- Project archiving already exists (`gateway/projects/sqlite-store.ts`, `gateway/http/app-projects-surface.ts`) — do NOT rebuild; explicitly out of scope.
- ITERATION-3 FINDINGS (this pass): task 1's PRODUCTION code is settled — two independent review rounds confirmed the rules-of-hooks fix (a235eea3), the web user-activation fix (8ad3b33d), and the native cache+share fix (141d2c1c); `bunx tsc --noEmit` exit 0 on branch HEAD; the 12 attachment test files pass in isolation (e.g. the 3 landing files 39/0 alone). BUT the canonical CI gate is RED: 3 consecutive `test`-job failures (runs on a235eea3, 8ad3b33d, 141d2c1c — `gh run list`), first red exactly at the commit that introduced `mock.module('react', ...)`. Root cause: `app/__tests__/authed-attachment-image-hooks.test.tsx:41-64` and `app/__tests__/authed-attachment-file-open.test.tsx:29-52` register process-global NARROW mocks of `react` / `react/jsx-runtime` / `react/jsx-dev-runtime` / `react-native`; in the shared-process CI chunk (`scripts/run-tests.sh`, 75-file chunks, app/* files sort together) they poison later files — CI log shows `SyntaxError: Export named 'useReducer' not found in module .../react/index.js` under `app/__tests__/docs-mutations-race.test.ts` (which does `import * as RealReact from 'react'` :35 and uses `RealReact.useReducer` :61) and `Export named 'Linking' not found in .../react-native/index.js` under `app/__tests__/docs-panes-render.test.ts`; reproduced locally (12 attachment files in ONE bun process → `React__namespace.forwardRef is not a function` from `react-textarea-autosize` under `landing/chat-react/__tests__/attachment-basename.test.ts`; 120 pass / 1 fail / 1 error). This is the EXACT incident class the repo already documents: `docs-mutations-race.test.ts:52-57` ("The old `mock.module('react', ...)` here was process-global in bun and survived `mock.restore()` … ~92 failures") and the react-native "Superset padding" convention (`diagnostics-pane-render.test.ts` RN mock comment; `docs-panes-render.test.ts:10-17`). The production fixes are NOT in question; the defect is TEST HYGIENE in the two new test files. Task 1 stays checked for its production scope; the gate-green completion is task 2 (the immediate top priority — PR #428 cannot merge red).
- Ordering note: tasks 3↔4 swapped relative to the prior plan revision by orchestrator directive this iteration: the narrow chat commands ship before voice/ASR. Voice is deferred one slot, NOT dropped — it remains a P0 parity item (audit blocker 3).

- [x] 1. **Attachment→agent threading + document (PDF) attachments end-to-end.** Server: widen `gateway/http/app-upload-surface.ts` (whitelist / `EXT_FROM_MIME` / `URL_PATH_RE` / `mimeFromExt`) to add `application/pdf`; export ONE URL→local-path resolver helper. Thread the attachments read at `open/wiring/app-ws.ts` into the `appWsChatTurn` call; extend `gateway/wiring/build-live-agent-turn.ts` turn input with `attachments` + an injected `resolveAttachment` seam (composer supplies it over `owner_home`); inject a `<user_attachments>` fragment (resolved local paths under `<owner_home>/chat-attachments/`) into the PROMPT on warm AND cold branches — never mutate `turn.user_text`. Clients: web accept + non-image chip render; Expo mime→ext + pdf; + 3 review-round fixes (hoisted hooks below early return → pure dispatcher split; synchronous `window.open` before awaits to preserve user activation; native open via cache write + OS share sheet). DONE: production code + 2 independent reviewer rounds clean, tsc clean, attachment tests green in isolation. Canonical-gate green is completed by task 2.
- [x] 2. **CI-green hotfix: de-pollute the process-global react/react-native module mocks in the two new app attachment test files.** Rewrite the mock scaffolding in `app/__tests__/authed-attachment-image-hooks.test.tsx` and `app/__tests__/authed-attachment-file-open.test.tsx` to the repo's established contract — every `mock.module` payload is a SUPERSET (spread the real module for `react` + jsx runtimes; the diagnostics-pane superset key-set for `react-native` incl. `Linking`/`useWindowDimensions`/`ScrollView`/`TextInput`/`ActivityIndicator`/`Modal`) and every overridden export DELEGATES through a mutable impl pointer that this suite sets in `beforeEach` and resets to the REAL implementation in `afterAll`, so nothing behaviorally leaks to later files in the same bun process. NO assertion changes — the Argus regression guards stay byte-identical in what they assert. Acceptance: PR #428 `test` check GREEN (flips the 3-run red streak). Persist this plan + AS_BUILT addendum in the same commit.
- [x] 3. **`/status` chat command (narrow re-map — NOT the Vajra topic-lifecycle set).** New `buildStatusChatCommandFilter` in `gateway/boot-chat-command-filters.ts` implementing `contracts/chat-command-filter.ts:25-44` (exact-command boundary — `/statusfoo` falls through null, K8 grammar precedent), chained at `open/composer.ts` into the shared `buildChainedChatCommandFilter([...])` so web + app-ws compose identically — no second command-parsing path. `/status`: deterministic snapshot from an injected thunk (bound via a `late<T>` holder to the live projects reader / `getBestModel` / reminder store `ReminderStore.listPending` / `workBoardStore.listActive` / Trident `TridentRunStore.listNonTerminal`) — active project, current model, pending reminders count, active work-board items, active trident builds. Tests (`gateway/__tests__/status-command-wiring.test.ts`, 9/0): reply text composes from the snapshot values (behavior, not a `toHaveBeenCalled` gap-test); project_id threaded/omitted; word-boundary fall-through never runs the thunk; chain-composition proof. Docs: SYSTEM-OVERVIEW chat-commands + AS_BUILT. DONE 2026-07-22.
- [ ] 4. **`/reset` chat command — CORRECTED mechanism (the plan's original `respawnSupervisedSession` primitive was verified WRONG).** GROUND TRUTH (verified 2026-07-22 against branch HEAD): `respawnSupervisedSession` (`runtime/adapters/claude-code/persistent/supervision.ts:59`) → `respawnReplSession(..., force=true)` → `planRespawn` ALWAYS `--resume`s the SAME transcript (`session-respawn.ts:24` "respawn ALWAYS resumes — never a fresh spawn"; `has_session`+`sessionId` PRESERVED), so it PRESERVES context — it does NOT reset it. Using it for `/reset` would be a no-op-that-looks-done (banned). The correct "fresh agent context; durable chat history stays" primitive is EITHER (a) the `/clear` PTY reset — `CONTEXT_RESET_COMMAND` `signatures.ts:164`, written to `session.child` at `pool.ts:380`, already the import warm-session per-turn reset (needs the `acquireTurn` mutex so it doesn't race a live turn), OR (b) a genuine FRESH (non-`--resume`) respawn (clear the registry record's `has_session` + evict the warm child so the next dispatch spawns fresh). BOTH need to TARGET the live session key, which folds the selected `cred.id` (`build-llm-call-substrate.ts:721`) into `poolKeyFor` (`pool.ts:191-205`) — unknown to the filter; resolve it credential-agnostically (a prefix scan of `supervisedBySessionKey` under the instance `replRegistryPath`, mirroring the existing scans at `supervision.ts:452,542`, keyed on `cc-agent-<owner>SEP<user>SEP<project_id>SEP`). Ryan sign-off needed on which primitive (this is a spec correction, not the plan's original mechanism). Tests: the reset actuation is invoked with the resolved live session key; non-command fallthrough returns null. Docs: SYSTEM-OVERVIEW + AS_BUILT.
- [ ] 5. **Voice notes: audio upload + real ASR transcription.** Server: widen the upload whitelist to `audio/mpeg`, `audio/mp4` (m4a), `audio/wav` (sniffers exist `binary-types.ts:181-224`); new `gateway/transcription/` module — OpenAI-compatible `POST {base}/v1/audio/transcriptions` multipart client (default `https://api.openai.com`, model `whisper-1`; key = `OPENAI_API_KEY` via the existing `config/index.ts` precedence — credential config, not a feature flag); transcribe at upload-complete and persist a content-addressed sidecar `<hash>.txt` beside the blob (idempotent — re-upload of same bytes never re-calls the API); the task-1 threading injects the TRANSCRIPT text for audio attachments (keyless/failed → path + one-line "transcription unavailable — set OPENAI_API_KEY" note, graceful). Transcript rides the normal turn → scribe/memory ingestion for free (`scribeOnUserTurn`, `open/wiring/app-ws.ts`). Clients: audio accept + audio chip (🎵 precedent `app/features/docs/docs-shared.ts:35`). Tests: transcription client unit (mocked fetch, multipart fields, error taxonomy); upload→sidecar artifact-on-disk assertion; turn-injection includes transcript text; keyless graceful path. Docs per task 1.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
