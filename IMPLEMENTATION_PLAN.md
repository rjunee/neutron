# IMPLEMENTATION_PLAN — PR number on the card: durable #NNN tag, clickable, + title-opens-doc (owner-requested 2026-08-14)

Governing spec: this Plan card (parts 1-4, acceptance (a)-(h)). Re-verified against branch `trident/put-the-pr-number-on-the-card-a-cli` 2026-08-14 (commits 6a4fdcd, daf6ddf, 1d105e2):

- DONE on branch: migration 0122 (`work_board_items.pr` + `pr_url`), `detachRun(pr_info)` writing both columns in the same transaction as status/completed_at (both outcomes), `attachRun` clearing them so a hand-reopened card does not keep a finished run's PR, `trident/repo-web-url.ts` (pure `githubWebUrlFromRemote` parser + promise-memoized resolver + `makeRepoWebUrlCache` with sync `peek`), `buildBoardReconcileObserver` resolving `pr_url` best-effort, `pr`/`pr_url` on the item in the fan literal + `AppWsWorkBoardItem` + web client type + `parseWorkBoardItems`, AND live `pr`/`pr_url` on `RunProgress` end-to-end (server `deriveRunProgress`/`runProgressForItem` with `peekRepoWebUrl`, `AppWsRunProgress`, client `RunProgress` + `parseRunProgress`, one cache in `open/composer.ts` feeding both `withRunProgress` and `fanWorkBoardChanged`). Acceptance (c)'s durable half is guarded by a test that drives the REAL observer → `detachRun` → store re-read.
- STILL MISSING: `WorkBoardTab.tsx` is untouched — the meta line (~998) renders only step tag + round, the completed row (~782) renders only `Merged · date`, the `cwb-doc-link` element (~924-940) still exists, and the title button fires `onStartEdit` unconditionally. No `.cwb-pr` CSS. No doc-H1→card-title sync (`DocStore.onMutationSuccess` still feeds only `AnchorWalker.handle`). Existing `run_progress` literals in `__tests__/work-board-tab.test.tsx` predate T2 and lack the now-required `pr_url` field (typecheck fallout owned by T3, the task that touches that file).

- [x] **T1 — durable PR on the item, written by the real detach path.** Migration 0122 adds `pr INTEGER` + `pr_url TEXT` to `work_board_items` (+ regen `migrations/expected-schema.txt`). `WorkBoardItem` gains `pr`/`pr_url`; `detachRun` gains an optional `pr_info` and writes both columns in the SAME transaction as status/completed_at, for BOTH outcomes; `attachRun` clears them. New `trident/repo-web-url.ts` (pure parser + cached shell resolver). `buildBoardReconcileObserver` resolves `pr_url` (injectable, internal default, failure → null, never blocks reconcile) and passes `{pr, pr_url}`. `pr`/`pr_url` added to the fan literal, `AppWsWorkBoardItem`, and the web client type + `parseWorkBoardItems`. Acceptance (c) guard test drives the REAL observer → `detachRun` → store re-read.
- [x] **T2 — live `pr_url` on `run_progress`.** `RunProgress` (server `trident/run-progress.ts` + `AppWsRunProgress` + web mirror in `work-board-client.ts`) gained `pr_url: string | null`; `deriveRunProgress` takes an optional `repo_web_url` and composes `<url>/pull/<pr>`; `runProgressForItem` takes an optional sync `peekRepoWebUrl(repo_path)`. `makeRepoWebUrlCache` in `trident/repo-web-url.ts`: SYNC `peek` returning the cached url or null, kicking a background warm on first miss. ONE cache built in `open/composer.ts`, fed to BOTH sync attachment sites (`withRunProgress` via `repo_web_urls` opt, and `fanWorkBoardChanged`). First post-boot snapshot may render the tag as plain text until the warm lands (documented, acceptable).
- [x] **T3 — render the tag (parts 1-3 in `WorkBoardTab.tsx`).** Meta line: `#NNN` FIRST, then step tag, then round (`#265 Building round 1`); number = `run_progress?.pr ?? item.pr`; URL = `run_progress?.pr_url ?? item.pr_url`. `<a target="_blank" rel="noopener noreferrer">` when a URL resolved, plain `<span>` when not; NOTHING when no PR (no placeholder, no empty tag). Widen the meta-line gate from `tag !== null` to `tag !== null || pr !== null` so terminal/detached lanes show it too. Completed row: `Merged #265 · Aug 14` from durable `item.pr`/`item.pr_url`; a PR-less completed card renders today's `Merged · Aug 14` byte-identically. CSS `.cwb-pr` in `landing/chat-react.html` next to `.cwb-round`, muted, no blue. Add `pr_url: null` to the pre-T2 `run_progress` fixtures in `__tests__/work-board-tab.test.tsx` (typecheck). Mutation-resistant tests incl. exact `href`/`target`/`rel`, DOM order (#NNN before the step tag), the no-PR no-tag case, the plain-text no-URL case, and the completed-row render from durable item fields with NO `run_progress` attached (acceptance a, b, d + the completed-render half of c).
- [ ] **T4 — title opens the doc; the doc-link line dies.** Delete the `cwb-doc-link` element (~924-940). A card WITH a resolvable doc (`docLabel !== null` and `onOpenDoc` wired): title click calls `onOpenDoc` — NO inline rename on that card at all, and no new control anywhere in `cwb-actions`. A card WITHOUT a doc keeps today's `onStartEdit` rename exactly. Title carries NO link styling (no blue, no underline; cursor may change on hover only). Tests: doc-card click opens doc and never enters edit; docless card still renames; no rename affordance exists anywhere on a doc card (acceptance e, g, h).
- [ ] **T5 — doc H1 → card title sync (part 4 mechanics).** New gateway module hooked off `DocStore.onMutationSuccess` (COMPOSED with the existing `AnchorWalker.handle` at the wiring site, not replacing it): on a `write` to `plans/*.md`, parse the first `# ` H1 — H1 wins over frontmatter; no readable H1 → NO-OP on the card. Find the card whose `design_doc_ref` resolves to that path in `workBoardScopeKey(owner, project_id)`; update its title (sanitized) when different; rewrite frontmatter `title:` to match ONLY when different (fixpoint, no loop). The FILENAME never changes; sync is doc → card only. Tests drive the REAL `writeDoc` path and assert: card renamed, frontmatter rewritten, filename unchanged, `design_doc_ref` still resolves; an H1-less save leaves the title untouched (acceptance f + the fake-test guard).
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
