# IMPLEMENTATION_PLAN — A gateway restart must not kill an in-flight build: recover from the pushed branch instead of reaping the run

Governing spec: this Plan card (owner instruction 2026-08-14: "Gateway restarts should not kill a build. The build should recover."), plus the BLOCKING addendum of 2026-08-14 23:40Z (rebase + migration renumber + three-way column merge).

State verified against origin/main (ffba66f) and origin/trident/a-gateway-restart-must-not-kill-an (9e7dfbe) on 2026-08-15:

- Main moved 51d17b7 → ffba66f (4 commits: #277, #278, #279, #280) since the last plan. `git merge-tree --write-tree origin/main origin/trident/…` produces a CLEAN tree — the only shared file (`open/__tests__/open-wiring-substrates.test.ts`) is touched in different regions, and no new migration ordinals landed on main (highest is still 0122), so the branch's `0123` remains free. NO rebase is required for mergeability.
- BLOCKING addendum: fully discharged (migration renumbered to `0123_code_trident_runs_crash_recoveries.sql`; `expected-schema.txt` ends `… inner_checkpoint_head TEXT, inner_checkpoint_findings TEXT, crash_recoveries INTEGER) STRICT`; `trident/store.ts` derives `INSERT_PLACEHOLDERS` from `COLS` so 31/31/31 holds by construction, pinned in `trident/store.test.ts` — commit 1a8c51b).
- Core recovery (T1, commit 5b3be91): `trident/orchestrator.ts` §1a-crash (~line 1122) claims a crashed row atomically via `beginCrashRecovery` (wired `gateway/composition/build-core-modules.ts`) and relaunches it as a continuation (same branch/PR/checkpoint, rounds untouched); past `DEFAULT_MAX_CRASH_RECOVERIES` = 3 (orchestrator.ts:290) it fails with a reason naming the crash-recovery budget and embedding the latched crash reason (never the token "exhausted"); `trident/delivery.ts:174` classes `crash-recovery budget` as infra AHEAD of the "exhausted" matcher.
- LOAD-BEARING TRAP (governs any store change): `saveIfActive` vetoes non-crashed writes onto a `crashed`-latched row and re-latches on a tombstoned `workflow_run_id`; recovery clears the latch and nulls the generation ONLY via the atomic `beginCrashRecovery` claim — never via `update`/`save`/`saveIfActive`.
- T2 (commit 9e7dfbe): `open/wiring/substrates.ts` `onChildCrash` (line 429) now latches `inner workflow child crashed: ${detail} (observed <iso>; gateway process booted <iso>)` — measurements, never an asserted cause; T1's budget-cap reason embeds the latched `failure_reason`, so the boot timestamp survives onto the unrecoverable run's stored reason. Mutation-red tests in `open/__tests__/open-wiring-substrates.test.ts` and `trident/crash-recovery.test.ts`.

- [x] B0 — REBASE onto origin/main 51d17b7: branch sits directly on it; merge into current main HEAD ffba66f verified conflict-free via merge-tree; `bun test trident/` green on the branch.
- [x] B1 — RENUMBER the migration off main's taken `0122`: done as `migrations/0123_code_trident_runs_crash_recoveries.sql` (read off origin/main's highest ordinal, not "branch highest + 1"); `runner.test.ts` expects 123; 0123 still free on main at ffba66f.
- [x] B2 — THREE-WAY COLUMN MERGE, both sides survive: `expected-schema.txt` carries `inner_checkpoint_head`, `inner_checkpoint_findings` AND `crash_recoveries` (last, matching migration run order); `store.ts` COLS/INSERT/bound array all carry all three.
- [x] B3 — PIN THE 31/31/31 INVARIANT IN A TEST: `INSERT_PLACEHOLDERS` derived from `COLS` (store.ts:277); `store.test.ts` pins COLS count = PRAGMA count and a distinct-value round-trip that reds on any swap/short bound array (commit 1a8c51b).
- [x] T0 — Harvest-before-reap ordering (acceptance (c)): step() §1 tries `parseInnerResult` FIRST even on a crashed row (orchestrator.ts:1118); asserted in `trident/crash-before-launch-save.test.ts`.
- [x] T1 — RECOVER, DON'T REAP + BOUNDED DURABLE BUDGET (acceptance (a), (b), (c)-preserved, (e)): migration 0123 `crash_recoveries`; atomic `beginCrashRecovery` claim; §1a-crash continuation relaunch; budget-capped terminal reason (no "exhausted") embedding the latched crash reason; `delivery.ts` infra class; mutation-red tests incl. cap-holds-across-restart, board never reads `failed` mid-recovery, crashed-with-result harvests with zero fires (commit 5b3be91).
- [x] T2 — REPORT THE MEASURED CAUSE (acceptance (d)): `onChildCrash` composes the latched reason with observation time + gateway boot time (`Date.now() - process.uptime() * 1000`), phrased strictly as measurements; boot timestamp survives into the budget-capped terminal `failure_reason`; mutation-red tests on both seams (commit 9e7dfbe).
- [x] T3 — DOCS + INVARIANTS + FINAL VERIFICATION. Append the change to `docs/AS_BUILT.md` (newest-first — new entry at TOP, under the header); add `docs/INVARIANTS.md` entries (§9 Lifecycle & concurrency: harvest-before-reap ordering, budget-bounded durable crash recovery, recovery never consumes `round`/`ralph_round`; §3 Data layer: INSERT column/placeholder/bound-array counts pinned by construction + test) with `file:line` + protecting-test anchors; brief `docs/SYSTEM-OVERVIEW.md` touch beside the hang-watchdog bullet (~line 3364) describing launcher-crash recovery. Re-run `bun test trident/` + `bun test open/__tests__/open-wiring-substrates.test.ts` green. Do NOT push and do NOT run `gh` — the outer loop reuses PR #267; mergeability against ffba66f already verified via merge-tree.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.

RESUME: the durable outer loop already owns PR #267 and branch trident/a-gateway-restart-must-not-kill-an. Commit to that SAME local branch. Do NOT push and do NOT run `gh`; the outer loop reuses the PR.
