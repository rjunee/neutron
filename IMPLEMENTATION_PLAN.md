# IMPLEMENTATION_PLAN.md — codex build bridge: the brief must ARRIVE, the run id must correlate

Card: "The codex build bridge loses the brief — chunked parts are written but the PARTS env never arrives, and the run id gets mistyped."

Planner survey (2026-08-17, read-only; branch `trident/the-codex-build-bridge-loses-the-br` @ 01100526 = 608ecce4 + the arrival proof (8233f8c5) + the PARTS_FILE wrapper input (01100526); PR #313 OPEN): the world moved under this branch. Main landed #321 ("make brief parts authoritative", 82ebb5b2) which REWROTE the same region of `trident/codex-build.sh` this branch touches: parts mode now requires `NEUTRON_CODEX_BUILD_BRIEF_PART_INTEGRITY` (aligned per-part `<bytes>:<fnv32>` receipts, count-checked, `CODEX_BUILD_BRIEF_PART_CORRUPT` on mismatch) and the whole-brief `NEUTRON_CODEX_BUILD_BRIEF_INTEGRITY` is checked ONLY on the non-parts fallback — `inner-workflow.mjs` (~1615) emits `PARTS` + `PART_INTEGRITY` and NO whole-brief receipt in parts mode. A sibling lane then arbitrated the collision: `origin/trident/313-and-main-both-built-brief-part` @ 903428b4 (PR #377, OPEN, merged with origin/main 2026-08-17 19:50) carries this card's entire net change RE-BASED onto the per-part contract — the arbitrated wrapper (PARTS_FILE resolves into `$BRIEF_PARTS` UPSTREAM of the receipt gate, in-file ARBITRATION comment at `codex-build.sh:764`), the adapted 86-case `codex-build.test.ts` (7 PARTS_FILE cases + a 7-case inline-vs-manifest symmetry suite), and a byte-identical copy of the arrival test. Its commits do NOT share ancestry with this branch (replayed; merge-base is still 608ecce4), and it never pushed to this branch as its own plan intended. TWO HOLES REMAIN, and one is live on PR #377: (1) the arrival suite was never RUN against the arbitrated emission — its assertion (d) still greps `NEUTRON_CODEX_BUILD_BRIEF_INTEGRITY='…'` from the prompt (arrival test line 334), which parts mode no longer emits, so the card's headline proof is red on the branch that supersedes it; (2) acceptance criteria 3 and 4 are built NOWHERE — no wrapper names the "parts exist on disk but PARTS was not passed" condition (the incident still produces the generic `CODEX_BUILD_NO_BRIEF`), and the bridge agent still retypes a ~2 KB run command carrying the run id in ~8 artifact paths. Refusal strictness is unchanged everywhere, as the card demands.

- [x] Launcher writes the brief parts by path (`trident/brief-parts.ts` `writeBriefParts`, run-id-sanitized filenames, integrity receipts) — shipped in #261, covered by `brief-parts.test.ts`
- [x] Manifest threaded into the workflow args (`trident/inner-loop.ts` `buildWorkflowArgs` `briefParts`, omitted when absent) — covered by `inner-workflow-assembly.test.ts`
- [x] Workflow emits `NEUTRON_CODEX_BUILD_BRIEF_PARTS` on the run command; since #321 it also emits aligned per-part receipts (`NEUTRON_CODEX_BUILD_BRIEF_PART_INTEGRITY`) and drops the whole-brief receipt in parts mode; malformed manifest still falls back byte-identical to full chunking
- [x] Wrapper assembles parts in order and fails closed — `CODEX_BUILD_BRIEF_PART_MISSING`, and since #321 per-part `CODEX_BUILD_BRIEF_PART_CORRUPT` + receipt-count enforcement — covered by `codex-build.test.ts`
- [x] Prompt→receipt lockstep: emitted chunk blocks run through real bash assemble to the prompt's own receipt — 2026-08-14 AS_BUILT entry
- [x] Run id is a string end-to-end on the launcher path (`TridentRun.id: string` → `buildWorkflowArgs.runId` → part filenames) — no numeric mistype found; typed at `trident/brief-parts.ts:71`
- [x] ARRIVAL proof for the codex build dispatch (`trident/codex-build-arrival.test.ts`, commit 8233f8c5): workflow-EMITTED chunk blocks + run command executed with real bash against the REAL wrapper, stub codex, exec seam dumping the CHILD's env + stdin; >30 KB brief; run-id-keyed artifact correlation; stripped-PARTS negative control refused at exit 3 before the seam runs — written pre-#321; its receipt assertion is re-based by the next task
- [x] Wrapper accepts the part list BY PATH — `NEUTRON_CODEX_BUILD_BRIEF_PARTS_FILE` manifest, inline wins, fail-closed `CODEX_BUILD_BRIEF_PARTS_FILE_MISSING` (commit 01100526); arbitrated with #321 by lane #377 into transport-only (manifest resolves into `$BRIEF_PARTS` upstream of the one receipt gate)
- [ ] ABSORB THE ARBITRATION AND MAKE THE ARRIVAL PROOF TRUE AGAIN (top): merge `origin/trident/313-and-main-both-built-brief-part` @ 903428b4 into this branch taking the arbitrated side for `trident/codex-build.sh` and `trident/codex-build.test.ts`; rewrite arrival assertion (d) to measure the child's stdin against the run command's OWN `NEUTRON_CODEX_BUILD_BRIEF_PART_INTEGRITY` receipts (whole-brief receipt provably absent from the command); all five suites green; push without force. This makes PR #313 and PR #377 converge instead of collide — whichever merges first leaves the other trivial.
- [ ] Criterion 3 — the orphaned-parts refusal names the incident: when `BRIEF_FILE` is set but missing/empty and NO parts input arrived, `trident/codex-build.sh` globs `${BRIEF_FILE}.a*`; any hit refuses with a NEW specific marker (e.g. `CODEX_BUILD_BRIEF_PARTS_ORPHANED: brief parts exist on disk beside <file> but NEUTRON_CODEX_BUILD_BRIEF_PARTS was not passed`), still exit 3 / DEFERRED, generic `CODEX_BUILD_NO_BRIEF` retained when no parts exist. Update the arrival negative control to assert the NEW marker (it reconstructs the 76c548cd incident exactly: a1/a2 on disk, PARTS stripped) and add `codex-build.test.ts` cases for both the named and generic branches. Refusal strictness unchanged.
- [ ] Criterion 4 — the bridge command stops being a retyping exercise: the LAUNCHER (fire time, `trident/inner-loop.ts` + a new launcher-written artifact beside `writeBriefParts`) writes a run-scoped LAUNCH SCRIPT embedding the run id, `CODEX_HOME`, branch/base/mergeMode, the wrapper path, checkpoint env, and the parts MANIFEST path (delivered via the already-landed `NEUTRON_CODEX_BUILD_BRIEF_PARTS_FILE`); `codexBuildPrompt` emits a SHORT fixed run command invoking that script with only round-scoped arguments (round number + the two workflow-composed a1/a2 receipts); every artifact path is derived INSIDE the script from its embedded run id, so a mistyped id is impossible (a mistyped script path fails loudly as file-not-found before anything runs); chunked fallback preserved for callers without a manifest. Pin it arrival-style: the emitted run command contains no multi-kilobyte literal and names the run id at most once, the child's artifacts land at ids derived not retyped, and existing refusals still hold.
