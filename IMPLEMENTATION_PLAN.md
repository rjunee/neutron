# IMPLEMENTATION_PLAN.md — Executor-Mode Reminders (P0-1 M2 PARITY, Neutron Open)

Spec of record: `docs/plans/executor-mode-reminders-2026-07-20.md` — the DEEPENED header block (lines 1-150) SUPERSEDES the original plan below it wherever they conflict. Honor the three Ryan overturns (SPEC Decisions Log 2026-07-20, neutron-managed): (1) Bash ports via approval, not exclusion; (2) dreaming's uncovered half goes into CORE MEMORY (split by tier: backlink repair = event-driven on sync hook; correction-promotion = reflect pass; daily-delta = stays a ritual); (3) ritual registration is AGENT-CALLABLE with in-chat approval — the approval RENDERING carries the security. NO FEATURE FLAGS — build the real default directly. One task per iteration; commit + PR every iteration.

Planner corrections (re-verified 2026-07-21, blocker-fix resume session):
- Branch `trident/executor-mode-reminders` (PR #426) is LIVE at `5e4c6672` with 21 commits, tasks 0-5 committed incl. task-5 Argus r1 (`6d399c2a`, `49c0988b`) + r2 (`6059a3b3`, `5e4c6672`) fixes. CONTINUE on it; the local worktree was cleaned — re-checkout from origin.
- Task 5 drew REQUEST_CHANGES on Argus round 3 (codex-confirmed major): `reminders/tick.ts:231` wraps the WHOLE `ritual_executor.fire(reminder)` in `fireAndForget('ritual-fire', …)`, detaching validation + spawn + the durable `code_ritual_runs` insertRunning from the tick body — so `stop()` (tick.ts:135-137 → SupervisedLoop quiesce, `loop/index.ts` stop awaits `inflight`) can resolve BEFORE the run row exists: a consumed #319 claim with NO durable record = data-loss window on shutdown/crash. Verified fix shape: fire() ALREADY detaches only the long substrate turn internally (`ritual-executor.ts:481` `fireAndForget('ritual-run', …)`) and resolves right after insertRunning + the registry running-flip — so the tick must simply AWAIT fire(); no executor restructuring needed. Task 5R below carries the fix + two same-area minors. Task 5's checked state stands for its landed scope; 5R is the review-closure task.
- Task 3 design LOCKED (prior pass): **no new table, no migration** — durable grants are `tool_approvals` rows (migration-0004 DDL verified `0004_gateway_core.sql:66-79`) with namespaced `tool_name` (`ritual:<id>` / `ritual-egress:<id>`), `args_json.content_hash`, `status='approved'`, `decided_by`/`decided_at` = approved_by/approved_at. The pending→approved/denied/expired machine + notifier seam are reused verbatim (the "extend, never parallel" instruction).
- Layering is FORCED: `.dependency-cruiser.cjs` `platform-stays-low` (severity error) forbids `tools/` (platform) → `reminders/` (services). The ritual layer lives at `reminders/ritual-approval.ts` importing `@neutronai/tools` (legal services→platform edge).
- Cadence arm CHOSEN: cadence is a hash input → cadence change invalidates the grant (re-approval). No hard floor / daily fire cap in v1.
- `ApprovalManager` is composed at `gateway/composition/build-core-modules.ts:275-278`; task 3's ritual request path is its first real production caller.

- [x] 0. Design doc deepened + Ryan review complete (plan doc STATUS: COMPLETE; three overturns locked in neutron-managed SPEC Decisions Log 2026-07-20; four-review findings folded into the header block)
- [x] 1. **M2-3 / P0-4 — collapse `NEUTRON_PERFECT_RECALL`; memory consolidation 6h ON by default.** Landed: `runtime/perfect-recall-flag.ts` + test deleted; all four gate sites in `open/wiring/memory.ts` un-gated; `WiredMemory` types tightened; scribe `supersede` option removed; `DEFAULT_REFLECT_INTERVAL_MS` 24h→6h; `nexus-emit.ts` re-export deleted; 8 flag-referencing test files + docs updated; §7.2 merge-safety gate (Argus r1 BLOCKER) fixed in `58933d1f`. AS_BUILT + SYSTEM-OVERVIEW committed (`d60d57ea`).
- [x] 2. **Ritual schema + registry module.** Migration **0106**: nullable `ritual_id` TEXT on `reminders`; `code_ritual_runs` run-history table (status vocab + `skip_reason` CHECK-coupled; `subagent_run_id`/`content_hash`/`failure_reason` + indexes); `agent_kind` CHECK widened to `'ritual'` + `AgentKind` widened. `reminders/rituals.ts`: `RitualDef` + `createRitualRegistry` + async `validateRitualFire` with REQUIRED `RitualApprovalCheck` seam (fail closed) + `MAX_RITUAL_PROMPT_BYTES`. Fire-time: unknown/missing-prompt/unapproved → log + SKIP. `ritual_id` read-through in `reminders/store.ts`. Argus r2+r3 hardening included.
- [x] 3. **Approval-gate infrastructure (content-hash bound).** `tools/approval.ts` `findApproved`; `reminders/ritual-approval.ts` with `computeRitualContentHash` (sha256 over canonical array of prompt ‖ sorted surface ‖ scope ‖ cadence ‖ tier ‖ timeout), `ritualCadenceString`, `requestRitualApproval` (egress:'web' mints a SECOND `ritual-egress:<id>` grant bound to the same hash), `createRitualApprovalCheck` (hash recomputed from LIVE prompt bytes every fire; db errors propagate → fail-close). Composer no-op `approval_notifier` swapped for `buildAppWsApprovalNotifier` (`open/wiring/approval-notifier.ts`). NO auto-approval anywhere.
- [x] 4. **Executor dispatch branch in the TICK + ritual executor + cc-ritual substrate + ritual lane + `code_ritual_runs` writer.** Landed: ritual lane split (`MAX_CONCURRENT_RITUALS=2`); `DispatchTurnInput.tools?` stub-ToolDef mapping; `PROFILE_RITUAL` byte-identity; `append_system_prompt_file` threading; `reminders/ritual-agent-base.md`; `makeRitualSubstrate` (`cc-ritual-*`, ephemeral); `reminders/ritual-runs.ts` sole writer; `reminders/ritual-executor.ts` (`fire` — fail-closed validate → durable skip / lane spawn / 'running' row / detached turn → terminal bookkeeping; NEVER throws, never awaits the turn); tick executor branch; composition wiring (`ritual_executor_factory`, llmPool-gated, registry rooted `<owner_home>/rituals`, ZERO defs until task 7). Full test set + depcruise clean.
- [x] 5. **Completion delivery + failure surfacing.** Terminal event → final text through the one `deliver()` seam to the resolved topic; `silent` skips success post; failed/timed-out/crashed → one-line failure notice; 3-consecutive-failure escalation (once per streak, cancel-aware re-arm per Argus r2); boot-reap orphans → `crashed` + notice (sync-SELECT-during-compose ordering); 30d retention prune. T3 + T6 tests. Argus r1+r2 fix rounds committed (`6d399c2a`…`5e4c6672`).
- [x] 5R. **CLOSE task-5 REQUEST_CHANGES (Argus r3 major + 2 minors) — ritual startup joins the tick quiescence boundary.** (1) MAJOR: `reminders/tick.ts:231` — replace `fireAndForget('ritual-fire', this.ritual_executor.fire(reminder))` with `await this.ritual_executor.fire(reminder)` so claim → validate → durable `code_ritual_runs` insert completes INSIDE the tick body (= inside SupervisedLoop's stop() quiescence await); ONLY the substrate turn stays detached (already internal at `ritual-executor.ts:481`). Drop the now-unused `fireAndForget` import (tick.ts:19); update the stale fire-and-forget comment block (tick.ts:226-227) + executor header line 5. Rework the invalidated old-contract test `tick.test.ts:646` ("runOnce RESOLVES while the executor turn is still pending") to assert the NEW contract; ADD the quiescence regression test: un-awaited `runOnce()` + immediate `await stop()` must leave exactly one durable 'running' row (REAL executor + real run store, never-settling turn) — never zero rows. (2) minor a: executor `postNotice` (ritual-executor.ts:169-183) honors spec §267 — post() accepted-boolean false → ONE retry → logged failure notice ('ritual_notice_post_not_persisted'); `gateway/http/deliver.ts` is NOT edited (its `{persisted:false}` reply contract is correct; the consumer honors it). (3) minor b: drop the executor-side `.slice(0, 160)` at ritual-executor.ts:278 — `formatRitualFailureNotice` (ritual-delivery.ts:60-63, MAX_REASON_CHARS) owns collapse+truncation; keep the :297 4000-char DB failure_reason cap (different concern). Tests: reworked contract test + quiescence regression + postNotice retry (false,false → 2 calls + log; false,true → 2 calls, no error) + collapsed-then-capped long-reason notice. `bun test` reminders/ + gateway/ green; AS_BUILT entry.
- [ ] 6. **T5 write-containment spike — HARD GATE for every writing/Bash ritual (incl. all ported Vajra rituals per overturn 1).** Prove a per-session settings.json deny rule fails CLOSED (run terminates; outside write ABSENT; no wedge) in the headless PTY, ACCOUNTING for the substrate auto-approver (`runtime/adapters/claude-code/persistent/spawn.ts:239-245`, `signatures.ts:89-90` incl. `runthiscommand`) — the ritual REPL must disable that scanner or the test is theater. Record the verdict in the plan doc. If fail-closed is unprovable → OS-level sandbox becomes its own prerequisite sprint and Bash/writing rituals stay gated.
- [ ] 7. **Bundled generic read-only example rituals** (ENGINE seeds — Ryan's specific ritual content is USER DATA via his Vajra import, never hardcoded): generic morning-brief + evening-wrap templates, surface `['Read','Glob','Grep']`, no Bash — Layer 1 (`--tools` default-deny, verified `build-repl-argv.ts:52-60`) alone contains these. Bundled defs register but stay UNAPPROVED until the owner's explicit task-8 act. T7 per-ritual acceptance test each (output must reference real fixture state — ported-prompt silent-no-op guard).
- [ ] 8. **Q3 — agent-callable registration with in-chat approval.** Reminders-core MCP tools to propose/register; approval via ButtonStore persisted option-set binding (`onboarding/interview/button-backed-answer.ts:198` precedent — eligibility only from `prior_agent_options`, `:207-209`): opaque proposal_id; approval resolves through task 3's `respondApproval`; no self-approval (`resolution_speaker_user_id`, `channels/button-store.ts:106`); render preformatted (never Markdown), never truncate (refuse over hard cap), NFC-normalize, reject bidi/zero-width, itemize URLs/paths/skills; describe CAPABILITY not tool names; no register-and-fire in one turn; no surface-widening without re-approval. Test T8: owner replies something UNRELATED → ritual stays pending_approval, never dispatches.
- [ ] 9. **Q2 — dreaming's uncovered half into core memory** (overturn 2, split by tier): (a) deterministic entity backlink repair EVENT-DRIVEN on the entity-writer sync hook (`runtime/entity-writer.ts` EntityWriteOutput `newLinks`/`removedLinks`); (b) correction-pattern promotion folded into `runReflectPass` (the 6h loop task 1 turned on); (c) daily-delta notes as a bundled ritual. Explicit toHaveBeenCalled/artifact assertions each.
- [ ] 10. **Docs + close-out.** `docs/AS_BUILT.md` entries per landed task; `docs/SYSTEM-OVERVIEW.md` new "Ritual executor" subsystem section (+ reminders section update, consolidation-cadence update); `reminders/AGENTS.md` refresh; final summary citing the executor branch file:line + approval-rendering file:line + T5 verdict.
