# IMPLEMENTATION_PLAN — Host-deploy approval must be usable where it lands (Plan card 2026-08-15)

Governing spec: this Plan card. Verified against branch `trident/the-deploy-approval-is-unusable-whe` (T1 = `3384c23`, T2 = `1634814`):

- T1 LANDED AND VERIFIED: `host_deploy_request` reads `ctx.topic_id` (`gateway/wiring/host-deploy-tool.ts`), `HostDeployService.request({ ref, topic_id })` resolves `approval_topic = topic_id ?? approval_topic_id` and uses it for BOTH the `tool_approvals` row and the emit; `pending_approval` carries `approval_topic_id` + a fallback `note`. Service/tool tests assert both seams independently.
- T2 LANDED AND VERIFIED: a dead-grant tap re-raises via claim-gated `cancelPending(id)` + `request({ ref, topic_id: input.topic_id })` (`reraise()`, `open/host-deploy.ts:741`); the swept-`expired` branch dedupes through `pendingGrantForRef()` so repeat taps point at the waiting prompt instead of minting one per tap; `up_to_date`/`refused`/`unavailable` re-raise outcomes read as one sentence each; `dispatch` fires zero times on any re-raise path. TTL stays 5 min — with the prompt now landing where the owner is and a stale tap one tap from recovery, the window fits the delivery (judgment call, noted in the T2 commit).
- MEASURED FACTS FOR T3 (next): the body's last line (`renderHostDeployApprovalBody`, `open/host-deploy.ts:459`) still says "Typing anything else will NOT approve this deploy" and the `hdp:<token>:a|d` strings exist ONLY in `button_prompts.options_json` — no surface renders them. `request()` mints `approval_id` at `:642` and builds both option values at `:668-679`, three lines below the render call at `:682` — the token merely needs computing once and passing into both. Typed text reaches `handleOwnerButtonAnswer` through the same capture seam as a tap (`user_text` trim + exact-match + `prior_option_values` membership), so the printed string IS the working string within the eligibility window. No existing test asserts the old last line.
- The four-prompt eviction seam is self-documented at `open/host-deploy.ts:788-797` — an evicted token returns null and falls through to the LLM (T4).
- `ApprovalManager.expireStale()` (`tools/approval.ts:187`) has no production caller; `open/wiring/approval-notifier.ts:69-83` pushes ONE `agent_message` frame to `registry.topics()` with no retraction; `landing/chat-react/controller.ts` appends `this.notices` after the durable transcript unconditionally with no dismissal path (T5/T6, measured).

- [x] **T1 — raise the Approve/Deny prompt on the topic that requested the deploy; the tool result names the destination.** DONE (`3384c23`): a request from `app:owner:<project>` puts its prompt on that topic; grant row topic and emit topic asserted independently; no-topic callers fall back with a `note` naming the destination; the tool description tells the agent to always say WHERE the button is.
- [x] **T2 — a dead-grant tap re-raises a fresh approval on the tap's topic instead of dead-ending.** DONE (`1634814`): TTL-expired and stale-sha taps claim the row via `cancelPending` (exactly-once, race-safe) and re-raise on `input.topic_id`; the swept-`expired` branch re-raises unless a pending grant for the same ref already waits (`pendingGrantForRef` dedupe); never an auto-approve — the fresh grant needs its own tap and `dispatch` fires zero times on any re-raise path.
- [x] **T3 — print the typed fallback IN the approval body.** Compute the two option values (`hdp:<token>:a` / `hdp:<token>:d`) ONCE in `request()` and pass them into BOTH the `ButtonOption[]` and `renderHostDeployApprovalBody` (new required fields `approve_value`/`deny_value`), replacing the `:459` dead-end line with the two exact strings that DO work, each rendered as inline code (token alphabet is base64url — no backtick can escape). One source for both means the printed string can never drift from the tappable one. Assert body⊇both option values at the service seam, and drive an approval end-to-end from a token EXTRACTED FROM THE RENDERED BODY.
- [ ] **T4 — an evicted prompt's tap must say so.** In `handleOwnerButtonAnswer`, when `HOST_DEPLOY_VALUE_RE` matches but the value is NOT in `prior_option_values`, return a sentence explaining the prompt aged out of the answer window and (per T2's guard) re-raise or point at the waiting prompt — never return null and let a raw `hdp:` token fall through to the LLM as unexplained text. Closes the seam the code documents at `host-deploy.ts:788-797`. NOTE the forged-token interaction: eligibility must still refuse tokens never offered — an unknown-row token gets the existing "not recognized" sentence, and cross-service tokens (`rap:`) stay null.
- [ ] **T5 — `expireStale()` gets a real production caller, and a swept grant retires its UNTAPPED button prompt.** Wire a periodic sweep (loop-registry registered, alongside the approval module init in `gateway/composition/build-core-modules.ts` or the reminders tick) so a pending `tool_approvals` row past TTL transitions to `expired` without waiting for a tap. Link grant → prompt (return deliver's `prompt_id` through `HostDeployEmit` and persist the linkage) so the sweep can resolve the orphaned `button_prompts` row with a sentinel and stamp spent-ness (`AppWsAdapter.recordPromptChoice` fans `prompt_resolved` + stamps `chosen_value`) — a dead grant must not leave a live-looking button. The notifier must not announce a grant already past its TTL; an expired grant is distinguishable from an unanswered one in what the owner is told.
- [ ] **T6 — the approval banner must be dismissible and must not outlive its grant.** `open/wiring/approval-notifier.ts` sends a retraction frame (or the banner derives from the row) when a grant is decided/expired; `landing/chat-react/controller.ts` gives ephemeral notices a dismissal path and stops pinning them below messages that arrived after them (arrival-order sort, no unconditional tail). Assert: send a notice, then a user message, and the notice does NOT render last; a decided/expired grant's banner disappears without a page reload.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
