# IMPLEMENTATION_PLAN.md — M2 P0 parity: input modalities (documents + voice) + minimal chat commands (Neutron Open)

Supersedes the executor-mode-reminders plan (landed via the PR #426 lineage; any residue is tracked in `docs/plans/executor-mode-reminders-2026-07-20.md`, not here). Scope source: `docs/research/vajra-neutron-parity-audit-2026-07-19.md` blocker 3 + Ryan 2026-07-21 ("only the chat commands that make sense for Neutron" → `/status` + `/reset` ONLY; the Telegram topic-lifecycle vocabulary is NOT ported). NO FEATURE FLAGS — build the real default. One task per Ralph iteration; main is protected — new worktree + branch + PR each iteration; update `docs/AS_BUILT.md` + `docs/SYSTEM-OVERVIEW.md` inside each task's PR.

Planner ground-truth (re-verified 2026-07-21 against HEAD 63fe4119 — every claim file:line):
- The audit UNDERSTATES the modality gap: attachments (INCLUDING images) never reach the agent. `open/wiring/app-ws.ts:756-770` reads `adapter_metadata.attachments` then drops them — the comment admits "Full attachment content isn't yet threaded into `LiveAgentTurnRequest` — its interface carries only `user_text`"; an attachment-only send dispatches the placeholder "Sent an attachment.". `gateway/wiring/build-live-agent-turn.ts` has zero attachment handling (grep-zero). Task 1 builds the attachment→turn threading as the foundation, then widens accepted types.
- Server-side type machinery already EXISTS: `gateway/storage/binary-types.ts` magic-byte sniffs `%PDF-` (:184-193) and audio RIFF/ID3/ftyp (:181-224); `BINARY_MIME_WHITELIST` (:47-58) already lists `application/pdf` + `audio/{mpeg,mp4,wav}`. Only `gateway/http/app-upload-surface.ts:66-71` (`IMAGE_MIME_WHITELIST`) restricts chat uploads; docs were punted there as "a P7 docs routing concern" (:22-24) — a deferral, not a recorded drop decision.
- No ASR/transcription code exists anywhere (grep `whisper|transcrib` → only unrelated hits: `app/features/docs/docs-shared.ts:35` file-icon, `runtime/substrate.ts:4` comment). An OpenAI credential path exists: `OPENAI_API_KEY` via `resolveOpenOpenAiPool` (`open/composer.ts:461-474`) + `config/index.ts:244,267-269,447-449`.
- Chat-command chain: ONE composer-level chain at `open/composer.ts:1403-1406` — `[coresWiring.chatCommandFilter, buildSkillForgeChatCommandFilter(...)]`, where the cores inner chain is `gateway/cores/mount-open-cores.ts:341-364` (`/cal /email /remind /research`); shared by web AND app-ws (comment :1396-1402). Builders live in `gateway/boot-chat-command-filters.ts`; the contract is `contracts/chat-command-filter.ts:25-44`. NO `/status` or `/reset` chat handler exists anywhere (grep-zero; `gateway/http/app-connect-auth.ts:151` '/status' is an unrelated HTTP sub-route). `gateway/composition.ts` chains no filters.
- Clients: web file input `landing/chat-react/ChatApp.tsx:1362-1369` (accept = images, +zip during import), allow-list `landing/chat-react/uploads.ts:35-41`, image-only bubble renderer `ChatApp.tsx:58-98`. Expo picker is ALREADY `type:'*/*'` (`app/components/ChatSyncSurface.tsx:222-226`); `app/lib/upload-client.ts:499-502` maps image mimes only.
- Project archiving already exists (`gateway/projects/sqlite-store.ts`, `gateway/http/app-projects-surface.ts`, SYSTEM-OVERVIEW.md:2002) — do NOT rebuild; explicitly out of scope.

- [x] 1. **Attachment→agent threading + document (PDF) attachments end-to-end.** Server: widen `gateway/http/app-upload-surface.ts` (whitelist / `EXT_FROM_MIME` / `URL_PATH_RE` / `mimeFromExt`) to add `application/pdf`; export ONE URL→local-path resolver helper. Thread the attachments read at `open/wiring/app-ws.ts:763` into the `appWsChatTurn` call; extend `gateway/wiring/build-live-agent-turn.ts` turn input with `attachments` + an injected `resolveAttachment` seam (composer supplies it over `owner_home`); inject a `<user_attachments>` fragment (resolved local paths under `<owner_home>/chat-attachments/`) into the PROMPT on warm (:874-888) AND cold (:893-903 `composeFirstTurnPrompt`) branches — never mutate `turn.user_text` (it feeds capture/reflection/scribe/persistence). The CC REPL Reads images AND PDFs natively from local paths — this fixes IMAGES too. Clients: web accept + non-image chip render; Expo mime→ext + pdf. Tests: upload-surface PDF accept/spoof/serve; composer-level e2e asserting the dispatched `spec.prompt` embeds the resolved path (artifact assertion, not bookkeeping); build-live-agent-turn attachments unit (warm/cold/unresolvable). Docs: AS_BUILT + SYSTEM-OVERVIEW chat-attachments coverage.
- [ ] 2. **Voice notes: audio upload + real ASR transcription.** Server: widen the upload whitelist to `audio/mpeg`, `audio/mp4` (m4a), `audio/wav` (sniffers exist `binary-types.ts:181-224`); new `gateway/transcription/` module — OpenAI-compatible `POST {base}/v1/audio/transcriptions` multipart client (default `https://api.openai.com`, model `whisper-1`; key = `OPENAI_API_KEY` via the existing `config/index.ts` precedence — credential config, not a feature flag); transcribe at upload-complete and persist a content-addressed sidecar `<hash>.txt` beside the blob (idempotent — re-upload of same bytes never re-calls the API); the task-1 threading injects the TRANSCRIPT text for audio attachments (keyless/failed → path + one-line "transcription unavailable — set OPENAI_API_KEY" note, graceful). Transcript rides the normal turn → scribe/memory ingestion for free (`scribeOnUserTurn`, `open/wiring/app-ws.ts`). Clients: audio accept + audio chip (🎵 precedent `app/features/docs/docs-shared.ts:35`). Tests: transcription client unit (mocked fetch, multipart fields, error taxonomy); upload→sidecar artifact-on-disk assertion; turn-injection includes transcript text; keyless graceful path. Docs per task 1.
- [ ] 3. **`/status` + `/reset` chat commands (narrow re-map — NOT the Vajra topic-lifecycle set).** New builder(s) in `gateway/boot-chat-command-filters.ts` implementing `contracts/chat-command-filter.ts:25-44` (lazy-import pattern like siblings; exact-command boundary — `/statusfoo` falls through null, K8 grammar precedent `boot-chat-command-filters.ts:147-159`), chained at `open/composer.ts:1403` so web + app-ws compose identically — no second command-parsing path. `/status`: deterministic snapshot from injected thunks — active project (projects store), current model (`getBestModel`), pending reminders count (reminders backend `mount-open-cores.ts:283-287`), active work-board items (`workBoardStore.listActive`, composer :3112-3115), active trident runs. `/reset`: respawn the CURRENT project's warm REPL session (fresh agent context; durable chat history stays) via the `respawnSupervisedSession` machinery (`runtime/adapters/claude-code/persistent/supervision.ts:59`; sessions keyed off the `metering_context.project_id` fold — `build-live-agent-turn.ts:970-974`, topic shape `app:<owner>:<project_id>` — `open/wiring/app-ws.ts:783-795`) — resolve the exact registry-key derivation from the persistent substrate BEFORE coding the actuation. Tests: filter units — status text composes from thunks; `/reset` invokes the respawn actuation `toHaveBeenCalledWith` the right session key; non-command fallthrough returns null. Docs: SYSTEM-OVERVIEW chat-commands list + AS_BUILT.
