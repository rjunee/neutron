# IMPLEMENTATION_PLAN — Credentials orphaned by a changed owner handle: migrate at boot, and stop calling it "not connected" (card 2026-08-14)

Governing spec: this Plan card (owner-approved design 2026-08-14 — unambiguous ⇒ auto-migrate + audit; ambiguous ⇒ touch nothing, surface `orphaned` + a migrate action; NEVER refuse boot). Verified against main 2026-08-14; T1 @ 30a65ed, T2 @ c936f85, T3 @ 7ca9005 on `trident/credentials-orphaned-by-a-changed-o`. Planner re-verified 7ca9005 in a clean worktree: all five card test files green (28 pass / 0 fail), `bun x tsc --noEmit` exit 0, broad `bun test gateway/cores gateway/__tests__ auth` = 1995 pass / 0 fail.

- The #451 rename reconciler (`migrations/scope-rekey.ts`, wired at `gateway/index.ts:283-342`) already sweeps `secrets.project_slug` and `project_credentials.owner_slug` — but ONLY when the `instance_scope_ledger` disagrees with the boot slug. On this box the ledger was seeded as the boot slug at first boot, so rows written pre-provisioning under `dev` are invisible to it forever. This card closes that gap.
- `SHARED_KEY_ENCRYPTED_TABLES` (`auth/secrets-store.ts:79`) = `['secrets', 'project_credentials']`; scope columns are `secrets.project_slug` and `project_credentials.owner_slug`. `api_keys.project_slug` is the metadata twin the BYO-key read path keys off — swept as a companion table.
- Collision keys (never-clobber guard): `secrets` UNIQUE(project_slug, kind, label) — migration 0009; `project_credentials` UNIQUE(owner_slug, project_id, service) — migration 0092; `api_keys` UNIQUE(project_slug, provider, label) — migration 0009.
- Audit precedent: boot journals `credential_scope_migrated` / `credential_scope_orphaned` through `SystemEventsStore` (`persistence/system-events.ts`).

- [x] (pre-existing half) 2026-05-12 rename-canonicalisation fix — the store keys on the FROZEN handle, branded `OwnerHandle` at the type level (`auth/secrets-store.ts` header, `PutInput`/`GetInput`). Prevents a RENAME orphaning rows; does nothing for rows frozen under a pre-provisioning handle.
- [x] (pre-existing half) #451 boot scope reconciler + ledger (`migrations/scope-rekey.ts`, migration 0114) — repairs the ledger-disagrees case only; correct as-is, not touched by this card.
- [x] **T1 — boot-time credential-scope reconciler (the core of the card).** DONE @ 30a65ed: `auth/credential-scope-reconcile.ts` (census across `SHARED_KEY_ENCRYPTED_TABLES` + `api_keys` companion; UNAMBIGUOUS ⇒ one-transaction metadata move, ciphertext untouched; AMBIGUOUS ⇒ zero writes + orphan counts), wired into `gateway/index.ts` after the #451 block inside a never-fail-boot try/catch, journaling `credential_scope_migrated` / `credential_scope_orphaned`. Tests: `auth/__tests__/credential-scope-reconcile.test.ts` + `gateway/__tests__/boot-credential-scope.test.ts` pin acceptance (a), (a2 rotation hazard, asserted on the decrypted value), (a3 real `boot()` never refuses), (c both tables + api_keys), (d no secret material), and the delete-the-migration mutation guard.
- [x] **T2 — a miss is DISTINGUISHABLE (acceptance b).** DONE @ c936f85: `buildIntegrationsStatus` (`gateway/cores/integrations.ts`) gained `orphaned_credentials` summary (per-table/per-handle counts via read-only `censusCredentialScope` + `listOrphanedSecretSlots`; no decrypt) and per-slot `orphaned: true` ("scoped to a previous handle", never bare `connected:false`); `MIGRATE_ORPHANED_ACTION` const names the way out. `db` threaded through both call sites. Tests: `gateway/cores/__tests__/integrations-orphaned.test.ts` (orphan reported, positive control connected under correct handle, genuinely-unconnected control, rotation hazard from the read side, `project_credentials` in summary, no plaintext in serialized status).
- [x] **T3 — the migrate ACTION for the ambiguous case.** DONE @ 7ca9005: explicit-mode `migrateOrphanedCredentialScope` in `auth/credential-scope-reconcile.ts` (per-table collision keys via `CREDENTIAL_SCOPE_COLLISION_KEYS`; move every orphaned row whose UNIQUE slot is free under the boot handle, SKIP + count any row that would collide); shared-brain `migrateOrphanedCredentials` in `gateway/cores/integrations.ts` journaling moved/skipped counts; agent tool `integrations_migrate_orphaned` (`approval_policy: 'prompt-user'`) + `POST /api/cores/integrations/migrate-orphaned`, both through the one brain. Tests: `gateway/cores/__tests__/integrations-migrate-orphaned.test.ts` (colliding row skipped AND the fresh decrypted value survives; non-colliding rows across all three tables move and read back decrypted; response + journal carry no secret material; tool + HTTP route wired and auth-gated) + surface routing in `gateway/__tests__/cores-integrations-surface.test.ts`.
- [x] **T4 — finalize the card: AS_BUILT.md changelog entry + the recorded reminders-scope decision (doc-only, last task).** DONE @ (this commit): `docs/AS_BUILT.md` gains its one-entry-per-merged-change record (newest-first, house prose style, no secret material). The card's out-of-scope clause — "say whether the scope key belongs on `reminders` at all before moving those rows" — gets its decision RECORDED in this plan (decision text supplied by the planner; rows untouched): `reminders.project_slug` is a read-path filter, not a crypto scope; no rotation hazard exists there because `id` is the UUID PK and no UNIQUE involves the slug; drop-or-sweep is a follow-up card. No source or test files change; card suites and typecheck stay green.

DECISION — reminders scope key (recorded per the card's out-of-scope clause; rows untouched): `reminders.project_slug` (migration `0004_gateway_core.sql`; `reminders/store.ts:133` — "SQL column name remains `project_slug`; value is the owner/instance slug") is a read-path FILTER key, not a crypto scope: nothing is encrypted under it, no frozen-handle contract applies to it, and the table lives in per-instance, single-owner SQLite. As an owner-identity scope the column does not belong on the table — it is legacy partitioning inside an already instance-scoped store, and its only runtime effect is to make rows written under a pre-provisioning slug invisible (measured 2026-08-14: `reminders_cancel` returned ok:false for a live pending row). Because `id` is a UUID primary key and no UNIQUE constraint involves the slug, moving those rows carries NO rotation hazard — the collision guard that gates the credential migration has nothing to guard here. Correct fix, deferred to a follow-up card: either drop the column (schema change + read-path updates at `reminders/store.ts:413/440/463/484`) or sweep it unconditionally at boot as a plain metadata companion. This card moves credential rows only.

- Commit IMPLEMENTATION_PLAN.md together with the AS_BUILT.md entry.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
