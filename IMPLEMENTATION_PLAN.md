# IMPLEMENTATION_PLAN — Publish-path rebase conflicts must be resolved, not escalated (Plan card 2026-08-15)

Governing spec: this Plan card (wire the Forge conflict resolver into `rebaseOntoObservedBase`). Build branch: `trident/a-rebase-conflict-on-the-publish-pa` (local tip `b90a60c` = T5; origin holds `26c19dd` = T4, so T5 is committed but NOT yet pushed). Verified 2026-08-15 against the branch tip and `origin/main` = `4eb3adc`:

- T1–T5 ARE LANDED AND VERIFIED ON THE BRANCH: `rebaseOntoObservedBase` takes an optional 7th arg `resolve?: { run: TridentRun; resolve_conflict: MergeConflictResolver }` and loops the resolver in the scratch worktree bounded by `MAX_CONFLICT_ROUNDS`, requiring the unresolved set to SHRINK every round; a staged-BYTES marker scan (`git diff --cached -U0`) catches a resolver that `git add`s a file with `<<<<<<<` still in it; the resolver input carries `mode: 'rebase' | 'replay'` so the prompt tells the truth at each call site; conflicted paths are read `-z` with `core.quotePath=false`; both git streams feed `publishFailureReason`. Real-git proof in `trident/publish-rebase-realgit.test.ts` (resolving / declining / marker-liar / partial-across-rounds / no-resolver), stub-host acceptance 1–6 in `trident/orchestrator.test.ts` including the review-gate second-fire proof. AS_BUILT + SYSTEM-OVERVIEW rewritten.
- BUT MAIN MOVED AGAIN. `origin/main` is now `4eb3adc` (merge base of the branch is `cbcfb65`): #294 (diagnostics), #297 (push), and #296 (`d08555e`) which REWROTE the PR path of `rebaseOntoObservedBase` — `gh pr diff` is now REDIRECTED to the diff file via `sh -c 'gh pr diff N > "<file>"'` (patch bytes never enter a JS string; `spawnCapture`'s trim corrupted any patch ending on a blank-line context line — run 63b16fb1, `corrupt patch at line 746`), the empty-diff check moved below the `if`, and `trident/publish-rebase-realgit.test.ts` gained a `trailingBlank` fixture + anti-fake blank-line test. `git merge-tree --write-tree --merge-base cbcfb65 4eb3adc b90a60c` reports exactly ONE conflicted file: `trident/publish-rebase-realgit.test.ts` (one hunk: main inserted its blank-line test right where the branch renamed the "genuine conflict" test). `trident/orchestrator.ts`, `trident/orchestrator.test.ts`, `docs/AS_BUILT.md`, `docs/SYSTEM-OVERVIEW.md` all auto-merge textually.
- THE HIDDEN SEMANTIC CONFLICT the clean merge conceals: main's #296 stubs must honour the redirect (`ghPrDiffTo` writes the diff to the target file parsed from the `sh -c … > "file"` command). The BRANCH's added stub-host tests in `trident/orchestrator.test.ts` (~9 sites, branch lines 866–1249) still do `if (joined.includes('gh pr diff')) return ok('diff --git …')` — post-merge the diff file is never written and production throws `outer publisher refused to rebase an empty diff` before any conflict is reached, failing every resolver test. Each site must become `ghPrDiffTo(joined, '<same diff string>')` (helper already defined at the top of main's file; the two pre-existing sites at branch lines 556/784 are converted by the auto-merge itself).
- STRATEGY: MERGE `main` into the branch, not rebase. One deterministic conflict instead of per-commit replays, and the push becomes a plain fast-forward from origin's `26c19dd` — no history rewrite, lease trivially safe.

- [x] Bounded Forge conflict resolver exists with the correct tool grant and ESCALATE/RESOLVED contract (`trident/conflict-resolver.ts`, #342/#361).
- [x] Local-mode merge path invokes it, bounded by `MAX_CONFLICT_ROUNDS` (`trident/merge.ts`).
- [x] Composer builds the resolver gated on the live-credential predicate and passes `resolve_conflict` into the orchestrator opts (`open/composer.ts`, no composer change needed).
- [x] **T1 — thread `resolve_conflict` into `rebaseOntoObservedBase`, invoke it at the throw site bounded by `MAX_CONFLICT_ROUNDS`, rewrite the two stale comment blocks, prove acceptance 1–6 in the stub-host suite** (on branch as `f72ac69` post-reconcile).
- [x] **T2 — real-git proof in `trident/publish-rebase-realgit.test.ts`**: resolving resolver's staged resolution commits and lands in the CAS-moved head; declining resolver stays an attention state with the branch ref unmoved; no-resolver case renamed (`06c27d8`).
- [x] **T3 — `docs/AS_BUILT.md` changelog entry**, newest-first, citing the three dead runs (`25b2327d`/`5a17ec86` #290, `9e813276` #289), root cause #291, and the kept invariants (`998ecf8`).
- [x] **T4 — reconcile the resolver threading with #290/#292's rewrite of `rebaseOntoObservedBase`** (wholesale-failure carve-out wins on empty unmerged; resolver loop on non-empty; `{ head, rebased, baseSha }` return and 7th `resolve?` arg coexist) (`26c19dd`, pushed).
- [x] **T5 — close review round-1**: staged-bytes lie-detector (mutation-proven), truthful `mode: 'replay'` prompt confined to the cwd with no test run, per-round shrink-or-bail progress guarantee, both-streams failure reasons, `-z`/`quotePath=false` path reads, SYSTEM-OVERVIEW + AS_BUILT honesty fixes, partial-resolution round test, `task` on the real-git `TridentRun` fixture (`b90a60c`, local only — NOT yet on origin).
- [x] **T6 — merge `origin/main` (`4eb3adc`, #294/#296/#297) into the branch; resolve the ONE textual conflict in `trident/publish-rebase-realgit.test.ts` (keep main's blank-line anti-trim test AND the branch's renamed no-resolver test title); convert the branch's ~9 stdout-returning `gh pr diff` stubs in `trident/orchestrator.test.ts` to main's redirect-honouring `ghPrDiffTo` helper; re-run the trident suites; push the branch (fast-forward from origin's `26c19dd`, lease-pinned).**
