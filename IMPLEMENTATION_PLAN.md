# IMPLEMENTATION_PLAN — Git truth comes from git: the publisher reads the commit OID (Part 1) and an unreadable head stops, never rebuilds (Part 2)

Governing spec: this Plan card (both parts — one root cause: a git fact routed through a model instead of read from git). Surveyed 2026-08-15 against **current `origin/main` = `6da6ddb`** (the local clone at `d8324cc` is 12 commits behind — per the spec's warning, ALL work is authored against `origin/main`, fetched first).

**State of the world, verified:**
- Branch `trident/the-publisher-must-read-the-commit` @ `dd15bc2` (PR #271, OPEN) already carries Part 1: T1 `5cc33da` (publisher `rev-parse --verify`s the head, claim demoted to a prefix CHECK, zero-ahead refusal, 7–40-hex claim decode in `inner-loop.ts`) and T2 `dd15bc2` (both `inner-workflow.mjs` publish handoffs stop throwing on sha shape; new `inner-workflow-publish-handoff.test.ts`).
- BUT that branch is based on `71a568a`, and main has since merged **#275** which rewrote `publishBuiltCommit` (rebase-onto-observed-base via `rebaseOntoObservedBase`, `headToPublish = rebased.head` flows into witness/diff/checkpoint). `git merge-tree` confirms 3-way CONFLICTS in `trident/orchestrator.ts`, `trident/orchestrator.test.ts`, `IMPLEMENTATION_PLAN.md`. On `origin/main` the Part 1 defect is therefore STILL LIVE: `inner-workflow.mjs:3764`/`:3906` still throw `…without a full local commit OID…`, `orchestrator.ts:669` still requires the agent-relayed sha to equal `rev-parse`, and `orchestrator.ts:1048` still fails a publish request with no OID.
- Part 2 is absent everywhere: `origin/main` `inner-workflow.mjs:2093` (`classifyResume`) still maps an empty `currentHead` → `{ mode: 'rebuild', reason: 'head-unreadable' }`; the head is read by a haiku PROBE AGENT (`readBranchHead`, ~line 2744), not by code; `inner-workflow-resume.test.ts:496` pins the buggy rebuild behavior.
- Standing hazard: `trident/derive-inline-activity-from-evidenc` is one commit ahead of PR #263 (`9a46680` locally on the production host) — never reset/delete/force past it.

- [x] **T1 (Part 1) — the publish boundary reads the OID from git**: `publishBuiltCommit` resolves the head via `rev-parse --verify` on the branch the inner loop NAMES; an agent sha is a CHECK only (full 40 = equality, 7+ = prefix); a mismatch fails naming BOTH values verbatim; a zero-ahead branch fails as "no new commits"; `inner-loop.ts` decode keeps a 7–40-hex claim verbatim. LANDED on the branch as `5cc33da` — but against pre-#275 code (see T4).
- [x] **T2 (Part 1) — the inner workflow hands the publisher a BRANCH NAME, not a model-relayed sha**: both publish handoffs (`forge-done` build, `fix-round-N`) write `publishHead: oidClaim(...)` (best-effort, null when implausible) and never throw for sha shape; workflow-level coverage in `inner-workflow-publish-handoff.test.ts`. LANDED on the branch as `dd15bc2`.
- [x] **T4 — RECONCILE the branch onto current `origin/main` (`6da6ddb`)**: re-author T1's publisher semantics inside the post-#275 `publishBuiltCommit` (which now rebases onto observed main before the lease push), hand-merging the three conflicted files; carry T2/inner-loop/handoff-test changes over verbatim; all five Part-1 acceptance criteria proven green by tests at the new tip. ← THIS ITERATION
- [ ] **T5 (Part 2a) — the resume live head is resolved in CODE by the outer loop**: `orchestrator.ts` (launch site ~805–852, where `resume_checkpoint`/`resume_checkpoint_head` are read) resolves the branch's live head via `opts.run_host` — `git ls-remote --heads origin refs/heads/<branch>` in pr mode / `git rev-parse` in local mode, matching `readBranchHead`'s existing authority — with up to 3 retries on command failure; threads it into the workflow args as `resumeLiveHead` (full-OID string on success; `''` on read-failure-after-retries; field OMITTED when there is no resume checkpoint or no recorded head, keeping args byte-identical for non-resume runs). `inner-loop.ts` `buildWorkflowArgs` + the `inner-workflow.mjs` args destructure gain the field; when present, `currentHeadAtResume` comes from it and the `head-probe-round-resume` agent seat is NOT dispatched (the agent value is at most a cross-check, never the source); when absent (old launcher), fall back to the probe. A successful `ls-remote` with EMPTY output is a real read ("branch absent on remote"), not a failure.
- [ ] **T6 (Part 2b) — `classifyResume` returns a bounded STOP on an unreadable head, never `rebuild`**: keep the safety property ("could not tell" is never "unchanged") but change the consequence — `currentHead` unreadable with a recorded OID present → `{ mode: 'stop', reason: 'head-unreadable' }`. The workflow's stop path writes a terminal result `{ ok: true, verdict: 'REQUEST_CHANGES', round: 0, checkpoint: <the ORIGINAL resume checkpoint, preserved so a later re-run resumes from the same point>, blockKind: 'infra-only', terminalCause: 'could not read the head of <branch>; the recorded work is at <oid>; re-run when the read succeeds' }` and returns WITHOUT entering planning, build, or review (the outer `innerTerminalFailureReason` already renders `blockKind: 'infra-only'` + `terminal_cause` as a measured reason — never "without Argus APPROVE"). Guard tests in `inner-workflow-resume.test.ts`: force the head read to fail → assert the run stops, the reason names the branch AND the recorded OID, and NO plan/forge/review agent labels were dispatched; update the line-496 expectation that currently pins `rebuild`/`head-unreadable`.
- [ ] **T7 — docs + close-out**: dated `docs/AS_BUILT.md` entry covering BOTH parts (one root cause, fired in sequence in the neutron-enterprise #439 run), including the standing recovery note that `trident/derive-inline-activity-from-evidenc` @ `9a46680` is one commit ahead of PR #263 and must be PUBLISHED, never rebuilt or branch-deleted, until publish-retry (`01KZZS9ZKVJM658ZYE6ZRBBJJ2`) lands; final full-suite verification at the branch tip; persist this plan with recorded shas matching the real branch state.
