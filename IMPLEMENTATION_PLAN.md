# IMPLEMENTATION_PLAN — A dev-slug boot must never steal the live credential scope: the scope-rekey needs a DIRECTION GUARD

Governing spec: this Plan card (defect 2026-08-14 19:35 — a `NEUTRON_INSTANCE_SLUG`-unset boot with `NEUTRON_HOME` inherited rekeyed every live credential row `juno → dev`; a later explicit boot moved them back; the owner pressed ▶ inside the window and the frozen-handle gateway read zero secrets). Verified against main `d8324cc` on 2026-08-14:

- The base reconciler (#451, `migrations/scope-rekey.ts`) is on main and working as written: ledger-agree → noop; ledger-disagree/absent → snapshot then rekey-forward inside one `BEGIN IMMEDIATE`. It is SYMMETRIC — it will migrate onto the `'dev'` fallback exactly as happily as onto a real handle. That symmetry is the defect.
- Slug provenance is currently DISCARDED at boot: `gateway/index.ts` `resolveOwnerSlugFromConfig` (~line 174) returns a bare string (`.url_slug` file > `config.instanceSlug` > `'dev'`), so by the time `reconcileInstanceScopeOnProjectDb(db, project_slug, { dbPath })` runs (~line 305) nobody can tell "explicitly dev" from "nobody told me". `config.instanceSlug` is `undefined` when the env var is unset (`config/index.ts:393`), so the distinction is recoverable at the resolver.
- PR #266 (credential-scope orphan migration, same file) is NOT yet on main. Per the owner's directive: build NOW, keep the guard narrow and additive, evaluated BEFORE the existing move logic; if #266 lands first, rebase and place direction-check before its safe/ambiguous split — direction first, safety second.
- Existing tests to keep green untouched: `migrations/__tests__/scope-rekey.test.ts`, `scope-sweep-coverage.test.ts`, `open/__tests__/open-scope-rekey-boot.test.ts` (boots the real server — proves the resolver refactor is behavior-identical).

- [x] **T0 — base scope reconciler (#451): ledger + snapshot + forward rekey** (on main; prior card, not this one).
- [x] **T1 — the direction guard + slug-provenance threading + unit suite (incl. the flip-flop decrypted-token test).** `gateway/index.ts` gains `resolveOwnerSlugSourceFromConfig` (bit-for-bit same precedence, plus `source: 'file' | 'env' | 'fallback'`) and threads `currentSlugIsFallback` into the reconcile call; `migrations/scope-rekey.ts` gains an additive guard evaluated after stale-key discovery and BEFORE snapshot/transaction: fallback slug + stranded non-fallback rows ⇒ `action: 'noop'` with a recorded `refused_direction` reason, one loud log line naming both handles and the row count, zero writes, NO snapshot. Boot is never refused. New `migrations/__tests__/scope-rekey-direction-guard.test.ts` pins all five acceptance bullets, asserting on the DECRYPTED GitHub token via a real `SecretsStore`, not row counts.
- [ ] **T2 — boot-level wiring regression.** Through the real `boot()` (mirroring `open/__tests__/open-scope-rekey-boot.test.ts`): boot a home with an explicit slug and a stored GitHub token, then re-boot the SAME home with `instanceSlug` undefined in the config. Assert the boot SUCCEEDS (no refuse-to-boot), every secrets row stays under the explicit handle, no `project.db.pre-rekey-*` snapshot appears, and `readGitHubToken` still returns the original plaintext. This is the only test that can catch the flag silently not being threaded at the composition root.
- [ ] **T3 — docs + #266 composition check.** Add the one-line invariant to `docs/INVARIANTS.md` (guard anchor + protecting test); before opening the PR, re-check whether #266 merged to main and, if so, rebase and compose (direction evaluated first) rather than duplicating its decision. Append the `docs/AS_BUILT.md` entry.

- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
