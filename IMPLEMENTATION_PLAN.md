# IMPLEMENTATION_PLAN — Git truth comes from git (Parts 1–3)

Regenerated 2026-08-15 against the card branch trident/git-truth-comes-from-git-the-publis, round 2 (= origin/main cd8215e + T5 8043eb9 + P2b 23c14a0/81d8072 + the Argus round-1 fixes). ⚠️ The local checkout is BEHIND origin — always fetch and work from the branch tip, never local main. CORRECTION to the prior regeneration: the "P3 residue" item cited line numbers (3785/3804/3809/3861/3881-3888/3953/4015) that match origin/main, not the branch — it failed to credit T5 (8043eb9), which is committed on this branch and fixes every cited site. Verified line-by-line; checked off below.

- [x] P1 — Outer publisher resolves the head itself: `publishBuiltCommit` runs `git rev-parse --verify refs/heads/<branch>` at the credentialed boundary; the branch NAME is the handoff (orchestrator.ts, shipped in #283, reconciled onto the rebase-publish flow).
- [x] P1 — An agent-supplied sha is a CHECK, not a source: prefix-match against the resolved head; a disagreement fails naming both values verbatim ("the build reported commit 'X' but branch B resolves to 'Y'").
- [x] P1 — No sha / 7-char sha still publishes: `oidClaim` carries a 7–40-hex claim or null; the forge-done and fix-round handoffs never throw on sha shape; `publishHead` is a cross-check only.
- [x] P1 — Zero commits ahead of the remote still fails: "the build left no new commits to publish" refusal, compared against the PRE-rebase local tip.
- [x] P2a — The resume live head is READ IN CODE by the launcher: `resolveResumeLiveHead` (orchestrator.ts ~628), 3 attempts, tri-state (40-hex / 'absent' / ''), threaded as `resume_live_head` → `resumeLiveHead`; the `head-probe-round-resume` agent seat is skipped whenever the launcher supplies it.
- [x] P2a — 'absent' (the authority says the branch is gone) is a real answer → rebuild as 'head-branch-absent', distinct from '' ("could not read").
- [x] P2b — A genuinely unreadable head is a bounded STOP, never a rebuild: `classifyResume` returns `{ mode: 'stop', reason: 'head-unreadable' }` for ''; the workflow call site (inner-workflow.mjs ~3648) writes a terminal failure naming the branch and the recorded OID, blockKind 'infra-only' + terminalCause, no checkpoint overwrite, no re-entry into plan/build/review. Shipped 23c14a0 with guards at both boundaries.
- [x] Reason quality — infra-only stops carry a MEASURED `terminalCause`; `innerTerminalFailureReason` emits the specific reason only with that signal; no "without Argus APPROVE" for failures Argus never reached.
- [x] P3 — the build-completion head is READ from git at both completion sites (CORRECTED: shipped in 8043eb9 on this branch; the prior plan iteration diffed origin/main and missed it). `readBuiltHead` (inner-workflow.mjs ~2806) runs `git rev-parse --verify refs/heads/<branch>` locally with one retry; `branchHead`/`reviewedHead`/`forge-done`/`ralph-task-built`/`fix-round-N` checkpoint heads all source from it; `forge.commitSha`/`fix.commitSha` are prefix CROSS-CHECKS that stop loudly naming both values verbatim; a no-sha local build proceeds pinned to the git-read OID; unreadable-after-retry → bounded `builtHeadStop`, never rebuild, never "without Argus APPROVE". Read ONCE at completion, deliberately never re-probed at publish/merge (the load-bearing #545 distinction). Guarded by inner-workflow-built-head.test.ts. NOTE: the read is carried by a one-command probe agent (the only exec seam a workflow script has); it is fail-closed end-to-end — the outer publisher independently rev-parses in real code (P1) and `--match-head-commit` refuses a mismatched pin — so a mangled transcript can refuse work but never certify or publish the wrong commit.
- [x] P3 acceptance 5 — "nothing was built" stays a REAL, separately-worded outcome. `readBuiltHead` is tri-state like `resolveResumeLiveHead`: `'absent'` (rev-parse --verify failed, rev-parse --git-dir succeeded → the branch does not exist) keeps the `branchHead === '' || diffFile === ''` throw ("produced no commit on `<branch>` and/or no diffFile — nothing was built"), placed AFTER the merge and Ralph questions exactly as before; `''` (a failed READ) is the only state that earns the infra-only stop, and only when the build DID leave a diff. Re-run advice is never emitted for a build that produced nothing.
- [x] P3 acceptance 4, fix rounds — the empty-head stop is symmetric. A fix round with an unreadable head in local mode now takes the same `builtHeadStop` instead of checkpointing `head: ''` and approving with an empty `reviewedHead` (which `classifyResume` maps to rebuild — the very defect Part 2 removes). `'absent'` is deliberately NOT stopped there: a vanished branch at a fix round is the merge/round-lost question `roundOutcome` already asks.
- [x] Both bounded stops report `ok: false`. One failure class (`blockKind: 'infra-only'`, measured `terminalCause`, verdict REQUEST_CHANGES), one shape.
- [x] `innerTerminalFailureReason` requires a cause that survives redaction with something left to read — an empty/whitespace cause takes the generic sentence instead of emitting a dangling colon.
- [x] P3 (pr mode) — the reviewed head is git truth end-to-end: the publisher's rev-parsed (post-rebase) OID is encoded in the `outer-published:<oid>:<remaining>:<round>` checkpoint; the re-fired workflow reviews exactly that recorded OID with a diff the publisher generated from it, never a re-probe.
- [x] P3 item 3 — `publishBuiltCommit`'s refusal message names the requested OID and the actual OID verbatim.
- [x] Orchestrator pre-fire short-circuit — when `resolveResumeLiveHead` returns `''` (3 failed code reads), stop the run at the outer boundary with the same measured infra-only reason (via `innerTerminalFailureReason`, wording identical to the inner stop) instead of paying a workflow fire that will immediately stop itself. `pr-merged` checkpoints are exempt (classifyResume resolves them to `merged` before the head check), and so is an EMPTY checkpoint name (classifyResume resolves that to rebuild/'no-checkpoint' before the head check — nothing recorded to preserve); `classifyResume` stays the single semantic decider — this is only the cheap fast-exit. Checkpoint columns survive untouched so a re-run after the read recovers resumes exactly. ROUND-3 FIX — the attempts are no longer zero-delay. A `pr`-mode read is `git ls-remote`, a NETWORK call, and three back-to-back attempts complete inside milliseconds, so a DNS blip failed all three: the retry existed but did not cover the one class that is actually transient. `resolveResumeLiveHead` now waits `RESUME_HEAD_RETRY_DELAYS_MS` (`[250, 1000]`) BETWEEN attempts — never before the first, never after the last — through an injected `sleep` seam (`BuildTridentOrchestratorOptions.sleep`) the suite replaces with a no-op, so the delay is real in production and free in the tests. REMAINING TRADE-OFF, surfaced not hidden: the stop is still TERMINAL (the card's explicit requirement) rather than self-healing via an expensive and, per this card, unsafe rebuild; the run is re-runnable from the preserved checkpoint. If auto-retry of failed runs is wanted, that belongs in the tick loop, not here.
- [ ] Recovery note (do not lose it, VERIFIED THIS ROUND): `trident/derive-inline-activity-from-evidenc` is at local `0010aee`, origin at `8407d9c` — ONE UNPUSHED COMMIT ahead. The prior "local == origin, no unpushed work at risk" line was stale and is corrected here. `9a46680` is also preserved in the object store (not an ancestor; the branch was rebase-republished). It needs a PUBLISH, not a rebuild. Never reset, force past, or gc it. Nothing in this diff touches that branch.

Round-3 (Argus round-2 findings) — all addressed on this branch:

- [x] BLOCKER half A — `builtHeadStop` reported a checkpoint it never wrote. `writeTerminalResult` does not touch `inner_checkpoint`; the outer loop copies `result.checkpoint` into the row and leaves `inner_checkpoint_head` alone, so naming `forge-done`/`fix-round-N` there landed the NEW name beside the PREVIOUS phase's OID — the drift the checkpoint invariant forbids, and from there `classifyResume` → rebuild of committed work. The stop now reports `checkpoint: null` and writes none; the phase is named verbatim in `terminalCause`.
- [x] BLOCKER half B — the `pr`-mode `&& !isPr` carve-out checkpointed `head: ''` even when the builder claimed a valid full OID, which `classifyResume` reads as `no-recorded-head` → REBUILD once a publish fails. Both completion sites now fall back to the claim when (and only when) the READ failed and the claim is a full 40-hex OID. Safe because it stays a claim: no fast path opens unless it equals the live head the launcher reads from git, so a wrong claim degrades to `head-moved` → rebuild, exactly as `''` did. `absent` and abbreviated claims still record `''`.
- [x] Head-probe budget — `BUILT_HEAD_READ_ATTEMPTS = 3`, the same budget `resolveResumeLiveHead` spends on the same question (was 2). Each attempt is a fresh agent dispatch, so they are spread over real time without a sleep the workflow runtime does not provide; every failed attempt is logged with its tag so a one-attempt blip and a three-attempt outage are distinguishable. The seat relay remains a stated limitation — see the PR description and docs/AS_BUILT.md — and is fail-closed end-to-end.
- [x] One shape for one failure class — both bounded stops now redact + cap their `terminalCause` through one `infraCause` helper (the resume stop passed its text through raw).
- [x] Stop wording claims nothing unobserved — a failed read did not see the checkout (a non-repo directory lands on the same `''`), so "the branch is preserved" is gone; the message says what the run DID. The "nothing was built" throw likewise says when a commit's absence was not measured.
- [x] One reading of the claim — the fix round reads `oidClaim(fix?.commitSha)` once and uses that value everywhere, matching round 1 (it previously null-tested `oidClaim` and then compared/printed the raw string).
- [x] docs/AS_BUILT.md now records BOTH fast-exit exemptions (`pr-merged` and the empty checkpoint name), matching the code and this plan.
- [ ] OUT OF SCOPE, file separately against `main`: Verdict C's `chat-core/session-cache.ts` and `landing/chat-react/controller.ts` findings. `git diff origin/main...HEAD -- chat-core landing` is 0 lines on this branch; those files were last changed by already-merged main commits.

Guard rails (not tasks): do NOT loosen `publishBuiltCommit`; do NOT re-probe heads at publish/merge time; do NOT edit `classifyResume` or inner-workflow.mjs for the remaining task.

Round-4 (Argus round-3 findings) — all addressed on this branch:

- [x] MAJOR (confirmed A+B) — the 300-char cap TRUNCATED the round-1 unreadable-head cause. Measured: with the live 43-char branch `trident/git-truth-comes-from-git-the-publis` (the maximum `slugify-task` can produce), a 40-hex claim and a `/tmp/trident-<slug>.diff` path, the composed cause is 331 chars and lost its trailing "re-run when the read succeeds" — the only actionable clause in it. The old guard passed only because its fixture branch was short. TWO defences, because the diff path is model-supplied and unbounded: (a) the cap is now `TERMINAL_CAUSE_MAX = 500`, mirrored in `trident/inner-loop.ts` (which clamps again on the way into the DB — the smaller of the two is what actually decides, so both had to move); (b) every composed cause now puts its re-run advice near the FRONT, so truncation can only ever cost detail. Guarded with a 43-char branch and with a 400-char diff path.
- [x] MAJOR (B) — the "head read failed, so whether a commit exists was not measured" wording never reached the operator. The empty-build exit is a THROW, and the catch persisted `{checkpoint:'inner-error'}` with no cause, so `innerTerminalFailureReason` fell through to "…without Argus APPROVE" on a path Argus never ran — the card's own acceptance criterion, and the exact shape of run 3d2696c3. The catch now carries `terminalCause: infraCause(thrownMessage)` (same redact+cap helper as the two bounded stops) and NO `blockKind`, because a throw is not a review verdict. `innerTerminalFailureReason` widens by exactly one value — `block_kind === null` — and emits `inner workflow failed at round N of M: <cause>`; 'code'/'round-lost'/'none' keep the generic sentence (a finding title describes the DIFF, not a terminal cause), and 'infra-only' keeps the only sentence licensed to say "review never ran".
- [x] MINOR (A+B) — the legacy `readBranchHead` probe now speaks the same TRI-STATE as `readBuiltHead`/`resolveResumeLiveHead`. It ran a bare `git rev-parse <branch>` (which prints the BRANCH NAME and exits 128 for a missing branch) and a plain `ls-remote` (which prints nothing), so a DELETED branch reached `classifyResume` as `''` = "could not read" — a permanent bounded stop no re-run could clear, on exactly the launchers with no `resume_live_head` to rescue them. Now `rev-parse --verify --quiet` + the `--git-dir` health check (local) and `ls-remote --exit-code`'s documented exit 2 (pr). `'absent'` is collapsed to `''` at the fix-round call site ONLY: `roundLanded` compares strings, and a literal `'absent'` would differ from the round-1 head and read as a landed round.
- [x] MINOR (A) — the pr-mode claim/head MISMATCH stop gets the same `!isPr` carve-out the unreadable-head stop already had. In `pr` mode `publishBuiltCommit` makes the identical comparison in real code against a `rev-parse` the host ran itself; here both sides arrive through a model seat, so the inner check is a strictly less trustworthy copy that can veto a build the publisher would have validated. Acceptance criterion 3 is unaffected — the claim travels out verbatim as `publishHead` and the publisher still refuses loudly, naming both values. In `local` mode there is no publisher, so the check stands.
- [x] MINOR (B) — the mismatch cause no longer deduces "wrong branch or wrong worktree". Two OIDs that disagree is all it measured, and it is not even the likeliest reading (an amending hook, or a second commit between the builder's `rev-parse` and the probe, produce the same signal). It names both OIDs and stops there, like the shipped Part-1 twin.
- [x] MINOR (A) — `RESUME_HEAD_RETRY_DELAYS_MS` docs corrected rather than the number changed: ~1.25 s covers a sub-second blip, NOT "a momentary GitHub 5xx" as `docs/AS_BUILT.md` and the docblock claimed. The trade-off is stated (a wider window stalls every genuinely-dead branch) and the failure remains recoverable — `failedRun` preserves the checkpoint columns.
- [x] NIT (B) — `readBuiltHead`'s docblock no longer asserts budget equivalence it cannot measure: the ATTEMPT COUNT matches the launcher, the SPACING does not (this runtime has no sleep; the launcher waits between attempts).
- [x] NIT (B) — `remainingTasks: 0` on a built-head stop in a Ralph run is documented as deliberate: it is the outer loop's RE-FIRE flag, not a claim that no tasks remain, and re-firing a run that just failed to read its own head spends another iteration on the condition that stopped it. The count is recoverable from the committed `IMPLEMENTATION_PLAN.md` and the untouched checkpoint columns.
- [x] NIT (A) — the dead duplicate `head-probe-round-` stub in `trident/__tests__/dying-reviewer-e2e.test.ts` is gone (one prefix match serves both probes).
- [ ] FOLLOW-UP, NOT DONE HERE (disclosed, and Argus rates it non-blocking): the build-completion OID is produced by `git rev-parse --verify` but still TRANSCRIBED by a model seat, because `inner-workflow.mjs` runs in a runtime whose only capability is `agent()`. Shell-substituting the OID inside the checkpoint Bash step would remove the last transcription; it is a separate change to `checkpoint.sh` and its callers and is deliberately not bundled with a review-fix round. Fail-closed today: in `pr` mode `publishBuiltCommit` rev-parses in real code and `--match-head-commit` refuses a mismatched pin, so a mangled transcript can refuse work but never certify or publish the wrong commit.
- [x] GATING NOTE accepted — Verdict C's `chat-core/session-cache.ts` blockers are off-artifact (`git show --name-only` on this branch's commits touches no such file) and are not addressed here.
- [ ] Recovery note UNCHANGED and re-verified this round: `trident/derive-inline-activity-from-evidenc` is at `0010aee` locally and at origin. `9a46680` remains in the object store. It needs a PUBLISH, not a rebuild. Nothing in this diff touches that branch.

Guard rails (still): do NOT loosen `publishBuiltCommit`; do NOT re-probe heads at publish/merge time; `classifyResume`'s tri-state semantics are unchanged by round 4 (only the PROBE that feeds it learned to speak them).
