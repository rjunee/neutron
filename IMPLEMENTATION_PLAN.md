# IMPLEMENTATION_PLAN — A gateway restart must not kill an in-flight build: recover from the pushed branch instead of reaping the run

Governing spec: this Plan card (owner instruction 2026-08-14: "Gateway restarts should not kill a build. The build should recover."), plus the BLOCKING addendum of 2026-08-14 23:40Z (rebase + migration renumber + three-way column merge).

State verified against origin/main (51d17b7) and origin/trident/a-gateway-restart-must-not-kill-an (1a8c51b) on 2026-08-15:

- BLOCKING addendum: fully discharged on the branch. Rebase sits directly on main HEAD 51d17b7; migration renumbered to `0123_code_trident_runs_crash_recoveries.sql`; `expected-schema.txt` ends `… inner_checkpoint_head TEXT, inner_checkpoint_findings TEXT, crash_recoveries INTEGER) STRICT`; `trident/store.ts` now DERIVES the INSERT placeholders from `COLS` (31/31/31 by construction) and `trident/store.test.ts` pins the remaining half (COLS count = PRAGMA table_info count; distinct-value round-trip goes red on any swap/short array) — commit 1a8c51b. `gh` is unauthenticated in the planner environment, so `gh pr view 267 --json mergeable` = MERGEABLE must be (re)confirmed executor-side at push time.
- Core recovery (T1) is built and committed (5b3be91): `trident/orchestrator.ts` §1a-crash claims a crashed row atomically via `begin_crash_recovery` (`TridentRunStore.beginCrashRecovery`, wired in `gateway/composition/build-core-modules.ts`) and relaunches it as a continuation (same branch/PR/checkpoint, rounds untouched); past the durable `crash_recoveries` budget (default cap `DEFAULT_MAX_CRASH_RECOVERIES` = 3) it fails with a reason naming the crash-recovery budget and EMBEDDING the latched crash reason (`Last crash: ${run.failure_reason …}`) — deliberately avoiding the token "exhausted", which `delivery.ts` would misclassify; a crash-recovery-budget `infra` class sits AHEAD of the "exhausted" matcher there.
- LOAD-BEARING TRAP (governs T2 and any store change): `saveIfActive` vetoes non-crashed writes onto a `crashed`-latched row and re-latches on a tombstoned `workflow_run_id`; recovery clears the latch and nulls the generation ONLY via the atomic `beginCrashRecovery` claim — never via `update`/`save`/`saveIfActive`.
- T2 seam confirmed still open: `open/wiring/substrates.ts` `onChildCrash` (~line 429) still hands `crashRunningByLauncher` the bare `inner workflow child crashed: ${detail}` — no timestamps. `crashRunningByLauncher` (`trident/store.ts` ~458) latches that string onto `failure_reason`, and the orchestrator's budget-cap reason embeds `run.failure_reason`, so composing the timestamps at the `onChildCrash` seam propagates them to the unrecoverable run's stored reason with NO orchestrator change.

- [x] B0 — REBASE onto origin/main (51d17b7): commits 5b3be91 + 1a8c51b sit directly on main HEAD, no conflict markers, `bun test trident/` green on the branch.
- [x] B1 — RENUMBER the migration off main's taken `0122`: done as `migrations/0123_code_trident_runs_crash_recoveries.sql` (read off origin/main's highest ordinal, not "branch highest + 1"); `runner.test.ts` expects 123.
- [x] B2 — THREE-WAY COLUMN MERGE, both sides survive: `expected-schema.txt` carries `inner_checkpoint_head`, `inner_checkpoint_findings` AND `crash_recoveries` (last, matching migration run order); `store.ts` COLS/INSERT/bound array all carry all three.
- [x] B3 — PIN THE 31/31/31 INVARIANT IN A TEST: `INSERT_PLACEHOLDERS` derived from `COLS` (placeholder count = column count by construction); `store.test.ts` pins COLS count = PRAGMA count and a distinct-value round-trip that reds on any swap/short bound array (commit 1a8c51b). PR #267 MERGEABLE check re-runs executor-side at every push (`gh` unauthenticated in the planner env).
- [x] T0 — Harvest-before-reap ordering (acceptance (c)): step() §1 tries `parseInnerResult` FIRST even on a crashed row; asserted in `trident/crash-before-launch-save.test.ts` (invariant = boundedness, not reaping).
- [x] T1 — RECOVER, DON'T REAP + BOUNDED DURABLE BUDGET (acceptance (a), (b), (c)-preserved, (e)): migration 0123 `crash_recoveries`; atomic `beginCrashRecovery` claim (clear latch, release slot, null tombstoned generation, spend budget); §1a-crash continuation relaunch; budget-capped terminal reason (no "exhausted") embedding the latched crash reason; `delivery.ts` infra class; mutation-red tests incl. cap-holds-across-restart, board never reads `failed` mid-recovery, crashed-with-result harvests with zero fires (`trident/crash-recovery.test.ts`, `store.test.ts`, `delivery.test.ts`).
- [x] T2 — REPORT THE MEASURED CAUSE (acceptance (d)). In `open/wiring/substrates.ts` `onChildCrash`, compose the latched reason with measured facts: observation time and this gateway process's boot time (`Date.now() - process.uptime() * 1000`), phrased strictly as measurements — `inner workflow child crashed: ${detail} (observed <iso>; gateway process booted <iso>)` — never as an asserted cause, and never containing the token "exhausted". Because T1's budget-cap reason embeds the latched `failure_reason`, the boot timestamp survives onto the unrecoverable run's stored reason for free. Mutation-red tests: (i) `open/__tests__/open-wiring-substrates.test.ts` — the reason latched by `crashRunningByLauncher` carries BOTH ISO timestamps (extend the existing #514 production-watchdog test, which already drives the real `onChildCrash` seam end-to-end); (ii) `trident/crash-recovery.test.ts` — a budget-capped run whose latched reason carries a sentinel boot timestamp still shows that timestamp in its terminal `failure_reason` (goes red if the orchestrator stops embedding the latched reason).
- [ ] T3 — DOCS + INVARIANTS. Append the change to `docs/AS_BUILT.md` (append-only — keep both sides on any conflict); add `docs/INVARIANTS.md` entries for (i) harvest-before-reap ordering (file:line + test), (ii) crash recovery is budget-bounded and the budget is durable, (iii) a recovery relaunch never consumes `round`/`ralph_round`, (iv) store INSERT column/placeholder/bound-array counts are pinned by construction + test. Brief SYSTEM-OVERVIEW touch where the trident crash path is described. Final push: re-verify `gh pr view 267 --json mergeable` = MERGEABLE and `bun test trident/` green.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.

RESUME: the durable outer loop already owns PR #267 and branch trident/a-gateway-restart-must-not-kill-an. Commit to that SAME local branch. Do NOT push and do NOT run `gh`; the outer loop reuses the PR.
