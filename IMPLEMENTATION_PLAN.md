# IMPLEMENTATION_PLAN — dogfood-night fixes 2026-07-21 (9 issues, Ryan-reported: 5 original + 4 added from the same dogfood session)

Scope: ONLY the 9 issues from Ryan's 2026-07-20/21 dogfooding session. Repo: neutron-open (main protected — all work on branch trident/dogfood-fixes-jul21, PR #429). NO FEATURE FLAGS — build the real default directly. Verify-before-assert: re-read cited code before editing; lines may have moved.

- [x] **1. ISSUES #380 — blank-screen CLASS fix: React-19 root auto-recovery + unmount-guard sweep.** PR #417 (merged 07-20 18:22) guarded DocumentsTab only (`landing/chat-react/DocumentsTab.tsx:105-201` mountedRef+abortRef); Ryan STILL hits blank screens after it. Two-part fix: (a) `main.tsx:102` — pass `onUncaughtError` to `createRoot` (React 19.1.0, `landing/package.json:16`) and auto-remount the app with a bounded crash policy (≤3 recoveries / rolling 60s, then a visible error card with Reload — never a silent blank; controller/OPFS store live outside React so remount restores the transcript); (b) sweep EVERY remaining async-continuation setState in `landing/chat-react/` — IntegrationsTab.tsx:95-198, SettingsTab.tsx:109-230, work-activity.tsx effects :100-168, PlansPane fetch paths, WorkBoardTab handler continuations :418-495 (its `aliveRef` covers list refresh only), ChatApp/useAttachmentDraft/useNeutronChat/tab-overflow/HtmlDoc — apply the DocumentsTab alive-ref + abort-reads pattern. Tests: new `root-recovery.test.tsx` + per-pane discriminating tests in the `doc-pane-unmount-503.test.tsx` style (READ its header first — jsdom cannot reproduce the fiber invariant; pin the defensive contract instead). DONE: commits 24b99eea, be79c161, 3d5bcc3e, 3f753b9a, b6525180.
- [x] **2. Personality-character suggester — wire the REAL Opus-backed suggester into the LIVE onboarding path.** `onboarding/interview/onboarding-preamble.ts:47-49` injects the static `DEFINED_PERSONALITY_CHARACTERS` (same 5 names for everyone) into every CC-session onboarding preamble; the real personalized suggester (`onboarding/interview/personality-character-suggester.ts:261`, wired `open/composer.ts:1284-1287`) is only consumed by the OLD phase-machine path (`engine-spec-resolution.ts:335`). Invoke the suggester for the live preamble path (user's real name/projects/interests), splice its picks into the preamble prompt + button options, keep `STATIC_PERSONALITY_CHARACTER_FALLBACK` as the deterministic fallback on LLM failure/timeout, and keep `button-backed-answer.ts` name-matching working for whatever names are actually shown. DONE: new `onboarding/interview/live-personality-suggestions.ts` coordinator (memoized-LLM guard picks via `guardCharacters`; background fingerprint-gated fire-and-forget `maybeKickoff`; `'llm'`-only persistence into the SAME `personality_character_suggestions` + `..._source` keys the old engine uses, plus a new `..._fingerprint` key so picks regenerate when name/projects/interests change and freeze on an unchanged `'llm'` fingerprint; re-read-before-write to avoid stale-phase clobber; static default until picks land / on failure — never a blocked turn). `personality-character-suggester.ts` exports `FALLBACK_CHARACTER_NAMES`; `onboarding-preamble.ts` step guard takes an optional `personality_characters` set (static `DEFINED_PERSONALITY_CHARACTERS` when absent, byte-identical old behavior) rendered as `- name (why)`, and the preamble goal-4 now points at the per-turn guard list (`<onboarding_required_steps>` PERSONALITY step, personalized to this owner) instead of enumerating a fixed list; `button-backed-answer.ts` anchor extended from the static 5 to `candidatePersonalityAnchorNames` (static ∪ 16 fallback-pool names ∪ memoized picks) so a tap/typed answer against ANY rendered list settles `agent_personality` while the import step stays a disjoint anchor; `open/composer.ts` builds the coordinator (`buildLivePersonalitySuggestionCoordinator`, seed = `project_slug`) and calls `maybeKickoff` + `guardCharacters` inside `onboardingContext` — the generate is NEVER awaited on the turn path. Tests: new `__tests__/live-personality-suggestions.test.ts` (gating/dedup, llm-persist keys+phase+advanced_at, fallback-no-persist+retry, fingerprint freeze/regenerate, settled-meanwhile skip, rejection swallowed, anchor union); updated `onboarding-preamble.test.ts` + `button-backed-answer.test.ts`. `bun test onboarding/interview` 555 pass; `tsc -p onboarding` + `tsc -p open` clean. NO FEATURE FLAGS.
- [ ] **3. Work Board add-item dropdown — remove from web + mobile; server auto-classifies build vs research.** Web: `landing/chat-react/WorkBoardTab.tsx:309` (`newTaskType` state), `:432` (`create(projectId, { title, task_type: newTaskType })`), `:645` (`<select>`). Mobile: `app/app/projects/[id]/workboard.tsx` (~:254 "Add something to do…"). Server: `gateway/http/work-board-surface.ts:277-305` reads optional `task_type`; `work-board/store.ts` defaults 'build'. Fix: delete both dropdowns; clients OMIT task_type on create; server auto-classifies the title when task_type is absent — cheap fast-model LLM call with tight timeout + deterministic keyword fallback (pattern: personality/persona suggesters), classification logic in ONE server-side place. Must not regress: ▶ play-button routing (`startBuild`/`startResearch`) passes explicit task_type and is unaffected.
- [ ] **4. Chat-dispatched work gives NO chat acknowledgment.** A chat turn that creates a work-board card (doctrine `gateway/wiring/operating-doctrine.ts:65` + build routing :81-90) updates the pane but the chat shows nothing back. Investigate the tool-call turn path: is assistant text after tool_use suppressed/dropped, or does the doctrine never instruct a spoken ack, or does a text turn exist but not render? Fix so dispatching work from chat ALWAYS yields a visible ack message (doctrine instruction + any turn-delivery/render fix found). Reproduce-then-fix test at whichever layer the drop is verified.
- [ ] **5. Per-project opening messages converge on the same 'took a first pass' shape + some projects get no rich doc at all.** `gateway/wiring/build-project-kickoff-composer.ts:135-136` REQUIRES "you took a first pass and drafted a starting document" as a content beat in every `opening_message` — loosen so the LLM varies how it introduces the drafted work, grounded in that project's actual content. Separately audit `onboarding/openings/kickoff.ts:194,249` `hasWorkSignal` gate: verify signal-sufficiency classification isn't starving legitimate projects of the rich starting-plan doc path (bare deterministic opening fallback), and fix/loosen if misclassifying.
- [ ] **6. Chat text input feels laggy vs Telegram's native feel.** Investigate `landing/chat-react/ChatApp.tsx` — the composer uses assistant-ui's `ComposerPrimitive.Input` (~line 1374) over a custom `ExternalStoreRuntime` adapter (see the 'BUG 7' comment ~line 568). No per-keystroke network call exists (already ruled out — the typing indicator only reflects in-flight sends). Likely cause: the ExternalStoreRuntime re-rendering more of the message thread than necessary on every keystroke. Needs REAL reproduction/profiling (React DevTools Profiler or equivalent instrumentation) to find the actual re-render culprit before changing anything — do not guess-fix. Write a test/benchmark that pins down the fix once found.
- [ ] **7. `research_deep` tool (`cores/free/research/src/research-orchestrator.ts` `deep()` ~line 306) fails to actually do research.** VERIFIED via a live failed task row: the dispatched sub-agent ran 31s, made ZERO tool calls (never called research_vault_search/research_web_search/research_web_fetch despite the system prompt in `sub-agent-prompt.ts` instructing mandatory tool use), and returned non-JSON text, so `extractJson` failed with 'no JSON object found' and the whole task died — `deep()` has NO retry on parse failure (unlike `start()`'s 2-attempt retry-with-parse-error-fed-back loop, `research-orchestrator.ts` ~line 220-291). Sub-agent model defaults to Haiku 4.5 (`sub-agent.ts:194` `DEFAULT_SUB_AGENT_MODEL`). REAL FIX (3 parts): (a) switch the default sub-agent model from Haiku to Sonnet — import `SONNET_MODEL` from `runtime/models.ts` instead of the hardcoded Haiku literal (Ryan's explicit direction: Haiku is documented for lightweight/cheap tasks only, this needs real reasoning + sustained tool-use discipline); (b) add at least one retry in `deep()` when `extractJson`/schema validation fails, mirroring `start()`'s retry-with-parse-error-fed-back pattern, so a single bad response doesn't discard the whole research budget; (c) enforce that the sub-agent cannot return a final answer with zero tool calls when tools were available for the query — either reject-and-retry with a stronger tool-use reminder, or escalate on retry. Write a reproduce-then-fix test: a fake sub-agent runtime returning non-JSON text with 0 tool calls should now retry/recover rather than immediately fail.
- [ ] **8. Fix 6 hardcoded Claude model-id literals that bypass `runtime/models.ts`'s centralized resolver** (its own docstring says 'never hardcode a Claude model id outside this file' — Ryan: 'that's bad design'). Verified via grep+read, exactly these 6, no more: `cores/free/research/src/sub-agent.ts:194` (`DEFAULT_SUB_AGENT_MODEL` — coordinate with task 7's Sonnet switch, don't do this twice), `cores/free/research/src/research-orchestrator.ts:353` (a SECOND hardcoded Haiku literal in a metadata-recording call — should reference the SAME resolved model actually used, not a separate hardcoded string), `tasks/prioritize-llm.ts:61` (`DEFAULT_TASK_PRIORITIZE_MODEL` → reference `FAST_MODEL`), `cores/free/email/src/tools.ts:175` (fallback → reference `FAST_MODEL`), `cores/free/code-gen/src/substrate-runtime.ts:519` (fallback → reference `SONNET_MODEL`), `gateway/tasks/p6/nudge-engine.ts:93` (`DEFAULT_NUDGE_MODEL` → reference `FAST_MODEL`). DO NOT touch the 3 `'claude-haiku-fallback'` sentinel values in `cores/free/calendar/src/mcp-tools-extra.ts`, `gateway/cores/mount-cores-scribe-fan-out.ts`, `gateway/cores/calendar-wiring.ts` — CONFIRMED BENIGN (verified: DI-default test/unconfigured-state placeholders; each docstring explicitly says production always passes the real resolved model, e.g. FAST_MODEL).
- [ ] **9. Work Board items don't clear their `inline_active` flag when reaching a terminal status via the GENERIC completion path.** VERIFIED live in a tenant's DB: a completed item (status='done') still had `inline_active=1`. ROOT CAUSE (read `work-board/store.ts` in full): the specialized `attachRun()`/`detachRun()` methods (run-binding lifecycle for trident/atlas dispatches) correctly clear `inline_active=0` as part of their transitions — but the GENERIC `update()`/`complete()` path (used for simple non-run-bound completions, e.g. `complete()` calling `update(project_slug, id, {status:'done'})`) has NO such clearing logic; `inline_active` is only written when a caller explicitly includes it in the patch, and this path doesn't. DO NOT consolidate attachRun/detachRun into the generic path — they have legitimately different run-binding semantics (already explained to Ryan, he agreed). FIX: inside the generic `update()` method's status-transition branch, make clearing `inline_active` to false an UNCONDITIONAL part of any transition to a terminal status ('done' or 'failed'), regardless of whether the caller's patch explicitly included it. Write a reproduce-then-fix test: create an item with inline_active=true, complete it via the generic `complete()`/`update()` path, assert inline_active is now false.
