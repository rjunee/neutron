# IMPLEMENTATION_PLAN.md — Executor-Mode Reminders (P0-1 M2 PARITY, Neutron Open)

Spec of record: `docs/plans/executor-mode-reminders-2026-07-20.md` — the DEEPENED header block (lines 1-150) SUPERSEDES the original plan below it wherever they conflict. Honor the three Ryan overturns (SPEC Decisions Log 2026-07-20, neutron-managed): (1) Bash ports via approval, not exclusion; (2) dreaming's uncovered half goes into CORE MEMORY (split by tier: backlink repair = event-driven on sync hook; correction-promotion = reflect pass; daily-delta = stays a ritual); (3) ritual registration is AGENT-CALLABLE with in-chat approval — the approval RENDERING carries the security. NO FEATURE FLAGS — build the real default directly. One task per iteration; commit + PR every iteration.

Planner corrections (re-verified 2026-07-21, resume session):
- Branch `trident/executor-mode-reminders` (PR #426) is LIVE at `ad7d6df4` with tasks 0-2 committed: task 2 (`ff7db60e`→`2e3bf2bd`) + Argus r2 (`e62251e7`: DispatchAgentKind narrowing, runtime enum guards, statSync byte-cap) + r3 (`ad7d6df4`: typeof-before-RegExp fail-closed guards on id + tool_surface). CONTINUE on it; do not recreate.
- Task 3 design LOCKED this pass: **no new table, no migration** — durable grants are `tool_approvals` rows (migration-0004 DDL verified `0004_gateway_core.sql:66-79`) with namespaced `tool_name` (`ritual:<id>` / `ritual-egress:<id>`), `args_json.content_hash`, `status='approved'`, `decided_by`/`decided_at` = approved_by/approved_at. The record key `(ritual_id, content_hash, approved_by, approved_at)` maps onto the existing row shape; the pending→approved/denied/expired machine + notifier seam are reused verbatim (the "extend, never parallel" instruction).
- Layering is FORCED: `.dependency-cruiser.cjs` `platform-stays-low` (severity error) forbids `tools/` (platform) → `reminders/` (services). The ritual layer therefore lives at `reminders/ritual-approval.ts` importing `@neutronai/tools` (legal services→platform edge; neither package imports the other today, no cycle).
- Cadence arm CHOSEN: cadence is a hash input → cadence change invalidates the grant (re-approval). No hard floor / daily fire cap in v1.
- `ApprovalManager` is composed at `gateway/composition/build-core-modules.ts:275-278` from the composer's `approval_notifier`; NOTHING calls `requestApproval` in production yet — task 3's ritual request path is the first real caller, and the composer no-op notify swaps for a real app-ws notifier per the `watchdogNotifier` precedent (composer ~3338-3368, `appWsRegistry` :2051).

- [x] 0. Design doc deepened + Ryan review complete (plan doc STATUS: COMPLETE; three overturns locked in neutron-managed SPEC Decisions Log 2026-07-20; four-review findings folded into the header block)
- [x] 1. **M2-3 / P0-4 — collapse `NEUTRON_PERFECT_RECALL`; memory consolidation 6h ON by default.** Landed on the branch: `runtime/perfect-recall-flag.ts` + test deleted; all four gate sites in `open/wiring/memory.ts` un-gated (RB1/RB3/RB4/RC2 unconditional); `WiredMemory` types tightened; scribe `supersede` option removed (always-on); `DEFAULT_REFLECT_INTERVAL_MS` 24h→6h; `nexus-emit.ts` re-export deleted; 8 flag-referencing test files + docs updated; §7.2 merge-safety gate (Argus r1 BLOCKER) fixed in `58933d1f`. AS_BUILT + SYSTEM-OVERVIEW entries committed (`d60d57ea`).
- [x] 2. **Ritual schema + registry module.** Migration **0106**: nullable `ritual_id` TEXT on `reminders` + regenerated `migrations/expected-schema.txt`; new `code_ritual_runs` run-history table (status vocab `skipped`/`running`/`finished`/`failed`/`timed_out`/`crashed` + `skip_reason` CHECK-coupled; `subagent_run_id`/`content_hash`/`failure_reason` + ritual and partial live indexes); `agent_kind` CHECK widened to `'ritual'` (create-copy-drop-rename, 0100 DDL + both indexes preserved) + `AgentKind` widened (`runtime/subagent/registry.ts`). `reminders/rituals.ts`: `RitualDef` (charset-guarded id; prompt derived `rituals/<id>.md`; description; scope; non-empty tool_surface incl. Bash [overturn 1]; egress 'none'|'web' register-time-consistent; silent; tier/timeout constants) + `createRitualRegistry` + async `validateRitualFire` with REQUIRED `RitualApprovalCheck` seam (approval-THROW → unapproved, fail closed) + `MAX_RITUAL_PROMPT_BYTES` pre-read cap. Fire-time: unknown/missing-prompt/unapproved → log + SKIP (never degrade-to-nudge, never `tools: []`). `ritual_id` read-through in `reminders/store.ts`. Argus r2+r3 hardening included. Run-history WRITER deferred to tasks 4-5.
- [x] 3. **Approval-gate infrastructure (content-hash bound).** LOCKED design (this pass): extend `tools/approval.ts` with `findApproved(project_slug, tool_name)`; new `reminders/ritual-approval.ts` (services→platform edge; add `@neutronai/tools` to `reminders/package.json`) with `computeRitualContentHash` (sha256 hex over canonical JSON array of prompt bytes ‖ sorted tool surface ‖ scope ‖ cadence ‖ model tier ‖ timeout — injection-proof, order-insensitive surface), `ritualCadenceString` (`spec:<cron>` | `legacy:<coarse>` | `once` from the row's mutually-exclusive `recurrence_spec`/`recurrence`, store.ts:41-49), `requestRitualApproval` (policy `prompt-user` through `ApprovalManager.requestApproval` — first real production caller; egress:'web' defs mint a SECOND separately-approved `ritual-egress:<id>` request bound to the SAME content_hash), and `createRitualApprovalCheck({manager, project_slug, cadence})` implementing task 2's seam — hash RECOMPUTED from live prompt bytes at EVERY fire (mutable ported prompts), content grant + (web defs) egress grant must BOTH match, db errors propagate so `validateRitualFire` fail-closes. Cadence-in-hash = update/cadence-change DROPS approval (`reminders_update` cancel+create mints a new id, `cores/free/reminders/src/mcp-tools-extra.ts:64` — assert the laundering pin). Composer's no-op `approval_notifier` swapped for `buildAppWsApprovalNotifier` (new `open/wiring/approval-notifier.ts`, watchdogNotifier app-ws precedent; plain-text body, NEVER prompt bytes, fail-soft). NO auto-approval anywhere (bundled rituals stay unapproved → SKIP until task 8's explicit affirmative act); no-self-approval enforcement is task 8 (ButtonStore `resolution_speaker_user_id`).
- [ ] 4. **Executor dispatch branch in the TICK** (`reminders/tick.ts` — NOT dispatcher.ts; the dispatcher's only-throws-before-delivery contract :271-282 stays nudge-only). Ritual rows: after the #319 claim, resolve+validate via `validateRitualFire` (checker from task 3: `createRitualApprovalCheck({manager, project_slug, cadence: ritualCadenceString(row)})`) → spawn fire-and-forget via `spawnSubagent` (`runtime/subagent/spawn.ts:67`) kind `'ritual'` — NOT DispatchService (throws `missing_board_item`, `agent-dispatch/service.ts:296-300`); new ephemeral `cc-ritual-*` substrate factory (current `makeEphemeralSubstrate` hardcodes `skip_permissions:true` + no tool bridge, `open/wiring/substrates.ts:296` — needs a variant); thread `appendSystemPromptFile` (prerequisite — unthreaded, a ritual REPL runs as the CHAT agent and inherits the enforce-reply Stop hook); separate ritual concurrency lane (don't starve `MAX_CONCURRENT_SUBAGENTS=8`); **SKIP `on_fired` for executor rows** (tick.ts:210-219 would push-notify up to 45min before output); record fires + skips into `code_ritual_runs` (+ table-ownership.json entry with the first writer); never await. Tests: T2 fire-and-forget + nudge-contract untouched.
- [ ] 5. **Completion delivery + failure surfacing.** Terminal event → post final text through the one `deliver()` seam to the resolved topic; `silent` skips the post; failed/timed-out/crashed → one-line failure notice (ritual id, status, run id); 3-consecutive-failure escalation notice; boot-reap orphans → `crashed` + notice; `code_ritual_runs` retention prune. Tests: T3 behavioural completion (artifact-on-disk + durable history row + silent + failure-notice variants), T6 crash surfacing.
- [ ] 6. **T5 write-containment spike — HARD GATE for every writing/Bash ritual (incl. all ported Vajra rituals per overturn 1).** Prove a per-session settings.json deny rule fails CLOSED (run terminates; outside write ABSENT; no wedge) in the headless PTY, ACCOUNTING for the substrate auto-approver (`runtime/adapters/claude-code/persistent/spawn.ts:239-245`, `signatures.ts:89-90` incl. `runthiscommand`) — the ritual REPL must disable that scanner or the test is theater. Record the verdict in the plan doc. If fail-closed is unprovable → OS-level sandbox becomes its own prerequisite sprint and Bash/writing rituals stay gated.
- [ ] 7. **Bundled generic read-only example rituals** (ENGINE seeds — Ryan's specific ritual content is USER DATA via his Vajra import, never hardcoded): generic morning-brief + evening-wrap templates, surface `['Read','Glob','Grep']`, no Bash — Layer 1 (`--tools` default-deny, verified `build-repl-argv.ts:52-60`) alone contains these. Bundled defs register but stay UNAPPROVED until the owner's explicit task-8 act. T7 per-ritual acceptance test each (output must reference real fixture state — ported-prompt silent-no-op guard).
- [ ] 8. **Q3 — agent-callable registration with in-chat approval.** Reminders-core MCP tools to propose/register; approval via ButtonStore persisted option-set binding (the `captureButtonBackedRequiredField` precedent, `onboarding/interview/button-backed-answer.ts:198` — eligibility only from `prior_agent_options`, `:207-209`): opaque proposal_id; approval is an explicit affirmative act resolving through task 3's `respondApproval`; no self-approval (`resolution_speaker_user_id`, `channels/button-store.ts:106` — resolver must be owner); render preformatted (never Markdown — button body is Markdown today, `channels/button-primitive.ts:193`), never truncate (refuse over hard cap), NFC-normalize, reject bidi/zero-width, itemize URLs/paths/skills; describe CAPABILITY not tool names; no register-and-fire in one turn; no surface-widening without re-approval. Test T8: owner replies something UNRELATED → ritual stays pending_approval, never dispatches.
- [ ] 9. **Q2 — dreaming's uncovered half into core memory** (overturn 2, split by tier): (a) deterministic entity backlink repair EVENT-DRIVEN on the entity-writer sync hook (writer already emits `newLinks`/`removedLinks` — `runtime/entity-writer.ts` EntityWriteOutput); (b) correction-pattern promotion folded into `runReflectPass` (the 6h loop task 1 turned on); (c) daily-delta notes as a bundled ritual (time-anchored — nothing in memory triggers it). Explicit toHaveBeenCalled/artifact assertions each.
- [ ] 10. **Docs + close-out.** `docs/AS_BUILT.md` entries per landed task; `docs/SYSTEM-OVERVIEW.md` new "Ritual executor" subsystem section (+ reminders section update, consolidation-cadence update); `reminders/AGENTS.md` refresh; final summary citing the executor branch file:line + approval-rendering file:line + T5 verdict.
