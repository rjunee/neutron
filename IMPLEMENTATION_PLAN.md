# IMPLEMENTATION_PLAN — The publisher must read the commit OID from git, not take a model's word for it (defect 2026-08-14, run 3d2696c3)

Governing spec: this Plan card. Verified against main 2026-08-14:

- The sha is round-tripped through a MODEL at three points today. (1) `trident/inner-workflow.mjs:3172-3176` (build handoff) and `:3360-3364` (fix-round handoff) THROW `…completed without a full local commit OID for the outer publisher` when `forge.commitSha` is not 40-hex — the exact crash that killed run `3d2696c3` after a successful build (worktree-cleanup kept the branch at `9a46680`, one commit ahead of PR #263). (2) `trident/inner-loop.ts:439-442` decodes `publish_head` to null unless full 40-hex, so an abbreviated sha is silently dropped. (3) `trident/orchestrator.ts:800-806` fails a null `publish_head` with reason `inner workflow requested outer publishing without a full commit OID`, and `publishBuiltCommit` (`orchestrator.ts:465`) uses the agent-supplied sha as the PIN, refusing (`no longer points at`) when `rev-parse` disagrees — the sha is the source, git is the check. The card inverts that: git (`rev-parse --verify` on the branch the inner loop names) is the source; an agent sha is only a CHECK, and a mismatch fails naming BOTH values.
- The #259 lease machinery in `publishBuiltCommit` (observe `ls-remote` → `--force-with-lease=<ref>:<observed>` → witness → empty-diff refusal, with git's stderr carried in failures) is sound and STAYS; the pin, witness comparison, diff range, and `outer-published:` checkpoint simply re-key onto the resolved head.
- Zero-ahead guard is REQUIRED by acceptance: with the no-sha throw gone, a build that committed nothing (local == remote) must fail as "nothing was built", not false-publish. Three existing test fixtures (`orchestrator.test.ts:141`, `:338`, `:359`) set pre-push `ls-remote` == local head and must switch to a stale-then-head sequence; eight hostResponders match `rev-parse refs/heads/feat-x` and must gain `--verify`.
- Reason quality: none of the new failure reasons may read "without Argus APPROVE" — Argus never ran in this class. All publish failures flow through `publish failed: <reason>` in `applyResult`, which already satisfies this; tests assert it.
- Recovery: `trident/derive-inline-activity-from-evidenc` @ `9a46680` needs a PUBLISH, not a rebuild — the branch must not be deleted (T3 records this; the retry-publish mechanism itself is card `01KZZS9ZKVJM658ZYE6ZRBBJJ2`, out of scope here).

- [x] **T1 — the publish boundary reads the OID from git: `publishBuiltCommit` resolves the head via `rev-parse --verify`, demotes an agent sha to a prefix CHECK (mismatch fails naming both verbatim), refuses a zero-ahead branch, and the decode accepts a 7-40-hex claim.** `trident/orchestrator.ts` (`publishBuiltCommit` + the `applyResult` publish branch), `trident/inner-loop.ts` decode + field doc, existing-test fixture updates + four new acceptance tests in `trident/orchestrator.test.ts`, decode cases in `trident/inner-loop.test.ts`.
- [ ] **T2 — the inner workflow stops throwing on a missing/abbreviated `commitSha` at BOTH publish handoffs** (`inner-workflow.mjs:3172-3186` build, `:3360-3376` fix-round): the handoff is the BRANCH NAME; `publishHead` becomes best-effort — the trimmed `commitSha` when it is 7-40 hex chars, else `null` — and the `publishResult` is written either way (never a throw for sha shape). Keep `ported-fixes.test.ts:404`'s `publishRequested: true` source assertion green; add workflow-level coverage that a no-sha build still writes a `publishRequested` terminal result.
- [ ] **T3 — changelog + recovery note:** `docs/AS_BUILT.md` entry for this fix, including the standing instruction that `trident/derive-inline-activity-from-evidenc` @ `9a46680` is one commit ahead of PR #263 and must be PUBLISHED by hand (never branch-deleted, never rebuilt) until publish-retry (`01KZZS9ZKVJM658ZYE6ZRBBJJ2`) lands.
