# IMPLEMENTATION_PLAN — `/reset` chat command (branch trident/reset-chat-command, PR vs main)

Scope: port task 4 of the input-modalities-commands work (deliberately left out of PR #428). Design pinned by Ryan 2026-07-23: `/reset` behaves like sending `/clear` to Claude Code itself — clear the MODEL's conversation context while the underlying warm REPL process stays alive. NOT a respawn: `respawnSupervisedSession` (`runtime/adapters/claude-code/persistent/supervision.ts:59` → `planRespawn`, `session-respawn.ts`) is verified context-PRESERVING ("Respawn-is-always-resume") and must not be used. The right primitive is `CONTEXT_RESET_COMMAND = '/clear'` (`signatures.ts:164`) actuated under the per-session `acquireTurn()` mutex exactly as the import warm-session per-turn reset does (`pool.ts:354` + `pool.ts:372-402`). Out of scope (Ryan 2026-07-21): `/promote`, `/archive`, `/new`, any other Vajra topic-lifecycle command; do NOT touch `/status`. NO FEATURE FLAGS — build directly as the default behavior. Verify-before-assert: re-grep every cited line before editing; lines may have moved.

- [x] **1. Build `buildResetChatCommandFilter` + `resetPooledSessionContext` end-to-end.** (a) NEW `runtime/adapters/claude-code/persistent/context-reset.ts` — `resetPooledSessionContext({substrate_instance_id, user_id, project_scope, acquire_wait_ms?, idle_quiet_ms?, idle_max_ms?})`: prefix-match warm `pool` entries (`pool-state.ts:227`) on the first three `SESSION_KEY_SEP`-joined key dimensions (credential dimension wildcarded), bounded-acquire the session's `acquireTurn()` mutex (self-releasing abandoned slot on timeout → `busy`, never a wedged chain), then mirror `pool.ts:378-385` verbatim: `waitForReplIdle` → `child.write(CONTEXT_RESET_COMMAND + '\r')` → sleep → `waitForReplIdle`; typed outcome `ok/sessions_reset | busy | no_live_session | reset_failed`. (b) `gateway/boot-chat-command-filters.ts` — `buildResetChatCommandFilter({reset})` mirroring `buildStatusChatCommandFilter` (:271-292) exactly: `isExactSlashCommand(body, '/reset')` boundary (`/resetfoo` falls through null), injected thunk, reply text composed from the live outcome via exported `formatResetOutcome` (never a canned lie: busy and no-live-session reply honestly); re-export via `gateway/composer-contract.ts` (:62-70). (c) `open/composer.ts` — build the filter next to `statusChatCommandFilter` (:1437-1446) binding the thunk to `resetPooledSessionContext` with `substrate_instance_id: 'cc-agent-' + owner_handle`, `user_id: OWNER_USER_ID`, `project_scope: input.project_id ?? 'general'` (MUST mirror `build-live-agent-turn.ts` `turn.project_id ?? 'general'` scope), and append it to `buildChainedChatCommandFilter([...])` (:1453-1457). Tests: new `gateway/__tests__/reset-command-wiring.test.ts` (template `status-command-wiring.test.ts`) + new `runtime/adapters/claude-code/persistent/__tests__/context-reset.test.ts` (real-PTY-write assertions, harness cloned from `import-warm-session-reset.test.ts` — assert the literal `'/clear\r'` write landed / did NOT land, session survives, busy path writes nothing and never wedges the mutex). Docs: `docs/AS_BUILT.md` newest-first entry + `docs/SYSTEM-OVERVIEW.md` "Narrow Neutron chat commands" section (~:71-90) — retitle to include `/reset` and replace the "sibling command still to land" sentence with the shipped mechanics. Gates: `bunx tsc --noEmit`, affected test suites, `bash scripts/ci/lint.sh`, `bash scripts/ci/depcruise.sh` (0 new violations, baseline untouched). Commit, push, open PR against main.

Documented limitation (deliberate non-goal this iteration, candidate ISSUES entry): when NO warm session exists for the scope (e.g. right after a gateway restart, before any turn), `/reset` replies `no_live_session` honestly — but a later cold spawn may `--resume` the prior transcript from the repl-registry, so context is not cleared in that edge. Full cold-reset semantics would need registry surgery on the respawn-is-always-resume machinery and is out of scope per Ryan's pinned live-session design.
