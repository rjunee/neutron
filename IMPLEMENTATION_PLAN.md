# IMPLEMENTATION_PLAN — dogfood-night fixes 2026-07-21 (Ryan-reported: 5 original + 4 added from the same dogfood session; +1 discovered-root-cause follow-up, task 10, added 2026-07-22)

Scope: the 9 issues from Ryan's 2026-07-20/21 dogfooding session + task 10 (verified root-cause extension of issue 7). Repo: neutron-open (main protected — all work on branch trident/dogfood-fixes-jul21, PR #429). NO FEATURE FLAGS — build the real default directly. Verify-before-assert: re-read cited code before editing; lines may have moved.

- [x] **1. ISSUES #380 — blank-screen CLASS fix: React-19 root auto-recovery + unmount-guard sweep.** PR #417 (merged 07-20 18:22) guarded DocumentsTab only (`landing/chat-react/DocumentsTab.tsx:105-201` mountedRef+abortRef); Ryan STILL hits blank screens after it. Two-part fix: (a) `main.tsx:102` — pass `onUncaughtError` to `createRoot` (React 19.1.0, `landing/package.json:16`) and auto-remount the app with a bounded crash policy (≤3 recoveries / rolling 60s, then a visible error card with Reload — never a silent blank; controller/OPFS store live outside React so remount restores the transcript); (b) sweep EVERY remaining async-continuation setState in `landing/chat-react/` — IntegrationsTab.tsx:95-198, SettingsTab.tsx:109-230, work-activity.tsx effects :100-168, PlansPane fetch paths, WorkBoardTab handler continuations :418-495 (its `aliveRef` covers list refresh only), ChatApp/useAttachmentDraft/useNeutronChat/tab-overflow/HtmlDoc — apply the DocumentsTab alive-ref + abort-reads pattern. Tests: new `root-recovery.test.tsx` + per-pane discriminating tests in the `doc-pane-unmount-503.test.tsx` style (READ its header first — jsdom cannot reproduce the fiber invariant; pin the defensive contract instead). DONE: commits 24b99eea, be79c161, 3d5bcc3e, 3f753b9a, b6525180.
- [x] **2. Personality-character suggester — wire the REAL Opus-backed suggester into the LIVE onboarding path.** `onboarding/interview/onboarding-preamble.ts:47-49` injects the static `DEFINED_PERSONALITY_CHARACTERS` (same 5 names for everyone) into every CC-session onboarding preamble; the real personalized suggester (`onboarding/interview/personality-character-suggester.ts:261`, wired `open/composer.ts:1284-1287`) is only consumed by the OLD phase-machine path (`engine-spec-resolution.ts:335`). Invoke the suggester for the live preamble path (user's real name/projects/interests), splice its picks into the preamble prompt + button options, keep `STATIC_PERSONALITY_CHARACTER_FALLBACK` as the deterministic fallback on LLM failure/timeout, and keep `button-backed-answer.ts` name-matching working for whatever names are actually shown. DONE: new `onboarding/interview/live-personality-suggestions.ts` coordinator (memoized-LLM guard picks via `guardCharacters`; background fingerprint-gated fire-and-forget `maybeKickoff`; `'llm'`-only persistence into the SAME `personality_character_suggestions` + `..._source` keys the old engine uses, plus a new `..._fingerprint` key so picks regenerate when name/projects/interests change and freeze on an unchanged `'llm'` fingerprint; re-read-before-write to avoid stale-phase clobber; static default until picks land / on failure — never a blocked turn). `personality-character-suggester.ts` exports `FALLBACK_CHARACTER_NAMES`; `onboarding-preamble.ts` step guard takes an optional `personality_characters` set (static `DEFINED_PERSONALITY_CHARACTERS` when absent, byte-identical old behavior) rendered as `- name (why)`, and the preamble goal-4 now points at the per-turn guard list (`<onboarding_required_steps>` PERSONALITY step, personalized to this owner) instead of enumerating a fixed list; `button-backed-answer.ts` anchor extended from the static 5 to `candidatePersonalityAnchorNames` (static ∪ 16 fallback-pool names ∪ memoized picks) so a tap/typed answer against ANY rendered list settles `agent_personality` while the import step stays a disjoint anchor; `open/composer.ts` builds the coordinator (`buildLivePersonalitySuggestionCoordinator`, seed = `project_slug`) and calls `maybeKickoff` + `guardCharacters` inside `onboardingContext` — the generate is NEVER awaited on the turn path. Tests: new `__tests__/live-personality-suggestions.test.ts` (gating/dedup, llm-persist keys+phase+advanced_at, fallback-no-persist+retry, fingerprint freeze/regenerate, settled-meanwhile skip, rejection swallowed, anchor union); updated `onboarding-preamble.test.ts` + `button-backed-answer.test.ts`. `bun test onboarding/interview` 555 pass; `tsc -p onboarding` + `tsc -p open` clean. NO FEATURE FLAGS.
- [x] **3. Work Board add-item dropdown — remove from web; server auto-classifies build vs research (VERIFIED anchors 2026-07-22).** DONE. Web-only UI change — MOBILE NEEDS NO CHANGE (verified: `app/app/projects/[id]/workboard.tsx` has NO dropdown; its `addItem` already sends `{ title }` only, the stale earlier brief said otherwise). Deleted the web dropdown from `landing/chat-react/WorkBoardTab.tsx` (the `WorkBoardTaskType` import, the `newTaskType` state + its reset, the `<select className="cwb-add-kind">` block, the deps entry) so the web add-form omits `task_type` on create. NEW `work-board/task-type-classifier.ts` (services band — `@neutronai/contracts` import legal, mirrors `tasks/package.json`) exports `classifyWorkBoardTaskType({ title, llm: LlmCallFn|null, timeout_ms? }) → Promise<WorkBoardTaskType>`: FAST_MODEL one-word classify with a 2.5s timeout + deterministic keyword fallback on null-llm/timeout/junk/reject (TOTAL — never rejects). `gateway/http/work-board-surface.ts` gained an optional `classify_task_type` seam applied ONLY when a create omits `task_type`, BEFORE the create_card/store.create branch so BOTH paths persist the classified value; explicit `task_type` from ANY caller (mobile, agent tools, ▶ retry) short-circuits it. `open/composer.ts` wires it next to the `proactiveLlm` pattern: `buildAnthropicLlmCall({ substrate: llmCallSubstrate, model: FAST_MODEL })`, null when LLM-less (→ keyword-only). NO new model literal — the classifier takes `LlmCallFn` (model injected at the composer). agent-tool + trident build-tool create paths send no task_type and KEEP the store 'build' default (out of scope). ▶ startBuild/startResearch route by the ITEM's stored task_type — untouched. Tests: new `work-board/task-type-classifier.test.ts` (LLM-wins, junk/both/reject/timeout/null → keyword fallback, table, capture-arg); `gateway/http/work-board-surface.test.ts` (classify-on-omit both branches, explicit-wins, reject→default, create_card path, #379 back-compat anchor unchanged); `landing/chat-react/__tests__/work-board-tab.test.tsx` (no picker, create body omits task_type). `bun test work-board` 230 pass, `bun test gateway/http/work-board-surface` 38 pass, landing tab+client 37 pass; `tsc -p work-board/open/gateway/landing` clean; eslint + depcruise (new edge) clean. NO FEATURE FLAGS. DONE: commit f1202214.
- [x] **4. Chat-dispatched work gives NO chat acknowledgment — deterministic server-side chat ack + doctrine spoken-ack (ROOT CAUSE VERIFIED + DESIGN PINNED 2026-07-22).** VERIFIED: the live agent's ONLY chat output is the dev-channel `reply()` tool, exactly ONE per turn, landing at TURN END (`runtime/adapters/claude-code/persistent/dev-channel-impl.ts:108-131`; Stop hook `hooks/enforce-reply.ts` enforces it; a 2nd reply is turn-id-rejected `dev-channel-impl.ts:184-198`); a chat-dispatched inline job runs INSIDE the turn per doctrine (`gateway/wiring/operating-doctrine.ts:65,79-95`) for up to 45 min (`gateway/wiring/build-live-agent-turn.ts:117` TURN_ABSOLUTE_CEILING_MS; 90s activity-reset inactivity :95) — so the card pops the pane instantly (store `onChange` → `work_board_changed`, `work-board/store.ts:18-19`) while chat stays silent until the turn settles; the trident route acks only if the model chooses to (no deterministic guarantee). RULED OUT: per-block text delivery (none exists — terminal text is invisible by design, `dev-channel-impl.ts:110`); approval wedge (`prompt-user` is log-only, `mcp/server.ts:124-127`); silent empty reply (posts FAILURE_BODY, `build-live-agent-turn.ts:1281-1293`). FIX (default-on, NO FLAGS): (a) NEW `work-board/chat-ack.ts` — `buildWorkBoardChatAck({resolve_chat_id, post, now?, dedup_window_ms?})` posting kind-shaped one-liners (card_added / build_dispatched / inline_started), per-(item_id,kind) 30s dedup, never-throws; (b) hooks at the AGENT-TOOL layer ONLY: `work-board/agent-tool.ts` `work_board_add` success + `work_board_update` inline_active false→true (pre-read `store.get`), `trident/work-board-build-tool.ts` `work_board_dispatch_build`/`work_board_start` success (title via `deps.work_board.get`; NO ack on rejection — #337 covers clarify); (c) composer wiring: one instance built from `tridentDeliveryChatId` (`open/composer.ts:2536`) + `buildClarifyPoster.post?.` (the #337 durable+live app-ws seam, bound `open/wiring/app-ws.ts:966`), threaded through `gateway/composition/input/misc-input.ts` (`work_board?`/`trident_build_dispatch?`) + `build-core-modules.ts:221,231`; (d) doctrine: append a spoken-ack sentence to DOCTRINE_PRINCIPLES[5] (the board principle) — the auto-confirmation is mechanical, the agent's reply must still speak to the started work; update `gateway/wiring/__tests__/operating-doctrine.test.ts`. Human HTTP adds / ▶ route post NOTHING (already covered by #337/#339). Tests: new `work-board/chat-ack.test.ts`; ack seams in `work-board/agent-tool.test.ts` + `trident/work-board-build-tool.test.ts`; doctrine test; `tsc -p work-board/trident/gateway/open`; eslint + depcruise; AS_BUILT entry. DONE: commit be469a74.
- [x] **5. Per-project opening messages converge on the same 'took a first pass' shape + some projects get no rich doc at all (ROOT CAUSES VERIFIED 2026-07-22 @ fcae0cd3).** (a) PROMPT CONVERGENCE: `gateway/wiring/build-project-kickoff-composer.ts:128-139` — the `opening_message` system prompt mandated a fixed 3-beat checklist incl. verbatim "that you took a first pass and drafted a starting document, and invite the owner to review it and tell you what to change", so every project's opener was the same sentence-plan with nouns swapped; NO test pinned this prompt text. Rewrote the branch: KEEP the hard invariants (output only message text, 2-3 sentences, second person, grounded ONLY in context / no invented facts, mention the drafted doc so the appended tappable link lands, no links/filenames, no greetings, no em dashes) but ban stock template phrasing + fixed sentence order and instruct leading with THIS project's most specific content. (b) GATE STARVATION — misclassification CONFIRMED: `onboarding/openings/kickoff.ts:249-254` `hasWorkSignal` required open_threads OR summary/slices OR (rationale AND topics), but the materializer's own grounding verdict `has_context` (`project-materializer.ts` = importCtx || slices; importCtx = `hasRealProjectContext`, `project-identity.ts`) counts the owner's OWN captured `project.rationale` — which never reaches KickoffSignal (only import-derived `matched` carries rationale, `finalize.ts`). So an owner-described work project with no import match had has_context=true yet failed the gate → generic deterministic opening while an import-matched sibling got the rich starting-plan doc (Ryan's exact variance). FIX: KickoffSignal gains `has_context` (from `input.outcome?.has_context ?? false`); `hasWorkSignal` checks `s.has_context` FIRST (single source of truth), and loosens rationale AND topics → OR (aligns with `hasInterestSignal`) as the outcome-null fallback; bare template README alone STILL does not qualify ("better nothing than a bad job" preserved — `has_context:false` projects keep the honest no-context prompt). Tests in `gateway/wiring/__tests__/build-project-kickoff.test.ts` (helpers default has_context:false so thin→null stays green) + new prompt-contract test on the REAL `buildProjectKickoffComposer` with a capturing fake client. `docs/AS_BUILT.md` entry. DONE: commit 33bda241.
- [x] **6. Chat text input feels laggy vs Telegram's native feel — ROOT CAUSE VERIFIED 2026-07-22 (planner render-count probes vs REAL @assistant-ui/react 0.14.23; supersedes the earlier "profile first" framing — profiling done at plan time).** REFUTED: keystroke fan-out through the assistant-ui store (10 setText calls → 0 message re-renders) and parent-re-render fan-out (assistant-ui memos rows internally: 5 parent bumps w/ unchanged adapter → 0; append → O(1)). CONFIRMED: `landing/chat-react/controller.ts` `computeVm()` rebuilt FRESH `RenderMessage` objects + a fresh `messages` array on EVERY `publish()` (20 sites — including PER STREAMING TOKEN, `agent_message_partial`), busting assistant-ui's per-message-identity converter cache + row memos: probe measured ALL 30/30 rows re-converted + re-rendered per publish (vs 0 with stable identities) → full un-memoized react-markdown re-parse (`Markdown.tsx`, fresh `components` closures per render) of the WHOLE transcript per token/frame — the main-thread stall Ryan feels as laggy typing. Secondary: `ChatApp.tsx` MountedConversation passed fresh `uploadsCtx`/`docLinkCtx` context literals per render (context bypasses memo into every TextPart), and `Composer`'s unselected `useComposer()` re-rendered the composer subtree per keystroke. DONE (all web, NO flags): (a) computeVm identity stability — per-id `renderCache` reusing prior RenderMessage objects via a total flat `sameRenderMessage` comparator (incl. `stream:` bubbles; notices already stable), + reuse the previous `messages` ARRAY when all elements are reference-equal (guard: first computeVm runs before `this.vm` exists); content byte-identical, ONLY identities change; (b) useMemo both context values via exported `useUploadsCtx`/`useDocLinkCtx` hooks; (c) `React.memo` `Markdown` + useMemo its `components` object; (d) Composer selector `useComposer((s) => s.text.trim().length > 0)` + imperative `composerRuntime.getState().text` in send(). Tests: NEW `__tests__/render-isolation.test.tsx` — T1 controller identity invariants (unrelated publish → SAME messages array; token → only stream bubble changes; reaction → only that row); T2 e2e counting-components over the REAL useChatRuntime+controller (unrelated frame → 0 durable-row re-renders; 3 tokens → bounded ≤15 total + 0 per durable row, not O(N)); T3 ctx-hook identity; T4 Markdown memo. `bun test landing/chat-react` 405 pass; tsc + eslint clean; AS_BUILT entry. `useNeutronChat.ts` untouched (SEV1 per-conversation runtime keying + #354 adapter memo preserved). DONE: commit f9cf465a.
- [x] **7. `research_deep` fails to actually do research — Sonnet sub-agent default + parse-failure retry + tool-grounding gate (ANCHORS RE-VERIFIED 2026-07-22 @ f9cf465a; branch = main for all research files).** VERIFIED via a live failed task row: the dispatched sub-agent ran 31s, made ZERO tool calls, and returned non-JSON text, so `extractJson` failed ('no JSON object found') and the task died — `deep()` (`cores/free/research/src/research-orchestrator.ts:306-410`) is SINGLE-ATTEMPT (parse :375-382, schema :383-387; no retry), unlike `start()`'s 2-attempt retry-with-parse-error-fed-back loop (:206-291). Default sub-agent model is a hardcoded Haiku literal (`sub-agent.ts:194` `DEFAULT_SUB_AGENT_MODEL`) and `deep()` passes NO `model` to `dispatchResearchSubAgent` (:330-342), so that Haiku default IS live (the :190-193 comment claiming "production wireup passes FAST_MODEL explicitly" is FALSE — fix the comment too). WIRED-REALITY CONSTRAINT (verified `substrate-runtime.ts:139-171`): the v1 production dispatcher honors `input.model` (so the switch is effective) but makes ONE tool-less API call and ALWAYS returns `tool_calls: []` — therefore the zero-tool-call gate MUST key on a new dispatcher-reported `tools_available` flag (v1 reports false → gate inert in production until task 10 ships the tool loop; a naive hard reject would brick every production deep run). FIX: (a) `DEFAULT_SUB_AGENT_MODEL` := `SONNET_MODEL` (import `@neutronai/runtime/models.ts`, same path style as `research-orchestrator.ts:17`); ALSO replace the second hardcoded Haiku literal at `research-orchestrator.ts:353` (error-path run metadata) with `DEFAULT_SUB_AGENT_MODEL` — recording Haiku after the switch would be a lie (this pre-satisfies task 8's :353 item); (b) 2-attempt retry loop in `deep()` for parse/schema failures, feeding the error + response head back via a new `retry_feedback` input appended to the sub-agent user prompt (dispatch-level errors — concurrency/timeout/transport — stay non-retried; claims/sources-cited stays single-shot, explicit non-goal); (c) zero-tool-call gate: when tools were requested AND the dispatcher reported `tools_available: true` AND `tool_calls.length === 0`, reject-and-retry with a stronger tool-use reminder; still zero on retry → fail. De-Haiku the now-false user-visible strings (`chat-commands.ts:227,254`, `package.json:275` tool description) + doc headers. Reproduce-then-fix tests: non-JSON+0-tool first response then valid second → completes; both bad → failed; tools_available-absent (v1 production shape) + 0 tool calls + valid JSON → completes first attempt. DONE: commit 5a34035b.
- [x] **8. Fix the remaining 4 hardcoded Claude model-id literals that bypass `runtime/models.ts`'s centralized resolver** (its docstring says 'never hardcode a Claude model id outside this file' — Ryan: 'that's bad design'). Originally 6; the 2 research-core literals (`cores/free/research/src/sub-agent.ts:194`, `cores/free/research/src/research-orchestrator.ts:353`) are handled by task 7 — re-verify they're gone, do NOT redo. Remaining, verified via grep+read: `tasks/prioritize-llm.ts:61` (`DEFAULT_TASK_PRIORITIZE_MODEL` → reference `FAST_MODEL`), `cores/free/email/src/tools.ts:175` (fallback → reference `FAST_MODEL`), `cores/free/code-gen/src/substrate-runtime.ts:519` (fallback → reference `SONNET_MODEL`), `gateway/tasks/p6/nudge-engine.ts:93` (`DEFAULT_NUDGE_MODEL` → reference `FAST_MODEL`). DO NOT touch the 3 `'claude-haiku-fallback'` sentinel values in `cores/free/calendar/src/mcp-tools-extra.ts`, `gateway/cores/mount-cores-scribe-fan-out.ts`, `gateway/cores/calendar-wiring.ts` — CONFIRMED BENIGN (DI-default test/unconfigured-state placeholders; each docstring explicitly says production always passes the real resolved model). DONE: commit 4f8a1085
- [x] **9. Work Board items don't clear their `inline_active` flag when reaching a terminal status via the GENERIC completion path.** DONE: commit 536feb20. VERIFIED live in a tenant's DB: a completed item (status='done') still had `inline_active=1`. ROOT CAUSE (read `work-board/store.ts` in full): the specialized `attachRun()`/`detachRun()` methods (run-binding lifecycle for trident/atlas dispatches) correctly clear `inline_active=0` as part of their transitions — but the GENERIC `update()`/`complete()` path (used for simple non-run-bound completions, e.g. `complete()` calling `update(project_slug, id, {status:'done'})`) has NO such clearing logic; `inline_active` is only written when a caller explicitly includes it in the patch, and this path doesn't. DO NOT consolidate attachRun/detachRun into the generic path — they have legitimately different run-binding semantics (already explained to Ryan, he agreed). FIX: inside the generic `update()` method's status-transition branch, make clearing `inline_active` to false an UNCONDITIONAL part of any transition to a terminal status ('done' or 'failed'), regardless of whether the caller's patch explicitly included it. Write a reproduce-then-fix test: create an item with inline_active=true, complete it via the generic `complete()`/`update()` path, assert inline_active is now false.
- [x] **10. NEW (discovered root cause, verified 2026-07-22) — `research_deep`'s production sub-agent CANNOT make tool calls at all: ship a real agentic tool loop.** `buildRuntimeResearchSubAgentDispatcher` (`cores/free/research/src/substrate-runtime.ts`) was the v1 single-call dispatcher whose own docstring said "actual tool-calling is deferred to a follow-up sprint" — it always returned `tool_calls: []` + `tools_available: false`, while the system prompt (`sub-agent-prompt.ts`) MANDATES research_vault_search/research_web_search/research_web_fetch use. So even on Sonnet (task 7), deep research was closed-book: claims could only be `confidence:"unverified"`. DESIGN PINNED BY WIRED REALITY: native Anthropic Messages-API `tools`/tool_use blocks are impossible here (production `llm_call` packs system+user into `AgentSpec.prompt` on the CC-subprocess substrate; `respondToTool` THROWS; direct HTTPS forbidden) — so the loop EMULATES the tool protocol over sequential text `llm_call` rounds: a strict JSON `{"tool_call":{"tool","input"}}` envelope, dispatcher executes the named executor via an injected map, threads `[TOOL_RESULT <name>]` back into the next round's user prompt, loops until the model emits the final brief JSON; bounded by `budget_ms` (finalize-margin pre-check) + a max-tool-round cap with a forced `[FINAL ANSWER REQUIRED]` last turn. Wired the three real executors in `wiring-production.ts` (vault via the SAME `ResearchStoreResolver` + `searchPriorBriefs`; web search via `buildTavilyProvider` + `webSearch`, key read per-dispatch from SecretsStore `{kind:'byo_api_key', label:'tavily'}` threaded from `mount-open-cores.ts`, graceful no-key degradation; web fetch via `webFetch` with `DEFAULT_WEB_FETCH_ALLOWLIST` + SSRF guards intact); forwarded `project_id` through `RuntimeSubAgentDispatchInput` (additive optional). Reports real `tool_calls` + `tools_available: true` (arms task 7's grounding gate in production). No-executors callers keep byte-identical v1 behavior (`tools_available:false` — back-compat, NOT a flag). Tests: `substrate-runtime-tool-loop.test.ts` (round-trip, round cap, budget cap, unknown-tool, throwing-executor, truncation, v1 back-compat, offered-intersection-empty), `wiring-production-tools.test.ts` (real vault search on empty sidecar via `deep()`, web-fetch allow-list reject, tavily-absent degradation, tavily-with-key hits), UPDATED `research-core-production-composer.test.ts` (harness `llm_call` scripts a vault tool round for Atlas first turn so the armed gate passes). Update docs/AS_BUILT.md, check this task off, commit, push. DONE: commit f1df3343
