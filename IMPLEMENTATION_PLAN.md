# IMPLEMENTATION_PLAN — the inner loop's `gh` has no credential: reads must carry the instance token, and an infra-only block must stop masquerading as REQUEST_CHANGES

Governing spec: this Plan card (run `8417b277` / PR #261, measured 2026-08-14). Verified against main (`d8324cc`) 2026-08-14; resumed on branch `trident/the-inner-loop-s-gh-has-no-credenti` (T1 committed as `a9b7cbd`) 2026-08-14.

- CONFIRMED on main: the inner workflow's three GitHub READ probes — `probeCi` (`trident/inner-workflow.mjs:2386`), `probeReviewReadiness` (:2407), `probePrMerged` (:2467) — compose bare `gh …` command strings executed by a subagent's Bash tool, which inherits the warm `cc-trident-fire-*` REPL env (`open/wiring/substrates.ts` ~:399). No `GH_TOKEN` reaches it. The WRITES precedent the card demands parity with is `open/composer.ts:2007-2008` — `makeLazyCredentialedHostRunner(async () => githubProcessEnv(await readGitHubToken(secretsStore, asOwnerHandle(owner_handle))))` — resolved per command, env-only, never on disk (`github/credential.ts` options 1–3 explicitly refused).
- CONFIRMED: `classifyReviewReadiness` (branch :2140) maps every unreadable probe to `{status:'unknown', reason:'PR readiness could not be read'}` and DISCARDS `raw` (the `gh auth login` text the log proves was in hand); `reviewPreconditionDeferred` (branch :2470) turns that into `{verdict:'REQUEST_CHANGES', blockKind:'infra-only'}`; the terminal result carries `blockKind` (branch :3528) but `parseInnerResult` (`trident/inner-loop.ts:455`) never parses it, so the orchestrator catch-all (`trident/orchestrator.ts:981-998`) writes the generic round sentence (`innerTerminalFailureReason`, :385) for a run no review seat ever judged. `trident/delivery.ts:195` then classifies that reason as klass `'unknown'` with a summary that says nothing about the lane.
- Threading precedent for getting a checked-in script to the workflow: `CHECKPOINT_SCRIPT_PATH` / `WORKTREE_CLEANUP_SCRIPT_PATH` (`trident/inner-loop.ts:203-222`) → `buildWorkflowArgs` → args destructure (`inner-workflow.mjs:82-186`). Test precedent for `.mjs` internals: source-extraction (`trident/__tests__/ci-gate.test.ts` `grab()`); for the store: `auth/__tests__/secrets-store.test.ts` (temp dir + `ProjectDb.open` + `applyMigrations`).

- [x] **T0 — the WRITE path already satisfies the card's parity target** (nothing to build; recorded so nobody re-solves it): outer publisher pushes with the per-command lazy credential + lease (#259, `orchestrator.ts:506-517`), verified live by PRs #263/#266/#267/#268. This is the shape T1 mirrors for reads.
- [x] **T1 — the inner loop's GitHub READS carry the instance credential, resolved per command from the store.** DONE (`a9b7cbd`): new checked-in runner `trident/gh-authed.ts` (resolves the token via `SecretsStore` + `readGitHubToken` at invocation time, execs `gh` with `githubProcessEnv(token)` in the child env only — never printed, never in argv, never on disk; no token → bare `gh`). Coordinates (`ghAuthedScript`/`bunBin`/`ghDataDir`/`ghOwnerHandle`/`dbPath`) threaded composer → misc-input → build-core-modules → orchestrator → `buildWorkflowArgs` → args destructure; the three probes now call `ghReadCommand()` (branch :2414) with bare-`gh` fallback when any coordinate is absent. Subprocess tests (`trident/gh-authed.test.ts`, `trident/__tests__/gh-read-command.test.ts`) prove the token from a real temp store arrives in the CHILD ENV and nowhere in argv, and deleting the store row turns the same assertion ABSENT.
- [x] **T2 — measure the cause, and having measured it, do not throw it away.** `classifyReviewReadiness` keeps the probe's own output — first line(s) of `raw`, redacted (mirror `redactPushError`'s two patterns locally in the `.mjs`) and length-capped — in `reason`; `classifyCi`'s unknown branches carry the same excerpt as `cause` and `ciDeferredPeer` quotes it; `reviewPreconditionDeferred`'s finding carries it (already flows via `readiness.reason`); the terminal result gains `terminalCause` (first LANE finding title, redacted+capped) when `blockKind === 'infra-only'`; `parseInnerResult` parses `blockKind` + `terminalCause` into `InnerResult`; `innerTerminalFailureReason` writes `review never ran (infra-only) at round N of M: <cause>` ONLY when block_kind is `infra-only` AND a non-empty measured cause arrived — the generic sentence remains for every result carrying no cause (the owner's rule: the specific message ships WITH the measured signal, never before it). Assert the stored reason for the 8417b277 shape contains the probe's own words (`gh auth login`) and never a credential.
- [ ] **T3 — an infra-only block is not the agent's work being rejected.** `trident/delivery.ts` `interpretFailure` recognises the T2 reason (`review never ran (infra-only)`) BEFORE the `inner workflow ended at round` branch, classifies it klass `'infra'` (retry-class messaging: no review seat ran, nothing was rejected), and its `input_needed`/summary quote the measured cause; the run row still terminates `failed` (the full no-terminate retry lane belongs to card 01KZZQ2J9MJFG0PXC8AA6D6EV4 — do NOT build it here). Test pins: an `infra-only` terminal result at round 1 is delivered as infrastructure, its message quotes the measured cause, and a genuine review REQUEST_CHANGES still classifies as before.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
