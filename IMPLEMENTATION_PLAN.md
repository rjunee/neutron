# IMPLEMENTATION_PLAN — Work Board hardening (branch trident/work-board-hardening, PR #431 vs main)

Scope: the LOCKED remainder of WAVE 3.5 Work Board (Decisions Log 2026-06-29, managed SPEC.md WAVE 3.5 — re-verified 2026-07-23). Layer A (board + per-turn injection + trident parallel-exec) is SHIPPED; Layer B context-management shipped in rounds 1-3 of this PR with ONE confirmed reviewer blocker remaining (round 4, below). NO FEATURE FLAGS — everything ships as default behavior. Verify-before-assert: re-grep every cited line before editing; lines cited at commit ccc00f28 and may have moved.

SUBSTRATE-SUPPORT CHECK (planning pass, 2026-07-23 — the Layer B gating question, answered): the CLI persistent-REPL context-editing beta (`clear_tool_uses` tool-result eviction) is NOT available — zero codebase hits for any context-editing primitive, no `claude --help` flag; `clear_tool_uses` is a Messages-API beta with no seam into a PTY-driven interactive `claude` REPL. The composer-side inject/rehydrate seam IS available and proven: `resetPooledSessionContext` (`runtime/adapters/claude-code/persistent/context-reset.ts:75`, merged with the `/reset` command) + the runner's cold-turn latch `contextSent` (`gateway/wiring/build-live-agent-turn.ts:692`, decision `:1142`, re-mark `:1361-1368`) — deleting a topicKey makes the next turn re-run `composeFirstTurnPrompt`, re-assembling the FULL grounding (persona + work board + reflection + memory index + nexus + services) with no new prompt-assembly code. Layer B therefore = periodic composer-side hard reset (`/clear`) + rehydrate-via-re-cold-turn, exactly the fallback the SPEC anticipated.

- [x] **Layer A / Phase 1 — board works** (managed SPEC WAVE 3.5, DONE 2026-06-30): `WorkBoardStore` + `work_board_*` agent tools + HTTP `/work-board` + per-turn `<work_board>` injection (`work-board/store.ts`, `work-board/agent-tool.ts`, `work-board/fragment.ts`, `gateway/wiring/build-live-agent-turn.ts` per-turn fragment resolve, `open/composer.ts:3379`) + web/mobile WorkBoardTab. Verified present in code this pass.
- [x] **Phase 2a — trident exec-model rearchitecture** (DONE 2026-06-30, #131): Workflow-tool dispatch, durable `code_trident_runs` harvest via TridentTickLoop. Verified: `trident/inner-loop.ts`, composer trident wiring.
- [x] **Phase 2b — trident bound + parallel** (DONE 2026-06-30, #140): required `board_item_id` on dispatch, N concurrent tridents, per-item activity icons, board-reconcile observer (`open/composer.ts:3645` `buildBoardReconcileObserver`).
- [x] **`/reset` chat command** (merged 2026-07-23, #430): `resetPooledSessionContext` primitive + `buildResetChatCommandFilter` + composer wiring (`open/composer.ts:1472-1487`). This is Layer B's actuation primitive.
- [x] **TASK A rounds 1-3 — Layer B context-management: periodic orchestrator hard-reset + lossless rehydrate** (commits 9aa5a2c6 / 03f45b05 / ccc00f28 on this PR). Shipped: `actuateSessionContextReset` extraction + `createPooledContextResetSweep` (baseline-delta no-loop invariant, rotation-robust, auto-compact re-anchor, honest per-scope report, under-mutex `onResetUnderMutex` un-mark) in `context-reset.ts`; `gateway/wiring/context-reset-policy.ts` tick loop (5 min / 2 MB / 45 min cooldown); runner `contextResetSignal` seam + `topicScopes` + per-scope `contextResetEpoch` post-dispatch re-mark guard in `build-live-agent-turn.ts`; composer reset-signal bus wired to BOTH the policy loop AND the `/reset` thunk; tests + AS_BUILT + SYSTEM-OVERVIEW. Verified present on branch this pass.
- [x] **TASK A round 4 — close the CONFIRMED warm/cold reset race (Argus r3 + codex cross-model panel, converged blocker) + the /reset partial-failure rehydration gap.** (1) Dispatch-time epoch recheck in `gateway/wiring/build-live-agent-turn.ts`: the turn currently captures `resetEpochAtStart` at the `isColdFirstTurn` decision (:1150) and re-checks ONLY at the post-dispatch re-mark (:1361) — the dispatch itself (retry loop :1289 → `dispatchOnce()` :1260 → `substrate.start(spec)` :1261) re-sends the stale warm prompt built at :1151-1166. Fix: re-check `contextResetEpoch` at the top of EVERY dispatch attempt immediately before `dispatchOnce()`; if advanced and the turn is warm, recompose COLD via `composeFirstTurnPrompt` and re-anchor the snapshot so the post-dispatch re-mark stays correct; rebuild the `AgentSpec` per attempt so prompt + inactivity budget track the effective mode. Document the residual adapter-mutex window (sweep already holding the session mutex when a warm dispatch enters the adapter) as self-healing via the existing re-mark guard. (2) `/reset` partial-failure: `resetPooledSessionContext` (`context-reset.ts:109-131`) short-circuits `busy`/`reset_failed` mid-loop after earlier sessions were `/clear`-ed, without threading `onResetUnderMutex` (the sweep threads it, :424-431), and the composer thunk emits rehydration only on aggregate `outcome.ok` (`open/composer.ts:1483`) — an already-cleared session's topics stay warm-marked against an emptied REPL. Fix: `resetPooledSessionContext` gains `on_reset_under_mutex?`, threaded per session; composer thunk emits under-mutex per session, aggregate-ok emit removed. Race-simulation tests (regression-catching, not toHaveBeenCalled gap-tests) + gates + AS_BUILT + plan persist. Commit to the SAME branch, push updates PR #431.
- [x] **TASK B — CC TodoWrite ingestion: sync the agent's native multi-step TodoWrite todos into the Work Board** (branch `feat/todowrite-workboard-sync`, new PR vs main). DONE 2026-07-24. Observation seam BUILT = a NEW `PostToolUse` hook (matcher `TodoWrite`, sibling of the enforce-reply Stop hook) — `runtime/adapters/claude-code/persistent/hooks/todo-sync.ts` reads CC's structured `tool_input.todos` from stdin and POSTs it to the substrate reply-sink's new `/todo-sync` route (token-gated loopback, same as the tool-bridge). The route resolves the session's active project scope and reconciles into the SHARED `WorkBoardStore` via a late-bound `todoSyncRef` (wired in `gateway/composition/build-core-modules.ts` alongside `setReplToolBridge`), so a synced create/update fires the store's `onChange` live-push. Reconcile logic = pure `work-board/todo-reconcile.ts` (`reconcileTodosIntoBoard`, precedent `trident/board-reconcile.ts`): idempotent title-keyed diff (pending→upcoming / in_progress→in_progress / completed→done; re-running an unchanged list creates ZERO cards). Hook wired ONLY on the owner's warm conversational REPL (`enableToolBridge === true`), so Trident-build / history-import REPLs never leak their internal todos onto the board. Tests: `work-board/todo-reconcile.test.ts` (real store, idempotency + transitions + scope isolation), `runtime/.../__tests__/todo-sync-hook.test.ts` (real hook subprocess I/O against a stub sink), `build-settings.test.ts` (+PostToolUse wiring). AS_BUILT + SYSTEM-OVERVIEW updated. NO FEATURE FLAGS.
