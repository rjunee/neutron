# IMPLEMENTATION_PLAN — Host-deploy approval must be usable where it lands (Plan card 2026-08-15)

Governing spec: this Plan card. Verified against branch `trident/the-deploy-approval-is-unusable-whe` (T1 = `3384c23`, T2 = `1634814`, T3 = `d24debe`):

- T1 LANDED AND VERIFIED: `host_deploy_request` reads `ctx.topic_id` (`gateway/wiring/host-deploy-tool.ts`), `HostDeployService.request({ ref, topic_id })` resolves `approval_topic = topic_id ?? approval_topic_id` and uses it for BOTH the `tool_approvals` row and the emit; `pending_approval` carries `approval_topic_id` + a fallback `note`. Service/tool tests assert both seams independently.
- T2 LANDED AND VERIFIED: a dead-grant tap re-raises via claim-gated `cancelPending(id)` + `request({ ref, topic_id: input.topic_id })` (`reraise()`, `open/host-deploy.ts:762`); the swept-`expired` branch dedupes through `pendingGrantForRef()`; `up_to_date`/`refused`/`unavailable` re-raise outcomes read as one sentence each; `dispatch` fires zero times on any re-raise path. TTL stays 5 min (judgment call, noted in the T2 commit).
- T3 LANDED AND VERIFIED: `approve_value`/`deny_value` are computed ONCE in `request()` (`open/host-deploy.ts:685-686`) and flow into BOTH the `ButtonOption[]` and `renderHostDeployApprovalBody` — the printed fallback IS the tappable string; tests assert body ⊇ both option values and drive an approval end-to-end from a token extracted from the rendered body.
- MEASURED FACTS FOR T4 (next): `handleOwnerButtonAnswer` (`open/host-deploy.ts:799`) returns `null` at `:820` when `HOST_DEPLOY_VALUE_RE` matches but the value is not in `prior_option_values` — the raw `hdp:` token then falls through to the LLM as unexplained text. The comment block at `:808-818` names this seam explicitly ("a silence the owner has to interpret"). Every downstream branch already produces a correct owner-facing sentence for a real row (owner-only `:825`, malformed token `:835`, unknown row `:844`, already-approved `:858`, swept-expired-with-dedupe-and-reraise `:866`, claim-gated TTL reraise `:892-908`), so the fix is to compute eviction as a flag instead of early-returning null and to add ONE new claim-gated branch for the evicted-but-still-pending row, reusing `pendingGrantForRef()` + `reraise()` from T2. Cross-service tokens (`rap:`) fail the regex at `:819` and MUST stay null. The wiring seam (`gateway/wiring/build-live-agent-turn.ts:1211`) runs the capture whenever the topic's four-prompt window is non-empty, which the eviction scenario guarantees (the four evicting prompts fill it) — no wiring change needed.
- The four-prompt eviction seam is self-documented at `open/host-deploy.ts:808-818` (T4).
- `ApprovalManager.expireStale()` (`tools/approval.ts`) has no production caller; `open/wiring/approval-notifier.ts:69-83` pushes ONE `agent_message` frame to `registry.topics()` with no retraction; `landing/chat-react/controller.ts` appends `this.notices` after the durable transcript unconditionally with no dismissal path (T5/T6, measured).

- [x] **T1 — raise the Approve/Deny prompt on the topic that requested the deploy; the tool result names the destination.** DONE (`3384c23`): a request from `app:owner:<project>` puts its prompt on that topic; grant row topic and emit topic asserted independently; no-topic callers fall back with a `note` naming the destination; the tool description tells the agent to always say WHERE the button is.
- [x] **T2 — a dead-grant tap re-raises a fresh approval on the tap's topic instead of dead-ending.** DONE (`1634814`): TTL-expired and stale-sha taps claim the row via `cancelPending` (exactly-once, race-safe) and re-raise on `input.topic_id`; the swept-`expired` branch re-raises unless a pending grant for the same ref already waits (`pendingGrantForRef` dedupe); never an auto-approve — `dispatch` fires zero times on any re-raise path.
- [x] **T3 — print the typed fallback IN the approval body.** DONE (`d24debe`): the two option values (`hdp:<token>:a` / `hdp:<token>:d`) are computed ONCE in `request()` and passed into BOTH the `ButtonOption[]` and `renderHostDeployApprovalBody` (required fields `approve_value`/`deny_value`); the dead-end last line is replaced with the two exact strings that work; tests assert body⊇both values and approve end-to-end from a token extracted from the rendered body.
- [x] **T4 — an evicted prompt's tap must say so.** DONE: in `handleOwnerButtonAnswer`, a value matching `HOST_DEPLOY_VALUE_RE` but absent from `prior_option_values` sets `evicted` instead of returning null, so the existing owner/decode/row/status gates answer an evicted tap too (forged→"not recognized", unknown row→"no longer valid", approved→"deploy already went out", swept-expired→dedupe/reraise), and ONE new claim-gated branch retires the evicted-but-still-pending row via `cancelPending(id)` and either points at a waiting grant (`pendingGrantForRef`) or re-raises on `input.topic_id`; the loser of a claim reads the settled status. No evicted path dispatches; ordinary text and `rap:` tokens still return null; the `:808-818` seam comment now documents the closed silence rather than the open one.
- [ ] **T5 — `expireStale()` gets a real production caller, and a swept grant retires its UNTAPPED button prompt.** Wire a periodic sweep (loop-registry registered, alongside the approval module init in `gateway/composition/build-core-modules.ts` or the reminders tick) so a pending `tool_approvals` row past TTL transitions to `expired` without waiting for a tap. Link grant → prompt (return deliver's `prompt_id` through `HostDeployEmit` and persist the linkage) so the sweep can resolve the orphaned `button_prompts` row with a sentinel and stamp spent-ness (`AppWsAdapter.recordPromptChoice` fans `prompt_resolved` + stamps `chosen_value`) — a dead grant must not leave a live-looking button. The notifier must not announce a grant already past its TTL; an expired grant is distinguishable from an unanswered one in what the owner is told.
- [ ] **T6 — the approval banner must be dismissible and must not outlive its grant.** `open/wiring/approval-notifier.ts` sends a retraction frame (or the banner derives from the row) when a grant is decided/expired; `landing/chat-react/controller.ts` gives ephemeral notices a dismissal path and stops pinning them below messages that arrived after them (arrival-order sort, no unconditional tail). Assert: send a notice, then a user message, and the notice does NOT render last; a decided/expired grant's banner disappears without a page reload.
