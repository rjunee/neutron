# IMPLEMENTATION_PLAN — Git truth comes from git (Parts 1–3)

Regenerated 2026-08-15 against origin/main @ cd8215e. ⚠️ The local checkout (d8324cc) is STALE — every task below builds against current origin/main. The card branch trident/git-truth-comes-from-git-the-publis is already at cd8215e.

- [x] P1 — Outer publisher resolves the head itself: `publishBuiltCommit` runs `git rev-parse --verify refs/heads/<branch>` at the credentialed boundary; the branch NAME is the handoff (orchestrator.ts ~782, shipped in #283, reconciled onto the rebase-publish flow).
- [x] P1 — An agent-supplied sha is a CHECK, not a source: prefix-match against the resolved head; a disagreement fails naming both values verbatim ("the build reported commit 'X' but branch B resolves to 'Y'").
- [x] P1 — No sha / 7-char sha still publishes: `oidClaim` (inner-workflow.mjs ~2064) carries a 7–40-hex claim or null; `parseInnerResult.publish_head` decodes 7–40 hex; the forge-done and fix-round handoffs never throw on sha shape (inner-workflow.mjs:3810-3818, 3955-3961).
- [x] P1 — Zero commits ahead of the remote still fails: "the build left no new commits to publish" refusal, compared against the PRE-rebase local tip.
- [x] P2a — The resume live head is READ IN CODE by the launcher: `resolveResumeLiveHead` (orchestrator.ts ~627), 3 attempts, tri-state (40-hex / 'absent' / ''), threaded as `resume_live_head` → `resumeLiveHead`; the `head-probe-round-resume` agent seat is skipped whenever the launcher supplies it (fallback only for old launchers).
- [x] P2a — 'absent' (the authority says the branch is gone) is a real answer → rebuild as 'head-branch-absent', distinct from '' ("could not read").
- [x] Reason quality — infra-only stops carry a MEASURED `terminalCause`; `innerTerminalFailureReason` emits the specific reason only with that signal, generic otherwise; no "without Argus APPROVE" for failures Argus never reached.
- [x] P3 (pr mode) — the reviewed head is git truth end-to-end: the publisher's rev-parsed (post-rebase) OID is encoded in the `outer-published:<oid>:<remaining>:<round>` checkpoint; the re-fired workflow reviews exactly that recorded OID with a diff the publisher generated from it, never a re-probe.
- [x] P3 item 3 — `publishBuiltCommit`'s refusal message names the requested OID and the actual OID verbatim.
- [x] **P2b — A genuinely unreadable head is a bounded STOP, never a rebuild**: `classifyResume` returns `{ mode: 'stop', reason: 'head-unreadable' }` for `''`; the workflow call site writes a terminal failure naming the branch and the recorded OID ("could not read the head of <branch>; the recorded work is at <oid>; re-run when the read succeeds"), carrying blockKind 'infra-only' + terminalCause, and does NOT re-enter planning, build, or review. Guard tests at the classifyResume boundary AND driving the real workflow body. (Acceptance item 4, second half; run 2aa070d7's 133k-token rebuild is the measured cost this stops.)
- [ ] P3 residue — the shas recorded at build completion are still the MODEL's report: `forge-done`/`ralph-task-built`/`fix-round-N` checkpoint heads (inner-workflow.mjs 3809/3861/3953) and the whole LOCAL-mode `branchHead`/`reviewedHead` path (3785/3804/4015). Resolve them from git via a deterministic code seam (checkpoint.sh-style script rev-parses the branch itself, value travels by path/ref — never retyped; `forge.commitSha` stays a cross-check that fails loudly naming both values), so a local-mode build whose agent reported no sha is not converted into a "nothing was built" throw (3881-3888). Do NOT re-probe at review/merge time — read once at build completion (the load-bearing distinction in the spec).
- [ ] Orchestrator pre-fire short-circuit — when `resolveResumeLiveHead` returns `''`, stop the run at the outer boundary with the same bounded-stop reason instead of paying a workflow fire that will immediately stop itself (keep `classifyResume` as the single semantic decider; this is only the cheap fast-exit).
- [x] Recovery note (do not lose it): `trident/derive-inline-activity-from-evidenc` is now local == origin at 8407d9c with PR #263 OPEN — no unpushed work at risk. 9a46680 is preserved in the object store (not an ancestor; the branch was rebase-republished). Never reset, force past, or gc it.
