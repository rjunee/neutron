# IMPLEMENTATION_PLAN — A build must rebase onto main before review (stale branch ≠ rejected build)

Governing spec: this Plan card. Verified against main 2026-08-15 (`d8324cc`); resumed from branch `trident/a-build-must-rebase-onto-main-befor` (tip `7964853`). T1+T2 re-verified green this iteration: 59/59 tests across `trident/orchestrator.test.ts` + `trident/publish-rebase-realgit.test.ts`, run from a clean checkout of the branch tip.

- The readiness probe is repaired and honest: `classifyReviewReadiness` (`trident/inner-workflow.mjs:2123`) reads `mergeable: CONFLICTING` and defers the review round. Correct behavior, now exposing that NOTHING rebased a pr-mode branch — fixed by T1 below; the only other rebase machinery in the tree is local-mode merge-time (`merge.ts` + `conflict-resolver.ts`).
- The lease push already exists and is correct: `publishBuiltCommit` (`orchestrator.ts`) pins `--force-with-lease=refs/heads/<b>:<sha>` to an `ls-remote`-OBSERVED sha, never the remote-tracking ref; third-party-advance refusal is asserted at the fake-host level (`orchestrator.test.ts`). Acceptance (c)'s mechanism is DONE at the fake-host level; (c) still needs a REAL-remote assertion (T2).
- DECIDED — WHEN: the rebase lives in the OUTER publisher, between the local-tip verification and the lease observation/push. Only the outer loop holds a push credential (the Forge sandbox strips `*TOKEN*`), so "rebase + re-push before the readiness probe" can only happen there; and because the publisher fires after EVERY build/fix round and before EVERY review re-fire, every fix round re-enters a branch already based on current main — post-round-1 code IS written against the tree it merges into, the closest to "before build" the credential boundary allows.
- DECIDED — MECHANISM (shallow-safe, governance #574/#571): never `git rebase` in the shared checkout (`.git/shallow` boundary makes merge-base lie). Replay the branch's own diff onto the OBSERVED base tip in a throwaway detached worktree: PR exists → `gh pr diff <n>` (server-side merge-base, shallow-immune); first publish → two-dot `git diff <base>..<branch>` against the local base ref the branch was cut from. `git apply --3way`, commit, move `refs/heads/<branch>` by compare-and-swap (`update-ref new old`). The replay squashes the branch into one commit — acceptable, the PR merge is `--squash` anyway.
- DECIDED — ON CONFLICT: a failed 3way apply is an ATTENTION state, never `REQUEST_CHANGES` and never auto-resolved: fail the run with a `TridentRebaseConflict` whose reason starts `REBASE CONFLICT — needs attention:` naming branch, base, and the conflicting paths (`diff --name-only --diff-filter=U` in the scratch worktree), stating no reviewer judged the code. The `resolve_conflict` Forge resolver is NEVER invoked on this path (it belongs to local-mode merges).

- [x] **P0 — lease-pinned publish**: `--force-with-lease` pinned to an observed sha, empty-lease first push, third-party refusal, git's own words in the failure (merged; `orchestrator.ts`, tests `orchestrator.test.ts`).
- [x] **P0b — honest readiness probe**: `CONFLICTING` → deferred review, no seat dispatched, no round consumed (merged; `inner-workflow.mjs:2123-2187`, `reviewPreconditionDeferred`). The condition it reports is what this plan removes.
- [x] **T1 — shallow-safe rebase-onto-observed-main in the outer publisher (+ conflict = attention state), with fake-host unit tests.** DONE on branch (commit `022797e`): exported `rebaseOntoObservedBase(...)` + `TridentRebaseConflict` in `orchestrator.ts`; wired into `publishBuiltCommit` after the local-tip check and before the lease observation; rebased head flows into the push, the post-push confirm, the review diff, and the re-fire checkpoint; PR probe hoisted so the replay can use `gh pr diff`; scratch worktree always torn down. 56/56 tests pass in `orchestrator.test.ts` (behind → rebased publish; current → no-op; conflict → attention; lease ordering preserved).
- [x] **T2 — REAL-git + REAL-shallow integration test (`trident/publish-rebase-realgit.test.ts`), modeled on `merge-realgit.test.ts`, guarding acceptance (a)/(c)/(d) against fake tests.** Seed a real origin whose `main` moved by a genuine non-conflicting commit AFTER the branch was cut (the anti-fake seed for (a), with an explicit behind-ness assertion in a FULL clone); the build checkout is `git clone --depth=1 file://…` and the test ASSERTS `.git/shallow` exists before exercising anything (the anti-fake guard for (d)). Assert: `rebaseOntoObservedBase` replays the branch onto main's tip with no false conflict, `rebased === true`, the new head's tree contains BOTH the branch's file and main's intervening file, and after the lease push a FULL clone proves `merge-base --is-ancestor <mainTip> origin/<branch>` (the MERGEABLE fact the probe will read); a second invocation is a no-op (`rebased === false`); a genuinely conflicting seed throws `TridentRebaseConflict` naming the path, leaves the branch ref unmoved and the scratch worktree removed, and never says `REQUEST_CHANGES`; a third-party push to the branch from a second clone makes the pinned lease push REFUSE against the real remote (real (c)).
- [x] **T3 — docs: record the decision + as-built.** Append the dated entry to `docs/AS_BUILT.md` (newest-first); extend the "THE PUBLISH BOUNDARY" bullet in `docs/SYSTEM-OVERVIEW.md` (~line 2886) with the rebase-before-lease ordering, the shallow workaround (#574), and conflict-as-attention; best-effort refresh of the project-root `STATUS.md` frontmatter (it lives OUTSIDE the code repo — skip with a note if not writable from the build worktree).
