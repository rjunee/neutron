# IMPLEMENTATION_PLAN — Credentials orphaned by a changed owner handle: migrate at boot, and stop calling it "not connected" (card 2026-08-14)

Governing spec: this Plan card (owner-approved design 2026-08-14 — unambiguous ⇒ auto-migrate + audit; ambiguous ⇒ touch nothing, surface `orphaned` + a migrate action; NEVER refuse boot). Verified against main 2026-08-14; T1 landed @ 30a65ed and T2 @ c936f85 on `trident/credentials-orphaned-by-a-changed-o`:

- The #451 rename reconciler (`migrations/scope-rekey.ts`, wired at `gateway/index.ts:283-342`) already sweeps `secrets.project_slug` and `project_credentials.owner_slug` — but ONLY when the `instance_scope_ledger` disagrees with the boot slug. On this box the ledger was seeded as the boot slug at first boot, so rows written pre-provisioning under `dev` are invisible to it forever: the every-boot path is a single SELECT noop. This card closes that gap.
- `SHARED_KEY_ENCRYPTED_TABLES` (`auth/secrets-store.ts:79`) = `['secrets', 'project_credentials']`; scope columns are `secrets.project_slug` and `project_credentials.owner_slug`. `api_keys.project_slug` is the metadata twin the BYO-key read path keys off — swept as a companion table.
- Collision keys (for the explicit migrate action's never-clobber guard): `secrets` UNIQUE(project_slug, kind, label) — migration 0009; `project_credentials` UNIQUE(owner_slug, project_id, service) — migration 0092; `api_keys` UNIQUE(project_slug, provider, label) — migration 0009.
- Audit precedent: boot journals `credential_scope_migrated` / `credential_scope_orphaned` through `SystemEventsStore` (`persistence/system-events.ts`); ambient sink via `resolveSystemEventSink` (`@neutronai/persistence/index.ts`).

- [x] (pre-existing half) 2026-05-12 rename-canonicalisation fix — the store keys on the FROZEN handle, branded `OwnerHandle` at the type level (`auth/secrets-store.ts` header, `PutInput`/`GetInput`). Prevents a RENAME orphaning rows; does nothing for rows frozen under a pre-provisioning handle.
- [x] (pre-existing half) #451 boot scope reconciler + ledger (`migrations/scope-rekey.ts`, migration 0114) — repairs the ledger-disagrees case only; correct as-is, not touched by this card.
- [x] **T1 — boot-time credential-scope reconciler (the core of the card).** DONE @ 30a65ed: `auth/credential-scope-reconcile.ts` (census across `SHARED_KEY_ENCRYPTED_TABLES` + `api_keys` companion; UNAMBIGUOUS ⇒ one-transaction metadata move, ciphertext untouched; AMBIGUOUS ⇒ zero writes + orphan counts), wired into `gateway/index.ts` after the #451 block inside a never-fail-boot try/catch, journaling `credential_scope_migrated` / `credential_scope_orphaned` (`persistence/system-events.ts`). Tests: `auth/__tests__/credential-scope-reconcile.test.ts` + `gateway/__tests__/boot-credential-scope.test.ts` pin acceptance (a), (a2 rotation hazard, asserted on the decrypted value), (a3 real `boot()` never refuses), (c both tables + api_keys), (d no secret material), and the delete-the-migration mutation guard.
- [x] **T2 — a miss is DISTINGUISHABLE (acceptance b).** DONE @ c936f85: `buildIntegrationsStatus` (`gateway/cores/integrations.ts`) gained `orphaned_credentials` summary (per-table/per-handle counts via read-only `censusCredentialScope` + `listOrphanedSecretSlots` exported from `auth/credential-scope-reconcile.ts`; no decrypt) and per-slot `orphaned: true` ("scoped to a previous handle", never bare `connected:false`); `MIGRATE_ORPHANED_ACTION` const names the way out. `db` threaded through both call sites (`gateway/http/cores-integrations-surface.ts`, `gateway/cores/integrations-tools.ts`). Tests: `gateway/cores/__tests__/integrations-orphaned.test.ts` (orphan reported, positive control connected under correct handle, genuinely-unconnected control, rotation hazard from the read side, `project_credentials` in summary, no plaintext in serialized status).
- [x] **T3 — the migrate ACTION for the ambiguous case (last task).** Explicit-mode move routine `migrateOrphanedCredentialScope` in `auth/credential-scope-reconcile.ts` (per-table collision keys; move every orphaned row whose UNIQUE slot is free under the boot handle, SKIP + count any row that would collide — an explicit action must still never overwrite a fresh credential); shared-brain `migrateOrphanedCredentials` in `gateway/cores/integrations.ts` journaling `credential_scope_migrated` with moved/skipped counts; exposed as agent tool `integrations_migrate_orphaned` (`approval_policy: 'prompt-user'`) in `integrations-tools.ts` AND `POST /api/cores/integrations/migrate-orphaned` on `gateway/http/cores-integrations-surface.ts`, both through the one brain. Tests: colliding row skipped AND the fresh decrypted value survives (assert the value, not the row count); non-colliding rows across all three tables move and read back decrypted; response + journal carry no secret material; tool + HTTP route wired and auth-gated.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
