# IMPLEMENTATION_PLAN — Host-deploy approval must be usable where it lands (Plan card 2026-08-15)

Governing spec: this Plan card. Verified against branch `trident/the-deploy-approval-is-unusable-whe` (T1 committed as `3384c23`):

- T1 LANDED AND VERIFIED: `host_deploy_request` reads `ctx.topic_id` (`gateway/wiring/host-deploy-tool.ts`), `HostDeployService.request({ ref, topic_id })` resolves `approval_topic = topic_id ?? approval_topic_id` and uses it for BOTH the `tool_approvals` row and the emit (`HostDeployEmit.topic_id`); the composer's emit delivers to `p.topic_id` (`open/composer.ts:2646`); `pending_approval` carries `approval_topic_id` + a fallback `note`. Service/tool tests assert both seams independently.
- `HOST_DEPLOY_APPROVAL_TTL_MS = 5 * 60_000` (`open/host-deploy.ts:143`) is enforced on the ANSWER only (`:793-801`); the TTL/stale/expired tap paths dead-end with "Ask again" — the re-raise the card demands does not exist yet (T2).
- MEASURED SEAM FACTS FOR T2: `ApprovalManager.cancelPending` returns true iff THIS call transitioned the pending row (`tools/approval.ts:293-310`) — the exactly-once gate for a re-raise; `findByToolName(project_slug, tool_name)` (`tools/approval.ts:274`) enables a "fresh grant already waiting" dedupe. A TAPPED prompt row is already resolved/spent by the #419 claim (`gateway/wiring/build-button-prompt-claim.ts` → `ButtonStore.resolve` was_new gate + `AppWsAdapter.recordPromptChoice` prompt_resolved fan), so the live-looking-dead-button case that remains is the UNTAPPED prompt — which only a sweep can retire; that belongs with T5's `expireStale()` caller, not T2.
- The body's last line (`host-deploy.ts:459`) still says typing won't work; the `hdp:` token appears only in `button_prompts.options_json` — no surface renders it (T3, measured).
- The four-prompt eviction seam is self-documented at `host-deploy.ts:738-743` — an evicted token returns null and falls through to the LLM (T4).
- `ApprovalManager.expireStale()` (`tools/approval.ts:187`) has no production caller; `open/wiring/approval-notifier.ts:69-83` pushes ONE `agent_message` frame to `registry.topics()` with no retraction; `landing/chat-react/controller.ts` appends `this.notices` after the durable transcript unconditionally with no dismissal path (T5/T6, measured).

- [x] **T1 — raise the Approve/Deny prompt on the topic that requested the deploy; the tool result names the destination.** DONE (`3384c23`): a request from `app:owner:<project>` puts its prompt on that topic; grant row topic and emit topic asserted independently; no-topic callers fall back with a `note` naming the destination; the tool description tells the agent to always say WHERE the button is.
- [x] **T2 — a dead-grant tap re-raises a fresh approval on the tap's topic instead of dead-ending.** In `handleOwnerButtonAnswer` (`open/host-deploy.ts`): parse `args_json` (pure, no side effects) BEFORE the status gate so `ref` is available on every dead-end branch. TTL-expired tap: replace `cancel(id)` with claim-gated `cancelPending(id)` and, on the claim, re-run `request({ ref, topic_id: input.topic_id })` — the fresh prompt lands where the owner just tapped — and append one sentence naming it. Stale-sha refusal: same claim-gated re-raise (fresh approval binds to the NEW sha). `row.status === 'expired'` (a swept or previously-refused grant): re-raise ONLY when no pending host-deploy grant for the same ref already exists (`findByToolName` + status/ref filter); otherwise point at the waiting prompt — no prompt spam on repeat taps. Re-raise `up_to_date`/`refused`/`unavailable` outcomes read as one sentence each. NEVER an auto-approve: the fresh grant needs its own owner tap; `dispatch` fires zero times on any re-raise path. TTL constant stays 5 min — with T1 landing the prompt where the owner is and a stale tap now one tap from recovery, the window fits the delivery (judgment call, noted).
- [ ] **T3 — print the typed fallback IN the approval body.** Replace the "Typing anything else will NOT approve this deploy" dead-end (`renderHostDeployApprovalBody`, `host-deploy.ts:459`) with the two exact strings that DO work (`hdp:<token>:a` / `hdp:<token>:d`), so the one string that works when buttons are unreachable is visible to a human, not only to the database. Requires threading the minted token into the render call (`request()` builds both).
- [ ] **T4 — an evicted prompt's tap must say so.** In `handleOwnerButtonAnswer`, when `HOST_DEPLOY_VALUE_RE` matches but the value is NOT in `prior_option_values`, return a sentence explaining the prompt aged out of the answer window and (per T2's guard) re-raise or point at the waiting prompt — never return null and let a raw `hdp:` token fall through to the LLM as unexplained text. Closes the seam the code documents at `host-deploy.ts:738-743`. NOTE the interaction with the forged-token test: eligibility must still refuse tokens that were never offered — distinguish "matches the shape but not offered recently" (explain) from cross-service tokens (`rap:`) which stay null.
- [ ] **T5 — `expireStale()` gets a real production caller, and a swept grant retires its UNTAPPED button prompt.** Wire a periodic sweep (loop-registry registered, alongside the approval module init in `gateway/composition/build-core-modules.ts` or the reminders tick) so a pending `tool_approvals` row past TTL transitions to `expired` without waiting for a tap. Link grant → prompt (the composer's emit already receives deliver's `prompt_id`; return it through `HostDeployEmit` and persist the linkage) so the sweep can resolve the orphaned `button_prompts` row with a sentinel and stamp spent-ness (`AppWsAdapter.recordPromptChoice` fans `prompt_resolved` + stamps `chosen_value`) — a dead grant must not leave a live-looking button when nobody ever tapped it. The notifier must not announce a grant already past its TTL; an expired grant is distinguishable from an unanswered one in what the owner is told.
- [ ] **T6 — the approval banner must be dismissible and must not outlive its grant.** `open/wiring/approval-notifier.ts` sends a retraction frame (or the banner derives from the row) when a grant is decided/expired; `landing/chat-react/controller.ts` gives ephemeral notices a dismissal path and stops pinning them below messages that arrived after them (arrival-order sort, no unconditional tail). Assert: send a notice, then a user message, and the notice does NOT render last; a decided/expired grant's banner disappears without a page reload.
