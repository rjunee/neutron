# IMPLEMENTATION_PLAN.md — Executor-Mode Reminders (P0-1 M2 PARITY, Neutron Open)

Spec of record: `docs/plans/executor-mode-reminders-2026-07-20.md` — the DEEPENED header block (lines 1-150) SUPERSEDES the original plan below it wherever they conflict. Honor the three Ryan overturns (SPEC Decisions Log 2026-07-20, neutron-managed): (1) Bash ports via approval, not exclusion; (2) dreaming's uncovered half goes into CORE MEMORY (split by tier: backlink repair = event-driven on sync hook; correction-promotion = reflect pass; daily-delta = stays a ritual); (3) ritual registration is AGENT-CALLABLE with in-chat approval — the approval RENDERING carries the security. NO FEATURE FLAGS — build the real default directly. One task per iteration; commit + PR every iteration.

Planner corrections to the plan doc (re-verified 2026-07-20, iteration 3):
- Migration numbering: **next free is 0106** (`migrations/0105_work_board_items_task_type.sql` is last; runner sorts `^\d{4}_.+\.sql$`, `migrations/runner.ts:23-25`).
- `AgentKind` is at `runtime/subagent/registry.ts:20` (plan said :21). Widening it is COMPILE-SAFE: only non-test consumers are `Partial<Record<AgentKind,...>>` (`runtime/subagent/watchdog.ts:102`, `agent-dispatch/prompts.ts:56`) — no exhaustive switch breaks.
- No FK / later migration references `code_subagent_registry` (grep clean) — the CHECK-widening create-copy-drop-rename rebuild is self-contained; recreate BOTH indexes (`idx_code_subagent_registry_live` partial, `idx_code_subagent_registry_owner`).
- `migrations/table-ownership.json` is OPT-IN coverage (currently `projects`, `onboarding_state`, `gbrain_sync_state`; `reminders` unmapped — store.ts edits are conformance-neutral). New `code_ritual_runs` gets an entry with `reminders/ritual-runs.ts` as sole writer.
- `reminders/` tests are COLOCATED (`store.test.ts`, `tick.test.ts` — no `__tests__/` dir).
- Local worktree `.trident-wt/executor-mode-reminders` may be BEHIND origin (origin has 3 commits, head `d60d57ea`) — hard-reset the worktree branch to `origin/trident/executor-mode-reminders` before any work.
- SPEC.md line citations inside the plan doc (":369-372", ":353-367") have DRIFTED (SPEC.md is a rolling log). The plan doc itself is the spec of record for this build.
- Grep-zero for "ritual" across `reminders/ runtime/ open/ cores/free/reminders/ scribe/ migrations/` on the branch (sole hit = unrelated 0025 onboarding phase-rename) — tasks 2+ start from scratch.

- [x] 0. Design doc deepened + Ryan review complete (plan doc STATUS: COMPLETE; three overturns locked in neutron-managed SPEC Decisions Log 2026-07-20; four-review findings folded into the header block)
- [x] 1. **M2-3 / P0-4 — collapse `NEUTRON_PERFECT_RECALL`; memory consolidation 6h ON by default.** DONE on branch (commits 5f7e4d26, 180ef993, d60d57ea): `runtime/perfect-recall-flag.ts` + test deleted; all four gate sites in `open/wiring/memory.ts` unconditional; `WiredMemory` types tightened; scribe `supersede` option removed (always-on); `DEFAULT_REFLECT_INTERVAL_MS` 24h→6h; `nexus-emit.ts` re-export deleted; 8 test files + docs updated; AS_BUILT + SYSTEM-OVERVIEW entries landed.
- [x] 2. **Ritual schema + registry module.** Migration **0106**: nullable `ritual_id` TEXT on `reminders` (precedent 0095 `recurrence_spec`) + new `code_ritual_runs` run-history table with own retention (the subagent registry is a liveness table — rows deleted on prune, `runtime/subagent/store.ts` remove()) + widen `agent_kind` CHECK to include `'ritual'` via create-copy-drop-rename (closed CREATE-time enum, `migrations/0100_code_subagent_registry.sql`) + regenerate `migrations/expected-schema.txt` (`bun run migrations/regen-snapshot.ts`) + `code_ritual_runs` ownership entry. Add `'ritual'` to `AgentKind` (`runtime/subagent/registry.ts:20`). New `reminders/rituals.ts`: `RitualDef` (id charset-guarded; prompt derived `<root>/rituals/<id>.md`; scope 'project'|'instance'; tool_surface non-empty, egress/bridge as capability booleans; silent; model tier + timeout as CONSTANTS — 'best', 45min; **NO `requires_approval` field** — approval lives in a separate record, task 3) + `validateRitualFire` with fail-CLOSED injected approval seam: unknown ritual_id / missing prompt / unapproved → SKIP verdict (never degrade-to-nudge, never `tools: []` — the #361 toolless class). New `reminders/ritual-runs.ts` store (async mutex-serialized ProjectDb.run writes — the runSync bypass hazard). `ritual_id` plumbing through `reminders/store.ts` types + INSERTs + rowToReminder (no behavior change). Tests: T1 registry/validation slice + store round-trips + migration CHECK assertions.
- [ ] 3. **Approval-gate infrastructure (content-hash bound).** Extend `tools/approval.ts` (state machine + `tool_approvals` migration-0004 rows exist; the composer's notifier is a no-op stub — wire it, do NOT ship a second parallel mechanism). Approval record keyed `(ritual_id, content_hash, approved_by, approved_at)`; hash = prompt bytes ‖ tool surface ‖ scope root ‖ cadence ‖ model tier ‖ timeout; **re-verified at EVERY fire** (ported prompts are mutable files) — implements task 2's `isApproved` seam; `reminders_update` mints a new id (`cores/free/reminders/src/mcp-tools-extra.ts:64`) so hash-keying makes update drop approval — assert it; cadence change requires re-approval (or hard floor + per-ritual daily fire cap); egress (WebSearch/WebFetch) is a separately-approved capability class.
- [ ] 4. **Executor dispatch branch in the TICK** (`reminders/tick.ts` — NOT dispatcher.ts; the dispatcher's only-throws-before-delivery contract :271-282 stays nudge-only). Ritual rows: after the #319 claim, resolve+validate → spawn fire-and-forget via `spawnSubagent` (`runtime/subagent/spawn.ts:67`) kind `'ritual'` — NOT DispatchService (throws `missing_board_item`, `agent-dispatch/service.ts:296-300`); new ephemeral `cc-ritual-*` substrate factory (current `makeEphemeralSubstrate` hardcodes `skip_permissions:true` + no tool bridge, `open/wiring/substrates.ts:296` — needs a variant); thread `appendSystemPromptFile` (prerequisite — unthreaded, a ritual REPL runs as the CHAT agent and inherits the enforce-reply Stop hook); separate ritual concurrency lane (don't starve `MAX_CONCURRENT_SUBAGENTS=8`, `runtime/subagent/registry.ts:16`); **SKIP `on_fired` for executor rows** (tick.ts:210-219 would push-notify up to 45min before output, even for silent rituals); record spawned/skipped rows into `code_ritual_runs`; never await. Tests: T2 fire-and-forget + nudge-contract untouched.
- [ ] 5. **Completion delivery + failure surfacing.** Terminal event → mark the `code_ritual_runs` row + post final text through the one `deliver()` seam to the resolved topic; `silent` skips the post; failed/timed-out/crashed → one-line failure notice (ritual id, status, run id); 3-consecutive-failure escalation notice; boot-reap orphans → `crashed` + notice; wire the run-history retention prune. Tests: T3 behavioural completion (artifact-on-disk + durable history row + silent + failure-notice variants), T6 crash surfacing.
- [ ] 6. **T5 write-containment spike — HARD GATE for every writing/Bash ritual (incl. all ported Vajra rituals per overturn 1).** Prove a per-session settings.json deny rule fails CLOSED (run terminates; outside write ABSENT; no wedge) in the headless PTY, ACCOUNTING for the substrate auto-approver that presses yes on permission prompts (`runtime/adapters/claude-code/persistent/spawn.ts:239-245`, `signatures.ts:89-90` incl. `runthiscommand`) — the ritual REPL must disable that scanner or the test is theater (the plan's original T4 would have false-passed). Record the verdict in the plan doc. If fail-closed is unprovable → OS-level sandbox becomes its own prerequisite sprint and Bash/writing rituals stay gated.
- [ ] 7. **Bundled generic read-only example rituals** (ENGINE seeds — Ryan's specific ritual content is USER DATA via his Vajra import, never hardcoded): generic morning-brief + evening-wrap templates, surface `['Read','Glob','Grep']`, no Bash — Layer 1 (`--tools` default-deny, verified: `build-repl-argv.ts:52-60`) alone contains these. T7 per-ritual acceptance test each (output must reference real fixture state — ported-prompt silent-no-op guard).
- [ ] 8. **Q3 — agent-callable registration with in-chat approval.** Reminders-core MCP tools to propose/register; approval via ButtonStore persisted option-set binding (the `captureButtonBackedRequiredField` precedent, `onboarding/interview/button-backed-answer.ts:198` — eligibility only from `prior_agent_options`, `:207-209`): opaque proposal_id; approval is an explicit affirmative act; no self-approval (`resolution_speaker_user_id`, `channels/button-store.ts:106` — resolver must be owner); render preformatted (never Markdown — button body is Markdown today, `channels/button-primitive.ts:193`), never truncate (refuse over hard cap), NFC-normalize, reject bidi/zero-width, itemize URLs/paths/skills; describe CAPABILITY not tool names; no register-and-fire in one turn; no surface-widening without re-approval. Test T8: owner replies something UNRELATED → ritual stays pending_approval, never dispatches.
- [ ] 9. **Q2 — dreaming's uncovered half into core memory** (overturn 2, split by tier): (a) deterministic entity backlink repair EVENT-DRIVEN on the entity-writer sync hook (writer already emits `newLinks`/`removedLinks` — `runtime/entity-writer.ts` EntityWriteOutput); (b) correction-pattern promotion folded into `runReflectPass` (the 6h loop task 1 turned on); (c) daily-delta notes as a bundled ritual (time-anchored — nothing in memory triggers it). Explicit toHaveBeenCalled/artifact assertions each.
- [ ] 10. **Docs + close-out.** `docs/AS_BUILT.md` entries per landed task; `docs/SYSTEM-OVERVIEW.md` new "Ritual executor" subsystem section (+ reminders section update, consolidation-cadence update); `reminders/AGENTS.md` refresh; final summary citing the executor branch file:line + approval-rendering file:line + T5 verdict.
