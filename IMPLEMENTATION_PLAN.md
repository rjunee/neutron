# IMPLEMENTATION_PLAN — A dev-slug boot must never steal the live credential scope: the scope-rekey needs a DIRECTION GUARD

Governing spec: this Plan card (defect 2026-08-14 19:35 — a `NEUTRON_INSTANCE_SLUG`-unset boot with `NEUTRON_HOME` inherited rekeyed every live credential row `juno → dev`; a later explicit boot moved them back; the owner pressed ▶ inside the window and the frozen-handle gateway read zero secrets). Verified against origin/main `ae4c7bb` and branch `trident/a-dev-slug-boot-must-never-steal-th` (`010924e`) on 2026-08-14:

- The base reconciler (#451, `migrations/scope-rekey.ts`) is on main: ledger-agree → noop; ledger-disagree/absent → snapshot then rekey-forward inside one `BEGIN IMMEDIATE`. Its SYMMETRY was the defect.
- PR #266 (credential-scope orphan migration, same file) is STILL NOT on main (checked 2026-08-14; the three new main commits since branch cut — `f0c0c7a`, `a84d6cb`, `ae4c7bb` — do not touch `migrations/scope-rekey.ts`). Per the owner's directive: keep the guard narrow and additive; if #266 lands before PR-open, rebase and place direction-check before its safe/ambiguous split — direction first, safety second.
- Existing tests to keep green untouched: `migrations/__tests__/scope-rekey.test.ts`, `scope-sweep-coverage.test.ts`, `open/__tests__/open-scope-rekey-boot.test.ts`.

- [x] **T0 — base scope reconciler (#451): ledger + snapshot + forward rekey** (on main; prior card, not this one).
- [x] **T1 — the direction guard + slug-provenance threading + unit suite (incl. the flip-flop decrypted-token test).** DONE on branch (`010924e`): `gateway/index.ts` gained `resolveOwnerSlugSourceFromConfig` (bit-for-bit same precedence, plus `source: 'file' | 'env' | 'fallback'`) and threads `currentSlugIsFallback` into the reconcile call; `migrations/scope-rekey.ts` gained an additive guard after stale-key discovery and BEFORE snapshot/transaction: fallback slug + stranded non-fallback rows ⇒ `action: 'noop'` with `refused_direction`, one loud `scope_rekey_refused_fallback_direction` log line, zero writes, NO snapshot, boot never refused; `persistence/system-events.ts` gained the `instance_scope_rekey_refused` event and the gateway journals it. `migrations/__tests__/scope-rekey-direction-guard.test.ts` pins all five acceptance bullets, asserting the DECRYPTED GitHub token via a real `SecretsStore`.
- [x] **T2 — boot-level wiring regression.** New `open/__tests__/open-scope-rekey-direction-boot.test.ts` through the real `boot()` with the production Open composer (mirroring `open/__tests__/open-scope-rekey-boot.test.ts`): seed a home with a completed onboarding row and a real-crypto GitHub token under `juno`, boot explicit `juno` → boot with `NEUTRON_INSTANCE_SLUG` DELETED → boot explicit `juno` again, all against one database. Assert every boot SUCCEEDS (no refuse-to-boot), secrets/onboarding rows and the ledger never leave `juno`, zero `project.db.pre-rekey-*` snapshots ever appear, `readGitHubToken` returns the original plaintext after every boot, and — positive evidence the flag was threaded at the composition root, not merely that nothing moved — a `system_events` row `instance_scope_rekey_refused` exists after the fallback boot and not before. This is the only test that can catch the flag silently not being threaded in `gateway/index.ts:boot()`.
  - DONE: `open/__tests__/open-scope-rekey-direction-boot.test.ts` — green on the branch as-is, and MUTATION-TESTED (stubbing `currentSlugIsFallback: false` at `gateway/index.ts:347` fails it with `secrets` moved `juno → dev`, reproducing the outage exactly).
- [ ] **T3 — docs + #266 composition check.** Add the one-line invariant to `docs/INVARIANTS.md` (guard anchor + protecting test); immediately before opening the PR, re-check whether #266 merged to main and, if so, rebase and compose (direction evaluated first) rather than duplicating its decision. Append the `docs/AS_BUILT.md` entry.

- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
