# IMPLEMENTATION_PLAN — Derive inline activity from evidence (Plan card, owner-approved 2026-08-14)

Governing spec: this Plan card. **NOT a gate** — nothing here may block, deny, or delay any tool call (the PreToolUse-gate plan is cancelled; this salvages Part 3 alone). Verified against the tree 2026-08-14; T1 (52106ab) and T2 (04d076f) are committed on this branch — do NOT redo them:

- The EMITTER side exists and needs no work: `runtime/adapters/claude-code/persistent/hooks/activity-tap.ts` POSTs every Pre/PostToolUse to the sink's `/activity` route; the composer's tap closure records each row into the in-process `ActivityInspector` keyed by `inspectorScopeKey(project_id)` — with the two-clock design where `last_real_activity_at` EXCLUDES synthetic keepalives. Every tool call is already observed, per project scope, in the same process that serves the board.
- CONSUMER progress: T1 shipped the pure `deriveInlineActive` + `INLINE_EVIDENCE_WINDOW_MS=90_000` (`work-board/inline-activity.ts`, rules R1–R4, mutant-pinned) and the O(1) `ActivityInspector.lastRealActivityAt(scope)` accessor. T2 shipped the `withDerivedInlineActive(items, reader, scope, now)` batch helper (ONE evidence read per board), the late-bound `inlineEvidenceReader` holder in `open/composer.ts` (defined ~:2221 before the closures, bound to the inspector at ~:4008), and mapped BOTH composer read boundaries — the rail extras (`scanItemsForRailSignals` input, ~:2245) and the WS `work_board_changed` frame (~:3679) — plus wiring tests + a composer source mutant pin (`open/__tests__/inline-activity-wiring.test.ts`).
- STILL RAW: the HTTP surface (`gateway/http/work-board-surface.ts` — list GET :224, reorder :493, and the create/update/complete item echoes) and the agent-facing per-turn fragment / `work_board_list`. Both clients' dot/`canPlay` read the wire field, so healing these server boundaries needs ZERO client changes.
- DESIGN DECISION (state in the PR): derive SERVER-SIDE at read/serialization boundaries; the wire field `inline_active` becomes the derived value, the stored column stays a hint. Evidence tier is **tier 1 only** (inspector clocks — one O(1) in-memory Map read per board render, never per row, never a shell-out). Tiers 2–3 (branch commits, dirty worktree) are recorded non-goals in the T1 module doc (acceptance e). Restart semantics are free and correct: the inspector buffer dies with the process, so after a crash evidence reads 0 and every stale flag reads NOT active — acceptance (b) by construction. Note `GENERAL_WORK_BOARD_PROJECT_ID === 'general' === GENERAL_SCOPE`, so the URL `project_id` feeds `inspectorScopeKey` directly, General included.

- [x] **T1 — the pure derivation + the cheap evidence accessor.** LANDED (52106ab): `work-board/inline-activity.ts` (`deriveInlineActive` with ordered rules — terminal→false; run-bound→false; stale/absent evidence→false, evidence WINS over the flag; fresh evidence → active iff hint OR `in_progress`) + `ActivityInspector.lastRealActivityAt(scope)` O(1) accessor + mutant-pinned tests (unconditional-true mutant, the latch, the keepalive mutant, the exact window boundary).
- [x] **T2 — composer wiring: rail + WS frame read the derived value.** LANDED (04d076f): `withDerivedInlineActive` batch helper; late-bound `inlineEvidenceReader` holder (the `buildClarifyPoster` pattern — unset holder ⇒ evidence 0 ⇒ not active, fail-soft); both composer call sites mapped; late binding after inspector construction; wiring tests incl. the crashed-session heal and the composer source mutant pin.
- [x] **T3 — HTTP surface wiring.** `gateway/http/work-board-surface.ts`: optional injected BATCH dep `derive_inline_active?: (items: WorkBoardItem[], project_id: string) => WorkBoardItem[]`; map EVERY item-bearing response through it (list GET, reorder list, create/update/complete echoes). Absent dep ⇒ raw flag passthrough (test-compat, degraded not broken). Composer passes the dep as a closure over `inlineEvidenceReader` + `inspectorScopeKey` (deref at call time — `createWorkBoardSurface` at :3941 runs BEFORE the inspector exists, the holder pattern already handles this). Surface tests: acceptance (a) fresh-evidence activation with the stored row untouched, (b) stale-flag heal, (c) quiet-means-quiet no-latch, (d) no route gains a denial shape, (e) reader-called-once-per-request spy; extend the composer source mutant pin to require the `derive_inline_active:` wiring.
- [ ] **T4 — agent-facing truth + comment/doc pass.** Feed the derived value to the per-turn fragment call site (the `workBoardSnapshot` seam feeding `work-board/fragment.ts`) and agent `work_board_list` so the AGENT also stops trusting the flag; update the now-resolved STALENESS CAVEAT comments in `app/lib/work-board-helpers.ts` `canPlay` and `landing/chat-react/WorkBoardTab.tsx` (the server now heals the wire value; clients unchanged); state the tier-1-only cost decision (acceptance e) in `docs/AS_BUILT.md`.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
