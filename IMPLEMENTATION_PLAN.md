# IMPLEMENTATION_PLAN — Work Board card removal: agent-exposed, run-cancelling, reason-carrying, doc-disposing (+ a shelved lane)

Governing spec: this Plan card (agent remove through the UI's own path; archived ≠ done; removal takes a reason and the plan doc's disposition follows it; removed = really gone). Verified against branch `trident/the-agent-must-be-able-to-remove-a` (d9987a9) 2026-08-14:

- MEASURED (T1 landed): `work-board/removal.ts` is the ONE removal chokepoint — `WorkBoardRemovalService.remove(scope, docs_project_id, item_id, { reason, plan_doc? })` cancels a live bound run FIRST (trident via the §F6a `terminate()` chokepoint claiming `cancelled_run` only on `won`; research via `cancel_dispatch`), then disposes the card's own `plans/` doc (MOVE to `plans/<reason>/<basename>` via `DocStore.moveDoc`; only an explicit `plan_doc:'delete'` destroys; failures degrade to `left_in_place`), then hard-deletes the row. HTTP DELETE rewired through it with `?reason=` (default `cancelled`) + `?plan_doc=delete`; composer builds ONE `workBoardRemoval` (~open/composer.ts:3931) and hands it to the surface. Ordering pinned mutant-red in `work-board/removal.test.ts` (skip-cancellation AND delete-first mutants go RED); real-store test pins acceptance (d).
- MEASURED gaps: `work-board/agent-tool.ts` still registers `list/add/update/complete/reorder` only — no remove tool, so the agent's only removal lever remains `complete` (the misreport). No `archived` status anywhere (0097 CHECK is `upcoming/in_progress/done/failed`; `failed` is run-driven, not a shelf).
- Layering constraint (holds): `work-board/package.json` has NO trident dep; the chokepoint takes structural deps only — the composer already satisfies them (`boardRunAccess`, `isTerminalPhase`, `dispatchService.stop`, `docStore`).

- [x] **T0 — (already built) UI X cancels an in-flight run before deleting the row, atomically-claimed, observer-chain included** (§F6a terminate chokepoint + dispatch stop; `store.delete` is a hard row delete).
- [x] **T1 — the removal CHOKEPOINT: `work-board/removal.ts` — reason-taking, run-cancelling-first, doc-disposing remove; HTTP DELETE rewired through it; live-run-cancellation ORDER pinned with mutant-red tests.** (Committed d9987a9: `work-board/removal.ts` + `removal.test.ts`, surface rewire, composer wiring, AS_BUILT entry.)
- [x] **T2 — expose it to the agent: `work_board_remove` tool riding the SAME chokepoint.** Register in `work-board/agent-tool.ts` (gated on a wired `opts.removal` — legacy/removal-less boots register the existing five tools byte-identically) with required `id` + required `reason` (enum shipped/cancelled/moved, each value described so the model picks honestly) + optional `delete_plan_doc: boolean` (the ONLY way a doc dies — deliberate, never implied). Handler: scope = `workBoardScopeKey(ctx.project_slug, ctx.project_id)`; docs id = `ctx.project_id ?? GENERAL_WORK_BOARD_PROJECT_ID` (mirrors add — the scope/docs-id conflation hazard documented in `spec-doc-service.ts`). Thread the composer's single `workBoardRemoval` through `misc-input.ts` `work_board.removal` → `build-core-modules.ts` → `registerWorkBoardToolSurface`. Tool result reports what happened: `cancelled_run`, `plan_doc` disposition + new path. Tests mirror the T1 ordering pin through the agent surface: agent remove of a card with a LIVE bound run (real store + `bindRun`) cancels FIRST — the acceptance's named test — and the removed card is gone from a subsequent `work_board_list`.
- [ ] **T3 — the SHELVED lane: `archived` status end-to-end, distinct from `done`.** Migration `0122_work_board_items_archived_status.sql` (0097-style STRICT-table rebuild, CHECK widened to include 'archived', task_type column from 0105 included; regenerate `migrations/expected-schema.txt`). Store: `listActive`/`listAllActive` exclude archived (drops it from the per-turn fragment); new `listArchived` (updated_at DESC); `list()` = active + archived + completed; a →archived transition clears `inline_active` (terminal-clear parity) and never stamps `completed_at` (archiving OFF done nulls it); archiving a card with a LIVE bound run REFUSES (isRunLive guard, generalised `WorkBoardRunStillLiveError`). Surface: add 'archived' to agent-tool `STATUS_VALUES` + HTTP `VALID_STATUSES` (client-writable, unlike 'failed'). Clients: `app/lib/work-board-helpers.ts` + `landing/chat-react/WorkBoardTab.tsx` bucket archived into a collapsed "Shelved" section — NOT the active lane (their `status != 'done'` bucketing would otherwise resurrect it as active) and NOT the done history. Pin acceptance (b): an archived card appears in no completed list, no completed count, no done-history render, and `completed_at` stays null.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests; append the build to docs/AS_BUILT.md.
