# IMPLEMENTATION_PLAN — The build brief must not be retyped by a model: pass it by PATH (defect 2026-08-13, run 000cedc8)

Governing spec: this Plan card. Verified against the tree 2026-08-14 (branch `trident/the-build-brief-must-not-be-retyped`, resumed at `284e0c5`; the stale work-board plan section previously concatenated above this one belongs to a different, already-merged card — dropped from this file). Card-relevant suites re-run at `284e0c5`: `brief-parts`, `codex-build`, `inner-workflow-assembly`, `codex-brief-chunking`, `inner-loop`, `terminal-failure-reason` — 141 pass, 0 fail.

- Chunked transport CONFIRMED shipped (#234, `3ff4b67`): `chunkTextOnLines` + `CODEX_BRIEF_CHUNK_BYTES=3072` in `trident/inner-workflow.mjs`, whole-file `<bytes>:<fnv32>` receipt unchanged, tests in `trident/codex-brief-chunking.test.ts`. This is the sanctioned FALLBACK, never to be removed-but-unreplaced.
- Acceptance item 3 (misreport) CONFIRMED shipped (#240, `f2ce340`): `innerTerminalFailureReason` no longer asserts "exhausted" for an early stop; pinned in `trident/terminal-failure-reason.test.ts`. The new `CODEX_BUILD_BRIEF_ARGS_CORRUPT` throw is also pinned as NOT the exhausted-rounds message.
- T3 CONFIRMED shipped on this branch (`284e0c5`): `codexBriefByPath` in `trident/inner-workflow.mjs` validates the manifest, receipt-checks args-carried task/reflection against the launcher's per-part receipts (mismatch → terminal `CODEX_BUILD_BRIEF_ARGS_CORRUPT`, never a silent build), slices the brief into head (`.a1`) / by-path parts / coda (`.a2`), sets `NEUTRON_CODEX_BUILD_BRIEF_PARTS` in the run command, keeps the whole-brief `NEUTRON_CODEX_BUILD_BRIEF_INTEGRITY` receipt, and falls back byte-identically to the fully-chunked prompt when the manifest is absent/malformed or the brief shape is unexpected. Docblocks updated; AS_BUILT entry appended; tests extended in `trident/inner-workflow-assembly.test.ts` (>30 KB task bytes in NO agent prompt, receipt identical to fallback's, reflection ordering, fail-closed on missing reflection metadata).
- GAP NOW CLOSED by T4 (the BUILD HAZARD's "prove the new path on a >30 KB brief"): no test EXECUTED the by-path prompt's emitted chunk Bash blocks and proved that the resulting `.a1`/`.a2` bytes plus the REAL launcher-written part files concatenate (in the `NEUTRON_CODEX_BUILD_BRIEF_PARTS` order the prompt itself lists) to EXACTLY the prompt's own whole-brief receipt. The workflow-side slicing and the wrapper-side assembly were each proven only against test-constructed counterparts; a slicing off-by-one would pass every existing suite yet turn every live build into `CODEX_BUILD_BRIEF_CORRUPT` exit 3 at deploy — the exact cannot-repair-itself failure the card warns about. That this was a real hole is now measured, not asserted: under a one-character head-slice shift, all 141 pre-existing tests stay GREEN while the two new tests go RED. `trident/codex-build.test.ts` already closes the far side of the chain (any parts assembling to the receipt → build runs, codex stdin byte-identical, >30 KB case included), so proving prompt-emitted parts ⇒ receipt closes acceptance item 1 end-to-end.

- [x] Chunked agent transport with one end-to-end `<bytes>:<fnv32>` receipt over the assembled brief (#234) — the FALLBACK path; keep working, never regress, receipt semantics untouched.
- [x] Regression guard on the misreport: a build that never started no longer terminates as "inner loop exhausted N round(s) without Argus APPROVE" (#240; `trident/terminal-failure-reason.test.ts`; the T3 mismatch throw is also pinned as not-that-message).
- [x] **T1 — the wrapper assembles the brief from part files on disk** (`6ad23cb`, reworked in `284e0c5`): `NEUTRON_CODEX_BUILD_BRIEF_PARTS` in `trident/codex-build.sh`, integrity over the assembled whole, `CODEX_BUILD_BRIEF_PART_MISSING` refusal, legacy path untouched when unset; tests in `trident/codex-build.test.ts` (>30 KB by-path byte-identical, corrupted part → `CODEX_BUILD_BRIEF_CORRUPT`, missing/empty part, order enforced by receipt, legacy unchanged).
- [x] **T2 — the launcher writes the part files and threads a `briefParts` manifest** (`4507c0c`): `trident/brief-parts.ts` (parity-pinned `briefIntegrity` twin + `writeBriefParts`, null-on-failure so the fire never dies), `buildWorkflowArgs`/`buildFireWorkflowPrompt` carry `briefParts` (omitted when null so legacy args stay byte-identical), `fireWorkflow` writes parts at fire time. Tests in `trident/brief-parts.test.ts` + `trident/inner-loop.test.ts`.
- [x] **T3 — the workflow stops retyping the launcher-held segments** (`284e0c5`): `codexBriefByPath` + by-path `codexBuildPrompt` in `trident/inner-workflow.mjs` per the design above; fallback prompt byte-identical when the manifest is absent or malformed; `CODEX_BUILD_BRIEF_ARGS_CORRUPT` terminal throw on args-transit receipt mismatch; docblocks + `docs/AS_BUILT.md` updated; tests in `trident/inner-workflow-assembly.test.ts`.
- [x] **T4 — end-to-end lockstep proof: the prompt's OWN emitted transport assembles to the prompt's OWN receipt.** In `trident/inner-workflow-assembly.test.ts`: for a >30 KB task (and again with non-empty reflection guidance), write the part files with the REAL `writeBriefParts` (temp dir), run the workflow with that real manifest, then mechanically EXECUTE the forge:build prompt's chunk Bash blocks with real `bash` (redirect targets remapped into the temp dir), read `NEUTRON_CODEX_BUILD_BRIEF_PARTS` and `NEUTRON_CODEX_BUILD_BRIEF_INTEGRITY` out of the prompt's run command, concatenate the listed files in the listed order, and assert `briefIntegrity(assembled)` equals the prompt's receipt — the byte-for-byte comparison `codex-build.sh` will make, now proven against the emitted transport rather than a test-built stand-in. Chained with the existing `codex-build.test.ts` by-path suite this completes acceptance item 1 with no agent retyping anywhere in the path. TEST-ONLY; no production file changed. Two build notes worth keeping: (a) the chunk blocks must NOT be path-rewritten wholesale, because the coda forming the `.a2` segment names `/tmp/trident-codex-build-<run>.diff` as brief TEXT and a blanket rewrite would corrupt the very bytes the receipt covers — only the `shSingleQuote`d redirect targets are remapped, and the assembled brief is asserted to contain neither segment path; (b) `tail: coda` is an EQUIVALENT mutant, because `chunkTextOnLines` re-attaches `\n` to every line and so normalizes a missing terminal newline, so sensitivity was verified with byte-moving mutations instead — a one-character head-slice shift and a one-character tail edit each turn both new tests RED on the receipt assertion while all 141 pre-existing tests stay green.
