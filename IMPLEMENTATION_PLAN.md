# IMPLEMENTATION_PLAN — A build must rebase onto main before review (stale branch ≠ rejected build)

Governing spec: this Plan card. Verified 2026-08-15 at tip `275093e`, based EXACTLY on current origin/main `ffba66f` (merge-base = `ffba66f`): 61 pass / 0 fail across `trident/orchestrator.test.ts` + `trident/publish-rebase-realgit.test.ts`. The branch was rebased over main commits `30ab8fe`..`ffba66f` (#277–#280) since the prior verification, with zero file overlap: main's #279/#280 range touched `trident/inner-workflow.mjs` + `trident/ci-gate.test.ts` (and `orchestrator.ts`'s credential plumbing), while this branch's diff touches `trident/orchestrator.ts`'s rebase-onto-observed-main addition, its two test files, and docs — the rebase carried six commits on top of `ffba66f` (feat `a0e78f5`, tests `82c5558`, docs `29d90cd`, docs-ordering `d5b1aa7`, plan close-outs `6149c12` + `275093e`).

- The readiness probe is repaired and honest: `classifyReviewReadiness` (`trident/inner-workflow.mjs`) reads `mergeable: CONFLICTING` and defers the review round. Correct behavior, now no longer exposing an un-rebased branch — T1 removed the condition; the only other rebase machinery in the tree is local-mode merge-time (`merge.ts` + `conflict-resolver.ts`).
- The lease push exists and is correct: `publishBuiltCommit` (`orchestrator.ts`) pins `--force-with-lease=refs/heads/<b>:<sha>` to an `ls-remote`-OBSERVED sha, never the remote-tracking ref; third-party-advance refusal is asserted at the fake-host level AND against a real remote (T2).
- DECIDED — WHEN: the rebase lives in the OUTER publisher, between the local-tip verification and the lease observation/push. Only the outer loop holds a push credential (the Forge sandbox strips `*TOKEN*`), so "rebase + re-push before the readiness probe" can only happen there; and because the publisher fires after EVERY build/fix round and before EVERY review re-fire, every fix round re-enters a branch already based on current main — post-round-1 code IS written against the tree it merges into, the closest to "before build" the credential boundary allows.
- DECIDED — MECHANISM (shallow-safe, governance #574/#571): never `git rebase` in the shared checkout (`.git/shallow` boundary makes merge-base lie). Replay the branch's own diff onto the OBSERVED base tip in a throwaway detached worktree: PR exists → `gh pr diff <n>` (server-side merge-base, shallow-immune); first publish → two-dot `git diff <base>..<branch>`. `git apply --3way`, commit, move `refs/heads/<branch>` by compare-and-swap (`update-ref <new> <old>`). The replay squashes the branch into one commit — acceptable, the PR merge is `--squash` anyway.
- DECIDED — ON CONFLICT: a failed 3way apply is an ATTENTION state, never `REQUEST_CHANGES` and never auto-resolved: fail the run with `TridentRebaseConflict` whose reason starts `REBASE CONFLICT — needs attention:` naming branch, base, and the conflicting paths, stating no reviewer judged the code. The `resolve_conflict` Forge resolver is NEVER invoked on this path.

- [x] **P0 — lease-pinned publish**: `--force-with-lease` pinned to an observed sha, empty-lease first push, third-party refusal, git's own words in the failure (merged; `orchestrator.ts`, tests `orchestrator.test.ts`).
- [x] **P0b — honest readiness probe**: `CONFLICTING` → deferred review, no seat dispatched, no round consumed (merged; `inner-workflow.mjs`, `reviewPreconditionDeferred`). The condition it reports is what this plan removed.
- [x] **T1 — shallow-safe rebase-onto-observed-main in the outer publisher (+ conflict = attention state), with fake-host unit tests.** DONE (now commit `a0e78f5` after rebase): exported `rebaseOntoObservedBase(...)` + `TridentRebaseConflict` in `orchestrator.ts`; wired into `publishBuiltCommit` after the local-tip check and before the lease observation; rebased head flows into the push, the post-push confirm, the review diff, and the re-fire checkpoint; PR probe hoisted so the replay can use `gh pr diff`; scratch worktree always torn down.
- [x] **T2 — REAL-git + REAL-shallow integration test (`trident/publish-rebase-realgit.test.ts`) guarding acceptance (a)/(b)/(c)/(d) against fake tests.** DONE (now commit `82c5558` after rebase): real origin whose `main` moved by a genuine non-conflicting commit after the branch was cut, behind-ness PROVED in a FULL clone; build checkout is `git clone --depth=1 file://…` and the test ASSERTS `.git/shallow` exists; replay onto main's tip with no false conflict; conflicting seed throws `TridentRebaseConflict` naming the path, ref unmoved, never `REQUEST_CHANGES`; third-party advance makes the pinned lease push REFUSE against the real remote.
- [x] **T3 — docs: record the decision + as-built.** DONE (now commit `29d90cd` after rebase): dated entry in `docs/AS_BUILT.md`; "THE PUBLISH BOUNDARY" section of `docs/SYSTEM-OVERVIEW.md` extended with rebase-before-lease ordering, the shallow workaround (#574), and conflict-as-attention.
- [x] **T4 — close-out: restore AS_BUILT newest-first ordering.** DONE (now commit `d5b1aa7` after rebase).
- [x] **T5 — close-out: persist the regenerated plan recording the tip verification.** DONE (commit `6149c12`, pre-rebase shas).
- [x] **T6 — terminal verification at the PRE-REBASE tip.** DONE at old tip `a05a858` on old base `51d17b7` (61/61 green) — SUPERSEDED: the branch has since been rebased onto `ffba66f`, so that attestation no longer describes the current tip.
- [x] **T7 — RE-VERIFICATION AT THE REBASED TIP + plan truth-refresh (no code changes): re-run the two governing suites at branch tip `275093e` (based on origin/main `ffba66f`) and commit a plan whose recorded shas match the real branch state.** DONE: merge-base of origin/main and the branch confirmed equal to origin/main's tip (`ffba66f`); ran `bun test trident/orchestrator.test.ts trident/publish-rebase-realgit.test.ts` at the branch tip from a fresh worktree — 61 pass / 0 fail, 264 expect() calls, no code changed.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
