# IMPLEMENTATION_PLAN — Work Board card removal: agent-exposed, run-cancelling, reason-carrying, doc-disposing (+ a shelved lane)

Governing spec: this Plan card (agent remove through the UI's own path; archived ≠ done; removal takes a reason and the plan doc's disposition follows it; removed = really gone). Verified against branch `trident/the-agent-must-be-able-to-remove-a` (0258f65) 2026-08-14:

- MEASURED (T1 landed, d9987a9): `work-board/removal.ts` is the ONE removal chokepoint — `WorkBoardRemovalService.remove(scope, docs_project_id, item_id, { reason, plan_doc? })` cancels a live bound run FIRST (trident via the §F6a `terminate()` chokepoint claiming `cancelled_run` only on `won`; research via `cancel_dispatch`), then disposes the card's own `plans/` doc (MOVE to `plans/<reason>/<basename>` via `DocStore.moveDoc`; only an explicit `plan_doc:'delete'` destroys; failures degrade to `left_in_place`), then hard-deletes the row. HTTP DELETE rewired through it with `?reason=` (default `cancelled`) + `?plan_doc=delete`; composer builds ONE `workBoardRemoval` and hands it to the surface. Ordering pinned mutant-red in `work-board/removal.test.ts`.
- MEASURED (T2 landed, 0258f65): `work_board_remove` registered in `work-board/agent-tool.ts` gated on a wired `opts.removal` (legacy boots register the five legacy tools byte-identically); required `id` + `reason` enum (shipped/cancelled/moved) + optional `delete_plan_doc`; threaded composer → `misc-input.ts` → `build-core-modules.ts` → tool surface; agent-path live-run-cancellation ordering + gone-from-`list` pinned in `work-board/agent-tool.test.ts`.
- MEASURED gap (T3, the last acceptance item — (b)): NO `archived` status anywhere. `WorkBoardStatus` is `upcoming|in_progress|done|failed` in `work-board/store.ts:35`, `landing/chat-react/work-board-client.ts:45`, `app/lib/work-board-client.ts:35`; the 0097 CHECK is the same four; `failed` is run-driven (terminal-reconcile-only), not a shelf. The agent's only deprioritise lever remains `complete` — the misreport this card exists to kill.
- Layering constraint (holds): `work-board/package.json` has NO trident dep; the removal chokepoint takes structural deps only.

- [x] **T0 — (already built) UI X cancels an in-flight run before deleting the row, atomically-claimed, observer-chain included** (§F6a terminate chokepoint + dispatch stop; `store.delete` is a hard row delete).
- [x] **T1 — the removal CHOKEPOINT: `work-board/removal.ts` — reason-taking, run-cancelling-first, doc-disposing remove; HTTP DELETE rewired through it; live-run-cancellation ORDER pinned with mutant-red tests.** (d9987a9.)
- [x] **T2 — expose it to the agent: `work_board_remove` tool riding the SAME chokepoint, with the agent-path live-run ordering pin and gone-from-`work_board_list` assertion.** (0258f65.)
- [x] **T3 — the SHELVED lane: `archived` status end-to-end, distinct from `done`.** Migration `0122_work_board_items_archived_status.sql` (0097-style STRICT rebuild, CHECK widened to include 'archived', 0105's `task_type` column + both indexes preserved; regenerate `migrations/expected-schema.txt`). Type widened in all three `WorkBoardStatus` declarations. Store: `listActive`/`listAllActive`/`reorder`'s active-lane query exclude archived (also drops it from the per-turn fragment, which rides `listActive`); new `listArchived` (updated_at DESC); `list()` = active + archived + completed; a genuine →archived transition clears `inline_active` and NEVER stamps `completed_at` (archiving OFF done nulls it; leaving archived re-appends to the active-lane end like a done-reopen); archiving a card with a LIVE bound run REFUSES with `WorkBoardRunStillLiveError` (same isRunLive guard as `complete`). Surfaces: 'archived' added to agent-tool `STATUS_VALUES` and HTTP `VALID_STATUSES` (client-writable, unlike 'failed'); PATCH and agent `work_board_update` catch the live-run refusal (409 `run_still_live` / `{ok:false}`) instead of crashing. Clients: `app/lib/work-board-helpers.ts` `splitBoard` goes three-way and `app/app/projects/[id]/workboard.tsx` + `landing/chat-react/WorkBoardTab.tsx` render archived in a collapsed "Shelved · N" section — NOT the active lane (their `status !== 'done'` bucketing would otherwise resurrect it as active) and NOT the Done history/count. Pin acceptance (b) mutant-red: an archived card appears in no completed list, no completed count, no Done bucket, and `completed_at` stays null.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests; append the build to docs/AS_BUILT.md.
