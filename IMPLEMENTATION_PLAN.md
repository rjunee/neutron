# IMPLEMENTATION_PLAN — salvage grows a working-tree leg: the commit-scoped sweep must see uncommitted work (card: port the lane shepherd's snapshot into trident)

Card: `sweepStrandedFailures` / `reconcileStranded` landed with PR #389 at `ec3bcc56`; this iteration is based on current `origin/main` at `5895e268`. The path decides "did this failed run build anything" from `git rev-list --count base..HEAD` — commit-scoped only. The one measured loss (run `9d115490`, 292 lines, ZERO commits, working tree only) is invisible to it. Port the shepherd's mechanics INSIDE `reconcile_stranded`: capture a dirty worktree into a locally anchored commit without mutating the live checkout, plus branch-and-run-scoped stash/reflog consultation before any built-nothing conclusion.

Planner survey began against `ec3bcc56`; the implemented branch was rebased by the outer loop onto origin/main `5895e268` (verified by merge-base). The original path contained no `stash` or `status --porcelain` on salvage; `reconcile_stranded` gated on `rev-list --count` alone and returned null for the 0-commit case. Both call sites are live: the terminal `step()` choke point and the boot sweep wired through `gateway/composition/build-core-modules.ts`. Sandbox verification established that `git stash create` preserves tracked work and the live index without changing HEAD or porcelain status, while a private index is needed to add untracked content to the anchored snapshot.

- [x] PREREQUISITE — #389 (`sweepStrandedFailures`/`reconcile_stranded`) merged to main as `ec3bcc56` and invoked on the real composition path (verified end to end per the card, 2026-08-19T12:17Z). Build against current origin/main; extend, do not reimplement.
- [x] THE CARD (Findings 1+2): extend `reconcile_stranded` in `trident/orchestrator.ts` with a working-tree leg — locate only an isolated linked worktree (never the primary checkout), build a full tracked+untracked snapshot through a private temporary index, anchor it at `refs/tags/trident-salvage/<run_id>`, and report BOTH commit and worktree legs in the terminal record. A clean tree consults both `git stash list` and `refs/stash`'s reflog, accepting exact-branch entries only inside this run's window. The startup sweep suppresses checkout inspection when a live run owns the reused branch while preserving commit publication. `stash create` is a best-effort index-parent probe: a live lock/unmerged index no longer aborts private-index capture, and its warning is persisted. Recovery refs use create-only compare-and-swap and are consulted independently of the row marker, so a retry after an anchor-success/store-failure cannot replace the first snapshot. Failed captures persist a bounded diagnostic on the terminal row for both live delivery and boot sweep. Success markers are written only after the ref anchors, and they do not lock out a later commit publish. Delivery strips the machine suffix for classification and renders the recovery ref or capture warning separately. Pinned by real-git tests for untracked-only nested content under `status.showUntrackedFiles=no`, tracked changes behind a live index lock, byte-identical HEAD/status, failed anchoring, retry-safe anchoring after a lost store write, later commit publication, stale/crafted stash rejection, shared-checkout exclusion, live-run branch reuse, and operator delivery copy. AS_BUILT entry. `tools/lane_review.sh <branch>` exits 0.

Out of scope, by the card's own text — do not build here:
- FINDING 3 (dirty-file count as positive liveness for the hang watchdog) belongs to card `01M08S2HFG`; #442 already landed the stage-event stand-down seam it feeds.
- Retention/GC of salvage snapshots is a separate card; v1 never deletes them — losing work is the failure being fixed.
- No pushes from the capture legs: snapshot capture is LOCAL (tag + reason text). Publishing commits stays the existing outer-loop `publishBuiltCommit` path.

remaining after top task: 0 — single-card, single-iteration change.
