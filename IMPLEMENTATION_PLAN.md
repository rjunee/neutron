# IMPLEMENTATION_PLAN — A gateway restart must not kill an in-flight build: recover from the pushed branch instead of reaping the run

Governing spec: this Plan card (owner instruction 2026-08-14: "Gateway restarts should not kill a build. The build should recover."), plus the BLOCKING addendum of 2026-08-14 23:40Z (rebase + migration renumber + three-way column merge).

State verified against origin/main (51d17b7) and origin/trident/a-gateway-restart-must-not-kill-an (5b3be91) on 2026-08-15:

- The defect routing is fixed on the branch: `trident/orchestrator.ts` step() §1a-crash claims a crashed row via `begin_crash_recovery` (wired to `TridentRunStore.beginCrashRecovery` in `gateway/composition/build-core-modules.ts`) and relaunches it as a continuation (same branch/PR/checkpoint, rounds untouched); past the durable budget (`crash_recoveries`, default cap 3) it fails with a reason naming the crash-recovery budget and embedding the latched crash reason. `delivery.ts` classifies that reason as `infra` AHEAD of the "exhausted" matcher.
- LOAD-BEARING TRAP (still governs T2 and any store change): `saveIfActive` vetoes non-crashed writes onto a `crashed`-latched row and re-latches on a tombstoned `workflow_run_id`; recovery clears the latch and nulls the generation ONLY via the atomic `beginCrashRecovery` claim — never via `update`/`save`/`saveIfActive`.
- BLOCKING addendum status, verified by inspection + a scratch-worktree test run: branch is rebased onto origin/main HEAD (merge-base = 51d17b7); migration renumbered `0122` → `0123_code_trident_runs_crash_recoveries.sql` (next free above main's `0122_trident_checkpoint_head.sql`); `migrations/expected-schema.txt` `code_trident_runs` ends `… harvested_at INTEGER, inner_checkpoint_head TEXT, inner_checkpoint_findings TEXT, crash_recoveries INTEGER) STRICT`; `trident/store.ts` COLS has all 31 columns, the INSERT has 31 `?` placeholders and 31 bound values (hand-counted); `migrations/runner.test.ts` ordinal list includes 123; `bun test trident/` on the branch: 1253 pass, 0 fail. NOT yet done: the addendum's required TEST that placeholder count = column count = bound-array length (today the 31/31/31 alignment is only hand-counted — the exact silent-corruption hazard the addendum names), and the `gh pr view 267 --json mergeable` = MERGEABLE verification (unavailable in the planner environment: gh unauthenticated).

- [x] B0 — REBASE onto origin/main (51d17b7): done; commit 5b3be91 sits directly on main HEAD, no conflict markers, `bun test trident/` green on the branch.
- [x] B1 — RENUMBER the migration off main's taken `0122`: done as `migrations/0123_code_trident_runs_crash_recoveries.sql` (read off origin/main's highest ordinal, not "branch highest + 1"); `runner.test.ts` expects 123.
- [x] B2 — THREE-WAY COLUMN MERGE, both sides survive: done — `expected-schema.txt` carries `inner_checkpoint_head`, `inner_checkpoint_findings` AND `crash_recoveries` (last, matching migration run order); `store.ts` COLS/INSERT/bound array all carry all three, 31/31/31.
- [x] B3 — PIN THE 31/31/31 INVARIANT IN A TEST + VERIFY PR #267 MERGEABLE (the two unfinished BLOCKING requirements). Derive the INSERT placeholder list from COLS in `trident/store.ts` (so placeholder count = column count by construction), export the column list for tests, and add `trident/store.test.ts` tests: (i) COLS count equals `PRAGMA table_info(code_trident_runs)` count, (ii) a distinct-value create()/get() round-trip that goes red if any two bound values are swapped or the array is short. Then re-fetch origin/main, rebase only if it moved (re-checking the migration ordinal is still free), push, and verify `gh pr view 267 --json mergeable` reports MERGEABLE.
- [x] T0 — Harvest-before-reap ordering (acceptance (c)) holds: step() §1 tries `parseInnerResult` FIRST even on a crashed row; asserted in `trident/crash-before-launch-save.test.ts` (updated to the budget-bounded contract, invariant = boundedness not reaping).
- [x] T1 — RECOVER, DON'T REAP + BOUNDED DURABLE BUDGET (acceptance (a), (b), (c)-preserved, (e)): migration 0123 `crash_recoveries`; `TridentRunStore.beginCrashRecovery` atomic claim (clear latch, release slot, null tombstoned generation, spend budget); orchestrator §1a-crash continuation relaunch, budget-capped terminal reason that avoids the word "exhausted" and embeds the latched crash reason; `build-core-modules.ts` seam; `delivery.ts` crash-recovery-budget `infra` class ahead of the "exhausted" matcher; mutation-red tests incl. cap-holds-across-restart (fresh orchestrator over same store), board never reads `failed` mid-recovery, crashed-with-result still harvests with zero fires (`trident/crash-recovery.test.ts`, `store.test.ts`, `delivery.test.ts`).
- [ ] T2 — REPORT THE MEASURED CAUSE (acceptance (d)). `open/wiring/substrates.ts` `onChildCrash` composes the latched reason with measured facts: observation time and this gateway process's boot time (`Date.now() - process.uptime()*1000`), phrased as measurements ("observed <iso>; gateway process booted <iso>"), never as an asserted cause. Because T1's budget-capped terminal reason embeds the latched crash reason, the boot timestamp survives onto the unrecoverable run's stored `failure_reason`. Tests: the reason handed to `crashRunningByLauncher` carries both timestamps (`open/__tests__/open-wiring-substrates.test.ts`), and an orchestrator test asserts the terminal reason still contains them.
- [ ] T3 — DOCS + INVARIANTS. Append the change to `docs/AS_BUILT.md` (append-only — keep both sides on any conflict); add `docs/INVARIANTS.md` entries for (i) harvest-before-reap ordering (file:line + test), (ii) crash recovery is budget-bounded and the budget is durable, (iii) a recovery relaunch never consumes `round`/`ralph_round`, (iv) store INSERT column/placeholder/bound-array counts are pinned by test. Brief SYSTEM-OVERVIEW touch where the trident crash path is described.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
