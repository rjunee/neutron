# IMPLEMENTATION_PLAN — the inner loop's `gh` has no credential: reads must carry the instance token, and an infra-only block must stop masquerading as REQUEST_CHANGES

Governing spec: this Plan card (run `8417b277` / PR #261, measured 2026-08-14). Verified against main (`d8324cc`) 2026-08-14:

- CONFIRMED on main: the inner workflow's three GitHub READ probes — `probeCi` (`trident/inner-workflow.mjs:2386`), `probeReviewReadiness` (:2407), `probePrMerged` (:2467) — compose bare `gh …` command strings executed by a subagent's Bash tool, which inherits the warm `cc-trident-fire-*` REPL env (`open/wiring/substrates.ts` ~:399). No `GH_TOKEN` reaches it. The WRITES precedent the card demands parity with is `open/composer.ts:2007-2008` — `makeLazyCredentialedHostRunner(async () => githubProcessEnv(await readGitHubToken(secretsStore, asOwnerHandle(owner_handle))))` — resolved per command, env-only, never on disk (`github/credential.ts` options 1–3 explicitly refused).
- CONFIRMED: `classifyReviewReadiness` (:2123) maps every unreadable probe to `{status:'unknown', reason:'PR readiness could not be read'}` and DISCARDS `raw` (the `gh auth login` text the log proves was in hand); `reviewPreconditionDeferred` (:2417) turns that into `{verdict:'REQUEST_CHANGES', blockKind:'infra-only'}`; the terminal result carries `blockKind` (:3475) but `parseInnerResult` (`trident/inner-loop.ts:416`) never parses it, so the orchestrator catch-all (`trident/orchestrator.ts:962-979`) writes the generic round sentence (`innerTerminalFailureReason`, :371) for a run no review seat ever judged. `trident/delivery.ts:204` then classifies that reason with the code-rejection class.
- Threading precedent for getting a checked-in script to the workflow: `CHECKPOINT_SCRIPT_PATH` / `WORKTREE_CLEANUP_SCRIPT_PATH` (`trident/inner-loop.ts:203-222`) → `buildWorkflowArgs` → args destructure (`inner-workflow.mjs:82-186`). Test precedent for `.mjs` internals: source-extraction (`trident/__tests__/ci-gate.test.ts` `grab()`); for the store: `auth/__tests__/secrets-store.test.ts` (temp dir + `ProjectDb.open` + `applyMigrations`).

- [x] **T0 — the WRITE path already satisfies the card's parity target** (nothing to build; recorded so nobody re-solves it): outer publisher pushes with the per-command lazy credential + lease (#259, `orchestrator.ts:506-517`), verified live by PRs #263/#266/#267/#268. This is the shape T1 mirrors for reads.
- [x] **T1 — the inner loop's GitHub READS carry the instance credential, resolved per command from the store.** New checked-in runner `trident/gh-authed.ts` (resolve token via `SecretsStore` + `readGitHubToken` at invocation time, exec `gh` with `githubProcessEnv(token)` spread into the child env only — never printed, never in argv, never on disk; no token → run `gh` bare, mirroring `githubProcessEnv(null)`). Thread its abs path + `bunBin` + `ghDataDir` + `ghOwnerHandle` through composer → misc-input → build-core-modules → orchestrator → `buildWorkflowArgs` → the args destructure, and rewrite the three probe command strings to invoke it (bare-`gh` fallback when the args are absent). Guard against the fake test: a subprocess test with a stub `gh` on PATH asserts the token from a real temp store arrives in the CHILD ENV and nowhere in argv, and deleting the store row makes the same assertion read ABSENT.
- [ ] **T2 — measure the cause, and having measured it, do not throw it away.** `classifyReviewReadiness` (and `classifyCi`'s unknown branch) keep the probe's own output — first line(s) of `raw`, redacted (mirror `redactPushError`'s two patterns locally in the `.mjs`) and length-capped — in `reason`; `reviewPreconditionDeferred`'s finding carries it; the terminal result gains an explicit `terminalCause` string (set from the deferral/infra findings when `blockKind === 'infra-only'`); `parseInnerResult` parses `blockKind` + `terminalCause` into `InnerResult`; the orchestrator catch-all writes a failure_reason that NAMES it (e.g. `review never ran (infra-only) at round 1: gh: To get started with GitHub CLI, please run: gh auth login`) instead of the generic round sentence — the generic sentence remains ONLY for results carrying no cause (the owner's rule: the specific message ships WITH the measured signal, never before it). Assert the stored reason for the 8417b277 shape contains the probe's own words and never a credential (positive control per #240-family tests).
- [ ] **T3 — an infra-only block is not the agent's work being rejected.** `trident/delivery.ts` `classifyTerminalFailure` recognises the new infra-only reason/cause as klass `'infra'` (retry-class messaging), not the without-Argus-APPROVE code-rejection class; the run row still terminates `failed` (the full no-terminate retry lane belongs to card 01KZZQ2J9MJFG0PXC8AA6D6EV4 — do NOT build it here). Test pins: an `infra-only` terminal result at round 1 is delivered as infrastructure, its message quotes the measured cause, and a genuine review REQUEST_CHANGES still classifies as before.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
