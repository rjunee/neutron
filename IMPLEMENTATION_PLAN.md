# IMPLEMENTATION_PLAN — PR number on the card: durable #NNN tag, clickable, + title-opens-doc (owner-requested 2026-08-14)

Governing spec: this Plan card (parts 1–4 + Part 1b, acceptance (a)–(i)). Re-verified against branch `trident/put-the-pr-number-on-the-card-a-cli` (head 1d105e2) vs `origin/main` (head 71a568a) on 2026-08-14:

- DONE on branch: durable `pr`/`pr_url` columns + `detachRun(pr_info)` in-transaction write + `attachRun` clear (T1, commits 6a4fdcd/daf6ddf), `trident/repo-web-url.ts` resolver/cache, reconcile-observer `pr_url` resolution, `pr`/`pr_url` in the fan literal + `AppWsWorkBoardItem` + web client type, and live `pr`/`pr_url` on `RunProgress` end-to-end with one cache in `open/composer.ts` (T2, commit 1d105e2). Acceptance (c)'s durable half is guarded by a real observer→detachRun→re-read test in `work-board/store.test.ts`.
- CORRECTION: the previously committed plan marked T3 `[x]`. That was WRONG — verified `git show <branch>:landing/chat-react/WorkBoardTab.tsx` has no `cwb-pr`, no PR tag on the meta line, no change to the completed row, and the branch diff touches no file under `landing/chat-react/` except `work-board-client.ts` + its test. T3 is re-opened below. Do not trust a checkbox over the tree.
- NEW BLOCKER (Part 1b, added 2026-08-14 22:45Z): the branch's `migrations/0122_work_board_items_pr.sql` collides with `0122_trident_checkpoint_head.sql`, MERGED on main. `migrations/runner.test.ts` ends at 122 on both sides. Governance #575 owns the defect; this branch must rebase + renumber before anything else lands, and the longer it waits the more the seven overlapping files (`open/composer.ts`, `trident/run-progress.ts`+test, `migrations/runner.test.ts`, `migrations/expected-schema.txt`, `gateway/http/work-board-surface.test.ts`, `IMPLEMENTATION_PLAN.md`) drift.

- [x] **T1 — durable PR on the item, written by the real detach path.** Migration adds `pr INTEGER` + `pr_url TEXT` to `work_board_items`; `detachRun` writes both in the SAME transaction as status/completed_at for BOTH outcomes; `attachRun` clears them; `trident/repo-web-url.ts` (pure parser + cached resolver); reconcile observer resolves `pr_url` best-effort; columns on the fan literal, `AppWsWorkBoardItem`, web client type + `parseWorkBoardItems`. Acceptance (c) guard drives the REAL observer → `detachRun` → store re-read.
- [x] **T2 — live `pr_url` on `run_progress`.** `RunProgress` (server + `AppWsRunProgress` + web mirror) gained `pr_url: string | null`; `deriveRunProgress` composes `<url>/pull/<pr>` from an optional `repo_web_url`; `makeRepoWebUrlCache` sync `peek` with background warm; ONE cache in `open/composer.ts` feeding both `withRunProgress` and `fanWorkBoardChanged`.
- [x] **T2.5 — REBASE onto origin/main + RENUMBER the migration to the next free ordinal (Part 1b, BLOCKING, acceptance i).** Rebase the branch onto current `origin/main` (which carries `0122_trident_checkpoint_head.sql`), resolving the seven overlapping files so BOTH sides' behavior survives (notably: main's `argus-request-changes-round-N` handling in `trident/run-progress.ts` merged with the branch's `repo_web_url`/`pr_url` params; main's host-deploy-remote-git + `gh_data_dir`/`gh_owner_handle` + `turnIsActive` changes in `open/composer.ts` merged with the branch's repo-web-url cache wiring). Rename `0122_work_board_items_pr.sql` → `0123_work_board_items_pr.sql` (ordinal = highest on `origin/main` AT REBASE TIME + first free — currently 0123; NEVER "highest on branch + 1"); file rename + header-comment line only, the two `ALTER TABLE` statements unchanged. `migrations/runner.test.ts` applied list must end `…, 121, 122, 123`. Regenerate `migrations/expected-schema.txt` via `bun run migrations/regen-snapshot.ts` (must show BOTH `inner_checkpoint_head` and `pr`/`pr_url`). Verify exactly ONE migration on the branch not on main and its ordinal absent from main. NEVER run the migration runner against a real DB (no `bun run migrate`, no inherited `NEUTRON_HOME`/`NEUTRON_DB_PATH`) — that is the exact incident (#575). State the merge-time operator step (one-time `_migrations` bookkeeping row on the owner's live instance, version matching the landed ordinal) in the PR description; do NOT pre-insert it and do NOT touch the live version-122 `work_board_items_pr` row. Full suite green; force-with-lease push of the rewritten branch.

  MERGE-TIME OPERATOR STEP (one-time, owner's live instance only): `work_board_items` already has `pr`/`pr_url` there (applied out of band during incident #575). Before or at first boot after this merge, insert a bookkeeping row into `_migrations` with version = the ordinal this PR actually landed as (currently 123) and name `work_board_items_pr`, so migration 0123 is skipped instead of failing with `duplicate column name: pr`. Do NOT insert this row anywhere before merge, and do NOT remove the existing `(122, 'work_board_items_pr')` row — it is the deliberate record of the incident.

- [ ] **T3 — render the tag (parts 1–3 in `WorkBoardTab.tsx`).** RE-OPENED — previously checked in error; the file is untouched on the branch. Meta line (~line 998): `#NNN` FIRST, then step tag, then round (`#265 Building round 1`); number = `run_progress?.pr ?? item.pr`; URL = `run_progress?.pr_url ?? item.pr_url`; `<a target="_blank" rel="noopener noreferrer">` when a URL resolved, plain `<span>` when not; NOTHING when no PR. Widen the meta-line render gate so terminal/detached lanes (`#261 Failed`) show it. Completed row (~line 781): `Merged #265 · Aug 14` from durable `item.pr`/`item.pr_url`; PR-less completed card renders today's `Merged · Aug 14` byte-identically. `.cwb-pr` CSS next to `.cwb-round` in `landing/chat-react.html`, muted, no blue. Add `pr_url: null` to pre-T2 `run_progress` fixtures in `__tests__/work-board-tab.test.tsx`. Tests: exact `href`/`target`/`rel`, DOM order, no-PR no-tag, plain-text-no-URL, completed-row render from durable item fields with NO `run_progress` attached (acceptance a, b, d + render half of c).
- [ ] **T4 — title opens the doc; the doc-link line dies (part 4 UI, acceptance e, g, h).** Delete the `cwb-doc-link` element (~lines 926–940). Card WITH a resolvable doc: title click calls `onOpenDoc` — NO inline rename on that card at all, no new control in `cwb-actions`. Card WITHOUT a doc: title click keeps today's `onStartEdit` rename exactly. No link styling on the title (cursor change on hover only). Tests: doc-card click opens doc and never enters edit; docless card still renames; no rename affordance anywhere on a doc card.
- [ ] **T5 — doc H1 → card title sync (part 4 mechanics, acceptance f).** New gateway module hooked off `DocStore.onMutationSuccess` (COMPOSED with the existing `AnchorWalker.handle`, not replacing it): on a `write` to `plans/*.md`, parse the first `# ` H1 — H1 wins over frontmatter; no readable H1 → NO-OP. Find the card whose `design_doc_ref` resolves to that path; update its title when different; rewrite frontmatter `title:` to match (fixpoint, no loop). FILENAME never changes; sync is doc → card only. Tests drive the REAL `writeDoc` path and assert: card renamed, frontmatter rewritten, filename unchanged, `design_doc_ref` still resolves, H1-less save leaves the title untouched.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
