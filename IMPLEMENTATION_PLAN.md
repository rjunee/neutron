# IMPLEMENTATION_PLAN — Work Board row state: a card must not claim a run it doesn't have (defect 2026-08-12, SPLIT card)

Governing spec: this Plan card (failed colour + ▶/↻ + `·building`, all keyed on the durable `status === 'failed'` lane; the dead-without-terminal-write pulse is OUT OF SCOPE, blocked on #534). Verified against main 2026-08-14:

- PR **#217** is MERGED (squash commit `9371540`) — the resume branch is landed, T1–T3 below are on main. The lockstep test was repaired per the card (derives through `scanItemsForRailSignals` + `deriveProjectActivity`, `app/__tests__/work-board-helpers.test.ts:320-420`); the `·building` guard is in (`work-board/fragment.ts:54-61`); both clients paint the durable failed lane and gate `canPlay` on the live run.
- REMAINING defect confirmed live on main: `isLinkedRunning` still treats `rp === undefined` as RUNNING in BOTH clients (`app/lib/work-board-helpers.ts:269`, `landing/chat-react/WorkBoardTab.tsx:102`). `runProgressForItem` (`trident/run-progress.ts:206-219`) returns null for any run id with no trident row, and `detachRun` KEEPS the binding on failure (#340, `trident/board-reconcile.ts:10-16`), so a failed card with a kept binding and no run row (trident run row gone; and any dispatch-lane binding) derives `isLinkedRunning=true` → `canPlay=false` → NO ↻ → unrecoverable from the UI. This is the research/dispatch path the card's acceptance says three runs left untouched.
- Safety fact for the fix: `attachRun` (`work-board/store.ts:717`) atomically sets `status='in_progress'` with every fresh binding, so `status='failed'` + live bound run is unrepresentable — the failed-lane short-circuit cannot hide a live run. The live-research case (`in_progress`, bound, rp forever undefined) must KEEP deriving running (asserted at `app/__tests__/work-board-helpers.test.ts:186`; `handleStart`'s research 409 guard depends on the same reading).
- The `inline_active` canPlay suppressor SHIPPED WITH THE CAVEAT the card forbids ("Do not ship the caveat") — the comment conceding the permanent pulse+no-▶ state is at `app/lib/work-board-helpers.ts:279-286` and `WorkBoardTab.tsx:113-117`. No durable signal exists to build a provable escape (that is the #534 family), so per the card's own disjunction the clause must be DROPPED from `canPlay`. The pulse/rail `inline_active` behavior is separately specified (merged lockstep suite) and stays.
- The fragment fix is UNTESTED: `work-board/fragment.test.ts` has no failed-card case, so deleting the `item.status !== 'failed'` guard at `fragment.ts:57` fails nothing — coverage the acceptance explicitly demands ("the fragment does not say ·building … and the tests must fail").

- [x] **T1 — mobile helpers: gate pulse and ▶/↻ on the LIVE run, paint the durable failed lane** (merged in 9371540).
- [x] **T2 — web mirror: same two edits + render-level cases** (merged in 9371540).
- [x] **T3 — row/rail lockstep repaired to derive through `scanItemsForRailSignals`/`deriveProjectActivity` (no tautology) + stale "auto-detached" comments corrected** (merged in 9371540).
- [x] **T4 — remove the rp-undefined→RUNNING inference: the durable failed lane wins in `isLinkedRunning`, both clients.** Add a `status === 'failed'` → `false` short-circuit (after the has-binding check, before the rp read) in `app/lib/work-board-helpers.ts` `isLinkedRunning` and the module-private twin in `landing/chat-react/WorkBoardTab.tsx`. Keep `rp === undefined → true` for NON-failed bound runs (live research dispatch — no trident row ever exists; test :186 stays green). Mutation-resistant tests both sides: a `status='failed'` card with kept binding and NO `run_progress` is not linked-running, offers ↻ (`canPlay` true + `isRetry` true), and renders a static failed dot; reverting the short-circuit in either file must fail the suites.
- [ ] **T5 — drop the `inline_active` suppressor from `canPlay`, both clients; do not ship the caveat.** Remove `&& !item.inline_active` from both `canPlay` bodies and delete the DELIBERATE SPEC EXTENSION / STALENESS CAVEAT comment blocks (the spec's two-clause form `status !== 'done' && !isLinkedRunning` is authoritative). A stuck `inline_active` card may still pulse (reviewed, merged rail/pulse behavior — unchanged) but is now RECOVERABLE: ▶ renders. Flip the two suppressor tests (`app/__tests__/work-board-helpers.test.ts:230`, `landing/chat-react/__tests__/work-board-tab.test.tsx:812` — case C keeps its pulsing-dot assertion, drops "NO play control" for "play control present"). Note in the commit: a competing-build guard belongs server-side on a durable signal (`handleStart` already 409s live runs); an inline heartbeat is the #534 family, out of scope.
- [ ] **T6 — fragment truth test: a terminally-failed card never says `·building`.** Add a mutation-resistant case to `work-board/fragment.test.ts`: an item with `status='failed'` and a KEPT `linked_run_id` (#340) renders `[failed]` with no `·building` (and no `·inline` when `inline_active` is false); deleting the `item.status !== 'failed'` guard at `work-board/fragment.ts:57` must fail it.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.

# IMPLEMENTATION_PLAN — The build brief must not be retyped by a model: pass it by PATH (defect 2026-08-13, run 000cedc8)

Governing spec: this Plan card. Verified against the tree 2026-08-14:

- Chunked transport CONFIRMED shipped (#234, `3ff4b67`): `chunkTextOnLines` + `CODEX_BRIEF_CHUNK_BYTES=3072` in `trident/inner-workflow.mjs` (~line 1002-1082), whole-file `<bytes>:<fnv32>` receipt unchanged, tests in `trident/codex-brief-chunking.test.ts`. Its own docblock (line ~978) says the real fix is by-path and calls it "a carded design change" — i.e. NOT done. Nothing in `trident/codex-build.sh` accepts parts: the wrapper reads exactly `NEUTRON_CODEX_BUILD_BRIEF_FILE` + `NEUTRON_CODEX_BUILD_BRIEF_INTEGRITY` (lines 640-666).
- Acceptance item 3 (misreport) CONFIRMED shipped (#240, `f2ce340`): `innerTerminalFailureReason` in `trident/orchestrator.ts` no longer asserts "exhausted" for an early stop, and `trident/terminal-failure-reason.test.ts` pins exactly the run shapes from 2026-08-13 (1daded20, 000cedc8, 03242fe5) plus the `interpretFailure` ordering. Do not re-do.
- Transport map verified: the launcher `trident/inner-loop.ts` (`buildWorkflowFirer`/`buildWorkflowArgs`) is real TS with fs access and already holds both large segments at fire time — `run.task` and `buildReflectionGuidance(input.reflection_context)`. The workflow (`inner-workflow.mjs`, no fs by design) composes the brief as `head + 'TASK:\n' + task + reflectionGuidance` (+ `codexBuildCoda()` on the codex route, lines 3130-3138 round 1, 3314-3326 fix rounds) and today ships ALL of it through the bridge agent in chunks (`codexBuildPrompt`, line 1084). So: task + reflection go BY PATH (launcher-written files); the workflow-composed head/ralph-note/findings/coda segments genuinely cannot (mid-workflow content, no fs) and keep the shipped chunked transport as the sanctioned fallback — which also satisfies the BUILD HAZARD rule (chunked path never removed).

- [x] Chunked agent transport with one end-to-end `<bytes>:<fnv32>` receipt over the assembled brief (#234) — the FALLBACK path; keep working, never regress, receipt semantics untouched.
- [x] Regression guard on the misreport: a build that never started no longer terminates as "inner loop exhausted N round(s) without Argus APPROVE" (#240; `trident/terminal-failure-reason.test.ts`).
- [x] **T1 — the wrapper assembles the brief from part files on disk.** `trident/codex-build.sh` gains `NEUTRON_CODEX_BUILD_BRIEF_PARTS` (newline-separated ordered absolute paths): when set, the wrapper concatenates the parts into `NEUTRON_CODEX_BUILD_BRIEF_FILE` itself, then runs the EXISTING integrity check unchanged over the assembled whole. Missing/empty part → new loud refusal `CODEX_BUILD_BRIEF_PART_MISSING` (exit 3, before any token). Env var absent → today's behavior byte-for-byte. Tests in `trident/codex-build.test.ts`: a >30 KB brief assembled from disk parts reaches the stub codex byte-identical with NO model in the transport; a deliberately corrupted part still hits `CODEX_BUILD_BRIEF_CORRUPT`; ordering is receipt-enforced.
- [ ] **T2 — the launcher writes the parts; the workflow stops retyping them.** `trident/inner-loop.ts`: at fire time write `task` and (when non-empty) `reflectionGuidance` to `/tmp/trident-brief-<runId>-{task,reflection}.part` via a TS port of `briefIntegrity`'s WTF-16→UTF-8(U+FFFD) encoding (parity test against the `.mjs` extract, lone-surrogate case included); thread `briefParts: { taskFile, taskIntegrity, reflectionFile, reflectionIntegrity }` through `buildWorkflowArgs`; a failed /tmp write falls back to `briefParts: null` (chunked path), never a dead fire. `trident/inner-workflow.mjs`: `forgeAgent`/`codexBuildPrompt` take the brief as ordered segments; a segment whose args-carried text matches its part-file receipt is carried by PATH in `NEUTRON_CODEX_BUILD_BRIEF_PARTS`; workflow-composed segments (contract head, ralph note, findings, coda + trailing `\n`) still cross via the chunked calls into per-run `.aN` files listed in the same manifest; receipt still computed once over the whole in-memory composition; a receipt MISMATCH between args text and part file throws a terminal error that NAMES args-transit corruption (never a silent fallback to possibly-corrupt in-memory text); `briefParts` absent → today's fully-chunked prompt exactly. Extend `trident/inner-workflow-assembly.test.ts`: with `briefParts`, a >30 KB task's bytes appear in NO agent prompt while the receipt covers them; without, the legacy prompt is unchanged. Update the stale transport docblocks (`codex-build.sh` header "THE BRIEF ARRIVES THROUGH AN LLM", `inner-workflow.mjs` `codexBuildPrompt`/chunking comments) and append the `docs/AS_BUILT.md` entry.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
