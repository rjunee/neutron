# IMPLEMENTATION_PLAN — Half the build wall-clock is re-planning and tick latency (Plan card 2026-08-15)

Governing spec: this Plan card (Part 1 = cheap `plan:next` continuation planner; Part 2 = wake-on-change for the 90 s tick). Verified against `origin/main` @ `cd8215e`:

MEASURED FACTS (survey, 2026-08-15):
- `trident/inner-workflow.mjs:3746-3762` — EVERY Ralph workflow invocation runs `agent(planFablePrompt(resuming), { label: 'plan:fable', ... })`; null plan is fatal at `:3755-3757`. `planFablePrompt()` at `:1436-1455` (steps 1-2 are the whole-repo survey = the 287 s). `ralphExecuteNote()` at `:1461-1470` — Forge persists+commits the returned `implementationPlan` verbatim with the task checked off, so the branch already carries the current plan.
- Continuation iterations ARRIVE AS RESUMES: `resuming = resumeCheckpoint !== null || prNumber !== null` (`:261`). A clean Ralph handoff is precisely `resumeCheckpoint === 'ralph-task-built'` (written WITH the head OID at `:3861` — `checkpoint('ralph-task-built', { pr, head: branchHead })`), and `classifyResume` (`:2110-2148`) returns `{ mode:'rebuild', reason:'unknown-checkpoint' }` for that name ONLY after the recorded-vs-live head comparison passed — head-moved/unreadable/absent return earlier with their own reasons. So `resumePlan.reason === 'unknown-checkpoint' && resumeCheckpoint === 'ralph-task-built'` is the code-verified "planned continuation, nothing moved in the crash window" predicate; every other resume shape is a genuine crash-resume and keeps the full planner + verbatim `resumeNote`.
- The iteration counter already exists durably: `run.ralph_round`, bumped per re-fire by `refireNextRalphTask` (`trident/orchestrator.ts:1124-1196`), but is NOT threaded into the workflow args (`buildWorkflowArgs`, `trident/inner-loop.ts:334-443`, threads `ralph: run.ralph` at `:345` and no round).
- The workflow script has NO direct shell — every fact it needs (checkpoints, head probes) comes from `agent()` Bash steps. So reading `git show <forgeBranch>:IMPLEMENTATION_PLAN.md` needs a cheap probe agent, and the plan:fable-vs-plan:next SELECTION must be plain code over args + probe output (testable via the `inner-workflow-assembly.test.ts` harness, which runs the real script body with mocked `agent()` and captures every `{label, prompt}`).
- `FORGE_SCHEMA` (`:804-816`) has NO deviation field today; in PR mode the build workflow exits at the publish handoff (`:3808-3819`) and `ralph-task-built` is written by the SECOND invocation (outer-published resume, checkpoint format `outer-published:<head>:<remaining>:<round>` regex-matched at `:2131`), so a Forge deviation signal must ride the publish handoff to survive to the next planner.
- `trident/tick.ts:196` — `interval_ms = tick_interval_ms ?? 90_000` into `SupervisedLoop({ intervalMs })`. `SupervisedLoop` (`loop/index.ts`) now HAS `wake()` with the pending-wake mid-flight guarantee (T3, commit `250a31a`), exposed through `TridentTickLoop.wake()`; `tick.test.ts` has a `TridentTickLoop.wake` describe block. `TridentRunStore.listNonTerminal` (`trident/store.ts:444`) already uses the module-private `TERMINAL_PHASE_SQL` (`:282`, `('done','failed','stopped')`) for the `phase NOT IN` filter the watcher query needs. Wiring: `gateway/composition/build-core-modules.ts` constructs the loop, `loopRegistry.register(loop.describe())`, `loop.start()`; shutdown awaits `loop.stop()` then `drain()` — start/stop signatures unchanged, so the watcher lives entirely inside `TridentTickLoop` and the composition file needs NO edit.

- [x] **T1 — `plan:next`: a continuation iteration plans from the committed plan, not from a whole-repo survey.** In `trident/inner-workflow.mjs`: add `PLAN_REFRESH_EVERY = 5` (named comment: the periodic full re-plan is deliberate — building task N can change task N+1; do not remove it to make numbers look better). At the Ralph plan site (`:3746`), when `ralph && resumePlan.reason === 'unknown-checkpoint' && resumeCheckpoint === 'ralph-task-built' && Number.isSafeInteger(ralphRound) && ralphRound >= 1 && ralphRound % PLAN_REFRESH_EVERY !== 0` (`>= 1`, corrected in T6: `ralph_round` is ZERO-BASED, so iteration 2 arrives as 1): run a cheap `plan:probe` Bash agent (`git fetch origin <forgeBranch> || true; git show <forgeBranch>:IMPLEMENTATION_PLAN.md`; returns `{planFound, uncheckedCount, planBody}`); if the probe returns a readable plan with ≥1 unchecked `- [ ]` task, run `plan:next` (same `PLAN_SCHEMA`, same model routing/phase as plan:fable, prompt embeds `planBody` + the task context, forbids reading SPEC.md or surveying the codebase, returns the committed body VERBATIM as `implementationPlan`, first unchecked task as `topTask`). Probe null/unreadable/zero-unchecked → fall through to the unchanged full `plan:fable`; `plan:next` returning null is fatal exactly as plan:fable null is (`:3755`). Thread `ralphRound: run.ralph_round` through `buildWorkflowArgs` (`trident/inner-loop.ts:345` area) + the args destructure (default null → full planner, fail-safe for legacy callers). `planFablePrompt` + `resumeNote` byte-identical. Behavioral tests via the assembly harness assert WHICH planner label fires per scenario (iteration 1 / clean continuation / round 5 / head moved / zero unchecked / probe dead / plan:next dead) — not merely that a function was called. DONE: commit `abd5436`.
- [x] **T2 — a Forge that deviated from the exec spec forces the next iteration back to the full planner.** Add OPTIONAL `deviatedFromSpec: boolean` to `FORGE_SCHEMA` (not required; `CODEX_FORGE_SCHEMA` untouched — absent → false) + one `ralphExecuteNote` line telling the executor to report material deviation. Local mode: write checkpoint `ralph-task-built-deviated` instead of `ralph-task-built` when set (classifyResume already sends unknown names to rebuild; T1's selection requires the exact clean name, so the deviated variant full-plans). PR mode: carry the flag through the publish handoff — publish result field → orchestrator appends a suffix to the `outer-published:` checkpoint (regex at `inner-workflow.mjs:2131` + `trident/orchestrator.ts` builder updated in lockstep) → the resumed invocation writes the deviated checkpoint variant. Tests: local + pr paths each produce a next iteration that runs `plan:fable`, and the non-deviating path still runs `plan:next`. DONE: commit `e4df54c`.
- [x] **T3 — `SupervisedLoop.wake()` with a not-lost-mid-tick guarantee, exposed through `TridentTickLoop`.** `loop/index.ts`: `wake()` = if not `started`, drop (no timer/tick may outlive `stop()`); if idle, `void this.runOnce()`; if a tick is in flight, set a pending-wake flag that `runOnce()` consumes after the in-flight tick settles by re-firing exactly once (`runOnce` is already single-flight, so a wake landing mid-tick would otherwise be `skipped` and the change missed for a full interval). `trident/tick.ts`: `wake(): void` delegating to the inner loop. Tests: wake-while-idle ticks once; wake-mid-tick re-runs exactly once after settle; wake-after-stop runs nothing; existing loop + `tick.test.ts` suites pass unchanged. DONE: commit `250a31a`.
- [x] **T4 — the 2 s change-watcher that gates the 90 s sweep.** `trident/store.ts`: `changeSignature(): string` — ONE query over `code_trident_runs WHERE phase NOT IN ${TERMINAL_PHASE_SQL}` (AMENDED in T7: the first shape was `COUNT(*) + MAX(last_advanced_at)`, which cannot tell the sweep's own write from an external one; it is now one `<last_advanced_at>\t<id>` line per non-terminal run, ordered by id). `trident/tick.ts`: a second internal `SupervisedLoop` ('trident-watch', `watch_interval_ms ?? 2_000`, `watch_interval_ms <= 0` disables) whose tick compares the signature to the last observed one and calls the main loop's `wake()` ONLY on change (first observation records without waking); it never calls `step`, never touches git/gh/network; a throwing query is contained by `guardedFire` and never stops the 90 s backstop, which stays exactly `tick_interval_ms ?? 90_000`. `start()` arms both, `stop()` quiesces both (watcher first). Tests: an out-of-band `last_advanced_at` bump (checkpoint.sh-style UPDATE) yields a full tick within one watcher cadence; a quiet interval yields ZERO extra tick-body runs; watcher throw → backstop still ticks; `stop()` leaves no live timer; `tick.test.ts` passes unchanged. DONE: built in this commit.
- [x] **T5 — AS_BUILT.md entry reporting the measured before and the honest after.** Record the card's measured baseline (287 s plan:fable per task; 3.0–3.8 min inter-cycle dead air; ~25 min + ~25 min over eight cycles) and what each fix removes mechanically; report any after-numbers actually measured on the next multi-task card rather than asserting the projection as fact. DONE: this commit.
- [x] **T6 — the review-round corrections (Argus round 1).** BLOCKER: `plan:probe` was the ONE optimisation-probe dispatch not wrapped in `seatAttempt`, so a throwing probe ended the whole lane — now `seatAttempt('plan-probe-round-N', …)` like `head-probe`/`ci-probe`, death → `null` → full `plan:fable`. Off-by-one: `ralph_round` is ZERO-BASED (`createRun` writes 0, `refireNextRalphTask` bumps per re-fire), so the `>= 2` gate made ITERATION 2 pay the full survey; gate is `>= 1` and the round matrix now covers 0-5 + 10. Integrity: the `plan:next` relay is checked against the probe's own measurements — the echoed `implementationPlan` against the verbatim `git show` body (mismatch → persist the COMMITTED body) and `remainingTasks` against `grep -c` minus one (mismatch → the measured count wins); the probe additionally reports a measurement of the file so a truncated relay is DETECTED rather than committed (`wc -l` here, `wc -lc` in T7, and a `cksum` checksum in T8 — counting could not close the byte-neutral cases), and it reads `origin/<branch>` in pr mode (the same authority `resolveResumeLiveHead` judged). Watcher damping: the sweep's own `saveIfActive` writes were being read as change, re-firing a full sweep every 2 s — the watcher now takes no sample while a tick is in flight and the baseline is settled at the tick's tail (post-sweep signature when it wrote, pre-sweep when it did not). `changeSignature()` MAXes over `replace(last_advanced_at,'Z','')` and checkpoint.sh stamps milliseconds, so a whole-second stamp can no longer mask a later millisecond one. `stop()` drops the observed signature; both timers register with the LoopRegistry via `describeAll()`. `CODEX_FORGE_SCHEMA` inherits `deviatedFromSpec` through the spread but the codex BRIDGE is never asked for it — documented in code as deliberate (absent → false), with the periodic full re-plan bounding drift on that route. DONE: this commit.
- [x] **T7 — the review-round corrections (Argus round 2).** The watcher's detector was AGGREGATE (`COUNT(*) + MAX(last_advanced_at)`), which cannot distinguish "the sweep re-stamped what it advanced" from "an out-of-process checkpoint landed mid-sweep": the settle absorbed the external checkpoint and sent the very handoff this card accelerates back to the 90 s backstop. `changeSignature()` is now PER-RUN and the sweep keeps a ledger of every row it committed → the stamp its own write left, so `changedOutsideSweepWrites` decides the question exactly. `checkpoint.sh` stamps milliseconds (`%FT%T.%3NZ`, validated with a whole-second fallback for a `date` without `%3N`). The plan relay's integrity guard gained `wc -c` alongside `wc -l`, because a line count alone passes a reword. DONE: commit `1651e55`.
- [x] **T8 — the review-round corrections (Argus round 3).** Two confirmed majors on the plan relay, both closed by measuring instead of counting: (a) the probe's `uncheckedCount` and `planBody` were never compared with each other although the count overrides `remainingTasks` (the re-fire gate) and the body overwrites the committed plan — the body's own `- [ ]` lines must now equal the probe's `grep -c`; (b) `wc -lc` passed the whole class it claimed to close — a byte-neutral `- [ ]` → `- [x]` flip, a re-order, and a one-byte truncation through the trailing-newline window — so the probe now reports `cksum` (POSIX CRC-32 + bytes) and the workflow recomputes it over the relayed body with `cksumOf()`, cross-checked against the real binary in the tests; a probe that omits the checksum fails the guard. Watcher: a tick that dies on `listNonTerminal` no longer consumes the wake it was fired for (it leaves the baseline alone and owes a re-fire). `watch_interval_ms` is plumbed through the production wiring. Comment/doc drift fixed (`checkpoint.sh`'s stamp format and the retired COUNT+MAX detector, `AS_BUILT`'s `resumeClass` → `resumePlan`), and the card's "unchecked tasks must match the recorded remaining count" fallback is RECORDED AS A DEVIATION in AS_BUILT (no durable channel carries the previous remaining count; it needs a column, and the bound is the periodic full re-plan). DONE: this commit.
