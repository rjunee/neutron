# IMPLEMENTATION_PLAN — Git truth comes from git (Parts 1–3, Plan card 2026-08-15)

Governing spec: the Plan card ("Git truth comes from git"). Verified against `origin/main` = `8dbfcdb` (PR #283 = `358131f` is merged; the LOCAL clone at `d8324cc` is STALE and must not be built against — fetch and branch from `origin/main` first).

Measured facts (all against `origin/main`, file `trident/inner-workflow.mjs` @ 4,182 lines):
- `orchestrator.ts:782-804` `publishBuiltCommit` rev-parses `refs/heads/<branch>`, treats `claimedHead` as a prefix-check only, and refuses a mismatch naming BOTH values verbatim; `:877-880` refuses zero-commits-ahead ("left no new commits to publish"). Part 1 + Part 3 item 3: DONE.
- `inner-workflow.mjs:3810-3818` and `:3955-3961`: the PR-mode publish handoffs no longer throw on sha shape (`oidClaim`, "NO THROW ON SHA SHAPE"); covered by `inner-workflow-publish-handoff.test.ts`. DONE.
- `classifyResume` (`:2108+`) exists with the bounded STOP on an unreadable head, and the launcher threads a code-read live head; covered by `inner-workflow-resume.test.ts`. Part 2: DONE.
- STILL DEFECTIVE: `:3780-3809` sets `branchHead = forgeSha` (the MODEL's report) and records it in the `forge-done` checkpoint; `:3804` `reviewedHead = branchHead`; the fix-round twin `:3951-3958` checkpoints `fix?.commitSha` and `:4015` pins `reviewedHead` to it. No git read happens at build completion in either mode; a local-mode build whose agent reports no sha still dies at `:3881-3887` ("produced no commitSha — nothing was built") with the commit sitting on the branch.

- [x] **T1 (Part 1) — the outer publisher reads the OID from git; an agent sha is a check, not a source; zero-ahead still fails.** Shipped in #283: `orchestrator.ts:776-804, 861-880` + `publish-rebase-realgit.test.ts` / `inner-workflow-publish-handoff.test.ts`.
- [x] **T2 (Part 1) — the inner publish handoffs never discard a finished build over sha shape.** Shipped in #283: `inner-workflow.mjs:3810-3818`, `:3955-3961`, `oidClaim` (`:2057-2067`).
- [x] **T3 (Part 2) — `classifyResume` stops (bounded) on an unreadable head instead of rebuilding; the launcher reads the live head in code.** Shipped in #283: `classifyResume`, `launcherLiveHead` (`:3602`), `inner-workflow-resume.test.ts`.
- [x] **T4 (Part 3 item 3) — `publishBuiltCommit`'s refusal names the requested OID and the actual OID.** Already shipped: `orchestrator.ts:800-803` ("the build reported commit 'X' but branch B resolves to 'Y'").
- [x] **T5 (Part 3, THE REMAINING HALF) — the build-completion head is READ from git at both completion sites; the agent's `commitSha` becomes a cross-check that fails loudly on disagreement; an unreadable read is retried then STOPS boundedly (never rebuild, never a discarded build, never "without Argus APPROVE").** New `readBuiltHead` probe (local `git rev-parse --verify refs/heads/<branch>`, one retry) at the round-1 site (`:3780`) and the fix-round site (`:3951`); `branchHead`/`reviewedHead`/checkpoint heads sourced from it; mismatch → typed infra-only terminal stop naming both values verbatim; local-mode no-sha builds now proceed pinned to the git-read OID; PR-mode handoff unchanged (publisher stays authoritative). Full test plan in the execution spec, including harness updates across the workflow-body test suites and a reason-quality assertion through `innerTerminalFailureReason`.

NOT tasks of this build (guard rails): do NOT loosen `publishBuiltCommit`; do NOT re-probe the head at publish/merge time (read ONCE at build completion — state this distinction in the PR body); do NOT touch branch `trident/derive-inline-activity-from-evidenc` (`9a46680`, one unpushed commit ahead of PR #263 — it needs a PUBLISH via the publish-retry card, not this build).
