# IMPLEMENTATION_PLAN — mutation gate: a test-infrastructure diff gets a legal outcome (PR #490)

Card: the mutation-proof gate had no reachable outcome for a diff whose only changed .ts files are declared tests — PR #489 (APPROVED, all checks green) could never merge because every changed file was rejected as a mutation target by the path-based test-file rule while the gate still demanded a proof, and the refusal blamed the build for an omission it could not avoid. Fix, all on this branch: classify targets by what DECLARES a test rather than a path prefix; state the anti-tautology rule directly (a guard may not run the mutated file as its own test); give the no-production-file diff an explicit recorded exemption distinct from prose-only; split the required-but-missing refusal into "a legal target existed and none was named" vs "no legal target existed", naming a disqualified file.

State (2026-09-03, round 24): PR #490 OPEN at 766ae106 == origin, MERGEABLE / CLEAN, 17 of 17 checks pass at that head. Requirements 1-4 are built and tested; rounds 1-23 closed twelve forged-proof escapes of the load-hook/runner-provenance family (the pytest module-shadow escape closed at 62638679 — done, do not redo). Round 24 is the round-8 review brief, one blocker first: (JOB 1, the Argus VETO) THE CONTROL CAN FORGE THE PROOF — `proveInWorktree` restores only `claim.file` between the mutated observations and the restored-guard observation, so a control whose module body rewrites the guard's own committed test file (or a `make` wrapper doing the same write) returns `ok:true, proved:true` with nothing asserting the target; reproduced by a seat at this head, pre-existing at the base. Close it at that seam: refuse tracked edits by name off `git status --porcelain`, then re-provision (`reset --hard` to the pinned head plus `clean -fdx`) before the restored-guard observation, and correct the control.description sentence THIS branch added claiming a control "merely has to stay green" (falsified by the repro). (JOB 2) the inherited-preload arm's own over-refusal is RECORDED rather than scoped — see the Deferred paragraph; the seam comment and control-side pinning row still to land. (JOBs 3-4, only after 1 and 2) pin the direct-`__tests__`-child exemption consequence as deliberate, and make the schema's node manifest clause carry `main` with an agreement test that DERIVES the key list from the prover source. Every round runs the FULL suite (`bun install --frozen-lockfile` first) and reports testsPassed with the runner's own summary line; the suite red is pre-existing and settled, so suiteOutcome='failed-preexisting' with the settled environmental failures named.

THE PLAN LIVES AT THE REPO-ROOT IMPLEMENTATION_PLAN.md ONLY. The file at trident/IMPLEMENTATION_PLAN.md is NOT this card's: at this branch's head it is byte-identical to origin/main's copy (an EARLIER MERGED card's plan), and zero commits on this branch touch that path. Leave it alone.

- [x] D1 — declared-test classifier in trident/mutation-prover.ts: basename `\.(test|spec)\.(?:[cm][jt]s|[jt]sx?)$` / `_test\.(go|py)$` and direct `__tests__/` children are tests (rejected as before); other files under a top-level `tests/`/`test/` are support libraries and LEGAL targets; documentation rejection unchanged.
- [x] D2 — anti-tautology check: a claim whose guard argv reaches `claim.file` is refused, with the refusal naming the tautology (every operand the runner reads is considered, not only recognised selectors).
- [x] D3 — explicit exemption when git says no production file changed, with its OWN reason string distinct from the prose-only one, recorded in the run record rather than silent.
- [x] D4 — the refusal distinguishes "the build nominated no mutation (a legal target existed)" from "no legal target existed", naming at least one considered-and-disqualified file.
- [x] Tests — red-then-green proof against a `tests/` support library with an unrelated control; classification table over the card's paths; tautology refusal; exempt reason is NOT the prose-only string; the load-bearing negative (a production file in the diff plus no claim is still REFUSED); positive controls against silently-empty extractions.
- [x] Wiring — trident/orchestrator.ts, trident/inner-workflow.mjs, trident/inner-loop-sim.ts carry the new outcomes; four production files changed on this branch.
- [x] CodeQL ReDoS on the loadable-scheme regex fixed (cfc38542), confirmed green by GitHub. Do not touch `LOADABLE_SCHEME`.
- [x] Round-7 BLOCKER: a package-script runner FORWARDS a bare positional to the script's own command line without `--`; `pathArgs` and `whyNoSelection` now drop every positional for npm/pnpm/yarn, with the previously-legal shape pinned as refused and a bun-spelled positive control beside it.
- [x] Round-8 BLOCKER: the wrapper hole reopened under `make` (a positional is a second GOAL, not a forwarded selector); `forwardsPositionalsToAScript` covers `make`, `whyNoSelection` gained a make-shaped sentence, with unit rows and a real-git end-to-end refusal plus positive control.
- [x] Round-9 BLOCKER (two reviewers, two routes): the guard-tautology invariant was bypassable via the space-separated option value (`carriedValueReaching` now pairs each operand with the bare option before it) and via the wrapper (`npm run …` whose branch-authored body preloads the target) — a wrapper is no longer a legal GUARD for any target and stays a legal CONTROL. Unit loops for both routes with positive controls, plus one real-git end-to-end test.
- [x] Round-9 major: the percent-decode inside `argvEscapesTheWorktree` is pinned by a killing test in three spellings, with a `%2D` positive control.
- [x] `TICK_NOTE_CEILING` pinned to 240 exactly.
- [x] EMIT-THE-NOMINATION rider: RETRACTED — see the round-16 item below for why it can never fire on this route.
- [x] Round-10 BLOCKER: an INDIRECT LOAD HOOK (`--preload=./tests/setup.ts` whose body imports the target) forged a proof; `loadHookCarrying` refuses the SHAPE (`--preload`, `--require`/`-r`, `--import`, `--loader`, `--experimental-loader`, in every spelling) whatever file it names, for EVERY target. Six refused spellings pinned plus four positive controls.
- [x] Round-10 major: FORGE_SCHEMA's guard description names every refused opaque shape, and a test reads both lists OUT OF THE PROVER'S SOURCE so the two can only agree by being the same list.
- [x] Round-11 BLOCKER: a REPORTER OPTION is a load hook (`--test-reporter|--reporter|--reporters` joined `LOAD_HOOK_OPTION`, anchored so `--test-reporter-destination`/`--reporter-outfile` stay legal); schema names all three; agreement test pins containment; six refused unit spellings, a positive control, and a real-git test that COMMITS a reporter module importing the target and refuses the guard before anything executes.
- [x] Round-12 BLOCKER: config spellings visible in the argv (`--experimental-config-file`, the valueless `--experimental-default-config-file`, short `-c`) joined the refused family; a LONG hook name is refused on its NAME, value or none; unit rows and positive controls (`go test -cover ./cmd/`, trailing `go test ./cmd/ -c`) pinned; both arms verified load-bearing.
- [x] Round-12 minors: three rows re-spelled off `--reporter`; tick-note/stage-reason docblocks corrected.
- [x] Round-13 BLOCKER: `node --run <script>` refused at the SHAPE (`isNodeRunOption`, both spellings), and go's one-dash hooks `-exec`/`-toolexec`/`-overlay` refused via `GO_TOOLCHAIN_HOOK_OPTION` matched WHOLE before the short-option reading; schema names all four; agreement test reads the go names out of the prover source; unit rows plus positive controls (`node --test tests/other.test.mjs`, `go test -count=1`, `go test -execute-nothing=1`, `go test ./pkg/other`).
- [x] Round-13 minors: the carried-value refusal fragments compose into grammatical sentences; `carriedValueReaching` and `extensionCompletions` pinned as load-bearing by name.
- [x] Round-14 BLOCKERS: `--tsconfig-override` (a resolution map) joined `LOAD_HOOK_OPTION` with schema/agreement/docblock coverage, and the node shape requires `--test` FIRST (`argv[1] === '--test'`) so `node <script> --test` can no longer wear the runner's name; regression rows and positive controls; the committed `tests/tsconfig.json` residual recorded.
- [x] Round-15 BLOCKER: `--conditions`/`-C` (the resolution map with the file left implicit) refused in both spellings; schema names both; agreement test pins fifteen hook names plus `-C…`; six refused rows with `--conditions-file=…` and `go test -cover ./cmd` kept legal and RUNNING; both arms verified load-bearing.
- [x] Round-16 — REQUIREMENT 5, built at 8f57c727: forgeBuildContract now ASKS for the nomination it gates merges on (report-step field list names mutationClaim; a titled MUTATION NOMINATION block states legality, the grep verification, and when null is legal), pinned by a prompt-capture test with a positive control against empty extraction. THE EMISSION RIDER IS RETRACTED AS UNREACHABLE, measured against the deployed checkout: on the codex route the schema is filled by the codex BRIDGE, which copies a six-line wrapper trailer (BRANCH/HEAD/REMOTE_HEAD/PR/DIFF/WORKTREE) carrying no nomination line, so it fills `null` — the only honest answer available to it — regardless of what any brief or plan says. The contract edit is correct latent work that takes effect only once merged AND deployed; the transport (a seventh trailer line, the bridge copying it) is a SEPARATE card. No round of THIS card should attempt to emit `mutationClaim` or build that transport.
- [x] Round-17 BLOCKER (round-10 seat, reproduced against 4629df58 — the ELEVENTH escape of the family): a branch forges a proof by SHADOWING an allowlisted python module. Commit `pytest/__main__.py` whose body imports the target, nominate guard `python3 -m pytest tests/unrelated_test.py` for a syntax-breaking mutation with an unrelated control: `-m` puts the working directory first on the module search path, so the worktree's own `pytest` package — branch-authored code — IS the runner; red mutated, green control, green restored, nothing asserted. The argv is clean; the forgery is an ENTRY IN THE TREE. Closed on RUNNER PROVENANCE at the worktree seam in `proveInWorktree` via `pythonModuleShadow(argv, topLevelNames)` off `git ls-tree` (fail closed), with FORGE_SCHEMA sentence, two-sided literal pin, unit rows and a real-git refusal plus bun-spelled positive control.
- [x] Round-18 BLOCKER/major (Argus round 1 on the round-17 fix): the round-17 provenance probe closed the NOMINATED MODULE and left two siblings open. Both refused at the same seam off ONE RECURSIVE listing: (a) `pytestConfigShadow` — a committed `conftest.py`/pytest ini at any depth, asked only for `-m pytest`; (b) `pythonImportShadow` — any top-level `*.py`/`*.pyc`/`*.so` refused for a `python3 -m` nomination. Legal-table over-refusal guards (`src/limit.py`, `tests/unrelated_test.py`) keep a python file a directory down legal. Also: the no-production-file exemption's file list capped by TOTAL LENGTH with the elision COUNTED; the dotted-module split annotated as unreachable defence-in-depth.
- [x] Round-19 BLOCKER/major (Argus round 2 on the round-18 fix): the TWELFTH escape — a top-level DIRECTORY the runner imports (`tests/__init__.py` whose body imports the target; `argparse/__init__.py` shadowing a runner dependency). `pythonPackageShadow` closes both off the same recursive listing, refusing on the directory's CONTENTS (a REGULAR package: `__init__`/`__main__` marker) and never its name; namespace packages, nested markers and a root `__init__.py` stay legal. Unit repro + predicate table with seven over-refusal guards + real-git refusal with bun positive control. Also: the DELETION-ONLY refusal's file list goes through `namesWithinBudget`; its docblock states the two deliberate budget excesses.
- [x] Round-19 recorded, no code (Argus round 2 minors/nits): (a) `python3 -m pytest` is de-facto UNNOMINATABLE for any conventional python repo — fails CLOSED, costs this repo nothing, spellable around; recorded so `TEST_COMMAND_SHAPES`' pytest entry is not read as a supported nomination. (b) `_test\.rs` is deliberately NOT in the declared-test basename rule — cargo has no such convention; the narrowing is intentional and must not be "restored". (c) The round-18 suite comparison mislabelled the merge-base; the `failed-preexisting` conclusion is unaffected.
- [x] Round-20 BLOCKERS/majors (Argus round 3): (a) THE THIRTEENTH ESCAPE — `TOP_LEVEL_PACKAGE_MARKER` now carries the `py|pyc|so` suffix class (bytecode-only package). (b) THE SAME QUESTION ASKED OF BUN: `bunConfigCandidates`/`bunConfigLoadHook` refuse a branch-changed `bunfig.toml` preload or tsconfig `paths`/`baseUrl`, diff-scoped via `ProveInput.changed_files`. (c) the single-seat ASSERTION-FREE GUARD recorded at the oracle as the standing LIMIT of mutation proofs. (d) schema pins for the new arms; `control`'s description states what `validateClaim` really enforces. (e) the python AGGREGATE over-refusal stated at `TEST_COMMAND_SHAPES`. (f) `REASON_NAME_BUDGET` 3,600; the forge contract's occurrence check spelled with an occurrence count, not a line count.
- [x] Round-21 BLOCKER/major (Argus round 4): the bun config arm matched ONE SPELLING of a key the parser accepts in four, and did not read the manifest at all. `TOML_PRELOAD_KEY` reads the key unanchored, quote-insensitive, over decoded TOML escapes; `package.json` joins the candidates refused on `imports`/`exports`. Eight-spelling tables both directions, FORGE_SCHEMA states the spellings and the second map with two-sided pins. Also: the `extends` rationale rewritten; `truncateNote`/`truncateStageReason` folded onto `truncateWithPointer`; the python `-m` aggregate carried as a NAMED follow-up card.
- [x] Round-22 BLOCKER/majors (Argus round 5): (a) the arm refused the FILE in the diff rather than a BRANCH-AUTHORED key — closed by comparing the KEY'S VALUE at head vs merge base (`bunConfigHookSlice`, `hookProvenance`), every step failing CLOSED, with real-git bump-vs-rewrite pair. (b) `node` joins `bunConfigCandidates` FOR `package.json` ONLY. (c) `TOP_LEVEL_PACKAGE_MARKER` carries ABI-tagged `__init__` spellings. (d) FORGE_SCHEMA's `control` description no longer says "no wrapper"; the guard description carries the node manifest arm and the value-comparison rule.
- [x] Round-23 BLOCKERS/majors (Argus round 6): (a) the bun/node config arms asked who wrote the CONFIG and a branch walked between them by rewriting THE FILE AN INHERITED PRELOAD NAMES — closed by `bunConfigInspected` (root bunfig read whether or not the branch touched it) plus `hookTargetChangedByBranch` (refuses when the preloaded file is one the BRANCH changed, whoever wrote the config), with spellings canonicalised, loader completions as candidates, the authorship arm asked only of a config IN THE DIFF, real-git end-to-end verified load-bearing, and the resolution maps deliberately NOT widened the same way. (b) `main` joins `MANIFEST_HOOK_KEY` with the identical merge-base value comparison (the codex seat's minority veto, reproduced on node v22). (c) `hookProvenance` distinguishes 'added' from 'branch' so a NEW manifest is refused with a remedy that exists. (d) `TOML_PRELOAD_KEY` looks at its delimiter instead of consuming it; the `TEST_COMMAND_SHAPES` python aggregate notes the config over-refusal is wider than "a python repo".

## Round 24 — the round-8 review brief. JOB 1 IS THE ROUND; 2-4 only after it.

- [x] Round-24 JOB 1 (Argus VETO — THE CONTROL CAN FORGE THE PROOF): `proveInWorktree` runs observe(guard) → observe(control) → restores ONLY `claim.file` → observe(guard) again, and nothing checks what the nominated commands did to the rest of the tree — a control whose module body rewrites the guard's own committed test file (or a `make` wrapper doing the same write) returned `ok:true, proved:true` at 766ae106 with nothing asserting the target. Close it at that seam, all three steps failing CLOSED: after the mutated observations and the target restore, (1) `git status --porcelain` — refuse by name when any TRACKED file was edited (lines not starting `??`), through the `namesWithinBudget` cap; (2) re-provision with `git reset --hard <headSha>` + `git clean -fdx` so untracked/ignored plants cannot reach the restored observation either (runner caches tolerated in (1), neutralised here); (3) only then observe the restored guard. No evidence-schema change; `prover_version` stays 1. Correct the control.description sentence in `trident/inner-workflow.mjs` (:911) that says a control "merely has to stay green" — state the real reason a wrapper stays a legal control (the tree is re-provisioned before the observation that decides the proof), keeping the pinned literals `LEGAL CONTROL` and `refused as a GUARD`, and add a new pin plus two-sided prover-source pins for the porcelain/reset/clean calls. Tests: real-git repro REFUSED with the tamper reason (verified to return ok:true, proved:true with the block reverted — say so in the comment); honest-claim positive control still proves in the same repo; untracked-plant row load-bearing for `clean -fdx` (guard asserts the planted file is absent restored); unit rows for the tracked-edit refusal (guard observed exactly once), the untracked tolerance with reset+clean ordered before the second guard run, the three fail-closed git-failure routes, and a 1000-line porcelain bounded by the name budget. Then run the FULL suite (`bun install --frozen-lockfile` first) and report with the runner's own summary line; suiteOutcome='failed-preexisting' naming the settled environmental failures.
- [ ] Round-24 JOB 2 (record the inherited-preload over-refusal — the Deferred paragraph below already names it): add the seam comment at `bunConfigInspected`/`hookTargetChangedByBranch` stating the aggregate (a diff touching a root-bunfig-preloaded support file has a legal TARGET under D1 but NO legal bun nomination for guard OR control, and `node --test` cannot run `bun:test` files, so neither printed remedy is available in a bun-only repo; fails CLOSED; relief is the named follow-up card), plus one control-side pinning row: root bunfig preload pointing at a branch-changed file refuses `claim.control` too, with the comment carrying the follow-up card's name.
- [ ] Round-24 JOB 3 (downgraded codex blocker, fails closed): a non-test helper DIRECTLY under `__tests__/` is a declared test, so an all-such diff buys the no-production-file exemption — the design is already recorded at the gate's soft-edge comment (1) (it names `src/__tests__/impl.ts` as the directory route) and the boundary rows exist (`src/__tests__/limit.ts` → test, `a/__tests__/support/helper.ts` → production). Make the CONSEQUENCE executable: one `runMutationProofGate` row where the diff is only `src/__tests__/impl.ts` + prose → exempt with the no-production-file reason (NOT the prose-only string), files named, with a comment stating this is deliberate (same build-written-names trust surface as the basename route, made reviewable by the full file list in the reason) and that the 13-support-libraries deferral stands.
- [ ] Round-24 JOB 4 (downgraded codex blocker, contract drift): FORGE_SCHEMA's guard description says "a package.json `imports`/`exports` map refuses `node --test` too" while the prover refuses a branch-authored `main` for node as well (`MANIFEST_HOOK_KEY`, `bunConfigCandidates` node arm). Reword the node clause to carry all three keys, and make the agreement test DERIVE the key list from the `MANIFEST_HOOK_KEY` literal in the prover source (parse the names, expect at least three as the positive control, and assert the node clause contains the joined `imports`/`exports`/`main` triple immediately before "map refuses `node --test` too") so a fourth key cannot drift the two apart; keep the existing literal pins.

Named follow-up card, opened by round 21 at a reviewer's request: **"trident: diff-scope the python runner-provenance arms so `python3 -m` is nominatable in a conventional python repo"** — `pytestConfigShadow` (matches `pyproject.toml`/`setup.cfg`/`tox.ini` at ANY depth), `pythonImportShadow` (any top-level `*.py`/`*.pyc`/`*.so`, so a root `setup.py` is enough) and `pythonPackageShadow` (`tests/__init__.py`) between them delete BOTH python spellings from `TEST_COMMAND_SHAPES` for the standard layout, leaving the allowlist entry unreachable in the repos it exists for. It fails CLOSED — an over-refusal, never a forged proof — and the fix is the scoping `bunConfigCandidates` already uses: read only the files THIS BRANCH CHANGED, which is sound because a config main already carried is main's, reviewed like any other merged file. Not done here because widening a forgery defence is not a side effect of a fix round.

Named follow-up card, opened by round 24 (JOB 2 of the round-8 brief): **"trident: scope the inherited-preload arm so a diff touching a root-bunfig-preloaded support file keeps a legal bun nomination"** — today such a diff has a legal TARGET (D1) and no legal GUARD or CONTROL: `bunConfigInspected` reads the root bunfig for ANY bun argv, `hookTargetChangedByBranch` refuses whoever wrote the config, the refusal covers the whole nominated pair, and `node --test` cannot run `bun:test` files, so in a bun-only repo neither printed remedy exists. Fails CLOSED. A candidate relief for the card to weigh: permit the pair when the preloaded file IS the mutation target AND the control runs under the same preload, so a load-crash reds both observations symmetrically and only a behaviour-asserting guard can red alone — a widening of a forgery defence, so it is that card's job, not this branch's.

Deferred to follow-up cards (every one recorded; most latent and failing closed, the one marked OPEN is a forgery-family spellings awaiting their own cards): the `.trident` prose-directory question; OPAQUE SCRIPT RUNNERS (the family is not closed by construction — argv-visible spellings are refused as found); PYTHON DEPENDENCY SHADOWING BY DIRECTORY is CLOSED at round 19 by `pythonPackageShadow` (contents, not name); what remains latent is the NAMESPACE-package half — a top-level directory with no `__init__.py`/`__main__.py` shadows a name but executes nothing on import, so it cannot forge a proof, and it is not refused. The committed `tests/tsconfig.json` residual stays (the python `conftest.py` half of that class is closed at round 18); fixtures/snapshots under `tests/` classifying production; the direct-`__tests__`-child rule declaring 13 support libraries tests; unittest `test*.py` and `tests/*.rs` classifying production yet un-nominatable; deletion-only production diffs unclearable; the sim's `{ok:false, exempt:true}` shape; the exempt stamp written before the merge attempt; mocha's `-R` and pytest's `-p` short letters; and the codex build route's reporting transport for `mutationClaim` (the retraction above — a seventh trailer line and the bridge copying it, its own card). Round-23 addition: WHERE A RESOLUTION MAP POINTS is not reasoned about (only where a `preload` points is) — a branch-changed file reached through an inherited `imports`/`exports`/`main`/`paths` map is the general transitive-import problem, which three exit codes cannot decide, and refusing on it would delete the bun nomination from nearly every PR; and `ProveInput.changed_files`/`base_ref` remain OPTIONAL, defaulting the whole config-provenance arm to OFF when omitted — correct at today's single wired call site (`runMutationProofGate`, pinned by a test), but a new caller could silently re-open a closed forgery class, so making them required on `proveInWorktree` is its own card. (The round-21 addition — the package.json arm as the WIDEST over-refusal on this seam — is CLOSED at round 22 and is no longer deferred.) Round-20 additions, all single-seat and unverified: runner-DISCOVERED test names (`src/thing_test.ts`, `testfoo.py`, `*_spec.ts`) classify production, so a test-only diff in those conventions misses the exemption; the 13 support libraries directly under `__tests__/` now also BUY that exemption rather than merely failing to be nominatable; and `unittest discover -s <dir>` makes a SUBDIRECTORY the first search-path entry too, which the top-level-only scan does not read (no execution escape was demonstrated). Round-24 additions: (a) THE INHERITED-PRELOAD ARM'S OWN OVER-REFUSAL — this repository's root bunfig preloads two support files under `tests/support/`, so a diff touching either one keeps its legal target and loses every bun guard AND control while `node --test` cannot run `bun:test` files; recorded above as a named follow-up card, fails CLOSED. (b) THE PROCESS-BOUNDARY RESIDUAL of the JOB-1 fix — a nominated command that leaves a BACKGROUND writer running past its own exit, or reaches OUTSIDE the worktree through the shared git directory or the executing user's own permissions, can still race the porcelain check and the re-provision; that is the same-user process boundary, a machine-level surface no in-tree seam can close, and the porcelain-plus-re-provision pair closes the whole in-tree, in-sequence family the reviewer reproduced. The full-repo suite red is pre-existing and environmental: 142 pass / 23 fail with byte-identical failure line-sets at base and head after timing-stripping, none of it under `trident/`.
