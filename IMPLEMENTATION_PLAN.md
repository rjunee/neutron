# IMPLEMENTATION_PLAN — The publisher must read the commit OID from git, not take a model's word for it (defect 2026-08-14, run 3d2696c3)

Governing spec: this Plan card. Verified against main 2026-08-14; RESUMED on branch `trident/the-publisher-must-read-the-commit` (T1 landed as `04e9d35`).

- The sha was round-tripped through a MODEL at three points. (1) `trident/inner-workflow.mjs` build handoff and fix-round handoff THROW `…completed without a full local commit OID for the outer publisher` when `forge.commitSha` is not 40-hex — the exact crash that killed run `3d2696c3` after a successful build (worktree-cleanup kept the branch at `9a46680`, one commit ahead of PR #263). On the resumed branch these sites are `inner-workflow.mjs:3529` (build) and `:3671` (fix-round) — STILL THROWING; T2 removes them. (2) `trident/inner-loop.ts` decoded `publish_head` to null unless full 40-hex — FIXED in T1: the decode now keeps any 7-40-hex claim verbatim (`inner-loop.ts:482-487`). (3) `trident/orchestrator.ts` used the agent-supplied sha as the PIN — FIXED in T1: `publishBuiltCommit` resolves the head itself via `rev-parse --verify` on the branch the inner loop NAMES; an agent sha is a CHECK only (full 40 = equality, 7+ = prefix), a mismatch fails naming BOTH values verbatim, and a zero-ahead branch fails as "nothing was built" instead of false-publishing.
- The #259 lease machinery in `publishBuiltCommit` (observe `ls-remote` → `--force-with-lease=<ref>:<observed>` → witness → empty-diff refusal, with git's stderr carried in failures) was preserved by T1; pin, witness comparison, diff range, and `outer-published:` checkpoint re-keyed onto the resolved head.
- Reason quality: no failure reason in this class reads "without Argus APPROVE" — Argus never ran. All publish failures flow through `publish failed: <reason>` in `applyResult`; T1 tests assert it.
- Recovery: `trident/derive-inline-activity-from-evidenc` @ `9a46680` needs a PUBLISH, not a rebuild — the branch must not be deleted (T3 records this; the retry-publish mechanism itself is card `01KZZS9ZKVJM658ZYE6ZRBBJJ2`, out of scope here).

- [x] **T1 — the publish boundary reads the OID from git: `publishBuiltCommit` resolves the head via `rev-parse --verify`, demotes an agent sha to a prefix CHECK (mismatch fails naming both verbatim), refuses a zero-ahead branch, and the decode accepts a 7-40-hex claim.** Landed as `04e9d35`: `trident/orchestrator.ts` (`publishBuiltCommit` + the `applyResult` publish branch), `trident/inner-loop.ts` decode + field doc, fixture updates + acceptance tests in `trident/orchestrator.test.ts`, decode cases in `trident/inner-loop.test.ts`.
- [x] **T2 — the inner workflow stops throwing on a missing/abbreviated `commitSha` at BOTH publish handoffs** (`inner-workflow.mjs:3529` build, `:3671` fix-round on the resumed branch): the handoff is the BRANCH NAME; `publishHead` becomes best-effort — the trimmed `commitSha` when it is 7-40 hex chars, else `null` — and the `publishResult` is written either way (never a throw for sha shape). Keep `ported-fixes.test.ts`'s `publishRequested: true` source assertion green; add workflow-level coverage (real-body harness, PR mode) that a no-sha build still writes a `publishRequested` terminal result, an abbreviated sha is carried verbatim, and the throw string is gone from the source.
- [ ] **T3 — changelog + recovery note:** `docs/AS_BUILT.md` entry for this fix, including the standing instruction that `trident/derive-inline-activity-from-evidenc` @ `9a46680` is one commit ahead of PR #263 and must be PUBLISHED by hand (never branch-deleted, never rebuilt) until publish-retry (`01KZZS9ZKVJM658ZYE6ZRBBJJ2`) lands.
