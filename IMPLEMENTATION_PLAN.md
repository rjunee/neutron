# IMPLEMENTATION_PLAN — Git truth comes from git: the publisher reads the commit OID (Part 1) and an unreadable head stops, never rebuilds (Part 2)

Governing spec: this Plan card (both parts — one root cause: a git fact routed through a model instead of read from git). Re-surveyed 2026-08-15 against **current `origin/main` = `39e3b6b`** ("recover a build whose launcher died", #267). ALL work is authored on branch `trident/git-truth-comes-from-git-the-publis`, whose tip sits ahead of that `origin/main` — fetch first; the stale local clone must never be the base.

**State of the world, verified at the branch tip:**
- Part 1 is DONE on this branch: `ae381e2` ("reconcile the read-the-OID publisher onto the post-rebase publish flow (T4)") re-authored T1's publisher semantics inside the post-#275 `publishBuiltCommit` and carries T2's handoff changes (`publishHead: oidClaim(...)`, no throw on sha shape) plus `inner-workflow-publish-handoff.test.ts`.
- Part 2a is DONE (T5, the commit carrying this plan update, one commit ahead of `ae381e2`): the live head at resume is read IN CODE by the launcher (`resolveResumeLiveHead` in `orchestrator.ts`) and threaded to the workflow as `resumeLiveHead`; the `head-probe-round-resume` agent seat now runs ONLY as the fallback for launchers predating that arg. `classifyResume` names a genuinely gone branch `head-branch-absent`.
- Part 2b is still ABSENT: `classifyResume` still maps an empty `currentHead` → `{ mode: 'rebuild', reason: 'head-unreadable' }`, and `inner-workflow-resume.test.ts` still pins that rebuild behaviour.
- Standing hazard: `trident/derive-inline-activity-from-evidenc` is one commit ahead of PR #263 (`9a46680` on the production host) — never reset/delete/force past it.

- [x] **T1 (Part 1) — the publish boundary reads the OID from git**: `publishBuiltCommit` resolves the head via `rev-parse --verify` on the branch the inner loop NAMES; an agent sha is a CHECK only (full 40 = equality, 7+ = prefix); a mismatch fails naming BOTH values verbatim; a zero-ahead branch fails as "no new commits"; `inner-loop.ts` decode keeps a 7–40-hex claim verbatim. LANDED (originally `5cc33da`, re-authored into `ae381e2`).
- [x] **T2 (Part 1) — the inner workflow hands the publisher a BRANCH NAME, not a model-relayed sha**: both publish handoffs (`forge-done` build, `fix-round-N`) write `publishHead: oidClaim(...)` (best-effort, null when implausible) and never throw for sha shape; workflow-level coverage in `inner-workflow-publish-handoff.test.ts`. LANDED (originally `dd15bc2`, re-authored into `ae381e2`).
- [x] **T4 — RECONCILE the branch onto current `origin/main`**: T1's publisher semantics re-authored inside the post-#275 `publishBuiltCommit` (rebase-onto-observed-base flow), the three conflicted files hand-merged, T2/inner-loop/handoff-test changes carried over, all five Part-1 acceptance criteria green at the new tip. LANDED as `ae381e2`, one commit ahead of `origin/main` `39e3b6b`.
- [x] **T5 (Part 2a) — the resume live head is resolved in CODE by the outer loop**: `launch()` in `orchestrator.ts` resolves the branch's live head via `opts.run_host` when (and only when) `resume_checkpoint !== null`, a full recorded OID exists (the `outer-published:<40hex>:r:t` capture takes precedence over `inner_checkpoint_head`, mirroring `inner-workflow.mjs`), and `run.branch` is set — `git ls-remote --heads origin refs/heads/<branch>` in pr mode / `git rev-parse --verify refs/heads/<branch>^{commit}` in local mode, matching `readBranchHead`'s existing authority split; up to 3 attempts on command failure. Threads the result into the workflow args as `resumeLiveHead`: full 40-hex OID on success, `'absent'` when the authority answered cleanly that the branch does not exist (empty ls-remote output in pr mode; failed rev-parse with a healthy `git rev-parse --git-dir` in local mode), `''` on read-failure-after-retries; field OMITTED entirely otherwise, keeping args byte-identical for non-resume runs. `inner-loop.ts` `InnerLoopInput` + `buildWorkflowArgs` and the `inner-workflow.mjs` args destructure gain the field; when present, `currentHeadAtResume` comes from it and the `head-probe-round-resume` agent seat is NOT dispatched; when absent (old launcher), fall back to the probe. `classifyResume` maps the literal `'absent'` (checked BEFORE `normalizeOid`) to `{ mode: 'rebuild', reason: 'head-branch-absent' }` — a truthful reason for a genuinely gone branch, keeping `''` to mean exclusively "could not read" so T6 can give it a STOP consequence. LANDED as the tip of this branch (the commit carrying this plan update), one commit ahead of `ae381e2`.
- [ ] **T6 (Part 2b) — `classifyResume` returns a bounded STOP on an unreadable head, never `rebuild`**: keep the safety property ("could not tell" is never "unchanged") but change the consequence — `currentHead` unreadable (`''`) with a recorded OID present → `{ mode: 'stop', reason: 'head-unreadable' }`. The workflow's stop path writes a terminal result `{ ok: true, verdict: 'REQUEST_CHANGES', round: 0, checkpoint: <the ORIGINAL resume checkpoint, preserved so a later re-run resumes from the same point>, blockKind: 'infra-only', terminalCause: 'could not read the head of <branch>; the recorded work is at <oid>; re-run when the read succeeds' }` and returns WITHOUT entering planning, build, or review (the outer `innerTerminalFailureReason` already renders `blockKind: 'infra-only'` + `terminal_cause` as a measured reason — never "without Argus APPROVE"). Guard tests in `inner-workflow-resume.test.ts`: force the head read to fail → assert the run stops, the reason names the branch AND the recorded OID, and NO plan/forge/review agent labels were dispatched; update the expectation that currently pins `rebuild`/`head-unreadable`. ← THIS ITERATION
- [ ] **T7 — docs + close-out**: dated `docs/AS_BUILT.md` entry covering BOTH parts (one root cause, fired in sequence in the neutron-enterprise #439 run), including the standing recovery note that `trident/derive-inline-activity-from-evidenc` @ `9a46680` is one commit ahead of PR #263 and must be PUBLISHED, never rebuilt or branch-deleted, until publish-retry (`01KZZS9ZKVJM658ZYE6ZRBBJJ2`) lands; final full-suite verification at the branch tip; persist this plan with recorded shas matching the real branch state.
