# IMPLEMENTATION_PLAN — Work Board row state: a card must not claim a run it doesn't have (defect 2026-08-12)

Governing spec: this Plan card (pulse, ▶/↻, failed colour — one root cause: `status === 'in_progress'` treated as proof of a live run). Verified against the tree 2026-08-13:

- Symptom 1 CONFIRMED at `app/lib/work-board-helpers.ts:237` (unconditional `pulse: true` off the status lane) and mirrored at `landing/chat-react/WorkBoardTab.tsx:191`.
- Symptom 2 CONFIRMED at `app/lib/work-board-helpers.ts:263-265` and mirrored at `landing/chat-react/WorkBoardTab.tsx:111-113`.
- Symptom 3 root verified with a CORRECTION to the card's mechanism: `WorkBoardStore.detachRun` (`work-board/store.ts:766-799`) does NOT clear the binding on failure — it KEEPS `linked_run_id` and sets the durable `status='failed'` (#340), and BOTH terminal reconciles route through it (`trident/board-reconcile.ts` and the dispatch reconcile via `open/composer.ts:1971`). The comments claiming "auto-detached" (`trident/store.ts:356-364`, `open/composer.ts:2219-2224`) are stale. What is actually missing is CLIENT-side: neither `dotState` has a `status === 'failed'` fallback branch, so a failed card whose `run_progress` is unavailable (research/dispatch run — never a trident row; or a deleted run row) falls through and paints the wrong colour. So FIX 3 resolves as **option A, already on main server-side** (durable signal ON THE ITEM: `status='failed'` + kept `linked_run_id`, set only by the terminal reconcile — positive data, not inference from absence); the remaining work is honoring it in both clients' `dotState`. State this choice in the PR.
- Retry path verified working server-side: `handleStart` (`gateway/http/work-board-surface.ts:352-405`) lets a failed card with a kept terminal binding re-dispatch; only the client `canPlay` gate hides the control.

- [x] **T1 — mobile helpers: gate pulse and ▶/↻ on the LIVE run, paint the durable failed lane.** `app/lib/work-board-helpers.ts`: `dotState` status-fallback pulses ONLY when `isLinkedRunning(item)` (a runless `in_progress` card renders a STATIC build dot) and gains a `status === 'failed'` → `{ colorKey: 'failed', pulse: false }` branch; `canPlay` drops the `status !== 'in_progress'` clause (keep `status !== 'done' && !isLinkedRunning(item)`). Update the two now-wrong expectations in `app/__tests__/work-board-helpers.test.ts` (:169, :199) and add mutation-resistant cases so reintroducing either status-lane gate fails the suite.
- [x] **T2 — web mirror.** Same two edits to the module-private `dotState` / `canPlay` in `landing/chat-react/WorkBoardTab.tsx` (`cwb-dot-failed`, no `cwb-dot-pulse`), plus a render-level case in `landing/chat-react/__tests__/work-board-tab.test.tsx`: a `status='failed'` card with no `run_progress` shows a static `cwb-dot-failed` dot (no `cwb-dot-pulse`) and the `↻` "Retry build" control; an `in_progress` card with no binding shows a NON-pulsing build dot.
- [x] **T3 — row/rail agreement + truth pass.** Add the lockstep test keeping the two derivations honest together (per the card: `railDotKind` has a parallel suite): for the failed-and-runless fixture, the row derives `{ colorKey: 'failed', pulse: false }` while the rail's `attention` activity derives `railDotKind === 'attention'` (`app/lib/project-rail-view.ts:157-164`, suite `app/__tests__/project-rail-view.test.ts`). Correct the stale "auto-detached on terminal reconcile" comments (`trident/store.ts` `latestByProjectScope` docstring, `open/composer.ts:2219-2224`) to describe the real #340 behavior (failed keeps the binding; the latest-run check is the secondary net), so the next reader doesn't re-derive this defect's wrong mechanism.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
