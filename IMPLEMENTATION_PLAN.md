# IMPLEMENTATION_PLAN.md — Executor-Mode Reminders (P0-1 M2 PARITY, Neutron Open)

Spec of record: `docs/plans/executor-mode-reminders-2026-07-20.md` — the DEEPENED header block (lines 1-150) SUPERSEDES the original plan below it wherever they conflict. Honor the three Ryan overturns (SPEC Decisions Log 2026-07-20, neutron-managed): (1) Bash ports via approval, not exclusion; (2) dreaming's uncovered half goes into CORE MEMORY (split by tier: backlink repair = event-driven on sync hook; correction-promotion = reflect pass; daily-delta = stays a ritual); (3) ritual registration is AGENT-CALLABLE with in-chat approval — the approval RENDERING carries the security. NO FEATURE FLAGS — build the real default directly. One task per iteration; commit + PR every iteration.

Planner corrections (re-verified 2026-07-21, task-6 spike resume session):
- Branch `trident/executor-mode-reminders` (PR #426) is LIVE at `d84c452a` with tasks 0-5R committed. CONTINUE on it; re-checkout from origin.
- Task 6 (this iteration) is the HARD SECURITY GATE — the T5 write-containment spike. It is NOT the reminders tick/executor/registry (all landed in tasks 4/5). It is a SUBSTRATE-layer proof: a per-session `settings.json` DENY rule must fail CLOSED in the real headless PTY. THE SUBTLETY that invalidated the original design (plan doc §Layer-3): the substrate registers a `tool-use-approve` auto-approver at `runtime/adapters/claude-code/persistent/spawn.ts:253-259` that presses `['1','enter']` on any `TOOL_USE_QUESTION_RE`+`TOOL_USE_SELECTOR_RE` prompt (`signatures.ts:89-90`; `TOOL_USE_QUESTION_RE` includes `runthiscommand` → Bash approvals are auto-pressed too). If a ritual REPL leaves that scanner enabled, a deny rule is THEATER — the approver clicks yes and the write succeeds. So the ritual variant MUST disable ONLY the `tool-use-approve` detector (keeping the wedged-prompt deadlock-recovery ladder registered at `spawn.ts:233` as the no-hang backstop), pair skip_permissions:false with a `permissions.deny` block, and the T5 test must PROVE fail-closed accounting for all of this.
- `buildSettings` (`runtime/adapters/claude-code/persistent/build-settings.ts`) today writes ONLY the Stop hook (no `permissions` key); called once at `spawn.ts:123` as `buildSettings({ settingsPath })`. It is the settings-write seam the deny rule threads through.
- Real-PTY opt-in E2E pattern to mirror EXACTLY: `runtime/adapters/claude-code/persistent/__tests__/dev-channel-pty-bind.e2e.test.ts` (`NEUTRON_PTY_E2E=1`, `describe.skipIf`, hand-builds argv+settings, spawns `BunTerminalHost`, dismisses disclaimer in `onData`, injects a turn via `POST /message`). Fake-host unit pattern: `__tests__/tool-restriction.test.ts`.
- Option-threading chain for the new knobs: `open/wiring/substrates.ts makeRitualSubstrate` → `buildLlmCallSubstrate` (profile knobs resolved at `build-llm-call-substrate.ts:674-697`) → `ClaudeCodeSubstrateOptions` (`runtime/adapters/claude-code/index.ts:48`) → `createClaudeCodeSubstrateAuto` (~:230-260) → `PersistentReplSubstrateOptions` (`persistent/types.ts:193` skip_permissions) → `spawnSession` (`spawn.ts:37`).
- The `substrate-profiles.test.ts` equivalence net FREEZES `PROFILE_RITUAL` deep-equals `{skip_permissions:true}`. Do NOT mutate PROFILE_RITUAL — thread the write-containment knobs (disable-auto-approve + permissions) as spawn/substrate OPTIONS, activated by a future writing-ritual factory, NOT baked into the shared profile constant. Read-only rituals (task 7) keep skip_permissions:true and never set them.
- Task 3 design LOCKED: durable grants are `tool_approvals` rows (0004 DDL) namespaced `ritual:<id>` / `ritual-egress:<id>`, hash-bound; no new table. Layering FORCED: ritual approval lives at `reminders/ritual-approval.ts` importing `@neutronai/tools`.

- [x] 0. Design doc deepened + Ryan review complete (three overturns locked; four-review findings folded into the header block).
- [x] 1. **M2-3 / P0-4 — collapse `NEUTRON_PERFECT_RECALL`; memory consolidation 6h ON by default.** Flag file+test deleted; four gate sites un-gated; `DEFAULT_REFLECT_INTERVAL_MS` 24h→6h; §7.2 merge-safety gate (Argus r1 BLOCKER) fixed; AS_BUILT + SYSTEM-OVERVIEW committed.
- [x] 2. **Ritual schema + registry module.** Migration 0106 (nullable `ritual_id`; `code_ritual_runs` history table; `agent_kind` CHECK+`AgentKind` widened to `'ritual'`); `reminders/rituals.ts` (`RitualDef`+`createRitualRegistry`+async `validateRitualFire` with REQUIRED `RitualApprovalCheck` seam fail-closed); `ritual_id` read-through in `reminders/store.ts`. Argus r2+r3 hardening.
- [x] 3. **Approval-gate infrastructure (content-hash bound).** `tools/approval.ts findApproved`; `reminders/ritual-approval.ts` (`computeRitualContentHash` sha256 over prompt‖surface‖scope‖cadence‖tier‖timeout; `requestRitualApproval`; `createRitualApprovalCheck` re-verifies from LIVE bytes every fire, db-error→fail-close); real notifier swapped in. NO auto-approval anywhere.
- [x] 4. **Executor dispatch branch in the TICK + ritual executor + cc-ritual substrate + ritual lane + `code_ritual_runs` writer.** Ritual lane split; `DispatchTurnInput.tools?`; `PROFILE_RITUAL`; `append_system_prompt_file` threading; `reminders/ritual-agent-base.md`; `makeRitualSubstrate` (`cc-ritual-*`, ephemeral); `reminders/ritual-runs.ts`; `reminders/ritual-executor.ts` (fail-closed validate→durable skip/lane spawn/'running' row/detached turn→terminal bookkeeping; NEVER throws); tick executor branch; composition wiring. Full test set + depcruise clean.
- [x] 5. **Completion delivery + failure surfacing.** Terminal event → final text via the one `deliver()` seam; `silent` skips success post; failed/timed-out/crashed → one-line failure notice; 3-consecutive-failure escalation (cancel-aware re-arm); boot-reap orphans → `crashed` + notice; 30d retention prune. T3+T6 tests. Argus r1+r2 committed.
- [x] 5R. **CLOSE task-5 REQUEST_CHANGES (Argus r3 major + 2 minors).** `reminders/tick.ts` now `await`s `ritual_executor.fire(reminder)` so claim→validate→durable insert completes INSIDE the tick quiescence boundary (only the substrate turn stays detached); quiescence regression test; postNotice retry-once; executor-side truncation removed. Committed `3bc78764`.
- [x] 6. **T5 write-containment spike — HARD GATE for every writing/Bash ritual (incl. all ported Vajra rituals per overturn 1).** Prove a per-session `settings.json` deny rule fails CLOSED (run terminates / write refused; outside write ABSENT; no wedge) in the real headless PTY, ACCOUNTING for the substrate auto-approver (`spawn.ts:253-259`, `signatures.ts:89-90` incl `runthiscommand`) — the ritual REPL must disable ONLY that scanner (keep the wedge-recovery ladder) or the test is theater. Wire the plumbing (buildSettings `permissions`; a `disableToolUseAutoApprove` spawn option; threaded end-to-end) + a fake-host unit proof it takes effect + the real-PTY E2E. Record the VERDICT in the plan doc. **VERDICT: UNPROVABLE (2026-07-21)** — a ritual REPL with `skip_permissions:false`+`permissions` bound its dev-channel MCP in only 1/6 real-PTY runs (sibling control 2/2), and the one bound run WEDGED on an interactive permission prompt; no clean fail-closed. → OS-level sandbox becomes its own prerequisite sprint; Bash/writing rituals STAY GATED; read-only rituals (task 7) still ship under Layer 1. Plumbing landed + dormant. Verdict recorded in `docs/plans/executor-mode-reminders-2026-07-20.md` → "T5 write-containment spike verdict — 2026-07-21".
- [x] 6R. **CLOSE task-6 REQUEST_CHANGES (2 blockers — targeted fixes, do not rewrite task 6).** (A) Migration 0106 `skip_reason` CHECK gains `'gated_tool_surface'` (`migrations/0106_ritual_schema.sql:86`); regen `migrations/expected-schema.txt:527`; stale 3-member list in `reminders/ritual-runs.ts:15` header corrected to the 5-member union; REAL-DDL tests: `ritual-runs.test.ts` `test.each` over all 5 `RitualFireSkipReason` members asserting `insertSkipped` lands a durable row (DDL↔TS lockstep freeze), + `ritual-executor.test.ts` end-to-end gated fire (register Bash-surface def → `fire()` RESOLVES, durable `skipped/gated_tool_surface` row, nothing spawned, turn never called). (B) `reminders/ritual-executor.ts:559-579` — wrap the launch CONSTRUCTION (`resolve_model()` + `turn(...)` evaluation) so a SYNCHRONOUS throw routes through the SAME `settleCrashed` path as a promise rejection (durable row → 'crashed', registry terminal frees the `ritual:<id>` spawn key, failure notice) and `fire()` RESOLVES (occurrence legitimately consumed — no claim revert, no stuck 'running', no live-key wedge); outer-catch comment amended; tests: sync-throwing `resolve_model` and sync-throwing `turn` each settle crashed + a re-fire after the crash is ADMITTED (key freed). `bun test reminders/` (316) `migrations/` (40) `gateway/` (2778) green; `tsc -p reminders` clean; AS_BUILT entry. Committed `d35db134` (A) + `cfb11935` (B).
- [ ] 7. **Bundled generic read-only example rituals** (ENGINE seeds; Ryan's ritual content is USER DATA via Vajra import, never hardcoded): generic morning-brief + evening-wrap templates, surface `['Read','Glob','Grep']`, no Bash — Layer 1 (`--tools` default-deny, `build-repl-argv.ts:52-60`) alone contains these; skip_permissions stays true (no write surface → the task-6 containment config is NOT set). Bundled defs register but stay UNAPPROVED until the owner's task-8 act. Per-ritual T7 acceptance test each (output must reference real fixture state — ported-prompt silent-no-op guard).
- [ ] 8. **Q3 — agent-callable registration with in-chat approval.** Reminders-core MCP tools to propose/register; approval via ButtonStore persisted option-set binding (`button-backed-answer.ts:198` precedent — eligibility only from `prior_agent_options`): opaque proposal_id; resolves through task-3 `respondApproval`; no self-approval (`resolution_speaker_user_id`); render preformatted (never Markdown), refuse over hard cap, NFC-normalize, reject bidi/zero-width, itemize URLs/paths/skills; describe CAPABILITY not tool names; no register-and-fire in one turn; no surface-widening without re-approval. Test T8: owner replies something UNRELATED → ritual stays pending_approval, never dispatches.
- [ ] 9. **Q2 — dreaming's uncovered half into core memory** (overturn 2, split by tier): (a) deterministic entity backlink repair EVENT-DRIVEN on the entity-writer sync hook (`runtime/entity-writer.ts` `newLinks`/`removedLinks`); (b) correction-pattern promotion folded into `runReflectPass` (the 6h loop task 1 turned on); (c) daily-delta notes as a bundled ritual. Explicit toHaveBeenCalled/artifact assertions each.
- [ ] 10. **Docs + close-out.** `docs/AS_BUILT.md` entries per landed task; `docs/SYSTEM-OVERVIEW.md` new "Ritual executor" subsystem section (+ reminders + consolidation-cadence updates); `reminders/AGENTS.md` refresh; final summary citing the executor branch file:line + approval-rendering file:line + T5 verdict.
