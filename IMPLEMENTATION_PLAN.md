# IMPLEMENTATION_PLAN.md — M2 P0 parity: input modalities (documents + voice) + minimal chat commands (Neutron Open)

Supersedes the executor-mode-reminders plan (landed via the PR #426 lineage; any residue is tracked in `docs/plans/executor-mode-reminders-2026-07-20.md`, not here). Scope source: `docs/research/vajra-neutron-parity-audit-2026-07-19.md` blocker 3 + Ryan 2026-07-21 ("only the chat commands that make sense for Neutron" → `/status` + `/reset` ONLY; the Telegram topic-lifecycle vocabulary is NOT ported). NO FEATURE FLAGS — build the real default. One task per Ralph iteration. THIS SEQUENCE CONTINUES ON THE EXISTING BRANCH `trident/input-modalities-commands` + PR #428 (main is protected — reuse the branch/PR, do NOT open new ones); update `docs/AS_BUILT.md` + `docs/SYSTEM-OVERVIEW.md` inside each task's commits.

Planner ground-truth (every claim file:line; original cites re-verified 2026-07-21 against 63fe4119; iteration-3 findings against 141d2c1c; iteration-5 findings verified 2026-07-22 against branch HEAD 70cfb7c6):
- The audit UNDERSTATES the modality gap: attachments (INCLUDING images) never reached the agent pre-task-1. Task 1 built the attachment→turn threading (`open/wiring/app-ws.ts` sanitize+thread at :792-848; `gateway/wiring/build-live-agent-turn.ts` `buildAttachmentsFragment` :232-267, `resolveAttachment` seam :595, splice :1051-1095) and widened accepted types to PDF. Its CI-red module-mock pollution was fixed by task 2 (superset+delegate mocks; the `test` job is the canonical gate and went green — historical detail in AS_BUILT).
- Server-side type machinery EXISTS: `gateway/storage/binary-types.ts` sniffs `%PDF-` + audio WAV RIFF (:175-182) / MP3 ID3+frame-sync (:196-208) / M4A ftyp (:210-224); `BINARY_MIME_WHITELIST` (:47-58) already lists `application/pdf` + `audio/{mpeg,mp4,wav}`; `MIME_ALIASES` (:61-75) canonicalizes `audio/x-m4a`→`audio/mp4`, `audio/x-wav|wave`→`audio/wav`, `audio/mp3`→`audio/mpeg`. Only `gateway/http/app-upload-surface.ts` (`CHAT_UPLOAD_MIME_WHITELIST` :72-78, `EXT_FROM_MIME` :82-88, `URL_PATH_RE` :93-94, `mimeFromExt` :355-370) still restricts chat uploads to images+PDF.
- No ASR/transcription code exists anywhere (re-verified on 70cfb7c6: `git ls-tree | grep -iE 'transcri|whisper'` → only unrelated code-gen/onboarding files). OpenAI credential path exists: `resolveOpenOpenAiPool` (`open/composer.ts:474-481`, single env var `OPENAI_API_KEY`) + `config/index.ts:244,267,447`; composer has `env` in scope (:639) and `log` (:336).
- Chat commands: `/status` SHIPPED (task 3, `gateway/boot-chat-command-filters.ts` `buildStatusChatCommandFilter`, chained at `open/composer.ts` shared chain; docs at SYSTEM-OVERVIEW :71-85). `/reset` remains BLOCKED on a product decision (task 4 below — Ryan sign-off pending; do not build until signed off).
- ITERATION-5 CORRECTIONS (this pass, verified against 70cfb7c6): (a) the prior claim "Expo picker is ALREADY `type:'*/*'`" is STALE — Argus r2 #7 replaced it with an explicit mime allow-list mirroring the server (`app/components/ChatSyncSurface.tsx:227-234`), so task 5 must extend that array; (b) the prior claim "Transcript rides the normal turn → scribe/memory ingestion for free" is WRONG as written — `open/wiring/app-ws.ts:870-877` passes only `text: userText` (the typed text or the "Sent an attachment." placeholder) to `scribeOnUserTurn`; the prompt fragment is injected downstream and never feeds scribe. Vajra parity (voice → text → memory) therefore needs an explicit, small seam: app-ws appends resolved transcripts to the scribe text (turn `user_text` stays unmutated). Task 5 scope updated accordingly.
- Client anchors for task 5 (verified): web accept attr `landing/chat-react/ChatApp.tsx:1410-1415`; web pre-flight allow-list `landing/chat-react/uploads.ts:38-43` + reject message :241-244; web image predicate `landing/chat-react/message-adapter.ts:32`; web non-image chip `ChatApp.tsx:84-135`; native chip `app/components/AuthedAttachmentImage.tsx` (`AuthedAttachmentFile` 📎 chip :264-268, shared predicates `app/lib/attachment-url.ts`); Expo mime→ext `app/lib/upload-client.ts` `mimeToExt` (:498-508; `classifyUploadKind` already routes non-zip → the generic upload route); 🎵 icon precedent `app/features/docs/docs-shared.ts:35`.
- Project archiving already exists (`gateway/projects/sqlite-store.ts`, `gateway/http/app-projects-surface.ts`) — do NOT rebuild; explicitly out of scope.
- Ordering note: task 4 (/reset) is intentionally SEQUENCED AFTER task 5 this iteration — it is blocked on Ryan's primitive choice (he has not signed off yet); voice/ASR has no dependency on it and proceeds first. Task 4's entry is preserved verbatim below, unmodified, pending sign-off.

- [x] 1. **Attachment→agent threading + document (PDF) attachments end-to-end.** Server: widen `gateway/http/app-upload-surface.ts` (whitelist / `EXT_FROM_MIME` / `URL_PATH_RE` / `mimeFromExt`) to add `application/pdf`; export ONE URL→local-path resolver helper. Thread the attachments read at `open/wiring/app-ws.ts` into the `appWsChatTurn` call; extend `gateway/wiring/build-live-agent-turn.ts` turn input with `attachments` + an injected `resolveAttachment` seam (composer supplies it over `owner_home`); inject a `<user_attachments>` fragment (resolved local paths under `<owner_home>/chat-attachments/`) into the PROMPT on warm AND cold branches — never mutate `turn.user_text`. Clients: web accept + non-image chip render; Expo mime→ext + pdf; + 3 review-round fixes (hoisted hooks below early return → pure dispatcher split; synchronous `window.open` before awaits to preserve user activation; native open via cache write + OS share sheet). DONE: production code + 2 independent reviewer rounds clean, tsc clean, attachment tests green in isolation. Canonical-gate green was completed by task 2.
- [x] 2. **CI-green hotfix: de-pollute the process-global react/react-native module mocks in the two new app attachment test files.** Rewrote the mock scaffolding in `app/__tests__/authed-attachment-image-hooks.test.tsx` and `app/__tests__/authed-attachment-file-open.test.tsx` to the repo's established superset+delegate contract so nothing behaviorally leaks to later files in the same bun process; assertions byte-identical. DONE — PR #428 `test` check green (red streak flipped).
- [x] 3. **`/status` chat command (narrow re-map — NOT the Vajra topic-lifecycle set).** New `buildStatusChatCommandFilter` in `gateway/boot-chat-command-filters.ts` implementing `contracts/chat-command-filter.ts:25-44` (exact-command boundary — `/statusfoo` falls through null, K8 grammar precedent), chained at `open/composer.ts` into the shared `buildChainedChatCommandFilter([...])` so web + app-ws compose identically — no second command-parsing path. `/status`: deterministic snapshot from an injected thunk (bound via a `late<T>` holder to the live projects reader / `getBestModel` / reminder store `ReminderStore.listPending` / `workBoardStore.listActive` / Trident `TridentRunStore.listNonTerminal`) — active project, current model, pending reminders count, active work-board items, active trident builds. Tests (`gateway/__tests__/status-command-wiring.test.ts`, 9/0): reply text composes from the snapshot values (behavior, not a `toHaveBeenCalled` gap-test); project_id threaded/omitted; word-boundary fall-through never runs the thunk; chain-composition proof. Docs: SYSTEM-OVERVIEW chat-commands + AS_BUILT. DONE 2026-07-22.
- [ ] 4. **`/reset` chat command — CORRECTED mechanism (the plan's original `respawnSupervisedSession` primitive was verified WRONG).** GROUND TRUTH (verified 2026-07-22 against branch HEAD): `respawnSupervisedSession` (`runtime/adapters/claude-code/persistent/supervision.ts:59`) → `respawnReplSession(..., force=true)` → `planRespawn` ALWAYS `--resume`s the SAME transcript (`session-respawn.ts:24` "respawn ALWAYS resumes — never a fresh spawn"; `has_session`+`sessionId` PRESERVED), so it PRESERVES context — it does NOT reset it. Using it for `/reset` would be a no-op-that-looks-done (banned). The correct "fresh agent context; durable chat history stays" primitive is EITHER (a) the `/clear` PTY reset — `CONTEXT_RESET_COMMAND` `signatures.ts:164`, written to `session.child` at `pool.ts:380`, already the import warm-session per-turn reset (needs the `acquireTurn` mutex so it doesn't race a live turn), OR (b) a genuine FRESH (non-`--resume`) respawn (clear the registry record's `has_session` + evict the warm child so the next dispatch spawns fresh). BOTH need to TARGET the live session key, which folds the selected `cred.id` (`build-llm-call-substrate.ts:721`) into `poolKeyFor` (`pool.ts:191-205`) — unknown to the filter; resolve it credential-agnostically (a prefix scan of `supervisedBySessionKey` under the instance `replRegistryPath`, mirroring the existing scans at `supervision.ts:452,542`, keyed on `cc-agent-<owner>SEP<user>SEP<project_id>SEP`). Ryan sign-off needed on which primitive (this is a spec correction, not the plan's original mechanism). Tests: the reset actuation is invoked with the resolved live session key; non-command fallthrough returns null. Docs: SYSTEM-OVERVIEW + AS_BUILT.
- [x] 5. **Voice notes: audio upload + real ASR transcription.** Server: widen `gateway/http/app-upload-surface.ts` to `audio/mpeg`(mp3) / `audio/mp4`(m4a) / `audio/wav`(wav) across `CHAT_UPLOAD_MIME_WHITELIST` + `EXT_FROM_MIME` + `URL_PATH_RE` ext group + `mimeFromExt` (sniffers already exist, `binary-types.ts:175-224`; `.txt` stays OUT of the GET ext group so sidecars are never servable). New `gateway/transcription/openai-transcription.ts` — OpenAI-compatible `POST {base}/v1/audio/transcriptions` multipart client (default base `https://api.openai.com`, model `whisper-1`, injected `fetch_impl`, timeout; typed error taxonomy http_error/network_error/timeout/bad_response; never throws). Key = `OPENAI_API_KEY` only (same single var as `resolveOpenOpenAiPool`, `open/composer.ts:474-481` — credential config, not a feature flag; no new env vars). Transcribe at upload-complete via an optional `transcribeAudio` seam on `AppUploadSurfaceOptions` and persist a content-addressed sidecar `<hash>.txt` beside the blob (atomic tmp+rename; idempotent — sidecar-exists ⇒ the API is never re-called; ASR failure NEVER fails the upload). Widen `resolveChatAttachmentLocalPath` to return `transcript` for audio (sidecar read; null when absent); `buildAttachmentsFragment` injects the TRANSCRIPT text for audio attachments, capped, with the keyless/failed graceful note "transcription unavailable — set OPENAI_API_KEY". CORRECTED scribe mechanics (iteration-5 ground truth): the transcript does NOT reach scribe for free — add an optional `attachmentTranscript` seam to the app-ws wiring deps and append resolved transcripts to the `scribeOnUserTurn` text (`open/wiring/app-ws.ts:870-877`); `turn.user_text` stays unmutated. Clients: web accept attr + `ACCEPTED_ATTACHMENT_TYPES` (+ alias forms per `MIME_ALIASES`) + 🎵 audio chip (`message-adapter.ts` predicate); Expo picker mime array (`ChatSyncSurface.tsx:227-234` — NOT `*/*`, corrected) + `mimeToExt` audio cases + native 🎵 chip (`attachment-url.ts` predicate; icon precedent `docs-shared.ts:35`). Tests: transcription client unit (mocked fetch — URL/auth/multipart fields/error taxonomy; NO network, NO process-global module mocks); upload→sidecar artifact-on-disk assertion + idempotency call-count + keyless no-sidecar; resolver transcript surfacing; fragment injection includes transcript text + unavailable note; app-ws scribe text includes transcript. Docs: SYSTEM-OVERVIEW durable-chat attachments block + AS_BUILT dated entry. DONE 2026-07-22.
- Commit IMPLEMENTATION_PLAN.md together with your code + tests.
