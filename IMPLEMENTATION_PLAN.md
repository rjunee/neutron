# IMPLEMENTATION_PLAN.md — Executor-Mode Reminders (P0-1 M2 PARITY, Neutron Open)

Spec of record: `docs/plans/executor-mode-reminders-2026-07-20.md` — the DEEPENED header block (lines 1-150) SUPERSEDES the original plan below it wherever they conflict, and the "T5 write-containment spike verdict — 2026-07-21" section (lines 254-278) records the UNPROVABLE verdict + STAY-GATED consequence. Honor the three Ryan overturns (SPEC Decisions Log 2026-07-20, neutron-managed): (1) Bash ports via approval, not exclusion; (2) dreaming's uncovered half goes into CORE MEMORY (split by tier: backlink repair = event-driven on sync hook; correction-promotion = reflect pass; daily-delta = stays a ritual); (3) ritual registration is AGENT-CALLABLE with in-chat approval — the approval RENDERING carries the security. NO FEATURE FLAGS — build the real default directly. One task per iteration; fresh worktree+branch off main, commit early+often, PR per iteration (main is PROTECTED).

Planner corrections (re-verified 2026-07-21, task-7 planning session):
- PR #426 (branch `trident/executor-mode-reminders`, tasks 0-6R) is MERGED to main at `63fe4119` and deployed. Iterations from task 7 onward start a FRESH worktree+branch off origin/main with a NEW PR each (mergeMode=pr) — do NOT resume the old branch.
- Verified current wiring on main: ritual registry rooted at `<owner_home>/rituals` with ZERO defs registered (`open/composer.ts:1889`); the executor passes the ritual prompt bytes AS the entire `user_message` — no fire-context suffix is appended (`reminders/ritual-executor.ts:566`), so templates must be fully self-contained; `tools: def.tool_surface` threads executor→turn (`ritual-executor.ts:571`); `scope_cwd` wires ONLY `'instance'`→owner_home — `'project'` throws → durable `unsupported_scope` skip (`open/composer.ts:1918-1926`); `makeRitualSubstrate` = `PROFILE_RITUAL` (skip_permissions:true, frozen by `substrate-profiles.test.ts`) + ephemeral + `RITUAL_AGENT_BASE_PROMPT` (`open/wiring/substrates.ts:411-433`); projects live at `<owner_home>/Projects/<slug>/STATUS.md` (`reminders/context.ts:30,39`) with docs under `Projects/<id>/docs/` (`open/composer.ts:2074,2348`).
- Task-7 shape LOCKED: bundled generic TEMPLATES ship in-repo at `reminders/rituals/<id>.md` (module-dir-resolved, the `reminders/prompt-path.ts` pattern); a seed step materializes them into `<owner_home>/rituals/` COPY-IF-ABSENT (an owner-edited or pre-existing file is NEVER clobbered — from first seed on it is user data); defs register inside the composer's `ritual_executor_factory` closure; NOTHING grants approval (defs stay unapproved until the owner's task-8 act — the content-hash check `createRitualApprovalCheck` re-verifies from live bytes every fire, so later owner edits drop approval by design); NO reminder rows are created (scheduling a ritual is task 8).
- T7 acceptance = two tiers: fast per-commit units (def shape incl. zero intersection with `GATED_WRITE_TOOLS`, seed/idempotency/never-clobber, static no-vajra-isms guard on template bytes, unapproved fire → durable `skipped/unapproved` row with turn NOT called + nothing spawned via the REAL `ApprovalManager` path, approved spec-shape pinning tools/prompt-bytes/cwd/timeout) + a gated real-PTY LLM E2E (`NEUTRON_PTY_E2E=1`, `describe.skipIf`, mirroring `runtime/adapters/claude-code/persistent/__tests__/dev-channel-pty-bind.e2e.test.ts`) asserting each ritual's output cites planted fixture markers (the ported-prompt silent-no-op guard, LLM-behavior half).
- `open/__tests__/open-composition-fields-characterization.test.ts:102` pins only FIELD PRESENCE of `ritual_executor_factory` — registering defs inside the factory closure does not break it. `gateway/composition/build-core-modules-ritual-executor.test.ts` tests the invocation seam with a FAKE factory — signature unchanged, stays green.

- [x] 0. Design doc deepened + Ryan review complete (three overturns locked; four-review findings folded into the header block).
- [x] 1. **M2-3 / P0-4 — collapse `NEUTRON_PERFECT_RECALL`; memory consolidation 6h ON by default.** Flag file+test deleted; four gate sites un-gated; `DEFAULT_REFLECT_INTERVAL_MS` 24h→6h; §7.2 merge-safety gate (Argus r1 BLOCKER) fixed; AS_BUILT + SYSTEM-OVERVIEW committed.
- [x] 2. **Ritual schema + registry module.** Migration 0106 (nullable `ritual_id`; `code_ritual_runs` history table; `agent_kind` CHECK+`AgentKind` widened to `'ritual'`); `reminders/rituals.ts` (`RitualDef`+`createRitualRegistry`+async `validateRitualFire` with REQUIRED `RitualApprovalCheck` seam fail-closed); `ritual_id` read-through in `reminders/store.ts`. Argus r2+r3 hardening.
- [x] 3. **Approval-gate infrastructure (content-hash bound).** `tools/approval.ts findApproved`; `reminders/ritual-approval.ts` (`computeRitualContentHash` sha256 over prompt‖surface‖scope‖cadence‖tier‖timeout; `requestRitualApproval`; `createRitualApprovalCheck` re-verifies from LIVE bytes every fire, db-error→fail-close); real notifier swapped in. NO auto-approval anywhere.
- [x] 4. **Executor dispatch branch in the TICK + ritual executor + cc-ritual substrate + ritual lane + `code_ritual_runs` writer.** Ritual lane split; `DispatchTurnInput.tools?`; `PROFILE_RITUAL`; `append_system_prompt_file` threading; `reminders/ritual-agent-base.md`; `makeRitualSubstrate` (`cc-ritual-*`, ephemeral); `reminders/ritual-runs.ts`; `reminders/ritual-executor.ts` (fail-closed validate→durable skip/lane spawn/'running' row/detached turn→terminal bookkeeping; NEVER throws); tick executor branch; composition wiring. Full test set + depcruise clean.
- [x] 5. **Completion delivery + failure surfacing.** Terminal event → final text via the one `deliver()` seam; `silent` skips success post; failed/timed-out/crashed → one-line failure notice; 3-consecutive-failure escalation (cancel-aware re-arm); boot-reap orphans → `crashed` + notice; 30d retention prune. T3+T6 tests. Argus r1+r2 committed.
- [x] 5R. **CLOSE task-5 REQUEST_CHANGES (Argus r3 major + 2 minors).** `reminders/tick.ts` now `await`s `ritual_executor.fire(reminder)` so claim→validate→durable insert completes INSIDE the tick quiescence boundary (only the substrate turn stays detached); quiescence regression test; postNotice retry-once; executor-side truncation removed. Committed `3bc78764`.
- [x] 6. **T5 write-containment spike — HARD GATE for every writing/Bash ritual.** Proved in the real headless PTY, accounting for the substrate auto-approver. **VERDICT: UNPROVABLE (2026-07-21)** — `skip_permissions:false`+`permissions` bound the dev-channel MCP in only 1/6 runs and the bound run WEDGED on an interactive prompt; no clean fail-closed. → OS-level sandbox becomes its own prerequisite sprint; Bash/writing rituals STAY GATED (`GATED_WRITE_TOOLS` fire-time refusal in `reminders/rituals.ts:106-112,365-371`); read-only rituals still ship under Layer 1. Plumbing (`buildSettings permissions` + `disableToolUseAutoApprove`) landed + dormant. Verdict recorded in the plan doc.
- [x] 6R. **CLOSE task-6 REQUEST_CHANGES (2 blockers).** (A) migration 0106 `skip_reason` CHECK gains `'gated_tool_surface'` + expected-schema regen + 5-member union header fix + REAL-DDL `test.each` freeze + end-to-end gated-fire test. (B) `ritual-executor.ts` launch CONSTRUCTION (sync `resolve_model()`/`turn(...)` throw) routes through `settleCrashed` (durable 'crashed', spawn key freed, occurrence consumed) — no claim revert, no stuck 'running'. `bun test reminders/` (316) `migrations/` (40) `gateway/` (2778) green; `tsc -p reminders` clean. Committed `d35db134` + `cfb11935`.
- [x] 7. **Bundled generic read-only example rituals** (ENGINE seeds; Ryan's ritual content is USER DATA via Vajra import, never hardcoded): generic `morning-brief` + `evening-wrap` templates shipped at `reminders/rituals/<id>.md`, seeded COPY-IF-ABSENT into `<owner_home>/rituals/`, registered in the composer factory. Surface exactly `['Read','Glob','Grep']`, egress 'none', scope 'instance', no Bash — Layer 1 (`--tools` default-deny, `build-repl-argv.ts:52-60`) alone contains these; skip_permissions stays true via `PROFILE_RITUAL` (no write surface → the task-6 containment config is NOT set). Bundled defs register but stay UNAPPROVED until the owner's task-8 act; no reminder rows created. Per-ritual T7 acceptance test each (output must reference real fixture state — ported-prompt silent-no-op guard): fast units + gated real-PTY LLM E2E. **AS-LANDED (2026-07-21):** `reminders/bundled-rituals.ts` (`BUNDLED_RITUAL_DEFS` frozen 2 defs + `seedBundledRituals` copy-if-absent/idempotent/never-clobber + `registerBundledRituals`) + `reminders/rituals/{morning-brief,evening-wrap}.md` templates + `reminders/index.ts` barrel export; `open/composer.ts` `ritual_executor_factory` seeds+registers on boot (WIRED — the registry is no longer empty). 11 fast units (`reminders/bundled-rituals.test.ts`) incl. the REAL-`ApprovalManager` unapproved-fire (durable `skipped`/`unapproved`, turn 0×, nothing spawned) + approved spec-shape pin; gated real-PTY E2E (`reminders/bundled-rituals.e2e.test.ts`) ran GREEN on-box (`claude` 2.1.215, both rituals cite RELAY-4471/CERT-ROTATE-9/HARBOR-812). `bun test reminders/` 327 pass/2 skip; wiring guards green; `tsc -p reminders`+`tsc -p open`+eslint+depcruise clean. Branch `trident/executor-reminders-p2`.
- [x] 8. **Q3 — agent-callable registration with in-chat approval.** Reminders-core MCP tools to propose/register; approval via ButtonStore persisted option-set binding (`button-backed-answer.ts:198` precedent — eligibility only from `prior_agent_options`): opaque proposal_id; resolves through task-3 `respondApproval`; no self-approval (`resolution_speaker_user_id`); render preformatted (never Markdown), refuse over hard cap, NFC-normalize, reject bidi/zero-width, itemize URLs/paths/skills; describe CAPABILITY not tool names; no register-and-fire in one turn; no surface-widening without re-approval. Test T8: owner replies something UNRELATED → ritual stays pending_approval, never dispatches. **AS-LANDED (2026-07-21):** `validateRitualDef` extracted from `reminders/rituals.ts`; `ritual_id` WRITE path + `hasPendingRitualRow` in `reminders/store.ts`; `content_id`/`egress_id` passthrough in `reminders/ritual-approval.ts`; NEW `reminders/ritual-registration.ts` (engine service: `propose` → NFC-normalize/reject-bidi-zero-width-C0/refuse-over-16KiB/`validateRitualDef`/instance-scope-only/never-clobber `wx`-write `<id>.md`+`<id>.def.json`/register/`requestRitualApproval`/CODE-rendered fence-hardened capability-language approval `ButtonPrompt` via the `deliver` seam; `handleOwnerButtonAnswer` turn-start EXACT-match against persisted `rap:` option values, OWNER-only, content-hash `isApproved` over LIVE bytes → schedule-on-approve with `ritual_id`; `status`; `loadPersistedRitualDefs` boot re-registration; opaque `rap:<base64url(uuid)>:a|d` tokens ≤37B); `DeliveryEnvelope.options/idempotency_key/metadata` extension in `gateway/http/deliver.ts`; `ritualApprovalCapture` seam in `gateway/wiring/build-live-agent-turn.ts` (deterministic reply, LLM turn short-circuited, opaque token never falls through to free-text capture); composer wiring inside `ritual_executor_factory` + late-bound getter through `mountOpenCores`→`buildCoresBackendFactories`; reminders-core `rituals_propose`/`rituals_status` MCP tools (manifest+TOOL_NAMES+`buildExtraTools` same commit, X2 lockstep). T8 test proves an unrelated owner reply NEVER flips the `tool_approvals` row. Branch `trident/executor-reminders-p2`.
- [ ] 9. **Q2 — dreaming's uncovered half into core memory** (overturn 2, split by tier): (a) deterministic entity backlink repair EVENT-DRIVEN on the entity-writer sync hook (`runtime/entity-writer.ts` `newLinks`/`removedLinks`); (b) correction-pattern promotion folded into `runReflectPass` (the 6h loop task 1 turned on); (c) daily-delta notes as a bundled ritual. Explicit toHaveBeenCalled/artifact assertions each.
- [ ] 10. **Docs + close-out.** `docs/AS_BUILT.md` entries per landed task; `docs/SYSTEM-OVERVIEW.md` new "Ritual executor" subsystem section (+ reminders + consolidation-cadence updates); `reminders/AGENTS.md` refresh; final summary citing the executor branch file:line + approval-rendering file:line + T5 verdict.
