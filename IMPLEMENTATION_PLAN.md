# IMPLEMENTATION_PLAN — PR number on the card: durable #NNN tag, clickable, + title-opens-doc (owner-requested 2026-08-14)

Governing spec: this Plan card (parts 1-4, acceptance (a)-(h)). Verified against main 2026-08-14 — NONE of it exists yet:

- `landing/chat-react/WorkBoardTab.tsx` meta line (~998) renders only `cwb-tag` + `cwb-round`; completed line (~781-786) renders `Merged · {formatCompletedShort(...)}`. No PR anywhere.
- `work_board_items` has NO `pr` column (migrations end at 0121; 0097's rebuild shows the full column set). `detachRun` (`work-board/store.ts:766`) writes status/completed_at only — the durable-PR gap the card warns about is real: `detachRun('done')` NULLs `linked_run_id`, so a completed card has no path back to `run.pr`.
- No owner/repo web-URL resolver exists anywhere in the tree — only `isGithubRemoteUrl` + `git remote get-url origin` shells (`trident/git-mode.ts`). It must be built (pure parser + cached shell resolver); the run row's `repo_path` is the source, per the card.
- The reconcile observer (`trident/board-reconcile.ts:35`) receives the FULL terminal `TridentRun` (carries `pr` + `repo_path`), so the durable write threads through it with no new plumbing to the tick loop. Production observer sites: `gateway/composition/build-core-modules.ts:500`, `trident/code-command.ts:277` — keep the resolver DEFAULTED inside board-reconcile so neither site changes.
- Wire: the `work_board_changed` fan (`open/composer.ts:3667-3683`) enumerates item fields EXPLICITLY — new columns must be added there, in `wire-types/app-ws-envelope.ts` `AppWsWorkBoardItem`, and in `parseWorkBoardItems` (`landing/chat-react/work-board-client.ts:298`). The HTTP GET passes store rows through whole, so it picks the columns up for free.
- Part 4: the doc-link element is `WorkBoardTab.tsx` ~924-940; the title button (~916-922) fires `onStartEdit` (inline rename). `DocStore.onMutationSuccess` (`gateway/http/doc-store.ts:404`, wired to `AnchorWalker.handle` at the composition root) is the existing doc-write signal to hang the H1→card sync on. Spec docs carry the title twice (`work-board/spec-doc.ts` `buildSpecDocMarkdown`: frontmatter `title:` + `# ` H1); filename is slug+id-suffix and must never change.

- [x] **T1 — durable PR on the item, written by the real detach path.** Migration 0122 adds `pr INTEGER` + `pr_url TEXT` to `work_board_items` (+ regen `migrations/expected-schema.txt`). `WorkBoardItem` gains `pr`/`pr_url`; `detachRun` gains an optional `pr_info` and writes both columns in the SAME transaction as status/completed_at, for BOTH outcomes (done and failed); `attachRun` clears them (a fresh binding must not wear a stale number). New `trident/repo-web-url.ts`: pure `githubWebUrlFromRemote(remote) → 'https://github.com/<o>/<r>' | null` + cached `makeRepoWebUrlResolver(run_host)` shelling `git -C <repo_path> remote get-url origin`. `buildBoardReconcileObserver` resolves `pr_url` (injectable resolver, internal default, failure → null, never blocks reconcile) and passes `{pr, pr_url}`. Add `pr`/`pr_url` to the fan literal, `AppWsWorkBoardItem`, and the web client type + `parseWorkBoardItems`. Acceptance (c) guard: the test drives the REAL observer → `detachRun` → store re-read with no `run_progress` in sight; dropping the column turns it red.
- [ ] **T2 — live `pr_url` on `run_progress`.** `RunProgress` (server `trident/run-progress.ts` + web mirror) gains `pr_url: string | null`; `deriveRunProgress` takes an optional `repo_web_url` and composes `<url>/pull/<pr>`. One shared repo-web-url cache with a SYNC `peek(repo_path)` (background warm on miss) feeds both sync attachment sites — `withRunProgress` in `gateway/http/work-board-surface.ts:180` and the fan in `open/composer.ts` — injected via surface opts / composer wiring; tests inject a pre-warmed cache. First post-boot snapshot may render the tag as plain text until the warm lands (documented, acceptable — checkpoints push and the 15s poll refreshes).
- [ ] **T3 — render the tag (parts 1-3 in `WorkBoardTab.tsx`).** Meta line: `#NNN` FIRST, then step tag, then round (`#265 Building round 1`); number = `run_progress?.pr ?? item.pr`; URL = `run_progress?.pr_url ?? item.pr_url`. Render an `<a target="_blank" rel="noopener noreferrer">` when a URL resolved, a plain `<span>` when not; NOTHING (no placeholder, no empty element) when no PR. Widen the `hasStatus` meta-line gate to `tag !== null || pr !== null`; terminal lanes show it too (`#261 Failed`). Completed row: `Merged #265 · Aug 14` from the durable `item.pr`/`item.pr_url`; a PR-less completed card renders today's `Merged · Aug 14` byte-identically. CSS in chat-react.html (`.cwb-pr`). Mutation-resistant tests incl. exact `rel`/`target`/href and the no-PR no-tag case.
- [ ] **T4 — title opens the doc; the doc-link line dies.** Delete the `cwb-doc-link` element (~924-940). A card WITH a resolvable doc (`docPathFromDesignRef !== null` and `onOpenDoc` wired): title click calls `onOpenDoc` — NO inline rename on that card at all, and no new control anywhere in `cwb-actions`. A card WITHOUT a doc keeps today's `onStartEdit` rename exactly. Title carries NO link styling (no blue, no underline; cursor may change). Tests: doc-card click opens doc and never enters edit; docless card still renames; no rename affordance exists on a doc card (acceptance e, g, h).
- [ ] **T5 — doc H1 → card title sync (part 4 mechanics).** New gateway module hooked off `DocStore.onMutationSuccess` (COMPOSED with the existing `AnchorWalker.handle` at the wiring site, not replacing it): on a `write` to `plans/*.md`, parse the first `# ` H1 — H1 wins over frontmatter; no readable H1 → NO-OP on the card. Find the card whose `design_doc_ref` resolves to that path in `workBoardScopeKey(owner, project_id)`; update its title (sanitized) when different; rewrite frontmatter `title:` to match ONLY when different (so the re-fired hook reaches a fixpoint, no loop). The FILENAME never changes; sync is doc → card only. Tests drive the REAL `writeDoc` path (not the parser directly) and assert afterwards: card renamed, frontmatter rewritten, filename unchanged, `design_doc_ref` still resolves; an H1-less save leaves the title untouched (acceptance f + the fake-test guard).
