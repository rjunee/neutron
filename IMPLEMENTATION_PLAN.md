# IMPLEMENTATION_PLAN — mutation gate: a test-infrastructure diff gets a legal outcome (PR #490)

Card: the mutation-proof gate had no reachable outcome for a diff whose only changed .ts files are declared tests — PR #489 (APPROVED, all checks green) could never merge because every changed file was rejected as a mutation target by the path-based test-file rule while the gate still demanded a proof, and the refusal blamed the build for an omission it could not avoid. Fix, all on this branch: classify targets by what DECLARES a test rather than a path prefix; state the anti-tautology rule directly (a guard may not run the mutated file as its own test); give the no-production-file diff an explicit recorded exemption distinct from prose-only; split the required-but-missing refusal into "a legal target existed and none was named" vs "no legal target existed", naming a disqualified file.

State (2026-09-03, round 24): review round 8 found the FOURTEENTH escape at a NEW seam — the CONTROL is branch-authored code that executes between the guard's RED and GREEN observations, and only `claim.file` was restored before the final one, so a control (or the guard's own first run) that WRITES a passing guard test forges `proved: true`; reproduced at 766ae106 and pre-existing at base bee6dfd9. This round re-provisions the proof worktree between the control observation and the restored-guard observation and corrects the `control.description` sentence this branch added that steered builds into the shape; the bunfig-preload over-refusal is recorded as a named deferral (JOB 2 below); the two panel-downgraded codex items (direct-`__tests__/` helper boundary rows, the node clause's missing `main`) queue behind it. PR #489 merged 2026-09-01 — nothing waits on this card; the defects are being finished because they are real. Earlier state (round 23): the bun/node config arm also asks WHERE AN INHERITED PRELOAD POINTS — a config main wrote, aimed at a file this branch rewrites, is still the branch's code in every test process — and `main` joins `imports`/`exports` as a manifest resolution key; see the round-23 entry. Earlier state (round 22): the arm refuses a branch-AUTHORED resolution map rather than any manifest in the diff, so a routine dependency bump keeps its nomination. Earlier state (2026-09-02, round 11): PR #490 OPEN, MERGEABLE / CLEAN, 17 of 17 checks pass at the head. Requirements 1-4 are built and tested. The nomination-emission instruction that led rounds 8-10 is WITHDRAWN as structurally unreachable (see the Round-16 entry: the codex BRIDGE fills the schema from a six-line wrapper trailer carrying no nomination line, so `null` is its only honest answer, and the transport is a SEPARATE card). The suite red is pre-existing and settled, so the honest pair stays testsPassed=true for the trident scope with suiteOutcome='failed-preexisting' and the base-branch comparison named.

THE PLAN LIVES AT THE REPO-ROOT IMPLEMENTATION_PLAN.md ONLY. The file at trident/IMPLEMENTATION_PLAN.md is NOT this card's: at this branch's head it is byte-identical to origin/main's copy (an EARLIER MERGED card's plan), and zero commits on this branch touch that path. Leave it alone.

- [x] D1 — declared-test classifier in trident/mutation-prover.ts: basename `\.(test|spec)\.(?:[cm][jt]s|[jt]sx?)$` / `_test\.(go|py)$` and direct `__tests__/` children are tests (rejected as before); other files under a top-level `tests/`/`test/` are support libraries and LEGAL targets; documentation rejection unchanged.
- [x] D2 — anti-tautology check: a claim whose guard argv reaches `claim.file` is refused, with the refusal naming the tautology (every operand the runner reads is considered, not only recognised selectors).
- [x] D3 — explicit exemption when git says no production file changed, with its OWN reason string distinct from the prose-only one, recorded in the run record rather than silent.
- [x] D4 — the refusal distinguishes "the build nominated no mutation (a legal target existed)" from "no legal target existed", naming at least one considered-and-disqualified file.
- [x] Tests — red-then-green proof against a `tests/` support library with an unrelated control; classification table over the card's paths; tautology refusal; exempt reason is NOT the prose-only string; the load-bearing negative (a production file in the diff plus no claim is still REFUSED); positive controls against silently-empty extractions.
- [x] Wiring — trident/orchestrator.ts, trident/inner-workflow.mjs, trident/inner-loop-sim.ts carry the new outcomes; four production files changed on this branch.
- [x] CodeQL ReDoS on the loadable-scheme regex fixed (cfc38542), confirmed green by GitHub. Do not touch `LOADABLE_SCHEME`.
- [x] Round-7 BLOCKER: a package-script runner FORWARDS a bare positional to the script's own command line without `--`; `pathArgs` and `whyNoSelection` now drop every positional for npm/pnpm/yarn, with the previously-legal shape pinned as refused and a bun-spelled positive control beside it.
- [x] Round-8 BLOCKER: the wrapper hole reopened under `make` (a positional is a second GOAL, not a forwarded selector); `forwardsPositionalsToAScript` covers `make`, `whyNoSelection` gained a make-shaped sentence, with unit rows and a real-git end-to-end refusal plus positive control.
- [x] Round-9 BLOCKER (two reviewers, two routes): the guard-tautology invariant was bypassable via the space-separated option value (`carriedValueReaching` now pairs each operand with the bare option before it) and via the wrapper (`npm run …` whose branch-authored body preloads the target) — a wrapper is no longer a legal GUARD for any target and stays a legal CONTROL. Unit loops for both routes with positive controls, plus one real-git end-to-end test.
- [x] Round-9 major: the percent-decode inside `argvEscapesTheWorktree` is pinned by a killing test in three spellings, with a `%2D` positive control.
- [x] `TICK_NOTE_CEILING` pinned to 240 exactly.
- [x] EMIT-THE-NOMINATION rider: RETRACTED — see the round-16 item below for why it can never fire on this route.
- [x] Round-10 BLOCKER: an INDIRECT LOAD HOOK (`--preload=./tests/setup.ts` whose body imports the target) forged a proof; `loadHookCarrying` refuses the SHAPE (`--preload`, `--require`/`-r`, `--import`, `--loader`, `--experimental-loader`, in every spelling) whatever file it names, for EVERY target. Six refused spellings pinned plus four positive controls.
- [x] Round-10 major: FORGE_SCHEMA's guard description names every refused opaque shape, and a test reads both lists OUT OF THE PROVER'S SOURCE so the two can only agree by being the same list.
- [x] Round-11 BLOCKER: a REPORTER OPTION is a load hook (`--test-reporter|--reporter|--reporters` joined `LOAD_HOOK_OPTION`, anchored so `--test-reporter-destination`/`--reporter-outfile` stay legal); schema names all three; agreement test pins containment; six refused unit spellings, a positive control, and a real-git test that COMMITS a reporter module importing the target and refuses the guard before anything executes.
- [x] Round-12 BLOCKER: config spellings visible in the argv (`--experimental-config-file`, the valueless `--experimental-default-config-file`, short `-c`) joined the refused family; a LONG hook name is refused on its NAME, value or none; unit rows and positive controls (`go test -cover ./cmd/`, trailing `go test ./cmd/ -c`) pinned; both arms verified load-bearing.
- [x] Round-12 minors: three rows re-spelled off `--reporter`; tick-note/stage-reason docblocks corrected.
- [x] Round-13 BLOCKER: `node --run <script>` refused at the SHAPE (`isNodeRunOption`, both spellings), and go's one-dash hooks `-exec`/`-toolexec`/`-overlay` refused via `GO_TOOLCHAIN_HOOK_OPTION` matched WHOLE before the short-option reading; schema names all four; agreement test reads the go names out of the prover source; unit rows plus positive controls (`node --test tests/other.test.mjs`, `go test -count=1`, `go test -execute-nothing=1`, `go test ./pkg/other`).
- [x] Round-13 minors: the carried-value refusal fragments compose into grammatical sentences; `carriedValueReaching` and `extensionCompletions` pinned as load-bearing by name.
- [x] Round-14 BLOCKERS: `--tsconfig-override` (a resolution map) joined `LOAD_HOOK_OPTION` with schema/agreement/docblock coverage, and the node shape requires `--test` FIRST (`argv[1] === '--test'`) so `node <script> --test` can no longer wear the runner's name; regression rows and positive controls; the committed `tests/tsconfig.json` residual recorded.
- [x] Round-15 BLOCKER: `--conditions`/`-C` (the resolution map with the file left implicit) refused in both spellings; schema names both; agreement test pins fifteen hook names plus `-C…`; six refused rows with `--conditions-file=…` and `go test -cover ./cmd` kept legal and RUNNING; both arms verified load-bearing.
- [x] Round-16 — REQUIREMENT 5, built at 8f57c727: forgeBuildContract now ASKS for the nomination it gates merges on (report-step field list names mutationClaim; a titled MUTATION NOMINATION block states legality, the grep -oF verification, and when null is legal), pinned by a prompt-capture test with a positive control against empty extraction. THE EMISSION RIDER IS RETRACTED AS UNREACHABLE, measured against the deployed checkout: on the codex route the schema is filled by the codex BRIDGE, which copies a six-line wrapper trailer (BRANCH/HEAD/REMOTE_HEAD/PR/DIFF/WORKTREE) carrying no nomination line, so it fills `null` — the only honest answer available to it — regardless of what any brief or plan says. The contract edit is correct latent work that takes effect only once merged AND deployed; the transport (a seventh trailer line, the bridge copying it) is a SEPARATE card. No round of THIS card should attempt to emit `mutationClaim` or build that transport.
- [x] Round-17 BLOCKER (round-10 seat, reproduced against 4629df58 — the ELEVENTH escape of the family): a branch forges a proof by SHADOWING an allowlisted python module. Commit `pytest/__main__.py` whose body imports the target, nominate guard `python3 -m pytest tests/unrelated_test.py` for a syntax-breaking mutation with an unrelated control: `-m` puts the working directory first on the module search path, so the worktree's own `pytest` package — branch-authored code — IS the runner; red mutated, green control, green restored, nothing asserted. The argv is clean; the forgery is an ENTRY IN THE TREE. Close it on RUNNER PROVENANCE at the worktree seam in `proveInWorktree`: for any guard/control spelled `python3 -m <mod>`, read the pinned tree's top level (`git ls-tree --name-only -z <headSha>` — the list comes from git, never the agent; a failed listing refuses, fail closed) and refuse when an entry could BE the module (`<mod>` as a directory — a namespace package needs no `__init__.py` — or `<mod>.py`/`<mod>.pyc`/`<mod>[.…].so`; `pytest.ini` and `conftest.py` match none and stay legal). Exported pure predicate `pythonModuleShadow(argv, topLevelNames)`; FORGE_SCHEMA guard description gains the sentence (foreseeability, the round-10-major invariant) with a two-sided literal pin in the agreement test; unit rows (the repro, the name table with positive controls, control-side, ls-tree-failure fail-closed, a no-probe pin for non-python claims, probe-fired positive control) plus one real-git test that COMMITS `pytest/__main__.py` and refuses before anything executes, with a bun-spelled positive control in the same repo. Keep every existing rejection; `TEST_COMMAND_SHAPES` untouched; prover_version stays 1. THEN RUN THE FULL SUITE (`bun install --frozen-lockfile` first) and report testsPassed=true with the runner's own summary line, suiteOutcome='failed-preexisting' naming the settled environmental failures — a round that does not prove the suite fails whatever else it does.

- [x] Round-18 BLOCKER/major (Argus round 1 on the round-17 fix): the round-17 provenance probe closed the NOMINATED MODULE and left two siblings open, one of them affirmatively pinned as legal by its own test table. Both are now refused at the same seam, off ONE listing which is now RECURSIVE (`ls-tree -r --name-only -z <headSha>`, top level derived as each path's first segment) because the three things it must see live at different depths. (a) `pytestConfigShadow` — pytest imports the rootdir `conftest.py` itself and obeys `addopts` out of `pytest.ini`/`.pytest.ini`/`tox.ini`/`setup.cfg`/`pyproject.toml`, all with NOTHING on the argv, so a committed `conftest.py` whose body imports the target reproduces the round-17 forgery; matched on the BASENAME AT ANY DEPTH because pytest derives its rootdir from the ARGS, and asked only for `-m pytest` (`unittest` reads none of them). (b) `pythonImportShadow` — the reviewer's executed repro: a committed top-level `argparse.py` shadows a module the REAL runner imports on its way up, and the round-17 check never looked at it because `argparse` is not the module the argv names; ANY top-level `*.py`/`*.pyc`/`*.so` is now refused for a `python3 -m` nomination, deliberately broader than the repro because which names a runner imports is not enumerable from the argv. The LEGAL test table loses `conftest.py`, `pytest.ini` and `mypytest.py` — a row pinning a residual as legal is a contract a later round must delete — and gains `src/limit.py` and `tests/unrelated_test.py` as over-refusal guards, so a python file A DIRECTORY DOWN stays legal. Also: the no-production-file exemption's file list is capped by TOTAL LENGTH (4000 chars) with the elision COUNTED, not by a file count, so an ordinary diff still names every file and a thousand-file test-only rename cannot interpolate hundreds of KB onto one line; and the dotted-module split in `pythonModuleShadow` is annotated as unreachable defence-in-depth (`TEST_COMMAND_SHAPES` gates argv[2] to the literal `pytest`/`unittest`) so it is not read as evidence a dotted `-m` spelling exists.
- [x] Round-19 BLOCKER/major (Argus round 2 on the round-18 fix): the TWELFTH escape, and the LAST arm of the python family — a top-level DIRECTORY the runner imports. A seat ran it against `7cfc3e07`: commit `tests/__init__.py` whose body is `import src.limit`, syntax-break `src/limit.py`, nominate `python3 -m unittest tests.unrelated_test` with an unrelated control, and the gate returned `ok:true, proved:true` with nothing asserting the target; `argparse/__init__.py` is the same escape shadowing a runner DEPENDENCY. Neither sibling could see it: `pythonModuleShadow` reads only the module the argv NAMES (`unittest`, unshadowed here), and `pythonImportShadow` matches SUFFIXES over top-level names, which a directory does not have. `pythonPackageShadow` closes both off the SAME recursive listing, refusing on the directory's CONTENTS and never its name — a top-level directory DIRECTLY containing `__init__.py`/`__main__.py`, i.e. a REGULAR package, the only shape that executes branch code on import. A namespace package (neither marker) shadows a name but runs nothing, so it cannot make a guard pass restored and fail mutated. That is why the round-18 deferral's stated reason no longer holds: a plain `src/`, a plain `tests/`, a marker A DIRECTORY DOWN (`src/pkg/__init__.py` — not on the one `sys.path` entry `-m` adds) and a root `__init__.py` all stay legal, so `python3 -m` is not deleted by another route. Refused LAST of the three arms so the narrower message wins the wording. Unit repro + three package rows + a both-directions predicate table with seven over-refusal guards, and a real-git end-to-end refusal whose marker file stays unwritten (nothing spawned) with a bun positive control proving red-then-green in the same repo. Also this round: the DELETION-ONLY refusal's file list now goes through `namesWithinBudget` like the exemption's (it was raw `join(', ')`, so a deletion-only diff of a large directory reproduced verbatim the multi-hundred-KB single log line the round-18 cap was added to bound), pinned by a 1000-file row with a first-name positive control; and `namesWithinBudget`'s docblock now states the two ways it deliberately EXCEEDS the budget (the `… +N more` suffix is appended after the budget is spent, and the first name is always printed however long) rather than implying an exact ceiling.
- [x] Round-19 recorded, no code (Argus round 2 minors/nits): (a) `python3 -m pytest` is de-facto UNNOMINATABLE for any conventional python repo — `pythonImportShadow` refuses any `python3 -m` when ANY top-level `*.py` exists (`setup.py`, `noxfile.py`, a root `conftest.py`) and `PYTEST_CONFIG_BASENAME` refuses `-m pytest` for a `pyproject.toml`/`setup.cfg`/`tox.ini` at ANY depth. It fails CLOSED, costs this repo nothing (no matching basename among 3284 paths at `7cfc3e07`), and is spellable around; it is written here so `TEST_COMMAND_SHAPES`' pytest entry is not later read as a supported nomination. (b) `_test\.rs` is deliberately NOT in the declared-test basename rule the card's requirement 1 sketched — cargo has no `_test.rs` convention, so including it would classify production files as tests; the narrowing is intentional and must not be "restored". (c) The round-18 suite comparison labelled `187a9209` "the merge-base"; the true merge-base is `bee6dfd9` and `187a9209` is its ancestor. The `failed-preexisting` conclusion is unaffected and was re-derived independently by both seats; the LABEL was wrong.

- [x] Round-20 BLOCKERS/majors (Argus round 3 on the round-19 fix). (a) THE THIRTEENTH ESCAPE, one file extension from the twelfth: `TOP_LEVEL_PACKAGE_MARKER` matched `__init__.py`/`__main__.py` only, while python imports a directory holding nothing but `argparse/__init__.pyc` as a regular package all the same, and a seat forged `proved:true` through it with all four arms null. The marker now carries the sibling's suffix class (`py|pyc|so`), pinned as a literal in the agreement test and as prove-level rows; the docstring's false "only shape" claim is corrected. (b) THE SAME QUESTION ASKED OF BUN, reproduced by two seats: a committed `bunfig.toml` `[test] preload`, or a `tsconfig.json` `paths`/`baseUrl`, loads the target into an unrelated `bun test` with nothing on the argv. `bunConfigCandidates`/`bunConfigLoadHook` refuse both, scoped to the files THIS BRANCH CHANGED (`ProveInput.changed_files`, threaded from the git-derived list the gate already read to bind the proof) — a tree-wide reading would refuse every `bun test` nomination in any repo that ships a bunfig, this one included, which is the "no legal nomination exists" defect the card exists to fix arriving through the door marked security. Refused on the KEY, not the filename. Unit rows, a two-sided gate-wiring pin with its positive control, and a real-git pair: branch-authored config refused before anything runs, the identical config inherited from main still proving red-then-green. (c) The single-seat ASSERTION-FREE GUARD is recorded at the oracle as the standing LIMIT of mutation proofs — three exit codes cannot see an assertion — with the structural mitigations named where the verdict is formed. (d) `pythonPackageShadow` and the bun arm now appear in FORGE_SCHEMA's guard description with two-sided literal pins, and `control`'s description states what `validateClaim` really enforces (a real test invocation, distinct from the guard, no general shell). (e) The python AGGREGATE over-refusal is stated at `TEST_COMMAND_SHAPES`: a conventional layout trips two arms at once, so neither python spelling is nominatable there; it fails closed, and relief means diff-scoping those arms as the bun one is scoped (follow-up card). (f) Minors: `REASON_NAME_BUDGET` dropped to 3,600 so the elision COUNT survives `STAGE_REASON_CEILING` (4,000); the forge contract's occurrence check is spelled `grep -oF … | wc -l`, since `grep -cF` counts LINES.

- [x] Round-21 BLOCKER/major (Argus round 4 on the round-20 fix): THE BUN CONFIG ARM MATCHED ONE SPELLING OF A KEY THE PARSER ACCEPTS IN FOUR, and did not read the manifest at all. Two seats reproduced end to end against `a25c7319`: reviewer B forged `ok:true, proved:true` through `"preload" = […]`, `'preload' = […]` and `test.preload = […]` (the bare spelling was correctly refused — the positive control), because `bunConfigLoadHook`'s rule was a line-anchored BARE-key regex; reviewer A forged the same through a branch-written `package.json` `imports` map redirecting a bare specifier inside a guard file main already carried, because `BUN_LOAD_CONFIG_BASENAME` excluded `package.json`. Same defect shape this branch had just closed for pytest: the defence spells the key one way, the runner reads it several. `TOML_PRELOAD_KEY` now reads the key UNANCHORED and quote-insensitive, over TOML basic-string escapes decoded first (`"preload"` is the same key), covering the bare, quoted, single-quoted, dotted and inline-table spellings; `package.json` joins the candidates and is refused on `imports`/`exports` — the two keys that decide which FILE a specifier means — and on nothing else, so a dependency bump keeps its bun nomination. Eight-spelling predicate table with three near-miss negative controls (`mypreload`, `preloader`, `[loader]`), the same eight end to end through `prove`, four through `runMutationProofGate` itself (the surface the repro used), the manifest arm both directions with its no-map positive control, and the FORGE_SCHEMA guard description now states the spellings and the second map with two-sided literal pins. Also: the `extends` rationale on `BUN_LOAD_CONFIG_BASENAME` was wrong in both directions (bun does NOT follow tsconfig `extends` for `paths`, and an extends-only root config holds no key so it is not refused on its own) — rewritten to say why the infix names are candidates in their OWN right; `truncateNote`/`truncateStageReason` fold onto one `truncateWithPointer(reason, ceiling)`; and the python `-m` aggregate over-refusal is now carried as a NAMED follow-up card (below) rather than a comment.

- [x] Round-22 BLOCKER/majors (Argus round 5 on the round-21 fix). (a) THE BUN CONFIG ARM REFUSED THE WRONG THING: it fired on the FILE being in the diff and the hook KEY being present at the head, never on the key being BRANCH-AUTHORED, so a dependency bump in any `exports`-carrying workspace manifest stripped a bun-only repo of its only nomination — the "no legal nomination exists" defect this card exists to fix, re-created by the fix. A reviewer executed it through `runMutationProofGate` on this repository's own byte-identical manifest, and proved the documented escape hatch ("land the manifest change on its own commit") cannot work, because `changedFilesWithStatus` reads the WHOLE range `<base>...<ref>`. Closed by comparing the KEY'S VALUE: `ProveInput.base_ref` is threaded from the gate's `base_branch`, `bunConfigHookSlice` canonicalises the value at the head and at the MERGE BASE, and only a difference is refused. Every step fails CLOSED (no base, no merge base, no file there, an unreadable value all refuse), a reformat is not a change, and `bunConfigLoadHooks` now returns EVERY key a file carries so an inherited `imports` map cannot shelter an `exports` map the branch wrote beside it. Unit rows for the bump, the rewrite, the sheltered map, the three fail-closed routes and the slice predicate itself, plus a real-git PAIR (bump proves red-then-green, rewrite refused before anything runs) on a repo whose MAIN carries the map. (b) THE ARM WAS GATED ON `argv[0] === 'bun'`, so the identical `package.json` forgery returned `ok:true, proved:true` under `node --test` — two seats, byte-identical forgeries, one refused and one not. `node` now joins `bunConfigCandidates` FOR `package.json` ONLY: node resolves `imports`/`exports` exactly as bun does, and reads neither a bunfig nor a tsconfig `paths` map, so refusing those for a node nomination would be an over-refusal with no forgery behind it. Refusal rows plus the positive control that the two bun-only files still leave a node nomination running. (c) `TOP_LEVEL_PACKAGE_MARKER` missed the ABI-TAGGED `__init__` spellings (`__init__.abi3.so`, `__init__.cpython-<ver>-<platform>.so`) that `importlib.machinery.EXTENSION_SUFFIXES` makes importable and that the file-half sibling `pythonModuleShadow` already matched; the marker now carries them, with a three-row table and three over-refusal controls. (d) FORGE_SCHEMA's `control` description said "no wrapper" while the prover deliberately keeps `npm run …`/`make …` legal as CONTROLS (the wrapper refusal is the GUARD's) — the schema over-constrained Forge with no forgery path behind it, and now states what is enforced; the guard description carries the node manifest arm and the value-comparison rule in place of the escape hatch that does not exist.

- [x] Round-23 BLOCKERS/majors (Argus round 6 on the round-22 fix). (a) THE THIRTEENTH ESCAPE OF THE FAMILY, reproduced by two seats against `05c73e47`: both bun/node config arms ask who wrote the CONFIG, and a branch can walk between them by leaving main's `bunfig.toml` alone and REWRITING THE FILE ITS PRELOAD NAMES. The config is not in the diff, so `bunConfigCandidates` produced no candidate; its `preload` is byte-identical to main's, so the value comparison called it main's — and a syntax-breaking mutation with an unrelated bun guard and a node control returned `ok:true, proved:true` with nothing asserting the target, because the branch's hook ran in every bun test process before a single test did. The docblock had conceded exactly this ("Where the map POINTS is still not reasoned about"). Closed by `bunConfigInspected` (the ROOT bunfig is read whether or not the branch touched it — `bun test` reads it out of its working directory) plus `hookTargetChangedByBranch`, which asks only of `preload`, the key whose value is code the runner EXECUTES unconditionally: if the preloaded file is one the BRANCH changed, the nomination is refused whoever wrote the config. Spellings are canonicalised like every other path on this seam and the loader's own completions are candidates (`./tests/support/hook.js` and `./tests/support/hook` are the `.ts` file). The authorship arm is now asked only of a config IN THE DIFF, so reading the root bunfig unconditionally cannot turn an unresolvable merge base into a refusal for main's own file. Unit rows for the escape, the three spellings, the node-reads-no-bunfig row, the predicate table, plus a real-git end-to-end test (`seedBunfigRepo('hook')`) whose expectation was verified to FAIL — `ok:true` — with the widening reverted; and the positive control that an inherited preload aimed at a file the branch did NOT touch still proves red-then-green. THE RESOLUTION MAPS ARE DELIBERATELY NOT WIDENED THE SAME WAY: `imports`/`exports`/`main`/`paths` redirect a SPECIFIER (loaded only if a test imports it, and the map's own text is already compared against the merge base), and refusing because main's map names a file this branch edited would take the bun nomination away from nearly every PR in a bun repo — the defect this card exists to fix. (b) `main` IS A RESOLUTION MAP WITH ITS KEY LEFT IMPLICIT — the codex seat's minority veto, reproduced on node v22: with no `exports` map present the CJS resolver falls back to `main`, so a branch-authored `"main": "./src/limit.cjs"` made `require('..')` inside an otherwise unrelated guard load the mutated file. `main` joins `imports`/`exports` in `MANIFEST_HOOK_KEY` and gets the identical merge-base value comparison, so an INHERITED `main` (which nearly every published manifest carries) costs a nomination nothing; unit pair plus a real-git pair, the refusal row verified to fail with the key removed. (c) A MANIFEST THIS BRANCH ADDS is still refused, but no longer with a remedy that cannot exist: `hookProvenance` distinguishes 'added' (no such file at the merge base) from 'branch', and the added case says so instead of telling the branch to "restore `exports` to what main carries", which for a new `packages/*/package.json` names nothing. (d) Minors/nit: `TOML_PRELOAD_KEY` now LOOKS AT the delimiter instead of consuming it, so two occurrences sharing one character can both match under the `g`-scan (unreachable in valid TOML, failed closed, pinned by a row that fails on the alternation); and the `TEST_COMMAND_SHAPES` python aggregate now states that the config over-refusal is wider than "a python repo" — the basename match runs at ANY depth, so one nested `pyproject.toml` in a polyglot tree kills `-m pytest` repo-wide (same direction, same follow-up card).

- [x] Round-24 BLOCKER (Argus round 7 on the round-23 fix; the card's review round 8, JOB 1): THE CONTROL CAN FORGE THE PROOF — the FOURTEENTH escape, and a DIFFERENT SEAM: not what the guard LOADS but what a nominated command WRITES. `proveInWorktree` (trident/mutation-prover.ts ~:3089-3106 at 766ae106) runs observe(guard) RED → observe(control) → restores ONLY `claim.file` (comparing only `file_sha256_restored` vs `file_sha256_before`) → observe(guard) again — so the control, branch-authored code deliberately legal as a wrapper or as any test file whose module body runs on load, executes between the two guard observations with write access to the WHOLE tree, and nothing checks what it did to anything but the target. Reproduced at 766ae106: guard `bun test tests/g.test.ts` (unconditionally red, never imports the target) plus a control whose module body writeFileSyncs a PASSING `g.test.ts` → `ok:true, proved:true`; same via `make`. Pre-existing at base bee6dfd9, closed on this branch because this card owns the gate. CLOSE IT BY RE-PROVISIONING the proof worktree (`worktree remove --force` then `worktree add --detach --force` at the PINNED headSha, via `deps.run_host`, `repo` threaded into `proveInWorktree`) between the control observation and the restored-guard observation, refusing when the re-add fails, so the final observation is BY CONSTRUCTION of a tree no nominated command edited — chosen over a `git status --porcelain` gate because porcelain is blind to writes under ignored paths (a control writing `node_modules/…` plants code the guard imports, porcelain-clean) and would refuse honest runners that drop caches (`__pycache__`, `.pytest_cache`, coverage). The same re-provision also closes the one-actor variant: a GUARD whose first (red) run rewrites ITSELF green. `file_sha256_restored` is read from the fresh tree; `evaluate`, the evidence schema, the proof token/HMAC and `MUTATION_PROVER_VERSION` (=1) unchanged. AND CORRECT THE SENTENCE THIS BRANCH ADDED that steers builds into the shape: `control.description` in trident/inner-workflow.mjs (~:911) claims a wrapper control "merely has to stay green" — falsified by the repro and contradicting the prover's own `sh -c` rationale; it now states that a control's exit code can only refuse a proof and that whatever its hidden script WRITES is discarded because the prover re-provisions the worktree — keeping the literal `LEGAL CONTROL` / `refused as a GUARD` phrases the agreement test pins, plus a NEW two-sided pin on the corrected rationale. Tests: mocked happy-path counts become 3 removes / 2 adds with a call-ORDER assertion (second add strictly after the control argv and strictly before the second guard argv, extraction positive-controlled); a `worktreeReAddFails` row refusing with its own reason BEFORE the final observation (exactly one guard spawn); real-git `seedControlForgeryRepo()` with the reviewer's repro row and the self-rewriting-guard row both refused on 'did not return to GREEN' (each verified once to FAIL — `ok:true` — with the re-provision hunk reverted, then restored), and the honest positive control proving red-then-green THROUGH the re-provision in the same repo. THEN RUN THE FULL SUITE (`bun install --frozen-lockfile` first) and report testsPassed=true with the runner's own summary line, suiteOutcome='failed-preexisting' naming the settled environmental failures. TWO EARLIER COMMITS ON THIS BRANCH (7180c470, 6dd9667f) closed this same seam with the OTHER mechanism the reviewer named — a `git status --porcelain --ignored` gate before the restored-guard observation. This round replaces it with the re-provision and deletes it: a status gate is blind in one direction and over-refuses in the other (it cannot see a write the runner's own cache explains away, and it refuses an honest runner that drops one), while a fresh checkout of the pinned sha needs no judgement about what a written path meant.
- [x] Round-24 JOB 2 — RECORDED, no code (this plan regeneration): the bunfig-preload OVER-REFUSAL is a named deferral now — see the Round-24 addition in the Deferred paragraph below. The shape: this repository's root `bunfig.toml` preloads `./tests/support/scrub-instance-env.ts`; `bunConfigInspected` (:1413) reads the root bunfig for ANY bun argv and the `hookTargetChangedByBranch` call site (~:2997) refuses the NOMINATED PAIR — guard and control alike — whenever the preload names a branch-changed file, and `node --test` cannot run `bun:test` files, so a diff touching a preloaded file has NO legal guard at all and neither remedy the refusal prints is available (D1 moved this shape from "no legal target" to "legal target, no legal guard"). RECORDING chosen over scoping because every sound narrowing examined re-opens the round-23 escape it defends against (rewrite the preloaded file, leave the config alone), and widening or narrowing a forgery defence is not a side effect of a fix round — the round-21 precedent. It fails CLOSED: an over-refusal, never a forged proof.
- [ ] Round-24 JOB 3 (codex blocker, panel-downgraded; only after JOBs 1-2): a non-test helper DIRECTLY under `__tests__/` classifies as a DECLARED TEST (`isDeclaredTestFile`, :278), so an all-such diff buys the no-production-file exemption (:4114). PIN THE DELIBERATE DESIGN rather than reverse it: boundary rows asserting that `gbrain-memory/__tests__/helper.ts` (no test suffix) classifies as a declared test, that a diff of ONLY such helpers plus markdown is exempt with the no-production-file reason (NOT the prose-only string), and the load-bearing negative beside it (the same diff plus one production file and no claim still REFUSES) — with a comment recording that reclassifying the 13 direct-`__tests__/`-child support libraries is the standing follow-up card's work, not a fix round's, and a positive control against an empty extraction as in every table here.
- [ ] Round-24 JOB 4 (codex blocker, panel-downgraded; only after JOBs 1-2): FORGE_SCHEMA's guard description names `imports`/`exports`/`main` in the BUN manifest clause but its NODE clause still reads "a package.json `imports`/`exports` map refuses `node --test` too", while the prover refuses a branch-authored `main` for node as well (`MANIFEST_HOOK_KEY`, :1477, is runner-agnostic for `package.json`). Add `main` to the node clause in trident/inner-workflow.mjs, and rework the agreement test (trident/inner-workflow-mutation-claim.test.ts ~:447-455) to DERIVE the backticked key phrase from the `MANIFEST_HOOK_KEY` literal as read out of the prover source and assert BOTH clauses carry the derived phrase (occurrence count of the one extracted phrase >= 2, with a positive control against an empty extraction), so the next key added to the constant reddens the test instead of the two sentences drifting apart. (PARTLY LANDED at 7180c470 on this branch: the node clause already names all three keys and an agreement row pins the new wording; what remains is deriving the list from `MANIFEST_HOOK_KEY` and asserting BOTH clauses off the one derived phrase.)

Named follow-up card, opened by round 21 at a reviewer's request: **"trident: diff-scope the python runner-provenance arms so `python3 -m` is nominatable in a conventional python repo"** — `pytestConfigShadow` (matches `pyproject.toml`/`setup.cfg`/`tox.ini` at ANY depth), `pythonImportShadow` (any top-level `*.py`/`*.pyc`/`*.so`, so a root `setup.py` is enough) and `pythonPackageShadow` (`tests/__init__.py`) between them delete BOTH python spellings from `TEST_COMMAND_SHAPES` for the standard layout, leaving the allowlist entry unreachable in the repos it exists for. It fails CLOSED — an over-refusal, never a forged proof — and the fix is the scoping `bunConfigCandidates` already uses: read only the files THIS BRANCH CHANGED, which is sound because a config main already carried is main's, reviewed like any other merged file. Not done here because widening a forgery defence is not a side effect of a fix round.

Deferred to follow-up cards (every one recorded; most latent and failing closed, the one marked OPEN is a forgery-family spellings awaiting their own cards): the `.trident` prose-directory question; OPAQUE SCRIPT RUNNERS (the family is not closed by construction — argv-visible spellings are refused as found); PYTHON DEPENDENCY SHADOWING BY DIRECTORY is CLOSED at round 19 by `pythonPackageShadow` (contents, not name); what remains latent is the NAMESPACE-package half — a top-level directory with no `__init__.py`/`__main__.py` shadows a name but executes nothing on import, so it cannot forge a proof, and it is not refused. The committed `tests/tsconfig.json` residual stays (the python `conftest.py` half of that class is closed at round 18); fixtures/snapshots under `tests/` classifying production; the direct-`__tests__`-child rule declaring 13 support libraries tests; unittest `test*.py` and `tests/*.rs` classifying production yet un-nominatable; deletion-only production diffs unclearable; the sim's `{ok:false, exempt:true}` shape; the exempt stamp written before the merge attempt; mocha's `-R` and pytest's `-p` short letters; and the codex build route's reporting transport for `mutationClaim` (the retraction above — a seventh trailer line and the bridge copying it, its own card). Round-24 addition (JOB 2, recorded above): a diff that TOUCHES A FILE THE ROOT BUNFIG'S `preload` NAMES has no legal guard at all — the root bunfig is read for any bun argv (`bunConfigInspected`), the `hookTargetChangedByBranch` refusal covers guard and control alike, and `node --test` cannot run `bun:test` files, so neither remedy the refusal prints exists for this repository's own `tests/support/scrub-instance-env.ts`; it fails CLOSED (an over-refusal, never a forged proof), and relief — e.g. asking the arm only of the runner the nominated guard actually is, or a guard route that provably does not read the root config — is its own card, because every narrowing examined re-opens the round-23 escape. Round-23 addition: WHERE A RESOLUTION MAP POINTS is not reasoned about (only where a `preload` points is) — a branch-changed file reached through an inherited `imports`/`exports`/`main`/`paths` map is the general transitive-import problem, which three exit codes cannot decide, and refusing on it would delete the bun nomination from nearly every PR; and `ProveInput.changed_files`/`base_ref` remain OPTIONAL, defaulting the whole config-provenance arm to OFF when omitted — correct at today's single wired call site (`runMutationProofGate`, pinned by a test), but a new caller could silently re-open a closed forgery class, so making them required on `proveInWorktree` is its own card. (The round-21 addition — the package.json arm as the WIDEST over-refusal on this seam — is CLOSED at round 22 and is no longer deferred.) Round-20 additions, all single-seat and unverified: runner-DISCOVERED test names (`src/thing_test.ts`, `testfoo.py`, `*_spec.ts`) classify production, so a test-only diff in those conventions misses the exemption; the 13 support libraries directly under `__tests__/` now also BUY that exemption rather than merely failing to be nominatable; and `unittest discover -s <dir>` makes a SUBDIRECTORY `sys.path[0]` too, which the top-level-only scan does not read (no execution escape was demonstrated). The full-repo suite red is pre-existing and environmental: 142 pass / 23 fail with byte-identical failure line-sets at base and head after timing-stripping, none of it under `trident/`.
