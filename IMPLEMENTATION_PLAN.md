# IMPLEMENTATION_PLAN — Git truth comes from git (Parts 1–3)

Regenerated 2026-08-15 against the card branch trident/git-truth-comes-from-git-the-publis @ 23c14a0 (= origin/main cd8215e + T5 8043eb9 + P2b 23c14a0). ⚠️ The local checkout is BEHIND origin — always fetch and work from the branch tip, never local main. CORRECTION to the prior regeneration: the "P3 residue" item cited line numbers (3785/3804/3809/3861/3881-3888/3953/4015) that match origin/main, not the branch — it failed to credit T5 (8043eb9), which is committed on this branch and fixes every cited site. Verified line-by-line this iteration; checked off below.

- [x] P1 — Outer publisher resolves the head itself: `publishBuiltCommit` runs `git rev-parse --verify refs/heads/<branch>` at the credentialed boundary; the branch NAME is the handoff (orchestrator.ts, shipped in #283, reconciled onto the rebase-publish flow).
- [x] P1 — An agent-supplied sha is a CHECK, not a source: prefix-match against the resolved head; a disagreement fails naming both values verbatim ("the build reported commit 'X' but branch B resolves to 'Y'").
- [x] P1 — No sha / 7-char sha still publishes: `oidClaim` carries a 7–40-hex claim or null; the forge-done and fix-round handoffs never throw on sha shape; `publishHead` is a cross-check only.
- [x] P1 — Zero commits ahead of the remote still fails: "the build left no new commits to publish" refusal, compared against the PRE-rebase local tip.
- [x] P2a — The resume live head is READ IN CODE by the launcher: `resolveResumeLiveHead` (orchestrator.ts ~628), 3 attempts, tri-state (40-hex / 'absent' / ''), threaded as `resume_live_head` → `resumeLiveHead`; the `head-probe-round-resume` agent seat is skipped whenever the launcher supplies it.
- [x] P2a — 'absent' (the authority says the branch is gone) is a real answer → rebuild as 'head-branch-absent', distinct from '' ("could not read").
- [x] P2b — A genuinely unreadable head is a bounded STOP, never a rebuild: `classifyResume` returns `{ mode: 'stop', reason: 'head-unreadable' }` for ''; the workflow call site (inner-workflow.mjs ~3648) writes a terminal failure naming the branch and the recorded OID, blockKind 'infra-only' + terminalCause, no checkpoint overwrite, no re-entry into plan/build/review. Shipped 23c14a0 with guards at both boundaries.
- [x] Reason quality — infra-only stops carry a MEASURED `terminalCause`; `innerTerminalFailureReason` emits the specific reason only with that signal; no "without Argus APPROVE" for failures Argus never reached.
- [x] P3 — the build-completion head is READ from git at both completion sites (CORRECTED: shipped in 8043eb9 on this branch; the prior plan iteration diffed origin/main and missed it). `readBuiltHead` (inner-workflow.mjs ~2806) runs `git rev-parse --verify refs/heads/<branch>` locally with one retry; `branchHead`/`reviewedHead`/`forge-done`/`ralph-task-built`/`fix-round-N` checkpoint heads all source from it; `forge.commitSha`/`fix.commitSha` are prefix CROSS-CHECKS that stop loudly naming both values verbatim; a no-sha local build proceeds pinned to the git-read OID (the "nothing was built" throw now gates on diffFile only); unreadable-after-retry → bounded `builtHeadStop`, never rebuild, never "without Argus APPROVE". Read ONCE at completion, deliberately never re-probed at publish/merge (the load-bearing #545 distinction). Guarded by inner-workflow-built-head.test.ts. NOTE: the read is carried by a one-command probe agent (the only exec seam a workflow script has); it is fail-closed end-to-end — the outer publisher independently rev-parses in real code (P1) and `--match-head-commit` refuses a mismatched pin — so a mangled transcript can refuse work but never certify or publish the wrong commit.
- [x] P3 (pr mode) — the reviewed head is git truth end-to-end: the publisher's rev-parsed (post-rebase) OID is encoded in the `outer-published:<oid>:<remaining>:<round>` checkpoint; the re-fired workflow reviews exactly that recorded OID with a diff the publisher generated from it, never a re-probe.
- [x] P3 item 3 — `publishBuiltCommit`'s refusal message names the requested OID and the actual OID verbatim.
- [x] Orchestrator pre-fire short-circuit — when `resolveResumeLiveHead` returns `''` (3 failed code reads), stop the run at the outer boundary with the same measured infra-only reason (via `innerTerminalFailureReason`, wording identical to the inner stop) instead of paying a workflow fire that will immediately stop itself. `pr-merged` checkpoints are exempt (classifyResume resolves them to `merged` before the head check) and `classifyResume` stays the single semantic decider — this is only the cheap fast-exit. Checkpoint columns survive untouched so a re-run after the read recovers resumes exactly.
- [x] Recovery note (do not lose it): `trident/derive-inline-activity-from-evidenc` is local == origin at 8407d9c with PR #263 OPEN — no unpushed work at risk. 9a46680 is preserved in the object store (not an ancestor; the branch was rebase-republished). Never reset, force past, or gc it.

Guard rails (not tasks): do NOT loosen `publishBuiltCommit`; do NOT re-probe heads at publish/merge time; do NOT edit `classifyResume` or inner-workflow.mjs for the remaining task.
