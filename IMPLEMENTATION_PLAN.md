# IMPLEMENTATION_PLAN — PR number on the card: durable #NNN tag, clickable, + title-opens-doc (owner-requested 2026-08-14)

Governing spec: this Plan card (parts 1-4, acceptance (a)-(h)). Verified against main 2026-08-14; T1 landed on this branch (commit 6a4fdcd) and is re-verified below:

- DONE on branch: migration 0122 (`work_board_items.pr` + `pr_url`), `detachRun(pr_info)` writing both columns in the same transaction as status/completed_at (both outcomes), `attachRun` clearing them, `trident/repo-web-url.ts` (`githubWebUrlFromRemote` pure parser + promise-memoized `makeRepoWebUrlResolver`), `buildBoardReconcileObserver` resolving `pr_url` best-effort with an injectable resolver, `pr`/`pr_url` in the composer fan literal (~3684), `AppWsWorkBoardItem`, and the web client item type + `parseWorkBoardItems`. Acceptance (c) test drives the REAL observer → detachRun → re-read.
- STILL MISSING: `run_progress` has no `pr_url` anywhere (server `RunProgress`, `AppWsRunProgress`, client `RunProgress`/`parseRunProgress`) — a LIVE card cannot link its `#NNN` yet. `WorkBoardTab.tsx` is untouched: meta line (~998) still renders only step tag + round, completed line (~781) still `Merged · date`, the `cwb-doc-link` element (~924-940) still exists, and the title button still fires `onStartEdit` unconditionally. No doc-H1→card-title sync exists (`DocStore.onMutationSuccess` at `gateway/http/doc-store.ts:404` only feeds `AnchorWalker.handle`).

- [x] **T1 — durable PR on the item, written by the real detach path.** Migration 0122 adds `pr INTEGER` + `pr_url TEXT` to `work_board_items` (+ regen `migrations/expected-schema.txt`). `WorkBoardItem` gains `pr`/`pr_url`; `detachRun` gains an optional `pr_info` and writes both columns in the SAME transaction as status/completed_at, for BOTH outcomes; `attachRun` clears them. New `trident/repo-web-url.ts` (pure parser + cached shell resolver). `buildBoardReconcileObserver` resolves `pr_url` (injectable, internal default, failure → null, never blocks reconcile) and passes `{pr, pr_url}`. `pr`/`pr_url` added to the fan literal, `AppWsWorkBoardItem`, and the web client type + `parseWorkBoardItems`. Acceptance (c) guard test drives the REAL observer → `detachRun` → store re-read.
- [x] **T2 — live `pr_url` on `run_progress`.** `RunProgress` (server `trident/run-progress.ts` + `AppWsRunProgress` + web mirror in `work-board-client.ts`) gains `pr_url: string | null`; `deriveRunProgress` takes an optional `repo_web_url` and composes `<url>/pull/<pr>`; `runProgressForItem` takes an optional sync `peekRepoWebUrl(repo_path)`. New `makeRepoWebUrlCache` in `trident/repo-web-url.ts`: SYNC `peek(repo_path)` returning the cached url or null, kicking a background warm (via the existing promise-memoized resolver) on first miss. ONE cache built in `open/composer.ts`, fed to BOTH sync attachment sites — `withRunProgress` in `gateway/http/work-board-surface.ts` (new `repo_web_urls` opt) and `fanWorkBoardChanged`. Tests inject a pre-warmed structural stub. First post-boot snapshot may render the tag as plain text until the warm lands (documented, acceptable — checkpoints push and the 15s poll refreshes).
- [ ] **T3 — render the tag (parts 1-3 in `WorkBoardTab.tsx`).** Meta line: `#NNN` FIRST, then step tag, then round (`#265 Building round 1`); number = `run_progress?.pr ?? item.pr`; URL = `run_progress?.pr_url ?? item.pr_url`. `<a target="_blank" rel="noopener noreferrer">` when a URL resolved, plain `<span>` when not; NOTHING when no PR. Widen the meta-line gate to `tag !== null || pr !== null`; terminal lanes show it too (`#261 Failed`). Completed row: `Merged #265 · Aug 14` from durable `item.pr`/`item.pr_url`; a PR-less completed card renders today's `Merged · Aug 14` byte-identically. CSS `.cwb-pr` in chat-react.html. Mutation-resistant tests incl. exact `rel`/`target`/href and the no-PR no-tag case (acceptance a, b, d + completed-render half of c).
- [ ] **T4 — title opens the doc; the doc-link line dies.** Delete the `cwb-doc-link` element (~924-940). A card WITH a resolvable doc (`docPathFromDesignRef !== null` and `onOpenDoc` wired): title click calls `onOpenDoc` — NO inline rename on that card at all, and no new control anywhere in `cwb-actions`. A card WITHOUT a doc keeps today's `onStartEdit` rename exactly. Title carries NO link styling (no blue, no underline; cursor may change). Tests: doc-card click opens doc and never enters edit; docless card still renames; no rename affordance exists on a doc card (acceptance e, g, h).
- [ ] **T5 — doc H1 → card title sync (part 4 mechanics).** New gateway module hooked off `DocStore.onMutationSuccess` (COMPOSED with the existing `AnchorWalker.handle` at the wiring site, not replacing it): on a `write` to `plans/*.md`, parse the first `# ` H1 — H1 wins over frontmatter; no readable H1 → NO-OP on the card. Find the card whose `design_doc_ref` resolves to that path in `workBoardScopeKey(owner, project_id)`; update its title (sanitized) when different; rewrite frontmatter `title:` to match ONLY when different (fixpoint, no loop). The FILENAME never changes; sync is doc → card only. Tests drive the REAL `writeDoc` path and assert: card renamed, frontmatter rewritten, filename unchanged, `design_doc_ref` still resolves; an H1-less save leaves the title untouched (acceptance f + the fake-test guard).
