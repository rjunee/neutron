# IMPLEMENTATION_PLAN — Email Core consolidation (SPEC.md § "Email Core consolidation — absorb the standalone email system")

Governing docs: SPEC.md P1–P4 build queue + docs/plans/2026-08-06-email-core-consolidation-plan.md § 5 (target architecture) / § 6 (phases). Verified this pass (2026-08-11): NONE of the P1 artifacts exist yet — no `cores/free/email/src/pipeline/`, no `cores/free/email/migrations-pipeline/`, no `gateway/cores/email-pipeline-wiring.ts`, no `modifyMessage`/generalized `ensureLabel` on the `GmailClient` contract. Purity constraints bind every task: fixtures `*.example.com` only, instance/owner vocabulary, leak gate `scripts/ci/leak-gate.sh` must pass, no new secrets, substrate-LLM-only.

- [x] **Phase 0 — prerequisites.** Push self-heal is live (`gateway/push/dispatcher.ts` prunes only `DeviceNotRegistered`, re-register heals — PR #114 semantics verified in tree). The Core's list/read path (PR #115) is in the tree (`google-client.ts` list + `getMessage` present and used by `email_list`/`email_search`).
- [x] **P1 — pipeline store + poller + classification + escalation (capability 2 end to end).** Instance-level sidecar `<owner_home>/email/pipeline.db` with its OWN migration tree `cores/free/email/migrations-pipeline/0001_email_pipeline.sql` (numbering per-DB-file, `migrations/runner.ts:58-63` — 0001, not 0002); tables `emails` (with folded escalation state `escalated_at`/`escalation_attempts`/`last_error`), `sender_cache`, `sender_rules` (ships EMPTY), `checkpoints` (`go_live_after`, `last_poll_at`, `consecutive_errors`, scribe watermark slot); `src/pipeline/{store,classify,prompts,escalate,poller}.ts`; contract additions `modifyMessage` + generalized `ensureLabel` on Google + in-memory backends and the multi-account router (processed label `Neutron/processed`); NEW `gateway/cores/email-pipeline-wiring.ts`; cron `email-pipeline-poll` ({kind:'interval_ms', interval_ms:300000}, skip_if_running:true) registered via the `registerIdleNudgeSweepCron` pattern; composition seam `open/composer.ts` → `CompositionInput.email_pipeline` → `build-core-modules.ts` tasksModule init, threading `deliver` (durability 'reply', topic `appWsTopicId(OWNER_USER_ID)`), `PushDispatcher.pushAll` (best-effort ALONGSIDE chat), `readOwnerTimezone`, and the substrate one-shot LLM (nullable — LLM-less box degrades to the deterministic cascade). Go-live cutoff: pre-cutoff mail archived, never classified. Acceptance is an observable outcome on a real install (escalation in chat within one poll interval). Update docs/AS_BUILT.md.
- [ ] **P2 — twice-daily brief DELIVERED AS EMAIL + `instance_metadata.email_digest_enabled` setting (user-facing product setting, not a flag) + the pre-cutover rehearsal mode (writes held back, brief emailed alongside the old service).** `src/digest/{window,generator,render}.ts`, `briefs` table usage, owner-local DST-correct windows via `readOwnerTimezone`.
- [ ] **P2.5 — classification setup by inbox SURVEY + owner INTERVIEW.** Mechanism ships with ZERO rules; setup samples the real inbox, clusters, interviews the owner, writes per-owner instance data. A hardcoded taxonomy fails acceptance.
- [ ] **P3 — retire the dead `triage-scheduler.ts`; scribe email→memory fan-out rides the poller** (watermark re-homed into pipeline `checkpoints`; email→memory observably still happening after deletion; `/email triage` on-demand tool untouched).
- [ ] **P4 — owner cutover + standalone-service decommission.** Sender-rules importer (reads owner-local files at runtime, PII-free script), hard switch on writes (no parallel mutation), old crons off → verified interval → service deleted; DB not imported.
