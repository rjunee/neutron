# IMPLEMENTATION_PLAN — Work Board hardening (branch trident/work-board-hardening, PR vs main)

Scope: the LOCKED remainder of WAVE 3.5 Work Board (Decisions Log 2026-06-29, managed SPEC.md WAVE 3.5 — verified 2026-07-23). Layer A (board + per-turn injection + trident parallel-exec) is SHIPPED; two items remain: Layer B context-management (aggressive orchestrator reset + rehydrate) and CC TodoWrite ingestion. NO FEATURE FLAGS — everything ships as default behavior. Verify-before-assert: re-grep every cited line before editing; lines may have moved.

SUBSTRATE-SUPPORT CHECK (planning pass, 2026-07-23 — the Layer B gating question, answered): the CLI persistent-REPL context-editing beta (`clear_tool_uses` tool-result eviction) is NOT available — zero codebase hits for any context-editing primitive, no `claude --help` flag; `clear_tool_uses` is a Messages-API beta with no seam into a PTY-driven interactive `claude` REPL. The composer-side inject/rehydrate seam IS available and proven: `resetPooledSessionContext` (`runtime/adapters/claude-code/persistent/context-reset.ts:73`, merged with the `/reset` command) + the runner's cold-turn latch `contextSent` (`gateway/wiring/build-live-agent-turn.ts:679`, checked `:1094`, added `:1297`) — deleting a topicKey makes the next turn re-run `composeFirstTurnPrompt`, re-assembling the FULL grounding (persona + work board + reflection + memory index + nexus + services) with no new prompt-assembly code. Layer B therefore = periodic composer-side hard reset (`/clear`) + rehydrate-via-re-cold-turn, exactly the fallback the SPEC anticipated.

- [x] **Layer A / Phase 1 — board works** (managed SPEC WAVE 3.5, DONE 2026-06-30): `WorkBoardStore` + `work_board_*` agent tools + HTTP `/work-board` + per-turn `<work_board>` injection (`work-board/store.ts`, `work-board/agent-tool.ts`, `work-board/fragment.ts`, `gateway/wiring/build-live-agent-turn.ts:994-1004`, `open/composer.ts:3379`) + web/mobile WorkBoardTab. Verified present in code this pass.
- [x] **Phase 2a — trident exec-model rearchitecture** (DONE 2026-06-30, #131): Workflow-tool dispatch, durable `code_trident_runs` harvest via TridentTickLoop. Verified: `trident/inner-loop.ts`, composer trident wiring.
- [x] **Phase 2b — trident bound + parallel** (DONE 2026-06-30, #140): required `board_item_id` on dispatch, N concurrent tridents, per-item activity icons, board-reconcile observer (`open/composer.ts:3645` `buildBoardReconcileObserver`).
- [x] **`/reset` chat command** (merged 2026-07-23): `resetPooledSessionContext` primitive + `buildResetChatCommandFilter` + composer wiring (`open/composer.ts:1457-1466`). This is Layer B's actuation primitive.
- [x] **TASK A — Layer B context-management: periodic orchestrator hard-reset + lossless rehydrate.** Composer-side (CLI context-editing beta confirmed absent — see check above). Runtime: extract shared actuation helper from `resetPooledSessionContext` + new `createPooledContextResetSweep` in `context-reset.ts` (2-dim pool-prefix sweep over the owner's `cc-agent-*` sessions, post-compact-bytes baseline-DELTA trigger via WeakMap per session object — cannot re-fire-loop regardless of whether CC rotates the session JSONL after `/clear`; idle-gated, mutex-safe, honest per-scope report; ReplSession gains `readonly cwd`). Gateway: new `gateway/wiring/context-reset-policy.ts` tick loop (5 min tick, 2 MB live-delta threshold, 45 min per-scope cooldown, unref'd timer, session-size-watchdog lifecycle pattern). Rehydration: new `contextResetSignal` input seam on `buildLiveAgentTurn` — listener deletes matching `contextSent` topicKeys (scope = `turn.project_id ?? 'general'`, recorded alongside `:1297`) so the NEXT turn is a cold turn = full re-grounding from board+STATUS+docs (external state, lossless). Composer: reset-signal bus wired to BOTH the policy loop AND the existing `/reset` thunk (closes the /reset persona-loss gap through the same single path); `realmodeCleanups.push(() => policy.stop())`. Tests: runtime sweep suite (real PTY-write assertions, no-loop invariant, busy/dead/legacy-key/post-compact-measurement cases) + policy suite (DI timers, cooldown, error-survival) + runner rehydration suite (warm→signal→cold-again, scope isolation). Docs: AS_BUILT entry recording the substrate finding + SYSTEM-OVERVIEW Layer B subsection. Gates: tsc, affected suites, lint, depcruise (0 new violations).
- [ ] **TASK B — CC TodoWrite ingestion (greenfield): sync the agent's native multi-step TodoWrite todos into the Work Board** so multi-step work shows on the board automatically, not only `work_board_add`-created items. `TodoWrite` is currently only an allowlisted tool name (`trident/inner-loop.ts:193`) — no observation seam exists yet. Planning pointers for next iteration: find/build the observation seam in the persistent adapter (`runtime/adapters/claude-code/persistent/hooks/` PostToolUse-style hook, or transcript JSONL observation via the existing dev-channel), then sync into `WorkBoardStore` (`work-board/store.ts`) following the observe-X-sync-Y precedent `skillForgeOnRunTerminal` (`open/composer.ts:960`, registered `:1116`/`:3645`). Dedup/idempotency against items the agent already created explicitly; per-turn injection must not double-show. Tests per `work-board/store.test.ts` + `agent-tool.test.ts` conventions (real state assertions). AS_BUILT + SYSTEM-OVERVIEW updates.
