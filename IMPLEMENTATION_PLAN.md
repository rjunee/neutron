# IMPLEMENTATION_PLAN — the hang watchdog must check run-scoped evidence before killing (Plan card #2)

Card: the 90-min hang watchdog killed 17 live builds in 30 days (incl. PRs #479/#480/#481/#484) because it reads only `last_advanced_at`, a column stamped on PHASE transitions that a long Forge round never re-stamps. Fix: declare a hang only on POSITIVE evidence — (1) live process for the run, (2) recent mtime on the run's own artifacts, (3) recent local ref movement on its branch — with three-valued observations (`activity`/`nothing`/`unknown`); `unknown` may never authorise a kill; the termination reason must disclose every probe and its answer; the reap must remain reachable (positive control). Do NOT raise the threshold, delete the watchdog, infer liveness from `last_advanced_at` alone, or let an unrunnable probe count as quiet.

Resume state (branch `trident/2-the-90-min-hang-watchdog-kills-li`): T1+T2+T3 are all contained in ONE replayed commit, e5d1e81c, sitting directly on main @ 34995c68 (`git log --oneline origin/main..<branch>` shows exactly that commit; the T1/T2 commit ids the previous plan cited were squashed by the replay). Do not rebase; keep committing on the branch. The root `IMPLEMENTATION_PLAN.md` inherited from the base is a stale, unrelated PR-#313 arbitration record — replace it with this body when persisting.

- [x] T1 — pure decision model + orchestrator consumption (in e5d1e81c). `trident/run-evidence.ts`: three-valued `EvidenceObservation`, `RunHangEvidence`, `decideHang` (any in-window activity → stand-down; else any unknown → defer; else reap), `freshestActivityAgeMs`, `describeRunEvidence` (three clauses, always all three), `unknownRunEvidence`; malformed ages demote to unknown. Orchestrator sections (1b-0)…(1b-v) consume it behind the optional `gather_run_evidence` seam: the 2 h inflight ceiling is checked FIRST and outranks every reprieve; run-scoped activity joins both sides of the dead-launcher split (inside `DEAD_LAUNCHER_OVERRIDE_MS` it can overturn a positive launcher death); DEFER is suspected-hang-path only; reap/stand-down disclosures are APPEND-ONLY so with the seam absent behaviour is byte-identical. 8+ orchestrator tests incl. the positive control and a throwing-gatherer-defers case.
- [x] T2 — production probes (in e5d1e81c). `trident/run-evidence-probes.ts`: `buildRunEvidenceGatherer` runs the card's three probes strongest-first — process-table scan for the run id in argv (refuses ids < 8 chars) + pid-file fallback with signal-0 existence check (never delivers a signal); scratch-file mtimes over the temp dirs + bounded symlink-safe worktree walk (capped-is-unknown); branch reflog with tip-committer-date fallback, `not a repository` = unknown. One classification rule: ENOENT/absent-ref/no-match/dead-pid = `nothing`; EACCES/EIO/spawn-failure/timeout/unparseable/capped = `unknown`. 15 s host-command bound; observation-only, no kill by name or pattern anywhere incl. tests. 27 tests.
- [x] T3 — the gatherer is WIRED at the composition root and consultation is PROVEN (in e5d1e81c). `gateway/composition/build-core-modules.ts` passes `gather_run_evidence: buildRunEvidenceGatherer()` unconditionally beside `latest_stage_event_at`. The composed suite (`gateway/composition/build-core-modules-trident-liveness-wiring.test.ts`) now carries the composed positive control (`a POSITIVELY QUIET run still reaps` — real repo without the branch, unique run id, no artifacts, three-probe disclosure asserted verbatim), plus `a fresh run-scoped artifact spares a run the reaper would have killed` (fails if the gatherer is built but never consulted) and `an unqueryable world DEFERS instead of reaping`.
- [x] T4 — re-stamp `last_advanced_at` from RUN-SCOPED positive evidence only, and stage the card's as-built entry (this iteration; see execution spec). The card's "consider also" clause, refined against the invariants T1 committed: on the (1b) stand-down branch, return `changed: true` (the tick's `saveIfActive` always re-stamps `last_advanced_at`; the `run` snapshot itself stays untouched) — but ONLY when the spare was authorised by run-scoped evidence: a dead-probe override (`stageBeatsDeath`/`runEvidenceBeatsDeath`), a fresh stage row (`stageFresh`), or a run-evidence stand-down. A spare carried SOLELY by `probe === 'alive'` does NOT re-stamp — that probe answers about a shared launcher GENERATION, not this run, so re-stamping on it would defeat the N4 immortality guard (a forever-alive launcher must still die at the 2 h ceiling; that test stays green UNMODIFIED). DEFER never re-stamps (unknown must not manufacture progress; both defer `changed:false` pins stay). Effects: display consumers (`STALLED_WARN_MS` badge via `progressSignature` in `trident/tick.ts`, `trident/run-driving.ts`) stop reporting phantom staleness with zero changes of their own; probes fire at most once per hang window per run instead of every tick; and the 2 h ceiling stops false-killing a healthy >2 h Forge round, because each run-scoped spare renews the window while defer-only and quiet runs still march to the ceiling/reap. The watchdog itself keeps consulting live evidence at decision time and never reads the re-stamped column as evidence (T1's rule, unchanged). Terminal delivery cannot fire on a spare: `tick.ts` gates `onTerminal` on `isTerminalPhase`, and a stand-down keeps the phase. Test premise changes are confined to `trident/orchestrator.test.ts` (two no-write pins flip to moved-clock pins, two stand-down `changed` expectations flip to true, two expiry timelines shift by one window) and three one-line clock assertions in the composed wiring suite; N4, N2d, both defer tests and both ceiling tests must pass unmodified.
