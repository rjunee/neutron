# IMPLEMENTATION_PLAN — A gateway restart must not kill an in-flight build (crash recovery; PR #267)

Regenerated 2026-08-18 against origin/main @ 5968cfed. Card status: PR #267 ("fix(trident): recover a build whose launcher died, don't reap it — durable, bounded crash recovery (T1)") is MERGED — 2026-08-15T02:56:39Z, merge commit 39e3b6b2, head branch trident/a-gateway-restart-must-not-kill-an. The implementation, the BLOCKING addendum (migration renumber + three-way column merge + landability), and a post-merge hardening (merged-PR adoption, incident 2026-08-16) are ALL on main and production-wired. One closure item remains: the card's own "VERIFY, don't assume" clause has not been executed in full against CURRENT main, which has since changed trident/orchestrator.ts (87 lines, via #311/#392/#395 replays).

- [x] (a) Recover, don't reap — §1a-crash routing in trident/orchestrator.ts (~2705–2807): a crashed launcher with no harvestable result is atomically CLAIMED (beginCrashRecovery clears the crash latch, releases the slot, tombstones the launcher generation, spends one durable budget unit) and relaunched as a CONTINUATION — launch() folds branch/pr/inner_checkpoint so the workflow resumes on the pushed branch and reuses the PR; round/ralph_round untouched. Tests: crash-recovery.test.ts describe "(a)" incl. the tombstoned-generation re-adoption guard; liveness-death-e2e.test.ts.
- [x] (a-edge) A crash BEFORE the launch save (NULL subagent_run_id) is routed into recovery, not an infinite re-fire — trident/crash-before-launch-save.test.ts (RED-mutation on the budget check).
- [x] (b) Bounded and visible — durable crash_recoveries column (sole writer beginCrashRecovery, trident/store.ts ~626–635), SEPARATE from the fix-round counter; past max_crash_recoveries (DEFAULT_MAX_CRASH_RECOVERIES, orchestrator.ts:1689) the run fails with a reason NAMING the budget (and deliberately avoiding the token "exhausted", which delivery.ts would misclassify). Budget survives a process restart — crash-recovery.test.ts describe "(b)" incl. the fresh-orchestrator continuation test.
- [x] (c) Harvest still wins — harvest runs FIRST; a crashed row with a parseable inner_result harvests with zero fires and no budget spent. crash-recovery.test.ts describe "(c)"; harvested_at is never stamped on any recovery path.
- [x] (d) Measured cause — open/wiring/substrates.ts:565–568 latches the crash reason with the gateway process boot timestamp (Date.now() − uptime) and observation time; the latched reason survives into the budget-capped terminal failure_reason (crash-recovery.test.ts:196, RED-mutation).
- [x] (e) Board state — the terminal reconcile fires only once the budget is used up; the card never reads failed while a recovery is in flight. crash-recovery.test.ts describe "(e)" with buildBoardReconcileObserver.
- [x] (f) Post-merge hardening (incident 2026-08-16 23:21: recovery blindly relaunched finished PRs #336/#337) — a merged PR is ADOPTED as done at recovery (claim precedes return so the crash latch cannot discard the terminal write), never rebuilt; a merged-probe outage never adopts; no recorded PR adopts by branch via the merged-list probe; local merge-mode issues no probe and relaunches. crash-recovery.test.ts describe "(f)".
- [x] BLOCKING-1 — migration renumbered off the 0122 collision: main carries 0122_trident_checkpoint_head.sql AND this card's 0123_code_trident_runs_crash_recoveries.sql; exactly one 0123_* exists (runner dedupes on ordinal alone — #575).
- [x] BLOCKING-2 — three-way column merge landed: migrations/expected-schema.txt:597 has crash_recoveries INTEGER in code_trident_runs; trident/store.ts column list (~305) carries inner_checkpoint_head, inner_checkpoint_findings AND crash_recoveries; the INSERT placeholder/column/bound-array alignment is pinned by trident/store.test.ts:742 ("the silent-corruption guard (BLOCKING addendum)").
- [x] BLOCKING-3 — landability: PR #267 MERGED (which supersedes the demanded MERGEABLE probe — a merged PR reports mergeable=UNKNOWN by design); crash/store subset re-run green 2026-08-18 (68 pass / 0 fail across crash-recovery, crash-before-launch-save, store tests).
- [x] Production wiring — gateway/composition/build-core-modules.ts:668 wires begin_crash_recovery = store.beginCrashRecovery (unwired would silently fall back to the legacy reap); begin_infra_retry wired beside it (separate card); detectMergedPr is orchestrator-internal (orchestrator.ts:1743), no extra wiring needed.
- [x] docs/AS_BUILT.md — append-only entries recording the crash-recovery budget reason and the durable crash-recovery model (~lines 6538, 20609–20616).
- [ ] FINAL VERIFICATION + card closure (the card's own "VERIFY, don't assume" clause, executed against CURRENT main): confirm PR #267 state=MERGED; run the FULL `bun test trident/` suite against origin/main content (not a diverged checkout — main has since touched trident/orchestrator.ts via #311/#392/#395); re-grep the five load-bearing anchors; then persist this plan all-[x]. Touch NO product code — a red check is recorded as a new unchecked item with evidence, never improvised into a fix.
- [ ] VERIFICATION EVIDENCE — the required crash-suite log sanity pin failed after the origin/main suite completed green: `crash-recovery.test.ts count=0` from `grep -c "crash-recovery.test.ts" /tmp/trident-main-verify.alF4ur/trident-suite.log` (suite tail: `2027 pass`, `0 fail`, `7239 expect() calls`, `Ran 2027 tests across 78 files. [110.33s]`).

NOTES (not tasks):
- SPEC.md §Post-window backlog still shows this entry as "- [ ]" (line ~362). SPEC.md is owner-owned — agents never rewrite it; flipping the checkbox is the owner's call, informed by this plan.
- Deliberately OUT of scope, adjacent cards per SPEC: deploy drain/defer ("a deploy must not kill the builds in flight" — why the child dies at all), infra auto-retry (executor deferred/transport 5xx; shares the classifier, sequenced separately), and re-dispatch checkpoint continuity ("a retry must resume from the checkpoint"). This card fixed only the launcher-crash reap.
- Commit .trident/plans/trident/a-gateway-restart-must-not-kill-an.md together with your code + tests.
