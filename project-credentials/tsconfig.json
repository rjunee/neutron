{
  // Server-side per-project credential store. Its own CI step is
  // `tsc -p project-credentials/tsconfig.json` (G5 — was previously
  // typechecked by no CI step at all).
  "extends": "../tsconfig.base.json",
  "include": ["./**/*.ts"]
}
